---
phase: 96-proposal-card-redesign
plan: 03
type: execute
wave: 2
depends_on:
  - 96-01
files_modified:
  - app/views/service-detail.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "service-detail.js renderInlineProposalCard() produces identical card HTML structure to project-detail.js"
    - "All STATUS_META, TRACK_NODES, _PROPOSAL_CHECK_SVG constants present in service-detail.js"
    - "4-node track renders correctly for all 6 statuses (draft through loss)"
    - "Title leads, proposal_id is secondary, stat chips show VALUE and STAGE AGE"
    - "Empty states removed: no 'No attachment', no 'No comms yet'"
    - "Submit for Approval button conditional on canDrive + draft/for_revision"
    - "loadProposalCard and confirmProposalInlineSubmit in service-detail.js are unchanged"
    - "D-08 CTA card (For Proposal + canDrive for services) is preserved"
  artifacts:
    - path: "app/views/service-detail.js"
      provides: "Rewritten renderInlineProposalCard() matching project-detail.js"
      contains: "STATUS_META"
  key_links:
    - from: "app/views/service-detail.js renderInlineProposalCard()"
      to: "styles/components.css"
      via: "CSS class names in HTML template string"
      pattern: "proposal-card-heading|proposal-card-track|proposal-track-node|proposal-stat-chip"
---

<objective>
Apply the identical `renderInlineProposalCard()` rewrite from Plan 02 (project-detail.js) to `app/views/service-detail.js` to achieve full parity (D-10).

Purpose: The two files are independent copies of the same proposal card logic (established pattern in this codebase). Plan 03 mirrors the Plan 02 changes so both views show the same redesigned card.
Output: Updated `renderInlineProposalCard()` and its helpers in service-detail.js. `loadProposalCard()` and submit flow are unchanged.
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\96-proposal-card-redesign\96-CONTEXT.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\PROJECT.md
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md

Spike prototype (source of truth):
@C:\Users\Admin\Roaming\pr-po\.planning\spikes\009-proposal-card-redesign\spike.html

Plan 02 result (already-implemented project-detail.js version — read this to copy the exact implementation):
@C:\Users\Admin\Roaming\pr-po\.planning\phases\96-proposal-card-redesign\96-02-SUMMARY.md

<interfaces>
<!-- service-detail.js imports available for use in renderInlineProposalCard(): -->
<!-- formatCurrency — from '../utils.js' (already imported) -->
<!-- escapeHTML    — from '../utils.js' (already imported) -->

<!-- service-detail.js equivalent of loadProposalCard (DO NOT CHANGE): -->
<!--   Uses currentServiceDocId (not currentProject.id) -->
<!--   Calls loadProposalCard(currentServiceDocId, 'services') on submit -->
<!--   el.innerHTML = renderInlineProposalCard(proposal, canDrive) — same call site -->

<!-- service-detail.js CTA card (lines ~1281–1285) — PRESERVE UNCHANGED: -->
<!--   window.openCreateProposalModal('${escapeHTML(parentDocId)}', window._startProposalCallback, 'services', '${escapeHTML(currentService?.service_code || '')}') -->
<!--   This signature passes 'services' and service_code as extra args — do not alter -->

<!-- service-detail.js renderAgeBadge import: -->
<!--   import { renderAgeBadge, _applyProposalStateTransition } from './proposals.js'; -->
<!--   renderAgeBadge is called in the OLD renderInlineProposalCard — remove that call; -->
<!--   the new chip-based age display replaces it. Keep _applyProposalStateTransition import. -->

<!-- CSS classes: identical to Plan 02 — all from 96-01-PLAN.md -->
<!--   .proposal-card-heading, .proposal-card-track, .proposal-track, .proposal-track-node -->
<!--   .t-dot, .t-label, .proposal-check-icon, .proposal-loss-badge-wrap, .proposal-loss-badge -->
<!--   .proposal-card-body, .proposal-card-title, .proposal-card-id -->
<!--   .proposal-chip-row, .proposal-stat-chip, .chip-warn -->
<!--   .proposal-chip-label, .proposal-chip-val, .proposal-chip-sub -->
<!--   .proposal-info-gap, .proposal-info-row, .proposal-info-link -->
<!--   .proposal-card-footer -->

<!-- Overdue detection: same as project-detail.js — read proposal.current_status_since, -->
<!--   fallback to proposal.created_at. ageDays > 7 AND not client_approved/loss = overdue. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hoist constants and replace private helpers in service-detail.js</name>
  <files>app/views/service-detail.js</files>
  <read_first>
    - app/views/service-detail.js (lines 1072–1182 — full proposal card section: comment block, _renderCardAttachment, _renderCardLatestComms, _proposalStageLabel, _proposalStatusDotColor, renderInlineProposalCard)
    - app/views/project-detail.js (lines 1327–1440 — the already-updated versions of the same functions, to ensure exact parity)
    - .planning/phases/96-proposal-card-redesign/96-02-SUMMARY.md (Plan 02 summary with exact implementation details)
  </read_first>
  <action>
    Mirror the Task 1 changes from Plan 02 exactly:

    Above `renderInlineProposalCard` (below the block comment at line ~1072) add:
    - `STATUS_META` constant (identical to project-detail.js version)
    - `TRACK_NODES` constant (identical)
    - `_PROPOSAL_CHECK_SVG` constant (identical)

    Replace `_renderCardAttachment(proposal)` with the new version that returns `''` for absent attachment_kind, and renders `.proposal-info-row` with `📎` + `.proposal-info-link` anchor for 'link' and 'file' kinds. Use `escapeHTML()` on all user-supplied strings. Hostname extraction via `new URL(url).hostname` with try/catch.

    Replace `_renderCardLatestComms(proposal)` with the new version that returns `''` for empty `comms_log`, and renders `.proposal-info-row` with `💬` + last entry's date and excerpt (max 60 chars). Use `escapeHTML()`.

    Remove `_proposalStageLabel` and `_proposalStatusDotColor` functions.

    These changes are structurally identical to project-detail.js Task 1. The only difference is the file being edited.
  </action>
  <verify>
    grep -n "STATUS_META\|TRACK_NODES\|_PROPOSAL_CHECK_SVG\|_proposalStageLabel\|_proposalStatusDotColor" app/views/service-detail.js
    Expected:
    - STATUS_META present
    - TRACK_NODES present
    - _PROPOSAL_CHECK_SVG present
    - _proposalStageLabel NOT present
    - _proposalStatusDotColor NOT present
  </verify>
  <done>
    - STATUS_META constant with 6 entries exists in service-detail.js
    - _proposalStageLabel and _proposalStatusDotColor are removed from service-detail.js
    - _renderCardAttachment returns '' for falsy attachment_kind
    - _renderCardLatestComms returns '' for empty comms_log
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite renderInlineProposalCard() in service-detail.js</name>
  <files>app/views/service-detail.js</files>
  <read_first>
    - app/views/service-detail.js (lines 1139–1182 — current renderInlineProposalCard to replace)
    - app/views/project-detail.js (lines 1392–1436 — the already-updated renderInlineProposalCard to mirror)
    - .planning/phases/96-proposal-card-redesign/96-02-SUMMARY.md (Plan 02 outcome — read to get exact implementation)
    - .planning/phases/96-proposal-card-redesign/96-CONTEXT.md (D-10 parity requirement)
  </read_first>
  <action>
    Replace the entire body of `renderInlineProposalCard(proposal, canDrive)` in service-detail.js with the same implementation written in project-detail.js Plan 02 Task 2.

    The implementation is structurally identical. No service-specific divergences are needed — both views use the same `proposals` Firestore collection, the same proposal schema, and the same CSS classes.

    Key elements to confirm match project-detail.js:
    - `_buildProposalTrack(status)` local helper (or inline — match whatever approach was used in project-detail.js)
    - Overdue border: `border-left: 3px solid #f59e0b` on outer `.proposal-inline-card` wrapper
    - Card structure: `.proposal-card-heading` → track section → `.proposal-card-body` → `.proposal-card-footer`
    - `onclicks` use `window.openProposalInlineSubmitModal` and `window.openProposalModal` (same window.* functions — both files share the same modal logic, registered via `window.openProposalInlineSubmitModal = openProposalInlineSubmitModal` in each file's `attachWindowFunctions`)
    - `renderAgeBadge` is NOT called (the new chip replaces it — remove the call)

    Do NOT alter `loadProposalCard`, `openProposalInlineSubmitModal`, `confirmProposalInlineSubmit`, or any other function in service-detail.js.
  </action>
  <verify>
    grep -n "proposal-card-heading\|proposal-card-track\|proposal-track-node\|proposal-chip-row\|proposal-card-footer\|proposal-loss-badge" app/views/service-detail.js
    Expected: each class name appears at least once.

    grep "No attachment\|No comms yet" app/views/service-detail.js
    Expected: 0 matches inside renderInlineProposalCard / _renderCardAttachment / _renderCardLatestComms.
  </verify>
  <done>
    - `renderInlineProposalCard` in service-detail.js produces same HTML structure as project-detail.js
    - `.proposal-card-heading` with text `PROPOSAL` present in the template
    - `t-passed`, `t-active`, `t-active-warn` conditionally applied per STATUS_META trackIdx
    - Loss state renders `.proposal-loss-badge`
    - No empty-state strings in _renderCardAttachment or _renderCardLatestComms
    - `renderAgeBadge` is not called from the new renderInlineProposalCard
    - `loadProposalCard` in service-detail.js still uses `currentServiceDocId` and calls `syncBottomRow` is absent (service-detail has no syncBottomRow — preserve that difference)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore proposal doc → card HTML | Same as Plan 02 — proposal fields are user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-03-01 | Injection | renderInlineProposalCard — proposal.title, proposal.proposal_id, attachment_url | mitigate | All user-supplied strings pass through escapeHTML(); attachment URL hostname via URL constructor |
| T-96-03-SC | Tampering | npm/pip installs | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "proposal-card-heading\|proposal-card-track\|proposal-track-node" app/views/service-detail.js` returns 3+
2. `grep "_proposalStageLabel\|_proposalStatusDotColor" app/views/service-detail.js` returns 0 matches
3. `grep "No attachment\|No comms yet" app/views/service-detail.js` returns 0 matches in the helper functions
4. `loadProposalCard` in service-detail.js still calls `renderInlineProposalCard(proposal, canDrive)` at line ~1301 (unchanged)
5. The CTA card at line ~1281 still passes `'services'` and `service_code` to `openCreateProposalModal` (unchanged)
</verification>

<success_criteria>
`renderInlineProposalCard()` in service-detail.js is structurally identical to the project-detail.js version. Both views show the same Spike 009 redesigned proposal card. D-10 parity is achieved.
</success_criteria>

<output>
Create `.planning/phases/96-proposal-card-redesign/96-03-SUMMARY.md` when done.
</output>
