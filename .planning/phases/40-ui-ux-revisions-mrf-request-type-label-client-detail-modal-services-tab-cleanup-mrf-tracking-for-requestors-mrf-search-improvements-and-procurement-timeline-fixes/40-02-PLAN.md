---
phase: 40-ui-ux-revisions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/clients.js
autonomous: true
requirements:
  - UX-04
must_haves:
  truths:
    - "Clicking a client row opens a read-only modal showing client info, linked projects, and linked services"
    - "Linked projects and services in the modal include budget and contract cost financials"
    - "Project and service names in the modal are clickable links to their detail pages"
    - "Edit and Delete buttons on table rows do NOT trigger the modal"
    - "Inline table editing continues to work unchanged"
  artifacts:
    - path: "app/views/clients.js"
      provides: "showClientDetail() function with modal rendering"
      contains: "showClientDetail"
  key_links:
    - from: "app/views/clients.js"
      to: "Firestore projects/services collections"
      via: "getDocs with where('client_code', '==', code)"
      pattern: "where.*client_code"
---

<objective>
Add a read-only client detail modal that opens when clicking a client row. The modal displays client info (code, company name, contact person, contact details) plus linked projects and services with financial data and clickable links to detail pages.

Purpose: Gives users quick visibility into a client's portfolio without leaving the Clients tab.
Output: New `showClientDetail()` function in clients.js with async Firestore queries and modal rendering.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-ui-ux-revisions-mrf-request-type-label-client-detail-modal-services-tab-cleanup-mrf-tracking-for-requestors-mrf-search-improvements-and-procurement-timeline-fixes/40-CONTEXT.md
@.planning/phases/40-ui-ux-revisions-mrf-request-type-label-client-detail-modal-services-tab-cleanup-mrf-tracking-for-requestors-mrf-search-improvements-and-procurement-timeline-fixes/40-RESEARCH.md
@app/views/clients.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add showClientDetail modal function</name>
  <files>app/views/clients.js</files>
  <action>
**1. Add Firebase imports needed for queries:**
Update the import line to include `query` and `where` (currently only has `db, collection, getDocs, addDoc, updateDoc, deleteDoc, doc, onSnapshot`):
```javascript
import { db, collection, getDocs, addDoc, updateDoc, deleteDoc, doc, onSnapshot, query, where } from '../firebase.js';
```

Also import `formatCurrency` from utils.js:
```javascript
import { showLoading, showToast, formatCurrency } from '../utils.js';
```

**2. Create `showClientDetail(clientId)` async function:**
- Find the client from `clientsData` by `id`
- If `editingClient === clientId`, return early (don't open modal when row is in edit mode)
- Show loading indicator
- Run two parallel Firestore queries via `Promise.all`:
  - `getDocs(query(collection(db, 'projects'), where('client_code', '==', client.client_code)))`
  - `getDocs(query(collection(db, 'services'), where('client_code', '==', client.client_code)))`
- Build modal HTML with sections:
  - **Client Information**: client_code, company_name, contact_person, email, phone, address
  - **Linked Projects** (table or card list): project_code, project_name, budget, contract_cost — each row clickable as `<a href="#/projects/detail/${project.project_code}">` to navigate to project detail
  - **Linked Services** (table or card list): service_code, service_name, budget, contract_cost — each row clickable as `<a href="#/services/detail/${service.service_code}">` to navigate to service detail
  - If no linked projects/services, show "No projects linked to this client" / "No services linked to this client"
- Format budget and contract_cost with `formatCurrency()` (imported from utils.js) prefixed with peso sign
- Use the existing modal pattern from the codebase: create a modal overlay div, append to `document.body` via `insertAdjacentHTML('beforeend', modalHtml)`, add a close button that removes the modal from DOM
- Style the modal consistently with existing modals in the app (window-style, centered, blur backdrop). Follow the pattern used in expense-modal.js or viewPRDetails/viewPODetails in procurement.js.
- NO delete button in the modal (locked decision — delete stays in table row only)
- Modal is read-only — no edit functionality

**3. Add row onclick to renderClientsTable():**
In the `renderClientsTable()` function, add an `onclick` handler to each non-edit `<tr>` element:
```javascript
<tr onclick="window.showClientDetail('${client.id}')" style="cursor: pointer;" class="clickable-row">
```

**Critical:** The Edit and Delete action buttons must have `onclick="event.stopPropagation()"` on their containing `<td>` (or on each button individually) to prevent the modal from opening when clicking action buttons. Check if this already exists in the code — if the actions `<td>` already has `event.stopPropagation()`, no change needed; if not, add it.

When `editingClient === client.id`, the row renders input fields (edit mode). In this state, the `onclick` should NOT be added to the `<tr>` — either guard with a conditional in the template, or have `showClientDetail()` return early when `editingClient` is set.

**4. Register window function:**
In `attachWindowFunctions()`, add:
```javascript
window.showClientDetail = showClientDetail;
```

In `destroy()`, clean up:
```javascript
delete window.showClientDetail;
```

Also ensure the modal element is removed from DOM in `destroy()` if it exists.

**5. Close modal function:**
Create a `closeClientDetailModal()` function that removes the modal element from DOM. Register on window for the close button's onclick.
  </action>
  <verify>
    <automated>grep -n "showClientDetail" app/views/clients.js && grep -n "client_code" app/views/clients.js | grep -i where</automated>
    <manual>Load Clients tab, click a client row — modal should appear with client info, linked projects/services with financials, and clickable links. Click Edit/Delete buttons — modal should NOT open. Click a project link in the modal — should navigate to project detail page.</manual>
  </verify>
  <done>Client detail modal opens on row click showing client info, linked projects with budget/contract_cost and clickable detail links, linked services with same; Edit/Delete buttons do not trigger modal; modal has close button and can be dismissed</done>
</task>

</tasks>

<verification>
1. Clicking a client row opens a modal with client information section
2. Modal shows linked projects with budget, contract_cost, and clickable links to `#/projects/detail/CODE`
3. Modal shows linked services with budget, contract_cost, and clickable links to `#/services/detail/CODE`
4. Edit and Delete buttons in the table do not trigger the modal
5. Editing a client inline still works after the modal feature is added
6. Modal close button removes modal from DOM
7. No JavaScript errors in console
</verification>

<success_criteria>
- showClientDetail() function exists and queries both projects and services by client_code
- Modal renders client info + linked projects + linked services with financials
- Links navigate to correct detail pages
- event.stopPropagation() prevents action buttons from triggering modal
- Window function registered in attachWindowFunctions() and cleaned up in destroy()
</success_criteria>

<output>
After completion, create `.planning/phases/40-ui-ux-revisions-mrf-request-type-label-client-detail-modal-services-tab-cleanup-mrf-tracking-for-requestors-mrf-search-improvements-and-procurement-timeline-fixes/40-02-SUMMARY.md`
</output>
