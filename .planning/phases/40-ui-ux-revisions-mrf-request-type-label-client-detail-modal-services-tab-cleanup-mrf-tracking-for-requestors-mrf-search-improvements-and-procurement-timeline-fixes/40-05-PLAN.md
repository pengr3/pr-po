---
phase: 40-ui-ux-revisions
plan: 05
type: execute
wave: 1
depends_on: ["40-04"]
files_modified:
  - app/views/mrf-records.js
  - app/views/mrf-form.js
autonomous: true
requirements: [UX-06]
gap_closure: true
must_haves:
  truths:
    - "My Requests table shows identical columns to Procurement MRF Records: MRF ID, Project, Date Needed, PRs, POs, MRF Status, Procurement Status"
    - "Each MRF row in My Requests fetches its PRs and POs from Firestore and displays them as sub-items (PR badges, PO IDs) identical to the procurement view"
    - "MRF Status column shows calculated status badge (Awaiting PR, X/Y PO Issued) from calculateMRFStatus logic"
    - "Procurement Status column shows read-only status badges per PO (not editable dropdowns)"
    - "Search, status filter, urgency filter, and pagination all still work"
  artifacts:
    - path: "app/views/mrf-records.js"
      provides: "Full MRF Records table with async PR/PO sub-rows, matching procurement.js renderPRPORecords layout"
      contains: "calculateMRFStatus"
    - path: "app/views/mrf-form.js"
      provides: "My Requests sub-tab using updated mrf-records.js controller"
  key_links:
    - from: "app/views/mrf-records.js"
      to: "Firestore prs collection"
      via: "getDocs query by mrf_id"
      pattern: "getDocs.*prs.*mrf_id"
    - from: "app/views/mrf-records.js"
      to: "Firestore pos collection"
      via: "getDocs query by mrf_id"
      pattern: "getDocs.*pos.*mrf_id"
---

<objective>
Rewrite mrf-records.js to render the full Procurement MRF Records table layout with async PR/PO sub-rows, replacing the simplified MRF-only table.

Purpose: UAT Test 10 failed because the user wants My Requests to appear "exactly the same as how MRF Records appear" in Procurement. The current mrf-records.js shows a simplified 7-column MRF-level table without PR/PO sub-queries. The gap closure rewrites the render pipeline to match the full procurement table (MRF ID, Project, Date Needed, PRs, POs, MRF Status, Procurement Status) with async per-row PR/PO Firestore queries, calculated MRF status badges, and read-only procurement status badges.

Output: Updated mrf-records.js with full procurement-matching table, updated mrf-form.js filter bar to match procurement filter layout
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/40-ui-ux-revisions-mrf-request-type-label-client-detail-modal-services-tab-cleanup-mrf-tracking-for-requestors-mrf-search-improvements-and-procurement-timeline-fixes/40-04-SUMMARY.md

@app/views/mrf-records.js
@app/views/mrf-form.js
@app/views/procurement.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite mrf-records.js render pipeline to match full Procurement MRF Records table</name>
  <files>app/views/mrf-records.js</files>
  <action>
Rewrite the `render()` function inside `createMRFRecordsController` to replicate the full `renderPRPORecords()` async pipeline from procurement.js (lines 2405-2732). The render function must become async and produce identical visual output. Specific requirements:

**Imports to add:**
- `formatTimestamp` from utils.js (add to existing import line -- currently only imports formatDate, getStatusClass, getUrgencyClass)

**Table structure -- 7 columns matching procurement.js (minus Actions since Timeline/viewPR/viewPO window functions are not available in mrf-form context):**
1. MRF ID (center, bold) -- display mrf_id, or tr_id for Transport type
2. Project (left) -- use getMRFLabel(mrf)
3. Date Needed (center) -- `mrf.date_needed ? formatDate(mrf.date_needed) : (formatTimestamp(mrf.date_submitted || mrf.created_at) || 'N/A')`
4. PRs (left) -- async fetched from `prs` collection where `mrf_id == mrf.mrf_id`, rendered as non-clickable status-badge `<span>` elements (NOT `<a>` with onclick -- viewPRDetails is a procurement.js window function unavailable here). For Transport type show dash.
5. POs (left) -- async fetched from `pos` collection where `mrf_id == mrf.mrf_id`, rendered as green-styled text with po_id + SUBCON badge if is_subcon (NOT clickable links -- viewPODetails unavailable). For Transport type show dash.
6. MRF Status (left) -- computed from `calculateMRFStatus(prDataArray, poDataArray)` rendered via `renderMRFStatusBadge()`. For Transport type show em dash.
7. Procurement Status (left) -- READ-ONLY status badges per PO (NOT the editable `<select>` dropdown from procurement.js). Use the statusColors map to render colored `<span>` badges. This is the key difference: requestors see but cannot edit PO procurement status.

**Why no Actions column:** The Timeline button calls `window.showProcurementTimeline()` which is registered in procurement.js init(). When the user is on the mrf-form route, procurement.js is NOT loaded, so that window function does not exist. Same for viewPRDetails and viewPODetails. Including non-functional buttons would be worse than omitting them.

**Functions to add inside createMRFRecordsController (local, not exported):**

1. `calculateMRFStatus(prs, pos)` -- copy directly from procurement.js lines 2360-2393. Returns `{status, badgeClass, description}`.

2. `renderMRFStatusBadge(statusObj)` -- copy from procurement.js lines 2398-2400. Returns `<span class="status-badge ${badgeClass}">${status}</span>`.

**Async render pipeline:**
- The `render()` function must become `async function render()`.
- Show loading state: "Loading document references..." while fetching PR/PO data.
- Use `Promise.all(pageItems.map(async (mrf) => { ... }))` to fetch PRs and POs for each MRF on the current page only (not all records).
- For each MRF, determine type: `mrf.request_type === 'service' ? 'Transport' : 'Material'`
- For Material type: query `prs` collection, build prDataArray, sort by PR number, render as status-badge `<span>` elements. Query `pos` collection, build poDataArray, sort by PO number, render as green-styled text with optional SUBCON badge.
- For Transport type: show dashes for PRs, POs, MRF Status columns.
- Procurement Status column: render read-only badges using this statusColors map:
  ```javascript
  const statusColors = {
      'Pending Procurement': { bg: '#fef3c7', color: '#f59e0b' },
      'Pending': { bg: '#fef3c7', color: '#f59e0b' },
      'Procuring': { bg: '#dbeafe', color: '#3b82f6' },
      'Processing': { bg: '#dbeafe', color: '#3b82f6' },
      'Procured': { bg: '#d1fae5', color: '#22c55e' },
      'Processed': { bg: '#d1fae5', color: '#22c55e' },
      'Delivered': { bg: '#eff6ff', color: '#2563eb' }
  };
  ```
  For each PO, render: `<span style="background: ${bg}; color: ${color}; padding: 0.25rem 0.5rem; border-radius: 4px; font-weight: 600; font-size: 0.75rem;">${currentStatus}</span>` -- no `<select>`, no `onchange`.

**Column alignment and styling** must match procurement.js exactly:
- Same thead background (#f8f9fa), same th padding (0.75rem 1rem), same th font-size (0.75rem), same border-bottom (2px solid #e5e7eb)
- Same td padding, border-bottom, font-size as procurement.js
- PR badges: `<span class="status-badge ${statusClass}">` with font-size 0.75rem, display: inline-block, margin-bottom: 0.25rem -- matching procurement.js line 2476-2481 visuals but as span not a
- PO items: green (#34a853), font-weight 600, font-size 0.8rem -- matching procurement.js line 2591-2593 visuals
- SUBCON badge: light blue (#e0f2fe bg, #0369a1 text), 0.65rem, same as procurement.js line 2592
- Each PO/PR sub-item wrapped in `<div style="min-height: 52px; display: flex; flex-direction: column; gap: 2px; justify-content: center;">` matching procurement.js

**Keep the existing controller pattern intact:**
- createMRFRecordsController still returns { load, filter, destroy }
- Instance-scoped state (allRecords, filteredRecords, currentPage) stays
- filterFn callback still works (My Requests uses it for requestor scoping)
- Pagination still works with window[`_mrfRecordsGoToPage_${containerId}`]

**Remove** the old simple 7-column table render -- replace entirely with the async procurement-matching pipeline.

**Do NOT add any window.* onclick handlers** -- the module is used in mrf-form.js context where procurement window functions are not available.
  </action>
  <verify>
    <automated>python -c "import http.server" 2>/dev/null && echo "Python available for manual test server"</automated>
    <manual>Navigate to Material Request > My Requests tab. Verify the table shows 7 columns (MRF ID, Project, Date Needed, PRs, POs, MRF Status, Procurement Status). Verify PRs show as colored badges, POs show as green-styled text with SUBCON labels where applicable, MRF Status shows computed Awaiting PR or X/Y PO Issued badges, and Procurement Status shows read-only colored status badges per PO.</manual>
  </verify>
  <done>My Requests table renders identical column layout and visual structure to Procurement MRF Records (minus Actions column and minus clickable links), with async PR/PO sub-row data fetched per MRF</done>
</task>

<task type="auto">
  <name>Task 2: Update My Requests filter bar to match Procurement MRF Records filter layout</name>
  <files>app/views/mrf-form.js</files>
  <action>
Update the `renderMyRequestsView()` function in mrf-form.js to match the Procurement MRF Records filter bar layout.

**Current My Requests filter bar** (from mrf-form.js):
- Search input, Status dropdown, Urgency dropdown, Refresh button -- all in a horizontal flex row inside the card-header.

**Procurement MRF Records filter bar** (from procurement.js lines 308-336):
- 3-column grid layout with labeled filter groups
- Uses `<div class="filter-group"><label>...</label><select/input>` pattern
- Grid: `display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem;`
- Wrapped in `<div style="padding: 0 1.5rem 1.5rem 1.5rem;">`

**Changes to make in renderMyRequestsView():**

1. Simplify the card-header to just have the title "My Requests" and a Refresh button (remove all filter inputs from header).

2. Add a filter section BELOW the card-header but INSIDE the card, matching procurement layout:
   ```html
   <div style="padding: 0 1.5rem 1.5rem 1.5rem;">
       <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem;">
           <div class="filter-group">
               <label>MRF Status</label>
               <select id="myRequestsStatusFilter" onchange="window._myRequestsFilter()">
                   <option value="">All MRF Statuses</option>
                   <option value="Pending">Pending</option>
                   <option value="PR Generated">PR Generated</option>
                   <option value="TR Submitted">TR Submitted</option>
                   <option value="Finance Approved">Finance Approved</option>
                   <option value="PO Issued">PO Issued</option>
                   <option value="Delivered">Delivered</option>
                   <option value="Completed">Completed</option>
               </select>
           </div>
           <div class="filter-group">
               <label>Urgency</label>
               <select id="myRequestsUrgencyFilter" onchange="window._myRequestsFilter()">
                   <option value="">All Urgencies</option>
                   <option value="Low">Low</option>
                   <option value="Medium">Medium</option>
                   <option value="High">High</option>
                   <option value="Critical">Critical</option>
               </select>
           </div>
           <div class="filter-group">
               <label>Search</label>
               <input type="text" id="myRequestsSearch" placeholder="MRF ID, Project..." onkeyup="window._myRequestsFilter()">
           </div>
       </div>
   </div>
   ```

3. Keep DOM element IDs unchanged: `myRequestsSearch`, `myRequestsStatusFilter`, `myRequestsUrgencyFilter` -- these are referenced by the window._myRequestsFilter function in initMyRequests().

4. The `initMyRequests()` function stays the same -- it already creates the controller with the correct filter IDs.
  </action>
  <verify>
    <automated>python -c "import http.server" 2>/dev/null && echo "Python available for manual test server"</automated>
    <manual>Navigate to Material Request > My Requests. Verify the filter bar uses a 3-column grid layout with labeled filter groups (MRF Status, Urgency, Search) matching the Procurement MRF Records filter bar style. Verify all three filters work correctly.</manual>
  </verify>
  <done>My Requests filter bar matches Procurement MRF Records filter layout (3-column grid with labeled filter groups), filters work correctly</done>
</task>

</tasks>

<verification>
1. Navigate to Material Request > My Requests tab
2. Verify table columns: MRF ID, Project, Date Needed, PRs, POs, MRF Status, Procurement Status
3. Verify PRs column shows colored status-badge spans for each PR
4. Verify POs column shows green-styled PO IDs with SUBCON badges where applicable
5. Verify MRF Status shows computed "Awaiting PR" / "X/Y PO Issued" badges
6. Verify Procurement Status shows read-only colored badges per PO
7. Verify filter bar uses 3-column grid matching Procurement tab
8. Verify search, status filter, and urgency filter all work
9. Verify pagination works with more than 10 records
10. Compare side-by-side with Procurement > MRF Records -- should look identical in structure (minus Actions column)
</verification>

<success_criteria>
- My Requests table is visually identical to Procurement MRF Records (same columns, same PR/PO sub-rows, same badge styles) minus Actions column
- Only differences: no clickable PR/PO links (window functions not available), no editable PO status dropdowns (read-only), no Actions/Timeline column
- Filter bar layout matches Procurement MRF Records
- All existing functionality (requestor filtering, search, pagination) still works
</success_criteria>

<output>
After completion, create `.planning/phases/40-ui-ux-revisions-mrf-request-type-label-client-detail-modal-services-tab-cleanup-mrf-tracking-for-requestors-mrf-search-improvements-and-procurement-timeline-fixes/40-05-SUMMARY.md`
</output>
