---
phase: 60-fix-tr-rejection-independence-decouple-tr-status-from-mrf-and-treat-trs-as-child-records-like-prs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [app/views/procurement.js]
autonomous: true
requirements: [TR-INDEP-03]
gap_closure: true

must_haves:
  truths:
    - "Clicking a rejected TR card opens an editable detail panel with the TR items in input fields"
    - "Procurement can change qty, unit cost, category, unit, and supplier on each TR item before resubmitting"
    - "Clicking Save Changes persists updated items_json and total_amount to Firestore and refreshes cachedRejectedTRs in-place"
    - "Resubmit to Finance still works after saving changes (or without saving)"
  artifacts:
    - path: "app/views/procurement.js"
      provides: "Editable TR item table in selectRejectedTR() + saveRejectedTRChanges()"
      exports: ["selectRejectedTR", "saveRejectedTRChanges", "resubmitRejectedTR"]
  key_links:
    - from: "selectRejectedTR detail panel"
      to: "saveRejectedTRChanges(trDocId)"
      via: "Save Changes button onclick"
      pattern: "window.saveRejectedTRChanges"
    - from: "saveRejectedTRChanges"
      to: "transport_requests Firestore doc"
      via: "updateDoc with items_json + total_amount"
      pattern: "updateDoc.*transport_requests"
---

<objective>
Make the rejected TR detail panel editable so Procurement can revise TR items before resubmitting to Finance.

Purpose: When Finance rejects a TR, Procurement needs to correct items (wrong qty, cost, supplier) before sending it back. The current panel shows a static read-only table — there is no way to revise. This mirrors how renderMRFDetails() already works: the same #lineItemsBody editable table pattern.

Output: selectRejectedTR() renders an editable #lineItemsBody table (same inputs as the MRF detail form). A new saveRejectedTRChanges(trDocId) function reads the DOM inputs, writes updated items_json and total_amount to Firestore, and refreshes cachedRejectedTRs in-place. Both Save Changes and Resubmit to Finance buttons appear in the panel.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-fix-tr-rejection-independence-decouple-tr-status-from-mrf-and-treat-trs-as-child-records-like-prs/60-02-SUMMARY.md

<interfaces>
<!-- Existing patterns the executor must use exactly. -->

From app/views/procurement.js — editable item table (renderMRFDetails):
The existing MRF detail form renders items using:
  <tbody id="lineItemsBody">
    <tr data-index="${index}">
      <input class="item-name table-input" value="...">
      <select class="item-category table-select"> ... options ... </select>
      <input class="item-qty table-input table-input-sm" oninput="window.calculateSubtotal(${index})">
      <select class="item-unit table-select"> ... options ... </select>
      <input class="unit-cost table-input table-input-cost" oninput="window.calculateSubtotal(${index})">
      <select class="supplier-select table-select"> ... options ... </select>
      <td class="subtotal-cell" id="subtotal-${index}"> ... </td>
    </tr>
  </tbody>

CRITICAL: The rejected TR panel must use this SAME id (#lineItemsBody) and the SAME class names
(.item-name, .item-category, .item-qty, .item-unit, .unit-cost, .supplier-select) so that
calculateSubtotal() works without modification.

From app/views/procurement.js — DOM read pattern used by generatePR/submitTransportRequest:
  const rows = document.querySelectorAll('#lineItemsBody tr');
  for (const row of rows) {
      const itemName   = row.querySelector('input.item-name')?.value?.trim() || '';
      const category   = row.querySelector('select.item-category')?.value || '';
      const qty        = parseFloat(row.querySelector('input.item-qty')?.value) || 0;
      const unit       = row.querySelector('select.item-unit')?.value || 'pcs';
      const unitCost   = parseFloat(row.querySelector('input.unit-cost')?.value) || 0;
      const supplier   = row.querySelector('select.supplier-select')?.value || '';
  }

From app/views/procurement.js — cachedRejectedTRs:
  let cachedRejectedTRs = [];  // populated by onSnapshot listener

From app/views/procurement.js — suppliersData:
  // Already loaded in scope; used by renderMRFDetails for the supplier-select dropdown options.
  // Use it the same way in the TR editable panel.

From app/views/procurement.js — calculateSubtotal(index):
  // Reads item-qty and unit-cost inputs at data-index="${index}" and writes to subtotal-${index}.
  // Works unchanged when #lineItemsBody is rendered by selectRejectedTR.

From app/views/procurement.js — window function registration (init):
  window.selectRejectedTR    = selectRejectedTR;
  window.resubmitRejectedTR  = resubmitRejectedTR;
  // Add: window.saveRejectedTRChanges = saveRejectedTRChanges;

From app/views/procurement.js — window function cleanup (destroy):
  delete window.selectRejectedTR;
  delete window.resubmitRejectedTR;
  // Add: delete window.saveRejectedTRChanges;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace static item table in selectRejectedTR() with editable #lineItemsBody</name>
  <files>app/views/procurement.js</files>
  <action>
Rewrite the `selectRejectedTR(trDocId)` function. Replace the static `<td>` cell table with the same editable `#lineItemsBody` table used by renderMRFDetails(). Keep the red rejection banner and the metadata block (MRF ID, Supplier, Project, Total) unchanged above the table.

Specific changes inside selectRejectedTR():

1. Replace the static itemsHtml generation (lines building `<td>${item.item}</td>` etc.) with an editable row template that matches renderMRFDetails() exactly:
   - `<input class="item-name table-input" value="${escapeHTML(item.item || '')}">` (no oninput needed)
   - `<select class="item-category table-select">` with all 11 options (CIVIL, ELECTRICAL, HVAC, PLUMBING, TOOLS & EQUIPMENTS, SAFETY, SUBCON, TRANSPORTATION, HAULING & DELIVERY, DELIVERY BY SUPPLIER, OTHERS) — selected by matching item.category
   - `<input class="item-qty table-input table-input-sm" value="${item.qty || ''}" min="1" oninput="window.calculateSubtotal(${index})">`
   - `<select class="item-unit table-select">` with same unit options as renderMRFDetails() — selected by matching item.unit
   - `<input class="unit-cost table-input table-input-cost" value="${item.unit_cost || ''}" step="0.01" oninput="window.calculateSubtotal(${index})">`
   - `<select class="supplier-select table-select">` — populated from suppliersData array; pre-select item.supplier
   - `<td class="subtotal-cell" id="subtotal-${index}">` showing formatted subtotal

2. Set the tbody id to `lineItemsBody` (same as MRF detail form).

3. Add a table header row with columns: Item, Category, Qty, Unit, Unit Cost, Supplier, Subtotal.

4. Add a tfoot row showing Grand Total (id="grandTotal") — calculate from items on render.

5. Replace the single "Resubmit to Finance" button at the bottom with two buttons side by side:
   - "Save Changes" button: `onclick="window.saveRejectedTRChanges('${tr.id}')"`  — btn-secondary style
   - "Resubmit to Finance" button: `onclick="window.resubmitRejectedTR('${tr.id}')"` — btn-primary style (unchanged)

Do NOT add a delete-row button (keep it simple; TR items are not expected to gain/lose rows, only values corrected). Do NOT call updateActionButtons() from the TR panel — that function is scoped to currentMRF edits.
  </action>
  <verify>Open Procurement > MRF Processing, click a rejected TR card in the Rejected Transport Requests panel. The right panel should show item rows with editable input fields instead of static text. Qty and Unit Cost fields should be number inputs. Category and Unit should be dropdowns. Supplier should be a populated dropdown. Two buttons appear at the bottom: Save Changes and Resubmit to Finance.</verify>
  <done>Clicking a rejected TR card renders an editable item table in #mrfDetails. All inputs are pre-populated from tr.items_json. Two buttons visible at bottom.</done>
</task>

<task type="auto">
  <name>Task 2: Add saveRejectedTRChanges() and register/deregister on window</name>
  <files>app/views/procurement.js</files>
  <action>
Add a new async function `saveRejectedTRChanges(trDocId)` immediately after `selectRejectedTR()`.

Function logic:

1. Guard: `if (window.canEditTab?.('procurement') === false)` → showToast error, return.

2. Find TR: `const tr = cachedRejectedTRs.find(t => t.id === trDocId); if (!tr) { showToast('TR not found', 'error'); return; }`

3. Read DOM inputs from #lineItemsBody — same pattern as generatePR/submitTransportRequest:
   ```
   const rows = document.querySelectorAll('#lineItemsBody tr');
   const updatedItems = [];
   for (const row of rows) {
       const itemName = row.querySelector('input.item-name')?.value?.trim() || '';
       const category = row.querySelector('select.item-category')?.value || '';
       const qty      = parseFloat(row.querySelector('input.item-qty')?.value) || 0;
       const unit     = row.querySelector('select.item-unit')?.value || 'pcs';
       const unitCost = parseFloat(row.querySelector('input.unit-cost')?.value) || 0;
       const supplier = row.querySelector('select.supplier-select')?.value || '';
       if (!itemName) continue;
       updatedItems.push({ item: itemName, category, qty, unit, unit_cost: unitCost, subtotal: qty * unitCost, supplier });
   }
   if (updatedItems.length === 0) { showToast('At least one item is required', 'error'); return; }
   ```

4. Calculate total: `const totalAmount = updatedItems.reduce((sum, i) => sum + i.subtotal, 0);`

5. Write to Firestore:
   ```
   showLoading(true);
   const trRef = doc(db, 'transport_requests', trDocId);
   await updateDoc(trRef, {
       items_json: JSON.stringify(updatedItems),
       total_amount: totalAmount
   });
   showLoading(false);
   ```

6. Update cachedRejectedTRs in-place (so the list panel reflects updated total without waiting for onSnapshot):
   ```
   const idx = cachedRejectedTRs.findIndex(t => t.id === trDocId);
   if (idx !== -1) {
       cachedRejectedTRs[idx] = { ...cachedRejectedTRs[idx], items_json: JSON.stringify(updatedItems), total_amount: totalAmount };
   }
   ```

7. Show success toast: `showToast('TR changes saved. Ready to resubmit.', 'success');`

8. Wrap in try/catch — on error: `showLoading(false); showToast('Failed to save changes: ' + error.message, 'error');`

Register on window in init():
   Add `window.saveRejectedTRChanges = saveRejectedTRChanges;` alongside existing rejected TR function registrations.

Deregister in destroy():
   Add `delete window.saveRejectedTRChanges;` alongside existing deletions.
  </action>
  <verify>In the editable rejected TR panel, change a qty or unit cost value and click Save Changes. A "TR changes saved" toast appears. Reopen the same TR card — the updated values should be pre-populated. In Firestore console, transport_requests document should show updated items_json and total_amount.</verify>
  <done>saveRejectedTRChanges writes updated items to Firestore, updates cachedRejectedTRs in-place, shows success toast. window.saveRejectedTRChanges registered in init() and deleted in destroy().</done>
</task>

</tasks>

<verification>
Manual verification steps:
1. Start local server: `python -m http.server 8000`
2. Navigate to Procurement > MRF Processing tab
3. Verify a rejected TR exists in the "Rejected Transport Requests" panel (if not, reject one via Finance > Pending Approvals)
4. Click the rejected TR card — right panel should show editable item rows (inputs, selects) not static text
5. Change a qty value, click Save Changes — toast appears, no console errors
6. Click the same TR card again — changed qty persists in the inputs
7. Click Resubmit to Finance — TR disappears from rejected panel (existing behavior, unchanged)
8. In Finance > Pending Approvals the resubmitted TR reappears (existing behavior, unchanged)
9. Verify window.saveRejectedTRChanges is defined after init() and undefined after destroy() (navigate away and back)
</verification>

<success_criteria>
- Rejected TR detail panel renders editable #lineItemsBody table (not static cells)
- saveRejectedTRChanges() reads DOM, writes to Firestore, updates cachedRejectedTRs in-place
- Save Changes button appears alongside Resubmit to Finance button
- window.saveRejectedTRChanges registered in init() and cleaned up in destroy()
- calculateSubtotal() updates subtotal cells when editing qty/unit cost (no new code needed — existing function works because #lineItemsBody id is shared)
- No regressions to resubmitRejectedTR() or the Finance prior-rejection notice
</success_criteria>

<output>
After completion, create `.planning/phases/60-fix-tr-rejection-independence-decouple-tr-status-from-mrf-and-treat-trs-as-child-records-like-prs/60-03-SUMMARY.md`
</output>
