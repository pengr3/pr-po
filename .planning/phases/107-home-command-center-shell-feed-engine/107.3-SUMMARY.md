---
phase: 107-home-command-center-shell-feed-engine
plan: "107.3"
subsystem: ui
tags: [home, command-center, feed-engine, briefing, es6-modules, firestore]

# Dependency graph
requires:
  - phase: 107.1
    provides: Command Center CSS + class-name contract (.cc-container, .cc-briefing, .cc-feed-*, empty/error states)
  - phase: 107.2
    provides: app/home-feed.js compute-on-load engine (assembleFeed return shape, FeedItem model, deep-link kinds)
  - phase: 87.1
    provides: home-queue approve/reject modal (homeQueueOpen*Modal), engagement form (renderEngagementForm/initEngagementForm/destroyEngagementForm), getHomeSubTabConfig
provides:
  - "Home Command Center shell — briefing header (greeting + role chip + dated attention one-liner) + severity-ranked attention-feed hero, wired to app/home-feed.js"
  - "Collapsed sub-nav: Command Center (default) | Proposals (non-default) | Dashboard (hidden, Phase 109); Overview + Engagements tabs retired"
  - "Empty ('You're all caught up') + neutral error (Retry) feed states; compute-on-load Refresh (no persistent listeners)"
  - "On-demand '+ New Proposal' modal (engagement form mounts once, no fixed-id collision)"
  - "Empty skeleton containers for 107.4: #ccKpiRow, #ccYourWork, #ccActivity, #ccDoorRail + _ccFeed module state"
affects: [107.4, 108]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Compute-on-load view: one assembleFeed() at init + on Refresh; NO onSnapshot across source collections (D-08)"
    - "Feed row deep-link dispatch: kind 'route' -> location.hash; kind 'modal' -> window[handler](arg) reusing existing home-queue handlers"
    - "On-demand modal mount for fixed-id forms (renderEngagementForm) to avoid duplicate-id collisions"

key-files:
  created: []
  modified:
    - app/views/home.js

key-decisions:
  - "Kept a minimal brand line (hero-title) above the sub-nav; the Command Center greeting is the prominent h1 (per plan's 'minimal brand line' guidance)"
  - "Removed the entire legacy procurement-stats mechanism (getDashboardMode/procurementCardHtml/loadStats/updateStatDisplay/cachedStats/statsListeners) — orphaned by the render() rewrite; onSnapshot( count 4 -> 1 (B1/D-08)"
  - "Wrapped greeting+attn-line in an unclassed .cc-briefing-text div so .cc-briefing (flex space-between) pushes actions right; wrapped Updated+Refresh in .cc-feed-header-actions"

patterns-established:
  - "Feed interaction: data-idx indexes into [...cap.visible, ...cap.rest]; window.ccOpenFeedItem(idx) resolves + dispatches the deep-link"
  - "Refresh re-renders BOTH the feed and the briefing count from the same fresh _ccFeed"

requirements-completed: [HOME-01, HOME-02, HOME-03, HOME-04]

# Metrics
duration: ~8min
completed: 2026-07-10
---

# Phase 107 Plan 107.3: Command Center Shell + Feed Hero Summary

**Rewrote the Home view's default surface into the Command Center: personalized briefing header (greeting + role chip + dated attention one-liner) and a severity-ranked "Needs your attention" feed hero driven by the `app/home-feed.js` compute-on-load engine — collapsing the sub-nav to Command Center | Proposals | (hidden) Dashboard and deleting the orphaned procurement-stats listener fleet (onSnapshot 4 → 1).**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-07-10T03:25:00Z (approx)
- **Completed:** 2026-07-10T03:33:00Z
- **Tasks:** 3
- **Files modified:** 1 (`app/views/home.js`)

## Accomplishments
- Collapsed the home sub-nav to `Command Center (default) | Proposals (non-default) | Dashboard (hidden)`; retired the Overview + Engagements tabs (D-02) and built the `.cc-container` skeleton (briefing, KPI row, feed section, Your Work/Recent Activity panels, door rail).
- Wired the briefing header: `Good {timeWord}, {firstName}.`, human-readable role chip, and the dated one-liner whose count is tone-colored (danger if any critical, warn if any high) — driven by `assembleFeed().total/hasCritical/hasHigh`.
- Rendered the severity-ranked feed hero with cap/expander/overflow, keyboard-accessible rows (`role=button` / `tabindex=0` / Enter-Space), route + modal deep-link dispatch, calm empty state, neutral error state with Retry, and a compute-on-load Refresh.
- Deleted the dead procurement-stats mechanism (3 `loadStats` onSnapshot listeners + helpers), moving the Home view fully to compute-on-load; the only remaining listener is the Proposals sub-tab's `_loadHomeProposalsTab`.

## Task Commits

Each task was committed atomically (only `app/views/home.js` staged per commit):

1. **Task 1: Sub-nav collapse + Command Center skeleton** — `56354825` (feat)
2. **Task 2: Briefing header + feed engine bootstrap + New Proposal modal** — `7532db76` (feat)
3. **Task 3: Feed hero render + Refresh + interaction + empty/error + destroy()** — `e617b9e8` (feat)

**Plan metadata:** committed separately with this SUMMARY.

## Files Created/Modified
- `app/views/home.js` — Rewrote `render()` (skeleton + sub-nav), `switchHomeTab()` (command↔proposals), `init()` (engine bootstrap + briefing/feed render + handler registration), `destroy()` (new cc* cleanup); added `renderBriefing()`, `renderFeed()`, `renderFeedRow()`, feed handlers, and `_ccFeed` state; removed the procurement-stats code.

## Contracts for 107.4 (consume without re-reading home.js internals)

### Empty skeleton containers left for 107.4 to fill
These IDs exist inside `#homeCommandContent > .cc-container` and are rendered **empty** by 107.3:

| Container ID | Class | Purpose (107.4) |
|--------------|-------|-----------------|
| `#ccKpiRow` | `.cc-kpi-row` | 2–4 role-tailored KPI chips (reuse `.project-scorecard-card` + `.cc-kpi-chip`) |
| `#ccYourWork` | `.cc-yourwork` (currently `display:none`) | Your Work buckets; unhide when populated |
| `#ccActivity` | `.cc-activity` | Recent Activity rows (per-user notifications slice) |
| `#ccDoorRail` | `.cc-door-rail` | 5 permission-scoped nav door tiles |

`#ccBriefing` and `#ccFeedSection` are already populated by 107.3 — do not overwrite them.

### `_ccFeed` module state (107.4 may read for the KPI "Needs Attention" chip)
Module-scoped `let _ccFeed` holds the last `assembleFeed()` result (or the failure-shaped fallback). Shape (from 107.2):
```js
{ items, cap:{visible,rest,overflow}, total, hasCritical, hasHigh, allSourcesFailed, fetchedAt }
```
- `_ccFeed.total` → the "Needs Attention" KPI count (danger tone if `_ccFeed.hasCritical`).
- Reset to `null` in `destroy()`. If 107.4 reads it, guard for `null` (init may not have completed / after destroy).
- 107.4 should NOT re-call `assembleFeed()` for the KPI count — reuse `_ccFeed` (one compute per load; Refresh already re-populates it via `window.ccRefreshFeed`).

### New `window.*` functions registered by 107.3 (all deleted in destroy())
`ccOpenNewProposal`, `ccCloseNewProposal`, `ccOpenFeedItem`, `ccToggleFeedExpand`, `ccRefreshFeed`. Preserved unchanged: `switchHomeTab`, `openProposalModal`, `homeQueue*`, `handleHomeProposal*`.

## Decisions Made
- **Minimal brand line retained.** Kept `<h1 class="hero-title">🏗️ CLMC</h1>` + subtitle above the sub-nav (plan gave the choice); the Command Center greeting is the prominent content h1.
- **DOM wrappers for flex layout.** `.cc-briefing` and `.cc-feed-header` are `display:flex; justify-content:space-between` (verified in `views.css`), so I wrapped the left text group (`.cc-briefing-text`, unclassed-styled) and the right header group (`.cc-feed-header-actions`) to keep two flex children. These wrapper classes are inert (no CSS rule) — purely structural.

## Deviations from Plan

### Auto-fixed / re-sequenced work

**1. [Rule 3 - Blocking / task re-sequence] Engagements eager-render block removed in Task 1 instead of Task 2**
- **Found during:** Task 1
- **Issue:** Task 1's acceptance requires `grep -c 'homeTabEngagements'` and `grep -c 'homeEngagementsContent'` to return 0, but the `init()` `if (canEngagements) {…}` eager-render block still referenced both ids (plan assigned that block's removal to Task 2/B3). Leaving it would fail Task 1's own acceptance.
- **Fix:** Removed the eager-render block (and dropped `canEngagements` from the init destructure) as part of Task 1; the block was already dead because Task 1's render rewrite deleted its mount container `#homeEngagementsContent`. Task 2 still owns the rest of B1/B3 (engine bootstrap + on-demand modal + procurement-stats deletion).
- **Files modified:** app/views/home.js
- **Verification:** Both greps return 0 after Task 1; `node --check` passes.
- **Committed in:** `56354825` (Task 1 commit)

**2. [Rule 3 - Blocking / valid-intermediate] `statsListeners` module var removed in Task 3 instead of Task 2**
- **Found during:** Task 2
- **Issue:** The plan removes `loadStats` (Task 2) but the `statsListeners.forEach` cleanup in `destroy()` is removed in Task 3. Deleting the `let statsListeners = []` declaration in Task 2 (as the plan literally states) would leave `destroy()` referencing an undeclared variable → a latent `ReferenceError` in the Task 2 intermediate commit.
- **Fix:** Kept `let statsListeners = []` through Task 2 (loop iterates an empty array — harmless no-op), then removed BOTH the declaration and the `destroy()` loop in Task 3. Identical final state; every intermediate commit is valid.
- **Files modified:** app/views/home.js
- **Verification:** `grep -c 'statsListeners'` = 0 after Task 3; `node --check` passes at every commit.
- **Committed in:** `e617b9e8` (Task 3 commit)

**3. [Rule 3 - Blocking / dead-code completion] Pruned orphaned `query`/`where`/`getDocs` imports**
- **Found during:** Task 3
- **Issue:** Removing `loadStats` orphaned the `query`, `where`, and `getDocs` named imports from `../firebase.js` (0 remaining call sites verified).
- **Fix:** Trimmed the firebase import to `{ db, collection, doc, onSnapshot, getDoc }` to complete the "remove dead procurement-stats code" objective. No behavior change (zero-build SPA, no tree-shaking dependency).
- **Files modified:** app/views/home.js
- **Verification:** `node --check` passes; `grep -c '\bquery(\|\bwhere(\|\bgetDocs('` all 0.
- **Committed in:** `e617b9e8` (Task 3 commit)

---

**Total deviations:** 3 (all task re-sequencing / dead-code completion to keep every intermediate commit valid and satisfy each task's own acceptance greps). No scope creep — final state matches the plan exactly.
**Impact on plan:** None on the delivered surface; only which commit performs each removal shifted.

## Issues Encountered
- Two acceptance greps (`homeEngagementsContent`, `loadStats`) initially matched my own explanatory comments. Reworded the comments so the literal identifiers appear zero times, matching the plan's grep-based acceptance intent.

## Threat Flags
None. The only destructive deep-link surfaced (proposal reject) reuses the existing `window.homeQueueOpenRejectModal` → `_openHomeQueueModal(id,'reject')` path (rejection-reason ≥10 chars, red confirm, fresh getDoc re-check). No new endpoints, auth paths, or trust-boundary surface introduced. Feed scoping is enforced by 107.2's source predicates (defense-in-depth per T-107-01/02).

## Known Stubs
The four skeleton containers (`#ccKpiRow`, `#ccYourWork`, `#ccActivity`, `#ccDoorRail`) render **empty by design** — they are the intended 107.4 hand-off seam (documented above), not stubs that block 107.3's goal. HOME-01–04 (briefing + feed hero + empty/error) are fully wired to live data via `assembleFeed`; no placeholder/mock data was introduced.

## Pending — Browser UAT (flagged, cannot run headless)
All grep-based acceptance criteria across the 3 tasks pass and `node --check app/views/home.js` is clean. The "Browser UAT (flag)" items require `python -m http.server 8000` + a signed-in session and are deferred to manual testing:
1. **Task 1:** landing shows a single Command Center column; sub-nav shows Command Center (active) + Proposals; Proposals tab still loads the existing scorecards+table.
2. **Task 2:** greeting reads `Good {morning/afternoon/evening}, {FirstName}.`; one-liner shows the dated attention count; `+ New Proposal` appears only for super_admin/operations_admin/services_admin and opens the engagement modal rendered once (no id collision).
3. **Task 3:** rows rank critical→high→medium; clicking a proposal-approval row opens the existing approve modal; clicking a rejected-MRF row navigates to `#/procurement/records`; Refresh updates the caption; an empty scope shows the calm card.

## Next Phase Readiness
- **107.4** can fill `#ccKpiRow` / `#ccYourWork` / `#ccActivity` / `#ccDoorRail` and reuse `_ccFeed` for the "Needs Attention" KPI chip (guard for `null`). Do not overwrite `#ccBriefing` / `#ccFeedSection`.
- No blockers. onSnapshot( in home.js is now 1 (Proposals sub-tab only); the Command Center is fully compute-on-load.

## Self-Check: PASSED
- `app/views/home.js` — FOUND (960 lines; `node --check` clean)
- `.planning/phases/107-home-command-center-shell-feed-engine/107.3-SUMMARY.md` — FOUND (this file)
- Commit `56354825` (Task 1) — FOUND
- Commit `7532db76` (Task 2) — FOUND
- Commit `e617b9e8` (Task 3) — FOUND
- onSnapshot( in home.js = 1 (down from pre-107 baseline of 4) — VERIFIED

---
*Phase: 107-home-command-center-shell-feed-engine*
*Completed: 2026-07-10*
