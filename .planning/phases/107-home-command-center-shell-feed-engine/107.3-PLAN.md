---
phase: 107-home-command-center-shell-feed-engine
plan: "107.3"
type: execute
wave: 2
depends_on: ["107.1", "107.2"]
files_modified:
  - app/views/home.js
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04]
must_haves:
  truths:
    - "On landing, a signed-in user sees 'Good {timeWord}, {firstName}.' + a dated one-liner counting items needing attention"
    - "The Command Center is the default sub-tab; Proposals is non-default; Overview + Engagements tabs are retired"
    - "The 'Needs your attention' feed renders scoped, severity-ranked, clickable rows via the home-feed engine"
    - "An empty feed shows the calm 'You're all caught up' card; a total fetch failure shows the neutral error card with Retry"
    - "The 3 orphaned loadStats onSnapshot listeners are removed; Refresh re-runs compute-on-load; no new persistent listeners are added across source collections"
  artifacts:
    - path: "app/views/home.js"
      provides: "Command Center shell (.cc-container), briefing header, feed hero, refresh, empty/error states"
      contains: "cc-container"
  key_links:
    - from: "app/views/home.js"
      to: "app/home-feed.js"
      via: "import { assembleFeed, getSourcesForUser }"
      pattern: "from '\\.\\./home-feed.js'"
    - from: "feed row click"
      to: "location.hash | window.homeQueueOpenApproveModal"
      via: "deepLink dispatch (route | modal)"
      pattern: "location.hash|homeQueueOpen"
---

<objective>
Rewrite the Home view's default surface into the **Command Center shell + briefing header + attention-feed hero** (HOME-01–04). Collapse the sub-nav to `[ Command Center (default) | Proposals (non-default) | Dashboard (hidden) ]`, retire the Overview + Engagements tabs, and render the personalized greeting, the dated attention one-liner, and the severity-ranked feed by consuming the `app/home-feed.js` engine (compute-on-load + Refresh).

Purpose: This is the hero experience — the "what should I do right now" surface. It builds the `.cc-container` skeleton with block containers that 107.4 fills (KPI chips, Your Work, Recent Activity, door rail).
Output: Rewritten `render()`, `init()`, `switchHomeTab()`, `destroy()` in home.js + new `renderBriefing`/`renderFeed`/refresh logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-home-command-center-shell-feed-engine/107-UI-SPEC.md
@.planning/phases/107-home-command-center-shell-feed-engine/107.1-SUMMARY.md
@.planning/phases/107-home-command-center-shell-feed-engine/107.2-SUMMARY.md
@app/views/home.js
@CLAUDE.md

<interfaces>
<!-- From 107.2 (app/home-feed.js). Consume these; do not re-read the engine internals. -->
import { assembleFeed, getSourcesForUser, SEVERITY } from '../home-feed.js';
  assembleFeed(user) → Promise<{ items, cap:{visible,rest,overflow}, total, hasCritical, hasHigh, allSourcesFailed, fetchedAt }>
  Each item: { dedupeKey, severity:'critical'|'high'|'medium', icon(inline-SVG str), title, subtitle, category, deepLink:{kind:'route',value} | {kind:'modal',handler,arg}, timestamp, isRollup? }

<!-- Already in home.js — PRESERVE these (feed modal deep-links reuse them): -->
  _openHomeQueueModal(id, mode) / _homeQueueConfirmAction / window.homeQueueOpenApproveModal / window.homeQueueOpenRejectModal / window.homeQueueConfirmAction / window.homeQueueCancelModal
  _loadHomeProposalsTab(canApproveQueue)  // the Proposals sub-tab — keep unchanged, still mounts into #homeProposalsContent
  getHomeSubTabConfig() → { showSubNav, canEngagements, canProposals, canApproveQueue }
  renderEngagementForm() / initEngagementForm() / destroyEngagementForm()  // '+ New Proposal' modal body

<!-- CSS class contract delivered by 107.1 (see 107.1-SUMMARY.md). Use verbatim. -->
  .cc-container .cc-briefing .cc-greeting .cc-attn-line .cc-attn-count(.--danger/.--warn) .cc-role-chip .cc-briefing-actions
  .cc-feed-section .cc-feed-header .cc-section-label .cc-refresh-btn .cc-updated .cc-feed-card .cc-feed-list
  .cc-feed-row(.--critical/.--high/.--medium) .cc-feed-icon .cc-feed-main .cc-feed-title .cc-feed-subtitle .cc-feed-age .cc-cat-chip(.--proposal/.--mrf)
  .cc-feed-expander .cc-feed-rollup .cc-feed-overflow
  .cc-empty .cc-empty-icon .cc-empty-heading .cc-empty-body .cc-empty-hint
  .cc-feed-error .cc-feed-error-heading .cc-feed-error-body .cc-feed-retry
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: render() + switchHomeTab() — sub-nav collapse and Command Center skeleton</name>
  <files>app/views/home.js</files>
  <read_first>
    - app/views/home.js render() L124-198, switchHomeTab() L205-225, getHomeSubTabConfig() L60-69
    - 107-UI-SPEC.md §"Layout & Composition" (sub-nav = Command Center default | Proposals non-default | Dashboard hidden; vertical order)
    - 107.1-SUMMARY.md (class contract)
  </read_first>
  <action>
    Rewrite `render()`:
    - Remove the `.dept-cards` nav-card block (5 old cards) and the `.quick-stats` procurement stats block from the DEFAULT body — the door rail (107.4) and KPI chips (107.4) replace them. Keep the `.hero-section` wrapper + `<h1 class="hero-title">🏗️ CLMC</h1>` brand IF the app expects it; otherwise the Command Center greeting is the new h1 — prefer keeping a minimal brand line above the sub-nav, then the Command Center owns the greeting.
    - Sub-nav: change the three tab buttons to exactly two visible + one hidden:
        • `homeTabCommand` — label `Command Center`, active by default, `onclick="window.switchHomeTab('command')"`
        • `homeTabProposals` — label `Proposals`, NON-default, `onclick="window.switchHomeTab('proposals')"`
        • `homeTabDashboard` — label `Dashboard`, `style="display:none;"` (Phase 109 reveals it) — render the button but keep it hidden; do NOT render any dashboard body.
      DELETE the `homeTabOverview` and `homeTabEngagements` buttons (tabs retire per D-02).
    - Default body container: replace `#homeOverviewContent` with `#homeCommandContent` holding the skeleton:
        <div class="cc-container">
          <section class="cc-briefing" id="ccBriefing"></section>
          <div class="cc-kpi-row" id="ccKpiRow"></div>                     <!-- filled by 107.4 -->
          <section class="cc-feed-section" id="ccFeedSection"></section>
          <div class="cc-panels" id="ccPanels">
            <section class="cc-yourwork" id="ccYourWork" style="display:none;"></section>   <!-- 107.4 -->
            <section class="cc-activity" id="ccActivity"></section>                          <!-- 107.4 -->
          </div>
          <nav class="cc-door-rail" id="ccDoorRail"></nav>                 <!-- 107.4 -->
        </div>
      Keep `#homeProposalsContent` (unchanged) for the Proposals tab body. Also DELETE the `<div id="homeEngagementsContent" ...></div>` container entirely — the Engagements tab retires (D-02); the engagement form now mounts on-demand only, inside the +New Proposal modal (Task 2). (B3)
    Rewrite `switchHomeTab(tab)`: toggle between `#homeCommandContent` (default, 'command') and `#homeProposalsContent` ('proposals'); set `home-sub-nav-tab--active` on the matching button; null-guard every element (finance/procurement_staff render with no sub-nav). Default/unknown tab → command.
  </action>
  <acceptance_criteria>
    - `grep 'cc-container' app/views/home.js` matches AND `grep 'id="ccBriefing"' app/views/home.js` AND `id="ccKpiRow"` AND `id="ccFeedSection"` AND `id="ccYourWork"` AND `id="ccActivity"` AND `id="ccDoorRail"` all present
    - `grep -c 'homeTabOverview' app/views/home.js` returns 0 (Overview tab removed)
    - `grep -c 'homeTabEngagements' app/views/home.js` returns 0 (Engagements tab removed)
    - `grep 'homeTabCommand' app/views/home.js` matches AND `grep "switchHomeTab('command')" app/views/home.js` matches
    - `grep 'homeTabDashboard' app/views/home.js` matches AND same line contains `display:none`
    - `grep -c 'dept-cards' app/views/home.js` returns 0 (old nav-card grid removed from default body)
    - `grep -c 'homeEngagementsContent' app/views/home.js` returns 0 (Engagements container removed — B3)
    - Browser UAT (flag): landing shows a single Command Center column; the sub-nav shows Command Center (active) + Proposals; the Proposals tab still loads the existing scorecards+table.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Briefing header + engine bootstrap + '+ New Proposal' modal</name>
  <files>app/views/home.js</files>
  <read_first>
    - app/views/home.js init() L676-735, destroy() L809-845
    - 107-UI-SPEC.md §"Briefing Header" (greeting rules, role chip, attention one-liner, +New Proposal gating)
    - 107.2-SUMMARY.md (assembleFeed return shape)
  </read_first>
  <action>
    Add module state near the top: `let _ccFeed = null;` (holds the last assembleFeed result).

    Import the engine: add `import { assembleFeed, getSourcesForUser } from '../home-feed.js';` to the top imports.

    Implement `function renderBriefing(user, feed)` returning HTML into `#ccBriefing`:
    - Greeting h1 `.cc-greeting`: `Good ${timeWord}, ${firstName}.` where timeWord = morning (<12), afternoon (12–17), evening (≥18) from `new Date().getHours()`; firstName = `(user?.full_name || 'there').trim().split(/\s+/)[0]` (fallback 'there'). escapeHTML the name.
    - Role chip `.cc-role-chip`: human label via a ROLE_LABELS map: { super_admin:'Super Admin', operations_admin:'Operations Admin', services_admin:'Services Admin', operations_user:'Operations User', services_user:'Services User', finance:'Finance', procurement_staff:'Procurement', management:'Management' }; fallback = the raw role.
    - Attention one-liner `.cc-attn-line`: `${Weekday}, ${Mon} ${D} · ` + attnPhrase, where date parts come from `new Date()` (`toLocaleDateString('en-US',{weekday:'long'})`, `{month:'short'}`, `.getDate()`). N = feed.total. attnPhrase when N>0 = `<span class="cc-attn-count ${tone}">${N}</span> item${N===1?'':'s'} need${N===1?'s':''} your attention` where tone = 'cc-attn-count--danger' if feed.hasCritical else 'cc-attn-count--warn' if feed.hasHigh else ''. When N===0 = `You're all caught up` (no count span).
    - Actions `.cc-briefing-actions` (right): the role chip, then the `+ New Proposal` button ONLY IF `getHomeSubTabConfig().canEngagements` is true: `<button class="btn btn-primary" onclick="window.ccOpenNewProposal()">+ New Proposal</button>` (label EXACTLY `+ New Proposal`).

    Implement `window.ccOpenNewProposal` — opens the engagement form as a window-style `.modal`:
    - Build a `.modal` overlay (id `cc-new-proposal-modal`, blur backdrop, centered) whose `.modal-body` = `renderEngagementForm()`, then `await initEngagementForm()`. Close button + backdrop click → `window.ccCloseNewProposal()` which calls `destroyEngagementForm()` then removes the modal. (Reuse existing engagement lifecycle — idempotent.)

    In `init()`: after computing `const user = window.getCurrentUser?.();`, do the engine bootstrap ONCE:
      `try { _ccFeed = await assembleFeed(user); } catch(e){ console.error('[Home] assembleFeed failed', e); _ccFeed = { items:[], cap:{visible:[],rest:[],overflow:0}, total:0, hasCritical:false, hasHigh:false, allSourcesFailed:true, fetchedAt:Date.now() }; }`
      then `document.getElementById('ccBriefing').innerHTML = renderBriefing(user, _ccFeed);` and (Task 3) render the feed.
      Register `window.ccOpenNewProposal` / `window.ccCloseNewProposal`.
    Preserve the existing homeQueue* / openProposalModal / proposal-tab window registrations and `_loadHomeProposalsTab(canApproveQueue)` call — the feed's modal deep-links reuse homeQueueOpen*Modal.

    REMOVE the legacy procurement-stats mechanism (B1 — replaced by 107.4 KPI chips; its 3 onSnapshot listeners are orphaned once render() drops `.quick-stats`, matching the Phase 106 listener-leak finding at home.js loadStats ~L747-778). In `app/views/home.js`: delete the `const mode = getDashboardMode(); loadStats(mode);` call at the top of init() (~L678-679), the `if (cachedStats.activeMRFs !== null) { ... stat-refreshing ... }` block (~L683-685), and the now-dead definitions `getDashboardMode()` (L46-53), `procurementCardHtml()` (L98-118), `loadStats()` (L741-790), `updateStatDisplay()` (L797-804), plus the `cachedStats` and `statsListeners` module-level vars (L23-30). Honors D-08 ("no persistent listener fleet"). (The `statsListeners` unsubscribe loop in destroy() is removed in Task 3.)

    REMOVE the legacy Engagements eager-render (B3): do NOT carry over the old init() `if (canEngagements) { ... renderEngagementForm() into #homeEngagementsContent ... await initEngagementForm(); }` block (~L693-701). The engagement form mounts ONLY on-demand inside `window.ccOpenNewProposal`. `destroyEngagementForm()` stays in destroy() (idempotent). This prevents the fixed non-namespaced form ids (`proposalName`, `proposalClient`, `proposalPersonnelSearchInput`, ...) from rendering twice and colliding when the modal opens — which would break `getElementById` for exactly the canEngagements roles (super_admin/operations_admin/services_admin) that see +New Proposal.
  </action>
  <acceptance_criteria>
    - `grep "from '../home-feed.js'" app/views/home.js` matches AND imports assembleFeed + getSourcesForUser
    - `grep 'let _ccFeed' app/views/home.js` matches
    - `grep 'function renderBriefing' app/views/home.js` matches
    - `grep 'Good ${timeWord}' app/views/home.js` OR equivalent template present; `grep -c "morning" app/views/home.js` ≥1 AND `afternoon` AND `evening` present
    - `grep '+ New Proposal' app/views/home.js` matches (exact CTA label)
    - `grep 'cc-attn-count--danger' app/views/home.js` AND `cc-attn-count--warn` present (tone logic)
    - `grep 'canEngagements' app/views/home.js` gate on the +New Proposal button present
    - `grep 'window.ccOpenNewProposal' app/views/home.js` AND `initEngagementForm` reused
    - `grep -c 'loadStats' app/views/home.js` returns 0 AND `grep -c 'getDashboardMode' app/views/home.js` returns 0 AND `grep -c 'procurementCardHtml' app/views/home.js` returns 0 (dead procurement-stats mechanism removed — B1)
    - `grep -c 'homeEngagementsContent' app/views/home.js` returns 0 AND the `canEngagements` eager-render block is gone (engagement form mounts on-demand only — B3)
    - Browser UAT (flag): greeting reads "Good {morning/afternoon/evening}, {FirstName}."; one-liner shows dated attention count; +New Proposal appears only for super_admin/operations_admin/services_admin and opens the engagement modal (rendered once, no id collision).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Feed hero render + Refresh + interaction + empty/error + destroy()</name>
  <files>app/views/home.js</files>
  <read_first>
    - app/views/home.js destroy() L809-845 (cleanup pattern), existing homeQueueOpen*Modal handlers L719-720
    - 107-UI-SPEC.md §"Feed Row Anatomy" (row markup, cap/expander/roll-up, keyboard a11y), §"Refresh", §"Empty State"
    - 107.1-SUMMARY.md (feed class contract)
  </read_first>
  <action>
    Implement `function renderFeed(feed)` writing into `#ccFeedSection`. Structure:
    - Header row `.cc-feed-header`: `<span class="cc-section-label">Needs your attention</span>` (left) + right group `<span class="cc-updated">Updated ${relTime}</span><button class="cc-refresh-btn" onclick="window.ccRefreshFeed()">↻ Refresh</button>` where relTime is a small inline relative-time from feed.fetchedAt ('just now' / `${m}m ago`).
    - Body:
        • If `feed.allSourcesFailed` → the error card: `.cc-feed-error` with heading `Couldn't load your feed`, body `Something went wrong fetching your items.`, and `<button class="cc-feed-retry" onclick="window.ccRefreshFeed()">Retry</button>`.
        • Else if `feed.total === 0` → the calm empty card `.cc-empty`: 40px `.cc-empty-icon` containing the check SVG `<svg viewBox="0 0 24 24" fill="none" stroke="#047857" stroke-width="1.75"><path d="M4.5 12.75l6 6 9-13.5"/></svg>`; heading `You're all caught up`; body `Nothing needs your attention right now.`; hint `New items appear here as work moves through the pipeline.`
        • Else → `.cc-feed-card > .cc-feed-list` of rows. Render `feed.cap.visible` (≤8) always; render `feed.cap.rest` inside a hidden `<div id="ccFeedRest" style="display:none;">…</div>`; if `feed.cap.rest.length>0` add `<button class="cc-feed-expander" id="ccFeedExpander" onclick="window.ccToggleFeedExpand()">Show all (${feed.total})</button>`; if `feed.cap.overflow>0` add `<div class="cc-feed-overflow">Showing 25 of ${feed.total} — open the relevant area to see the rest.</div>`.
    - Row builder `function renderFeedRow(item, idx)`:
        `<div class="cc-feed-row cc-feed-row--${item.severity}" role="button" tabindex="0" data-idx="${idx}" aria-label="${escapeHTML(item.title)} — ${item.severity} — ${age}" onclick="window.ccOpenFeedItem(${idx})" onkeydown="if(event.key==='Enter'||event.key===' '){event.preventDefault();window.ccOpenFeedItem(${idx});}">`
          + `<span class="cc-feed-icon">${item.icon}</span>`
          + `<div class="cc-feed-main"><div class="cc-feed-title">${escapeHTML(item.title)}</div><div class="cc-feed-subtitle">${escapeHTML(item.subtitle)}${catChip}</div></div>`
          + `<span class="cc-feed-age">${age}</span>` then `</div>`
        where catChip = `<span class="cc-cat-chip cc-cat-chip--${item.category}">${categoryLabel}</span>` (Proposal/MRF) and age = a compact age string from item.timestamp (`just now`/`${m}m`/`${h}h`/`${d}d`). Roll-up rows (item.isRollup) render with `.cc-feed-rollup` styling and the `+${K} more ${category}` title.
    - Interaction dispatch `window.ccOpenFeedItem = (idx) => { const item = _ccFeed?.cap ? [..._ccFeed.cap.visible, ..._ccFeed.cap.rest][idx] : null; if(!item) return; const dl=item.deepLink; if(dl.kind==='route'){ location.hash = dl.value; } else if(dl.kind==='modal'){ const fn = window[dl.handler]; if(typeof fn==='function') fn(dl.arg); } }`.
      (data-idx indexes into the concatenated visible+rest list — keep the builder's idx consistent with this lookup.)
    - `window.ccToggleFeedExpand` — toggle `#ccFeedRest` display and swap the expander label between `Show all (${total})` and `Show less`.
    - `window.ccRefreshFeed = async () => { const user = window.getCurrentUser?.(); try { _ccFeed = await assembleFeed(user); } catch(e){ _ccFeed = {items:[],cap:{visible:[],rest:[],overflow:0},total:0,hasCritical:false,hasHigh:false,allSourcesFailed:true,fetchedAt:Date.now()}; } renderFeed(_ccFeed); document.getElementById('ccBriefing').innerHTML = renderBriefing(user, _ccFeed); }` — refresh re-renders BOTH the feed and the briefing count. (Compute-on-load; NO onSnapshot added.)

    In `init()`: after the engine bootstrap + briefing (Task 2), call `renderFeed(_ccFeed)` and register `window.ccOpenFeedItem`, `window.ccToggleFeedExpand`, `window.ccRefreshFeed`.

    Extend `destroy()`: `delete` window.ccOpenNewProposal, window.ccCloseNewProposal, window.ccOpenFeedItem, window.ccToggleFeedExpand, window.ccRefreshFeed; remove any `#cc-new-proposal-modal`; set `_ccFeed = null`. Keep existing cleanup for homeQueue*/proposal-tab window fns + the `_proposalListener` unsubscribe. REMOVE the `statsListeners.forEach(unsubscribe => ...)` loop and `statsListeners = []` line — those listeners no longer exist after Task 2's loadStats removal (B1). The feed itself adds NO listeners (compute-on-load), so there is nothing new to unsubscribe.
  </action>
  <acceptance_criteria>
    - `grep 'function renderFeed' app/views/home.js` matches AND `renderFeedRow` present
    - `grep 'Needs your attention' app/views/home.js` matches (feed section heading, exact)
    - `grep '↻ Refresh' app/views/home.js` AND `Updated ' app/views/home.js` (refresh control + caption)
    - `grep "You're all caught up" app/views/home.js` AND `Nothing needs your attention right now.` AND `New items appear here as work moves through the pipeline.` all present (empty state copy exact)
    - `grep 'Couldn' app/views/home.js` — error heading `Couldn't load your feed` + body `Something went wrong fetching your items.` + `Retry` present
    - `grep 'Show all (' app/views/home.js` AND `Show less` present (expander)
    - `grep "role=\"button\"" app/views/home.js` AND `tabindex="0"` AND `onkeydown` (Enter/Space a11y on feed rows)
    - `grep 'window.ccOpenFeedItem' app/views/home.js` AND dispatch handles both `kind==='route'` (location.hash) and `kind==='modal'` (window[handler](arg))
    - `grep -c 'onSnapshot(' app/views/home.js` DECREASES by exactly 3 vs the pre-107 baseline (4 → 1): the 3 `loadStats` listeners are deleted (Task 2), leaving only the single `_loadHomeProposalsTab` listener. NOTE: match `onSnapshot(` (the call form) — a bare `onSnapshot` also counts the import + comment/string mentions (9 today), masking the delta. The feed is compute-on-load (no onSnapshot). (B1)
    - `grep 'delete window.ccRefreshFeed' app/views/home.js` AND `_ccFeed = null` in destroy()
    - Browser UAT (flag): rows rank critical→high→medium; clicking a proposal-approval row opens the existing approve modal; clicking a rejected-MRF row navigates to #/procurement/records; Refresh updates the caption; empty scope shows the calm card.
  </acceptance_criteria>
</task>

</tasks>

<security_note>
The only destructive deep-link the hero exposes is proposal reject, dispatched through the EXISTING `window.homeQueueOpenRejectModal` → `_openHomeQueueModal(id,'reject')` → `_homeQueueConfirmAction` path (rejection-reason ≥10 chars, red confirm, fresh getDoc re-check). No new destructive UI is added here. Feed scoping (which items appear) is enforced by 107.2's source predicates + Firestore rules (defense-in-depth) — see 107.2 threat model T-107-01/02.
</security_note>

<verification>
- home.js imports the engine from ../home-feed.js and calls assembleFeed exactly at init + on refresh (compute-on-load, D-08).
- No new onSnapshot listener across source collections; destroy() cleans every new window function.
- Overview + Engagements tabs are gone; Command Center is default; Proposals tab still works unchanged.
</verification>

<success_criteria>
- HOME-01: greeting + dated attention one-liner render from live feed.total.
- HOME-02/03: feed rows are scoped (engine) + severity-ranked + clickable to route or modal.
- HOME-04: empty feed → calm "You're all caught up"; total failure → neutral error + Retry.
</success_criteria>

<output>
After completion, create `.planning/phases/107-home-command-center-shell-feed-engine/107.3-SUMMARY.md` noting the skeleton container IDs (#ccKpiRow, #ccYourWork, #ccActivity, #ccDoorRail) left EMPTY for 107.4, and the `_ccFeed` module-state shape 107.4 may read for the KPI "Needs Attention" chip.
</output>
