---
phase: 107-home-command-center-shell-feed-engine
plan: "107.4"
type: execute
wave: 3
depends_on: ["107.3"]
files_modified:
  - app/views/home.js
autonomous: true
requirements: [HOME-05, HOME-06, HOME-07, HOME-08]
must_haves:
  truths:
    - "Role-tailored KPI chips (2–4) summarize the user's world; a chip whose count can't resolve is omitted, never shown as 0 or —"
    - "The 'Your work' panel shows up to three ownership/assignment buckets, each hidden when empty; the whole panel hides when all three are empty"
    - "The 'Recent activity' panel shows a read-only slice of the user's notifications (no mark-as-read)"
    - "The five nav doors render as compact tiles, each shown only if the role can reach that area (mirrors index.html top-nav gating)"
  artifacts:
    - path: "app/views/home.js"
      provides: "KPI chip row, Your Work panel (3 buckets), Recent Activity panel, condensed door rail"
      contains: "renderDoorRail"
  key_links:
    - from: "door rail"
      to: "window.getCurrentPermissions().tabs[route].access"
      via: "same predicate as auth.js updateNavForAuth"
      pattern: "getCurrentPermissions"
    - from: "Recent Activity"
      to: "notifications collection"
      via: "getDocs where user_id==uid orderBy created_at desc limit"
      pattern: "where\\('user_id'"
---

<objective>
Fill the Command Center skeleton (built in 107.3) with the four summarizing surfaces: the role-tailored **KPI chips** (HOME-05), the **Your work** panel of three ownership/assignment buckets (HOME-06), the read-only **Recent activity** panel sourced from notifications (HOME-07), and the condensed permission-scoped **nav door rail** (HOME-08).

Purpose: These are the "summarize my scope + keep every area reachable" surfaces that surround the feed hero.
Output: `renderKpiChips`, `renderYourWork`, `renderRecentActivity`, `renderDoorRail` in home.js, mounted into `#ccKpiRow`, `#ccYourWork`, `#ccActivity`, `#ccDoorRail`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-home-command-center-shell-feed-engine/107-UI-SPEC.md
@.planning/phases/107-home-command-center-shell-feed-engine/107.1-SUMMARY.md
@.planning/phases/107-home-command-center-shell-feed-engine/107.3-SUMMARY.md
@app/views/home.js
@CLAUDE.md

<interfaces>
<!-- Skeleton containers 107.3 left empty (see 107.3-SUMMARY.md): -->
  #ccKpiRow  #ccYourWork(display:none by default)  #ccActivity  #ccDoorRail
  Module state _ccFeed (from 107.3): { total, hasCritical, hasHigh, ... } — read for the "Needs Attention" KPI chip.

<!-- utils.js scoping predicates: -->
  getAssignedProjectCodes(): string[] | null   // null = SEE_ALL (hide bucket b / dept-portfolio chip uses full count)
  getAssignedServiceCodes(): string[] | null
  window.getCurrentUser(): { uid, full_name, role, ... }

<!-- notifications.js — Recent Activity source (read-only; mirror loadRecentForDropdown pattern): -->
  TYPE_META[type] → { label, icon(inline-SVG str), color, target_route }
  Query pattern: query(collection(db,'notifications'), where('user_id','==',uid), orderBy('created_at','desc'), limit(8))
  Notification doc fields: { type, message, object_name, actor_name, created_at, link, source_id }
  Navigate on click: n.link || TYPE_META[n.type]?.target_route  (NO read-state mutation here)

<!-- Door-rail gating predicate — SAME as app/auth.js updateNavForAuth (L403-419): -->
  window.getCurrentPermissions?.()?.tabs?.[route]?.access ?? true
  Door → data-route key: Clients→'clients', Projects→'projects', Services→'services', Procurement→'procurement', Finance→'finance'

<!-- CSS classes from 107.1 (verbatim): -->
  .cc-kpi-row .project-scorecard-card .cc-kpi-chip(.--danger/.--warn) .scorecard-count .scorecard-label
  .cc-yourwork .cc-yourwork-bucket .cc-yourwork-bucket-label .cc-yourwork-row .cc-yourwork-more
  .cc-activity .cc-activity-row .cc-activity-icon .cc-activity-empty
  .cc-door-rail .cc-door .cc-door-icon .cc-door-label

<!-- ACTUAL role strings in this codebase (reconcile with UI-SPEC map): -->
  super_admin | operations_admin | services_admin | operations_user | services_user | finance | procurement_staff
  UI-SPEC "procurement" row → actual role `procurement_staff`. UI-SPEC "management" → not a real role here; include a defensive alias mapping to the super_admin/admin chip set but key primarily off the real strings above.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Role-tailored KPI chips with lightweight counts (omit-on-unresolved)</name>
  <files>app/views/home.js</files>
  <read_first>
    - app/views/home.js (the `#ccKpiRow` container + `_ccFeed` module state from 107.3; NOTE 107.3 has already DELETED the old loadStats/getDashboardMode/procurementCardHtml mechanism — do not reference it)
    - app/views/projects.js (confirm the 'active' signal — the app uses a boolean `project.active` flag [projects.js L357 `project.active ? 'Yes':'No'`] + a separate `project_status` enum; there is NO `status=='active'` field) AND app/views/services.js (same `active` flag for services)
    - app/views/finance.js (confirm the collectibles/payables collection + field names before writing those queries — do NOT copy logic; OMIT the chip if the source can't be confirmed)
    - 107-UI-SPEC.md §"KPI Chip Map (per role)" + the "Data-source note for the planner" (omit rule)
  </read_first>
  <action>
    Implement `async function renderKpiChips(user, feed)` that builds the per-role chip set and writes into `#ccKpiRow`. Chip HTML = `.project-scorecard-card.cc-kpi-chip` (+ `.cc-kpi-chip--danger`/`--warn` tone) containing `<span class="scorecard-count">${count}</span><span class="scorecard-label">${label}</span>`, whole chip clickable via `onclick`.

    Per-role chip map (implement VERBATIM from the UI-SPEC; role keyed off the real role strings):
    - super_admin AND management: [ Active Projects → `#/projects` ] · [ Active Services → `#/services` ] · [ Proposals to Approve → `#/?tab=proposals`, WARN if >0 ] · [ Needs Attention (feed.total) → scroll-to-feed, DANGER if feed.hasCritical ]
    - finance: [ Collectibles Due → `#/finance/collectibles` ] · [ Payables Owed → `#/finance` ] · [ PRs/TRs to Approve → `#/finance`, WARN if >0 ]
    - procurement_staff: [ MRFs Pending → `#/procurement/mrfs`, WARN if >0 ] · [ Active POs → `#/procurement/records` ] · [ Needs Attention (feed.total) → scroll-to-feed ]
    - operations_admin: [ Active Projects → `#/projects` ] · [ My Proposals For Revision (created_by==uid) → scroll-to Your Work, WARN if >0 ] · [ Needs Attention → scroll-to-feed ]
    - services_admin: [ Active Services → `#/services` ] · [ My Proposals For Revision → scroll-to Your Work, WARN if >0 ] · [ Needs Attention → scroll-to-feed ]
    - operations_user: [ My Open Items (Your-Work total) → scroll-to Your Work ] · [ My For-Revision Proposals → scroll, WARN if >0 ] · [ My Rejected MRFs (requestor==me) → `#/procurement/records`, DANGER if >0 ]
    - services_user: [ My Open Items → scroll-to Your Work ] · [ My For-Revision Proposals → scroll, WARN if >0 ]

    Counts:
    - FREE (already available): Needs Attention = `feed.total`; Proposals to Approve can reuse the approval-queue size (getDocs proposals where status=='pending_internal', dept-scoped like home-feed source 1) — or read feed items of category 'proposal' with the approval dedupeKey.
    - LIGHTWEIGHT getDocs-count (compute-on-load, NO listeners): Active Projects = `getDocs(query(collection(db,'projects'), where('active','==',true)))`.size — use the boolean `active` flag (W6). Do NOT use `where('status','==','active')`: the `projects` collection has NO `status` field, so that query silently returns 0 and the omit-rule can't catch it. Active Services = `getDocs(query(collection(db,'services'), where('active','==',true)))`.size (confirm the same `active` flag in services.js). For dept admins, best-effort dept-scope only if a department field is confirmed; else a portfolio-wide count is acceptable. MRFs Pending = `getDocs(query(collection(db,'mrfs'), where('status','==','Pending')))`.size; Active POs = getDocs `pos` filtered `procurement_status !== 'Delivered'`; PRs to Approve = `getDocs(query(collection(db,'prs'), where('finance_status','==','Pending')))`.size.
    - Collectibles Due / Payables Owed: use the finance view's confirmed collection + field. **If the collection/field cannot be confirmed from finance.js, OMIT that chip entirely** (per the UI-SPEC rule — never render 0 or —).
    - GENERAL OMIT RULE: if a chip's count resolves to `null`/`undefined` (query failed or source unknown), skip the chip; do not render it as 0 or —. Tone (danger/warn) colors only the `.scorecard-count`.

    Register `window.ccScrollTo = (id) => document.getElementById(id)?.scrollIntoView({ behavior:'smooth', block:'start' });` — chips that "scroll to feed" call `ccScrollTo('ccFeedSection')`, "scroll to Your Work" call `ccScrollTo('ccYourWork')`. Route chips use `onclick="location.hash='…'"`.

    Call `renderKpiChips(user, _ccFeed)` from `init()` after the feed renders (await it; it does getDocs). Refresh (ccRefreshFeed from 107.3) should also re-invoke renderKpiChips so counts stay in sync — add that call into ccRefreshFeed.
  </action>
  <acceptance_criteria>
    - `grep 'function renderKpiChips' app/views/home.js` matches
    - `grep 'cc-kpi-chip' app/views/home.js` matches AND `scorecard-count` used
    - `grep 'Needs Attention' app/views/home.js` matches AND its count reads `feed.total`
    - `grep "where('active', '==', true)" app/views/home.js` matches (Active Projects/Services use the boolean `active` flag — W6)
    - `grep -c "where('status', '==', 'active')" app/views/home.js` returns 0 (the non-existent projects.status field is NOT used)
    - `grep 'ccScrollTo' app/views/home.js` matches AND `ccFeedSection` + `ccYourWork` scroll targets present
    - `grep -c 'onSnapshot(' app/views/home.js` stays at 1 (the `_loadHomeProposalsTab` listener; 107.3 already removed the 3 loadStats listeners; 107.4 adds none — KPI counts are getDocs). NOTE: match `onSnapshot(` (call form), not bare `onSnapshot` (which also counts the import/comment mentions).
    - Omit rule present: a comment/code path skipping a chip when count is null (grep `omit` or the null-guard on push)
    - Browser UAT (flag): super_admin sees ~4 chips incl. Needs Attention (danger-tinted if a critical item exists); finance sees Collectibles/Payables/PR-TR (or fewer if a finance collection can't resolve); procurement_staff sees MRFs Pending + Active POs + Needs Attention; a *_user sees My Open Items / For-Revision.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Your Work panel — three ownership/assignment buckets, hidden when empty</name>
  <files>app/views/home.js</files>
  <read_first>
    - app/views/home.js (getCurrentUser usage; _ccFeed may already carry for-revision items)
    - app/utils.js getAssignedProjectCodes L333 / getAssignedServiceCodes L413 (null semantics)
    - 107-UI-SPEC.md §"Your Work panel" (3 buckets, max 5 rows + `+N more`, panel hidden when all empty)
  </read_first>
  <action>
    Implement `async function renderYourWork(user)` writing into `#ccYourWork` and toggling its `display`.

    Bucket a — `Proposals for revision` (label `.cc-yourwork-bucket-label` = `Proposals for revision`): source = proposals where `created_by==user.uid` AND `status=='for_revision'`. Reuse `sourceMyProposalsForRevision(user)` from home-feed.js (import it) so the query isn't duplicated, OR getDocs the scoped query directly. Row → `#/?tab=proposals` (or the proposal modal). Row title = proposal.title; meta = project_code.

    Bucket b — `Assigned projects & services`: codes = getAssignedProjectCodes() + getAssignedServiceCodes(). If BOTH return null (SEE_ALL role) → OMIT bucket b entirely (assignment scoping is a no-op for those roles). Otherwise, for each non-null array, getDocs the matching docs (use `where(codeField,'in',chunk)` in chunks of ≤10; codeField = the project/service code field — confirm from projects/services schema) and list them. Row → project/service detail route. Row title = name; meta = code + status.

    Bucket c — `Submitted MRFs`: source = mrfs where `requestor_name==user.full_name` (the established scoping — mrf-form.js L869). getDocs that scoped query (all statuses, newest first). Row → `#/procurement/records`. Row title = mrf_id; meta = status + project_name.

    Row HTML = `.cc-yourwork-row` (title 14px/600 ink + meta 12px muted + a right chevron `›`), min-height 44px, whole row clickable. Each bucket shows MAX 5 rows; if more, append `<span class="cc-yourwork-more" onclick="…">+${N-5} more</span>` linking to the relevant list. Buckets render in a→b→c order; **omit any empty bucket entirely** (no per-bucket empty text). If all three buckets are empty → keep `#ccYourWork` `display:none` (no panel-level empty state); otherwise set it visible with a `<div class="cc-yourwork-bucket-label">Your work</div>`-style card heading at the top (UI-SPEC: card titled `Your work`).

    Call `renderYourWork(user)` from `init()` (await — it does getDocs). Register any click handlers on window as needed (or use inline location.hash).
  </action>
  <acceptance_criteria>
    - `grep 'function renderYourWork' app/views/home.js` matches
    - `grep 'Proposals for revision' app/views/home.js` AND `Assigned projects & services` AND `Submitted MRFs` all present (bucket labels exact)
    - `grep 'Your work' app/views/home.js` matches (panel heading exact — lowercase w)
    - `grep "requestor_name" app/views/home.js` matches (bucket c scoping)
    - `grep 'getAssignedProjectCodes' app/views/home.js` AND `getAssignedServiceCodes` present (bucket b)
    - `grep 'cc-yourwork-more' app/views/home.js` matches (the +N more affordance)
    - Panel hides when empty: `grep "ccYourWork').style.display" app/views/home.js` or equivalent toggle present; SEE_ALL role omits bucket b
    - Browser UAT (flag): an operations_user with a for-revision proposal + assigned codes + a submitted MRF sees all three buckets; a finance user (no owned items) sees the panel hidden entirely.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Recent Activity panel + condensed door rail + destroy() finalize</name>
  <files>app/views/home.js</files>
  <read_first>
    - app/notifications.js loadRecentForDropdown L239-256 (the notifications query pattern) + TYPE_META
    - app/auth.js updateNavForAuth L403-419 (the exact door gating predicate)
    - app/views/home.js top imports L7 (add orderBy, limit) + destroy() L809-845
    - 107-UI-SPEC.md §"Recent Activity panel" + §"Nav Door Rail"
  </read_first>
  <action>
    Import: extend the firebase.js import in home.js to include `orderBy, limit` (currently imports db, collection, doc, query, where, onSnapshot, getDocs, getDoc). Import `TYPE_META` from '../notifications.js' (home.js already imports createNotification/NOTIFICATION_TYPES from there — add TYPE_META).

    Implement `async function renderRecentActivity(user)` writing into `#ccActivity`:
    - Card heading `Recent activity` (exact — lowercase a).
    - Query: `getDocs(query(collection(db,'notifications'), where('user_id','==',user.uid), orderBy('created_at','desc'), limit(8)))` (read-only slice — distinct from the bell; NO mark-as-read). Wrap in try/catch.
    - Each row `.cc-activity-row`: `.cc-activity-icon` = 28px tonal circle (`style="background:${meta.color}22;color:${meta.color};"`) holding `TYPE_META[n.type].icon`; text = `TYPE_META[n.type].label` (or n.message) + object_name; right-aligned relative time from n.created_at (inline relative-time helper). Whole row clickable → `location.hash = n.link || TYPE_META[n.type]?.target_route || '#/'` (NO state mutation).
    - Empty (0 rows): `<div class="cc-activity-empty">No recent activity.</div>` (exact). This panel MAY show its empty line even when Your Work is hidden.

    Implement `function renderDoorRail(user)` writing into `#ccDoorRail`:
    - Doors (UI-SPEC table): [Clients 📋 `#/clients` route-key `clients`] [Projects 🏗️ `#/projects` `projects`] [Services 🔧 `#/services` `services`] [Procurement 🛒 `#/procurement` `procurement`] [Finance 💰 `#/finance` `finance`].
    - Gate each door with the SAME predicate as auth.js: `const perms = window.getCurrentPermissions?.(); const canReach = perms?.tabs?.[routeKey]?.access ?? true;` — render the tile ONLY if canReach. Do NOT invent a new gate.
    - Tile HTML = `.cc-door` (onclick `location.hash='#/…'`) containing `<span class="cc-door-icon">${emoji}</span><span class="cc-door-label">${label}</span>`. No paragraph, no button.

    Call `renderRecentActivity(user)` (await) and `renderDoorRail(user)` from `init()`.

    Finalize `destroy()`: delete `window.ccScrollTo` (from Task 1) and any other window fns registered by 107.4 tasks; the panels add NO listeners (all getDocs), so there is nothing new to unsubscribe. Confirm all 107.3 + 107.4 window fns are removed and no getDocs are left pending after teardown.
  </action>
  <acceptance_criteria>
    - `grep 'function renderRecentActivity' app/views/home.js` AND `function renderDoorRail` present
    - `grep 'Recent activity' app/views/home.js` matches (exact heading) AND `No recent activity.` present (empty copy exact)
    - `grep "where('user_id', *'==', *user.uid)" app/views/home.js` AND `orderBy('created_at', 'desc')` AND `limit(8)` present (notifications read-only slice)
    - `grep 'orderBy, limit' app/views/home.js` OR both orderBy and limit added to the firebase import line
    - `grep 'getCurrentPermissions' app/views/home.js` matches AND `tabs?.[' app/views/home.js` (door gate mirrors auth.js) — 5 doors present (`#/clients`,`#/projects`,`#/services`,`#/procurement`,`#/finance`)
    - `grep 'cc-door-icon' app/views/home.js` matches AND emojis 📋 🏗️ 🔧 🛒 💰 present
    - `grep -c 'onSnapshot(' app/views/home.js` stays at 1 (panels add no listeners; only `_loadHomeProposalsTab` remains — match the `onSnapshot(` call form, not bare `onSnapshot`)
    - `grep 'delete window.ccScrollTo' app/views/home.js` in destroy()
    - Browser UAT (flag): Recent Activity lists the user's latest notifications (read-only, clicking navigates without marking read); the door rail shows only the areas the role can reach and each tile navigates.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- KPI/Your Work/Recent Activity/door rail all use getDocs (compute-on-load) — grep confirms no onSnapshot increase.
- Door rail gate is byte-identical in intent to auth.js (`getCurrentPermissions().tabs[route].access ?? true`).
- Recent Activity is read-only (no markNotificationRead call anywhere in the new code).
- destroy() removes every window function 107.3 + 107.4 registered.
</verification>

<success_criteria>
- HOME-05: role-tailored KPI chips render with resolvable counts (unresolved chips omitted, never 0/—).
- HOME-06: Your Work shows 3 buckets, each hidden when empty, panel hidden when all empty.
- HOME-07: Recent Activity shows the user's notification slice, read-only.
- HOME-08: 5 permission-scoped door tiles keep every reachable area one click away.
</success_criteria>

<output>
After completion, create `.planning/phases/107-home-command-center-shell-feed-engine/107.4-SUMMARY.md` confirming the four surfaces mount into the 107.3 skeleton and listing every new window function + its destroy() removal (for the phase-close listener/leak check).
</output>
