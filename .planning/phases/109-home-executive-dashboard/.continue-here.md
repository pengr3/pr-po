---
context: phase
phase: 109-home-executive-dashboard
paused_command: /gsd-discuss-phase 109
task: 4
total_tasks: 9
status: in_progress
last_updated: 2026-07-13T10:06:57Z
---

# Resume — Phase 109 discuss-phase (paused mid-flight, inside spike 108)

_No BLOCKING constraints this session — nothing was discovered through methodological failure that gates
resumption. One advisory (Chart.js animation) is recorded below._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Chart.js blank-on-first-paint | Default entrance animation needs `requestAnimationFrame`; in a background/preview tab rAF doesn't tick, so canvases are created but paint 0%. Hit during spike verification. | advisory | Set `animation:false` (or a short fixed duration) on every Chart in the real Phase 77 restore + Dashboard. |
| Scope creep via enrichment | User chose enrichments (risk strip, procurement throughput, supplier concentration) that EXTEND DASH-01…07. | advisory | Before planning, explicitly decide: fold into Phase 109 scope, or split to a follow-on phase. Do not silently plan them as in-scope. |

<current_state>
Paused inside `/gsd-discuss-phase 109`. The discussion identified 4 gray areas; the user selected all 4.
**Area 1 (role content model) was spiked** rather than decided abstractly → `spike 108`.

- **Area 1 is effectively resolved:** user chose **Model A (Uniform, scoped data)**, then asked to make it
  weightier → enriched Model A built (iteration 2). Session paused at the iter-2 **verification checkpoint**
  (user's "keep all / trim / add more" reaction is the pending action).
- **Areas 2, 3, 4 have NOT been discussed at all.**
- **109-CONTEXT.md does NOT exist yet.**

The live artifact is the enriched Model A mockup (spike 108). All numbers verified consistent + scoped.
</current_state>

<completed_work>
- discuss-phase 109 setup: loaded PROJECT/REQUIREMENTS/STATE, prior contexts (107, 108), scoped the codebase
  (Chart.js already loaded; `homeTabDashboard` stub in home.js:707; scoping predicates + finance derivations).
- Identified 4 gray areas: (1) role content model, (2) dept scoping, (3) data freshness, (4) chart/table substance.
- Area 1 → spike 108. Iter 1: A/B/C model shootout, user picked **A**. Iter 2: enriched Model A built + verified.
</completed_work>

<remaining_work>
- Area 1: get user's reaction to enriched Model A → finalize spike verdict + commit the spike (build step i).
- Areas 2, 3, 4: not started (see the "gray areas" annotations below to restart the discussion cleanly).
- Write `109-CONTEXT.md` capturing Model A + enrichment decisions, and resolve the scope-extension question.
</remaining_work>

<decisions_made>
- **Content MODEL = A (Uniform, scoped data).** Verified against B/C in a 7-combo role×model matrix. Faithful to
  DASH-02..06 (which name specific tiles/charts, not per-role variants); one render path; scoping does per-role work.
- **Enrich Model A** with all 4 themes: cash-flow/receivables ribbon, ₱-value-weighted charts, risk & delivery,
  procurement throughput & suppliers. (Base A showed inventory, not insight — user's exact critique.)
- **Chart.js `animation:false`** for reliable paint (also applies to the Phase 77 restore).
- **Scope flag:** risk strip + procurement throughput + supplier concentration EXTEND DASH-01…07 — deliberate
  user choice, to be confirmed as in-scope-extension or split before planning.
- Key data insight from the spike: ₱-weight toggle reveals On-going = ~47% by count but **~75% by ₱ value**.
</decisions_made>

<blockers>
- None technical. Pending human action: user reacts to the enriched Model A checkpoint (keep all / trim / add more).
</blockers>

## The 4 gray areas (to resume the discussion at Area 2)

1. **Role content model** — RESOLVED: Model A + enriched (pending final sign-off).
2. **Dept scoping** — DASH-07 says "dept admins = their dept," but the app's real model (quick 260706-mco) is
   "home-dept see-all + assigned cross-dept" (`PROJECT_SEE_ALL_ROLES`/`SERVICE_SEE_ALL_ROLES`). Decide: strict
   home-dept, home-dept + assigned (matches every list/feed → recommended), or both depts. Drives every figure.
3. **Data freshness** — compute-on-load + Refresh (Strategy A, matches Command Center, honors 106 leak findings)
   vs live `onSnapshot` (the retired Phase 77 charts were live). Recommend compute-on-load.
4. **Chart & table substance** — spend-trend window/metric, proposal-funnel stages (+ the funnel-by-₱ data
   dependency: proposals need an estimate field — researcher to confirm), portfolio-table health signal
   (at-risk accent + progress previewed in the spike).

## Required Reading (in order)
1. `.planning/spikes/108-executive-dashboard-content-model/README.md` — the full spike record (both iterations,
   A/B/C matrix, enrichment table, scope flag, verified numbers).
2. `.planning/spikes/108-executive-dashboard-content-model/spike.html` — the live enriched-Model-A mockup.
3. `.planning/ROADMAP.md` §"Phase 109" + `.planning/REQUIREMENTS.md` DASH-01…07 — the fixed scope.
4. `.planning/phases/108-.../108-CONTEXT.md` + `107-.../107-CONTEXT.md` — Home engine/shell contracts + Strategy A.
5. Memory `project_admin_crossdept_scoping` — the dept-scoping model behind Area 2.

## Infrastructure State
- Static spike served via `python -m http.server 8000` from repo root (likely stopped on pause). URL:
  `http://localhost:8000/.planning/spikes/108-executive-dashboard-content-model/spike.html`
- Chart.js v4.4.7 already in `index.html`; CSP allows `cdn.jsdelivr.net` — no loader work for the real build.
- Serve from the MAIN repo checkout, not an auto-review worktree (memory `feedback_serve_from_main_repo_not_worktree`).
- Branch `v4.2` (tracks `origin/v4.2`).

## Pre-Execution Critique Required
- Design artifact: `.planning/spikes/108-executive-dashboard-content-model/spike.html` (enriched Model A).
- Critique focus: is the enriched dashboard too dense? Which extensions (risk/throughput/supplier) truly belong
  in Phase 109 vs a follow-on? Does funnel-by-₱ have a real data source?
- Gate: resolve the scope-extension question before `/gsd:plan-phase 109`.

<context>
User is decisive and prefers to decide UI by SEEING (spiked rather than pick abstractly — same as spike 041/107).
Model A is locked; the open thread is how much enrichment ships in Phase 109 vs later. Areas 2–4 are untouched —
restart the discussion there. When ready, `/gsd-discuss-phase 109` will pick this handoff up; then write CONTEXT.md.
</context>

<next_action>
Start with: re-open the spike URL, get the user's keep/trim/add reaction to enriched Model A, then finalize the
spike (verdict + commit). Then resume `/gsd-discuss-phase 109` at **Area 2 (dept scoping)** using the gray-area
notes above; discuss Areas 2–4; then write `109-CONTEXT.md` (including the explicit scope-extension decision).
</next_action>
