---
phase: 100-project-detail-lifecycle-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - styles/views.css
autonomous: true
requirements:
  - SC-2
  - SC-7
  - SC-8

must_haves:
  truths:
    - "The lifecycle accordion card renders visually — collapsed and expanded — using only CSS classes from views.css"
    - "Stage nodes render in done/current/future/revision/loss states via .s-done/.s-current/.s-future/.s-revision/.s-loss"
    - "Attach zone tabs (.az, .az-tabs, .az-tab.active) render correctly with link/file panel toggle"
    - "Doc rollup (.doc-rollup, .doc-slot, .ds-empty) renders 4-slot list with empty/filled states"
    - "Post-approval mini-track (.pa-track, .pa-dot states) renders correctly"
  artifacts:
    - path: "styles/views.css"
      provides: "Full .lc-* CSS block — all lifecycle accordion rules"
      contains: ".lc-accordion"
  key_links:
    - from: "styles/views.css"
      to: "project-detail.js Plan 02"
      via: ".lc-accordion, .lc-track, .stage-circle, .lc-body, .lc-active, .lc-complete"
      pattern: "lc-accordion"
---

<objective>
Add the complete lifecycle accordion CSS block to styles/views.css. This is the visual contract — all CSS classes used by Plans 02-04 must exist here first.

Purpose: Zero-build SPA; JS builds innerHTML with class names that must exist in CSS before first render. CSS plan in Wave 1 unblocks all JS plans.
Output: A self-contained `/* ── LIFECYCLE ACCORDION CARD (Phase 100) ── */` block appended to styles/views.css containing every class used by the spike.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\.planning\phases\100-project-detail-lifecycle-rebuild\100-CONTEXT.md

<interfaces>
<!-- CSS classes the executor must create exactly as named — Plans 02-04 reference these by name -->
<!-- Source: spike.html styles block lines 91-300 -->

Accordion shell:
  .lc-accordion           — card wrapper, border 1.5px solid var(--border), border-radius 12px, overflow hidden, transition border-color+box-shadow
  .lc-accordion.lc-active — border-color #93c5fd, box-shadow 0 2px 12px rgba(26,115,232,0.08)
  .lc-accordion.lc-complete — border-color #86efac, box-shadow 0 2px 12px rgba(5,150,105,0.08)
  .lc-accordion.open .lc-chevron — transform rotate(180deg)
  .lc-accordion.open > .lc-card-header — border-color var(--border)

Card header (always visible):
  .lc-card-header         — flex, align-items center, justify-content space-between, padding 10px 18px, cursor pointer, user-select none, border-bottom 1px solid transparent, transition border-color
  .lc-card-header:hover   — background #fafbfc
  .lc-header-left         — flex, align-items center, gap 10px
  .lc-card-title          — font-size 11px, font-weight 700, color var(--muted), text-transform uppercase, letter-spacing 0.07em
  .lc-cur-badge           — inline-flex, align-items center, gap 5px, padding 3px 10px, border-radius 20px, font-size 11px, font-weight 700
  .lc-header-right        — flex, align-items center, gap 10px
  .lc-chevron             — font-size 11px, color var(--muted), transition transform 0.22s, flex-shrink 0

Track section:
  .lc-track-wrap          — padding 14px 18px 12px, overflow-x auto
  .lc-track               — flex, align-items flex-start, min-width 580px

Stage nodes:
  .stage-node             — flex-direction column, align-items center, gap 5px, flex 1, min-width 0
  .stage-circle           — 48x48px, border-radius 50%, flex center, font-size 20px, position relative, transition transform+box-shadow, flex-shrink 0
  .s-done                 — background #d1fae5, border 2.5px solid #059669
  .s-current              — background #eff6ff, border 3px solid var(--primary), box-shadow 0 0 0 4px rgba(26,115,232,0.14), transform scale(1.1)
  .s-future               — background #f1f5f9, border 2px solid #cbd5e1, opacity 0.55
  .s-loss                 — background #fee2e2, border 2px solid #ef4444
  .s-revision             — background #fef3c7, border 3px solid #f59e0b, box-shadow 0 0 0 4px rgba(245,158,11,0.14), transform scale(1.1)
  .stage-check            — absolute top -3px right -3px, 16x16px, border-radius 50%, background #059669, border 2px solid white, flex center, font-size 8px, color white
  .stage-label            — font-size 10.5px, font-weight 500, color var(--text2), text-align center, line-height 1.25, max-width 62px
  .stage-node.s-current-node .stage-label  — color var(--primary), font-weight 700
  .stage-node.s-done-node .stage-label     — color #047857
  .stage-node.s-revision-node .stage-label — color #d97706, font-weight 700
  .stage-chip             — font-size 9px, font-weight 800, text-transform uppercase, letter-spacing 0.05em, padding 2px 6px, border-radius 8px
  .chip-done              — background #d1fae5, color #065f46
  .chip-here              — background #dbeafe, color #1d4ed8
  .chip-end               — background #f1f5f9, color #64748b
  .chip-gap               — background #fee2e2, color #991b1b
  .chip-revision          — background #fef3c7, color #92400e
  .connector              — width 22px, height 2px, flex-shrink 0, margin-top 23px, background #cbd5e1, transition background 0.3s
  .connector.done         — background #059669

Expandable body:
  .lc-body                — max-height 0, overflow hidden, transition max-height 0.3s ease + padding 0.2s, padding 0 18px
  .lc-accordion.open .lc-body — max-height 900px, padding 14px 18px 18px

Body content:
  .gate-label             — font-size 10px, font-weight 700, color var(--muted), text-transform uppercase, letter-spacing 0.07em, margin-bottom 12px, flex+align-items center+gap 8px; ::before and ::after as flex 1 height 1px background var(--border) lines
  .lc-desc                — font-size 12.5px, color var(--text2), line-height 1.65, margin-bottom 14px
  .action-row             — flex, gap 9px, align-items center, flex-wrap wrap, margin-top 12px
  .action-note            — font-size 11px, color var(--muted)
  .gate-warn              — flex, align-items center, gap 7px, background #fef3c7, border 1px solid #fde68a, border-radius 6px, padding 8px 11px, font-size 11.5px, color #92400e, margin-bottom 10px

Attach zone (.az):
  .az                     — background var(--bg), border 1px solid var(--border), border-radius 8px, padding 11px 13px, margin-bottom 9px
  .az.az-ok               — background #f0fdf4, border-color #86efac
  .az-lbl                 — font-size 10px, font-weight 700, text-transform uppercase, letter-spacing 0.06em, color var(--muted), margin-bottom 7px
  .az-tabs                — flex, gap 6px, margin-bottom 7px
  .az-tab                 — padding 3px 9px, border-radius 5px, border 1px solid var(--border), background white, font-size 11px, font-weight 500, cursor pointer, color var(--text2), transition all 0.12s
  .az-tab.active          — background var(--primary), color white, border-color var(--primary)
  .az-row                 — flex, gap 7px, align-items center
  .az-input               — flex 1, padding 6px 10px, border 1px solid var(--border), border-radius 6px, font-size 12.5px, outline none
  .az-input:focus         — border-color var(--primary), box-shadow 0 0 0 2px rgba(26,115,232,0.12)
  .az-doc                 — flex, align-items center, gap 9px, background white, border 1px solid #86efac, border-radius 6px, padding 8px 11px
  .az-doc-icon            — font-size 17px, flex-shrink 0
  .az-doc-info            — flex 1, min-width 0
  .az-doc-name            — font-size 12.5px, font-weight 600, color #065f46, overflow hidden, text-overflow ellipsis, white-space nowrap
  .az-doc-kind            — font-size 10.5px, color var(--muted)

Built panel:
  .built                  — background #f0fdf4, border 1px solid #bbf7d0, border-radius 7px, padding 11px 13px, margin-bottom 12px
  .built-title            — font-size 12px, font-weight 700, color #065f46, margin-bottom 4px
  .built-desc             — font-size 11.5px, color #047857, line-height 1.55, margin-bottom 10px

Post-approval mini-track:
  .pa-track               — flex, align-items center, background #f8fafc, border 1px solid var(--border), border-radius 8px, padding 9px 13px, margin-bottom 13px, overflow-x auto
  .pa-stage               — flex 1, min-width 56px, flex-direction column, align-items center, gap 3px
  .pa-dot                 — 24x24px, border-radius 50%, flex center, font-size 10px, font-weight 700, border 2px solid var(--border), background white
  .pa-dot.pa-done         — background var(--success), border-color var(--success), color white
  .pa-dot.pa-active       — background var(--primary), border-color var(--primary), color white
  .pa-dot.pa-future       — background #f1f5f9, color var(--muted)
  .pa-line                — width 28px, height 2px, background var(--border), flex-shrink 0
  .pa-line.pa-done        — background var(--success)
  .pa-lbl                 — font-size 9.5px, color var(--text2), text-align center, line-height 1.2
  .pa-lbl.pa-active       — color var(--primary), font-weight 700
  .pa-lbl.pa-done         — color var(--success), font-weight 600

Document rollup:
  .doc-rollup             — border 1px solid var(--border), border-radius 8px, overflow hidden, margin-top 14px
  .doc-rollup-hdr         — flex, align-items center, justify-content space-between, padding 7px 12px, background #f8fafc, border-bottom 1px solid var(--border)
  .doc-rollup-title       — font-size 10px, font-weight 700, color var(--muted), text-transform uppercase, letter-spacing 0.07em
  .doc-rollup-count       — font-size 10px, color var(--primary), font-weight 700
  .doc-slot               — flex, align-items center, gap 9px, padding 8px 12px, border-bottom 1px solid var(--border)
  .doc-slot:last-child    — border-bottom none
  .doc-slot.ds-empty      — opacity 0.4
  .ds-icon                — font-size 15px, width 20px, text-align center, flex-shrink 0
  .ds-info                — flex 1, min-width 0
  .ds-stage               — font-size 9.5px, font-weight 700, text-transform uppercase, letter-spacing 0.05em, color var(--muted)
  .ds-name                — font-size 12px, font-weight 600, color var(--text), overflow hidden, text-overflow ellipsis, white-space nowrap
  .ds-name.none           — color var(--muted), font-weight 400, font-size 11.5px
  .ds-link                — font-size 11px, color var(--primary), font-weight 600, cursor pointer, flex-shrink 0

Completed grid:
  .comp-grid              — grid, grid-template-columns 1fr 1fr, gap 7px, margin-bottom 12px
  .comp-cell              — background var(--bg), border 1px solid var(--border), border-radius 6px, padding 7px 10px
  .comp-cell-lbl          — font-size 9.5px, font-weight 700, text-transform uppercase, letter-spacing 0.05em, color var(--muted), margin-bottom 2px
  .comp-cell-val          — font-size 11.5px, color var(--text), font-weight 500, word-break break-all
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append lifecycle accordion CSS block to views.css</name>
  <files>styles/views.css</files>
  <read_first>
    - C:\Users\Admin\Roaming\pr-po\.planning\spikes\031-lifecycle-accordion-card\spike.html (lines 91–301: the full style block — this is the authoritative source for every value)
    - C:\Users\Admin\Roaming\pr-po\styles\views.css (read the last 20 lines to find the insertion point; append after the final existing block)
  </read_first>
  <action>
Append a clearly delimited CSS block to the end of styles/views.css. The section comment must be exactly:

  /* ── LIFECYCLE ACCORDION CARD (Phase 100) ────────────────────────────── */

Transcribe every rule from the spike.html style block into this section. The rules to include (in order):

1. .lc-accordion + .lc-accordion.lc-active + .lc-accordion.lc-complete
2. .lc-card-header + .lc-accordion.open > .lc-card-header + .lc-card-header:hover
3. .lc-header-left + .lc-card-title + .lc-cur-badge + .lc-header-right + .lc-chevron
4. .lc-accordion.open .lc-chevron
5. .lc-track-wrap + .lc-track
6. .stage-node + .stage-circle + .s-done + .s-current + .s-future + .s-loss + .s-revision
7. .stage-check + .stage-label
8. .stage-node.s-current-node .stage-label + .stage-node.s-done-node .stage-label + .stage-node.s-revision-node .stage-label
9. .stage-chip + .chip-done + .chip-here + .chip-end + .chip-gap + .chip-revision
10. .connector + .connector.done
11. .lc-body + .lc-accordion.open .lc-body
12. .gate-label + .gate-label::before + .gate-label::after
13. .lc-desc + .action-row + .action-note + .gate-warn
14. .az + .az.az-ok + .az-lbl + .az-tabs + .az-tab + .az-tab.active
15. .az-row + .az-input + .az-input:focus + .az-doc + .az-doc-icon + .az-doc-info + .az-doc-name + .az-doc-kind
16. .built + .built-title + .built-desc
17. .pa-track + .pa-stage + .pa-dot + .pa-dot.pa-done + .pa-dot.pa-active + .pa-dot.pa-future
18. .pa-line + .pa-line.pa-done + .pa-lbl + .pa-lbl.pa-active + .pa-lbl.pa-done
19. .doc-rollup + .doc-rollup-hdr + .doc-rollup-title + .doc-rollup-count
20. .doc-slot + .doc-slot:last-child + .doc-slot.ds-empty
21. .ds-icon + .ds-info + .ds-stage + .ds-name + .ds-name.none + .ds-link
22. .comp-grid + .comp-cell + .comp-cell-lbl + .comp-cell-val

Use CSS custom properties already present in views.css (--primary, --success, --border, --muted, --text, --text2, --bg) — do NOT hardcode hex values that map to existing variables. For colors not covered by existing vars (e.g. #93c5fd, #86efac, #d1fae5, #fef3c7, etc.) use the hex literal as in spike.html. Do NOT import spike.html's sidebar/shell/audit/spike-ctrl CSS — those are prototype-only.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('styles/views.css','utf8');const checks=['.lc-accordion','.lc-active','.lc-complete','.lc-card-header','.lc-cur-badge','.lc-chevron','.lc-track-wrap','.lc-track','.stage-node','.stage-circle','.s-done','.s-current','.s-future','.s-loss','.s-revision','.stage-check','.stage-label','.stage-chip','.chip-done','.chip-here','.chip-end','.chip-gap','.chip-revision','.connector','.lc-body','.gate-label','.lc-desc','.action-row','.gate-warn','.az','.az-ok','.az-lbl','.az-tabs','.az-tab','.az-row','.az-input','.az-doc','.built','.pa-track','.pa-dot','.pa-line','.pa-lbl','.doc-rollup','.doc-slot','.ds-empty','.ds-name','.ds-link','.comp-grid','.comp-cell'];const missing=checks.filter(c2=>!c.includes(c2));if(missing.length){console.error('MISSING CSS:',missing);process.exit(1);}console.log('All',checks.length,'CSS classes present');"</automated>
  </verify>
  <acceptance_criteria>
    - `styles/views.css` contains `/* ── LIFECYCLE ACCORDION CARD (Phase 100) */` section comment
    - All 47 class selectors listed in the verify command are present in the file
    - `.lc-accordion.open .lc-body` rule sets `max-height: 900px` and `padding: 14px 18px 18px`
    - `.lc-body` rule sets `max-height: 0` and `overflow: hidden`
    - `.connector.done` sets `background: #059669`
    - `.gate-label::before` and `.gate-label::after` are present (decorative divider lines)
    - No sidebar, .spike-ctrl, .audit-strip, .audit-row CSS from spike.html prototype included
    - No new CSS classes conflicting with existing `.pa-*` or `.az-*` usage elsewhere in views.css (grep confirms no prior usage)
  </acceptance_criteria>
  <done>Node verify script exits 0 with "All 47 CSS classes present". The lifecycle accordion can be rendered visually without any inline styles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS-only plan | No user data, no network calls — CSS is static; no threat surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01-SC | Tampering | styles/views.css append | accept | CSS-only file, no secrets, no executable code; file is version-controlled |
</threat_model>

<verification>
- `node` verify script passes with 0 missing classes
- No duplicate CSS section markers (grep for "LIFECYCLE ACCORDION CARD" returns exactly 1 match)
- views.css remains valid CSS (no unclosed braces): `node -e "require('fs').readFileSync('styles/views.css','utf8')" ` exits 0
</verification>

<success_criteria>
All .lc-*, .stage-*, .connector, .az*, .pa-*, .doc-rollup, .doc-slot, .ds-*, .comp-* CSS rules exist in views.css. Plans 02-04 can reference these classes in innerHTML strings without any inline style fallbacks.
</success_criteria>

<output>
Create `.planning/phases/100-project-detail-lifecycle-rebuild/100-01-SUMMARY.md` when done.
</output>
