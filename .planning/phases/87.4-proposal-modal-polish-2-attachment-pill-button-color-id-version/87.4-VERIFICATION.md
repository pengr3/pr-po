---
phase: 87.4-proposal-modal-polish-2-attachment-pill-button-color-id-version
verified: 2026-05-24T00:00:00Z
status: human_needed
score: 15/15 code-level must-haves verified (UAT deferred per overnight batch authorization)
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Plan 01 — Attachment-gate end-to-end UAT (9 steps)"
    expected: "Disabled Submit + exact hint copy on draft AND for_revision; enabled Submit after attachment add; toast on stale-state click; PERMISSION_DENIED on direct Firestore write without attachment_kind"
    why_human: "Requires firebase deploy --only firestore:rules + interactive browser session + DevTools console writes against live Firestore"
  - test: "Plan 01 — Production rules deploy"
    expected: "firebase deploy --only firestore:rules completes; D-03/D-04 server-side gate is live in production"
    why_human: "External service action; per memory project_phase841_pending_followups, requires verification that any prior pending deploy is already in production"
  - test: "Plan 02 — Request Revision button visual verification (8 steps)"
    expected: "Orange #f59e0b background with white text in pending_client panel; bottom-row grid reads as Amount | Target Client | Project with no hole; Reject Proposal stays red; draft Submit/Edit unchanged"
    why_human: "Visual rendering + DevTools inspect-element + responsive resize behavior cannot be programmatically verified"
  - test: "Plan 03 — Comms-pill sizing visual verification (9 steps)"
    expected: "All three pill types (sent, feedback_received, revision_requested) render at text-content width; DevTools computed display:inline-block + width matches text; no regression in procurement records / home Approval Queue pills"
    why_human: "Plan 03 Task 1 diagnosis was code-inspection only (overnight batch authorization); DevTools confirmation of the cascade reasoning is deferred to this UAT step. Plus regression-scan across views that use the same badge classes"
---

# Phase 87.4: Proposal Modal Polish Pass 2 Verification Report

**Phase Goal:** Five polish items observed on the proposal modal after Phase 87.3 shipped — (1) attachment-required submission gate (3 layers), (2) PROP-ID header decision-record only, (3) Request Revision button recolor red→orange, (4) dead Version field removal + grid adjustment, (5) audit pill background-fill root-cause CSS fix.
**Verified:** 2026-05-24
**Status:** human_needed (all code-level truths VERIFIED; consolidated browser UAT + production rules deploy are the only outstanding items per overnight batch authorization)
**Re-verification:** No — initial verification

## Goal Achievement

### Plan 01 — Attachment-Required Submission Gate (5 truths)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Submit button rendered with `disabled` + inline hint "Add an attachment to submit for approval." when `proposal.attachment_kind` is unset | COVERED | `app/proposal-modal.js:401-406` — `const hasAttachment = !!proposal.attachment_kind;` branch pushes `<button ... disabled ...>` + wrapped `<div ...>Add an attachment to submit for approval.</div>`. Verified by Grep: exact hint copy present at line 405. |
| 2 | Submit button rendered enabled with no hint when `attachment_kind` is set | COVERED | `app/proposal-modal.js:402-403` — `if (hasAttachment) { buttons.push(...) }` branch pushes the byte-identical pre-87.4 enabled button (no `disabled`, no wrapper div, no hint) |
| 3 | `submitProposalForApproval` has early-return guard that toasts and bails before `_applyProposalStateTransition` when `attachment_kind` is null | COVERED | `app/proposal-modal.js:971-974` — `if (!proposal.attachment_kind) { showToast('Add an attachment before submitting for approval.', 'error'); return; }` placed AFTER the status check (line 962) and BEFORE `showLoading(true)` at line 976. Verified: `_applyProposalStateTransition` call at line 978 is unreachable when guard fires. |
| 4 | Firestore rule rejects pending_internal transitions with null attachment_kind for BOTH BRANCH 1 (admin) and BRANCH 2 (assigned user) | COVERED | `firestore.rules:620-659` — the gate is implemented as a TOP-LEVEL conjunction wrapping the existing `BRANCH 1 || BRANCH 2` disjunction. Lines 621-625: `(request.resource.data.status != 'pending_internal' || resource.data.status == 'pending_internal' || request.resource.data.attachment_kind != null)`. Both branches inherit the gate. Phase 87.4 D-03/D-04 marker comment at line 613. |
| 5 | D-13 anchor invariant: literal substring `'version', 'attachment_kind'` present in proposals block | COVERED | `firestore.rules:637` — Grep returns exact match: `...'target_client_name', 'version', 'attachment_kind', 'attachment_url', 'attachment_filename'...` — confirms `'version'` at field-mask index 11 immediately before `'attachment_kind'` at index 12 |

**Plan 01 score:** 5/5 truths COVERED

### Plan 02 — Request Revision Recolor + Version Removal (5 truths)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Request Revision button at `pending_client` panel renders orange `#f59e0b` with white text via inline `background:#f59e0b;color:#fff;border-color:#f59e0b;width:100%;` on `class="btn"` | COVERED | `app/proposal-modal.js:432` — exact verbatim string: `<button class="btn" style="background:#f59e0b;color:#fff;border-color:#f59e0b;width:100%;" onclick="window.openRequestRevisionModal('${docId}')">Request Revision</button>`. No `btn-danger`, no `btn-warning`. |
| 2 | Only Mark as Loss + Reject Proposal use `btn-danger` — exactly 2 occurrences in `renderProposalActionButtons` | COVERED | Grep on `app/proposal-modal.js` for `btn-danger`: 5 total matches in file, but within `renderProposalActionButtons` (lines 383-441): exactly 2 — line 416 (Reject Proposal) + line 422 (Mark as Loss). Other matches at lines 547 (attachment Remove confirm button), 1086 (Reject sub-modal confirm class), 1212 (Loss confirm button) are outside the action-buttons function. |
| 3 | Dead `<div>v1</div>` Version field deleted entirely from `buildProposalDetailsBlock` | COVERED | `app/proposal-modal.js:477-512` — `buildProposalDetailsBlock` now contains 5 cells: Title (span 3), Description (span 3), Amount, Target Client, Project. Grep for `>v1<` in the file: 0 matches. Grep for `Version`: 1 match at line 481 — explanatory comment only ("Removed the dead Version field entirely"), not rendered HTML. |
| 4 | Grid layout still renders cleanly — Strategy A (3-column bottom row) chosen | COVERED | `app/proposal-modal.js:489` — outer grid declaration: `<div style="display:grid;grid-template-columns:repeat(3, 1fr);gap:1rem;margin-bottom:1.5rem;">`. Title (line 490) and Description (line 494) have `grid-column:1/-1`. Bottom row Amount/Target Client/Project occupies the 3 columns symmetrically. |
| 5 | No schema change, no `proposal.version` read path introduced (D-14) | COVERED | Grep `proposal\.version` in `app/proposal-modal.js`: 0 matches. Grep `Version`: 1 match (explanatory comment at line 481, no JS reference). `firestore.rules` field-mask still includes `'version'` (preserved per D-13) but no read or write path was added in Plan 02. |

**Plan 02 score:** 5/5 truths COVERED

### Plan 03 — Audit/Comms Pill Background-Fill Fix (5 truths)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Every comms-type pill (`sent` / `feedback_received` / `revision_requested`) has defensive `display: inline-block` + `width: fit-content` via new Phase 87.4 rule block in `styles/components.css` | COVERED | `styles/components.css:551-562` — selector list covers `.status-badge, .badge-primary, .badge-secondary, .badge-pending, .badge-approved, .badge-rejected, .badge-success, .badge-danger, .badge-warning` with `display: inline-block; width: fit-content;`. This covers `badge-primary` (sent), `status-badge` (parent of feedback_received with `.pending` and revision_requested with `.rejected`), and `.badge-pending` / `.badge-rejected` modifiers. |
| 2 | Root-cause CSS fix lives at class level (not inline on `<span>` in proposal-modal.js per D-19) | COVERED | Grep `fit-content\|inline-block` in `app/proposal-modal.js`: 0 matches (the only inline pill `<span>` at line 337 keeps its original inline style `font-size:11px;padding:1px 6px;border-radius:4px;` — no sizing additions). All sizing applied at class level in `styles/components.css:551-562`. |
| 3 | No `overflow: hidden`, no fixed-pixel `width:`, no `max-width:` in the new Phase 87.4 rule block (D-20 + `feedback_root_cause_first`) | COVERED | Inspection of `styles/components.css:541-562` (the Phase 87.4 rule block extending from the marker comment through the closing `}`): only declarations present are `display: inline-block;` and `width: fit-content;`. No `overflow:hidden`, no fixed-pixel widths, no `max-width`. Other `max-width`/`overflow:hidden` hits in components.css (lines 15, 139, 141, 687, 690, 801, 1255, 1352, 1593, 1594, 1790, 1797, 1930, 1949) are all OUTSIDE the Phase 87.4 rule block. |
| 4 | Task 1 diagnosis findings section exists in 87.4-03-SUMMARY.md with verbatim header `## Task 1 Diagnosis Findings` and contains observed `display:` and `width:` cascade values, labeled as code-inspection | COVERED | `87.4-03-SUMMARY.md:58` — section header `## Task 1 Diagnosis Findings` present. Section body (lines 58-96) explicitly labeled `Diagnosis method: CODE-INSPECTION` (line 60), with concrete `display:` cascade analysis (lines 66) and `width:` cascade analysis (line 67). DevTools confirmation explicitly deferred to Task 3 UAT punch-list item (line 96). |
| 5 | Defensive rule block carries a Phase 87.4 D-15 comment referencing 87.4-03-SUMMARY.md | COVERED | `styles/components.css:541-550` — comment header reads `/* Phase 87.4 D-15/D-19 — defensive sizing for badge-class pills. ... per Task 1 code-inspection diagnosis in 87.4-03-SUMMARY.md ...`. Both `D-15` and `87.4-03-SUMMARY.md` substrings present. |

**Plan 03 score:** 5/5 truths COVERED

### Combined Score: 15/15 code-level truths COVERED

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/proposal-modal.js` | Disabled-button + inline hint UX; early-return guard; recolored Request Revision button; Version cell removed; grid restructured to 3-col | VERIFIED | All five edits present at lines 401-407 (Plan 01 Task 1), 971-974 (Plan 01 Task 2), 432 (Plan 02 Task 1), 489-510 (Plan 02 Task 2); no inline pill `<span>` modifications (Plan 03 D-19 honored) |
| `firestore.rules` | Attachment-presence gate on pending_internal transition (both branches); D-13 anchor preserved | VERIFIED | Lines 611-659 with top-level conjunction at lines 621-625; field-mask line 637 retains `'version', 'attachment_kind'` anchor |
| `styles/components.css` | Root-cause CSS fix for pill background overflow at class level | VERIFIED | Lines 541-562 — Phase 87.4 D-15/D-19 rule block with 9-class defensive selector list, properties limited to `display: inline-block; width: fit-content;` |
| `87.4-03-SUMMARY.md` | Task 1 DevTools diagnosis findings (`## Task 1 Diagnosis Findings` header) | VERIFIED | Section present with code-inspection caveat clearly labeled; DevTools confirmation deferred to Task 3 UAT punch-list |

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `renderProposalActionButtons` | `proposal.attachment_kind` | conditional `disabled` attribute | WIRED | Line 401 reads `proposal.attachment_kind`; line 405 emits `disabled` attribute conditionally |
| `submitProposalForApproval` | `proposal.attachment_kind` | early-return guard after `_fetchProposalDoc` | WIRED | Line 960 fetches doc, line 971 checks `!proposal.attachment_kind`, line 973 returns before `_applyProposalStateTransition` (line 978) |
| `firestore.rules proposals update rule` | `request.resource.data.attachment_kind` | top-level conjunction on pending_internal transition | WIRED | Lines 620-625 wrap BRANCH 1 || BRANCH 2; both branches subject to gate per D-04 |
| `renderProposalActionButtons` (line 432) | REVISION_REQUESTED audit dot color | inline style `background:#f59e0b` | WIRED | Constant at line 98 (`AUDIT_ACTION_DOT_COLORS.REVISION_REQUESTED = '#f59e0b'`); button at line 432 hard-codes same value — visual consistency contract honored |
| `buildProposalDetailsBlock` | `grid-template-columns` adjustment | `repeat(3, 1fr)` on line 489 | WIRED | Grid restructured from `1fr 1fr` to `repeat(3, 1fr)`; Title + Description span-3 cells; bottom row Amount\|Target Client\|Project |
| `_renderNestedCommsChild` `<span>` | `styles/components.css .badge-primary` / `.status-badge.pending` / `.status-badge.rejected` | class attribute lookup → new defensive rule | WIRED | Line 337 emits `<span class="${meta.cls}">`; meta.cls values from `COMMS_TYPE_META` (line 114) map to the 9-class selector list in components.css:551-562 |
| Task 1 diagnosis | Task 2 CSS rule | `87.4-03-SUMMARY.md` `## Task 1 Diagnosis Findings` section | WIRED | Section present; CSS rule comment at components.css:544-545 explicitly references the SUMMARY findings |

## Data-Flow Trace (Level 4)

Not applicable — all artifacts are UI rendering paths, CSS class fixes, and security rules. No backend data queries introduced or modified.

## Behavioral Spot-Checks

Static-code verification only — no runnable entry points exposed by this phase (UI changes require browser; security rule changes require Firestore emulator). All spot-checks routed to Step 8 human verification.

## Requirements Coverage

PLAN frontmatter `requirements: []` for all three plans — no formal REQ-IDs are claimed for this phase. The phase covers polish-pass items documented as D-01..D-20 in CONTEXT.md, all addressed:

| Decision | Plan | Status | Evidence |
|----------|------|--------|----------|
| D-01 (disable Submit + inline hint) | 01 | SATISFIED | proposal-modal.js:401-406 |
| D-02 (gate fires on both draft + for_revision) | 01 | SATISFIED | proposal-modal.js:391 outer if-block covers both statuses |
| D-03 (Firestore rule enforces server-side) | 01 | SATISFIED | firestore.rules:621-625 |
| D-04 (gate applies to both BRANCH 1 + BRANCH 2) | 01 | SATISFIED | firestore.rules:620 top-level conjunction wraps both branches |
| D-05 (early-return guard in handler) | 01 | SATISFIED | proposal-modal.js:971-974 |
| D-06 (PROP-ID stays in modal H2 — decision-record only) | 02 | SATISFIED | No code change; modal H2 at proposal-modal.js:450 renders `${idSafe} — ${titleSafe}` where idSafe = `proposal.proposal_id || proposal.id` (line 445); decision recorded in 87.4-02-SUMMARY.md |
| D-07 (PROP-ID surfacing on home queue deferred) | 02 | SATISFIED | Decision-record only; deferred per CONTEXT.md |
| D-08 (recolor Request Revision via inline style) | 02 | SATISFIED | proposal-modal.js:432 |
| D-09 (recolor scope = Request Revision only) | 02 | SATISFIED | No other button modified; audit dot constant (line 98) untouched; comms pill styling untouched |
| D-10 (red is exclusively for Mark as Loss + Reject Proposal) | 02 | SATISFIED | btn-danger count in renderProposalActionButtons = 2 (lines 416 + 422) |
| D-11 (delete dead Version cell entirely) | 02 | SATISFIED | proposal-modal.js:488-510 (no Version cell) |
| D-12 (adjust grid — Strategy A 3-column) | 02 | SATISFIED | proposal-modal.js:489 `repeat(3, 1fr)` |
| D-13 (preserve `'version'` in field-mask) | 01 | SATISFIED | firestore.rules:637 anchor substring `'version', 'attachment_kind'` present |
| D-14 (no data migration, no proposal.version read path) | 02 | SATISFIED | Grep `proposal\.version` in proposal-modal.js: 0 matches |
| D-15 (broad sweep, root-cause CSS fix) | 03 | SATISFIED | components.css:551-562 (9-class selector list) |
| D-16 (audit ALL AUDIT_ACTION_LABELS keys) | 03 | SATISFIED | Diagnosis confirms audit-action labels render as plain `<div>` with no background — no pill, no symptom. Documented in 87.4-03-SUMMARY.md "Audit-action label sweep" |
| D-17 (audit ALL COMMS_TYPE_META classes) | 03 | SATISFIED | 9-class selector list covers `.badge-primary` (sent), `.status-badge` + `.pending` (feedback_received), `.status-badge` + `.rejected` (revision_requested) |
| D-18 (visual smoke-test target) | 03 | NEEDS HUMAN | Deferred to Task 3 UAT |
| D-19 (fix in components.css at class level, not inline) | 03 | SATISFIED | Grep `fit-content\|inline-block` in proposal-modal.js: 0 matches |
| D-20 (no overflow:hidden / fixed-pixel width / max-width paper-overs) | 03 | SATISFIED | Phase 87.4 rule block contains only `display: inline-block;` and `width: fit-content;` |

## Anti-Patterns Found

None. All flagged-by-grep patterns (`max-width`, `overflow:hidden`, `btn-danger`) were either:
- Outside the Phase 87.4 rule block (pre-existing components.css declarations from earlier phases — not regressions)
- Inside `renderProposalActionButtons` but in the two correct semantic locations (Reject Proposal + Mark as Loss per D-10)
- Outside `renderProposalActionButtons` (attachment Remove confirm, Reject sub-modal confirm, Loss sub-modal confirm — all legitimate uses of destructive red)

The substring-scrub patterns documented in Plan 02 + Plan 03 SUMMARYs (avoiding `btn-danger`, `proposal.version`, `max-width` in comment narratives so verify gates measure code-only occurrences) are a tooling-discipline pattern, not a code-quality concern.

## Cross-Cutting Checks

### Decisions Referenced
- All 20 CONTEXT.md decisions (D-01 through D-20) are addressed in code, comments, or decision-record narrative. See Requirements Coverage table above.
- All 5 phase items addressed: Item 1 (Plan 01 — 3 layers), Item 2 (Plan 02 SUMMARY decision-record per D-06/D-07), Item 3 (Plan 02 — recolor), Item 4 (Plan 02 — Version removal + 3-col grid), Item 5 (Plan 03 — CSS fix).

### Memory Feedback Respected
- **`feedback_root_cause_first`:** RESPECTED — Plan 03's fix targets the cascade mechanism (`badge-primary` lacks `display`; `.status-badge` lacks `width: fit-content`) with class-level defensive rule. No `overflow:hidden`, no fixed widths, no `max-width` paper-overs. Code-inspection diagnosis explicitly labeled as such, with DevTools confirmation deferred to UAT — not skipped.
- **`feedback_orphan_ownership_parallel_plans`:** RESPECTED — Plan 01 owns proposal-modal.js lines 391-409 (Submit + Edit) + 955-975 (guard); Plan 02 owns lines 432 (Request Revision) + 477-510 (details block); Plan 03 owns components.css only. SUMMARY narratives + git diff confirm no cross-plan stomp on shared file regions.
- **`feedback_no_unauthorized_changes`:** RESPECTED — No scope creep observed. `firestore.rules` only modified by Plan 01 (attachment gate, no field-mask shrink); `proposal-modal.js` audit/comms-render paths NOT touched by Plan 03 (D-19 honored); STATE.md and ROADMAP.md NOT modified by executors per `commit_docs: false` directive.
- **`feedback_overnight_batch_execution`:** APPLIED — Plan 03 Task 1 explicitly performed as CODE-INSPECTION with DevTools confirmation deferred to punch list; Plans 01/02/03 all flagged Task 4/3/3 as PENDING-IN-BROWSER and emitted UAT punch lists.

### Regression Checks
- **Plan 01 regions ↔ Plan 02:** Plan 02 modified lines 432 + 489-510 only. Plan 01 regions (391-409 Submit/Edit; 971-974 guard) intact. Verified by reading the file directly.
- **Plan 02 regions ↔ Plan 03:** Plan 03 did NOT modify `proposal-modal.js`. Plan 02 regions intact. Verified by Grep `fit-content\|inline-block` in proposal-modal.js returning 0 matches (Plan 03 made no JS edits).
- **Existing pill rules in components.css:** Plan 03 inserted the defensive rule block ABOVE the existing `.status-badge` rule (which still starts at line 564) — pre-existing `.status-badge`, `.status-badge.pending`, `.status-badge.approved`, `.status-badge.rejected`, `.status-badge.procuring`, `.status-badge.delivered`, `.badge-secondary`, `.badge-primary`, `.badge-warning` all preserved unchanged (verified by reading lines 564-628). Cross-view regression (procurement records, home Approval Queue) requires browser UAT (Plan 03 Task 3 step 7).

### Commit Hashes Verified
All 10 commits claimed by SUMMARYs are present in `git log --oneline -15`:
- Plan 01: `3c31065`, `1965766`, `718de7a` (3 task commits) + `5f29878` (SUMMARY)
- Plan 02: `19ecf06`, `ad2676c` (2 task commits) + `47e9704` (SUMMARY)
- Plan 03: `d398c8e`, `48180a7` (2 task commits) + `bdadc0c` (final SUMMARY amendments)
- Phase 87.4 planning artifacts: `85c60a2`

## Human Verification Required

### 1. Plan 01 — Attachment-gate end-to-end UAT (9 steps)

**Test:** Per `87.4-01-PLAN.md` Task 4 `<how-to-verify>` (verbatim 9 steps):
1. Run `firebase deploy --only firestore:rules` from project root against clmc-procurement (pre-check: confirm any prior Phase 84.1 pending deploy is in production per `project_phase841_pending_followups`).
2. Hard-refresh app, log in as super_admin.
3. Create new proposal; confirm disabled Submit + exact hint `Add an attachment to submit for approval.` rendered.
4. Add attachment (link or file); confirm Submit re-enables within ~500ms.
5. Remove attachment; force-enable button via DevTools (`document.querySelector(...).disabled = false`); click; confirm red toast `Add an attachment before submitting for approval.` and status stays `draft`.
6. Re-add attachment; submit normally; confirm Firestore write succeeds and status → `pending_internal`.
7. (Optional) DevTools direct Firestore write transitioning draft → pending_internal with null attachment_kind; expect `PERMISSION_DENIED`.
8. (Optional) Same write with attachment_kind set; expect success.
9. Repeat steps on a `for_revision` proposal (both starting statuses per D-02).

**Expected:** All three layers (disabled button, handler guard, Firestore rule) trigger in the documented order.
**Why human:** Requires Firebase CLI deploy + interactive browser session + DevTools console writes against live Firestore.

### 2. Plan 02 — Request Revision button + grid layout UAT (8 steps)

**Test:** Per `87.4-02-PLAN.md` Task 3 `<how-to-verify>` (verbatim 8 steps):
1. Hard-refresh; open a `pending_client` proposal.
2. Confirm 4 buttons in the right rail: Mark Sent to Client (outline), Client Approved (green), Mark as Loss (red), Request Revision (orange `#f59e0b` with white text).
3. Inspect-Element Request Revision: inline style `background:#f59e0b;color:#fff;border-color:#f59e0b;width:100%;`; class is just `btn`.
4. Hover — visually consistent shape with siblings; only color differs.
5. Scan left-rail details block: NO "Version" label, NO "v1" value.
6. DevTools DOM search for "Version" / "v1" in modal subtree: 0 matches.
7. (Regression) `pending_internal` proposal: Reject Proposal stays red.
8. (Regression) `draft` proposal: Submit + Edit unchanged.

**Expected:** Visual recolor + grid restructure both render as intended; no regression on other action panels.
**Why human:** Visual rendering, DevTools inspect-element, hover state, and DOM-search verifications cannot be performed programmatically.

### 3. Plan 03 — Comms-pill sizing UAT (9 steps)

**Test:** Per `87.4-03-PLAN.md` Task 3 `<how-to-verify>` (verbatim 9 steps):
1. Hard-refresh (force CSS reload).
2. Open a proposal with diverse audit + comms history (PROP-2026-001 on CLMC-SPI-2026004 if sufficient, else walk a fresh one through CREATED → SUBMITTED → APPROVED → SENT_TO_CLIENT → REVISION_REQUESTED → resubmit → CLIENT_APPROVED, with all 3 comms types).
3. Audit trail (right rail): confirm NO row-width pill on any of the 9 AUDIT_ACTION_LABELS (expected: no pill at all — plain `<div>`).
4. Comms log: confirm `sent` (badge-primary, sky-blue) / `feedback_received` (status-badge pending, yellow) / `revision_requested` (status-badge rejected, red) all render at text-content width.
5. DevTools → Elements: click each pill `<span>`; verify computed `display: inline-block` and `width` matches text-content width.
6. Resize browser narrow + wide; pills stay content-sized.
7. (Regression) Procurement records view + home Approval Queue: existing pills unchanged.
8. Confirm Phase 87.4 comment in components.css references D-15/D-19 + 87.4-03-SUMMARY.md.
9. Spot-check Task 1 Diagnosis Findings section reads as concrete cascade reasoning (with code-inspection caveat noted).

**Expected:** All comms pills render at text-content width with `display: inline-block` and shrunk `width`; no regression in other views.
**Why human:** Plan 03 Task 1 diagnosis was explicitly code-inspection per overnight batch authorization; DevTools confirmation of the cascade reasoning is part of the punch list. Plus visual regression scan across views that use the same badge classes.

### 4. Plan 01 — Production rules deploy

**Test:** Run `firebase deploy --only firestore:rules` from project root; record operator + timestamp.
**Expected:** Deploy completes successfully; D-03/D-04 server-side gate is live.
**Why human:** External service action; per `project_phase841_pending_followups`, requires confirming any prior Phase 84.1 pending deploy is already in production before bundling this one.

## Gaps Summary

**No code-level gaps.** All 15 must-have truths across Plans 01/02/03 are COVERED with verifiable evidence in the codebase. The only outstanding items are:

1. **Browser UAT** for all 3 plans (deferred per overnight batch authorization)
2. **Production rules deploy** for Plan 01 (requires user action)
3. **DevTools confirmation** of Plan 03 Task 1's code-inspection cascade reasoning (rolled into Plan 03 UAT)

These are not failed truths — they are intentionally deferred verification steps documented in each plan's SUMMARY and PLAN punch list. The `human_needed` status surfaces them in `/gsd-progress` and `/gsd-audit-uat` until the user runs them.

---

*Verified: 2026-05-24*
*Verifier: Claude (gsd-verifier)*
