---
phase: 87.4-proposal-modal-polish-2-attachment-pill-button-color-id-version
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/proposal-modal.js
autonomous: false
requirements: []
tags:
  - proposals
  - ui-polish
  - button-color
  - dead-ui-removal
must_haves:
  truths:
    - "The Request Revision button in the `pending_client` action panel renders in orange `#f59e0b` (matching AUDIT_ACTION_DOT_COLORS.REVISION_REQUESTED) with white text"
    - "After this plan ships, only the Mark as Loss button uses the `btn-danger` red affordance — no other action button shares it"
    - "The dead `<div>v1</div>` Version field at lines 482-485 of buildProposalDetailsBlock is deleted entirely (both the label cell and the value div)"
    - "The buildProposalDetailsBlock grid renders without a visible hole where Version used to sit — the bottom row reads as an intentional layout, not a misalignment"
    - "No schema change, no migration, no new `version` field added to proposal docs"
  artifacts:
    - path: "app/proposal-modal.js"
      provides: "Recolored Request Revision button + removed Version dead UI + adjusted grid"
      contains: "#f59e0b"
  key_links:
    - from: "renderProposalActionButtons (line 411 region)"
      to: "REVISION_REQUESTED audit dot color"
      via: "color value #f59e0b applied via inline style"
      pattern: "#f59e0b"
    - from: "buildProposalDetailsBlock"
      to: "grid-template-columns adjustment"
      via: "Version cell removed, grid still renders cleanly"
      pattern: "grid-template-columns"
---

<objective>
Two small UI polish items on `app/proposal-modal.js`:
- **Item 3 (D-08, D-09, D-10)**: Recolor the Request Revision button from red (`btn-danger`) to orange (`#f59e0b`). Red becomes uniquely Mark as Loss.
- **Item 4 (D-11, D-12, D-13, D-14)**: Delete the dead hardcoded `<div>v1</div>` Version field in `buildProposalDetailsBlock` and adjust the grid layout so the freed cell does not leave a hole.

Purpose: Both items reduce visual noise / affordance ambiguity that was flagged in the super_admin walkthrough after Phase 87.3 shipped. Single file, single concern (small visual polish), no schema work.

Output: Updated `app/proposal-modal.js` only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/87.4-proposal-modal-polish-2-attachment-pill-button-color-id-version/87.4-CONTEXT.md
@app/proposal-modal.js

<interfaces>
<!-- Key code surfaces this plan modifies. Line numbers verified 2026-05-24. -->

From app/proposal-modal.js:

Line 411 — Request Revision button (current):
```js
buttons.push(`<button class="btn btn-danger" style="width:100%;" onclick="window.openRequestRevisionModal('${docId}')">Request Revision</button>`);
```

Line 98 — Color constant for visual consistency:
```js
AUDIT_ACTION_DOT_COLORS.REVISION_REQUESTED = '#f59e0b'
```

Lines 482-485 — Version cell (current):
```js
<div>
    <div class="modal-detail-label" style="font-size:0.75rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;color:#64748b;margin-bottom:4px;">Version</div>
    <div style="font-weight:600;color:#1e293b;">v1</div>
</div>
```

Line 461 — Grid declaration (current — DO NOT change the outer `grid-template-columns:1fr 1fr` unless the chosen layout strategy requires it):
```js
<div style="display:grid;grid-template-columns:1fr 1fr;gap:1rem;margin-bottom:1.5rem;">
```

Bottom row of the grid (lines 470-486, post-removal scope):
- Amount cell (lines 470-473)
- Target Client cell (lines 474-477)
- Project cell (lines 478-481)
- Version cell (lines 482-485) — DELETE THIS ENTIRELY

NOT TOUCHED by this plan (Plan 01 owns these):
- Submit button at line 393 (Plan 01 D-01/D-02)
- submitProposalForApproval guard at line 935 (Plan 01 D-05)
- firestore.rules (Plan 01 D-03/D-04)
</interfaces>

<parallel_safety_boundary>
<!-- Per memory feedback_orphan_ownership_parallel_plans — both this plan and Plan 01 edit renderProposalActionButtons. -->
- Plan 01 owns line 391-396 (the `if (status === 'draft' || status === 'for_revision')` block — Submit button + Edit Proposal button).
- Plan 02 (this plan) owns line 405-413 (the `else if (status === 'pending_client')` block — specifically line 411, the Request Revision button push).
- Plans 01 and 02 modify DIFFERENT, NON-OVERLAPPING line ranges inside the same function. They can run in parallel (Wave 1) but the executor of Plan 02 must NOT edit lines 391-396 and the executor of Plan 01 must NOT edit lines 405-413. Each executor should re-read the function after its sibling commits to confirm no merge conflict on outer braces / closing parens.
</parallel_safety_boundary>

<btn_warning_check>
<!-- Revision 1 (checker BLOCKER #1): Option A (.btn-warning class) is FORBIDDEN.
     Verified 2026-05-24: styles/main.css:16 defines `--warning: #fbbc04` (Google amber YELLOW, NOT the
     `#f59e0b` orange D-08 mandates). styles/components.css:217-220 resolves .btn-warning to
     `background: #fbbc04; color: var(--gray-900)` (yellow background + dark text). Both the color
     value AND the text color would violate D-08 + must_haves truth #1 ("white text"). DO NOT use
     `.btn-warning`. The inline-style approach below is the ONLY allowed implementation. -->
</btn_warning_check>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Recolor Request Revision button to orange (D-08, D-09, D-10)</name>
  <files>app/proposal-modal.js</files>
  <read_first>
    - .planning/phases/87.4-proposal-modal-polish-2-attachment-pill-button-color-id-version/87.4-CONTEXT.md (D-08, D-09, D-10)
    - app/proposal-modal.js renderProposalActionButtons (line 383-420) — confirm line 411 is the Request Revision push inside the `pending_client` branch
    - styles/main.css:16 (`--warning: #fbbc04` — confirms why .btn-warning cannot be used)
    - styles/components.css:217-224 (`.btn-warning` resolves to yellow + dark text — forbidden per revision 1)
  </read_first>
  <action>
    Modify ONLY line 411 of `app/proposal-modal.js`. This plan's owned range inside `renderProposalActionButtons` is lines 405-413 (the `else if (status === 'pending_client')` branch); do NOT touch any other line. In particular: do NOT modify line 393 (Submit button — owned by Plan 01), and do NOT modify line 403 (Reject Proposal in the `pending_internal` branch — that's a legitimate destructive action where red is appropriate per D-10's spirit; this plan's scope is the Request Revision button only).

    Per revision 1 (checker BLOCKER #1): the existing `.btn-warning` class in styles/components.css resolves to yellow `#fbbc04` with dark text, which violates D-08 (mandates `#f59e0b` orange + white text) AND must_haves truth #1. `.btn-warning` is FORBIDDEN. Write the inline-style version verbatim — no class-based alternative, no "or equivalent".

    Replace line 411 EXACTLY with the following line (whitespace + characters identical):

    ```js
    buttons.push(`<button class="btn" style="background:#f59e0b;color:#fff;border-color:#f59e0b;width:100%;" onclick="window.openRequestRevisionModal('${docId}')">Request Revision</button>`);
    ```

    Notes on the verbatim string:
    - `class="btn"` only (no `btn-danger`, no `btn-warning`)
    - `style` attribute contains, in order: `background:#f59e0b;color:#fff;border-color:#f59e0b;width:100%;`
    - The four CSS declarations must appear with these exact values and no extra whitespace inside the value strings
    - The onclick handler is unchanged
    - The button label is unchanged: `Request Revision`

    Per D-09: the recolor is scoped to this ONE button only. Do NOT modify:
    - The REVISION_REQUESTED audit dot (`AUDIT_ACTION_DOT_COLORS` constant at line 98) — it's already `#f59e0b`
    - The comms-log REVISION_REQUESTED pill styling (handled by Plan 03's broad CSS sweep)
    - Any other action button

    Per D-10 verification: after this edit, the rendered `pending_client` action panel should show four distinct affordances — Mark Sent to Client (outline), Client Approved (green), Mark as Loss (red), Request Revision (orange). Visual scan all branches of `renderProposalActionButtons` and confirm no other button uses `btn-danger` except line 403 (Reject Proposal — appropriate red for destructive admin action) and line 409 (Mark as Loss — appropriate red for terminal failure). If a future maintainer adds a third `btn-danger` button without justification, that's a regression — but enforcement is via code review, not this plan.
  </action>
  <behavior>
    - Test 1: The Request Revision push at line 411 no longer contains `btn-danger`
    - Test 2: The Request Revision push at line 411 contains the literal string `#f59e0b`
    - Test 3: The Request Revision push at line 411 does NOT contain `btn-warning`
    - Test 4: Line 409 (Mark as Loss) still uses `btn-danger`
    - Test 5: Line 403 (Reject Proposal) still uses `btn-danger`
    - Test 6: Exactly 2 `btn-danger` occurrences remain in `renderProposalActionButtons`
  </behavior>
  <verify>
    <automated>node -e "const{execSync}=require('child_process');const fs=require('fs');const s=fs.readFileSync('app/proposal-modal.js','utf8');const fn=s.slice(s.indexOf('function renderProposalActionButtons'),s.indexOf('function buildProposalDetailModalHtml'));const revLine=fn.split('\\n').find(l=>l.includes('openRequestRevisionModal'));if(!revLine){console.error('FAIL: Request Revision button line not found');process.exit(1);}if(/btn-danger/.test(revLine)){console.error('FAIL: Request Revision still uses btn-danger:',revLine.trim());process.exit(1);}if(/btn-warning/.test(revLine)){console.error('FAIL: btn-warning is FORBIDDEN per revision 1 (resolves to yellow #fbbc04, not orange #f59e0b):',revLine.trim());process.exit(1);}if(!revLine.includes('#f59e0b')){console.error('FAIL: literal #f59e0b not present on Request Revision line:',revLine.trim());process.exit(1);}if(!revLine.includes('color:#fff')){console.error('FAIL: white text declaration (color:#fff) missing on Request Revision line:',revLine.trim());process.exit(1);}const dangerCount=(fn.match(/btn-danger/g)||[]).length;if(dangerCount!==2){console.error('FAIL: expected exactly 2 btn-danger occurrences (Reject Proposal + Mark as Loss), got',dangerCount);process.exit(1);}console.log('OK — Request Revision recolored with inline #f59e0b + white text; btn-danger count:',dangerCount);"</automated>
  </verify>
  <done>
    - Request Revision button line no longer contains `btn-danger` or `btn-warning`
    - Request Revision button line contains the literal string `#f59e0b` AND `color:#fff` (white text per D-08)
    - Exactly 2 `btn-danger` occurrences remain in `renderProposalActionButtons` (Reject Proposal + Mark as Loss)
    - SUMMARY records that the inline-style approach was used (the only allowed implementation per revision 1)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete dead Version field + adjust grid layout (D-11, D-12, D-14)</name>
  <files>app/proposal-modal.js</files>
  <read_first>
    - .planning/phases/87.4-proposal-modal-polish-2-attachment-pill-button-color-id-version/87.4-CONTEXT.md (D-11, D-12, D-13, D-14 + "Claude's Discretion" line 72 on post-removal grid)
    - app/proposal-modal.js buildProposalDetailsBlock (lines 456-488) — the Version cell to delete is lines 482-485
  </read_first>
  <action>
    In `buildProposalDetailsBlock` (app/proposal-modal.js line 456-488), delete the entire Version grid cell — lines 482-485 inclusive (the outer `<div>` opening, the label div, the value div, and the closing `</div>`). After deletion, the function body's grid contains 5 cells: Title (span 2), Description (span 2), Amount (1), Target Client (1), Project (1).

    Per D-12, adjust the grid layout so the bottom row doesn't look like it has a hole. The 2-column grid (`grid-template-columns:1fr 1fr`, line 461) currently has the bottom row as `Amount | Target Client` and the next row as `Project | Version`. After removing Version, the layout becomes:
    - Row 1: Title (span 2)
    - Row 2: Description (span 2)
    - Row 3: Amount | Target Client
    - Row 4: Project | (empty)

    Pick ONE of the two layout strategies (Claude's discretion per CONTEXT.md line 72 — pick what reads cleanest):

    Strategy A — Three-column bottom row:
    Restructure the bottom row to `grid-template-columns:repeat(3, 1fr)` for just that row by wrapping Amount/Target Client/Project in a sub-grid OR by changing the outer grid to 3 columns and making Title/Description span 3. Cleanest if the three short-value cells (Amount / Target Client / Project) read symmetrically.

    Strategy B — Keep 2-column grid, accept that Project sits alone in its row:
    Simply delete the Version cell. Project will occupy column 1 of its row with an empty column 2. If this reads as an intentional left-aligned terminal cell (especially given Project is the longest of the three short values), it can look fine. Visual verify required.

    Strategy C — Promote Project to span across both columns (NOT preferred; loses visual parity with Amount and Target Client, but lists it):
    Add `style="grid-column:1/-1;"` to the Project cell wrapper so it spans the full width like Title and Description.

    Recommend Strategy A for symmetric layout. Document the chosen strategy in SUMMARY.

    Per D-13 (negative assertion enforced by Plan 01): do NOT touch `firestore.rules` — the `'version'` key STAYS in the field-mask. This task is purely a UI change in `app/proposal-modal.js`.

    Per D-14: no data migration. Do NOT add any cleanup script that strips `version` fields from existing Firestore docs. Read paths simply ignore the field.
  </action>
  <behavior>
    - Test 1: The substring `>Version<` does NOT appear in `buildProposalDetailsBlock` after the edit
    - Test 2: The substring `>v1<` does NOT appear in `buildProposalDetailsBlock` after the edit
    - Test 3: The function still returns a valid template string (`<div style="display:grid;...>`)
    - Test 4: The Title, Description, Amount, Target Client, and Project cells are all preserved
    - Test 5: If Strategy A chosen, the bottom row uses `repeat(3, 1fr)` or equivalent three-column layout
    - Test 6: No reference to `'version'` or `proposal.version` was introduced in this plan (D-14)
  </behavior>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app/proposal-modal.js','utf8');const fn=s.slice(s.indexOf('function buildProposalDetailsBlock'),s.indexOf('function buildAttachmentSection'));if(/>Version</.test(fn)){console.error('FAIL: Version label still present');process.exit(1);}if(/>v1</.test(fn)){console.error('FAIL: v1 literal still present');process.exit(1);}if(!/Title/.test(fn)||!/Description/.test(fn)||!/Amount/.test(fn)||!/Target Client/.test(fn)||!/Project/.test(fn)){console.error('FAIL: a required cell was removed');process.exit(1);}if(/proposal\\.version/.test(fn)){console.error('FAIL: D-14 violation — proposal.version referenced');process.exit(1);}console.log('OK');"</automated>
  </verify>
  <done>
    - Version cell deleted from `buildProposalDetailsBlock`
    - Grid layout adjusted (Strategy A, B, or C — documented in SUMMARY)
    - No `proposal.version` reference introduced
    - All other cells (Title, Description, Amount, Target Client, Project) preserved
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visually verify button recolor + grid layout</name>
  <what-built>
    - Request Revision button is now orange `#f59e0b` with white text (was red `btn-danger`)
    - The dead "v1" Version field is gone; the grid renders cleanly without a visible hole
  </what-built>
  <how-to-verify>
    1. Hard-refresh the dev server or deployed app. Open a proposal currently in `pending_client` status (use the home Approval Queue or any `pending_client` card; if none exists, walk a draft proposal through Submit → Approve → Mark Sent to Client first).
    2. Confirm the right-rail action panel shows four buttons in this exact visual order: Mark Sent to Client (outline/border-only), Client Approved (green), Mark as Loss (red), Request Revision (orange `#f59e0b` with WHITE text).
    3. Inspect-Element the Request Revision button. Expected: inline style with `background:#f59e0b;color:#fff;border-color:#f59e0b;width:100%;`. The class attribute should be just `btn` (NOT `btn-warning`, NOT `btn-danger`).
    4. Hover the Request Revision button. Expected: visually consistent shape (radius, padding, font-weight) with Mark as Loss / Client Approved — only the color differs. Text remains readable in white on orange.
    5. Scan the left-rail details block (Title / Description / Amount / Target Client / Project). Expected: NO "Version" label, NO "v1" value. The layout should look intentional — not like a missing cell.
    6. Open DevTools and search the modal DOM for the strings "Version" and "v1". Expected: zero matches inside the proposal detail modal subtree (the existing status badge or comms-log entries are unaffected).
    7. (Regression) Open a proposal in `pending_internal` status (or use Submit on a fresh draft to reach this state). Confirm the action panel shows Approve Proposal (green) + Reject Proposal (still red — this is intentional per D-10's spirit; only Request Revision is recolored, Reject keeps `btn-danger`).
    8. (Regression) Open a proposal in `draft` status. Confirm Submit + Edit render normally (Plan 01's disabled-state behavior is verified separately by Plan 01's checkpoint).

    If the grid layout looks like it has a hole, paste a screenshot and we'll pick a different strategy.
  </how-to-verify>
  <resume-signal>Type "approved" if both items pass, or describe issues observed.</resume-signal>
</task>

</tasks>

<verification>
- Grep assertions in tasks 1-2 confirm the code-level invariants (Task 1 enforces literal `#f59e0b` + `color:#fff` + absence of `btn-danger`/`btn-warning`)
- Manual UAT (Task 3 checkpoint) confirms visual rendering — white text on orange background
- Negative assertion (cross-plan): Plan 01's anchor check verifies that `'version'` remains in `firestore.rules` field-mask (D-13) — this plan does not touch firestore.rules
</verification>

<success_criteria>
- Request Revision button renders in `#f59e0b` orange with white text via verbatim inline style (`background:#f59e0b;color:#fff;border-color:#f59e0b;width:100%;`)
- `.btn-warning` is NOT used (forbidden per revision 1 — resolves to yellow #fbbc04 with dark text, violating D-08)
- Only Mark as Loss + Reject Proposal use `btn-danger` (2 occurrences in `renderProposalActionButtons`)
- Version cell + literal `v1` are absent from `buildProposalDetailsBlock`
- Grid layout reads as intentional (Strategy A / B / C documented in SUMMARY)
- `firestore.rules` is unchanged by this plan (D-13 preserved by Plan 01's separate anchor check)
- No `proposal.version` read path introduced (D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/87.4-proposal-modal-polish-2-attachment-pill-button-color-id-version/87.4-02-SUMMARY.md`. Record:
- Confirmation that the inline-style approach was used (the only allowed implementation per revision 1 — Option A/`.btn-warning` was forbidden because `--warning: #fbbc04` resolves to yellow, not orange)
- Which grid-adjustment strategy was chosen (A / B / C) and why
- Note that Item 2 (PROP-ID header surface, D-06/D-07) is a decision-record only — no code change was made for it. Reference D-06 in the SUMMARY for the architectural-decision record.
- Confirm `firestore.rules` was NOT touched by this plan (Plan 01 owns rules changes)
- UAT observations from Task 3
</output>
</output>
