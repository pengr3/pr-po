# Phase 87.4: Proposal Modal Polish Pass 2 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-23
**Phase:** 87.4-proposal-modal-polish-2-attachment-pill-button-color-id-version
**Areas discussed:** Version field, PROP-ID header, Attachment-required block, Audit pill sweep

---

## Round 1 — Gray area selection

User selected all four gray areas for discussion via multi-select. No areas were skipped or deferred at this stage.

| Area | Selected |
|------|----------|
| Version field — remove vs wire up | ✓ |
| PROP-ID header surface — keep vs remove | ✓ |
| Attachment-required block — UX shape | ✓ |
| Audit pill sweep — scope of the fix | ✓ |

Note: Request Revision button color (Item 3) was NOT presented as a gray area because the roadmap already locked the color (`#f59e0b`). Implementation form was filed as Claude's discretion.

---

## Version field — remove vs wire up

| Option | Description | Selected |
|--------|-------------|----------|
| Remove entirely (Recommended) | Delete the `<div>v1</div>` at `proposal-modal.js:484`. Adjust grid so Target Client and Project span the freed column. No schema change, no write logic. Aligns with project memory: CLMC is not an authoring/version-history platform. | ✓ |
| Wire up real versioning | Add a real version field, bump on Revision Requested → Mark Sent cycles, render the actual number. Pushes 87.4 from polish into a feature phase. | |

**User's choice:** Remove entirely
**Notes:** Confirmed; aligns with memory `project_clmc_not_authoring_platform`. Field-mask slot in `firestore.rules` preserved for a future real-versioning phase (D-13) to avoid a rules redeploy if that ever happens.

---

## PROP-ID header surface — keep vs remove

| Option | Description | Selected |
|--------|-------------|----------|
| Keep (Recommended) | PROP-YYYY-NNN stays in the modal H2. Zero code change. | |
| Remove from header | Strip from H2; generator + audit + NOTIF + queue link still use the field internally. | |
| Investigate PROP-ID rendering first | Pause and confirm whether PROP-IDs are actually rendering for current proposals. | ✓ (Round 2) |

**User's choice (initial):** "I think we don't really generate Proposal code" — i.e., user believed the generator was not wired up.

**Investigation outcome:** Verified live code:
- `app/proposal-id.js:16` exports `generateProposalId()` returning `PROP-YYYY-NNN`
- `app/proposal-modal.js:887` calls it on every CREATE
- `app/proposal-modal.js:897` writes `proposal_id` to the doc payload
- `app/proposal-modal.js:429` renders it in the modal H2: `${proposal.proposal_id || proposal.id}`
- `app/views/home.js:235` (Approval Queue table) renders ONLY `p.title` — no PROP-ID surface there

Generator is correctly wired; the user's perception came from the queue/stage-card surfaces, which don't display PROP-IDs.

---

## PROP-ID header surface (Round 2 — post-investigation)

| Option | Description | Selected |
|--------|-------------|----------|
| Keep header surface as-is (Recommended) | Zero code change. Item 2 collapses to a decision record. | ✓ |
| Remove from modal header | Strip from H2; ID still used internally elsewhere. | |
| Keep + add to queue / stage cards | Additive scope — surface PROP-YYYY-NNN on home queue and stage cards. | |

**User's choice:** Keep header surface as-is
**Notes:** Surfacing PROP-ID on home queue / stage cards was explicitly noted as deferred (D-07). If wanted later, file as a separate small phase.

---

## Attachment-required block — UX shape

| Option | Description | Selected |
|--------|-------------|----------|
| Disable button + inline hint (Recommended) | Submit button disabled when `attachment_kind` is unset; helper hint renders nearby. Plus Firestore rule. | ✓ |
| Keep button enabled, toast on click | Reactive UX — user learns the rule by hitting it. Plus Firestore rule. | |
| Both — disable AND toast as backstop | Belt-and-suspenders; slight code redundancy. Plus Firestore rule. | |

**User's choice:** Disable button + inline hint
**Notes:** Pure-prevention UX. Belt-and-suspenders still present at two levels — early-return guard in `submitProposalForApproval` (D-05) + Firestore rule (D-03/D-04). Block applies to both `draft → pending_internal` and `for_revision → pending_internal` transitions (D-02).

---

## Audit pill sweep — scope of the fix

| Option | Description | Selected |
|--------|-------------|----------|
| Broad sweep (Recommended) | Researcher pinpoints CSS root cause; defensive rule applied to underlying class; visual smoke-test across all action types. | ✓ |
| Narrow fix — only REVISION REQUESTED | Targeted fix on the one pill; leaves others as-is. | |

**User's choice:** Broad sweep
**Notes:** Per `feedback_root_cause_first` — no papering over with `overflow:hidden` / fixed widths (D-20). Fix lives in `styles/components.css` if a class-level rule resolves it (D-19). Researcher must FIRST diagnose which element actually has the row-width background — the audit-action label currently renders as a plain `<div>` with no class, while comms-child pills use `COMMS_TYPE_META.cls` — only the latter is a plausible pill candidate.

---

## Claude's Discretion

- Exact disabled-button styling (opacity, cursor) for D-01 — defaults to `opacity:0.5;cursor:not-allowed;` if no global `.btn:disabled` rule exists in `components.css`
- Exact inline hint copy and placement for D-01 — guide: `"Add an attachment to submit for approval."`
- Exact post-removal grid layout for D-12 — three-cell bottom row or two-column reorganization, whichever reads cleanest
- Exact form of Request Revision orange (D-08) — inline style vs reusing an existing `btn-warning` class if one exists; do NOT introduce a single-use class
- Per-pill verification methodology for D-18 — manual DevTools scan acceptable; automated test not required
- Final CSS-rule shape for D-15/D-19 — `width: fit-content`, `display: inline-block`, or both, depending on diagnosis

## Deferred Ideas

- **Surface PROP-YYYY-NNN on home Approval Queue table and stage cards** — additive scope, filed as a future small phase
- **Real proposal versioning** — explicitly rejected for 87.4; if needed later, open a v4.1+ phase with full schema work, write logic, and history browser. Field-mask slot preserved in `firestore.rules` (D-13).
- **Global `.btn:disabled` styling rule in `components.css`** — small polish benefiting future disabled buttons; filed as v4.1 housekeeping
- **Field-mask cleanup (remove `'version'` from proposals update rule field-mask)** — only worth doing if "real versioning" is rejected long-term
- **Audit-trail visual redesign** — out of scope; Item 5 is a bug fix, not a redesign
