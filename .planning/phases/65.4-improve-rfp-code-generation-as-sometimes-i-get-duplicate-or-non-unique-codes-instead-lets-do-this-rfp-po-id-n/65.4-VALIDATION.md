---
phase: 65.4
slug: improve-rfp-code-generation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-17
---

# Phase 65.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Manual browser testing (zero-build SPA — no automated test framework) |
| **Config file** | None |
| **Quick run command** | `python -m http.server 8000` then navigate to `#/procurement/records` |
| **Full suite command** | Same — manual walkthrough of all 3 success criteria |
| **Estimated runtime** | ~2 minutes |

---

## Sampling Rate

- **After every task commit:** Smoke test: submit an RFP on any PO, check the generated RFP ID format
- **After every plan wave:** Full 3-criteria walkthrough against RFPID-01
- **Before `/gsd:verify-work`:** All 3 success criteria must pass
- **Max feedback latency:** ~2 minutes (manual)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 65.4-01-01 | 01 | 1 | RFPID-01 | manual | n/a | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

None — no test infrastructure needed; project uses manual testing exclusively.

*Existing manual testing process covers all phase requirements.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| New RFPs get PO-scoped IDs in format RFP-{PO-ID}-{n} | RFPID-01 | Zero-build SPA | Right-click a PO ID in `#/procurement/records` MRF Records → submit a new RFP. The generated RFP ID in the Finance Payables table should be `RFP-PO-2026-XXX-1` (or `RFP-PO-2026-XXX-2` if one already exists for that PO) |
| Sequential counter scoped per PO, not per project | RFPID-01 | Zero-build SPA | Submit two RFPs on the same PO — IDs should be `RFP-{PO-ID}-1` and `RFP-{PO-ID}-2`; submit one on a different PO with same project code — counter starts fresh at `-1` |
| Existing RFP-CLMC-### IDs still display correctly | RFPID-01 | Zero-build SPA | Navigate to `#/finance/payables` — any existing `RFP-CLMC-###` IDs display normally in both tables without errors |

---

## Validation Sign-Off

- [ ] All tasks have manual verify instructions
- [ ] Sampling continuity: smoke test after every commit
- [ ] Wave 0: N/A
- [ ] No watch-mode flags
- [ ] Feedback latency < 2 minutes
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
