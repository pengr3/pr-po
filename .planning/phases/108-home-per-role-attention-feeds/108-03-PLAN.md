---
phase: 108-home-per-role-attention-feeds
plan: 03
type: execute
wave: 3
depends_on: ["108-01", "108-02"]
files_modified:
  - app/home-feed-sources.js
autonomous: true
requirements: [HOME-09, HOME-10, HOME-11]
must_haves:
  truths:
    - "The cross-department portfolio & admin sources exist: pending user registrations, overdue projects, overdue services, DLP windows expiring, stale progress, own billing requests, open issues"
    - "Every assignment-scoped source self-gates via getAssignedProjectCodes()/getAssignedServiceCodes() (null=see-all) — no ad-hoc role checks — so the same function yields super_admin=all, dept-admin=dept, user=assigned"
    - "The open-issues source uses a BOUNDED subcollection fan-out (inner Promise.all over scoped parent doc-ids, chunked 'in') — no collectionGroup, no onSnapshot"
    - "DLP/engagement severity is RECOMPUTED against the D-01 THRESHOLDS (not signal.level) using status-derivation primitives"
  artifacts:
    - path: "app/home-feed-sources.js"
      provides: "+7 portfolio/admin source functions appended to Plan 02's file"
      contains: "sourceOpenIssues"
  key_links:
    - from: "app/home-feed-sources.js"
      to: "app/utils.js"
      via: "import getAssignedProjectCodes/getAssignedServiceCodes for self-scoping"
      pattern: "import.*getAssignedProjectCodes.*getAssignedServiceCodes.*from './utils.js'"
    - from: "sourceOverdueProjects / sourceOverdueServices"
      to: "projects / services"
      via: "where('project_status','in',[funnel]) scoped query, client stage-band"
      pattern: "where\\('project_status', ?'in'"
    - from: "sourceOpenIssues"
      to: "<parent>/{id}/issues"
      via: "bounded inner Promise.all of getDocs(issues where status=='open')"
      pattern: "where\\('status', ?'==', ?'open'\\)"
---

<objective>
Append the **7 cross-department portfolio & admin feed sources** (HOME-09, HOME-10, HOME-11) to `app/home-feed-sources.js` created in Plan 02. These are the assignment-aware sources: they self-scope via the existing `getAssignedProjectCodes()`/`getAssignedServiceCodes()` predicates (D-04, RESEARCH § scoping) so a SINGLE function serves super_admin (sees all), dept admins (their dept), and users (assigned only) — the registry (Plan 04) decides which roles get which source; the source decides which DATA to show.

Key correctness rule (RESEARCH gotcha 5): the portfolio urgency helper uses its own bands, but the FEED must apply the **D-01 `THRESHOLDS`** — so these sources recompute severity from status-derivation primitives (`stageDaysInStage`, `normalizeUpdatedAt`, `getDlpState`, dlp-expiry math), NOT from `getEngagementSignal(...).level`.

The heaviest source, **#6 open issues**, uses a **bounded per-assignment/dept subcollection fan-out** (resolved decision #3): resolve scoped parent doc-ids, then an inner `Promise.all` of `getDocs(<parent>/{id}/issues where status=='open')`. No `collectionGroup`, no `open_issue_count` denormalization — those are recorded as the out-of-scope scale path.

This plan runs in Wave 3 (sequential after Plan 02 — same file, no parallel conflict). At the end the file exports 17 sources but still has no registry; Plan 04 wires everything.

Purpose: populate the super_admin, dept-admin, and user role feeds.
Output: `app/home-feed-sources.js` grows from 10 → 17 source functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md
@.planning/phases/108-home-per-role-attention-feeds/108-02-SUMMARY.md
@app/home-feed-sources.js
@app/status-derivation.js

<interfaces>
<!-- Scoping predicates (utils.js, exported). Both read window.getCurrentUser() internally, no arg. -->
getAssignedProjectCodes() → string[] of allowed project_codes, or null = "no filter / see all" (super_admin, finance, procurement, operations_admin, or all_projects flag). [] = scoped user with zero assignments (sees nothing). (utils.js:333)
getAssignedServiceCodes() → same shape for service_codes; null for super_admin/finance/procurement/services_admin. (utils.js:413)

<!-- Firestore 'in' supports up to 10 values per clause — chunk larger code lists. -->

Project/service status vocabulary (RESEARCH § Field & Status Reference):
funnel (stage-clock) statuses: 'For Proposal','Proposal for Internal Approval','Proposal Under Client Review','For Revision','Client Approved','For Mobilization','For Inspection'  (7 values — one `in` clause)
activity-clock status: 'On-going'   |   terminal: 'Completed','Loss'
Fields: status_changed_at (stage clock), last_activity_at (On-going clock), dlp_expires_at (YYYY-MM-DD), dlp_months, retention_released_at, project_code/service_code, project_name.

Issue subcollection (project-detail.js:3541): projects/{id}/issues and services/{id}/issues, fields: status ('open'|'resolved'), title, issue_type, created_at, resolved_at. NO project_code/department on the issue doc (gotcha 3) — parent identity comes from the parent doc-id you fanned out from.

THRESHOLDS (defined in home-feed-sources.js by Plan 02): STAGE_CRITICAL_D:14, STAGE_HIGH_D:7, DLP_CRIT_D:3, DLP_HIGH_D:7, DLP_MED_D:14, PROGRESS_CRIT_D:30, PROGRESS_HIGH_D:14.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Cheap admin sources — #4 pending user registrations + #10 own billing requests</name>
  <read_first>
    - app/views/user-management.js lines 245-256 (users where status=='pending', LOWERCASE, orderBy created_at desc)
    - app/views/project-detail.js lines 1457-1472 (billing_requests schema: requested_by_uid, requested_by_name, department, project_code/service_code, amount_requested, tranche_label)
    - app/home-feed.js lines 261-295 (seed source template — approver-gate pattern for role-gated sources)
    - .planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md § "Per-Source Spec Table" rows #4, #10; § "Landmines" gotcha 2 (lowercase status) and gotcha 10 (#/admin deep-link)
  </read_first>
  <action>
    Add the utils scoping import to the file header (extend Plan 02's imports):
    `import { getAssignedProjectCodes, getAssignedServiceCodes } from './utils.js';`
    (getRFPTotal is already imported from utils by Plan 02 — merge into one import line.)

    **#4 sourcePendingUserRegistrations** (HOME-09 super_admin) — RESEARCH row #4:
    - Approver gate (mirror seed #1): `if (user?.role !== 'super_admin') return [];`.
    - Query: `getDocs(query(collection(db,'users'), where('status','==','pending')))` — LOWERCASE 'pending' (gotcha 2, user-management.js:248).
    - Severity: fixed `SEVERITY.high` (action state).
    - Item: dedupeKey `user-pending:{id}`, icon `TYPE_META.REGISTRATION_PENDING.icon`, category `'issue'`, title `'New user awaiting approval'`, subtitle from `u.full_name || u.email || '—'` + `(u.requested_role||u.role||'')`, deepLink `{kind:'route', value:'#/admin?section=user-management'}` (degrades to #/admin if the query param is ignored — A2), timestamp `u.created_at`, overdueScore 0.

    **#10 sourceOwnBillingRequests** (HOME-10 ops/services admin) — RESEARCH row #10. Own submissions being tracked:
    - Gate: `if (!user?.uid) return [];`.
    - Query: `getDocs(query(collection(db,'billing_requests'), where('requested_by_uid','==', user.uid), where('status','==','pending')))` — LOWERCASE 'pending'.
    - Severity: fixed `SEVERITY.medium` (tracking, not overdue — D-01).
    - Item: dedupeKey `billreq-own:{id}`, icon `TYPE_META.BILLING_REQUEST_SUBMITTED.icon`, category `'finance'`, title `'Your billing request is pending'`, subtitle from `br.project_code||br.service_code` + `br.tranche_label` + amount, deepLink `{kind:'route', value:'#/finance/collectibles'}`, timestamp `br.created_at || br.requested_at`, overdueScore 0.
  </action>
  <acceptance_criteria>
    - `grep -n "user?.role !== 'super_admin'" app/home-feed-sources.js` matches (#4 approver gate).
    - `grep -c "where('status', '==', 'pending')" app/home-feed-sources.js` counts LOWERCASE usages including #4 and #10 (plus billing-to-decide from Plan 02).
    - `grep -n "where('requested_by_uid', '==', user.uid)" app/home-feed-sources.js` matches (#10 ownership scope).
    - `grep -nE "user-pending:|billreq-own:" app/home-feed-sources.js` returns 2 distinct dedupeKey prefixes.
    - `grep -n "getAssignedProjectCodes, getAssignedServiceCodes" app/home-feed-sources.js` shows the scoping import is present (used by Task 2/3).
    - #10 severity is `SEVERITY.medium` (grep the source body).
  </acceptance_criteria>
  <verify>As super_admin, `await sourcePendingUserRegistrations(getCurrentUser())` returns pending users; as any other role returns []. As an admin, `await sourceOwnBillingRequests(getCurrentUser())` returns only own pending billing requests.</verify>
  <done>#4 and #10 exist with correct lowercase status, gates, and item shapes; scoping import added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Engagement/DLP sources — #5a overdue projects, #5b overdue services, #11 stale progress, #7 DLP windows expiring</name>
  <read_first>
    - app/status-derivation.js (stageDaysInStage, normalizeUpdatedAt, getDlpState, getEngagementSignal, URGENCY_THRESHOLDS)
    - app/views/projects.js lines 940-1034 (getProjectSignal reference — confirm the extracted copy behaves the same)
    - app/utils.js lines 318-422 (getAssignedProjectCodes/getAssignedServiceCodes semantics: null=see-all, []=sees-nothing)
    - .planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md § "Per-Source Spec Table" rows #5a, #5b, #7, #11; § "Landmines" gotcha 5 (recompute band, don't use signal.level), gotcha 6 (getDlpState needs Completed pre-filter)
  </read_first>
  <action>
    Add these 4 sources. Define a small local helper `scopeByCodes(docs, codes, codeField)` that returns all docs when `codes === null`, else filters `docs` to those whose `codeField` ∈ `codes` (and returns [] when codes is []). Reuse it across 5a/5b/11.

    **#5a sourceOverdueProjects** (HOME-09/10/11) — RESEARCH row #5a. Funnel-stage projects overdue-in-stage, self-scoped to projects:
    - `const codes = getAssignedProjectCodes();` (null=see all; []=none → early `return [];`).
    - Query: `getDocs(query(collection(db,'projects'), where('project_status','in', ['For Proposal','Proposal for Internal Approval','Proposal Under Client Review','For Revision','Client Approved','For Mobilization','For Inspection'])))` (7 funnel statuses; excludes On-going/Completed/Loss).
    - Client-scope with `scopeByCodes(rows, codes, 'project_code')`, then for each: `const d = stageDaysInStage(p, Date.now()); if (d == null || d <= THRESHOLDS.STAGE_HIGH_D) return;` (only surface overdue >7d).
    - Severity: `d > THRESHOLDS.STAGE_CRITICAL_D` → critical; else → high.
    - Item: dedupeKey `project-overdue:{id}`, icon `TYPE_META.PROJECT_STATUS_CHANGED.icon`, category `'project'`, title `(p.project_name||p.project_code||'Project')`, subtitle `p.project_status + ' · ' + Math.round(d) + 'd in stage'` (optionally append getEngagementSignal(p,Date.now()).text), deepLink `{kind:'route', value:'#/projects'}`, timestamp `p.status_changed_at || p.updated_at`, overdueScore `d`.

    **#5b sourceOverdueServices** (HOME-09/10/11) — RESEARCH row #5b. Mirror of #5a on the services collection:
    - `const codes = getAssignedServiceCodes();` → early return if [].
    - Query: `collection(db,'services')` with the SAME 7-status `where('project_status','in',[…])`.
    - `scopeByCodes(rows, codes, 'service_code')`; same stage-band; dedupeKey `service-overdue:{id}`; category `'service'`; deepLink `{kind:'route', value:'#/services'}`; else identical to #5a.

    **#11 sourceStaleProgress** (HOME-10/11) — RESEARCH row #11. On-going engagements gone quiet (progress bands 14/30), spanning BOTH collections, each self-scoped:
    - Resolve `const pCodes = getAssignedProjectCodes(); const sCodes = getAssignedServiceCodes();`.
    - Query both in an inner Promise.all: projects `where('project_status','==','On-going')`, services `where('project_status','==','On-going')`.
    - scopeByCodes each (projects by project_code/pCodes, services by service_code/sCodes).
    - For each: `const ms = normalizeUpdatedAt(e.last_activity_at) ?? normalizeUpdatedAt(e.updated_at); if (ms == null) return; const d = (Date.now()-ms)/86400000; if (d < THRESHOLDS.PROGRESS_HIGH_D) return;` (surface ≥14d).
    - Severity: `d >= THRESHOLDS.PROGRESS_CRIT_D` → critical; else → high.
    - Item: dedupeKey `stale-progress:{id}` (DISTINCT from project-overdue/service-overdue — gotcha 13, different concern), icon `TYPE_META.PROJECT_STATUS_CHANGED.icon`, category `e is a service ? 'service' : 'project'`, title `(e.project_name||e.project_code||e.service_code||'Engagement')`, subtitle `'No progress in ' + Math.round(d) + 'd'`, deepLink route `#/projects` or `#/services` per collection, timestamp `e.last_activity_at || e.updated_at`, overdueScore `d`.

    **#7 sourceDlpWindowsExpiring** (HOME-09/10) — RESEARCH row #7. Completed engagements whose DLP retention window closes within 14d, spanning BOTH collections, self-scoped:
    - Resolve pCodes/sCodes; query both collections `where('project_status','==','Completed')` (inner Promise.all); scopeByCodes each (gotcha 6 — Completed pre-filter is required for getDlpState).
    - For each: `if (getDlpState(e) !== 'in-dlp') return; const exp = new Date(e.dlp_expires_at).getTime(); const days = (exp - Date.now())/86400000; if (days < 0 || days > THRESHOLDS.DLP_MED_D) return;` (surface 0..14d to expiry).
    - Severity: `days <= THRESHOLDS.DLP_CRIT_D` → critical; else if `days <= THRESHOLDS.DLP_HIGH_D` → high; else → medium.
    - Item: dedupeKey `dlp-expiring:{id}` (DISTINCT from retention-release: — gotcha 13), icon `TYPE_META.PROJECT_COST_CHANGED.icon`, category `'dlp'`, title `'DLP window closing — ' + (e.project_code||e.service_code||'')`, subtitle `Math.ceil(days) + 'd until retention release'`, deepLink route `#/projects` or `#/services`, timestamp `e.dlp_expires_at`, overdueScore `THRESHOLDS.DLP_MED_D - days` (nearer expiry sorts higher).
  </action>
  <acceptance_criteria>
    - `grep -c 'export async function source' app/home-feed-sources.js` returns 16 after this task (running count: Plan 02 left 10 → Task 1 added #4/#10 = 12 → this task adds #5a/#5b/#11/#7 = 16). The local `scopeByCodes` helper is NOT an `export async function source`, so it does not inflate the count.
    - `grep -c "where('project_status', 'in'" app/home-feed-sources.js` returns 2 (#5a + #5b funnel queries).
    - `grep -c "where('project_status', '==', 'On-going')" app/home-feed-sources.js` returns 2 (#11 projects + services).
    - `grep -c "where('project_status', '==', 'Completed')" app/home-feed-sources.js` returns 4 total in the file (2 from Plan 02 #8 retention + 2 from #7 DLP).
    - `grep -n "getDlpState(e) !== 'in-dlp'" app/home-feed-sources.js` matches (#7); `grep -nE "THRESHOLDS.STAGE_CRITICAL_D|THRESHOLDS.PROGRESS_CRIT_D|THRESHOLDS.DLP_CRIT_D" app/home-feed-sources.js` shows the D-01 bands drive severity (gotcha 5 — not signal.level).
    - `grep -nE "project-overdue:|service-overdue:|stale-progress:|dlp-expiring:" app/home-feed-sources.js` returns 4 DISTINCT dedupeKey prefixes.
    - `grep -n 'getAssignedProjectCodes()' app/home-feed-sources.js` and `grep -n 'getAssignedServiceCodes()' app/home-feed-sources.js` both match (self-scoping, no ad-hoc role checks).
  </acceptance_criteria>
  <verify>As operations_user, sourceOverdueServices returns [] (no service assignment) while sourceOverdueProjects returns only assigned projects; as super_admin both return cross-dept rows; severities recomputed from THRESHOLDS.</verify>
  <done>#5a, #5b, #11, #7 exist; each self-scopes via getAssigned*Codes(); severity recomputed against D-01 THRESHOLDS; distinct dedupeKey prefixes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: #6 open issues — bounded per-assignment/dept subcollection fan-out</name>
  <read_first>
    - app/views/project-detail.js lines 3541-3552 (issue schema: status 'open'|'resolved', title, issue_type, created_at — NO parent identity on the issue doc)
    - app/utils.js lines 318-422 (getAssignedProjectCodes/getAssignedServiceCodes: null=see-all, []=none)
    - app/home-feed.js lines 202-232 (assembleFeed's Promise.all — each source runs concurrently; keep this source's own work bounded, gotcha 11)
    - .planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md § "Costly Sources" row #6 (the full decision) and § resolved-decision open-issues scale strategy; § "Landmines" gotcha 3 (no collectionGroup filtering) + gotcha 11 (inner Promise.all, bounded)
  </read_first>
  <action>
    Add **#6 sourceOpenIssues** (HOME-10/11) as a bounded subcollection fan-out (resolved decision #3 — ship (a) bounded parallel fan-out; do NOT implement collectionGroup or open_issue_count this phase).

    Algorithm:
    1. Resolve scoped PARENT doc-ids for each collection:
       - Projects: `const pCodes = getAssignedProjectCodes();`
         - If `pCodes === null` (see-all admin/super): `getDocs(collection(db,'projects'))` to enumerate the dept's project doc-ids (bounded by dept size — accepted per decision #3; projects are a near-static reference collection, F-025).
         - Else if `pCodes.length === 0`: no project ids.
         - Else: chunk `pCodes` into groups of 10 and `getDocs(query(collection(db,'projects'), where('project_code','in', chunk)))` per chunk; collect `{ id, name: p.project_name||p.project_code, kind:'project' }`.
       - Services: same with `getAssignedServiceCodes()` and `where('service_code','in', chunk)`.
    2. Build the list of parent descriptors. To keep worst-case bounded, cap the parent set at a documented maximum (e.g. `parents.slice(0, 60)`) and note the cap in a comment + SUMMARY.
    3. Fan out issue reads with an INNER Promise.all — one read per parent:
       `await Promise.all(parents.map(async parent => { const snap = await getDocs(query(collection(db, parent.kind === 'service' ? 'services' : 'projects', parent.id, 'issues'), where('status','==','open'))); snap.forEach(issueDoc => items.push(shape(parent, issueDoc))); }));`
       (Firestore nested-collection ref: `collection(db, 'projects', parentId, 'issues')`.)
    4. Shape each open issue → item: dedupeKey `issue:{parentId}:{issueId}` (composite — gotcha 3; the issue doc has no parent identity so the key carries it), severity fixed `SEVERITY.high` (an open issue is an action), icon `TYPE_META.MRF_REJECTED.icon` (nearest analogue), category `'issue'`, title `(issue.title || issue.issue_type || 'Open issue')`, subtitle `parent.name`, deepLink `{kind:'route', value: parent.kind==='service' ? '#/services' : '#/projects'}`, timestamp `issue.created_at`, overdueScore 0.

    Do NOT wrap the whole source in try/catch — let a failure propagate to assembleFeed's per-source catch (T-107-03). The inner Promise.all is fine; if one parent read throws, the whole source counts as failed for THAT source only (isolated by the engine).

    Record in SUMMARY: the documented SCALE PATH (out of scope this phase) — (b) `collectionGroup(db,'issues') where status=='open'` needs a new composite index + a `match /{path=**}/issues/{id}` rules block AND parent-identity denormalization (issues carry no project_code/department, gotcha 3); (c) an `open_issue_count` denormalized onto the parent (write-path change at issue create/resolve/reopen + one-time backfill) enabling a single scoped `where`. Neither is built now.
  </action>
  <acceptance_criteria>
    - `grep -c 'export async function source' app/home-feed-sources.js` returns 17 total after this task.
    - `grep -n "where('status', '==', 'open')" app/home-feed-sources.js` matches (issue subcollection query).
    - `grep -n "'issues'" app/home-feed-sources.js` matches AND `grep -n 'Promise.all' app/home-feed-sources.js` shows the inner fan-out.
    - `grep -n "where('project_code', 'in'" app/home-feed-sources.js` and `grep -n "where('service_code', 'in'" app/home-feed-sources.js` both match (chunked scoped parent resolution).
    - `grep -n 'issue:' app/home-feed-sources.js` shows the composite `issue:{parentId}:{issueId}` dedupeKey.
    - `grep -c 'collectionGroup' app/home-feed-sources.js` returns 0 (scale path NOT implemented).
    - `grep -c 'onSnapshot' app/home-feed-sources.js` returns 0.
    - SUMMARY documents the (b) collectionGroup and (c) open_issue_count scale paths as out-of-scope.
  </acceptance_criteria>
  <verify>As operations_user with N assigned projects, Network tab shows ≤ N+1 issue reads (bounded); returned items carry dedupeKey `issue:{parentId}:{issueId}` and category 'issue'; no whole-collectionGroup read.</verify>
  <done>#6 exists as a bounded, chunked, inner-Promise.all fan-out with no collectionGroup/denormalization; file now holds 17 sources; scale path recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (authenticated user) → Firestore reads | The material boundary for this phase. Portfolio/admin sources read projects/services/issues/billing_requests/users. A source must never surface a record outside the viewer's assignment (for *_user) or department (for dept admins). Client-side READ scoping only; Firestore rules (unchanged) govern actual authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | I (Information Disclosure — cross-scope leak) | #5a, #5b, #6, #7, #11 | mitigate | Every source self-gates via `getAssignedProjectCodes()`/`getAssignedServiceCodes()` (null=see-all, []=sees-nothing) exactly as the seed sources scope — no ad-hoc role logic. A *_user sees only assigned codes; a dept admin sees only its home-dept collection (the OTHER collection's getAssigned*Codes returns [] → early return). Grep asserts the predicates are called. |
| T-108-02 | E (Elevation / broken access control) | source-role fit | mitigate | Sources are inert until Plan 04's declarative registry lists them; a mis-listed source degrades to [] because it self-gates, never leaking another role's data. |
| T-108-03 | D (DoS via unbounded fan-out) | #6 sourceOpenIssues | mitigate | Bounded per-assignment/dept fan-out with chunked `in` parent resolution + inner Promise.all + a documented parent cap (~60); collectionGroup/denormalization recorded as the out-of-scope scale path. assembleFeed's per-source try/catch isolates a slow/failed fan-out from the rest of the wave. |
| T-108-04 | T (Tampering) | new sources | accept | Read-only (`getDocs` only); no writes, no Firestore rules changed; the collectionGroup rule (`match /{path=**}/issues`) is explicitly NOT added. |
</threat_model>

<verification>
- `grep -c 'export async function source' app/home-feed-sources.js` returns 17.
- Every assignment-scoped source calls getAssignedProjectCodes()/getAssignedServiceCodes() (grep).
- #6 uses bounded inner Promise.all fan-out with `where('status','==','open')`; zero collectionGroup; zero onSnapshot.
- DLP/engagement severities reference THRESHOLDS constants (recomputed bands, gotcha 5).
</verification>

<success_criteria>
- 7 portfolio/admin sources appended; file holds 17 total source functions.
- Same function yields super_admin=all, dept-admin=dept, user=assigned via getAssigned*Codes() — no per-role variants.
- Open-issues fan-out is bounded; scale path documented.
- File remains dead code (no registry) until Plan 04.
</success_criteria>

<output>
After completion, create `.planning/phases/108-home-per-role-attention-feeds/108-03-SUMMARY.md` — list the 7 sources with dedupeKey prefixes/categories/deepLinks/severity rules, the open-issues scale path, and confirmation of self-scoping + zero collectionGroup/onSnapshot.
</output>
