---
phase: 108-home-per-role-attention-feeds
plan: 02
type: execute
wave: 2
depends_on: ["108-01"]
files_modified:
  - app/home-feed-sources.js
autonomous: true
requirements: [HOME-12, HOME-13]
must_haves:
  truths:
    - "Finance's six functional feed sources exist as self-gating async (user) => item[] functions (PRs pending, TRs pending, RFPs overdue/due-this-week, billing requests to decide, collectibles overdue, retention releases)"
    - "Procurement's four functional feed sources exist (MRFs pending processing, aging POs, rejected TRs, delivered POs missing proof)"
    - "Every costly source (#9 RFP, #14 collectibles, #17 aging POs, #19 delivered POs) issues a scoped where() BEFORE any client-side filter"
    - "Lowercase status literals are used for billing_requests ('pending'); Capitalized literals for the procurement chain ('Pending','Rejected','Delivered')"
    - "No onSnapshot is added; each source is one batched getDocs; the file imports status-derivation (not finance.js/projects.js/services.js)"
  artifacts:
    - path: "app/home-feed-sources.js"
      provides: "File scaffold (imports + THRESHOLDS D-01 bands) + 10 Finance/Procurement source functions"
      contains: "export const THRESHOLDS"
  key_links:
    - from: "app/home-feed-sources.js"
      to: "app/status-derivation.js"
      via: "import derive/urgency formulas for #9/#14/#8"
      pattern: "import.*from './status-derivation.js'"
    - from: "app/home-feed-sources.js"
      to: "app/home-feed.js"
      via: "import { SEVERITY } (leaf export)"
      pattern: "import \\{ SEVERITY \\} from './home-feed.js'"
    - from: "sourceCollectiblesOverdue / sourceOverdueRfpPayments"
      to: "collectibles / rfps"
      via: "where('due_date','<', <YYYY-MM-DD>) string-range scoped query"
      pattern: "where\\('due_date', ?'<'"
---

<objective>
Create `app/home-feed-sources.js` (the new sources module) with its scaffold — imports + the declarative D-01 `THRESHOLDS` constant — and build the **10 Finance + Procurement functional feed sources** (HOME-12, HOME-13). These are dept-agnostic functional feeds that follow the 3 seed sources in `home-feed.js` as their template: a scoped `getDocs`, self-gating, item-shaping per the D-11 contract, propagating failures to the engine's per-source try/catch.

At the end of this plan the file exports 10 source functions but has **no registry yet** — it is not imported by anyone, so the app still runs the 3 seed sources for everyone (no broken intermediate state). Plan 03 adds the portfolio/admin sources; Plan 04 wires the registry and rewires the engine.

Every source is **one batched read** in the engine's `Promise.all` wave (Strategy A / 107 D-08). The four costly sources (#9, #14, #17, #19) MUST scope server-side with a `where()` before any client filter (mirrors the 107.6 `pos` full-scan fix, RESEARCH § Costly Sources).

Purpose: populate the Finance and Procurement role feeds.
Output: `app/home-feed-sources.js` with scaffold + 10 sources.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md
@.planning/phases/108-home-per-role-attention-feeds/108-01-SUMMARY.md
@app/home-feed.js
@app/status-derivation.js

<interfaces>
<!-- The seed-source TEMPLATE the executor copies (home-feed.js:261-365). Each new source mirrors this shape. -->
Item contract (home-feed.js:8-24): { dedupeKey, severity, icon, title, subtitle, category, deepLink, timestamp, overdueScore, isRollup? }
deepLink: { kind:'route', value:'#/…' } OR { kind:'modal', handler:'window fn name', arg:id }
SEVERITY (home-feed.js:40): SEVERITY.critical | SEVERITY.high | SEVERITY.medium  (import from './home-feed.js')

TYPE_META icon keys available (notifications.js:75-97): PR_REVIEW_NEEDED, TR_REVIEW_NEEDED, RFP_REVIEW_NEEDED, COLLECTIBLE_CREATED, BILLING_REQUEST_SUBMITTED, MRF_SUBMITTED, PO_DELIVERED, PROJECT_COST_CHANGED, MRF_REJECTED, REGISTRATION_PENDING, PROJECT_STATUS_CHANGED. Use `TYPE_META.<KEY>.icon`.

From status-derivation.js (Plan 01): deriveRFPStatus(rfp), deriveCollectibleStatus(coll), getCollectibleUrgency(coll)→{tier,daysOverdue,daysUntilDue,isOverdue}, getDlpState(engagement), normalizeUpdatedAt(v).
From utils.js (exported): getRFPTotal(rfp).

Status casing (RESEARCH § Field & Status Reference — case-sensitive, gotcha 2):
- prs.finance_status / transport_requests.finance_status: 'Pending' | 'Approved' | 'Rejected' (Capitalized)
- mrfs.status: 'Pending' | 'Approved' | 'Rejected' | … (Capitalized)
- pos.procurement_status: 'Pending Procurement' | 'Pending' | 'Procuring' | 'Procured' | 'Delivered' (Capitalized)
- billing_requests.status: 'pending' (LOWERCASE)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold home-feed-sources.js + Finance/Procurement action-state sources (#12, #13, billing-to-decide, #16, #18)</name>
  <read_first>
    - app/home-feed.js lines 234-365 (the 3 seed sources — the exact template) and lines 35-40 (SEVERITY export)
    - app/notifications.js lines 75-97 (TYPE_META icon keys)
    - app/views/finance.js lines 5282-5345 (the canonical PR/TR pending queries: prs where finance_status=='Pending'; transport_requests where finance_status=='Pending')
    - .planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md § "Per-Source Spec Table" rows #12, #13, #16, #18 and the sourceBillingRequestsToDecide note in § "Registry Shape"
  </read_first>
  <action>
    Create `app/home-feed-sources.js`. Header imports:
    ```js
    import { db, collection, query, where, getDocs } from './firebase.js';
    import { SEVERITY } from './home-feed.js';
    import { TYPE_META } from './notifications.js';
    import { getRFPTotal } from './utils.js';
    import { deriveRFPStatus, deriveCollectibleStatus, getCollectibleUrgency, getDlpState, normalizeUpdatedAt } from './status-derivation.js';
    ```
    (getAssigned*Codes are added by Plan 03; do not import them here yet.)

    Add the D-01 bands constant (RESEARCH § Registry Shape) — this is DISTINCT from status-derivation's URGENCY_THRESHOLDS (portfolio bands). Name it `THRESHOLDS`:
    ```js
    export const THRESHOLDS = {
      STAGE_CRITICAL_D: 14, STAGE_HIGH_D: 7,
      DLP_CRIT_D: 3, DLP_HIGH_D: 7, DLP_MED_D: 14,
      PROGRESS_CRIT_D: 30, PROGRESS_HIGH_D: 14,
      MONEY_CRIT_D: 30, MONEY_DUE_SOON_D: 7,
      PO_AGE_CRIT_D: 30, PO_AGE_HIGH_D: 14,
    };
    ```

    Build these 5 sources (each `export async function (user) => item[]`, one scoped getDocs, no client role logic beyond the natural query filter; let any throw propagate — do NOT wrap in try/catch, the engine owns that):

    **#12 sourcePendingPRs** (HOME-12, finance) — RESEARCH row #12:
    - Query: `getDocs(query(collection(db,'prs'), where('finance_status','==','Pending')))` (Capitalized).
    - Item: dedupeKey `pr-pending:{id}`, severity `SEVERITY.high` (fixed), icon `TYPE_META.PR_REVIEW_NEEDED.icon`, category `'pr'`, title `(pr.pr_id||'PR')+' needs review'`, subtitle from `pr.supplier_name` + `pr.total_amount` (₱ formatted), deepLink `{kind:'route', value:'#/finance'}`, timestamp `pr.date_generated`, overdueScore 0.

    **#13 sourcePendingTRs** (HOME-12, finance) — RESEARCH row #13:
    - Query: `where('finance_status','==','Pending')` on `transport_requests` (Capitalized).
    - Item: dedupeKey `tr-pending:{id}`, severity high, icon `TYPE_META.TR_REVIEW_NEEDED.icon`, category `'tr'`, title `(tr.tr_id||'TR')+' needs review'`, subtitle `tr.supplier_name` + amount, deepLink route `#/finance`, timestamp `tr.date_generated || tr.created_at`, overdueScore 0.

    **sourceBillingRequestsToDecide** (HOME-12, finance) — RESEARCH § Registry note (distinct from sourceOwnBillingRequests):
    - Query: `getDocs(query(collection(db,'billing_requests'), where('status','==','pending')))` — LOWERCASE 'pending' (gotcha 2).
    - Item: dedupeKey `billreq-decide:{id}`, severity high, icon `TYPE_META.BILLING_REQUEST_SUBMITTED.icon`, category `'finance'`, title `'Billing request to decide'`, subtitle from `br.requested_by_name` + `br.project_code||br.service_code` + amount (`br.amount_requested`), deepLink `{kind:'route', value:'#/finance/collectibles'}`, timestamp `br.created_at || br.requested_at`, overdueScore 0.

    **#16 sourceMrfsPendingProcessing** (HOME-13, procurement) — RESEARCH row #16:
    - Query: `getDocs(query(collection(db,'mrfs'), where('status','==','Pending')))` — Capitalized 'Pending'.
    - Item: dedupeKey `mrf-pending:{id}`, severity high, icon `TYPE_META.MRF_SUBMITTED.icon`, category `'mrf'`, title `(mrf.mrf_id||'MRF')+' pending processing'`, subtitle `mrf.project_name || mrf.project_code || '—'`, deepLink `{kind:'route', value:'#/procurement/mrfs'}`, timestamp `mrf.created_at || mrf.date_needed || null`, overdueScore 0.

    **#18 sourceRejectedTRs** (HOME-13, procurement) — RESEARCH row #18:
    - Query: `getDocs(query(collection(db,'transport_requests'), where('finance_status','==','Rejected')))` — Capitalized 'Rejected'.
    - Item: dedupeKey `tr-rejected:{id}`, severity high, icon `TYPE_META.TR_REVIEW_NEEDED.icon`, category `'tr'`, title `(tr.tr_id||'TR')+' rejected — re-edit'`, subtitle `tr.supplier_name || tr.project_code || '—'`, deepLink `{kind:'route', value:'#/procurement/records'}`, timestamp `tr.rejected_at || tr.updated_at || null`, overdueScore 0.

    Use the seed sources' exact iteration style (`snap.forEach(docSnap => { const x = { id: docSnap.id, ...docSnap.data() }; items.push({…}); })`). Format currency with `Number(x).toLocaleString()` prefixed by `₱` as the seeds do.
  </action>
  <acceptance_criteria>
    - `grep -c 'export async function source' app/home-feed-sources.js` returns 5 after this task.
    - `grep -n "where('finance_status', '==', 'Pending')" app/home-feed-sources.js` matches (Capitalized) for #12 and #13.
    - `grep -n "where('status', '==', 'pending')" app/home-feed-sources.js` matches (LOWERCASE) for billing-to-decide.
    - `grep -n "where('status', '==', 'Pending')" app/home-feed-sources.js` matches (Capitalized) for #16.
    - `grep -n "where('finance_status', '==', 'Rejected')" app/home-feed-sources.js` matches for #18.
    - `grep -n 'export const THRESHOLDS' app/home-feed-sources.js` matches; `grep -n 'STAGE_CRITICAL_D: 14' app/home-feed-sources.js` matches.
    - `grep -nE "pr-pending:|tr-pending:|billreq-decide:|mrf-pending:|tr-rejected:" app/home-feed-sources.js` returns 5 distinct dedupeKey prefixes.
    - `grep -nE "onSnapshot|from './views/finance.js'|from './views/projects.js'" app/home-feed-sources.js` returns NO matches.
  </acceptance_criteria>
  <verify>Grep checks pass; in DevTools as a finance user, `await sourcePendingPRs(getCurrentUser())` returns rows only from prs with finance_status Pending, each carrying dedupeKey prefix `pr-pending:`.</verify>
  <done>Scaffold + THRESHOLDS + 5 action-state sources exist with correct status casing and item shapes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Finance money-derivation sources (#9 overdue RFP payments, #14 collectibles overdue, #8 retention releases)</name>
  <read_first>
    - app/status-derivation.js (deriveRFPStatus, deriveCollectibleStatus, getCollectibleUrgency, getDlpState)
    - .planning/phases/108-home-per-role-attention-feeds/108-01-SUMMARY.md (the confirmed RFP due_date format verdict — gates #9's query)
    - app/views/finance.js lines 85-164 (deriveRFPStatus/deriveCollectibleStatus/getCollectibleUrgency originals — confirm the extracted copies match) and lines 2043-2079 (collectibles schema: department, project_code/service_code)
    - .planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md § "Per-Source Spec Table" rows #8, #9, #14 and § "Costly Sources" rows #9, #14
  </read_first>
  <action>
    Add these 3 sources to home-feed-sources.js (append; do not touch Task 1's code).

    **#9 sourceOverdueRfpPayments** (HOME-09 super_admin + HOME-12 finance) — RESEARCH row #9. HOME-12 wording = "RFPs overdue/due this week", so cover BOTH past-due and the next 7 days:
    - Compute `const cutoff = new Date(Date.now() + THRESHOLDS.MONEY_DUE_SOON_D*86400000).toISOString().slice(0,10);` (today + 7 days, YYYY-MM-DD).
    - Query (string range — SAFE per Plan 01's confirmed YYYY-MM-DD format): `getDocs(query(collection(db,'rfps'), where('due_date','<', cutoff)))`.
    - Client filter: exclude fully-paid via `if (deriveRFPStatus(rfp) === 'Fully Paid') return;`.
    - Compute `const daysPastDue = Math.floor((Date.now() - new Date(rfp.due_date).getTime())/86400000);`.
    - Severity: `daysPastDue > THRESHOLDS.MONEY_CRIT_D` → critical; else if `daysPastDue >= 0` → high; else (future, due within 7d) → medium.
    - Item: dedupeKey `rfp-overdue:{id}`, icon `TYPE_META.RFP_REVIEW_NEEDED.icon`, category `'rfp'`, title `(rfp.rfp_id||'RFP')+(daysPastDue>=0?' payment overdue':' due soon')`, subtitle from `rfp.po_id`/`rfp.tranche_label` + `'₱'+getRFPTotal(rfp).toLocaleString()`, deepLink `{kind:'route', value:'#/finance'}`, timestamp `rfp.due_date`, overdueScore `daysPastDue`.
    - If Plan 01's SUMMARY reported the format is NOT YYYY-MM-DD, instead use a bounded recent-window scan (query without the range, cap results client-side) and record the deviation — but per the confirmed finding the string range applies.

    **#14 sourceCollectiblesOverdue** (HOME-12 finance) — RESEARCH row #14. "Overdue" only (past-due, not fully paid):
    - Compute `const todayISO = new Date().toISOString().slice(0,10);`.
    - Query (collectibles due_date IS YYYY-MM-DD — confirmed finance.js:139): `getDocs(query(collection(db,'collectibles'), where('due_date','<', todayISO)))`.
    - Client filter: `if (deriveCollectibleStatus(coll) !== 'Overdue') return;` (excludes fully-paid / partially-paid-but-not-overdue).
    - `const u = getCollectibleUrgency(coll);` severity: `u.tier === 'critical'` (≥30d overdue) → critical; else → high.
    - Item: dedupeKey `collectible-overdue:{id}`, icon `TYPE_META.COLLECTIBLE_CREATED.icon`, category `'finance'`, title `'Collectible overdue'`, subtitle from `coll.project_code||coll.service_code` + `'₱'+Number(coll.amount_requested).toLocaleString()` + `u.daysOverdue+'d overdue'`, deepLink `{kind:'route', value:'#/finance/collectibles'}`, timestamp `coll.due_date`, overdueScore `u.daysOverdue`.

    **#8 sourceRetentionReleases** (HOME-12 finance) — RESEARCH row #8. DLP-expired retention still held; spans BOTH collections:
    - Query both collections in an inner Promise.all, each filtered to Completed:
      `const [projSnap, svcSnap] = await Promise.all([ getDocs(query(collection(db,'projects'), where('project_status','==','Completed'))), getDocs(query(collection(db,'services'), where('project_status','==','Completed'))) ]);`
    - For each doc: `if (getDlpState(e) !== 'expired') return;` (getDlpState returns 'expired' when dlp_expires_at is past AND retention_released_at is null — gotcha 6, pre-filtered to Completed by the query).
    - Severity: fixed `SEVERITY.high`.
    - Item: dedupeKey `retention-release:{id}`, icon `TYPE_META.PROJECT_COST_CHANGED.icon`, category `'dlp'`, title `'Retention release to record'`, subtitle from `e.project_code||e.service_code` + `e.project_name||''` + `'DLP expired'`, deepLink `{kind:'route', value:'#/finance'}`, timestamp `e.dlp_expires_at`, overdueScore `Math.max(0, Math.floor((Date.now()-new Date(e.dlp_expires_at).getTime())/86400000))`.
    - Tag each item with which collection it came from only via the category/subtitle (single interleaved list, D-04).
  </action>
  <acceptance_criteria>
    - `grep -c 'export async function source' app/home-feed-sources.js` returns 8 after this task (5 + 3).
    - `grep -nE "where\('due_date', ?'<'" app/home-feed-sources.js` returns 2 matches (#9 and #14) — a scoped where() precedes any client filter.
    - `grep -n "deriveRFPStatus(rfp) === 'Fully Paid'" app/home-feed-sources.js` matches (#9 excludes fully-paid); `grep -n "deriveCollectibleStatus(coll) !== 'Overdue'" app/home-feed-sources.js` matches (#14).
    - `grep -n "getDlpState(e) !== 'expired'" app/home-feed-sources.js` matches (#8); `grep -c "where('project_status', '==', 'Completed')" app/home-feed-sources.js` returns 2 (projects + services).
    - `grep -nE "rfp-overdue:|collectible-overdue:|retention-release:" app/home-feed-sources.js` returns 3 distinct dedupeKey prefixes.
    - `grep -n "THRESHOLDS.MONEY_CRIT_D" app/home-feed-sources.js` matches (#9 critical band references the D-01 constant, not a magic number).
  </acceptance_criteria>
  <verify>Grep checks pass; DevTools Network tab shows #9/#14 issue a `due_date`-ranged read (not a whole-collection scan); as a finance user, returned items carry the expected dedupeKey prefixes and severities.</verify>
  <done>#9, #14, #8 exist; each scopes server-side before client filtering; severity bands reference THRESHOLDS; derivation comes from status-derivation.js.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Procurement scan sources (#17 aging POs, #19 delivered POs missing proof)</name>
  <read_first>
    - app/views/procurement.js lines 486-490, 945, 7838-7851 (PO procurement_status enum + updated_at bumped on every status change)
    - app/proof-modal.js lines 100-118 (PO proof fields: proof_url, proof_remarks, proof_attached_at)
    - app/status-derivation.js (normalizeUpdatedAt)
    - .planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md § "Per-Source Spec Table" rows #17, #19; § "Costly Sources" rows #17, #19; § "Landmines" gotcha 4 (POs have no status-change timestamp — use updated_at)
  </read_first>
  <action>
    Add these 2 procurement sources to home-feed-sources.js (append).

    **#17 sourceAgingPOs** (HOME-13 procurement) — RESEARCH row #17. Active POs aging in status:
    - Query (active statuses only — scoped, not a whole scan): `getDocs(query(collection(db,'pos'), where('procurement_status','in', ['Pending Procurement','Pending','Procuring','Procured'])))`.
    - Client age: `const ms = normalizeUpdatedAt(po.updated_at) ?? normalizeUpdatedAt(po.date_issued); if (ms == null) return; const d = (Date.now()-ms)/86400000;` (gotcha 4 — updated_at is the age-in-status proxy, date_issued fallback).
    - Only surface aging POs: `if (d <= THRESHOLDS.PO_AGE_HIGH_D) return;`.
    - Severity: `d > THRESHOLDS.PO_AGE_CRIT_D` → critical; else → high.
    - Item: dedupeKey `po-aging:{id}`, icon `TYPE_META.PO_DELIVERED.icon`, category `'po'`, title `(po.po_id||'PO')+' aging in '+po.procurement_status`, subtitle from `po.supplier_name` + `Math.round(d)+'d in status'`, deepLink `{kind:'route', value:'#/procurement/records'}`, timestamp `po.updated_at || po.date_issued || null`, overdueScore `d`.

    **#19 sourceDeliveredPOsMissingProof** (HOME-13 procurement) — RESEARCH row #19. Scoped to Delivered, then client-filter absent proof (can't where() on a missing field):
    - Query: `getDocs(query(collection(db,'pos'), where('procurement_status','==','Delivered')))`.
    - Client filter: `if (po.proof_url || po.proof_remarks) return;` (only POs with NO proof surface).
    - Severity: fixed `SEVERITY.high`.
    - Item: dedupeKey `po-noproof:{id}`, icon `TYPE_META.PO_DELIVERED.icon`, category `'po'`, title `(po.po_id||'PO')+' delivered — proof missing'`, subtitle from `po.supplier_name` + `po.project_name||po.project_code||'—'`, deepLink `{kind:'route', value:'#/procurement/records'}`, timestamp `po.updated_at || po.date_issued || null`, overdueScore 0.

    Record in SUMMARY: the collectionGroup/denormalization approaches are NOT used; the `procurement_status` `where()` bounds both scans (single-field auto-indexed), matching the 107.6 pos full-scan precedent.
  </action>
  <acceptance_criteria>
    - `grep -c 'export async function source' app/home-feed-sources.js` returns 10 after this task (8 + 2).
    - `grep -n "where('procurement_status', 'in'" app/home-feed-sources.js` matches (#17 active-status scoped query).
    - `grep -n "where('procurement_status', '==', 'Delivered')" app/home-feed-sources.js` matches (#19 scoped to Delivered before the client proof filter).
    - `grep -n "po.proof_url || po.proof_remarks" app/home-feed-sources.js` matches (#19 absent-proof client filter).
    - `grep -nE "po-aging:|po-noproof:" app/home-feed-sources.js` returns 2 distinct dedupeKey prefixes.
    - `grep -n "THRESHOLDS.PO_AGE_CRIT_D" app/home-feed-sources.js` matches (#17 band references the D-01 constant).
    - `grep -c 'onSnapshot' app/home-feed-sources.js` returns 0.
  </acceptance_criteria>
  <verify>Grep checks pass; DevTools as a procurement user: #17 returns only active-status POs older than 14d; #19 returns only Delivered POs lacking both proof fields.</verify>
  <done>#17 and #19 exist; both scope on procurement_status server-side before client filtering; file now holds 10 Finance/Procurement sources and zero onSnapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (authenticated user) → Firestore reads | Each source issues a scoped `getDocs`. Firestore security rules (unchanged this phase) govern actual read authorization; these sources add client-side presentation only. Finance/Procurement sources are functional (dept-agnostic) — no cross-assignment scoping needed, but they must never widen beyond their functional intent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | I (Information Disclosure) | all 10 sources | mitigate | Each source's `where()` is its scope; results are read-only rows a Finance/Procurement user is already authorized to read via existing Firestore rules. No new fields exposed beyond id/amount/status already visible in finance/procurement views. |
| T-108-02 | E (Elevation via broken access control) | source-role fit | mitigate | Sources are functional and only enter Finance/Procurement role arrays in Plan 04's registry; a source is inert until listed. No ad-hoc role branching inside these sources. |
| T-108-06 | I (casing bug → wrong data) | billing-to-decide (#), #16, #18, #12/#13 | mitigate | Status literals use exact casing (lowercase 'pending' for billing_requests; Capitalized 'Pending'/'Rejected'/'Delivered' for the procurement chain) per RESEARCH gotcha 2 — a casing mismatch would silently never-match, not leak. Grep asserts the exact literals. |
| T-108-04 | T (Tampering) | new sources | accept | Read-only (`getDocs` only); no writes, no Firestore rules changed. |
</threat_model>

<verification>
- `app/home-feed-sources.js` loads with no console error; `grep -c 'export async function source'` returns 10.
- Every costly source (#9, #14, #17, #19) has a scoped `where()` before any client filter (grep).
- Zero `onSnapshot`; zero imports from finance.js/projects.js/services.js.
- Status casing matches the exact literals per RESEARCH gotcha 2.
</verification>

<success_criteria>
- 10 Finance/Procurement source functions exist and self-gate via their scoped queries.
- The four costly sources scope server-side first (Strategy A preserved).
- THRESHOLDS D-01 bands drive the money/PO-age severities.
- File is dead code (no registry) so the running app is unaffected until Plan 04 wires it.
</success_criteria>

<output>
After completion, create `.planning/phases/108-home-per-role-attention-feeds/108-02-SUMMARY.md` — list the 10 sources with their dedupeKey prefixes, categories, deepLinks, and severity rules; confirm zero onSnapshot and the costly-source scoping.
</output>
