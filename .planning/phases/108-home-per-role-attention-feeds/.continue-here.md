---
context: phase
phase: 108-home-per-role-attention-feeds
task: 1
total_tasks: 5
status: in_progress
last_updated: 2026-07-12T17:02:16Z
---

# Phase 108 — Paused mid-UAT (code done, 5 browser tests remain)

**TL;DR:** All 4 plans executed + independently code-verified (18/18 must-haves, 0 gaps) + code-reviewed (0 critical / 3 warning / 4 info) with all 3 warnings FIXED. Tree is clean, everything committed on `v4.2`. The ONLY thing left before Phase 108 is complete is the **5 in-browser UAT items** — Test 1 of 5 was presented and is awaiting the operator's result. **On resume: continue the conversational UAT — do NOT re-execute anything.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| gsd-sdk calls | `gsd-sdk` is NOT installed in this env (only legacy gsd-tools.cjs). Any `gsd-sdk query …` in a workflow will fail. | advisory | Substitute inline: plain `git` for commits, manual Edit for STATE/ROADMAP, direct file reads for context. |
| Re-executing a done phase | Phase 108's 4 plans are all committed with SUMMARYs. Re-running execute-phase would waste effort. | advisory | On resume, jump straight to the UAT continuation; verify completion via the 4 `*-SUMMARY.md` + `108-VERIFICATION.md` on disk. |

<current_state>
`/gsd-verify-work 108` is mid-session. `108-HUMAN-UAT.md` has `status: testing`, Current Test = Test 1 ("Per-role feed renders with no module-load error"). Test 1 was presented to the operator (load Home, feed renders or calm empty state, no red console error / no "cannot access 'SEVERITY' before initialization"). Awaiting their plain-text response. Tests 2-5 pending.
</current_state>

<completed_work>

- **Execution (all 4 waves, sequential, no worktrees):**
  - W1 108-01: NEW `app/status-derivation.js` (7 pure formulas extract-by-copy; RFP due_date CONFIRMED YYYY-MM-DD)
  - W2 108-02: NEW `app/home-feed-sources.js` scaffold + THRESHOLDS + 10 Finance/Procurement sources (4 costly server-side-scoped)
  - W3 108-03: +7 portfolio/admin self-scoped sources (getAssigned*Codes; #6 open-issues bounded chunked fan-out; →17)
  - W4 108-04: moved 3 seeds + ROLE_SOURCES registry + getSourcesForUser + rewired engine (→20 sources; engine pristine; getSourcesForUser re-exported for home.js)
- **Verification (inline, since delegated verifier hit the spend limit):** 108-VERIFICATION.md, 18/18 must-haves, 0 gaps, status human_needed. Python name-resolution: 20 defined = 20 referenced, 0 missing; per-role 6/6/6/5/5/6/4.
- **Code review + fixes:** 108-REVIEW.md (0 crit / 3 warn / 4 info). WR-01 (collectibles UTC→local cutoff), WR-02 (open-issues per-collection cap split), WR-03 (corrected fail-closed comment) all FIXED + committed. 4 info left advisory.
</completed_work>

<remaining_work>

- **UAT-1** (presented, awaiting result): per-role feed renders, no module-load ReferenceError.
- **UAT-2**: feed content matches role (finance `pr-pending`/`tr-pending`; procurement `mrf-pending`/`po-aging`; super_admin `proposal-approval`/`user-pending`/`project-overdue`).
- **UAT-3**: costly sources (#9/#14/#17/#19) issue scoped reads, not full scans; #6 ≤ (assigned parents + 1) reads (DevTools Network); no onSnapshot.
- **UAT-4**: assignment isolation — operations_user: `sourceOverdueServices`→[], `sourceOverdueProjects`→assigned only; super_admin→cross-dept.
- **UAT-5**: deep-link clicks route correctly (#/finance, #/finance/collectibles, #/procurement/records, #/projects, #/services, #/admin?section=user-management).
- **After all-pass:** run `transition.md` inline to mark Phase 108 complete (ROADMAP checkbox, STATE advance, REQUIREMENTS HOME-09..13). Then Phase 109 (Executive Dashboard).
- **On any fail:** diagnose root cause → gsd-planner gap_closure → gsd-plan-checker → `/gsd-execute-phase 108 --gaps-only`.
</remaining_work>

<decisions_made>

- Sequential single-plan waves, no worktrees (02/03/04 chain-edit the same file — zero parallelism possible).
- Orchestrator owned STATE.md/ROADMAP.md writes; executors did code + SUMMARY only (gsd-sdk absent).
- KEEP services_admin non-approver gate (self-gates to [] — decision #1); engine kept pristine except the getSourcesForUser import/re-export.
- WR-01: local-date cutoff + 1d slack (superset of the authoritative client filter). WR-02: per-collection budget split. WR-03: comment-only correction (functional queues are dept-agnostic by design).
</decisions_made>

<blockers>
- UAT needs operator sign-in as up to 7 roles against prod Firebase — Claude cannot authenticate (credentials off-limits, no role accounts). Operator-driven: they run the 5 checks and report; Claude records + diagnoses. (Environmental note: the phase verifier + first code-review attempt earlier died on the ACCOUNT MONTHLY SPEND LIMIT; both since completed successfully.)
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable state (task pointers, next_action).
2. `.planning/phases/108-home-per-role-attention-feeds/108-HUMAN-UAT.md` — the live UAT session (5 tests, current = Test 1).
3. `.planning/phases/108-home-per-role-attention-feeds/108-VERIFICATION.md` — the 18/18 code-level verdict + the 5 human_verification items.
4. `.planning/phases/108-home-per-role-attention-feeds/108-REVIEW.md` — code review + the 3 applied fixes.
5. `.planning/STATE.md` — Current Position block (Phase 108 EXECUTED + code-verified + reviewed).

## Infrastructure State
- Branch `v4.2`, upstream `origin/v4.2`. Being pushed to origin now as part of this pause (77 commits ahead → published). Netlify auto-deploys only on `main`, so pushing v4.2 does NOT deploy to prod (clmcop.netlify.app).
- gsd-sdk NOT installed — work inline.
- No running servers/watchers.

<context>
Phase 108 is functionally done and high-confidence. The remaining UAT is observation-only (operator confirms the feeds render/scope correctly in-browser). Resume by collecting the Test 1 result and continuing conversationally. The code has already been adversarially checked (verifier + code review + 3 fixes), so UAT failures, if any, are most likely data/environment-specific rather than logic bugs.
</context>

<next_action>
Start with: read `108-HUMAN-UAT.md`, then ask the operator for the Test 1 result (or re-present Test 1). Record pass/issue, advance through Tests 2-5. On all-pass → transition.md to mark Phase 108 complete → offer Phase 109. On issue → diagnose + gap-closure plan.
</next_action>
