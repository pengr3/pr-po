---
phase: 108-home-per-role-attention-feeds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/status-derivation.js
autonomous: true
requirements: [HOME-09, HOME-10, HOME-11, HOME-12]
must_haves:
  truths:
    - "A new app/status-derivation.js exports the pure derivation formulas the feed sources need (no Firestore I/O, no heavy-view imports)"
    - "RFP due_date storage format is confirmed YYYY-MM-DD so Plan 02 source #9 can safely ship a where('due_date','<',…) string-range query"
  artifacts:
    - path: "app/status-derivation.js"
      provides: "normalizeUpdatedAt, stageDaysInStage, getDlpState, getEngagementSignal, deriveCollectibleStatus, getCollectibleUrgency, deriveRFPStatus, URGENCY_THRESHOLDS — extracted-by-copy from the private view helpers"
      exports: ["normalizeUpdatedAt", "stageDaysInStage", "getDlpState", "getEngagementSignal", "deriveCollectibleStatus", "getCollectibleUrgency", "deriveRFPStatus", "URGENCY_THRESHOLDS"]
  key_links:
    - from: "app/status-derivation.js"
      to: "app/utils.js"
      via: "import { getRFPTotal } for deriveRFPStatus"
      pattern: "import.*getRFPTotal.*from './utils.js'"
    - from: "app/status-derivation.js"
      to: "(nothing heavy)"
      via: "MUST NOT import from finance.js/projects.js/services.js/firebase.js"
      pattern: "no import from ./views/ or ./firebase.js"
---

<objective>
Create `app/status-derivation.js` — a lightweight, pure-formula module that Phase 108's feed sources import for status/urgency/DLP/collectible/RFP derivation, WITHOUT eagerly loading the giant routed views (finance.js 7,000+ lines, projects.js/services.js) into the landing-page critical path.

The formulas are **extract-by-copy** (verbatim) from their canonical private locations. This is the sanctioned reading of D-02 "reuse the helpers" (RESEARCH § Derivation-Helper Reuse Map, Assumption A6): same math, one new home, keeping Strategy A "fast" intact. Do NOT refactor finance.js/projects.js/services.js this phase — accept temporary duplication (recorded as a hygiene follow-up).

Also settle Open Question 1 / Assumption A1: confirm the RFP `due_date` storage format so Plan 02's source #9 (overdue RFP payments) can safely use a `where('due_date','<',…)` string-range optimization instead of a full-collection scan.

Purpose: unblock all derived feed sources (5a/5b/7/8/9/11/14) with a single leaf module.
Output: `app/status-derivation.js` + a recorded RFP-due_date format finding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md
@.planning/phases/108-home-per-role-attention-feeds/108-CONTEXT.md

<interfaces>
<!-- EXACT source formulas to copy. Cited file:line so the executor transcribes, not re-derives. -->

From app/views/projects.js:53 — the portfolio urgency bands used INTERNALLY by getEngagementSignal:
```js
const URGENCY_THRESHOLDS = {
    FOR_PROPOSAL_WATCH: 2,        FOR_PROPOSAL_URGENT: 5,
    INTERNAL_APPROVAL_WATCH: 2,   INTERNAL_APPROVAL_URGENT: 5,
    CLIENT_REVIEW_WATCH: 7,       CLIENT_REVIEW_URGENT: 14,
    FOR_REVISION_WATCH: 2,        FOR_REVISION_URGENT: 3,
    CLIENT_APPROVED_WATCH: 3,     CLIENT_APPROVED_URGENT: 7,
    MOBILIZATION_WATCH: 3,        MOBILIZATION_URGENT: 10,
    FOR_INSPECTION_WATCH: 2,      FOR_INSPECTION_URGENT: 5,
    ONGOING_QUIET_WATCH: 7,       ONGOING_QUIET_URGENT: 14,
    DLP_SOON_DAYS: 14
};
```

From app/views/projects.js:911 (normalizeUpdatedAt), :925 (stageDaysInStage), :897 (getDlpState), :931 (getDefaultOkSignal), :942 (getProjectSignal) — copy verbatim. getDlpState is IDENTICAL in services.js:952; stageDaysInStage identical to services.js:945 (stageDaysInStageService); normalizeUpdatedAt identical to services.js.

From app/views/finance.js:89 (deriveRFPStatus), :110 (deriveCollectibleStatus), :129 (getCollectibleUrgency) — copy verbatim. deriveRFPStatus/deriveCollectibleStatus/getCollectibleUrgency are the canonical formulas; deriveRFPStatus uses the exported getRFPTotal (utils.js:71).

From app/views/services.js:971 (getServiceSignal) — near-duplicate of getProjectSignal; the ONLY divergence is the On-going branch (services have no last_activity urgent clock → watch-only). The unified getEngagementSignal keeps the projects.js On-going activity clock; services degrade gracefully (feed source #11 recomputes its own progress band, so this divergence does not affect the feed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/status-derivation.js with the extracted pure formulas</name>
  <read_first>
    - app/views/projects.js lines 44-63 (URGENCY_THRESHOLDS), 888-1034 (getDlpState, normalizeUpdatedAt, stageDaysInStage, getDefaultOkSignal, getProjectSignal, computeUrgencySignals) — the source of truth to copy
    - app/views/services.js lines 940-1062 (getServiceSignal, getDlpState, stageDaysInStageService) — confirm they match the project versions; note the On-going divergence
    - app/views/finance.js lines 85-164 (deriveRFPStatus, deriveCollectibleStatus, getCollectibleUrgency, getCollectibleLastPayment) — the source of truth to copy
    - app/utils.js lines 55-77 (getRFPTotal, getRFPFees — exported; deriveRFPStatus depends on getRFPTotal)
    - .planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md § "Derivation-Helper Reuse Map" and § "Landmines / Gotchas" items 1, 5, 6
  </read_first>
  <action>
    Create `app/status-derivation.js`. Add ONE import at the top:
    `import { getRFPTotal } from './utils.js';`
    (No other imports. MUST NOT import db/collection/query/getDocs from firebase.js, and MUST NOT import from ./views/finance.js, ./views/projects.js, or ./views/services.js — that would eager-load heavy routed views onto the landing page and defeat Strategy A.)

    Copy these formulas VERBATIM from their cited source lines (add a `// extracted-by-copy from <file>:<line> (Phase 108, D-02) — canonical copy lives there; keep in sync` comment above each):
    1. `export const URGENCY_THRESHOLDS = { … }` — verbatim from projects.js:53 (the full 17-key object shown in <interfaces>).
    2. `export function normalizeUpdatedAt(v) { … }` — verbatim from projects.js:911.
    3. `export function stageDaysInStage(p, now) { … }` — verbatim from projects.js:925 (uses normalizeUpdatedAt on status_changed_at ?? updated_at ?? created_at).
    4. `export function getDlpState(engagement) { … }` — verbatim from projects.js:897 (returns 'active' unless dlp_months set AND project_status==='Completed', then 'released'|'expired'|'in-dlp'). Rename the param to `engagement` for project/service neutrality; body unchanged.
    5. `function getDefaultOkSignal(p) { … }` — verbatim from projects.js:931 (internal helper; does NOT need export).
    6. `export function getEngagementSignal(p, now) { … }` — copy getProjectSignal verbatim from projects.js:942 and rename to getEngagementSignal. Keep the projects.js On-going activity-clock branch. Add a comment noting services diverge (On-going watch-only, no last_activity urgent tier — services.js:971) and that this is acceptable because feed severity is recomputed against the D-01 bands, not signal.level (RESEARCH gotcha 5).
    7. `export function deriveRFPStatus(rfp) { … }` — verbatim from finance.js:89 (Fully Paid > Overdue > Partially Paid > Pending; uses getRFPTotal).
    8. `export function deriveCollectibleStatus(coll) { … }` — verbatim from finance.js:110 (calls getCollectibleUrgency).
    9. `export function getCollectibleUrgency(coll) { … }` — verbatim from finance.js:129 (returns { tier, daysOverdue, daysUntilDue, isOverdue }; tier ∈ 'critical'|'overdue'|'near-due'|'partial'|'healthy'|'paid').

    Add a top-of-file banner comment: this module is a leaf of pure derivation formulas with NO Firestore I/O and NO view imports, so importing it into home-feed-sources.js keeps the landing page fast (Strategy A / 107 D-08). List the canonical source lines it mirrors and the recorded hygiene duplication (status-derivation.js vs finance.js/projects.js/services.js private copies; and the pre-existing expense-modal.js deriveCollectibleStatus dup noted at finance.js:106-108).
  </action>
  <acceptance_criteria>
    - `grep -c 'export function' app/status-derivation.js` returns at least 7 (normalizeUpdatedAt, stageDaysInStage, getDlpState, getEngagementSignal, deriveCollectibleStatus, getCollectibleUrgency, deriveRFPStatus) plus `export const URGENCY_THRESHOLDS`.
    - `grep -n "from './utils.js'" app/status-derivation.js` shows exactly `import { getRFPTotal } from './utils.js';` and NOTHING else imported.
    - `grep -nE "from '\./firebase\.js'|from '\./views/" app/status-derivation.js` returns NO matches (no Firestore I/O, no heavy-view import).
    - `grep -nE "onSnapshot|getDocs|collection\(|query\(" app/status-derivation.js` returns NO matches (pure formulas, no reads).
    - `grep -n "return 'in-dlp'" app/status-derivation.js` matches (getDlpState copied intact); `grep -n "tier = 'critical'" app/status-derivation.js` matches (getCollectibleUrgency 30-day critical band copied intact).
    - Console: import the module in DevTools — `getDlpState({dlp_months:12, project_status:'Completed', dlp_expires_at:'2020-01-01', retention_released_at:null})` returns `'expired'`; `deriveRFPStatus({payment_records:[], due_date:'2020-01-01'})` returns `'Overdue'`.
  </acceptance_criteria>
  <verify>Grep checks above pass; module loads without error and the two console spot-checks return the expected literals.</verify>
  <done>app/status-derivation.js exists, exports the 7 functions + URGENCY_THRESHOLDS, imports only getRFPTotal from utils.js, has zero Firestore I/O and zero view imports, and the copied formulas match their cited originals.</done>
</task>

<task type="auto">
  <name>Task 2: Verify + record the RFP due_date storage format (Open Question 1 / A1)</name>
  <read_first>
    - app/views/procurement.js lines 1188-1200, 1330-1342, 1478-1490 (the three `<input type="date" id="rfpDueDate">` fields in the RFP modals)
    - app/views/procurement.js lines 1793-1810, 1938-1958, 2043-2100 (the three RFP save paths reading `document.getElementById('rfpDueDate')?.value` into `dueDate`, then writing `due_date: dueDate`)
    - .planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md § "Costly Sources & Optimizations" (#9 row) and § "Open Questions" (Q1) and § "Assumptions Log" (A1)
  </read_first>
  <action>
    This is a read-only verification whose OUTPUT gates Plan 02's source #9. Confirm what shape RFP `due_date` is stored in:
    - The three RFP write paths (procurement.js ~1873, ~1994, ~2097) all set `due_date: dueDate`, where `dueDate = document.getElementById('rfpDueDate')?.value` (~1797, ~1942, ~2047).
    - `rfpDueDate` is an `<input type="date">` (procurement.js ~1192, ~1336, ~1483). An HTML5 `type="date"` input's `.value` is ALWAYS the `YYYY-MM-DD` string form.
    - Therefore RFP `due_date` is stored as `YYYY-MM-DD` — the same shape as collectibles `due_date` (confirmed finance.js:139). The `where('due_date','<', <ISO-YYYY-MM-DD>)` string-range query is SAFE for source #9.

    Record the finding in this plan's SUMMARY.md under a "RFP due_date format — CONFIRMED YYYY-MM-DD" heading with the three write-path line numbers and the input-type evidence, so Plan 02 can cite it. If (contrary to the above) any RFP write path is found to store a non-`YYYY-MM-DD` value (e.g. an ISO datetime or a Date object), record that instead and flag that Plan 02 #9 MUST use the bounded recent-window scan fallback rather than the string range.

    Do NOT modify procurement.js — this task only reads and records.
  </action>
  <acceptance_criteria>
    - SUMMARY.md contains a "RFP due_date format" finding stating the confirmed format (`YYYY-MM-DD`) with the evidence: `due_date: dueDate` writes at procurement.js ~1873/~1994/~2097, sourced from `<input type="date" id="rfpDueDate">.value`.
    - The finding explicitly states whether Plan 02 #9 may use `where('due_date','<', todayPlus7ISO)` (YES if YYYY-MM-DD) or must use the scan fallback.
    - `grep -n 'type="date" id="rfpDueDate"' app/views/procurement.js` returns 3 matches (evidence the field is a date input).
  </acceptance_criteria>
  <verify>SUMMARY records the format verdict with cited line numbers; grep confirms the three date-input fields.</verify>
  <done>RFP due_date format is verified and recorded; Plan 02 #9 has a definitive go/no-go on the string-range optimization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser, authenticated user) → Firestore | This plan adds NO reads — it is a pure-formula module + a read-only code inspection. No boundary is crossed at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-00 | I (Information Disclosure) | status-derivation.js | accept | Module is pure math over data already fetched by callers; it performs no reads and holds no data. No scoping concern originates here — scoping is enforced by the source functions (Plans 02/03) that consume these formulas. |
| T-108-04 | T (Tampering) | new module | accept | No write paths, no Firestore rules touched. Read-only extraction + a read-only inspection. |
</threat_model>

<verification>
- `app/status-derivation.js` loads in the browser with no console error.
- No import of firebase.js or any ./views/ module (grep clean).
- The RFP due_date format verdict is recorded in SUMMARY for Plan 02.
</verification>

<success_criteria>
- status-derivation.js exports normalizeUpdatedAt, stageDaysInStage, getDlpState, getEngagementSignal, deriveCollectibleStatus, getCollectibleUrgency, deriveRFPStatus, and URGENCY_THRESHOLDS.
- The module has zero Firestore I/O and zero heavy-view imports (Strategy A preserved).
- RFP due_date format is confirmed and recorded so Plan 02 source #9 has a definitive optimization decision.
</success_criteria>

<output>
After completion, create `.planning/phases/108-home-per-role-attention-feeds/108-01-SUMMARY.md` — include the exported-function list, confirmation of zero heavy imports, and the RFP due_date format finding.
</output>
