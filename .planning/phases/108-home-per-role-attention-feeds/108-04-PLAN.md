---
phase: 108-home-per-role-attention-feeds
plan: 04
type: execute
wave: 4
depends_on: ["108-02", "108-03"]
files_modified:
  - app/home-feed-sources.js
  - app/home-feed.js
autonomous: true
requirements: [HOME-09, HOME-10, HOME-11, HOME-12, HOME-13]
must_haves:
  truths:
    - "getSourcesForUser(user) returns EXACTLY the role's source set for each of the 7 roles (super_admin, operations_admin, services_admin, operations_user, services_user, finance, procurement)"
    - "The 3 seed sources (proposals-awaiting-approval, my-proposals-for-revision, my-rejected-MRFs) + scopeProposalToDept now live in home-feed-sources.js and are wired into the correct role arrays"
    - "home-feed.js engine is otherwise UNCHANGED — item model, rankItems/dedupeItems/rollUpByCategory/capItems, assembleFeed, cap 8/25, roll-up ≥5, dedupe all intact; its only change is importing getSourcesForUser from home-feed-sources.js"
    - "The app loads with no ReferenceError; no circular-import failure (SEVERITY used only inside source function bodies)"
    - "No onSnapshot added anywhere; every source remains one batched getDocs (Strategy A)"
  artifacts:
    - path: "app/home-feed-sources.js"
      provides: "3 moved seed sources + scopeProposalToDept + ROLE_SOURCES registry + getSourcesForUser"
      contains: "const ROLE_SOURCES"
    - path: "app/home-feed.js"
      provides: "engine with seed bodies removed, imports getSourcesForUser from sources module"
      contains: "import { getSourcesForUser } from './home-feed-sources.js'"
  key_links:
    - from: "app/home-feed.js"
      to: "app/home-feed-sources.js"
      via: "import { getSourcesForUser } (assembleFeed default arg)"
      pattern: "import \\{ getSourcesForUser \\} from './home-feed-sources.js'"
    - from: "app/home-feed-sources.js getSourcesForUser"
      to: "ROLE_SOURCES[user.role]"
      via: "declarative role → source-set lookup"
      pattern: "ROLE_SOURCES\\[user.role\\]"
---

<objective>
Wire everything together. Move the 3 seed sources (and `scopeProposalToDept`) OUT of `app/home-feed.js` INTO `app/home-feed-sources.js`, add the declarative `ROLE_SOURCES` registry + `getSourcesForUser(user)`, and rewire `home-feed.js` to import `getSourcesForUser` from the sources module. This is the atomic switch that makes every role see its real per-role feed (HOME-09..13).

This is the ONLY plan in Wave 4 and the only one touching both shared files, so there is no parallel-plan conflict. It must be atomic: moving the seed bodies and repointing the engine happen together (if seeds were moved without repointing the engine, home-feed.js's old getSourcesForUser would reference now-absent functions and break the feed).

Import direction after this plan: engine (home-feed.js) → sources (home-feed-sources.js) → status-derivation/utils/notifications/proposals. Sources import ONLY `SEVERITY` back from the engine (a frozen leaf constant, used only inside function bodies) — resolved decision #4. Verify this benign cycle does not cause a runtime ReferenceError.

Purpose: activate the per-role feeds.
Output: `home-feed-sources.js` (registry complete, 20 sources total incl. 3 seeds) + slimmed `home-feed.js` engine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md
@.planning/phases/108-home-per-role-attention-feeds/108-02-SUMMARY.md
@.planning/phases/108-home-per-role-attention-feeds/108-03-SUMMARY.md
@app/home-feed.js
@app/home-feed-sources.js

<interfaces>
<!-- The registry to build (RESEARCH § Registry Shape & Module Placement). Function names must match Plans 02/03 + the 3 moved seeds. -->
Roles (7): super_admin, operations_admin, services_admin, operations_user, services_user, finance, procurement.

ROLE_SOURCES mapping (RESEARCH § Registry Shape):
- super_admin:      [sourceProposalsAwaitingApproval, sourcePendingUserRegistrations, sourceOverdueProjects, sourceOverdueServices, sourceDlpWindowsExpiring, sourceOverdueRfpPayments]        (HOME-09)
- operations_admin: [sourceProposalsAwaitingApproval, sourceOverdueProjects, sourceDlpWindowsExpiring, sourceOpenIssues, sourceStaleProgress, sourceOwnBillingRequests]                       (HOME-10)
- services_admin:   [sourceProposalsAwaitingApproval, sourceOverdueServices, sourceDlpWindowsExpiring, sourceOpenIssues, sourceStaleProgress, sourceOwnBillingRequests]                       (HOME-10)
- operations_user:  [sourceMyProposalsForRevision, sourceOverdueProjects, sourceOpenIssues, sourceMyRejectedMRFs, sourceStaleProgress]                                                       (HOME-11)
- services_user:    [sourceMyProposalsForRevision, sourceOverdueServices, sourceOpenIssues, sourceMyRejectedMRFs, sourceStaleProgress]                                                       (HOME-11)
- finance:          [sourcePendingPRs, sourcePendingTRs, sourceOverdueRfpPayments, sourceBillingRequestsToDecide, sourceCollectiblesOverdue, sourceRetentionReleases]                        (HOME-12)
- procurement:      [sourceMrfsPendingProcessing, sourceAgingPOs, sourceRejectedTRs, sourceDeliveredPOsMissingProof]                                                                         (HOME-13)

<!-- The 3 seed sources to MOVE (currently home-feed.js:261-365): sourceProposalsAwaitingApproval, sourceMyProposalsForRevision, sourceMyRejectedMRFs; and the helper scopeProposalToDept (home-feed.js:249). They depend on getAgeInStageDays/isOverdueInStage (proposals.js), TYPE_META, SEVERITY, db/query/where/getDocs — none of these depend on the engine, so the move is clean. -->

<!-- services_admin decision (resolved decision #1): the approver gate inside sourceProposalsAwaitingApproval stays ['super_admin','operations_admin']. services_admin is a non-approver — the source returns [] for it harmlessly. It is LISTED in services_admin's array for symmetry (returns [] → no leak). Do NOT widen the gate, canApproveQueue, or firestore.rules. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Move the 3 seed sources into home-feed-sources.js + add ROLE_SOURCES + getSourcesForUser</name>
  <read_first>
    - app/home-feed.js lines 244-383 (the 3 seed source bodies + scopeProposalToDept + the old getSourcesForUser stub — the exact code to move)
    - app/home-feed.js lines 35-40 (imports + SEVERITY export the sources rely on)
    - app/home-feed-sources.js (Plans 02/03 output — its current imports + the 17 sources)
    - .planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md § "Registry Shape & Module Placement" (import-cycle break) and § "Per-Source Spec Table" seed rows #1-3
    - .planning/phases/108-home-per-role-attention-feeds/108-CONTEXT.md § Per-role feed definitions (confirm the role→source map)
  </read_first>
  <action>
    In `app/home-feed-sources.js`:
    1. Extend the header imports so the moved seed sources have their deps:
       - Ensure `import { getAgeInStageDays, isOverdueInStage } from './views/proposals.js';` is present (safe — proposals.js is a listener-free helper module, already imported by the engine; does NOT eager-load a heavy view).
       - SEVERITY, TYPE_META, db/query/where/getDocs are already imported (Plans 02/03).
    2. MOVE these VERBATIM from home-feed.js into home-feed-sources.js (copy the bodies unchanged; keep the `export` on the three sources):
       - `function scopeProposalToDept(role) { … }` (home-feed.js:249)
       - `export async function sourceProposalsAwaitingApproval(user) { … }` (home-feed.js:261) — KEEP its approver gate `['super_admin','operations_admin']` unchanged (resolved decision #1).
       - `export async function sourceMyProposalsForRevision(user) { … }` (home-feed.js:305)
       - `export async function sourceMyRejectedMRFs(user) { … }` (home-feed.js:338)
    3. Add the declarative registry + lookup at the bottom of home-feed-sources.js EXACTLY per the <interfaces> mapping:
       ```js
       const ROLE_SOURCES = {
         super_admin:      [ … ],   // per <interfaces>
         operations_admin: [ … ],
         services_admin:   [ … ],
         operations_user:  [ … ],
         services_user:    [ … ],
         finance:          [ … ],
         procurement:      [ … ],
       };
       export function getSourcesForUser(user) {
         if (!user) return [];
         return ROLE_SOURCES[user.role] || [];
       }
       ```
       All referenced function names must be defined in THIS file (the 17 from Plans 02/03 + the 3 moved seeds = 20). A registry that references an undefined name is a module-load ReferenceError — do a name-by-name check against the <interfaces> mapping before saving.
    4. Add a comment above services_admin's array: "sourceProposalsAwaitingApproval self-gates to [] for services_admin (non-approver, decision #1) — listed for symmetry, returns no rows."
  </action>
  <acceptance_criteria>
    - `grep -c 'export async function source' app/home-feed-sources.js` returns 20 (17 + 3 moved seeds).
    - `grep -n 'const ROLE_SOURCES' app/home-feed-sources.js` matches; `grep -c ':' ` within the ROLE_SOURCES object shows 7 role keys — verify by `grep -nE "super_admin:|operations_admin:|services_admin:|operations_user:|services_user:|finance:|procurement:" app/home-feed-sources.js` returns 7 matches inside the registry.
    - `grep -n 'return ROLE_SOURCES\[user.role\]' app/home-feed-sources.js` matches (getSourcesForUser delegates to the map).
    - `grep -n "\['super_admin', 'operations_admin'\]" app/home-feed-sources.js` matches (seed #1 approver gate unchanged — decision #1).
    - `grep -n "from './views/proposals.js'" app/home-feed-sources.js` matches (seed deps imported).
    - `grep -nE "scopeProposalToDept|sourceProposalsAwaitingApproval|sourceMyProposalsForRevision|sourceMyRejectedMRFs" app/home-feed-sources.js` shows all four moved symbols present.
    - Console: `getSourcesForUser({role:'finance'}).map(f=>f.name)` returns the 6 finance source names; `getSourcesForUser({role:'procurement'}).map(f=>f.name)` returns the 4 procurement names; `getSourcesForUser({role:'super_admin'}).length === 6`; every role in the map returns a non-empty array except unknown roles → [].
  </acceptance_criteria>
  <verify>Registry name-check passes; getSourcesForUser returns the exact per-role arrays from <interfaces> for all 7 roles; unknown role → [].</verify>
  <done>3 seed sources + scopeProposalToDept moved in; ROLE_SOURCES (7 roles) + getSourcesForUser exist and reference only defined functions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Slim home-feed.js to the pristine engine + import getSourcesForUser; verify no circular-import break</name>
  <read_first>
    - app/home-feed.js FULL FILE (know exactly what to keep vs remove)
    - app/home-feed-sources.js (confirm getSourcesForUser + the 3 seed sources now live there — do not remove from engine until confirmed present in sources)
    - .planning/phases/108-home-per-role-attention-feeds/108-RESEARCH.md § "Registry Shape & Module Placement" (one-directional engine←sources, SEVERITY leaf)
  </read_first>
  <action>
    In `app/home-feed.js`:
    1. DELETE the now-moved code: `scopeProposalToDept`, the 3 seed source bodies (sourceProposalsAwaitingApproval / sourceMyProposalsForRevision / sourceMyRejectedMRFs), and the old `getSourcesForUser` stub (home-feed.js:249-383).
    2. Remove the now-unused import `import { getAgeInStageDays, isOverdueInStage } from './views/proposals.js';` (only the moved seeds used it). Keep `import { TYPE_META } from './notifications.js';` ONLY if any remaining engine code still references it — the engine roll-up rows use icon:'' so TYPE_META is likely unused now; remove it if grep shows no remaining `TYPE_META` reference in home-feed.js. Keep the firebase import ONLY if still used (the engine itself does no getDocs — sources do; likely removable). Trim unused imports conservatively: remove an import only after grep confirms zero remaining references in home-feed.js.
    3. Add: `import { getSourcesForUser } from './home-feed-sources.js';` near the top.
    4. KEEP untouched: `export const SEVERITY`, SEVERITY_RANK, CATEGORY_LABEL, categoryListRoute, toMillis, rankItems, dedupeItems, rollUpByCategory, capItems, assembleFeed. assembleFeed's signature `assembleFeed(user, sources = getSourcesForUser(user))` now resolves getSourcesForUser via the import — do NOT change the ranking, dedupe, cap (8/25), or roll-up (≥5) logic.
    5. Circular-import note: home-feed.js imports getSourcesForUser from home-feed-sources.js, and home-feed-sources.js imports SEVERITY from home-feed.js. This 2-node cycle is benign because SEVERITY is a frozen const evaluated at engine module-init and is only READ inside source function bodies (call time), never at sources' module top-level; getSourcesForUser is only invoked at assembleFeed call time. Verify at runtime (below) — do not attempt to "fix" it by duplicating SEVERITY unless a real ReferenceError appears.
  </action>
  <acceptance_criteria>
    - `grep -c 'export async function source' app/home-feed.js` returns 0 (seed bodies removed).
    - `grep -n 'getSourcesForUser' app/home-feed.js` shows ONLY the import line (no local definition).
    - `grep -n "import { getSourcesForUser } from './home-feed-sources.js'" app/home-feed.js` matches.
    - `grep -n 'export function assembleFeed' app/home-feed.js` matches AND `grep -n 'sources = getSourcesForUser(user)' app/home-feed.js` matches (default arg preserved).
    - `grep -nE 'export function rankItems|export function dedupeItems|export function rollUpByCategory|export function capItems' app/home-feed.js` returns all 4 (engine intact).
    - `grep -n 'export const SEVERITY' app/home-feed.js` matches (leaf export retained for sources).
    - `grep -c 'onSnapshot' app/home-feed.js` returns 0 and `grep -c 'onSnapshot' app/home-feed-sources.js` returns 0 (Strategy A across both files).
    - `grep -n "from './views/proposals.js'" app/home-feed.js` returns NO match (unused import removed).
    - Console (runtime, per role): load Home signed in as each of the 7 roles (or stub getCurrentUser) and run `await assembleFeed(getCurrentUser())` — no ReferenceError / no "cannot access SEVERITY before initialization"; `.items` is an array; `.total` is a number; the feed renders. As finance, items include `pr-pending:`/`tr-pending:` dedupeKeys; as procurement, `mrf-pending:`/`po-aging:`; as super_admin, `proposal-approval:`/`user-pending:`/`project-overdue:` etc.
  </acceptance_criteria>
  <verify>Engine greps confirm rank/dedupe/rollup/cap/SEVERITY intact and seeds/stub removed; app loads with no circular-import error; assembleFeed runs per role and returns role-appropriate items.</verify>
  <done>home-feed.js is the pristine engine importing getSourcesForUser; no ReferenceError; per-role feeds are live; zero onSnapshot in both files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (authenticated user) → Firestore reads | This plan adds no new reads — it wires existing self-gating sources into a per-role registry. The registry is the access-control surface: it selects WHICH sources a role runs; each source still self-gates the DATA it returns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-02 | E (Elevation / broken access control) | ROLE_SOURCES registry | mitigate | Declarative role→source map reviewed name-by-name against the RESEARCH mapping; because every source additionally self-gates (approver gate / getAssigned*Codes / uid / full_name), even a mis-listed source returns [] rather than leaking. services_admin's inclusion of sourceProposalsAwaitingApproval is intentional and returns [] (decision #1) — no widening of canApproveQueue or firestore.rules. |
| T-108-01 | I (Information Disclosure) | getSourcesForUser | mitigate | Unknown/absent role → `ROLE_SOURCES[user.role] || []` → empty feed (fail-closed); no user → []. |
| T-108-05 | D (Availability — module-load break) | circular import / bad registry name | mitigate | Registry references only functions defined in the same file (verified by grep + console); the engine↔sources SEVERITY cycle is benign (function-body-only use) and confirmed by a runtime assembleFeed smoke test per role. |
| T-108-04 | T (Tampering) | engine + registry | accept | Read-only feed; no write paths, no Firestore rules changed. |
</threat_model>

<verification>
- getSourcesForUser returns the exact per-role source set for all 7 roles (console); unknown role → [].
- home-feed.js engine unchanged except the getSourcesForUser import (rank/dedupe/rollup/cap/SEVERITY greps intact; seed bodies + stub removed).
- App loads with no circular-import ReferenceError; assembleFeed runs per role.
- Zero onSnapshot in home-feed.js and home-feed-sources.js.
</verification>

<success_criteria>
- Each role's getSourcesForUser returns exactly its HOME-0X sources (Super Admin 6, dept admins 6, users 5, finance 6, procurement 4).
- The 3 seed sources are reused verbatim from the sources module and wired into the correct arrays.
- Engine invariants (item model, ranking, cap 8/25, roll-up ≥5, dedupe) are untouched.
- Strategy A holds: no onSnapshot, every source one batched getDocs.
</success_criteria>

<output>
After completion, create `.planning/phases/108-home-per-role-attention-feeds/108-04-SUMMARY.md` — include the final per-role getSourcesForUser name lists (all 7 roles), confirmation the engine is unchanged, the circular-import runtime verdict, and the zero-onSnapshot check. Note any browser-UAT items to confirm in-app (per-role feed render + deep-link clicks) for the phase verifier.
</output>
