---
context: phase
phase: 88-management-tab-shell-create-engagement
task: 4 of 8 (Plan 88-01 UAT awaiting user)
status: paused_at_uat_checkpoint
last_updated: 2026-05-11T00:32:17Z
---

<current_state>
Plan 88-01 (pure refactor: extract `app/engagement-create.js` and migrate `projects.js` + `services.js` call sites) is **fully committed** (4 commits). The orchestrator paused at the Plan 88-01 UAT checkpoint — user has not yet verified in the browser that the 5 existing-flow smoke tests pass. Plan 88-02 (the new Proposals tab, view module, inline form, and Draft-status consumer audit) has not started.
</current_state>

<completed_work>

This session shipped a lot — full chain through Phase 86.8 → 87 context → 88 context + plans + Plan 88-01 execution.

**Phase 86.8 (complete, 16 commits, closed in `7f932b1`):**
- 7 Gantt UX features (arrow right-click remove, collapse, parent-drag cascade, keyboard shortcuts, critical path, progress %, search/filter)
- 8 UAT-driven defect fixes (arrow tagging, parent drag, click target, grid hierarchy, search relocation, chain-only critical path, chain-drag wrong task, phantom successor cascade)
- Critical-path tooltip + memory: `feedback_root_cause_first.md`

**Phase 87 CONTEXT captured (`.planning/phases/87-proposal-lifecycle/87-CONTEXT.md`):**
- Sequencing decision: Phase 88 ships first, then 87 plugs into the existing Proposals tab
- Locked decisions: link OR single replaceable file attachment (no version history); embedded `audit_log[]` + `comms_log[]` arrays on the proposal doc; role-based approvers (Operations Admin / Super Admin); required free-text loss reason
- Memory: `project_clmc_not_authoring_platform.md` — CLMC is not an authoring platform; never design version-history or template features

**Phase 88 CONTEXT + plans:**
- `88-CONTEXT.md` — tab labeled "Proposals" (renamed from Management); nav placement between Finance and Admin; inline form pattern; default `project_status = 'Draft'`; D-08 services require client_code
- `88-01-PLAN.md` and `88-02-PLAN.md` drafted
- `88-PLAN-REVIEW.md` — verdict WARN → PASS after the **`<supersedes_from_plan_review>`** block at the top of `88-02-PLAN.md` (HIGH-1 utils.js import; HIGH-2 UAT role; MED-1 finance.js pointer + filter-only lock; MED-2 dropped dead `.status-badge.draft`; MED-3 home.js Draft palette entry)

**Plan 88-01 executed (4 commits):**
- `ca3c9d9` feat(88-01): extract engagement-create.js helper
- `688f647` refactor(88-01): projects.js delegates to createEngagement
- `3005188` refactor(88-01): services.js delegates to createEngagement
- `8c8f9ec` docs(88-01): SUMMARY

Pure refactor — same form behavior, same toast copy, same optimistic UI, same recordEditHistory shape, same Phase 78 D-04 clientless-creation handling, same Phase 85 D-09 `collection_tranches: []`.

</completed_work>

<remaining_work>

**Immediate (Plan 88-01 close):**
1. **Plan 88-01 UAT** — user verifies in browser (5 flows: project create, clientless project, service create, edit existing project, DevTools console clean). Expected outcome: byte-equivalent behavior — no defects.

**Plan 88-02 (Wave 2, depends on 88-01 UAT approval):**
2. **Task 1:** Add "Proposals" nav link to `index.html` (between Finance and Admin), update `app/auth.js` `updateNavForAuth` (lines 387-411) to show only for Super Admin, add `#/proposals` route to `app/router.js` (lines 35-116) with role gate at lines 272-285 routing pattern.
3. **Task 2:** Create `app/views/proposals.js` (render/init/destroy lifecycle) with inline "New Engagement" form section (8 fields per MGMT-06). Submit calls `createEngagement(...)` with `projectStatus: 'Draft'`. Reserve `#proposal-queue-mount` + `#proposal-dashboard-mount` divs for Phases 89 + 87. **Imports both sync helpers from `../utils.js`** (HIGH-1 fix — not from services.js).
4. **Task 3:** Draft consumer audit:
   - `app/views/procurement.js` (loadProjects lines 2262-2288) — exclude Draft from MRF project picker
   - `app/views/mrf-form.js` (loadProjects lines 1107-1129) — same
   - `app/views/finance.js` (`refreshProjectExpenses()` line 3582-3597) — return null inside `.docs.map(...)` when `project_status === 'Draft'`, then `.filter(Boolean)` — **filter only, no badge alternative** (MED-1 lock)
   - `app/views/home.js` — add `'Draft'` to `UNIFIED_STATUS_OPTIONS` (line 9-20) AND add `'Draft': 'rgba(107, 114, 128, 0.50)'` to `MONOCHROMATIC_STATUS_COLORS` (line 49) (MED-3)
   - **Do NOT add `.status-badge.draft` CSS rule** — list views don't map `project_status` to CSS class (MED-2)
5. **Task 4:** Browser UAT for Plan 88-02 (per the supersedes-corrected steps — Operations Admin can create projects directly per firestore.rules:206, so test MGMT-07 as Finance role OR replace with acknowledgement note).

**Phase close:**
6. Update `.planning/STATE.md` and `.planning/ROADMAP.md` — mark Phase 88 complete. Also rename the Phase 89 ROADMAP stub from "Management Tab — Proposal Approval Queue" → "Proposals Tab — Approval Queue" for consistency.

</remaining_work>

<decisions_made>

See `HANDOFF.json` for the full list. Highlights:
- **Phase 88 ships before Phase 87** (Phase 87 D-01).
- **Tab renamed "Management" → "Proposals"** per user during `/gsd-discuss-phase 88`.
- **CLMC is not an authoring platform** — captured in user memory `project_clmc_not_authoring_platform.md`. Future phases must not design version history, templates, or in-app document authoring.
- **Plan-checker fixes** folded into `88-02-PLAN.md` via a `<supersedes_from_plan_review>` block (top of file). Executor MUST honor that block before any conflicting language below it.

</decisions_made>

<blockers>
- **Plan 88-01 UAT** — user paused; needs browser verification before Plan 88-02 starts. Workaround: skip UAT, mark pending in 88-01-SUMMARY, and run both Plan 88-01 + 88-02 UATs together at the end of Wave 2.
</blockers>

## Required Reading (in order)

1. `.planning/phases/88-management-tab-shell-create-engagement/88-CONTEXT.md` — Phase 88 locked decisions D-01..D-08.
2. `.planning/phases/88-management-tab-shell-create-engagement/88-02-PLAN.md` — **start with the `<supersedes_from_plan_review>` block at the top.** Then Tasks 1-4 + threat_model.
3. `.planning/phases/88-management-tab-shell-create-engagement/88-PLAN-REVIEW.md` — context for the 5 plan-checker findings.
4. `.planning/phases/88-management-tab-shell-create-engagement/88-01-SUMMARY.md` — what Plan 88-01 actually shipped + adjustments to plan-specified verify gates.
5. `.planning/phases/87-proposal-lifecycle/87-CONTEXT.md` — Phase 87 decisions (Phase 88's eventual consumer; D-01 sequencing).
6. `CLAUDE.md` — project conventions (zero-build SPA, view-module structure, listener cleanup, status-string casing, window-function pattern).
7. `C:\Users\franc\.claude\projects\C--Users-franc-dev-projects-pr-po\memory\MEMORY.md` — user memory index. Read `project_clmc_not_authoring_platform.md`, `feedback_root_cause_first.md`, and `feedback_orphan_ownership_parallel_plans.md` before designing any new feature.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Re-opening locked CONTEXT decisions in PLAN | The original 88-02-PLAN.md let the executor choose "filter Draft" OR "show with Draft badge" — contradicting CONTEXT D-05's explicit "Filtering: Draft … should NOT appear in operational lists" lock. plan-checker flagged as MED-1. | advisory | When writing PLAN.md, copy locked decisions verbatim from CONTEXT.md `<decisions>` block; never re-list them as executor discretion. |
| Designing version-history / authoring features for CLMC | CLMC tracks/approves external work; it is NOT a document authoring platform. User explicitly rejected PROP-06's auto-incrementing version requirement. | advisory | Before designing any feature that touches documents, contracts, proposals, or "produced artifacts," confirm with user whether they want authoring infra or just a replaceable attachment slot. See `project_clmc_not_authoring_platform.md` in user memory. |
| Plan claims CSS visual outcome without rendering wiring | 88-02-PLAN.md originally added `.status-badge.draft` CSS + UAT step 24 verifying badge color, but no list-view template currently emits `<span class="status-badge ${project_status}">` (existing badges follow `active`/`inactive`). plan-checker flagged as MED-2. | advisory | When adding a CSS class, grep the rendering layer for a corresponding emitter; if none, either wire the renderer or drop the class. |
| Wrong import attribution (services.js vs utils.js) | Original 88-02-PLAN.md said `syncServicePersonnelToAssignments` was exported from `services.js`. Truth: it lives in `app/utils.js:717`. plan-checker HIGH-1. | advisory | When a plan references a helper, grep `^export.*helperName` to verify the source file before writing the plan. |

## Infrastructure State

- **Branch:** `v3.3` — ~25 commits ahead of `main`; not pushed; no PR. User has not requested ship-to-remote.
- **Working tree:** clean except `.claude/worktrees/` (untracked, orphan executor worktrees from earlier sessions; harmless).
- **Auto mode:** was active when paused.
- **No running dev servers / watchers.**
- **No background tasks pending.**

<context>
Auto mode was active. After Plan 88-01 (4 commits, refactor) landed cleanly and the orchestrator presented the UAT prompt, the user invoked `/gsd-pause-work` to save state.

The pure-refactor nature of Plan 88-01 means the UAT is low-risk smoke testing — the executor's verify gates confirmed `node --check` passes, `addDoc` was correctly removed from `addProject`/`addService` (now goes through `createEngagement`), and edit-history / personnel-sync / clientless-handling are preserved. Browser UAT should confirm "nothing changed" rather than "this new thing works."

The next significant work is Plan 88-02 — three auto tasks creating a new view module + nav integration + 4-file Draft-consumer audit. The plan-checker review pre-corrected five issues; the executor must read the `<supersedes_from_plan_review>` block first.
</context>

<next_action>
On resume:
1. Read `.planning/HANDOFF.json` for structured state.
2. Read this file (`.continue-here.md`).
3. Re-present the Plan 88-01 UAT prompt to the user via AskUserQuestion (options: approved / defects / skip UAT and run both UATs at end of Wave 2).
4. If approved: spawn `gsd-executor` for Plan 88-02 with a prompt that explicitly calls out the `<supersedes_from_plan_review>` block as the first thing to read.
5. After Plan 88-02 returns at its UAT checkpoint, run Plan 88-02 UAT.
6. Close phase: update STATE.md + ROADMAP.md + rename Phase 89 stub.
</next_action>
