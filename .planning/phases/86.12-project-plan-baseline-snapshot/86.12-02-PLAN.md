---
phase: 86.12
plan: 86.12-02
type: execute
wave: 2
depends_on:
  - 86.12-01
files_modified:
  - app/views/project-plan.js
  - styles/views.css
autonomous: true
requirements_addressed:
  - REQ-86.12-3
  - REQ-86.12-4
  - REQ-86.12-5

must_haves:
  truths:
    - "When a baseline is loaded, a slip summary row appears above the Gantt showing 'X behind / Y ahead / Z on track' counts"
    - "When no baseline is loaded, the slip summary row is hidden (display:none)"
    - "After every gantt.refresh(), dashed grey outline SVG rects appear at the baseline date ranges for each task that has a baseline entry"
    - "Tasks with end_date later than baseline end show a red pill badge (e.g. '+8d') beside their Gantt bar"
    - "Tasks with end_date earlier than baseline end show a green pill badge (e.g. '-3d') beside their Gantt bar"
    - "On-track tasks (end_date matches baseline end) show the dashed outline only — no badge"
    - "Bar fill colors for overdue/complete/not-started/in-progress bars are NOT changed by the baseline overlay (Phase 86.11 bar-status classes remain authoritative)"
    - "After switching Gantt zoom (Day/Week/Month), the dashed outline rects and slip badges re-appear correctly at the new scale — overlay is re-injected by setGanttZoom() after SVG rebuild"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "dateToX(), injectBaselineOverlay(), renderSlipSummary() functions; calls after applyCriticalPathStyles()"
      contains: "injectBaselineOverlay"
    - path: "styles/views.css"
      provides: "CSS rules for .gantt-baseline-outline, .gantt-slip-badge, .gantt-slip-badge-bg, .baseline-slip-summary"
      contains: "gantt-baseline-outline"
  key_links:
    - from: "renderGantt() post-render block (after step 5e applyCriticalPathStyles)"
      to: "injectBaselineOverlay() + renderSlipSummary()"
      via: "direct function calls inserted between applyCriticalPathStyles() and renderTodayLine()"
    - from: "setGanttZoom() (after applyCriticalPathStyles() call)"
      to: "injectBaselineOverlay() + renderSlipSummary()"
      via: "same re-apply pattern used by renderTodayLine/applyCriticalPathStyles on zoom rebuild"
    - from: "injectBaselineOverlay()"
      to: "_baselineData.tasks[taskId]"
      via: "for-loop over tasks[] cross-referenced against _baselineData.tasks map"
    - from: "dateToX(dateStr)"
      to: "gantt.gantt_start + ganttXPerDay()"
      via: "same coordinate math as renderTodayLine() — gantt_start anchor + day-diff * px-per-day"
    - from: ".baseline-slip-summary"
      to: "DOM element #baselineSlipSummary"
      via: "renderSlipSummary() updates innerHTML and toggles display:flex/none"
---

<objective>
Inject the baseline visual overlay into the Gantt SVG after every `gantt.refresh()` and render the slip
summary counts row. This plan adds `dateToX()`, `injectBaselineOverlay()`, and `renderSlipSummary()` to
project-plan.js, wires them into the renderGantt() post-render sequence, adds the slip summary container
to render(), and adds all required CSS rules to views.css.

Purpose: Delivers the user-visible payoff of Phase 86.12 — schedule drift is visible at a glance without
obscuring bar-color status signals from Phase 86.11.
Output: Dashed outline rects + slip badge pills in the Gantt SVG; slip count summary row above the Gantt.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\app\views\project-plan.js
@C:\Users\Admin\Roaming\pr-po\styles\views.css

<interfaces>
From app/views/project-plan.js — ganttXPerDay() (lines 2790-2795) — reuse in dateToX():

  function ganttXPerDay() {
    if (!gantt || typeof gantt.config !== 'object') return 45;
    const unitDays = { day: 1, week: 7, month: 30, year: 365 };
    const daysPerColumn = (unitDays[gantt.config.unit] || 1) * (gantt.config.step || 1);
    return (gantt.config.column_width || 45) / daysPerColumn;
  }

From app/views/project-plan.js — renderTodayLine() coordinate pattern (lines 2797-2825) — dateToX() follows
the same anchor + day-diff logic:

  const startDate = gantt.gantt_start instanceof Date ? gantt.gantt_start : new Date(gantt.gantt_start);
  startDate.setHours(0,0,0,0);
  // day-diff: Math.round((targetDate - startDate) / (1000*60*60*24))
  // x = dayDiff * ganttXPerDay()

Frappe Gantt bar row height reference from spike 014: each bar-wrapper row is approximately 38px tall
(gantt.options.bar_height default 20 + padding). The row y position for bar-wrapper[data-id="taskId"]
can be read from the .bar rect element's y attribute. For the outline rect, use the same y and height
values as the .bar rect — read them from the DOM after gantt.refresh() completes.

From app/views/project-plan.js — renderGantt() post-render insertion point (lines 2037-2062):

  // 5e. Phase 86.8 Feature 4: apply the .critical class to bars + arrows.
  applyCriticalPathStyles();

  // 6. Today line + one-shot scroll-to-today
  renderTodayLine();

Insert between 5e and 6:
  // Phase 86.12: baseline overlay (dashed outline rects + slip badges + summary row)
  injectBaselineOverlay();
  renderSlipSummary();

From app/views/project-plan.js — render() HTML (lines 118-152):
The plan-split-pane div starts at line 142:
  <div class="plan-split-pane" id="planSplitPane">

Insert immediately before this line (but inside <div class="plan-view-surface">):
  <div class="baseline-slip-summary" id="baselineSlipSummary" style="display:none"></div>

Phase 86.11 bar-status CSS classes (do NOT touch these in Plan 02):
  .gantt-container .bar-wrapper.bar-status-overdue .bar { fill: #ef4444; }
  .gantt-container .bar-wrapper.bar-status-complete .bar { fill: #059669; }
  .gantt-container .bar-wrapper.bar-status-not-started .bar { fill: #94a3b8; }
injectBaselineOverlay() MUST NOT modify .bar fill, .bar class, or bar-wrapper class attributes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add slip summary container to render() and CSS rules to views.css</name>
  <files>app/views/project-plan.js, styles/views.css</files>

  <read_first>
  Read app/views/project-plan.js lines 140-155 to see the exact line before plan-split-pane for the
  baseline-slip-summary div insertion.
  Read styles/views.css lines 2779-2802 (end of PHASE 86 FRAPPE GANTT OVERLAYS section through to the
  toolbar checkbox comment) to confirm the CSS insertion point — new rules go after the last critical-path
  rule and before the toolbar checkbox comment.
  </read_first>

  <action>
  CHANGE 1 — render() HTML (app/views/project-plan.js).
  Locate `<div class="plan-split-pane" id="planSplitPane">` in the render() return string (around line 142).
  Insert the following line immediately above it (same indentation level):
    <div class="baseline-slip-summary" id="baselineSlipSummary" style="display:none"></div>

  CHANGE 2 — CSS insertion (styles/views.css).
  After the last line of the Phase 86 FRAPPE GANTT OVERLAYS section —
  `.gantt-container g.arrow path.critical { stroke: #ef4444; stroke-width: 2; }` —
  and before the `/* Toolbar checkbox for critical-path toggle */` comment block, insert:

    /* Phase 86.12 — baseline overlay: dashed outline rect + slip badge pill */
    .gantt-baseline-outline {
        fill: none;
        stroke: #94a3b8;
        stroke-width: 2;
        stroke-dasharray: 4 3;
        pointer-events: none;
    }
    .gantt-slip-badge {
        font-size: 10px;
        font-weight: 600;
        pointer-events: none;
    }
    .gantt-slip-badge-bg {
        pointer-events: none;
    }
    /* Slip summary row above the Gantt */
    .baseline-slip-summary {
        padding: 4px 12px;
        font-size: 12px;
        color: #64748b;
        background: #f8fafc;
        border-bottom: 1px solid #e2e8f0;
        display: flex;
        gap: 16px;
        align-items: center;
    }
    .baseline-slip-summary .slip-behind { color: #ef4444; font-weight: 600; }
    .baseline-slip-summary .slip-ahead  { color: #059669; font-weight: 600; }
    .baseline-slip-summary .slip-label  { color: #64748b; }

  Do not modify any existing CSS rules.
  </action>

  <acceptance_criteria>
  - `id="baselineSlipSummary"` element is present in the render() HTML string with `style="display:none"` and class `baseline-slip-summary`, positioned immediately above `<div class="plan-split-pane"`
  - `.gantt-baseline-outline` CSS rule exists in views.css with `stroke: #94a3b8`, `stroke-dasharray: 4 3`, `fill: none`, `pointer-events: none`
  - `.gantt-slip-badge` CSS rule exists with `font-size: 10px` and `font-weight: 600`
  - `.baseline-slip-summary` CSS rule exists with `display: flex` and `border-bottom: 1px solid #e2e8f0`
  - `.baseline-slip-summary .slip-behind` has `color: #ef4444`
  - `.baseline-slip-summary .slip-ahead` has `color: #059669`
  - Existing FRAPPE GANTT OVERLAYS section rules are unmodified
  </acceptance_criteria>

  <verify>
    <automated>node -e "const fs=require('fs'); const css=fs.readFileSync('styles/views.css','utf8'); const js=fs.readFileSync('app/views/project-plan.js','utf8'); const checks=[css.includes('gantt-baseline-outline'),css.includes('stroke-dasharray: 4 3'),css.includes('baseline-slip-summary'),css.includes('slip-behind'),css.includes('slip-ahead'),js.includes('baselineSlipSummary'),js.includes('display:none')]; const fail=checks.findIndex(c=>!c); if(fail>=0){console.error('Check '+fail+' failed');process.exit(1);}console.log('All checks pass');"</automated>
  </verify>

  <done>
  Slip summary container div is in the rendered HTML (hidden by default). All CSS classes required by
  the overlay and summary row are defined in views.css.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement dateToX(), injectBaselineOverlay(), renderSlipSummary(); wire into renderGantt()</name>
  <files>app/views/project-plan.js</files>

  <read_first>
  Read app/views/project-plan.js lines 2030-2065 (renderGantt() post-render block) to see the exact
  insertion point between applyCriticalPathStyles() at step 5e and renderTodayLine() at step 6.
  Read lines 2790-2825 (ganttXPerDay() and renderTodayLine()) to understand the coordinate math pattern
  that dateToX() must replicate.
  </read_first>

  <action>
  CHANGE 1 — Wire overlay calls in renderGantt() post-render block AND in setGanttZoom().

  Step A — renderGantt() insertion:
  Locate the comment `// 6. Today line + one-shot scroll-to-today` and the `renderTodayLine();` call
  immediately below it (around line 2041). Insert the following two lines between the
  `applyCriticalPathStyles();` call (step 5e) and the `// 6. Today line` comment:

    // Phase 86.12: baseline overlay — dashed outline rects, slip badge pills, summary row
    injectBaselineOverlay();
    renderSlipSummary();

  Step B — setGanttZoom() insertion:
  Locate setGanttZoom() (around line 2389). After the `applyCriticalPathStyles();` call (last line of
  the function body, around line 2410), insert before the closing `}`:

    // Phase 86.12: re-apply baseline overlay after Frappe SVG rebuild on zoom change
    injectBaselineOverlay();
    renderSlipSummary();

  Rationale: `gantt.change_view_mode()` rebuilds the SVG from scratch, erasing all injected overlay
  elements. Every other SVG overlay (renderTodayLine, applyCriticalPathStyles) is explicitly re-called
  inside setGanttZoom() — baseline overlay must follow the same pattern (checker blocker B1).

  CHANGE 2 — Add the three new functions.
  Add all three as module-level functions, located after ganttXPerDay() (around line 2795) and before
  renderTodayLine() (around line 2797). This keeps all SVG overlay helpers together.

  --- dateToX(dateStr) ---
  Converts a YYYY-MM-DD string to an SVG x-coordinate using gantt.gantt_start as the anchor.
  Returns 0 (fail-open) if gantt is null or dateStr is falsy.
  Logic mirrors renderTodayLine():
    if (!gantt || !dateStr) return 0;
    const anchor = gantt.gantt_start instanceof Date ? gantt.gantt_start : new Date(gantt.gantt_start);
    anchor.setHours(0,0,0,0);
    const d = new Date(dateStr + 'T00:00:00');
    d.setHours(0,0,0,0);
    return Math.round((d - anchor) / (1000*60*60*24)) * ganttXPerDay();
  Wrap in try/catch, return 0 on error.

  --- injectBaselineOverlay() ---
  Injects dashed outline rects and slip badge pills into the Gantt SVG for each task that has a
  baseline entry in _baselineData.
  Early return conditions (any one → return immediately with no DOM mutation):
    - !_baselineData or !_baselineData.tasks
    - gantt is null
    - document.querySelector('#ganttPane svg') returns null
  Main logic:
    const ns = 'http://www.w3.org/2000/svg';
    const ganttSvg = document.querySelector('#ganttPane svg');
    // Remove previous overlay elements
    ganttSvg.querySelectorAll('.gantt-baseline-outline, .gantt-slip-badge, .gantt-slip-badge-bg').forEach(el => el.remove());
    // For each task, find its bar-wrapper in the SVG, compute baseline x coords, inject rect
    for (const task of tasks) {
      const tid = task.task_id;
      const bl = _baselineData.tasks[tid];
      if (!bl || !bl.start || !bl.end) continue;
      const wrapper = ganttSvg.querySelector(`.bar-wrapper[data-id="${CSS.escape(tid)}"]`);
      if (!wrapper) continue;
      // Read bar y and height from the inner .bar rect element (already rendered by Frappe)
      const barRect = wrapper.querySelector('.bar');
      if (!barRect) continue;
      const barY = parseFloat(barRect.getAttribute('y') || '0');
      const barH = parseFloat(barRect.getAttribute('height') || '20');
      // Compute baseline x extents
      const x1 = dateToX(bl.start);
      const x2 = dateToX(bl.end);
      if (x2 <= x1) continue; // degenerate range — skip
      // Inject dashed outline rect
      const rect = document.createElementNS(ns, 'rect');
      rect.setAttribute('class', 'gantt-baseline-outline');
      rect.setAttribute('x', x1);
      rect.setAttribute('y', barY);
      rect.setAttribute('width', x2 - x1);
      rect.setAttribute('height', barH);
      rect.setAttribute('rx', '3');
      ganttSvg.appendChild(rect);
      // Compute slip badge — compare current end_date vs baseline end
      if (!task.end_date) continue;
      const curEnd = new Date(task.end_date + 'T00:00:00');
      const blEnd  = new Date(bl.end + 'T00:00:00');
      curEnd.setHours(0,0,0,0);
      blEnd.setHours(0,0,0,0);
      const diffDays = Math.round((curEnd - blEnd) / (1000*60*60*24));
      if (diffDays === 0) continue; // on-track — outline only, no badge
      // Badge text and color
      const badgeText = diffDays > 0 ? `+${diffDays}d` : `${diffDays}d`;
      const badgeColor = diffDays > 0 ? '#ef4444' : '#059669';
      const badgeBg    = diffDays > 0 ? '#fee2e2' : '#d1fae5';
      // Position badge to the right of the current bar's end x (not baseline)
      const curX2 = dateToX(task.end_date);
      const bx = curX2 + 4;
      const by = barY + barH / 2;
      // Background rect
      const badgeWidth = badgeText.length * 6 + 6;
      const bg = document.createElementNS(ns, 'rect');
      bg.setAttribute('class', 'gantt-slip-badge-bg');
      bg.setAttribute('x', bx);
      bg.setAttribute('y', by - 8);
      bg.setAttribute('width', badgeWidth);
      bg.setAttribute('height', 16);
      bg.setAttribute('rx', '3');
      bg.setAttribute('fill', badgeBg);
      ganttSvg.appendChild(bg);
      // Text label
      const text = document.createElementNS(ns, 'text');
      text.setAttribute('class', 'gantt-slip-badge');
      text.setAttribute('x', bx + badgeWidth / 2);
      text.setAttribute('y', by + 4);
      text.setAttribute('text-anchor', 'middle');
      text.setAttribute('fill', badgeColor);
      text.textContent = badgeText;
      ganttSvg.appendChild(text);
    }
  Wrap entire loop body in try/catch per-task so one bad task cannot abort the rest; log errors silently.
  Wrap the whole function in an outer try/catch; on outer error log `[Plan] injectBaselineOverlay error:` + error.

  --- renderSlipSummary() ---
  Updates the #baselineSlipSummary element with behind/ahead/on-track counts.
  Early return: if !_baselineData or !_baselineData.tasks, find the element and set display='none', return.
  Count logic (iterate tasks[]):
    let behind = 0, ahead = 0, onTrack = 0;
    for each task: look up _baselineData.tasks[task.task_id]; if no entry skip; if !task.end_date skip;
    compute diffDays = (new Date(task.end_date) - new Date(bl.end)) / (1000*60*60*24) rounded;
    if diffDays > 0 → behind++; if diffDays < 0 → ahead++; else → onTrack++;
  Build innerHTML:
    `<span class="slip-behind">${behind} behind</span>
     <span class="slip-ahead">${ahead} ahead</span>
     <span class="slip-label">${onTrack} on track</span>
     <span class="slip-label" style="margin-left:auto;color:#94a3b8;">Baseline: ${escapeHTML(_baselineData.label || '')}</span>`
  Set el.style.display = 'flex'.
  If the element is not found in the DOM (e.g., renderSlipSummary called before DOM is ready), return silently.
  Wrap in try/catch; on error log `[Plan] renderSlipSummary error:` and hide the element.
  </action>

  <acceptance_criteria>
  - `injectBaselineOverlay();` and `renderSlipSummary();` appear in renderGantt() between applyCriticalPathStyles() and renderTodayLine()
  - `injectBaselineOverlay();` and `renderSlipSummary();` also appear in setGanttZoom() after the `applyCriticalPathStyles();` call (last overlay re-apply in that function)
  - `function dateToX(dateStr)` is defined at module level using gantt.gantt_start and ganttXPerDay()
  - `function injectBaselineOverlay()` is defined at module level; it queries `.bar-wrapper[data-id]` for each task; appends `rect.gantt-baseline-outline` to the SVG; appends `text.gantt-slip-badge` and `rect.gantt-slip-badge-bg` for non-zero slip
  - `function renderSlipSummary()` is defined at module level; it counts behind/ahead/onTrack; sets #baselineSlipSummary innerHTML and display:flex, or hides if no _baselineData
  - injectBaselineOverlay() calls `ganttSvg.querySelectorAll('.gantt-baseline-outline, .gantt-slip-badge, .gantt-slip-badge-bg').forEach(el => el.remove())` at the start to clean up previous overlay before re-injecting
  - injectBaselineOverlay() does NOT set fill on .bar elements, does NOT add/remove bar-status-* classes, does NOT call applyCriticalPathStyles() — bar color logic is untouched
  - dateToX() returns 0 when gantt is null (fail-open; does not throw)
  - On-track tasks (diffDays === 0) get outline rect only — no badge appended
  </acceptance_criteria>

  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('app/views/project-plan.js','utf8'); const checks=[src.includes('injectBaselineOverlay()'),src.includes('renderSlipSummary()'),src.includes('function dateToX'),src.includes('function injectBaselineOverlay'),src.includes('function renderSlipSummary'),src.includes('gantt-baseline-outline'),src.includes('gantt-slip-badge'),src.includes('gantt-slip-badge-bg'),src.includes('baselineSlipSummary')]; const fail=checks.findIndex(c=>!c); if(fail>=0){console.error('Check '+fail+' failed: '+['injectOverlayCall','slipSummaryCall','dateToX','injectBaselineOverlay','renderSlipSummary','outline-class','badge-class','badge-bg-class','slipSummary-el'][fail]);process.exit(1);}console.log('All checks pass');"</automated>
  </verify>

  <done>
  After every gantt.refresh(), dashed outline rects appear at baseline date ranges and slip badges
  appear for tasks with drift. The slip summary row shows behind/ahead/on-track counts. Bar fill colors
  are unchanged — baseline overlay is additive SVG-only, not modifying Frappe bar classes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore read → SVG DOM | Baseline task data (date strings) from Firestore is used to compute SVG coordinates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.12-04 | Tampering | SVG coordinate injection | accept | dateToX() inputs are date strings from Firestore (read by active users only); output is a numeric x-coordinate used only as SVG attributes — no innerHTML or eval paths |
| T-86.12-05 | Information Disclosure | baseline label in slip summary | mitigate | Label rendered via escapeHTML() before inserting into innerHTML |
| T-86.12-06 | Denial of Service | malformed date strings in _baselineData.tasks | mitigate | try/catch per-task in injectBaselineOverlay(); dateToX() returns 0 on parse error; Gantt rendering continues even if overlay fails |
</threat_model>

<verification>
1. Open a project plan with at least 3 tasks that have start/end dates.
2. Click "Set Baseline" — confirm success toast.
3. Modify one task's end date (drag the Gantt bar or edit the date field) to be later than its baseline end date.
4. Confirm a red pill badge (e.g. "+3d") appears beside that task's bar after the Gantt re-renders.
5. Modify another task's end date to be earlier than baseline — confirm a green pill (e.g. "-2d") appears.
6. Leave a third task unchanged — confirm it shows a dashed outline rect only, no badge.
7. Confirm the slip summary row above the Gantt shows non-zero counts matching the visible badges.
8. Confirm overdue/complete/not-started bar fill colors from Phase 86.11 are unchanged — the baseline overlay does not alter bar color.
9. Switch Gantt zoom (Day → Week → Month) — confirm overlay re-renders correctly at the new scale.
</verification>

<success_criteria>
- Dashed outline rects appear at baseline date ranges for all tasks with baseline entries
- Slip badge pills appear beside bars with drift (red for late, green for early); on-track bars show outline only
- Slip summary row shows "X behind / Y ahead / Z on track" with correct counts
- Bar colors (overdue=red, complete=green, not-started=gray, in-progress=blue) are unaffected by the overlay
- Overlay is re-injected correctly after every gantt.refresh() (zoom change, data update, etc.)
</success_criteria>

<output>
Create `.planning/phases/86.12-project-plan-baseline-snapshot/86.12-02-SUMMARY.md` when done.
</output>
