---
phase: 86.12-project-plan-baseline-snapshot
plan: 86.12-01
subsystem: database
tags: [firestore, firestore-rules, project-plan, baseline, gantt]

# Dependency graph
requires:
  - phase: 86.11-project-plan-milestone-ux-overdue-status-visual
    provides: project-plan.js toolbar structure and window function wiring pattern
provides:
  - baselines subcollection Firestore security rules (read/create/update/delete)
  - loadBaseline() function — fetches latest baseline doc into _baselineData
  - saveBaseline() function — snapshots task dates to Firestore baselines subcollection
  - _baselineData module-scope state variable (null-safe, cleared on destroy)
  - "Set Baseline" toolbar button wired to window.saveBaseline()
affects:
  - 86.12-02 (visual overlay will consume _baselineData)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Immutable subcollection pattern: allow update: if false to prevent baseline tampering"
    - "Baseline snapshot keyed by task_id with start/end string fields"
    - "loadBaseline() called in init() after guard passes; _baselineData cleared in destroy()"

key-files:
  created: []
  modified:
    - firestore.rules
    - app/views/project-plan.js

key-decisions:
  - "Baselines are immutable: allow update: if false enforced at Firestore rules level"
  - "Label derived from existing doc count + 1 (Baseline 1, Baseline 2, ...)"
  - "saveBaseline() refreshes _baselineData in-memory immediately after write via loadBaseline()"
  - "orderBy created_at desc + limit(1) used in loadBaseline() to always fetch the most recent baseline"

patterns-established:
  - "Baseline subcollection pattern: projects/{id}/baselines/{autoId} with label, created_at, tasks map"

requirements-completed:
  - REQ-86.12-1
  - REQ-86.12-2
  - REQ-86.12-6

# Metrics
duration: 12min
completed: 2026-05-29
---

# Phase 86.12 Plan 01: Baseline Snapshot Data Foundation Summary

**Firestore baselines subcollection rules added plus saveBaseline()/loadBaseline() data layer wired into project-plan.js toolbar with _baselineData state management**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-29T00:00:00Z
- **Completed:** 2026-05-29T00:12:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added `match /baselines/{baselineId}` security rules nested inside `match /projects/{projectId}` — read for all active users, create/delete for super_admin + operations_admin, update blocked (immutable)
- Implemented `saveBaseline()` — builds tasks map from current `tasks[]` array, counts existing baselines for sequential label, writes to Firestore, refreshes `_baselineData` in memory, shows toast
- Implemented `loadBaseline()` — queries latest baseline by created_at desc with limit(1), populates `_baselineData`, null-safe with try/catch
- Added `_baselineData` module-scope state variable, cleared in `destroy()`, populated in `init()` via `await loadBaseline()` after clientless guard
- Added "Set Baseline" toolbar button (`plan-export-btn plan-baseline-btn`) wired to `window.saveBaseline()`

## Task Commits

Each task was committed atomically:

1. **Task 1: Add baselines subcollection Security Rules to firestore.rules** - `e10105e` (feat)
2. **Task 2: Add _baselineData state, saveBaseline(), loadBaseline(), toolbar button, init/destroy wiring** - `68abdc4` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `firestore.rules` - Added baselines subcollection block inside projects match block
- `app/views/project-plan.js` - Added orderBy/limit imports, _baselineData state, loadBaseline(), saveBaseline(), toolbar button, init() wiring, destroy() cleanup

## Decisions Made
- Used `orderBy('created_at', 'desc'), limit(1)` in loadBaseline() to always fetch the most recent baseline efficiently
- Label derived from `countSnap.size + 1` (sequential: Baseline 1, Baseline 2) for user-friendly naming
- saveBaseline() calls loadBaseline() immediately after addDoc() so _baselineData is fresh for Plan 02 overlay rendering
- Button styled with existing `plan-export-btn` class plus new `plan-baseline-btn` class for targeted override in Plan 02 if needed

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `_baselineData` is now populated in memory whenever a baseline exists for the loaded project
- Plan 02 can directly access `_baselineData.tasks` to render the visual baseline overlay on the Gantt
- Firestore rules are in place; deploy with `firebase deploy --only firestore:rules` before browser UAT

---
*Phase: 86.12-project-plan-baseline-snapshot*
*Completed: 2026-05-29*
