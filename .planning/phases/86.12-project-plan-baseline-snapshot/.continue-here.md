---
context: phase
phase: 86.12-project-plan-baseline-snapshot
task: UAT
total_tasks: UAT
status: blocked_on_ux_fix
last_updated: 2026-05-29T10:25:25Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: UX_GAPS_FOUND — UAT revealed two UX issues that must be fixed before phase can be closed. Do NOT mark phase UAT-complete until both are resolved and re-tested.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Closing phase before UX gaps resolved | Phase is code-complete but UAT discovered real usability issues; closing without fixing would ship broken UX | blocking | Fix both UX issues first, re-run UAT checks 1–7, then close |

<current_state>
Phase 86.12 is **code-complete** (both plans executed, code review passed, verification 13/13). UAT was in progress when the user discovered two UX gaps during browser testing:

1. **No single-baseline model**: Clicking "Set Baseline" creates Baseline 1, Baseline 2, etc. Each new click replaces the visible baseline (loadBaseline always loads latest) but old baselines accumulate silently in Firestore with no UI to access them. User expected overwrite behaviour.
2. **No "Clear Baseline" capability**: Once a baseline is set, there is no way to dismiss/disable the overlay. No button, no toggle.

The user paused before agreeing on the fix approach. The AskUserQuestion was rejected (user invoked /gsd-pause-work instead).
</current_state>

<completed_work>

Completed Tasks:
- Plan 01 (data layer): saveBaseline(), loadBaseline(), _baselineData state, Firestore subcollection, security rules — Done (commits f6da826, 62f9ebd, af22e19, 06651a0)
- Plan 02 (visual overlay): dateToX(), injectBaselineOverlay(), renderSlipSummary(), CSS rules, wired into renderGantt() + setGanttZoom() — Done
- Code review (86.12-REVIEW.md): WR-01 and WR-02 fixed in commit 6a2b685
- Verification (86.12-VERIFICATION.md): 13/13 truths verified, human_needed status with 7 browser UAT items
- UAT started: user opened project plan, clicked Set Baseline — overlay and toast confirmed working
</completed_work>

<remaining_work>

1. **Fix UX gap 1 — Single baseline model**: When "Set Baseline" is clicked and a baseline already exists, overwrite it (delete old subcollection docs and write one new one) rather than accumulating. OR confirm with user if they want history kept with a selector.
2. **Fix UX gap 2 — Clear Baseline button**: Add ability to dismiss/disable the baseline overlay. Proposed approach: toggle the toolbar button — reads "Set Baseline" when none loaded, "Clear Baseline" when one is active.
3. **Re-run UAT checks 1–7** after the above fixes.
4. **Close phase**: commit, mark done in ROADMAP.md.
</remaining_work>

<decisions_made>

- Baseline overlay is additive SVG only — never modifies bar fills (Phase 86.11 colours remain authoritative)
- Badge positioned at curX2 + 4px (right of current bar end)
- On-track tasks (diffDays === 0) show outline only, no badge
- Per-task try/catch in injectBaselineOverlay() prevents one bad date from aborting the full render
- WR-01 fix: injectBaselineOverlay + renderSlipSummary called immediately after saveBaseline() completes (not waiting for next snapshot)
- WR-02 fix: dateToX() clones gantt.gantt_start via new Date(gantt.gantt_start.getTime()) to prevent mutation

**UNDECIDED (needs user input before implementing fix):**
- Single baseline (overwrite) vs multi-baseline with selector vs keep-history-latest-only
- Clear UX: toggle button vs separate always-visible button vs context-menu
</decisions_made>

<blockers>
- UX design decision pending: user needs to choose single-baseline vs multi-baseline model and clear-baseline UX pattern before the fix can be implemented.
</blockers>

## Required Reading (in order)
1. `.planning/phases/86.12-project-plan-baseline-snapshot/86.12-02-SUMMARY.md` — full implementation details for the overlay system
2. `.planning/phases/86.12-project-plan-baseline-snapshot/86.12-VERIFICATION.md` — 7 browser UAT checks still outstanding

## Infrastructure State
- Dev server: not running (needs `python -m http.server 8000`)
- Firebase: production (no staging)
- Firestore `baselines` subcollection rules: deployed and verified

<context>
Session started from UAT continuation. User confirmed "Set Baseline" button worked and overlay appeared. During UAT they clicked the button twice, noticed Baseline 2 replaced Baseline 1 with no history or toggle, and asked how to disable baseline. These are real UX gaps — single-baseline overwrite and a clear/toggle button are the minimal fixes needed. The question of approach (toggle vs two buttons, overwrite vs history) was asked but user paused before answering.
</context>

<next_action>
Resume by asking user two design questions:
1. Baseline model: single (overwrite) vs multi with selector?
2. Clear UX: toggle button (Set Baseline ↔ Clear Baseline) vs separate "Clear Baseline" button?

Then implement the fix via /gsd:quick, re-run UAT items 1–7, and close the phase.
</next_action>
