---
plan: 86.11-03
phase: 86.11
wave: 3
depends_on:
  - 86.11-02
files_modified:
  - app/views/project-plan.js
  - styles/views.css
autonomous: true
requirements_addressed:
  - REQ-86.11-4

must_haves:
  truths:
    - "Overdue leaf task Gantt bars render in red (#ef4444)"
    - "Complete leaf task Gantt bars render in green (#059669)"
    - "Not-started leaf task Gantt bars render in gray (#94a3b8)"
    - "In-progress leaf task Gantt bars render in blue (#3b82f6, default Frappe color — no class assigned)"
    - "Milestone Gantt bars remain amber diamonds (milestone-marker class unchanged)"
    - "Parent summary bars remain charcoal (parent-summary-bar class unchanged)"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "Status bar color customClass logic in renderGantt()"
      contains: "bar-status-overdue"
    - path: "styles/views.css"
      provides: "bar-status-overdue, bar-status-complete, bar-status-not-started CSS fill rules"
      contains: "bar-status-overdue"
  key_links:
    - from: "renderGantt() appendNode() customClass block (line 1922)"
      to: "bar-status-overdue / bar-status-complete / bar-status-not-started"
      via: "computeStatus(t, today) in else branch after milestone and parent guards"
    - from: ".bar-wrapper.bar-status-overdue .bar"
      to: "SVG rect fill #ef4444"
      via: "CSS fill property on Frappe bar element"
---

<objective>
Extend Gantt bar rendering in `renderGantt()` to assign status-based CSS classes to leaf bars, then add the three bar color rules to views.css so overdue/complete/not-started bars display in red/green/gray.
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\86.11-project-plan-milestone-ux-overdue-status-visual\86.11-03-PLAN.md
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\app\views\project-plan.js
@C:\Users\Admin\Roaming\pr-po\styles\views.css

<interfaces>
From app/views/project-plan.js — renderGantt() appendNode() customClass block (lines 1922–1924):

  let customClass = '';
  if (t.is_milestone) customClass = 'milestone-marker';
  else if (isParent) customClass = 'parent-summary-bar';

This is the exact block to extend. A new `else` branch must be appended after the `else if (isParent)` line. It must:
  1. Compute today (one line: `const today = new Date().toISOString().slice(0, 10)`) at the TOP of appendNode() before the customClass block — or compute it once at the top of renderGantt() and use it inside appendNode via closure
  2. Call computeStatus(t, today) for leaf tasks
  3. Map the status string to a CSS class: 'overdue' → 'bar-status-overdue', 'complete' → 'bar-status-complete', 'not-started' → 'bar-status-not-started', 'in-progress' → '' (no class; Frappe default blue applies)

IMPORTANT: `today` should be computed ONCE at the start of renderGantt() (before appendNode is defined) and used inside appendNode via closure. Computing it inside appendNode per-task is wasteful but not incorrect. However, computing once outside is cleaner and consistent with Plan 01's pattern.

appendNode is defined as a nested function inside renderGantt() at line 1913. It receives `t` (a task object). `isParent` is already declared inside appendNode at line 1914.

Frappe's `custom_class` field is set at line 1941:
  frappeTasks.push({ ..., custom_class: customClass });

Frappe adds `custom_class` to the `.bar-wrapper` SVG group element as an additional CSS class. So `.bar-wrapper.bar-status-overdue` is the correct selector for the status bar CSS rules.

From styles/views.css — insertion point for bar status CSS:

The PHASE 86 FRAPPE GANTT OVERLAYS section ends at line 2786 (after `.gantt-container g.arrow path.critical { stroke: #ef4444; stroke-width: 2; }`). Insert the three new bar status rules after line 2786, before the `/* Toolbar checkbox...` comment block.

Existing pattern for bar fill override:
  .gantt-container .bar-wrapper.critical .bar { fill: #ef4444; }
Use the same selector pattern: `.gantt-container .bar-wrapper.bar-status-overdue .bar { fill: #ef4444; }`

Do NOT add a bar-status-in-progress rule — in-progress leaf tasks use Frappe's default bar color (#3b82f6) with no class override.
Do NOT modify milestone-marker or parent-summary-bar CSS — those are already correct.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend renderGantt() customClass logic with status bar classes</name>
  <files>app/views/project-plan.js</files>

  <read_first>
  Read app/views/project-plan.js lines 1905–1945 to see the full renderGantt() appendNode() function and the exact customClass block at lines 1922–1924.
  Confirm computeStatus() was added by Plan 01 (grep for "function computeStatus" — should exist as a module-level function).
  </read_first>

  <action>
  Step 1 — Compute today once in renderGantt().

  Locate the `function renderGantt()` function declaration (around line 1855). Near the top of the function body, before the `const frappeTasks = [];` line (around line 1911), add: `const today = new Date().toISOString().slice(0, 10);`. This variable will be available to appendNode() via closure.

  Step 2 — Add the else branch for status-based customClass.

  In the appendNode() nested function, find these three lines (around 1922–1924):
    let customClass = '';
    if (t.is_milestone) customClass = 'milestone-marker';
    else if (isParent) customClass = 'parent-summary-bar';

  Append a new `else` block immediately after the `else if (isParent)` line. Inside the else:
    - Call computeStatus(t, today) and store in a local const, e.g. `const status = computeStatus(t, today);`
    - Use an object map or if/else chain to assign customClass:
      - status 'overdue'     → customClass = 'bar-status-overdue'
      - status 'complete'    → customClass = 'bar-status-complete'
      - status 'not-started' → customClass = 'bar-status-not-started'
      - status 'in-progress' → customClass = '' (already '', no assignment needed)
    - An object lookup `const map = { overdue: 'bar-status-overdue', complete: 'bar-status-complete', 'not-started': 'bar-status-not-started' }; customClass = map[status] || '';` is preferred for readability.

  The milestone and parent branches are UNTOUCHED. The new else block only fires when the task is not a milestone AND not a parent — i.e., it is a leaf task.

  No other changes to renderGantt().
  </action>

  <acceptance_criteria>
  - `const today = new Date().toISOString().slice(0, 10);` appears inside renderGantt() before appendNode is called
  - The customClass block now has three branches: is_milestone → 'milestone-marker', isParent → 'parent-summary-bar', else → status map lookup
  - Milestone tasks still get 'milestone-marker' only (the if branch short-circuits before the else)
  - Parent tasks still get 'parent-summary-bar' only
  - Leaf tasks get the correct bar-status-* class from computeStatus()
  - In-progress leaf tasks get customClass = '' (no bar-status class, default Frappe blue)
  </acceptance_criteria>

  <verify>
  Browser: Open a project plan. Open DevTools. In the Gantt SVG, inspect a bar-wrapper element for an overdue task — confirm it has class "bar-wrapper bar-status-overdue" (among other Frappe classes). Inspect a complete task bar — confirm "bar-status-complete". Inspect a milestone — confirm "milestone-marker" only, no bar-status-* class. Inspect a parent bar — confirm "parent-summary-bar" only.
  </verify>

  <done>renderGantt() appendNode() assigns bar-status-* classes to leaf tasks via computeStatus(), with milestone and parent branches unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add bar status fill CSS rules to views.css</name>
  <files>styles/views.css</files>

  <read_first>
  Read styles/views.css lines 2779–2800 (end of PHASE 86 FRAPPE GANTT OVERLAYS section) to confirm the insertion point after the critical-path rules and before the toolbar checkbox comment.
  </read_first>

  <action>
  After the `.gantt-container g.arrow path.critical { stroke: #ef4444; stroke-width: 2; }` rule (around line 2786) and before the `/* Toolbar checkbox for critical-path toggle */` comment, insert a new comment and three CSS rules:

  Comment: `/* Phase 86.11 — bar status fills (leaf tasks only; milestone + parent bars untouched) */`

  Rule 1: `.gantt-container .bar-wrapper.bar-status-overdue .bar { fill: #ef4444; }` — red for overdue tasks.

  Rule 2: `.gantt-container .bar-wrapper.bar-status-complete .bar { fill: #059669; }` — green for complete tasks.

  Rule 3: `.gantt-container .bar-wrapper.bar-status-not-started .bar { fill: #94a3b8; }` — gray for not-started tasks.

  Do NOT add a rule for bar-status-in-progress — in-progress tasks have no class and keep Frappe's default blue.
  Do NOT add bar-progress fill overrides — only the base `.bar` rect fill is changed.
  Do NOT add pointer-events or handle changes — bar dragging behavior is unchanged.
  </action>

  <acceptance_criteria>
  - `.gantt-container .bar-wrapper.bar-status-overdue .bar { fill: #ef4444; }` exists in views.css
  - `.gantt-container .bar-wrapper.bar-status-complete .bar { fill: #059669; }` exists in views.css
  - `.gantt-container .bar-wrapper.bar-status-not-started .bar { fill: #94a3b8; }` exists in views.css
  - No bar-status-in-progress or bar-status-milestone rules added
  - Rules appear inside the PHASE 86 FRAPPE GANTT OVERLAYS section (before the toolbar checkbox comment)
  </acceptance_criteria>

  <verify>
  Browser: Open a project plan. Find an overdue task's Gantt bar — it must render in red (#ef4444). Find a complete task's bar — it must render in green (#059669). Find a not-started task's bar — it must render in gray (#94a3b8). Find a milestone bar — it must render as an amber diamond (unchanged). Find a parent summary bar — charcoal strip (unchanged). Find an in-progress task bar — blue (Frappe default, unchanged).
  </verify>

  <done>Three bar-status CSS fill rules exist in views.css. Overdue/complete/not-started leaf bars render in the correct colors. All other bar types (milestone, parent, in-progress) are visually unchanged.</done>
</task>

</tasks>

<verification>
Browser test sequence for Plan 03:
1. Load a project plan page.
2. Identify at least one task in each state. Verify in DevTools Network / console that tasks exist with:
   - end_date before today, progress &lt; 100 (overdue)
   - progress = 100 (complete)
   - start_date after today, progress = 0 (not-started)
   - start_date <= today, progress > 0 &lt; 100 (in-progress)
   - is_milestone: true (milestone)
   - has children (parent)
3. Overdue bar: red (#ef4444) fill visible in Gantt. Bar-wrapper has class bar-status-overdue.
4. Complete bar: green (#059669) fill. Bar-wrapper has class bar-status-complete.
5. Not-started bar: gray (#94a3b8) fill. Bar-wrapper has class bar-status-not-started.
6. In-progress bar: blue (Frappe default). No bar-status-* class on bar-wrapper.
7. Milestone bar: amber diamond. bar-wrapper has class milestone-marker only, no bar-status-*.
8. Parent bar: charcoal strip. bar-wrapper has class parent-summary-bar only.
9. Marking an in-progress task as complete (set progress to 100) — bar turns green after re-render.
10. DevTools console: no JS errors.
</verification>

<success_criteria>
- renderGantt() appendNode() has three-branch customClass logic: milestone / parent / status map
- today computed once inside renderGantt() via closure
- computeStatus() reused from Plan 01 with no duplication
- Three bar-status fill rules in views.css with exact hex values per spec
- Gantt bars correctly colored by status for leaf tasks
- Milestone and parent bar visuals unchanged
- Full phase REQ-86.11-1 through REQ-86.11-4 satisfied across Plans 01–03
</success_criteria>

<output>
Create `.planning/phases/86.11-project-plan-milestone-ux-overdue-status-visual/86.11-03-SUMMARY.md` when done.
</output>
