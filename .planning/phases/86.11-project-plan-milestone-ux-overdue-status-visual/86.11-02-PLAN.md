---
plan: 86.11-02
phase: 86.11
wave: 2
depends_on:
  - 86.11-01
files_modified:
  - app/views/project-plan.js
  - styles/views.css
autonomous: true
requirements_addressed:
  - REQ-86.11-1
  - REQ-86.11-2

must_haves:
  truths:
    - "Right-clicking a single task shows a 'Mark as Milestone' / 'Remove Milestone' menu item in amber (#f59e0b)"
    - "Clicking 'Mark as Milestone' sets is_milestone: true on the Firestore project_tasks doc and the row re-renders"
    - "Milestone rows render with amber tint (#fffbeb) and 3px amber left border (#f59e0b) via tg-row-milestone"
    - "A ◆ symbol appears before the task name on milestone rows without polluting the Firestore name value"
    - "The Gantt bar for a milestone already renders as an amber diamond (milestone-marker CSS already exists)"
    - "Status tinting from Plan 01 does NOT appear on milestone rows (milestone class takes precedence)"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "gridToggleMilestone() + window registration + context menu entry"
      contains: "gridToggleMilestone"
    - path: "styles/views.css"
      provides: "tg-row-milestone amber tint + ◆ CSS prefix"
      contains: "tg-row-milestone"
  key_links:
    - from: "context menu single-task branch (line ~1286 menu.innerHTML)"
      to: "window.gridToggleMilestone(taskId)"
      via: "onclick inline in menu item div"
    - from: "renderTaskGrid() <tr> line 746"
      to: "tg-row-milestone class"
      via: "t.is_milestone check appended to class string"
    - from: ".tg-row-milestone .tg-name::before"
      to: "◆ visual prefix"
      via: "CSS content property (no JS string modification)"
---

<objective>
Add "Mark as Milestone / Remove Milestone" to the single-task right-click context menu, wire it to a Firestore updateDoc, and apply amber row tinting + ◆ CSS prefix for milestone rows in the grid.
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\86.11-project-plan-milestone-ux-overdue-status-visual\86.11-02-PLAN.md
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\app\views\project-plan.js
@C:\Users\Admin\Roaming\pr-po\styles\views.css

<interfaces>
From app/views/project-plan.js:

SINGLE-TASK context menu innerHTML — the closing `</div>` at line 1319 ends the Paste item. The milestone menu entry must be appended INSIDE the menu.innerHTML template string before the closing backtick, after a divider.

Firestore write pattern from gridIndentTask (line 1353):
  await updateDoc(doc(db, 'project_tasks', taskId), {
    parent_task_id: newParent.task_id,
    updated_at: serverTimestamp()
  });
Use the same pattern for gridToggleMilestone, setting is_milestone to true or false.

Local patch pattern from gridIndentTask (line 1358):
  const i = tasks.findIndex(x => x.task_id === taskId);
  if (i >= 0) tasks[i] = { ...tasks[i], parent_task_id: newParent.task_id };
Use same pattern to patch tasks[i].is_milestone optimistically.

Window registration pattern — confirm by checking existing at bottom of file: window.gridIndentTask = gridIndentTask. Add window.gridToggleMilestone = gridToggleMilestone in the same location.

Row rendering at line 746: current class string is `"tg-row${contextClass}"`. Plan 01 adds `${statusClass}`. This plan must also conditionally append `${milestoneClass}`.

IMPORTANT: When a row has is_milestone true, statusClass from Plan 01 will still be computed. The milestone class must appear in the class string so CSS specificity can override the status tint. Do this by computing milestoneClass as ` tg-row-milestone` when t.is_milestone is truthy, empty string otherwise. Append it after statusClass. The CSS for tg-row-milestone must have enough specificity to override tg-row-overdue etc. — use `.tg-row.tg-row-milestone` (double class) to beat `.tg-row.tg-row-overdue`.

tg-name cell structure at line 748:
  `<td class="tg-name" style="padding-left:${indent}px;">`
The ◆ prefix is applied via `.tg-row-milestone .tg-name::before` in CSS — no change to the input value.

Frappe customClass logic at lines 1922–1924 (renderGantt):
  let customClass = '';
  if (t.is_milestone) customClass = 'milestone-marker';
  else if (isParent) customClass = 'parent-summary-bar';
No change needed in this plan — milestone-marker already handles the Gantt diamond. Plan 03 adds status bar colors in the else branch.

CSS already in views.css — milestone-marker diamond is at lines 2733–2748. No new Gantt CSS needed here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gridToggleMilestone() and context menu entry</name>
  <files>app/views/project-plan.js</files>

  <read_first>
  Read app/views/project-plan.js lines 1268–1340 (single-task menu innerHTML and closing brace) to see the exact structure to extend.
  Read app/views/project-plan.js lines 1338–1370 (gridIndentTask) for the updateDoc + local patch pattern to replicate.
  Read app/views/project-plan.js lines 3580–3601 (end of file) to find where window.* registrations live.
  </read_first>

  <action>
  Step 1 — Add gridToggleMilestone async function.

  Find the block of window function registrations at the end of the file (where window.gridIndentTask, window.gridOutdentTask, etc. are assigned). Directly above those window assignments, insert the `async function gridToggleMilestone(taskId)` function. The function must:
    - Find the task: `const t = tasks.find(x => x.task_id === taskId);` — return early if not found
    - Determine the new value: `const newVal = !t.is_milestone;`
    - Call: `await updateDoc(doc(db, 'project_tasks', taskId), { is_milestone: newVal, updated_at: serverTimestamp() });`
    - Apply local patch: find index in tasks array, spread-assign is_milestone: newVal
    - On catch: log the error with prefix [ProjectPlan] and call showToast with a user-facing error message (mirror the pattern in gridIndentTask's catch block)

  Step 2 — Register on window.

  Immediately after `async function gridToggleMilestone(...)`, add: `window.gridToggleMilestone = gridToggleMilestone;`

  Step 3 — Add menu item to single-task context menu.

  In the `showTaskContextMenu` function, inside the `else` branch (SINGLE MENU), locate the closing `</div>` of the Paste item at line 1319. After the Paste `</div>` but before the closing backtick of menu.innerHTML (the `\`` at line 1320), append a divider and the milestone menu item.

  The divider is `<div style="border-top:1px solid #f1f5f9;margin:4px 0;"></div>`.

  The milestone item label must be: `t.is_milestone ? 'Remove Milestone' : 'Mark as Milestone'` — the label is conditional. The item's color is always #f59e0b (amber) regardless of whether it shows "Mark" or "Remove". Use the same `onmouseenter`/`onmouseleave` background pattern as other menu items. The hover background for this item is #fffbeb (amber-50) on mouseenter and transparent on mouseleave. The onclick is: `document.getElementById('taskGridContextMenu')?.remove(); window.gridToggleMilestone('${escapeHTML(taskId)}')`

  The milestone entry must appear only in the SINGLE MENU branch (the `else` block), NOT in the GROUP MENU branch.
  </action>

  <acceptance_criteria>
  - `async function gridToggleMilestone(taskId)` exists in project-plan.js
  - `window.gridToggleMilestone = gridToggleMilestone` is registered
  - Right-clicking a single task row shows either "Mark as Milestone" or "Remove Milestone" in amber color (#f59e0b)
  - Clicking the item calls updateDoc on project_tasks with is_milestone toggled
  - After toggle, the onSnapshot listener fires and re-renders the row with/without tg-row-milestone
  - Group context menu (multiple rows selected) does NOT show the milestone item
  </acceptance_criteria>

  <verify>
  Browser: Right-click a single task row. Confirm the amber "Mark as Milestone" item appears at the bottom of the menu after a divider. Click it. Confirm the row re-renders with amber tint. Right-click again — confirm label now reads "Remove Milestone". Click it. Confirm amber tint removed. Check Firestore (DevTools Network or Firebase console) to verify is_milestone field toggled.
  </verify>

  <done>gridToggleMilestone exists, is window-registered, and the context menu shows the conditional amber item in the single-task branch.</done>
</task>

<task type="auto">
  <name>Task 2: Append tg-row-milestone to row rendering + add CSS</name>
  <files>app/views/project-plan.js, styles/views.css</files>

  <read_first>
  Read app/views/project-plan.js lines 723–750 (the map callback and &lt;tr&gt; template) to see Plan 01's statusClass variable and the current class string.
  Read styles/views.css lines 2540–2600 (status tinting rules added by Plan 01) to find the correct insertion point for milestone CSS.
  </read_first>

  <action>
  JavaScript changes — app/views/project-plan.js:

  Inside the `visibleSorted.map(t => {` callback (around line 723), after the `const statusClass = ...` line added by Plan 01, add: `const milestoneClass = t.is_milestone ? ' tg-row-milestone' : '';`

  On the &lt;tr&gt; class string (line 746), extend it to include milestoneClass: `class="tg-row${contextClass}${statusClass}${milestoneClass}"`. The milestone class must appear LAST so it can override earlier status classes via CSS specificity.

  CSS changes — styles/views.css:

  After the Phase 86.11 status tinting block added by Plan 01 (after .tg-row.tg-row-not-started:hover), add a new comment: `/* Phase 86.11 — milestone row tint (amber) + ◆ prefix */`

  Add two CSS rules:

  Rule 1: `.tg-row.tg-row-milestone` sets background to #fffbeb and box-shadow to `inset 3px 0 0 0 #f59e0b`. This selector has equal specificity to .tg-row.tg-row-overdue but appears later in the file, so it overrides the status tint when both classes are present.

  Rule 2: `.tg-row.tg-row-milestone:hover` sets background to #fef3c7.

  Rule 3: `.tg-row.tg-row-milestone .tg-name::before` sets content to '"◆ "', color to #f59e0b, and font-size to 0.9em. This inserts the diamond prefix before the tg-name cell's content without touching the input value.

  Note on the ::before approach: The `::before` pseudo-element targets the `<td class="tg-name">` cell, not the input inside it. The ◆ character will appear as inline content before the div.tg-name-inner. This is purely decorative and does not affect the input value or Firestore data.
  </action>

  <acceptance_criteria>
  - `const milestoneClass = t.is_milestone ? ' tg-row-milestone' : '';` exists in the map callback
  - The &lt;tr&gt; class string includes `${milestoneClass}` after `${statusClass}`
  - `.tg-row.tg-row-milestone` CSS rule sets background #fffbeb and inset 3px amber box-shadow
  - `.tg-row.tg-row-milestone .tg-name::before` rule sets content "◆ " in amber
  - Milestone rows visually show amber tint even if they are also "overdue" (milestone CSS wins by cascade order)
  - ◆ appears visually before the task name in milestone rows without modifying the input value
  </acceptance_criteria>

  <verify>
  Browser: Mark a task as milestone. Confirm the row turns amber (#fffbeb) with a 3px amber left stripe. Confirm ◆ appears before the task name. Click into the name input — confirm the input value does NOT contain "◆". Mark an overdue task (red tint) as milestone — confirm the amber tint takes over. Remove milestone — confirm original overdue red tint returns.
  </verify>

  <done>Milestone rows render with amber tint, ◆ CSS prefix, and override status tints. Non-milestone rows are unaffected.</done>
</task>

</tasks>

<verification>
Browser test sequence for Plan 02:
1. Load a project plan. Right-click a single task row — confirm amber "Mark as Milestone" appears after a divider at the bottom.
2. Click "Mark as Milestone" — row turns amber with ◆ prefix visible, 3px amber left border present.
3. Right-click the same row — confirm label reads "Remove Milestone".
4. Click "Remove Milestone" — amber tint gone, ◆ gone, status tint from Plan 01 resumes.
5. Right-click a task in a multi-select group — confirm milestone item does NOT appear.
6. Mark an overdue task (red tint) as milestone — confirm amber overrides red.
7. Open Firestore console or network tab — confirm is_milestone field toggled correctly.
8. Check DevTools console — no JS errors.
</verification>

<success_criteria>
- gridToggleMilestone async function exists and is window-registered
- Context menu single-task branch shows conditional "Mark as Milestone" / "Remove Milestone" in amber
- tg-row-milestone class appended to milestone rows in renderTaskGrid
- CSS rules for amber tint + ◆ prefix exist in views.css
- Milestone rows override status tinting (cascade order)
- No regressions to existing context menu items (Indent, Outdent, Insert Row Above, Delete Row, Copy, Paste)
</success_criteria>

<output>
Create `.planning/phases/86.11-project-plan-milestone-ux-overdue-status-visual/86.11-02-SUMMARY.md` when done.
</output>
