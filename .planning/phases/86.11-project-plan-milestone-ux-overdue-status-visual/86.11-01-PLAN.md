---
plan: 86.11-01
phase: 86.11
wave: 1
depends_on: []
files_modified:
  - app/views/project-plan.js
  - styles/views.css
autonomous: true
requirements_addressed:
  - REQ-86.11-3
  - REQ-86.11-4

must_haves:
  truths:
    - "Overdue leaf rows show a red-tinted background (#fff5f5) with a 3px red left border"
    - "Complete leaf rows show a green-tinted background (#f0fdf4) with a 3px green left border"
    - "Not-started leaf rows show a light gray fill (#f8fafc)"
    - "Parent rows are exempt from status tinting (computeStatus is leaf-only)"
    - "computeStatus returns correct value for each of the four states"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "computeStatus() pure helper + status class appended to tg-row <tr>"
      contains: "function computeStatus"
    - path: "styles/views.css"
      provides: "tg-row-overdue, tg-row-complete, tg-row-not-started CSS rules"
      contains: "tg-row-overdue"
  key_links:
    - from: "renderTaskGrid() map at line 723"
      to: ".tg-row-overdue / .tg-row-complete / .tg-row-not-started"
      via: "computeStatus(t, today) called per leaf row, class appended to <tr>"
---

<objective>
Introduce the `computeStatus()` pure helper and wire it into grid row rendering so leaf task rows are tinted by their status (overdue/complete/not-started) via three new CSS classes.
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\86.11-project-plan-milestone-ux-overdue-status-visual\86.11-01-PLAN.md
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\app\views\project-plan.js
@C:\Users\Admin\Roaming\pr-po\styles\views.css

<interfaces>
From app/views/project-plan.js (relevant landmarks):

Line 723: `const rowsHtml = visibleSorted.map(t => {`
Line 725: `const isParent = isParentSet.has(t.task_id);`
Line 737: `const contextClass = filterMeta.isContextOnly ? ' tg-row-context' : '';`
Line 746: `return \`\n          <tr class="tg-row${contextClass}" data-task-id="${escapeHTML(t.task_id)}">\``

The `computeStatus()` helper must be inserted as a module-level pure function (not inside any existing function).
The status class string must be computed from `computeStatus(t, today)` and appended to the class string on line 746.
`today` must be computed once before the `visibleSorted.map(...)` call using `new Date().toISOString().slice(0, 10)`.

Firestore field names in use:
- `t.progress` (number 0–100)
- `t.end_date` (string "YYYY-MM-DD")
- `t.start_date` (string "YYYY-MM-DD")

Exact locked logic for computeStatus:
  if (task.progress >= 100)                           return 'complete';
  if (task.end_date < today)                          return 'overdue';
  if (task.start_date > today && task.progress === 0) return 'not-started';
  return 'in-progress';

From styles/views.css (row class landmarks):
Line 2410: `.tg-row { height: 42px; … }`
Line 2415: `.tg-row:hover { background: #f8f9fa; }`
Line 2548: `.tg-row.tg-selected { background: #eff6ff; … }`
Line 2594: `.tg-row.tg-row-context { opacity: 0.55; }`
Line 2597: (end of tg-row rules block — insertion point for new status rules)

No status CSS classes exist yet — tg-row-overdue, tg-row-complete, tg-row-not-started must all be created.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add computeStatus() helper to project-plan.js</name>
  <files>app/views/project-plan.js</files>

  <read_first>
  Read app/views/project-plan.js lines 700–750 to see the full renderTaskGrid map context.
  Read app/views/project-plan.js lines 1338–1370 (gridIndentTask) to confirm the module-level function placement style used throughout the file.
  </read_first>

  <action>
  Step 1 — Add the pure helper function.

  Find the line containing `// ---- Task grid hierarchy + reorder operations (Plan 02) ----` (around line 1206). Insert the `computeStatus` function directly above that comment. The function signature is `function computeStatus(task, today)`. It must be a plain function declaration (not arrow, not async). The locked logic (from planning context) is:
    - Return 'complete' if task.progress >= 100
    - Return 'overdue' if task.end_date < today (string comparison works for YYYY-MM-DD ISO dates)
    - Return 'not-started' if task.start_date > today AND task.progress === 0
    - Return 'in-progress' otherwise

  Step 2 — Compute today once before the map.

  Find the `const rowsHtml = visibleSorted.map(t => {` line (around line 723). Immediately before that line, insert: `const today = new Date().toISOString().slice(0, 10);`

  Step 3 — Append status class to each leaf <tr>.

  Inside the map callback, after the `const contextClass = ...` line (around line 737) but before the `return` template literal, add:
    - `const statusClass = isParent ? '' : (' tg-row-' + computeStatus(t, today));`

  Then on the `<tr class="tg-row${contextClass}"` template string (line 746), change it to `<tr class="tg-row${contextClass}${statusClass}"`. The milestone class (tg-row-milestone) is NOT added here — that is Plan 02's responsibility.

  Important: Apply statusClass ONLY to leaf rows (guard with `isParent`). Parent rows get empty string. Do not apply status classes to milestone rows either — milestone rows will be guarded in Plan 02 by checking `t.is_milestone` before assigning statusClass; for now the classes will coexist harmlessly since milestone CSS does not exist yet and will override via specificity in Plan 02.
  </action>

  <acceptance_criteria>
  - `function computeStatus(task, today)` exists as a module-level declaration in project-plan.js
  - `const today = new Date().toISOString().slice(0, 10);` appears immediately before `const rowsHtml = visibleSorted.map`
  - `tg-row-overdue`, `tg-row-complete`, or `tg-row-not-started` appears in rendered <tr> elements for leaf rows
  - Parent rows do NOT receive a status class (isParent guard verified)
  </acceptance_criteria>

  <verify>
  Open browser DevTools console after loading a project plan. Run:
    document.querySelectorAll('.tg-row:not(.tg-empty-row)').forEach(r => console.log(r.dataset.taskId, r.className))
  Confirm leaf rows have one of tg-row-overdue / tg-row-complete / tg-row-not-started / (none for in-progress).
  Confirm parent rows have no tg-row-* status class.
  </verify>

  <done>computeStatus helper exists, today computed once per render, leaf rows have status class in their className.</done>
</task>

<task type="auto">
  <name>Task 2: Add status row tinting CSS to views.css</name>
  <files>styles/views.css</files>

  <read_first>
  Read styles/views.css lines 2540–2600 to see the tg-row class block and its current end. The insertion point is after `.tg-row.tg-row-context:hover { opacity: 0.85; }` around line 2595.
  </read_first>

  <action>
  Append four new CSS rule blocks immediately after the `.tg-row.tg-row-context:hover` rule (around line 2595), inside the same Phase 86 task-grid section. The rules are:

  Rule 1 — Overdue: selector `.tg-row.tg-row-overdue` sets background to #fff5f5 and box-shadow inset to `inset 3px 0 0 0 #ef4444`.

  Rule 2 — Overdue hover: selector `.tg-row.tg-row-overdue:hover` sets background to #fee2e2.

  Rule 3 — Complete: selector `.tg-row.tg-row-complete` sets background to #f0fdf4 and box-shadow inset to `inset 3px 0 0 0 #059669`.

  Rule 4 — Complete hover: selector `.tg-row.tg-row-complete:hover` sets background to #dcfce7.

  Rule 5 — Not-started: selector `.tg-row.tg-row-not-started` sets background to #f8fafc. No box-shadow (spec says light gray fill only).

  Rule 6 — Not-started hover: selector `.tg-row.tg-row-not-started:hover` sets background to #f1f5f9.

  Add a brief section comment before the block: `/* Phase 86.11 — row status tinting (overdue, complete, not-started) */`

  Do not add any tg-row-milestone or tg-row-in-progress rules — those are Plan 02 and Plan 03 responsibilities.
  </action>

  <acceptance_criteria>
  - `.tg-row.tg-row-overdue` rule exists with background: #fff5f5 and box-shadow inset 3px red
  - `.tg-row.tg-row-complete` rule exists with background: #f0fdf4 and box-shadow inset 3px green
  - `.tg-row.tg-row-not-started` rule exists with background: #f8fafc
  - Hover variants exist for all three
  - No tg-row-milestone or bar-status-* rules added in this plan (those are Plans 02 and 03)
  </acceptance_criteria>

  <verify>
  Browser: Open a project plan. Identify an overdue task (end_date in the past, progress &lt; 100). Inspect its <tr> — confirm className includes tg-row-overdue and background is #fff5f5 (or #fee2e2 on hover). Inspect the left edge — confirm 3px red left border via box-shadow. Repeat for a complete task (progress = 100) and a not-started task (start_date in future, progress = 0).
  </verify>

  <done>Three status CSS classes exist in views.css with correct colors per spec. Row tinting visually distinguishes overdue/complete/not-started leaf rows.</done>
</task>

</tasks>

<verification>
Browser test sequence for Plan 01:
1. Load a project plan page in the browser (e.g., #/projects/PROJ-001/plan)
2. Open DevTools Console
3. Run: document.querySelectorAll('.tg-row-overdue').length — expect >= 0 (no crash)
4. Run: document.querySelectorAll('.tg-row-complete').length — expect >= 0
5. Run: document.querySelectorAll('.tg-row-not-started').length — expect >= 0
6. Identify a task with end_date before today and progress &lt; 100 — its row must have red tint
7. Identify a task with progress = 100 — its row must have green tint
8. Identify a task with start_date after today and progress = 0 — its row must have gray fill
9. Confirm parent rows (collapsible, with ▼ toggle) have no tg-row-* status class
10. Confirm no JS errors in console
</verification>

<success_criteria>
- computeStatus() pure function exists in project-plan.js
- today variable computed once before visibleSorted.map() in renderTaskGrid()
- Leaf rows have correct tg-row-overdue / tg-row-complete / tg-row-not-started class
- Parent rows have no status class
- Three CSS rules exist with hex values matching spec exactly
- No regressions to existing row tinting (tg-selected, tg-multi-selected, tg-row-context)
</success_criteria>

<output>
Create `.planning/phases/86.11-project-plan-milestone-ux-overdue-status-visual/86.11-01-SUMMARY.md` when done.
</output>
