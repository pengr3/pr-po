---
phase: "87.3"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - firestore.rules
autonomous: true
requirements_addressed:
  - D-09
  - D-10
  - D-11

must_haves:
  truths:
    - "An assigned operations_user can create a new proposal (start one via the inline card)"
    - "An assigned operations_user can save edits to title, description, amount, target_client, version, and attachment fields on a proposal"
    - "The expanded rules include a comment explaining the create and update branches"
  artifacts:
    - path: "firestore.rules"
      provides: "create + update rules for proposals block with expanded field-mask and assignment-gated create"
      contains: "allow create"
  key_links:
    - from: "firestore.rules proposals block"
      to: "projects/$(resource.data.project_id)"
      via: "get() call in create and update rules"
      pattern: "get\\(/databases/\\$\\(database\\)/documents"
---

<objective>
Expand the Firestore security rules for the `proposals` collection to close two permission gaps:

1. (D-09) Expand the operations_user / services_user `update` field-mask from the narrow 5-key allow-list shipped in Phase 87.2 to include the full set of editable proposal fields: `title`, `description`, `amount`, `target_client_id`, `target_client_name`, `version`, `attachment_kind`, `attachment_url`, `attachment_filename`.
2. (D-10) Add a `create` rule for operations_user / services_user that mirrors the update rule's assignment + department-alignment predicate, and pins `created_by` to the caller.
3. (D-11) Add a comment block above both branches explaining the rationale.

Purpose: Without D-10 the "Start Proposal" button (Plan 03) fails with a Firestore permission error for non-admin users. Without D-09 the "Edit Proposal" flow in the full modal fails for the same users on fields like `title` and `amount`.

Output: Updated `firestore.rules` with an expanded proposals block. No JS changes in this plan.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand update field-mask and add create rule for operations_user / services_user in proposals block</name>
  <files>firestore.rules</files>
  <read_first>
    - firestore.rules lines 565–615 — the full current proposals block (read the EXACT current text before editing)
  </read_first>
  <action>
    Edit firestore.rules at the proposals block (starting at `match /proposals/{proposalId}`).

    STEP 1 — Replace the comment block immediately above the `match /proposals/{proposalId}` line (lines ~556–571). The current comment says: "Phase 87.2 — Proposal lifecycle rules …". Replace it with a comment that covers the Phase 87.3 additions as well. Keep all existing explanatory text; ADD the following to the comment:

    Add a note explaining that BRANCH 2 update was expanded in Phase 87.3 D-09 to include editable proposal content fields (title, description, amount, target_client_id, target_client_name, version, attachment_kind, attachment_url, attachment_filename) so that assigned ops/services users can save edits via the full proposal modal. Add a note that BRANCH 3 create rule (Phase 87.3 D-10) mirrors BRANCH 2's assignment and department-alignment logic and pins created_by to the caller's UID so that assigned users can start proposals from the project/service detail page.

    STEP 2 — In the `allow create` line (currently line ~577):
    The existing create rule is:
      `allow create: if hasRole(['super_admin', 'operations_admin']) && request.resource.data.created_by == request.auth.uid;`

    Replace this single allow create with TWO separate allow create statements (or combine as OR branches within a single statement — choose whichever is readable). The result must allow create if:
      - BRANCH A (admin): `hasRole(['super_admin', 'operations_admin']) && request.resource.data.created_by == request.auth.uid`
      - BRANCH B (assigned ops/services user): caller is `operations_user` or `services_user` + assignment check (caller UID in parent doc's `personnel_user_ids`) + department alignment + `request.resource.data.created_by == request.auth.uid`

    For BRANCH B, the exact predicate structure is:
    ```
    (
      isRole('operations_user')
      && request.resource.data.get('parent_collection', 'projects') == 'projects'
      && request.auth.uid in get(/databases/$(database)/documents/projects/$(request.resource.data.project_id)).data.personnel_user_ids
      && request.resource.data.created_by == request.auth.uid
    )
    ||
    (
      isRole('services_user')
      && request.resource.data.get('parent_collection', 'projects') == 'services'
      && request.auth.uid in get(/databases/$(database)/documents/services/$(request.resource.data.project_id)).data.personnel_user_ids
      && request.resource.data.created_by == request.auth.uid
    )
    ```

    NOTE: In the create rule, use `request.resource.data.project_id` (not `resource.data.project_id`) and `request.resource.data.get('parent_collection', 'projects')` — the document does not yet exist when create fires.

    STEP 3 — In the `allow update` BRANCH 2 predicate, expand the `affectedKeys().hasOnly([...])` allow-list from 5 keys to 14 keys. The new allow-list must be exactly:
      `['comms_log', 'audit_log', 'status', 'current_status_since', 'updated_at', 'title', 'description', 'amount', 'target_client_id', 'target_client_name', 'version', 'attachment_kind', 'attachment_url', 'attachment_filename']`

    The BRANCH 2 update rule structure (created_by immutability check, get() assignment, department alignment) is UNCHANGED — only the hasOnly([...]) list changes.

    STEP 4 — Verify structural integrity: the proposals `match` block must still have exactly `allow read`, `allow create`, `allow update`, `allow delete` in that order. The delete rule (`allow delete: if isRole('super_admin')`) is unchanged.

    Do NOT modify any other collection's rules outside the proposals match block.
  </action>
  <verify>
    <automated>grep -n "allow create" firestore.rules | grep -A2 "proposalId" || grep -n "allow create\|allow update\|allow delete\|allow read" firestore.rules</automated>
  </verify>
  <acceptance_criteria>
    - firestore.rules contains an `allow create` block inside `match /proposals/{proposalId}` that references `isRole('operations_user')` and `isRole('services_user')` as separate branches
    - The create rule uses `request.resource.data.get('parent_collection', 'projects')` (not `resource.data.parent_collection`) for the department check
    - The create rule uses `request.resource.data.project_id` (not `resource.data.project_id`) in the get() path
    - The create rule contains `request.resource.data.created_by == request.auth.uid` for both the admin branch and the ops/services branch
    - The `allow update` BRANCH 2 `affectedKeys().hasOnly([...])` list contains `'title'`, `'description'`, `'amount'`, `'target_client_id'`, `'target_client_name'`, `'version'`, `'attachment_kind'`, `'attachment_url'`, `'attachment_filename'` in addition to the original 5 keys
    - The `allow update` BRANCH 2 assignment check (`get(...)...personnel_user_ids`) and department alignment are UNCHANGED
    - The `allow delete: if isRole('super_admin')` line is unchanged
    - A comment above or within the proposals block references Phase 87.3 D-09 (expanded field-mask) and D-10 (create rule)
    - No other collection's rules are modified
  </acceptance_criteria>
  <done>Firestore rules proposals block has: (1) admin + assigned-user create branches, (2) 14-key update field-mask for BRANCH 2, (3) explanatory comment. No other rules changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops/services_user → Firestore proposals | Non-admin write path; must be assignment-gated to prevent creation/editing of proposals on projects they are not attached to |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.3-01-01 | Elevation of Privilege | proposals create rule BRANCH B | mitigate | Assignment check `request.auth.uid in get(...).data.personnel_user_ids` enforced in Firestore rules; attacker without UID in personnel_user_ids is rejected server-side |
| T-87.3-01-02 | Tampering | proposals update BRANCH 2 field-mask expansion | mitigate | `affectedKeys().hasOnly([...])` remains the hard boundary; newly added keys (title, amount, etc.) are editable fields by design; created_by + created_at immutability preserved via separate equality check |
| T-87.3-01-03 | Spoofing | created_by pinning in create rule | mitigate | `request.resource.data.created_by == request.auth.uid` in both branches prevents a caller from setting another user's UID as creator |
</threat_model>

<verification>
After applying the rules change, verify via Firebase Console Firestore → Rules Playground:
1. Simulate a `create` on `proposals/{id}` as an `operations_user` UID that IS in the project's `personnel_user_ids` with `parent_collection: 'projects'` and `created_by: <uid>` → expect ALLOW
2. Simulate a `create` on `proposals/{id}` as an `operations_user` UID that is NOT in `personnel_user_ids` → expect DENY
3. Simulate an `update` on `proposals/{id}` as an assigned `operations_user` changing only `title` and `updated_at` → expect ALLOW
4. Simulate an `update` as an assigned `operations_user` attempting to change `created_by` → expect DENY
</verification>

<success_criteria>
- `allow create` in proposals block covers both admin and assigned ops/services_user branches
- `allow update` BRANCH 2 field-mask includes all 14 keys
- `allow delete` unchanged (super_admin only)
- Comment documents D-09 (field-mask expansion) and D-10 (create rule) rationale
</success_criteria>

<output>
After completion, create `.planning/phases/87.3-proposal-card-polish-create-from-detail-edit-permissions-inl/87.3-01-SUMMARY.md`
</output>
