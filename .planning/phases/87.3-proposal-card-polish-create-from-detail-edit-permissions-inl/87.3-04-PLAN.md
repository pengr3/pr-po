---
phase: "87.3"
plan: "04"
type: execute
wave: 3
depends_on:
  - "87.3-03"
files_modified:
  - app/proposal-modal.js
  - app/views/service-detail.js
autonomous: true
requirements_addressed:
  - CR-1
  - CR-2
gap_closure: true

must_haves:
  truths:
    - "A proposal created via 'Start Proposal' from service-detail.js stores parent_collection: 'services' in Firestore — the services_user Firestore rule BRANCH B is satisfied and no permission-denied is thrown"
    - "State transitions on a service-context proposal target the services collection, not projects"
    - "The project select in the Create modal is correctly preselected to the service doc ID when invoked from service-detail.js — validation does not fire 'Please select a project'"
    - "project_code in docPayload resolves to the service's service_code when the modal is invoked from a service context"
    - "project-detail.js callers still work identically — openCreateProposalModal(projectDocId, callback) with 2 args is unaffected"
  artifacts:
    - path: "app/proposal-modal.js"
      provides: "Module vars _createModalParentCollection and _createModalLockedProjectCode; extended openCreateProposalModal signature; synthetic option injection; parent_collection in docPayload; fallback projectCode; reset in closeCreateProposalModal"
      contains: "_createModalParentCollection"
    - path: "app/views/service-detail.js"
      provides: "Updated Start Proposal onclick in loadProposalCard passing 'services' and service_code"
      contains: "'services'"
  key_links:
    - from: "service-detail.js loadProposalCard Start Proposal button"
      to: "openCreateProposalModal"
      via: "onclick passes parentCollection='services' and lockedProjectCode=service_code"
      pattern: "openCreateProposalModal.*services"
    - from: "openCreateProposalModal"
      to: "_createModalParentCollection"
      via: "stores third param value at module level"
      pattern: "_createModalParentCollection = parentCollection"
    - from: "saveProposal docPayload"
      to: "parent_collection field"
      via: "_createModalParentCollection written into Firestore payload"
      pattern: "parent_collection: _createModalParentCollection"
---

<objective>
Fix CR-1 and CR-2: propagate parent_collection into the Firestore create payload and make the service doc ID preselect work in the Create Proposal modal.

Purpose: Without these fixes "Start Proposal" from service-detail.js always fails with permission-denied (Firestore BRANCH B requires parent_collection == 'services') and projectCode resolves to null. The feature is completely non-functional for services context after plans 01-03.

Output: proposal-modal.js with _createModalParentCollection/_createModalLockedProjectCode vars, extended openCreateProposalModal signature, synthetic option injection for service context, parent_collection in docPayload, fallback projectCode. service-detail.js with updated onclick.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Admin/Roaming/pr-po/.planning/PROJECT.md
@C:/Users/Admin/Roaming/pr-po/.planning/ROADMAP.md

<interfaces>
<!-- Extracted from app/proposal-modal.js — current module-level vars block (lines 70-82) -->

Current module vars (lines 70-82):
  let currentProposal = null;
  let createModalMode = 'create';
  let createModalEditingId = null;
  let _createModalOnClose = null;
  let _modalProjectsData = [];
  let _modalClientsData = [];
  let _modalProjectsLoaded = false;
  let _parentDocCache = null;

openCreateProposalModal current signature (line 663):
  async function openCreateProposalModal(preselectedProjectId = null, onClose = null)

closeCreateProposalModal current body (lines 791-799):
  resets createModalMode, createModalEditingId, _createModalOnClose; fires callback

saveProposal CREATE path — projectCode resolution (lines 841-842):
  const project = _modalProjectsData.find(p => p.id === projectId);
  const projectCode = project?.project_code || null;

saveProposal CREATE path — docPayload (lines 880-901):
  Fields: proposal_id, project_id, project_code, title, description, amount,
          target_client_id, target_client_name, status, attachment_kind,
          attachment_url, attachment_storage_path, attachment_filename,
          audit_log, comms_log, loss_reason, current_status_since,
          created_by, created_at, updated_at
  MISSING: parent_collection

<!-- Extracted from app/views/service-detail.js — Start Proposal button (lines 1272-1280) -->

Current loadProposalCard Start Proposal path (lines 1273-1280):
  window._startProposalCallback = () => loadProposalCard(parentDocId, parentCollection);
  el.innerHTML = `...
    <button class="btn btn-primary"
      onclick="window.openCreateProposalModal('${escapeHTML(parentDocId)}', window._startProposalCallback)">
      Start Proposal
    </button>...`;

<!-- service-detail.js module state (lines 16-18) -->
  let currentService = null;
  let currentServiceDocId = null;

  currentService has shape: { service_code, project_status, personnel_user_ids, ... }
  currentServiceDocId is the Firestore document ID of the service (equals parentDocId inside loadProposalCard when parentCollection === 'services')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend proposal-modal.js — parent_collection and lockedProjectCode support</name>
  <files>app/proposal-modal.js</files>
  <read_first>
    Read app/proposal-modal.js lines 68-82 (module vars block) to locate insertion point for new vars.
    Read app/proposal-modal.js lines 660-680 (openCreateProposalModal body) to understand current preselect logic.
    Read app/proposal-modal.js lines 789-800 (closeCreateProposalModal body) for reset block.
    Read app/proposal-modal.js lines 838-845 (saveProposal projectCode resolution) for the fix target.
    Read app/proposal-modal.js lines 878-902 (saveProposal docPayload) for the field insertion point.
  </read_first>
  <action>
    1. After the `let _createModalOnClose = null;` declaration on line 73, add two new module-level vars on consecutive lines:
       - `let _createModalParentCollection = 'projects';  // 'projects' | 'services'`
       - `let _createModalLockedProjectCode = null;       // non-null when modal is locked to a service`

    2. Change the `openCreateProposalModal` function signature from:
         `async function openCreateProposalModal(preselectedProjectId = null, onClose = null)`
       to:
         `async function openCreateProposalModal(preselectedProjectId = null, onClose = null, parentCollection = 'projects', lockedProjectCode = null)`

    3. In the `openCreateProposalModal` body, immediately after the line `_createModalOnClose = onClose;`, add:
         `_createModalParentCollection = parentCollection;`
         `_createModalLockedProjectCode = lockedProjectCode;`

    4. In the `openCreateProposalModal` body, inside the `if (preselectedProjectId)` block, find the block that sets `projectSelectEl.value = preselectedProjectId`. Replace the entire `if (preselectedProjectId)` block with this logic:
       - The new block must: check `if (preselectedProjectId)`, then check `if (lockedProjectCode)` inside — if true, create a synthetic `<option>` element with `value = preselectedProjectId` and text `lockedProjectCode + ' (Service)'`, then call `projectSelectEl.insertBefore(syntheticOpt, projectSelectEl.firstChild)`. Then (unconditionally within the outer if): set `projectSelectEl.value = preselectedProjectId`, set `projectSelectEl.disabled = true`, dispatch `new Event('change')` on the element.
       - Use exact identifier names: `syntheticOpt` for the option element, `projectSelectEl` for the DOM element.
       - The `lockedProjectCode` variable is in scope from the function parameter.

    5. In `saveProposal`, replace the projectCode resolution line:
         `const projectCode = project?.project_code || null;`
       with:
         `const projectCode = project?.project_code || _createModalLockedProjectCode || null;`

    6. In `saveProposal` CREATE path `docPayload`, add `parent_collection: _createModalParentCollection,` as the first field after `project_id: projectId,`. This ensures the Firestore BRANCH B rule `request.resource.data.get('parent_collection', 'projects') == 'services'` receives the correct value.

    7. In `closeCreateProposalModal`, after the line `createModalEditingId = null;`, add:
         `_createModalParentCollection = 'projects';`
         `_createModalLockedProjectCode = null;`

    Do NOT modify any EDIT-mode logic. Do NOT change _loadModalDropdownData. Do NOT alter the client auto-select change listener in showCreateModal. Do NOT add fenced code blocks — implement directly.
  </action>
  <verify>
    <automated>grep -n "_createModalParentCollection" app/proposal-modal.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "_createModalParentCollection" app/proposal-modal.js returns at least 5 (declaration, assignment in openCreateProposalModal, usage in saveProposal docPayload, reset in closeCreateProposalModal, default value)
    - grep -c "_createModalLockedProjectCode" app/proposal-modal.js returns at least 4 (declaration, assignment, fallback in projectCode resolution, reset)
    - grep -c "parent_collection: _createModalParentCollection" app/proposal-modal.js returns exactly 1 (inside docPayload)
    - grep -n "openCreateProposalModal" app/proposal-modal.js shows the function signature contains "parentCollection = 'projects', lockedProjectCode = null"
    - grep -n "insertBefore" app/proposal-modal.js shows the synthetic option insertion inside openCreateProposalModal
    - grep -n "project_code || _createModalLockedProjectCode" app/proposal-modal.js returns 1 match
  </acceptance_criteria>
  <done>proposal-modal.js stores parent_collection and lockedProjectCode at module level; extended openCreateProposalModal injects a synthetic option in service context; saveProposal docPayload includes parent_collection field; projectCode falls back to lockedProjectCode; closeCreateProposalModal resets both vars to defaults</done>
</task>

<task type="auto">
  <name>Task 2: Update service-detail.js — Start Proposal onclick passes services context</name>
  <files>app/views/service-detail.js</files>
  <read_first>
    Read app/views/service-detail.js lines 1268-1285 (the Start Proposal button innerHTML block) to see the exact current onclick string.
    Read app/views/service-detail.js lines 1250-1268 (start of loadProposalCard, variable declarations) to confirm currentService is in scope.
  </read_first>
  <action>
    In `loadProposalCard`, locate the Start Proposal button HTML (inside the `if (snap.empty)` + `if (currentService?.project_status === 'For Proposal' && canDrive)` branch). The button currently has:
      `onclick="window.openCreateProposalModal('${escapeHTML(parentDocId)}', window._startProposalCallback)"`

    Replace that onclick attribute value with:
      `onclick="window.openCreateProposalModal('${escapeHTML(parentDocId)}', window._startProposalCallback, 'services', '${escapeHTML(currentService?.service_code || '')}')"`

    The third argument `'services'` is a string literal. The fourth argument uses template literal interpolation of `currentService?.service_code`. `currentService` is a module-level var accessible in this function's closure.

    Do NOT change project-detail.js — its callers use the 2-argument form which already defaults parentCollection to 'projects'.
    Do NOT change any other calls to openCreateProposalModal in service-detail.js (there are none other than this one).
  </action>
  <verify>
    <automated>grep -n "openCreateProposalModal" app/views/service-detail.js</automated>
  </verify>
  <acceptance_criteria>
    - grep output for openCreateProposalModal in service-detail.js shows the onclick contains the four-argument call with 'services' as the third argument
    - The onclick string contains `escapeHTML(currentService?.service_code` (the fourth arg)
    - No other occurrences of openCreateProposalModal in service-detail.js have been added or changed beyond this one button
    - grep -c "openCreateProposalModal.*'services'" app/views/service-detail.js returns 1
  </acceptance_criteria>
  <done>The Start Proposal button in service-detail.js passes parentCollection='services' and service_code as the third and fourth arguments to openCreateProposalModal, enabling the synthetic option injection and parent_collection payload field added in Task 1</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Firestore | proposal CREATE payload crosses security rules gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.3-04-01 | Elevation of Privilege | parent_collection field in CREATE payload | mitigate | Value sourced from openCreateProposalModal parameter (default 'projects'); never derived from user input; Firestore BRANCH B rule re-validates the value against the caller's role |
| T-87.3-04-02 | Spoofing | lockedProjectCode from service_code in onclick | accept | service_code is display-only label for dropdown; project_id (doc ID) is the actual FK; Firestore rules validate project_id, not project_code |
</threat_model>

<verification>
1. Open service-detail.js for a service with project_status='For Proposal' as a services_user role
2. Click Start Proposal — modal should open with the service's service_code shown in the locked project select
3. Fill in Title, click Save Proposal — should NOT receive permission-denied toast
4. Firestore proposals collection should contain a new doc with parent_collection: 'services'
5. Repeat from project-detail.js — should still work with 2-arg call (no regression)
</verification>

<success_criteria>
- proposal-modal.js has _createModalParentCollection and _createModalLockedProjectCode module vars
- openCreateProposalModal signature accepts parentCollection and lockedProjectCode (both defaulted)
- saveProposal docPayload includes parent_collection: _createModalParentCollection
- saveProposal projectCode resolution falls back to _createModalLockedProjectCode
- closeCreateProposalModal resets both new vars to defaults
- service-detail.js Start Proposal button passes 'services' and service_code as 3rd/4th args
- project-detail.js Start Proposal callers are untouched (2-arg form still works)
</success_criteria>

<output>
After completion, create `.planning/phases/87.3-proposal-card-polish-create-from-detail-edit-permissions-inl/87.3-04-SUMMARY.md`
</output>
