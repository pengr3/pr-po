---
phase: "87.3"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/proposal-modal.js
autonomous: true
requirements_addressed:
  - D-04
  - D-03

must_haves:
  truths:
    - "openCreateProposalModal() can be called with no arguments (backward-compatible — home.js Engagements sub-tab continues to work)"
    - "openCreateProposalModal(projectId) pre-selects the given project in the dropdown and disables the dropdown"
    - "When a project is pre-selected, the client dropdown auto-fills via the existing change event listener"
    - "After the Create Proposal modal closes (save or cancel), the caller-supplied onClose callback fires"
  artifacts:
    - path: "app/proposal-modal.js"
      provides: "openCreateProposalModal with optional preselectedProjectId param + optional onClose callback"
      contains: "function openCreateProposalModal"
  key_links:
    - from: "openCreateProposalModal"
      to: "showCreateModal"
      via: "_loadModalDropdownData() then preselect step then showCreateModal(null)"
      pattern: "preselectedProjectId"
    - from: "closeCreateProposalModal"
      to: "onClose callback"
      via: "stored _createModalOnClose module variable"
      pattern: "_createModalOnClose"
---

<objective>
Extend `openCreateProposalModal()` in `app/proposal-modal.js` with:

1. (D-04) An optional `preselectedProjectId` parameter (default `null`) so callers from project/service detail pages can pre-select and lock the project dropdown.
2. (D-03) An optional `onClose` callback parameter (default `null`) that fires when `closeCreateProposalModal()` is called — allowing the detail views to refresh the inline card after save or cancel.

Both additions are backward-compatible: existing callers (home.js) call `openCreateProposalModal()` with no args and observe no change in behavior.

Purpose: Plan 03 (project-detail.js + service-detail.js) will call `window.openCreateProposalModal(parentDocId, () => loadProposalCard(...))` from the "Start Proposal" button. The pre-selection and the post-close refresh cannot happen without these hooks in the modal layer.

Output: Modified `app/proposal-modal.js` — `openCreateProposalModal` signature change + preselect logic + `_createModalOnClose` module variable wired in `closeCreateProposalModal`.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
From app/proposal-modal.js (current state — read before editing):

openCreateProposalModal signature (line ~662):
  async function openCreateProposalModal() {
      createModalMode = 'create';
      createModalEditingId = null;
      await _loadModalDropdownData();
      showCreateModal(null);
  }

closeCreateProposalModal (line ~781):
  function closeCreateProposalModal() {
      const el = document.getElementById('proposalCreateModal');
      if (el) el.remove();
      createModalMode = 'create';
      createModalEditingId = null;
  }

showCreateModal change-event listener (lines ~764–778):
  The `#proposalCreateProject` change listener that auto-fills the client dropdown is
  already attached inside showCreateModal(). The preselect step fires AFTER showCreateModal()
  so the listener is already bound when dispatchEvent fires.

Module-level state variables (near top of file, around line 80–120):
  let createModalMode = 'create';
  let createModalEditingId = null;
  (No _createModalOnClose exists yet — must be added)

window registration (line ~1611):
  window.openCreateProposalModal = openCreateProposalModal;
  window.closeCreateProposalModal = closeCreateProposalModal;
  (Both already registered in openProposalModal() and deleted in closeProposalModal())
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add preselectedProjectId param and _createModalOnClose callback to openCreateProposalModal / closeCreateProposalModal</name>
  <files>app/proposal-modal.js</files>
  <read_first>
    - app/proposal-modal.js lines 80–130 (module-level let declarations — to locate where to add _createModalOnClose)
    - app/proposal-modal.js lines 658–790 (openCreateProposalModal, showCreateModal, closeCreateProposalModal — full functions, read before editing)
  </read_first>
  <action>
    CHANGE 1 — Add module-level variable for the callback. Near the other `let createModal*` declarations (around lines 80–130), add:
      `let _createModalOnClose = null;`

    CHANGE 2 — Update `openCreateProposalModal` signature and body. The new function:
      - Accepts `(preselectedProjectId = null, onClose = null)` as optional parameters
      - Sets `_createModalOnClose = onClose;` before calling `showCreateModal(null)`
      - After `showCreateModal(null)` returns (synchronously), if `preselectedProjectId` is truthy:
        a. Find the `#proposalCreateProject` select element
        b. Set `projectSelectEl.value = preselectedProjectId`
        c. Set `projectSelectEl.disabled = true`
        d. Dispatch a synthetic `change` event on `projectSelectEl` so the existing client auto-fill listener fires:
           `projectSelectEl.dispatchEvent(new Event('change'))`
      - No other changes to the function body

    CHANGE 3 — Update `closeCreateProposalModal` to invoke the callback. After removing the modal element and resetting `createModalMode`/`createModalEditingId`, add:
      ```
      const cb = _createModalOnClose;
      _createModalOnClose = null;
      if (typeof cb === 'function') cb();
      ```
      Store the callback in a local variable BEFORE nulling `_createModalOnClose` to prevent double-fire if the callback itself somehow triggers closeCreateProposalModal again.

    BACKWARD COMPATIBILITY: existing callers that call `openCreateProposalModal()` (no args) pass `null` for both params, so the preselect block is skipped (if `preselectedProjectId` is falsy) and `_createModalOnClose` is set to null (no-op in closeCreateProposalModal).

    Do NOT change `openEditProposalModal`, `saveProposal`, `showCreateModal`, or any window function registrations. The window registration (`window.openCreateProposalModal = openCreateProposalModal`) already exists in `openProposalModal()` and will automatically pick up the new signature.
  </action>
  <verify>
    <automated>grep -n "preselectedProjectId\|_createModalOnClose\|onClose" app/proposal-modal.js</automated>
  </verify>
  <acceptance_criteria>
    - `openCreateProposalModal` function declaration contains `preselectedProjectId = null` in its parameter list
    - `openCreateProposalModal` function declaration contains `onClose = null` in its parameter list (or the callback is the second param)
    - `_createModalOnClose` is declared as a module-level `let` variable (near the other `createModal*` vars)
    - `openCreateProposalModal` body contains `_createModalOnClose = onClose` (or equivalent assignment before showCreateModal call)
    - `openCreateProposalModal` body contains `projectSelectEl.disabled = true` inside an `if (preselectedProjectId)` guard
    - `openCreateProposalModal` body contains `projectSelectEl.dispatchEvent(new Event('change'))` inside the same guard
    - `closeCreateProposalModal` body contains `_createModalOnClose = null` and a `if (typeof cb === 'function') cb()` pattern
    - No existing callers' behavior is changed (preselect block is skipped when `preselectedProjectId` is null/falsy)
    - `openEditProposalModal` is NOT modified
  </acceptance_criteria>
  <done>openCreateProposalModal accepts preselectedProjectId and onClose; closeCreateProposalModal fires the callback and clears it; backward-compatible with all existing no-arg callers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → openCreateProposalModal | caller provides a projectId string; used only to set dropdown value — no direct Firestore write at this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.3-02-01 | Tampering | preselectedProjectId dropdown lock | accept | projectSelectEl.disabled=true prevents user from changing the project; server-side Firestore create rule (Plan 01) enforces the real assignment check. UI lock is UX-only. |
| T-87.3-02-02 | Denial of Service | _createModalOnClose callback re-entry | mitigate | Callback stored in local `cb` variable before `_createModalOnClose = null` prevents double-fire if callback itself calls closeCreateProposalModal |
</threat_model>

<verification>
Manual smoke test (no automated test suite):
1. Open home.js Engagements sub-tab → click "New Engagement" → Create Proposal opens with project dropdown enabled → no regression
2. Open a project in 'For Proposal' status → click "Start Proposal" (Plan 03 button) → Create Proposal modal opens with project dropdown pre-selected and disabled → correct project auto-filled
3. Cancel the modal → inline card refreshes (loadProposalCard fires via onClose callback from Plan 03)
4. Save a proposal → inline card refreshes showing the new proposal card
</verification>

<success_criteria>
- openCreateProposalModal(projectId, cb): dropdown pre-selected, disabled, client auto-filled via change event, callback stored
- openCreateProposalModal() (no args): no pre-selection, no callback — identical to current behavior
- closeCreateProposalModal(): fires stored callback then clears it
</success_criteria>

<output>
After completion, create `.planning/phases/87.3-proposal-card-polish-create-from-detail-edit-permissions-inl/87.3-02-SUMMARY.md`
</output>
