---
phase: 87.3-proposal-card-polish-create-from-detail-edit-permissions-inl
paused_at: 2026-05-22T10:15:00Z
status: uat_complete
next_action: close_phase
branch: v3.3
last_commit: 791f1c4
---

# Phase 87.3 — Pause Handoff

## Where We Are

UAT is **complete**. All 3 testable items passed (Test 4 skipped — no observable symptom). Phase is ready to close.

## What Was Done This Session

### UAT Fixes Applied
1. **Missing `transport_requests` composite index** (`97fca12`)
   - `firestore.indexes.json` was missing the `service_code + total_amount` index for `transport_requests`
   - Blocked `refreshServiceExpense` in `service-detail.js` from loading
   - Added and deployed

2. **`window.closeCreateProposalModal` / `window.saveProposal` not registered** (`791f1c4`)
   - `showCreateModal` rendered HTML with `onclick="window.closeCreateProposalModal()"` but never registered the function on `window`
   - Registration only existed inside `openProposalModal` (the view/detail modal), not the standalone create flow
   - Fixed in `showCreateModal`: now registers both functions before inserting HTML; `closeCreateProposalModal` deletes them on cleanup

3. **Stale Firestore rules** (deployed, no code commit)
   - BRANCH B services_user create rule existed locally but had not been deployed to `clmc-procurement-dev`
   - Redeployed `firestore:rules` — unblocked Test 1

### UAT Results
| Test | Result |
|------|--------|
| 1. services_user create proposal from service-detail | **pass** |
| 2. operations_user create proposal from project-detail (regression) | **pass** |
| 3. client_approved inline card label + green dot | **pass** |
| 4. No handler leak on service-detail re-init | skipped |

## Next Action

Run `/gsd:progress` to close phase 87.3 and advance to the next phase on the roadmap.

## Notes

- The "Under Client Review" status after internal approval is **intentional** per 87-CONTEXT.md line 24 — there is no intermediate "internally approved" state in the current design
- `firestore.indexes.json` now has the `transport_requests` index alongside `prs` and `pos` entries
