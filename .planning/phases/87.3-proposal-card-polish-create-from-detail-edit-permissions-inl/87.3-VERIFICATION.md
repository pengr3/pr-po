---
phase: "87.3"
verified: "2026-05-22T09:30:00Z"
status: human_needed
score: 4/4 must-haves verified
overrides_applied: 0
re_verification: false
human_verification:
  - test: "Create proposal from service-detail.js as a services_user"
    expected: "Modal opens with service_code shown in locked project select as '{code} (Service)'; Save does NOT produce permission-denied; Firestore doc has parent_collection: 'services'"
    why_human: "Firestore Security Rules BRANCH B evaluation requires a live authenticated session and a real write attempt"
  - test: "Create proposal from project-detail.js as an operations_user"
    expected: "Modal opens with project pre-selected and disabled; Save succeeds; Firestore doc has parent_collection: 'projects'"
    why_human: "Regression test for the unchanged 2-arg call path — requires live session"
  - test: "Inline proposal card for a proposal with status 'client_approved'"
    expected: "Card header shows 'Client Approved' label with a green dot (#059669) in both project-detail and service-detail views"
    why_human: "Visual rendering of status label and dot color requires browser inspection"
  - test: "service-detail.js re-init does not leak event handlers"
    expected: "Navigate to service-detail, switch to another tab that triggers re-init without destroy(), fire a permissionsChanged event; renderServiceDetail() is called exactly once, not twice"
    why_human: "Event handler counting requires DevTools console observation in a live browser session"
---

# Phase 87.3 Plans 04 + 05: Verification Report

**Phase Goal:** Fix critical code review findings (CR-1, CR-2) and warning findings (WR-1, WR-2) from Phase 87.3 plans 01-03 that made "Start Proposal" non-functional for services context and caused incorrect proposal status display.

**Verified:** 2026-05-22T09:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 (CR-1) | Proposal created via Start Proposal from service-detail.js stores `parent_collection: 'services'` in the Firestore payload — satisfying BRANCH B rule | VERIFIED | `app/proposal-modal.js` line 895: `parent_collection: _createModalParentCollection,` inside CREATE docPayload. `_createModalParentCollection` is set to the `parentCollection` arg on line 669 and defaults to `'projects'`; service-detail passes `'services'` explicitly. |
| 2 (CR-2) | Service doc ID preselects correctly in Create modal — synthetic option injected so validation does not fire "Please select a project"; `project_code` resolves to `service_code` | VERIFIED | `openCreateProposalModal` lines 676-684: when `lockedProjectCode` is truthy, a synthetic `<option value=preselectedProjectId>service_code (Service)</option>` is inserted at list head, then `projectSelectEl.value = preselectedProjectId` is set and the select is disabled. `saveProposal` line 854: `project?.project_code \|\| _createModalLockedProjectCode \|\| null` fallback chain used. |
| 3 (WR-1) | A proposal with status `client_approved` displays "Client Approved" label and a green dot (`#059669`) in the inline card in both project-detail.js and service-detail.js | VERIFIED | `project-detail.js` lines 1362 + 1375: `client_approved: 'Client Approved'` and `client_approved: '#059669'`. `service-detail.js` lines 1116 + 1129: identical keys. Old bare `approved:` key is absent from both files (grep confirmed 0 matches). Both helpers called at render time via `_proposalStageLabel(proposal.status)` and `_proposalStatusDotColor(proposal.status)`. |
| 4 (WR-2) | On re-init of service-detail.js, permission and assignment event handlers do not leak — exactly one handler attached per event type, unconditional stored-reference overwrite | VERIFIED | `service-detail.js` init() lines 73-93: detach-then-reattach pattern. `removeEventListener` called first (if prior handle exists), new handler created, `addEventListener` called, then `window._serviceDetailPermissionHandler = permissionChangeHandler` and `window._serviceDetailAssignmentHandler = assignmentChangeHandler` assigned unconditionally with no if-guard. Stale `if (!window._serviceDetailPermissionHandler)` guards are absent (grep confirmed 0 matches). `destroy()` at lines 183-190 unchanged — uses `removeEventListener` + `delete`, matching the plan's preservation requirement. |

**Score:** 4/4 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/proposal-modal.js` | `_createModalParentCollection` and `_createModalLockedProjectCode` module vars; extended `openCreateProposalModal` signature; synthetic option injection; `parent_collection` in CREATE docPayload; fallback projectCode; reset in `closeCreateProposalModal` | VERIFIED | All 6 requirements met. Grep counts: `_createModalParentCollection` x4 (line 74 declaration, 669 assignment, 806 reset, 895 payload). `_createModalLockedProjectCode` x4 (line 75 declaration, 670 assignment, 807 reset, 854 fallback). `parent_collection: _createModalParentCollection` x1 (line 895). `insertBefore` x1 (line 680). Signature at line 665 contains `parentCollection = 'projects', lockedProjectCode = null`. `closeCreateProposalModal` resets both at lines 806-807. |
| `app/views/service-detail.js` | Updated Start Proposal onclick in `loadProposalCard` passing `'services'` and `service_code` as 3rd/4th args | VERIFIED | Line 1280: `onclick="window.openCreateProposalModal('${escapeHTML(parentDocId)}', window._startProposalCallback, 'services', '${escapeHTML(currentService?.service_code \|\| '')}')"`. Grep confirmed `openCreateProposalModal.*'services'` count = 1. |
| `app/views/project-detail.js` | `_proposalStageLabel` and `_proposalStatusDotColor` with `client_approved` key | VERIFIED | Lines 1362 and 1375: `client_approved: 'Client Approved'` and `client_approved: '#059669'`. grep `client_approved:` count = 2; grep `^\s*approved:` count = 0. |
| `app/views/service-detail.js` (WR-1) | `_proposalStageLabel` and `_proposalStatusDotColor` with `client_approved` key | VERIFIED | Lines 1116 and 1129: `client_approved: 'Client Approved'` and `client_approved: '#059669'`. grep `client_approved:` count = 2; grep `^\s*approved:` count = 0. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `service-detail.js loadProposalCard` Start Proposal button | `openCreateProposalModal` | onclick passes `parentCollection='services'` and `lockedProjectCode=service_code` | WIRED | Line 1280 passes `'services'` as 3rd arg and `currentService?.service_code \|\| ''` as 4th arg |
| `openCreateProposalModal` | `_createModalParentCollection` | stores third param at module level | WIRED | Line 669: `_createModalParentCollection = parentCollection;` |
| `saveProposal` CREATE docPayload | `parent_collection` field | `_createModalParentCollection` written into Firestore payload | WIRED | Line 895: `parent_collection: _createModalParentCollection,` |
| `renderInlineProposalCard (project-detail.js)` | `_proposalStageLabel` / `_proposalStatusDotColor` | passes `proposal.status`; returns correct label/color for `'client_approved'` | WIRED | Lines 1403-1404: both helpers called with `proposal.status`; `client_approved` key present in both maps |
| `service-detail.js init()` | `_serviceDetailPermissionHandler` | removeEventListener then addEventListener then unconditional overwrite | WIRED | Lines 74-81: detach-then-reattach, unconditional `window._serviceDetailPermissionHandler = permissionChangeHandler` at line 81 |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase fixes key enumeration and event handler registration bugs, not data ingestion pipelines. No new onSnapshot listeners or fetch paths were introduced.

---

### Behavioral Spot-Checks

Step 7b: SKIPPED — changes are pure client-side logic corrections (key renames in static objects, event handler pattern fix). No runnable entry point can exercise the Firestore permission gate programmatically without a live authenticated session.

---

### Probe Execution

Step 7c: SKIPPED — no probe scripts declared in PLAN frontmatter, and this phase is not a migration/tooling phase.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| CR-1 | 87.3-04 | `parent_collection: 'services'` missing from CREATE payload — Firestore BRANCH B rule fails for services_user | SATISFIED | `parent_collection: _createModalParentCollection` at line 895; `_createModalParentCollection` set from `parentCollection` param; service-detail passes `'services'` |
| CR-2 | 87.3-04 | Service doc ID not in `_modalProjectsData` — project select stays at default, validation fires "Please select a project"; `projectCode` resolves to null | SATISFIED | Synthetic option injected at list head (line 680); select disabled; `_createModalLockedProjectCode` fallback in projectCode chain (line 854) |
| WR-1 | 87.3-05 | `approved:` key in `_proposalStageLabel` / `_proposalStatusDotColor` — `client_approved` status shows fallback "Proposal" label and gray dot | SATISFIED | `client_approved:` key present in all 4 locations; `approved:` key absent from both maps in both files |
| WR-2 | 87.3-05 | Stale if-guard in service-detail init() — second init() call leaks a handler closure, causing stale `renderServiceDetail()` fires | SATISFIED | if-guard removed; detach-then-reattach pattern implemented; unconditional overwrite at lines 81, 93 |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None found | — | No TBD/FIXME/XXX markers, no stub return values, no unregistered handlers in modified files |

---

### Human Verification Required

#### 1. Create proposal from service-detail.js as services_user

**Test:** Log in as a services_user assigned to a service with `project_status === 'For Proposal'` and no linked proposal. Open the service detail page. Verify "Start Proposal" button is visible. Click it — modal should open with the service code shown in the locked, disabled project select (e.g. "CLMC-SVC-001 (Service)"). Fill in Title, click Save Proposal.

**Expected:** No permission-denied toast. A new Firestore document appears in the `proposals` collection with `parent_collection: 'services'`. The inline card now shows the new proposal.

**Why human:** Firestore Security Rules BRANCH B evaluation (`request.resource.data.get('parent_collection', 'projects') == 'services'`) requires a real write attempt from an authenticated browser session. Cannot be verified programmatically.

---

#### 2. Create proposal from project-detail.js (regression test)

**Test:** Log in as an operations_user assigned to a project with `project_status === 'For Proposal'` and no linked proposal. Open the project detail page. Click "Start Proposal". Fill in Title, click Save Proposal.

**Expected:** No regression — modal opens with project pre-selected and disabled, Save succeeds, Firestore doc has `parent_collection: 'projects'`, inline card refreshes.

**Why human:** Requires a live authenticated session to confirm the 2-arg backward-compatible call path through Firestore rules.

---

#### 3. Inline card label/dot for `client_approved` proposal

**Test:** Open a project or service that has a linked proposal with `status: 'client_approved'`. Verify the inline card header.

**Expected:** Header shows "Client Approved" text and a green dot (hex #059669). Neither "Proposal" (fallback label) nor a gray dot should appear.

**Why human:** Visual rendering of CSS color and label text requires browser inspection to confirm.

---

#### 4. service-detail.js event handler leak on re-init

**Test:** Navigate to a service-detail page. Switch between tabs in a way that triggers `init()` a second time without an intervening `destroy()` (e.g., if the router does not call `destroy()` between same-view tab switches). Open DevTools console. Fire `window.dispatchEvent(new Event('permissionsChanged'))`. Observe how many times `renderServiceDetail()` is called.

**Expected:** Exactly one call to `renderServiceDetail()`. Under the old code this would be two (stale + fresh handler). Under the new detach-then-reattach pattern it should be one.

**Why human:** Event handler counting and DOM re-render observation requires DevTools console monitoring in a live browser session.

---

### Gaps Summary

No gaps. All four must-have truths are verified in the codebase. Four human verification items require a live browser session with authenticated user accounts to confirm Firestore rule evaluation and browser rendering behavior. These are standard UAT items, not code defects.

---

_Verified: 2026-05-22T09:30:00Z_
_Verifier: Claude (gsd-verifier)_
