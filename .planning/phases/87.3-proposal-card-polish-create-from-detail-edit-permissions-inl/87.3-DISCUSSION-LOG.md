# Phase 87.3: Proposal Card Polish — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-22
**Phase:** 87.3 — Proposal Card Polish — create from detail, edit permissions, inline card redesign
**Areas discussed:** Create from detail, Edit permissions, Inline card redesign

---

## Create from detail

### Q1: What should the inline card show when status is 'For Proposal' and no proposal exists?

| Option | Description | Selected |
|--------|-------------|----------|
| "Start Proposal" button | Opens existing Create Proposal modal pre-filled with the current project | ✓ |
| Always-visible card with create button | Card always visible even when no proposal | |
| No card until proposal exists | Plain empty state | |

**User's choice:** "Start Proposal" button that opens the existing Create Proposal modal from proposal-modal.js, pre-filled with the current project.

---

### Q2: Who can CREATE a new proposal from the detail page?

| Option | Description | Selected |
|--------|-------------|----------|
| Operations user + admins | Admins + assigned operations/services users | |
| Admins only | Keep Phase 87 D-10 admin-only restriction | |
| Same as canDrive from 87.2 | Mirrors the 87.2 dual-flag canDrive set | ✓ |

**User's choice:** Same as canDrive from 87.2 — super_admin, operations_admin, services_admin, assigned operations_user, assigned services_user.

---

### Q3: After creating a proposal from the detail page, what should happen?

| Option | Description | Selected |
|--------|-------------|----------|
| Modal closes, card refreshes | loadProposalCard() re-fires; card shows new proposal | ✓ |
| Modal closes, navigate to proposal | Route change to proposal detail | |

**User's choice:** Modal closes, card refreshes via loadProposalCard().

---

### Q4: Should the project be pre-selected and locked in the Create modal?

| Option | Description | Selected |
|--------|-------------|----------|
| Pre-selected and locked | Project dropdown auto-selects and is disabled | ✓ |
| Pre-selected but editable | User can still change the project | |

**User's choice:** Pre-selected and locked — pass current project ID to openCreateProposalModal(), project dropdown auto-selects and is disabled. Also auto-fills target client from project.client_id.

---

### Q5: Should "Start Proposal" only appear when project status is exactly "For Proposal"?

| Option | Description | Selected |
|--------|-------------|----------|
| "For Proposal" status only | Only show when project_status === 'For Proposal' | ✓ |
| Any time no proposal is linked | Show button regardless of project status if no proposal exists | |

**User's choice:** "For Proposal" status only.

---

### Q6: Should the "Start Proposal" button also appear in service-detail.js?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — full parity with project-detail | Same logic in service-detail.js | ✓ |
| Projects only for now | Service-detail parity deferred | |

**User's choice:** Yes — full parity with project-detail.js.

---

### Q7: Should the proposal section appear for projects NOT in a proposal-range status?

| Option | Description | Selected |
|--------|-------------|----------|
| No — keep current behavior | Proposal section only for proposal-range statuses | |
| Show throughout once activated | Once a proposal is linked, card persists for full lifetime | ✓ |

**User's choice:** "Once proposal life cycle is activated it shows it throughout."

**Notes:** Confirmed — once a proposal is linked to a project, the inline card stays visible even when the project moves to later statuses like 'Client Approved', 'On-going', or 'Completed'. The `isInProposalRange` gate is removed from the render path; `loadProposalCard()` always fires.

---

## Edit permissions

### Q1: Should the inline card have its own 'Edit' button shortcut?

| Option | Description | Selected |
|--------|-------------|----------|
| Add 'Edit' button to inline card | Direct shortcut to openEditProposalModal() | |
| Full modal only — no change | Edit stays inside the full proposal modal | ✓ |

**User's choice:** Full modal only — no change.

---

### Q2: What specific permission issue is 'edit permissions' addressing?

| Option | Description | Selected |
|--------|-------------|----------|
| Editing at 'pending_client' status | Allow minor edits while proposal is with client | |
| Operations_user edit restrictions | Ops user gets blocked when saving edits | ✓ |
| No specific bug — covered by create | Phase name refers to create permissions already discussed | |

**User's choice:** Operations_user edit restrictions — ops_user gets a permission error when saving via openEditProposalModal even though canDrive is true.

**Notes:** Root cause: the 87.2 field-mask `['comms_log', 'audit_log', 'status', 'current_status_since', 'updated_at']` is too narrow. `saveProposal()` writes title, description, amount, target_client_id, target_client_name — none in the allowlist.

---

### Q3: How should the Firestore update rule be expanded?

| Option | Description | Selected |
|--------|-------------|----------|
| Expand field-mask to include edit fields | Add title, description, amount, target_client_id, etc. | ✓ |
| Status-gated full update | Allow full update when status is draft/for_revision | |

**User's choice:** Expand field-mask to include edit fields. Assignment + department checks stay.

---

### Q4: Should operations_user be allowed to CREATE new proposals?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — allow assigned ops_user to create | Add create rule with same assignment check | ✓ |
| Admins-only for create | Ops users can only update existing proposals | |

**User's choice:** Yes — allow assigned operations_user / services_user to create proposals. Without this, the "Start Proposal" button would fail for non-admin users.

---

## Inline card redesign

### Q1: What's the primary driver for the inline card redesign?

| Option | Description | Selected |
|--------|-------------|----------|
| Static 'Active Proposal' label should reflect lifecycle stage | Dynamic header showing stage | |
| Card needs more context fields | Additional data visible without opening modal | |
| Layout/visual polish | Spacing, typography, button arrangement | |
| All of the above | All three | ✓ |

**User's choice:** All of the above.

---

### Q2: Which additional fields should the inline card show?

| Option | Description | Selected |
|--------|-------------|----------|
| Target client name | Client the proposal was submitted to | |
| Submitter name | Who submitted the proposal | |
| Version number | Proposal version number | ✓ |
| You decide | "i cant seem to decide right now" | ✓ |

**User's choice:** Version number confirmed; other fields left to Claude's discretion.

---

### Q3: How should the lifecycle stage be communicated in the header?

| Option | Description | Selected |
|--------|-------------|----------|
| Replace 'Active Proposal' with the status label | Human-readable stage label replaces static text + removes badge | ✓ |
| Keep 'Active Proposal' label + status badge | No change to header structure | |
| Stage-section header above the card | Small heading above card | |

**User's choice:** Replace 'Active Proposal' with the status label. Status badge removed (no duplicate).

---

### Q4: Should additional action buttons appear on the inline card?

| Option | Description | Selected |
|--------|-------------|----------|
| No — keep just Submit + View | Minimal card; all other actions inside the modal | ✓ |
| Add 'Mark Sent to Client' shortcut | Quick action for the most common step | |
| Add 'Request Revision' shortcut | Quick action for revision cycle | |

**User's choice:** No — keep just Submit for Approval + View Proposal. Card stays minimal.

---

## Claude's Discretion

- **Status label color / dot**: whether to add a colored dot before the stage label using `getProposalStatusBadge` palette
- **Target client name**: may include if it fits without clutter; submitter name lower priority — omit if complex
- **"Start Proposal" card layout**: dashed-border card vs minimal banner; must feel actionable not empty
- **Exact styling values**: font weights, padding, button spacing — match existing detail-page card aesthetic
- **Firestore rules helper naming**: whether to extract `isProposalWriterAssigned()` or inline the predicate

## Deferred Ideas

- Edit button on inline card — rejected; editing stays in full modal
- Mark Sent / Request Revision quick actions on inline card — rejected; card stays minimal
- "Revision Sent" as distinct project_status — explicitly rejected (87.2 D-14)
- Multi-attachment per proposal — v4.1+
- Notification triggers for proposal create/edit by ops_user — v4.1+
