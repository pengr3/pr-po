---
phase: "87.3"
plan: "05"
type: execute
wave: 3
depends_on:
  - "87.3-04"
files_modified:
  - app/views/project-detail.js
  - app/views/service-detail.js
autonomous: true
requirements_addressed:
  - WR-1
  - WR-2
gap_closure: true

must_haves:
  truths:
    - "A proposal with status 'client_approved' displays the label 'Client Approved' (not the fallback 'Proposal') in the inline card header in both project-detail.js and service-detail.js"
    - "A proposal with status 'client_approved' shows a green dot (#059669) in the inline card header in both files"
    - "On a second init() call in service-detail.js without destroy(), the permission and assignment event handlers do not leak — exactly one handler is attached for each event type"
    - "The stored window._serviceDetailPermissionHandler and window._serviceDetailAssignmentHandler always reference the most-recently-created handler closure after init() returns"
  artifacts:
    - path: "app/views/project-detail.js"
      provides: "_proposalStageLabel with client_approved key, _proposalStatusDotColor with client_approved key"
      contains: "client_approved"
    - path: "app/views/service-detail.js"
      provides: "_proposalStageLabel and _proposalStatusDotColor with client_approved key; detach-then-reattach listener pattern for permission and assignment handlers"
      contains: "client_approved"
  key_links:
    - from: "renderInlineProposalCard (project-detail.js)"
      to: "_proposalStageLabel / _proposalStatusDotColor"
      via: "passes proposal.status; must return correct label/color for 'client_approved'"
      pattern: "_proposalStageLabel\\(proposal\\.status\\)"
    - from: "service-detail.js init()"
      to: "_serviceDetailPermissionHandler"
      via: "removeEventListener then addEventListener then unconditional overwrite"
      pattern: "window._serviceDetailPermissionHandler = permissionChangeHandler"
---

<objective>
Fix WR-1 and WR-2: replace the incorrect 'approved' status key with 'client_approved' in both inline card helper functions across both view files, and fix the stale listener guard in service-detail.js to mirror the detach-then-reattach pattern used by project-detail.js.

Purpose: WR-1 causes client-approved proposals to display "Proposal" label and a gray dot instead of "Client Approved" and green — visually misleading and functionally incorrect. WR-2 causes a handler closure leak on service-detail.js re-init which fires renderServiceDetail() against stale currentService state.

Output: 4 key replacements in _proposalStageLabel and _proposalStatusDotColor across two files; 2 listener registration blocks in service-detail.js refactored to match project-detail.js pattern.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Admin/Roaming/pr-po/.planning/PROJECT.md
@C:/Users/Admin/Roaming/pr-po/.planning/ROADMAP.md

<interfaces>
<!-- Extracted from app/views/project-detail.js — lines 1355-1379 -->

_proposalStageLabel (project-detail.js lines 1356-1366):
  const labels = { draft, pending_internal, pending_client, for_revision, approved: 'Client Approved', loss }
  Bug: key is 'approved' — must be 'client_approved'

_proposalStatusDotColor (project-detail.js lines 1369-1379):
  const colors = { draft, pending_internal, pending_client, for_revision, approved: '#059669', loss }
  Bug: key is 'approved' — must be 'client_approved'

<!-- Extracted from app/views/service-detail.js — lines 1107-1131 -->

_proposalStageLabel (service-detail.js lines 1108-1118): same structure, same bug
_proposalStatusDotColor (service-detail.js lines 1121-1131): same structure, same bug

<!-- Extracted from app/views/service-detail.js — listener registration (lines 72-91) -->

Current broken pattern (lines 73-91):
  const permissionChangeHandler = () => { renderServiceDetail(); };
  window.addEventListener('permissionsChanged', permissionChangeHandler);
  if (!window._serviceDetailPermissionHandler) {   // BUG: stale guard
      window._serviceDetailPermissionHandler = permissionChangeHandler;
  }
  const assignmentChangeHandler = () => { if (currentService) { checkServiceAccess(); } };
  window.addEventListener('assignmentsChanged', assignmentChangeHandler);
  if (!window._serviceDetailAssignmentHandler) {   // BUG: stale guard
      window._serviceDetailAssignmentHandler = assignmentChangeHandler;
  }

<!-- Extracted from app/views/project-detail.js — correct listener pattern (lines 80-99) -->

Correct pattern from project-detail.js:
  if (window._projectDetailPermissionHandler) {
      window.removeEventListener('permissionsChanged', window._projectDetailPermissionHandler);
  }
  const permissionChangeHandler = () => { renderProjectDetail(); };
  window.addEventListener('permissionsChanged', permissionChangeHandler);
  window._projectDetailPermissionHandler = permissionChangeHandler;  // unconditional overwrite

  if (window._projectDetailAssignmentHandler) {
      window.removeEventListener('assignmentsChanged', window._projectDetailAssignmentHandler);
  }
  const assignmentChangeHandler = () => { if (currentProject) checkProjectAccess(); };
  window.addEventListener('assignmentsChanged', assignmentChangeHandler);
  window._projectDetailAssignmentHandler = assignmentChangeHandler;  // unconditional overwrite
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix client_approved key in project-detail.js helper functions</name>
  <files>app/views/project-detail.js</files>
  <read_first>
    Read app/views/project-detail.js lines 1354-1380 to see both _proposalStageLabel and _proposalStatusDotColor in full and confirm the exact location of the 'approved:' keys before editing.
  </read_first>
  <action>
    In `_proposalStageLabel` (around line 1362), change the object key from `approved:` to `client_approved:`. The value 'Client Approved' stays unchanged.

    In `_proposalStatusDotColor` (around line 1375), change the object key from `approved:` to `client_approved:`. The value '#059669' stays unchanged.

    These are the only two changes in this file. Do not touch any other keys, values, function names, or surrounding code. Do not add a 'won' key or any other status key.
  </action>
  <verify>
    <automated>grep -n "approved" app/views/project-detail.js | grep -i "proposalStage\|proposalStatus\|labels\|colors\|client_approved"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "client_approved:" app/views/project-detail.js returns exactly 2 (one per function)
    - grep -c "^\s*approved:" app/views/project-detail.js returns 0 (old bare key gone from both maps — note: this grep checks leading whitespace + 'approved:' without 'client_')
    - The function _proposalStageLabel still has exactly 6 keys: draft, pending_internal, pending_client, for_revision, client_approved, loss
    - The function _proposalStatusDotColor still has exactly 6 keys with the same names
    - No other lines in project-detail.js were modified (git diff --stat should show only project-detail.js with 2 changed lines)
  </acceptance_criteria>
  <done>project-detail.js _proposalStageLabel and _proposalStatusDotColor both use 'client_approved' as the key; a proposal with status 'client_approved' will render "Client Approved" label and green dot</done>
</task>

<task type="auto">
  <name>Task 2: Fix client_approved key and listener leak in service-detail.js</name>
  <files>app/views/service-detail.js</files>
  <read_first>
    Read app/views/service-detail.js lines 68-95 (full listener registration block inside init()) to see both handlers and the stale if-guard pattern.
    Read app/views/service-detail.js lines 1107-1132 to see both _proposalStageLabel and _proposalStatusDotColor in full.
    Read app/views/project-detail.js lines 78-102 (correct listener pattern) as the reference for the fix shape.
  </read_first>
  <action>
    CHANGE 1 — _proposalStageLabel (around line 1114): change key `approved:` to `client_approved:`. Value 'Client Approved' unchanged.

    CHANGE 2 — _proposalStatusDotColor (around line 1127): change key `approved:` to `client_approved:`. Value '#059669' unchanged.

    CHANGE 3 — Listener registration block in init() (around lines 73-91): Replace the two if-guarded registration blocks with the detach-then-reattach pattern. The replacement must:

    For the permission handler:
    - First: if (window._serviceDetailPermissionHandler) { window.removeEventListener('permissionsChanged', window._serviceDetailPermissionHandler); }
    - Then: declare const permissionChangeHandler = () => { renderServiceDetail(); };
    - Then: window.addEventListener('permissionsChanged', permissionChangeHandler);
    - Then: window._serviceDetailPermissionHandler = permissionChangeHandler;   (unconditional assignment — NO if-guard)

    For the assignment handler:
    - First: if (window._serviceDetailAssignmentHandler) { window.removeEventListener('assignmentsChanged', window._serviceDetailAssignmentHandler); }
    - Then: declare const assignmentChangeHandler = () => { if (currentService) { checkServiceAccess(); } };
    - Then: window.addEventListener('assignmentsChanged', assignmentChangeHandler);
    - Then: window._serviceDetailAssignmentHandler = assignmentChangeHandler;   (unconditional assignment — NO if-guard)

    The comment above the permission handler block should remain: "// Listen for permission changes and re-render"
    The comment above the assignment handler block should remain: "// Re-check access when assignments change"
    The destroy() function (lines 180-189) removes and deletes these handlers — do NOT change destroy().
    The callback body inside permissionChangeHandler must call renderServiceDetail() (not renderProjectDetail).
    The callback body inside assignmentChangeHandler must call checkServiceAccess() (not checkProjectAccess).
  </action>
  <verify>
    <automated>grep -n "_serviceDetailPermissionHandler\|_serviceDetailAssignmentHandler" app/views/service-detail.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "client_approved:" app/views/service-detail.js returns exactly 2 (one per helper function)
    - grep -c "if (!window._serviceDetailPermissionHandler)" app/views/service-detail.js returns 0 (stale guard gone)
    - grep -c "if (!window._serviceDetailAssignmentHandler)" app/views/service-detail.js returns 0 (stale guard gone)
    - grep -n "window._serviceDetailPermissionHandler = permissionChangeHandler" app/views/service-detail.js returns exactly 1 match (the unconditional assignment inside init)
    - grep -n "window._serviceDetailAssignmentHandler = assignmentChangeHandler" app/views/service-detail.js returns exactly 1 match (the unconditional assignment inside init)
    - grep -n "removeEventListener.*permissionsChanged" app/views/service-detail.js returns exactly 2 matches (one in init's detach block, one in destroy)
    - grep -n "removeEventListener.*assignmentsChanged" app/views/service-detail.js returns exactly 2 matches (one in init's detach block, one in destroy)
    - destroy() function body is unchanged (still deletes both window._serviceDetail* vars)
  </acceptance_criteria>
  <done>service-detail.js _proposalStageLabel and _proposalStatusDotColor use 'client_approved' key; listener registration in init() uses detach-then-reattach with unconditional stored-reference overwrite — no closure leak on re-init</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM event→handler | permissionsChanged / assignmentsChanged custom events arrive from auth module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.3-05-01 | Denial of Service | stale handler accumulation on re-init | mitigate | detach-then-reattach pattern prevents unbounded handler growth |
| T-87.3-05-02 | Information Disclosure | stale currentService in leaked closure | accept | service-detail data is only shown to authenticated users with role-checked access; stale render is a UX issue, not a security boundary crossing |
</threat_model>

<verification>
1. Open a proposal with status 'client_approved' in project-detail.js inline card — verify label reads "Client Approved" and dot is green (#059669)
2. Open a proposal with status 'client_approved' in service-detail.js inline card — same check
3. Navigate to service-detail.js, navigate away (same view, different tab triggers re-init without destroy), confirm no duplicate event handler fires via browser console (no doubled renderServiceDetail calls on permissionsChanged)
</verification>

<success_criteria>
- project-detail.js and service-detail.js both have 'client_approved' as the key in _proposalStageLabel and _proposalStatusDotColor (4 total replacements)
- service-detail.js init() listener registration uses detach-then-reattach, unconditional stored-reference overwrite — no if-guard on assignment
- destroy() in service-detail.js is unchanged
- No other code in either file was modified
</success_criteria>

<output>
After completion, create `.planning/phases/87.3-proposal-card-polish-create-from-detail-edit-permissions-inl/87.3-05-SUMMARY.md`
</output>
