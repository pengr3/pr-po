---
phase: "87.3"
plan: "05"
subsystem: "proposals / detail views"
tags: [bug-fix, listener-leak, proposal-status, inline-card]
dependency_graph:
  requires: ["87.3-04"]
  provides: ["WR-1 fix: client_approved label+dot", "WR-2 fix: service-detail listener dedup"]
  affects: ["app/views/project-detail.js", "app/views/service-detail.js"]
tech_stack:
  added: []
  patterns: ["detach-then-reattach listener pattern"]
key_files:
  modified:
    - app/views/project-detail.js
    - app/views/service-detail.js
decisions:
  - "client_approved is the correct Firestore status key — 'approved' was never written to Firestore; 'client_approved' is the canonical enum value from Phase 87 design"
  - "Detach-then-reattach with unconditional stored-reference overwrite mirrors project-detail.js pattern — no if-guard on assignment so re-init always uses the newest closure"
metrics:
  duration: "~5 minutes"
  completed: "2026-05-22"
  tasks_completed: 2
  tasks_total: 2
---

# Phase 87.3 Plan 05: Fix client_approved key and service-detail listener leak Summary

**One-liner:** Replace incorrect `approved` status key with `client_approved` in both detail view helper functions, and apply detach-then-reattach listener pattern in service-detail.js to eliminate WR-2 closure leak.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Fix client_approved key in project-detail.js helper functions | 4e7355e | app/views/project-detail.js |
| 2 | Fix client_approved key and listener leak in service-detail.js | f6ecb99 | app/views/service-detail.js |

## Changes Made

### project-detail.js
- `_proposalStageLabel`: key `approved` → `client_approved` (value `'Client Approved'` unchanged)
- `_proposalStatusDotColor`: key `approved` → `client_approved` (value `'#059669'` unchanged)
- Only 2 lines changed; no other code touched

### service-detail.js
- `_proposalStageLabel`: key `approved` → `client_approved` (CHANGE 1)
- `_proposalStatusDotColor`: key `approved` → `client_approved` (CHANGE 2)
- Listener registration block in `init()` (CHANGE 3): replaced stale if-guard pattern with detach-then-reattach:
  - `if (window._serviceDetailPermissionHandler) { removeEventListener... }` before creating new handler
  - Unconditional `window._serviceDetailPermissionHandler = permissionChangeHandler` (no if-guard)
  - Same shape for `_serviceDetailAssignmentHandler`
- `destroy()` unchanged

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. All changes are pure client-side logic corrections within existing functions.

## Self-Check

Automated verification results:
- `grep -c "client_approved:" app/views/project-detail.js` → **2** (pass)
- `grep -c "^\s*approved:" app/views/project-detail.js` → **0** (pass)
- `grep -c "client_approved:" app/views/service-detail.js` → **2** (pass)
- `grep -c "if (!window._serviceDetailPermissionHandler)" app/views/service-detail.js` → **0** (pass)
- `grep -c "if (!window._serviceDetailAssignmentHandler)" app/views/service-detail.js` → **0** (pass)
- `grep -n "window._serviceDetailPermissionHandler = permissionChangeHandler"` → **1 match** at line 81 (pass)
- `grep -n "window._serviceDetailAssignmentHandler = assignmentChangeHandler"` → **1 match** at line 93 (pass)
- `removeEventListener.*permissionsChanged` → **2 matches** (lines 75 + 184 — init + destroy) (pass)
- `removeEventListener.*assignmentsChanged` → **2 matches** (lines 85 + 189 — init + destroy) (pass)
- destroy() body at lines 182-196 unchanged (pass)

## Self-Check: PASSED
