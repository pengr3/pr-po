---
phase: 65.5-fix-po-ref-column-with-clickable-po-ids-in-rfp-processing
plan: 01
subsystem: ui
tags: [finance, rfp, po-details, modal, firestore]

# Dependency graph
requires:
  - phase: 65.1-finance-payables-tab-dual-table-revamp-rfp-po-payments
    provides: RFP Processing table with PO Ref column and rfpsData structure
provides:
  - Clickable PO IDs in Finance RFP Processing table that open a PO details modal
affects: [finance-payables-tab, rfp-processing-table]

# Tech tracking
tech-stack:
  added: []
  patterns: [getDoc single-doc fetch for on-demand detail modal, overlay pattern with click-outside-to-close]

key-files:
  created: []
  modified:
    - app/views/finance.js

key-decisions:
  - "rfp.po_id in finance.js is the Firestore document ID of the PO — used directly as poDocId in getDoc(doc(db, 'pos', poDocId))"
  - "Modal overlay uses id=poDetailsOverlay to allow remove-before-create deduplication"
  - "viewPODetailsFromRFP reuses formatPODate, formatCurrency, escapeHTML, getMRFLabel — no new imports needed"

patterns-established:
  - "On-demand PO detail modal: fetch single doc via getDoc, build overlay, append to body, click-outside + X button to close"

requirements-completed:
  - POREF-01

# Metrics
duration: 8min
completed: 2026-03-24
---

# Phase 65.5 Plan 01: Fix PO Ref Column with Clickable PO IDs in RFP Processing Summary

**Clickable PO IDs in Finance RFP Processing table — onclick fetches PO from Firestore and renders a detail modal with metadata grid and line items**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-24T04:46:00Z
- **Completed:** 2026-03-24T04:54:45Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added `async function viewPODetailsFromRFP(poDocId)` to finance.js that fetches PO details from Firestore on click
- Wired `onclick="window.viewPODetailsFromRFP(...)"` on the PO Ref anchor in `renderRFPTable()` row template
- Registered `window.viewPODetailsFromRFP` in `initFinanceView()` and cleaned it up in `destroy()`

## Task Commits

Each task was committed atomically:

1. **Task 1: Add viewPODetailsFromRFP and wire clickable PO IDs in RFP table** - `8f99968` (feat)

**Plan metadata:** _(docs commit — see below)_

## Files Created/Modified
- `app/views/finance.js` - Added viewPODetailsFromRFP function, wired onclick on PO Ref cell, registered/cleaned up window function

## Decisions Made
- `rfp.po_id` in the RFP data is the Firestore document ID (confirmed by posAmountMap usage at line 750) — used directly as `poDocId` in `getDoc(doc(db, 'pos', poDocId))`
- Modal overlay id `poDetailsOverlay` allows idempotent removal before creation, preventing duplicate overlays

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Finance RFP Processing table now has fully clickable PO IDs with detail modal
- Modal pattern (overlay with grid + items table) is consistent with procurement.js viewPODetails

---
*Phase: 65.5-fix-po-ref-column-with-clickable-po-ids-in-rfp-processing*
*Completed: 2026-03-24*
