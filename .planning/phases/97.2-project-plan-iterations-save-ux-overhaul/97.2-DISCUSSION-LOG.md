# Phase 97.2: Project Plan Iterations — Save Model Overhaul - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-02
**Phase:** 97.2-project-plan-iterations-save-ux-overhaul
**Areas discussed:** Auto-snapshot fate, Undo safety net, Rail card redesign, Save trigger UX

---

## Auto-snapshot fate

| Option | Description | Selected |
|--------|-------------|----------|
| Hidden in Firestore, never in rail | Auto-snapshot still written to Firestore; never rendered in rail. Only undo toast reveals it. | ✓ |
| In-memory only, no Firestore write | JS memory for 5 seconds, no Firestore write — cheaper but undo dies on page refresh. | |
| Separate collapsed section in rail | Auto-saves move to collapsible "Auto-saves" section below named saves. | |
| Delete auto-snapshot after undo window expires | Written then silently deleted when undo toast dismisses. | |

**User's choice:** Hidden in Firestore, never in rail

---

| Option | Description | Selected |
|--------|-------------|----------|
| Silently hidden from rail (no delete) | renderIterRail() filters iter.auto === true; existing docs stay in Firestore untouched. | ✓ |
| Delete old auto-snapshots on init() | Batch-delete all auto:true docs on load. Cleans Firestore but destructive. | |
| Leave as-is for now | Keep existing docs, just filter from rail — accumulate silently. | |

**User's choice:** Silently hidden from rail (no delete)

---

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — remove auto tag CSS and logic | Remove iter-auto-tag chip, .auto dot class, and CSS. Dead code after filtering. | ✓ |
| Keep the auto tag code but hidden | Leave rendering code in place for hypothetical future use. | |

**User's choice:** Yes — remove auto tag CSS and logic

---

## Undo safety net

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — update copy to clarify it's ephemeral | New copy: 'Loaded "X" — Undo to revert.' No reference to "auto-saved." | ✓ |
| Keep current toast copy | Leave 'Previous state auto-saved.' as-is. | |
| Remove undo toast entirely | No toast, no undo window. Immediate permanent load. | |

**User's choice:** Yes — update copy

---

| Option | Description | Selected |
|--------|-------------|----------|
| Delete from Firestore on toast dismiss | dismissUndoToast() also calls deleteDoc(autoSnapId). Keeps project_iterations clean. | ✓ |
| Leave in Firestore forever (current behavior) | Auto-snapshot stays after undo window closes. | |
| Keep for 24h then expire | Out of scope (requires backend job). | |

**User's choice:** Delete from Firestore on toast dismiss

---

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — fold WR-01 fix in here | Add dismissUndoToast() unconditionally at top of restoreIteration() before iter.auto check. | ✓ |
| No — ship separately | Defer to standalone quick fix. | |

**User's choice:** Yes — fold WR-01 fix in here

---

## Rail card redesign

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — add a delete button | × icon per card with confirm → deleteDoc. Users can clean history. | ✓ |
| No delete — permanent records | Keep read-only cards; rail grows indefinitely. | |
| Bulk delete only | 'Clear all' in rail header rather than per-card. | |

**User's choice:** Yes — add a delete button

---

| Option | Description | Selected |
|--------|-------------|----------|
| Re-sequence — numbered 1, 2, 3... for named saves only | idx + 1 in filtered render loop. _iterSeq already tracks non-auto count. | ✓ |
| Keep current global position numbers | Numbers skip (1, 3, 5…) — confusing. | |
| Remove numbered dots entirely | Drop dot gutter — plain content rows. | |

**User's choice:** Re-sequence for named saves only

---

| Option | Description | Selected |
|--------|-------------|----------|
| Relative for recent, absolute for older | < 24h: "2 hours ago"; ≥ 24h: "Jun 2, 2026". | ✓ |
| Always absolute date | Current behavior — toLocaleDateString() always. | |
| Always relative time | "3 days ago", "2 months ago" — loses precision. | |

**User's choice:** Relative for recent, absolute for older

---

## Save trigger UX

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — styled inline modal | Centered modal matching app style; name input prefilled; Save/Cancel. | ✓ |
| Keep window.prompt() | Native browser prompt. Zero-code but looks out-of-place. | |
| Inline input in rail header | Input field inline in rail. Requires rail to be open first. | |

**User's choice:** Yes — styled inline modal

---

| Option | Description | Selected |
|--------|-------------|----------|
| Keep both entry points, same modal for both | Toolbar + rail both open same modal. No behavioral difference. | ✓ |
| Remove toolbar 'Save Iteration', rail-only | Force users to open rail to save. Adds friction. | |
| Toolbar button auto-opens rail after save | Opens modal, saves, then auto-opens rail to show new entry. | |

**User's choice:** Keep both entry points, same modal for both

---

| Option | Description | Selected |
|--------|-------------|----------|
| Keep silent truncation at 60 chars | Current label.substring(0, 60) behavior preserved. | ✓ |
| Show character counter in modal | Add "12/60" counter below input. | |
| Set maxlength=60 on input | HTML attribute stops typing past 60. | |

**User's choice:** Keep silent truncation at 60 chars

---

## Claude's Discretion

- Exact placement of WR-01 fix: `dismissUndoToast()` before or after the `if (!iter) return` early exit — either is valid. Planner decides.
- Delete confirmation UX detail: inline text or separate modal — lightweight inline text preferred given `openIterConfirm()` pattern.

## Deferred Ideas

None — discussion stayed within phase scope.
