---
phase: "97.2"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/project-plan.js
  - styles/views.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Auto-snapshot entries (iter.auto === true) never appear in the iteration history rail"
    - "The badge count on the History button reflects only visible (non-auto) iterations"
    - "Each timeline card shows a relative timestamp: '< 1h ago', 'Xh ago', or a localized date"
    - "Each timeline card has a delete button that triggers an inline confirm and then removes the iteration"
    - "Dot numbering in the rail is sequential from 1 for visible iterations only"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "renderIterRail() with filter, _formatIterTimestamp helper, delete button, re-sequenced numbers"
      contains: "filter(i => !i.auto)"
    - path: "styles/views.css"
      provides: "CSS for delete button; .iter-auto-tag and .iter-tl-dot.auto removed"
  key_links:
    - from: "renderIterRail()"
      to: "_iterations"
      via: ".filter(i => !i.auto).map((iter, idx) => ...)"
      pattern: "filter\\(i => !i\\.auto\\)"
    - from: "_formatIterTimestamp()"
      to: "iter.saved_at"
      via: ".toDate() then diff < 3600000 / 86400000"
      pattern: "_formatIterTimestamp"
---

<objective>
Overhaul `renderIterRail()` in `app/views/project-plan.js` to hide auto-snapshots, show relative timestamps, add per-card delete buttons, and re-sequence dot numbering. Remove the `iter-auto-tag` chip rendering and its CSS.

Purpose: The iteration rail should only show deliberate named saves. Auto-snapshots are an undo safety net — they belong in Firestore but not in the visible UI. Timestamps should communicate recency. Delete buttons give users control over their history.

Output: Modified `renderIterRail()`, new `_formatIterTimestamp()` helper function, `window.deleteIteration()` handler, CSS for the delete button, and removal of dead auto-tag CSS rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/97.2-project-plan-iterations-save-ux-overhaul/97.2-CONTEXT.md
@.planning/phases/97-project-plan-iterations/97-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _formatIterTimestamp helper and overhaul renderIterRail render loop</name>
  <files>app/views/project-plan.js</files>

  <read_first>
    - app/views/project-plan.js lines 3363–3412 — the full renderIterRail() function (render loop, badge update, empty-state, dot class, auto-tag chip, savedLabel, taskCount, isActive, the returned HTML)
    - app/views/project-plan.js lines 112–120 — module-scope state variables (_iterations, _iterSeq, _autoSnapId) to confirm naming
    - app/views/project-plan.js lines 340–360 — the init() section where window functions are assigned, to know where to add window.deleteIteration
    - app/views/project-plan.js lines 605–625 — the destroy() section to know where to add delete window.deleteIteration
    - .planning/phases/97.2-project-plan-iterations-save-ux-overhaul/97.2-CONTEXT.md — D-01, D-03, D-07, D-08, D-09 decisions
  </read_first>

  <action>
    STEP 1 — Add _formatIterTimestamp helper.
    Insert the following pure function immediately before the renderIterRail() function definition (around line 3363). Do not use a fenced code block in this action — implement the function with this logic:
    - Function name: _formatIterTimestamp(savedAt)
    - If savedAt is falsy or savedAt.toDate is not a function, return the string 'Just now'.
    - Get date = savedAt.toDate().
    - Compute diff = Date.now() - date.getTime().
    - If diff is less than 3600000 (one hour in ms), return the string '< 1h ago'.
    - Else if diff is less than 86400000 (24 hours in ms), return Math.floor(diff / 3600000) + 'h ago'.
    - Else return date.toLocaleDateString().

    STEP 2 — Overhaul renderIterRail().
    Current renderIterRail() at line 3363 begins with: const timeline = document.getElementById('iterRailTimeline') and uses _iterations directly. Apply all of the following changes:

    a. At the top of the function body (after the early return if !timeline), compute:
       const visible = _iterations.filter(i => !i.auto);
    
    b. Change the badge update from:
       badge.textContent = _iterations.length
       to:
       badge.textContent = visible.length
    
    c. Change the empty-state guard from:
       if (_iterations.length === 0)
       to:
       if (visible.length === 0)
    
    d. Change the render loop from:
       timeline.innerHTML = _iterations.map((iter, idx) => {
       to:
       timeline.innerHTML = visible.map((iter, idx) => {
    
    e. Change the savedLabel line from:
       const savedLabel = iter.saved_at?.toDate?.().toLocaleDateString() ?? 'Just now';
       to:
       const savedLabel = _formatIterTimestamp(iter.saved_at);
    
    f. Change the dot number from:
       ${_iterations.length - idx}
       to:
       ${visible.length - idx}
    
    g. Remove the auto class from the dot. Change:
       <div class="iter-tl-dot ${iter.auto ? 'auto' : ''}">
       to:
       <div class="iter-tl-dot">
       (The auto class is being removed from CSS in Task 2; since auto items are filtered out, the conditional is dead code — remove it.)
    
    h. Remove the auto-tag chip entirely. Delete the template expression:
       ${iter.auto ? ' <span class="iter-auto-tag">auto</span>' : ''}
       from the .iter-tl-name line. The name line should become simply:
       <div class="iter-tl-name">${escapeHTML(iter.label)}</div>
    
    i. Add a delete button to each card. After the closing </div> of .iter-tl-actions, add a delete button as a sibling of .iter-tl-actions (or append it inside .iter-tl-actions after the existing load-btn). The button:
       - class: iter-tl-delete-btn
       - onclick: window.deleteIteration('${iter.id}')
       - text content: x (the multiplication sign ×, unicode ×)
       - title attribute: "Delete iteration"
    
    STEP 3 — Add window.deleteIteration handler.
    Add the function deleteIteration(iterationId) to the file (near the other iter-related functions, after renderIterRail()). The function:
    - Finds the iter in _iterations by id (const iter = _iterations.find(i => i.id === iterationId)).
    - If not found, returns early.
    - Shows an inline confirm via document.createElement('div') with the same createElement pattern as openIterConfirm() at line 3423. The modal overlay uses:
      position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:rgba(0,0,0,0.3);z-index:10001;
      Inner card: background:#fff;border-radius:8px;padding:28px 24px;min-width:320px;max-width:420px;box-shadow:0 8px 32px rgba(0,0,0,0.2);
    - The modal title is "Delete Iteration". The body text is: Delete "${iter.label}"? This cannot be undone.
    - Two buttons: Cancel (removes modal, no action) and Delete (red background #ef4444, calls the deleteDoc logic then removes modal).
    - On confirm: call await deleteDoc(doc(db, 'project_iterations', iterationId)). Then call await loadIterations() and renderIterRail(). Wrap in try/catch; on error call showToast('Failed to delete iteration.', 'error').
    - Assign window.deleteIteration = deleteIteration in the same init() block where other window functions are assigned (lines ~340–360).
    - Add delete window.deleteIteration in destroy() alongside the other delete window.X calls (lines ~605–625).

    Per D-01, D-02, D-03, D-07, D-08, D-09.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('app/views/project-plan.js','utf8');const ok=[c.includes('_formatIterTimestamp'),c.includes('filter(i => !i.auto)'),c.includes('visible.map('),c.includes('visible.length'),c.includes('< 1h ago'),c.includes('h ago'),!c.includes('iter-auto-tag'),c.includes('deleteIteration'),c.includes('window.deleteIteration'),c.includes('delete window.deleteIteration')];console.log(ok.every(Boolean)?'PASS':'FAIL — '+ok.map((v,i)=>v?null:i).filter(Boolean))"</automated>
  </verify>

  <acceptance_criteria>
    - renderIterRail() contains the expression .filter(i => !i.auto) applied to _iterations before the .map() call
    - renderIterRail() uses visible.length (not _iterations.length) for the badge count and empty-state check
    - _formatIterTimestamp function exists and contains the string '< 1h ago', the expression Math.floor(diff / 3600000), and toLocaleDateString()
    - The template string in renderIterRail() no longer contains iter-auto-tag (the chip is gone)
    - The template string in renderIterRail() no longer contains iter.auto ? (no auto class conditional on the dot)
    - The template string in renderIterRail() contains iter-tl-delete-btn (delete button present on each card)
    - deleteIteration function exists in the file
    - window.deleteIteration = deleteIteration appears in init()
    - delete window.deleteIteration appears in destroy()
  </acceptance_criteria>

  <done>renderIterRail() renders only non-auto iterations, uses relative timestamps, shows a delete button on each card, and numbers dots sequentially from visible items. _formatIterTimestamp handles all three time cases. deleteIteration shows a confirm modal then calls deleteDoc.</done>
</task>

<task type="auto">
  <name>Task 2: Remove iter-auto-tag and .iter-tl-dot.auto CSS; add .iter-tl-delete-btn CSS</name>
  <files>styles/views.css</files>

  <read_first>
    - styles/views.css lines 2460–2530 — the full iteration rail CSS block containing .iter-tl-dot, .iter-tl-dot.auto, .iter-tl-content, .iter-tl-name, .iter-auto-tag, .iter-tl-actions, .iter-tl-btn rules
    - .planning/phases/97.2-project-plan-iterations-save-ux-overhaul/97.2-CONTEXT.md — D-03 (remove .iter-auto-tag and .iter-tl-dot.auto) and D-07 (delete button style)
  </read_first>

  <action>
    STEP 1 — Remove dead CSS rules.
    Delete the entire .iter-tl-dot.auto block at line 2471:
      .iter-tl-dot.auto { background: #d1fae5; border-color: #059669; color: #059669; }
    Delete the entire .iter-auto-tag block at line 2492:
      .iter-auto-tag { background: #d1fae5; color: #065f46; font-size: 9px; font-weight: 700; padding: 1px 5px; border-radius: 6px; text-transform: uppercase; }
    These are dead code after D-01 (auto items filtered from render) and D-03 (chip removed from HTML).

    STEP 2 — Add .iter-tl-delete-btn CSS.
    After the .iter-tl-actions block (around line 2501), add a new CSS rule for .iter-tl-delete-btn:
    - position: absolute with right: 6px; top: 6px — OR — float: right style on the card. Simpler: add it as position:absolute inside the .iter-timeline-item container. Ensure .iter-timeline-item has position:relative if it does not already.
    - Alternatively (and simpler since the delete button sits inside .iter-tl-actions alongside the other buttons): style it as a small inline button with:
      padding: 3px 6px; font-size: 11px; font-weight: 700; border: none; border-radius: 4px; background: #fee2e2; color: #ef4444; cursor: pointer; margin-left: auto;
    - Add :hover style: background: #ef4444; color: #fff;
    - The margin-left:auto pushes it to the right end of the flex row in .iter-tl-actions.

    Per D-03, D-07.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('styles/views.css','utf8');const ok=[!c.includes('iter-auto-tag'),!c.includes('iter-tl-dot.auto'),c.includes('iter-tl-delete-btn')];console.log(ok.every(Boolean)?'PASS':'FAIL — '+ok.map((v,i)=>v?null:i).filter(Boolean))"</automated>
  </verify>

  <acceptance_criteria>
    - styles/views.css does NOT contain the string iter-auto-tag (block fully removed)
    - styles/views.css does NOT contain the string iter-tl-dot.auto (block fully removed)
    - styles/views.css contains iter-tl-delete-btn with at least background and color rules
  </acceptance_criteria>

  <done>.iter-auto-tag and .iter-tl-dot.auto CSS blocks deleted. .iter-tl-delete-btn CSS added with a red-tinted hover style that pushes to the right of the action row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → deleteIteration modal | iteration label is rendered in modal body HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97.2-01 | Tampering | deleteIteration confirm modal body | mitigate | Use escapeHTML(iter.label) when injecting label into the confirm modal innerHTML — same pattern as openIterConfirm() at line 3435 |
| T-97.2-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete, open the project plan view in a browser:
1. Open the iteration history rail — only manually named iterations appear (no "Auto-save before..." entries).
2. Badge count on the History button matches the number of visible iteration cards.
3. Iterations saved less than 1 hour ago show "< 1h ago"; those saved 1–23 hours ago show "Xh ago"; older ones show a locale date string.
4. Each card has a × delete button; clicking it shows a confirm modal. Confirming removes the entry from Firestore and re-renders the rail.
5. Dot numbers count up cleanly from 1 (bottom) to N (top) with no gaps.
</verification>

<success_criteria>
- renderIterRail() filters auto-snapshots before rendering (D-01, D-02)
- iter-auto-tag chip and .auto class are gone from JS and CSS (D-03)
- Relative timestamps display correctly for all three age ranges (D-09)
- Per-card delete button triggers confirm modal then deleteDoc (D-07)
- Dot numbers reflect visible iteration count only (D-08)
</success_criteria>

<output>
Create `.planning/phases/97.2-project-plan-iterations-save-ux-overhaul/97.2-01-SUMMARY.md` when done.
</output>
