---
phase: "97.2"
plan: "03"
type: execute
wave: 3
depends_on: ["97.2-02"]
files_modified:
  - app/views/project-plan.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "Clicking 'Save Iteration' (toolbar or rail button) opens a styled modal with a pre-filled name input, not a browser prompt"
    - "The modal input is pre-filled with 'Iteration N' where N is _iterSeq + 1"
    - "Both the toolbar Save Iteration button and the rail '+ Save current state' button trigger the same modal"
    - "Saving truncates the label to 60 characters silently"
    - "window.prompt( no longer appears in project-plan.js"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "saveIteration() with inline modal replacing window.prompt()"
      contains: "iterSaveModal"
  key_links:
    - from: "saveIteration()"
      to: "iterSaveModal DOM"
      via: "document.createElement('div') with id iterSaveModal"
      pattern: "iterSaveModal"
    - from: "modal Save button"
      to: "saveIteration(label)"
      via: "saveIteration(labelInput.value.trim())"
      pattern: "saveIteration"
---

<objective>
Replace the `window.prompt()` call in `saveIteration()` with a styled inline modal matching the `openIterConfirm()` pattern. The modal has a pre-filled text input, Save and Cancel buttons, and 60-char silent truncation at submit time.

Purpose: `window.prompt()` is a browser-native dialog with no styling, blocks the thread, and is blocked in some contexts (iframes, strict CSPs). The existing `openIterConfirm()` pattern provides a clean, consistent modal solution that matches the app's UI.

Output: Modified `saveIteration()` — the early prompt branch is replaced with modal creation. Both toolbar and rail entry points continue to call `saveIteration(null)` which triggers the new modal path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/97.2-project-plan-iterations-save-ux-overhaul/97.2-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace window.prompt() in saveIteration() with styled inline modal</name>
  <files>app/views/project-plan.js</files>

  <read_first>
    - app/views/project-plan.js lines 3313–3361 — the full saveIteration() and promptSaveIteration() functions; the prompt branch is at lines 3314–3316
    - app/views/project-plan.js lines 3423–3444 — openIterConfirm() in full — this is the EXACT style reference (overlay cssText, inner card style, button styles, document.createElement pattern) to replicate for the save modal
    - .planning/phases/97.2-project-plan-iterations-save-ux-overhaul/97.2-CONTEXT.md — D-10, D-11, D-12
  </read_first>

  <action>
    The current saveIteration() begins:
      async function saveIteration(label = null) {
          if (label === null) {
              label = window.prompt(`Save iteration as:`, `Iteration ${_iterSeq + 1}`);
              if (label === null) return; // user cancelled
          }
          label = label.trim();
          if (!label) return;
          label = label.substring(0, 60);
          try { ... }
      }

    Replace the window.prompt branch (the entire if (label === null) block at lines 3314–3316) with an inline modal. The new logic when label === null:

    1. Remove any existing save modal: document.getElementById('iterSaveModal')?.remove()
    2. Create a modal div with: modal.id = 'iterSaveModal'
    3. Overlay style (same as openIterConfirm): position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:rgba(0,0,0,0.3);z-index:10001;
    4. Inner card style: background:#fff;border-radius:8px;padding:28px 24px;min-width:320px;max-width:420px;box-shadow:0 8px 32px rgba(0,0,0,0.2);
    5. Modal content:
       - Title (h3): "Save Iteration" (font-size:16px;font-weight:700;color:#1e293b;margin:0 0 12px)
       - Label element (label) with text "Name:" (font-size:13px;color:#475569;display:block;margin-bottom:6px)
       - Text input with id iterSaveLabelInput, value pre-filled to Iteration ${_iterSeq + 1}, style: width:100%;padding:7px 10px;font-size:13px;border:1px solid #e5e7eb;border-radius:6px;box-sizing:border-box;margin-bottom:16px
       - Button row (div with display:flex;justify-content:flex-end;gap:8px):
         - Cancel button: removes modal on click, same style as openIterConfirm Cancel button (padding:7px 16px;font-size:13px;border:1px solid #e5e7eb;border-radius:6px;background:#fff;color:#475569;cursor:pointer)
         - Save button: calls the save logic on click, same style as openIterConfirm Load button (padding:7px 16px;font-size:13px;border:none;border-radius:6px;background:#1a73e8;color:#fff;cursor:pointer;font-weight:600)
    6. Append modal to document.body.
    7. Focus the input after appending: document.getElementById('iterSaveLabelInput')?.select()
    8. Return early from saveIteration() — do NOT fall through to the try { ... } block. The modal Save button onclick handles calling saveIteration(label) with the actual value.

    The Save button onclick:
    - Reads: const lbl = document.getElementById('iterSaveLabelInput')?.value?.trim()
    - If lbl is empty, do nothing (keep modal open).
    - Remove the modal: document.getElementById('iterSaveModal')?.remove()
    - Call: saveIteration(lbl) — this re-enters saveIteration with a non-null label, skipping the modal branch and proceeding to the try { ... } save logic.

    The remainder of saveIteration() (label.trim(), label.substring(0,60), try { addDoc ... }) is unchanged.

    ALSO: Add keydown Enter handler to the input. In the modal setup, after creating the input element via innerHTML, attach: the Save button's click logic should also fire when the user presses Enter in the input field. Implement by selecting the Save button and calling .click() on Enter keydown on the input, or by inline onkeydown="if(event.key==='Enter'){this.closest('#iterSaveModal').querySelector('.iter-save-confirm-btn').click()}"

    Give the Save button a class iter-save-confirm-btn so the input's onkeydown can target it.

    Per D-10, D-11, D-12.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('app/views/project-plan.js','utf8');const ok=[!c.includes('window.prompt('),c.includes('iterSaveModal'),c.includes('iterSaveLabelInput'),c.includes('Iteration ${_iterSeq + 1}'),c.includes('iter-save-confirm-btn')];console.log(ok.every(Boolean)?'PASS':'FAIL — '+ok.map((v,i)=>v?null:i).filter(Boolean))"</automated>
  </verify>

  <acceptance_criteria>
    - window.prompt( no longer appears anywhere in app/views/project-plan.js
    - The string iterSaveModal appears in the file (modal id)
    - The string iterSaveLabelInput appears in the file (input id)
    - The input value is pre-filled with the template expression: Iteration ${_iterSeq + 1}
    - The Save button has class iter-save-confirm-btn
    - saveIteration() returns early (the return statement exists inside the if (label === null) block) after appending the modal, so the try { addDoc } block is not executed in the modal-creation path
    - saveIteration(lbl) is called from the modal Save button onclick after reading the input value — confirming the modal re-calls saveIteration with a real label
    - label.substring(0, 60) still exists in the function body for the non-null path (D-12 truncation preserved)
  </acceptance_criteria>

  <done>saveIteration() shows a styled inline modal (matching openIterConfirm pattern) when called with no label. Input pre-filled with "Iteration N". Enter key triggers save. window.prompt() is gone. Both toolbar and rail entry points continue to call saveIteration(null) unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user text input → Firestore label field | Label entered in the save modal becomes the iteration label stored in Firestore and rendered in the rail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97.2-03 | Tampering | iterSaveModal label input → Firestore | mitigate | label.trim() and label.substring(0,60) sanitize length. escapeHTML() is applied on read (in renderIterRail). No innerHTML injection risk — label is stored as a plain string, rendered with escapeHTML() |
| T-97.2-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After the task completes:
1. Click "Save Iteration" from the toolbar — a modal appears with a pre-filled input containing "Iteration N".
2. Click "+ Save current state" from the rail — the same modal appears.
3. Change the name and click Save — the iteration is saved to Firestore and appears in the rail.
4. Open the modal and click Cancel — no iteration is saved, modal closes.
5. Open the modal, clear the input, press Enter or click Save — nothing happens (empty label rejected).
6. Enter a name longer than 60 characters — the saved label in the rail is silently truncated to 60 chars.
</verification>

<success_criteria>
- window.prompt() replaced with styled inline modal (D-10)
- Both toolbar and rail save buttons trigger the same modal path (D-11)
- 60-char truncation preserved at save time (D-12)
</success_criteria>

<output>
Create `.planning/phases/97.2-project-plan-iterations-save-ux-overhaul/97.2-03-SUMMARY.md` when done.
</output>
