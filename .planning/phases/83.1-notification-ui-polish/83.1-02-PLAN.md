---
phase: 83.1-notification-ui-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 83.1-01
files_modified:
  - styles/components.css
  - app/notifications.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Notification dropdown slides in from above (translateY -12px → 0) with opacity fade over 200ms"
    - "Dropdown closes with the same 200ms motion, visibility deferred until animation completes"
    - "Bell button toggles the .open class on #notifDropdownMenu; no inline style manipulation"
    - "Clicking outside the dropdown closes it"
  artifacts:
    - path: "styles/components.css"
      provides: "Animation CSS on .notif-dropdown-menu (closed) and .notif-dropdown-menu.open (open) states"
      contains: "translateY(-12px)"
    - path: "app/notifications.js"
      provides: "toggleNotificationsDropdown that adds/removes .open class (no display:none manipulation)"
      contains: "classList.toggle"
  key_links:
    - from: "toggleNotificationsDropdown()"
      to: "#notifDropdownMenu"
      via: "classList.add/remove('open')"
      pattern: "classList.*open"
---

<objective>
Replace the current display:none/flex open mechanism with a CSS translateY slide animation (D-02). The dropdown gets a smooth 200ms ease-out entry and a deferred-visibility exit. Bell click toggles .open class only — no inline style manipulation. Outside-click close preserved.

Purpose: The abrupt display:none toggle feels jarring. A 200ms slide + fade matches modern notification UIs and was validated in spike 004a.

Output: Updated styles/components.css with transition/transform rules on .notif-dropdown-menu; updated app/notifications.js toggleNotificationsDropdown to use classList toggle only (no display manipulation).
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\PROJECT.md
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\.planning\STATE.md
@C:\Users\Admin\Roaming\pr-po\.planning\phases\83.1-notification-ui-polish\83.1-CONTEXT.md
</context>

<interfaces>
From styles/components.css (lines 1853–1872) — current .notif-dropdown-menu rules:
  .notif-dropdown-menu {
      display: none;          ← REMOVE this; replace with animation-based hiding
      position: absolute;
      top: calc(100% + 6px);
      right: 0;
      width: 360px;
      ...structural rules unchanged...
  }
  .notif-dropdown-menu.open {
      display: flex;          ← REMOVE this; replace with opacity/visibility/transform
  }

From app/notifications.js toggleNotificationsDropdown (lines 335–352):
  Current open branch: menu.classList.add('open')  ← keep this unchanged
  Current close branch: menu.classList.remove('open')  ← keep this unchanged
  The function also sets aria-expanded and calls loadRecentForDropdown() — both preserved.
  destroyNotifications() also calls menu.classList.remove('open') — preserved.

The JS toggle logic is already correct (class-only). Only the CSS needs to change from display:none to animation-based hiding.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace display:none toggle with translateY animation in styles/components.css (D-02)</name>
  <files>styles/components.css</files>
  <read_first>styles/components.css — read lines 1853–1872 (the two .notif-dropdown-menu rule blocks) to confirm exact current values before editing</read_first>
  <action>
    Replace the two .notif-dropdown-menu rule blocks. Keep all structural/positioning properties unchanged; only replace the show/hide mechanism.

    BEFORE (remove these two properties and the entire .open rule):
      .notif-dropdown-menu { display: none; ... }
      .notif-dropdown-menu.open { display: flex; }

    AFTER — replace with animation-based visibility:

    Closed state (base .notif-dropdown-menu rule) — add these properties, keep all existing structural properties:
      opacity: 0;
      transform: translateY(-12px);
      visibility: hidden;
      pointer-events: none;
      transition: transform 200ms ease-out, opacity 200ms ease-out, visibility 0ms 200ms;

    Remove the `display: none` line from the base rule. The dropdown must remain in the layout flow (not display:none) so CSS transitions can animate.
    Keep existing structural properties: position, top, right, width, max-width, background, border, border-radius, box-shadow, z-index, max-height, overflow, flex-direction.

    Open state (.notif-dropdown-menu.open rule) — replace `display: flex` with:
      opacity: 1;
      transform: translateY(0);
      visibility: visible;
      pointer-events: auto;
      transition: transform 200ms ease-out, opacity 200ms ease-out, visibility 0ms 0ms;

    The key difference between open and closed transitions: `visibility 0ms 200ms` on close (deferred — element hides AFTER the 200ms fade completes) vs `visibility 0ms 0ms` on open (immediate — element appears before the animation plays).

    Do NOT touch the mobile media query for .notif-dropdown-menu (around line 2024) which sets width and right — those structural overrides are unchanged.
  </action>
  <verify>
    <automated>grep -c "translateY(-12px)" styles/components.css</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "translateY(-12px)" styles/components.css returns 1
    - grep -c "visibility: hidden" styles/components.css returns at least 1 (the base .notif-dropdown-menu rule)
    - grep -c "visibility 0ms 200ms" styles/components.css returns 1 (close transition delay)
    - grep -c "visibility 0ms 0ms" styles/components.css returns 1 (open transition — no delay)
    - grep -c "display: none" styles/components.css for the .notif-dropdown-menu rule returns 0 (display:none removed from that block)
    - grep -c "display: flex" styles/components.css for the .notif-dropdown-menu.open rule returns 0 (flex moved to .notif-dropdown-menu base)
    - The .notif-dropdown-menu base rule still contains: position: absolute; top: calc(100% + 6px); width: 360px; z-index: 200
  </acceptance_criteria>
  <done>Dropdown CSS uses opacity+transform+visibility for open/close; display:none/flex mechanism removed; structural rules intact.</done>
</task>

<task type="auto">
  <name>Task 2: Verify toggleNotificationsDropdown uses classList only; add outside-click guard if missing</name>
  <files>app/notifications.js</files>
  <read_first>app/notifications.js — read lines 323–355 (the toggleNotificationsDropdown function and surrounding context) to confirm the current open/close mechanism before deciding what to change</read_first>
  <action>
    The current toggleNotificationsDropdown function already uses classList.add('open') and classList.remove('open') — this is correct for the CSS animation approach and requires NO change to the toggle logic.

    Check: does a document click listener already exist that closes the dropdown on outside-click? Search the file for `document.addEventListener` near the bell/dropdown code.

    If an outside-click listener already exists: no change needed.

    If NO outside-click listener exists: add one inside initNotifications(), after the bellListener assignment. The listener must:
      - Check if the click target is outside both #notifBellWrap AND #notifDropdownMenu
      - If outside: call menu.classList.remove('open') and update aria-expanded to 'false'
      - Be stored in a module-scope variable (e.g., let _outsideClickHandler = null) so destroyNotifications() can remove it via document.removeEventListener('click', _outsideClickHandler)
      - In destroyNotifications(): remove the listener and null the variable alongside the bellListener teardown

    Do NOT change the toggleNotificationsDropdown function logic (stopPropagation, classList.add/remove, aria-expanded, loadRecentForDropdown call).
    Do NOT add any display style manipulation.
  </action>
  <verify>
    <automated>grep -c "classList.*open" app/notifications.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "display.*none\|display.*flex" app/notifications.js (for notif dropdown manipulation) returns 0 — no inline display toggling on the dropdown
    - grep -c "classList.*open" app/notifications.js returns >= 2 (add and remove calls)
    - If outside-click handler was added: grep -c "_outsideClickHandler" app/notifications.js returns >= 3 (declaration, assignment, removeEventListener)
    - Open dropdown in browser, click outside: dropdown closes with slide-out animation
    - Click bell: dropdown slides in from above with 200ms ease-out
    - Click bell again: dropdown slides out (opacity fades, visibility deferred)
  </acceptance_criteria>
  <done>Bell toggle works via .open class; outside-click closes dropdown; animation plays on open and close; no inline display manipulation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document click handler | Listens on document; only reads event.target, does not process user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83.1-02-01 | Denial of Service | Outside-click listener never removed | mitigate | Store handler reference in module-scope var; remove in destroyNotifications() |
| T-83.1-02-SC | Tampering | No npm installs in this plan | accept | Pure JS/CSS edit, no package manager involved |
</threat_model>

<verification>
After both tasks complete:
1. Open browser at http://localhost:8000, log in
2. Click bell: dropdown slides in from above over 200ms
3. Click bell again: dropdown fades out over 200ms, then hidden (not an abrupt disappear)
4. Click bell to open, then click anywhere outside: dropdown closes with animation
5. grep -c "translateY(-12px)" styles/components.css → 1
6. grep -c "visibility 0ms 200ms" styles/components.css → 1
</verification>

<success_criteria>
- .notif-dropdown-menu uses opacity+transform+visibility CSS transitions
- Open: translateY(0) + opacity 1 + visibility visible over 200ms ease-out
- Close: translateY(-12px) + opacity 0 + visibility hidden, visibility deferred 200ms
- Bell click toggles .open class only; no inline style manipulation
- Outside-click closes dropdown
</success_criteria>

<output>
Create `.planning/phases/83.1-notification-ui-polish/83.1-02-SUMMARY.md` when done
</output>
