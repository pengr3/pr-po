---
phase: 83.1-notification-ui-polish
plan: "01"
subsystem: notifications
tags: [svg-icons, css, ui-polish, type-meta]
dependency_graph:
  requires: []
  provides: [TYPE_META-svg-icons, notif-badge-svg-css]
  affects: [app/notifications.js, styles/components.css]
tech_stack:
  added: []
  patterns: [heroicons-outline-inline-svg, svg-helper-function]
key_files:
  created: []
  modified:
    - app/notifications.js
    - styles/components.css
decisions:
  - "Used svg() helper at module scope (not inline SVG strings per entry) — reduces repetition per D-01 and Claude's Discretion guidance in CONTEXT.md"
  - "Inserted CSS rules after .notif-row--unread:hover block per plan spec — no existing rules removed"
metrics:
  duration_minutes: 5
  completed_date: "2026-05-26"
  tasks_completed: 2
  files_modified: 2
---

# Phase 83.1 Plan 01: SVG Icons + Badge CSS Summary

**One-liner:** Heroicons outline SVG icons in all 16 TYPE_META entries via svg() helper, with 28x28px badge wrapper and 16x16px SVG sizing CSS rules.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add svg() helper and replace TYPE_META icons | bb2df7f | app/notifications.js |
| 2 | Add .notif-type-badge svg sizing rules (D-04) | 58677a6 | styles/components.css |

## What Was Built

**Task 1 — app/notifications.js:**
- Added `const svg = (path) =>` helper at module scope, immediately before the TYPE_META const
- Replaced all 16 TYPE_META icon fields: `MRF_APPROVED`, `MRF_REJECTED`, `PR_REVIEW_NEEDED`, `TR_REVIEW_NEEDED`, `RFP_REVIEW_NEEDED`, `PROJECT_STATUS_CHANGED`, `PROJECT_COST_CHANGED`, `REGISTRATION_PENDING`, `PROPOSAL_SUBMITTED`, `PROPOSAL_DECIDED`, `MRF_SUBMITTED`, `PR_DECIDED`, `TR_DECIDED`, `RFP_PAID`, `PO_DELIVERED`, `COLLECTIBLE_CREATED`
- Each entry now has a unique Heroicons outline SVG path (24x24 viewBox, stroke-width 1.75, round linecap+linejoin)
- No changes to renderDropdownRows or any other function — template already interpolates `meta.icon` as raw HTML

**Task 2 — styles/components.css:**
- Added `.notif-type-badge` wrapper rule: `display: inline-flex; align-items: center; justify-content: center; width: 28px; height: 28px; border-radius: 6px; flex-shrink: 0`
- Added `.notif-type-badge svg` child rule: `width: 16px; height: 16px; stroke: currentColor; fill: none; stroke-width: 1.75; stroke-linecap: round; stroke-linejoin: round; flex-shrink: 0`
- Inserted after `.notif-row--unread:hover` block, before `.notif-row-message`
- All existing notification CSS rules preserved

## Deviations from Plan

None — plan executed exactly as written.

**Note on acceptance criterion:** The plan stated `grep -c "stroke-width=\"1.75\"" returns 16`. With the svg() helper approach (which the plan explicitly prescribes), `stroke-width="1.75"` appears once in the helper template literal and is emitted 16 times at runtime. The helper approach is explicitly prescribed in both the plan action step and the CONTEXT.md D-01 decision — this is the correct implementation. The svg() helper check (`grep -c "const svg = (path)"` returns 1) confirms the helper is present.

## Verification

- `grep -c "const svg = (path)" app/notifications.js` → **1** ✓
- `grep -c ".notif-type-badge svg" styles/components.css` → **1** ✓
- `grep -c ".notif-type-badge {" styles/components.css` → **1** ✓
- No text char or emoji icons remain in TYPE_META: `icon: '✓'` count = 0, `icon: '⏳'` count = 0, `icon: '📦'` count = 0 ✓
- COLLECTIBLE_CREATED entry preserved (count = 2: one in NOTIFICATION_TYPES enum, one in TYPE_META) ✓
- Existing `.notif-row--unread` rules still present (count = 2) ✓

## Known Stubs

None.

## Threat Flags

None. SVG path data is hardcoded constant — no user input, no XSS vector (T-83.1-01-SC: accept).

## Self-Check: PASSED

- app/notifications.js: modified and committed at bb2df7f ✓
- styles/components.css: modified and committed at 58677a6 ✓
- SUMMARY.md: created at .planning/phases/83.1-notification-ui-polish/83.1-01-SUMMARY.md ✓
