---
phase: 83.1-notification-ui-polish
plan: "03"
subsystem: ui
tags: [css, notification-row, left-border-indicator, unread-indicator, history-page]
dependency_graph:
  requires:
    - phase: 83.1-01
      provides: TYPE_META-svg-icons, notif-badge-svg-css
    - phase: 83.1-02
      provides: notif-dropdown-slide-animation
  provides:
    - notif-row-left-border-unread-indicator
    - D-03-css-alignment-anchor
  affects: [styles/components.css, app/views/notifications.js]
tech-stack:
  added: []
  patterns: [css-left-border-unread-indicator, border-transparent-alignment-anchor]
key-files:
  created: []
  modified:
    - styles/components.css
    - app/views/notifications.js
key-decisions:
  - "D-03 left-border pattern replaces full-block #eff6ff: unread rows get border-left: 3px solid #1a73e8 + background: #f8fbff + padding-left: calc(1rem - 3px); all rows get border-left: 3px solid transparent as alignment anchor"
  - "History page row div inline style trimmed to display/align-items/gap/cursor only; padding and border-bottom ownership transferred to .notif-row CSS rule so padding-left compensation can apply on unread rows"
  - "Pre-existing border-left: 3px solid transparent on .mobile-nav-item (line 1348) is unrelated — plan acceptance criterion 'grep -c returns 1' was for the .notif-row rule specifically; both occurrences are correct"

patterns-established:
  - "CSS alignment anchor: base rule carries border-left: transparent so adding a colored border on a modifier class never shifts content horizontally"
  - "padding-left: calc(1rem - 3px) compensation: when a left border is applied, reduce left padding by border width to keep content in the same horizontal position"

requirements-completed: []

duration: 5min
completed: "2026-05-26"
---

# Phase 83.1 Plan 03: Left-Border Unread Indicator Summary

**D-03 left-border unread indicator: 3px #1a73e8 border + #f8fbff tint replaces full-block #eff6ff wash; transparent alignment anchor on all rows; history page inline overrides removed so CSS compensation takes effect.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-26T05:55:00Z
- **Completed:** 2026-05-26T06:01:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Replaced old `.notif-row--unread { background: #eff6ff }` full-block indicator with D-03 left-border pattern (3px solid #1a73e8 + barely-perceptible #f8fbff tint + padding-left calc compensation)
- Added `border-left: 3px solid transparent` to base `.notif-row` rule so content never shifts horizontally between read and unread rows
- History page (`app/views/notifications.js` renderRows) row div inline style trimmed — padding and border-bottom removed so CSS rule owns them, enabling `padding-left: calc(1rem - 3px)` to work on unread rows

## Task Commits

Each task was committed atomically:

1. **Task 1: Update .notif-row unread indicator CSS to D-03 left-border pattern** - `c4deaaa` (feat)
2. **Task 2: Fix history page row structure so .notif-row--unread CSS applies** - `de8e8ca` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `styles/components.css` — `.notif-row` now has `border-left: 3px solid transparent`; `.notif-row--unread` now has `background: #f8fbff`, `border-left: 3px solid #1a73e8`, `padding-left: calc(1rem - 3px)`; `.notif-row--unread:hover` changed from `#dbeafe` to `#eff6ff`
- `app/views/notifications.js` — Row div `style` attribute in `renderRows()` trimmed from `display:flex;align-items:flex-start;gap:0.75rem;padding:0.875rem 1rem;border-bottom:1px solid #e5e7eb;cursor:pointer` to `display:flex;align-items:flex-start;gap:0.75rem;cursor:pointer`

## Decisions Made
- Trimmed inline style to keep display/align/gap/cursor only. The slight border-bottom color change (from inline `#e5e7eb` to CSS `#f1f5f9`) is an acceptable minor visual delta per plan spec.
- Pre-existing `border-left: 3px solid transparent` on `.mobile-nav-item` is unrelated to the `.notif-row` anchor — both occurrences are correct.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered
- Edit tool could not match the multi-line template literal string in notifications.js (backtick string interpolation caused match failure). Used a Python one-liner to replace the inline style attribute string directly — same targeted edit, identical outcome.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phase 83.1 complete: all 3 waves shipped (Plan 01: SVG icons; Plan 02: dropdown slide animation; Plan 03: left-border unread indicator)
- Both dropdown and history page now show consistent left-border unread indicator
- No blockers for next phase

## Known Stubs

None.

## Threat Flags

None. CSS class names applied based on `notification.read` field — no user-controlled HTML injection vector (T-83.1-03-SC: accept per threat model).

---
*Phase: 83.1-notification-ui-polish*
*Completed: 2026-05-26*
