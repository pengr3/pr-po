---
phase: 86.9-gantt-curtain-divider-pdf-export
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/project-plan.js
  - styles/views.css
autonomous: true
requirements:
  - GANTT-UX-PDF-EXPORT

must_haves:
  truths:
    - "An Export button appears in the Gantt toolbar row, visually grouped with the Day / Week / Month zoom buttons"
    - "Clicking Export generates a print view and opens the browser print dialog, which allows PDF download via 'Save as PDF'"
    - "The print view contains a header with project name and export date/time"
    - "The print view contains a visual snapshot of the current Gantt chart viewport (the #ganttPane element)"
    - "The print view contains a task completion summary table listing task name, progress %, start date, end date, and status"
    - "Status is computed as: 'Complete' if progress >= 100, 'In Progress' if progress > 0, 'Not Started' if progress == 0"
    - "The Export button is styled consistently with the existing toolbar (matching .zoom-pill styling)"
    - "destroy() cleans up the export handler to prevent stale window references"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "exportGanttPDF() function; Export button in render() HTML; window.exportGanttPDF registration; destroy() cleanup"
      contains: "exportGanttPDF"
    - path: "styles/views.css"
      provides: "Export button style rules (.plan-export-btn)"
      contains: "plan-export-btn"
  key_links:
    - from: "Export button onclick"
      to: "window.exportGanttPDF()"
      via: "onclick attribute on button in render() HTML"
      pattern: "exportGanttPDF"
    - from: "exportGanttPDF()"
      to: "tasks[] module-scope array"
      via: "direct read of tasks[] to build the summary table"
      pattern: "tasks\\.filter|tasks\\.map"
    - from: "exportGanttPDF()"
      to: "window.print()"
      via: "dynamically created print iframe or a print-specific CSS @media print block"
      pattern: "window\\.print"
---

<objective>
Add an Export button to the Gantt toolbar that generates a PDF-ready print view (using window.print() + @media print CSS) containing a project name/date header, a visual Gantt snapshot, and a task completion summary table.

Purpose: Users need a shareable snapshot of the project plan. The window.print() approach requires zero CDN dependencies (no html2canvas, no jsPDF), works reliably in the zero-build SPA, and produces a clean PDF via the browser's native "Save as PDF" print destination.

Output: `exportGanttPDF()` function in project-plan.js; Export button in toolbar HTML; @media print CSS rules in views.css; window registration and destroy() cleanup.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\PROJECT.md
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\.planning\STATE.md

<!-- This plan is INDEPENDENT of Plan 01 (no file overlap conflict for wave assignment, but both touch the same two files). -->
<!-- Execute Plan 01 first if running sequentially; if parallel, ensure no git merge conflict on the two files. -->
<!-- Recommended: run Plan 02 after Plan 01 is committed. -->

<interfaces>
<!-- Toolbar HTML (render() lines 114-134): -->
<!-- The zoom pill group ends with the Month button, then comes the critical-path toggle label, then the search bar div. -->
<!-- Export button goes AFTER .zoom-pill-group, BEFORE .plan-toolbar-toggle label. -->
<!-- Pattern: <button type="button" class="plan-export-btn" onclick="window.exportGanttPDF()">Export</button> -->

<!-- Module-scope tasks[] array (line 14): -->
<!-- tasks = raw task docs from onSnapshot; each task has: task_id, task_name, progress (number 0-100, default 0), start_date (YYYY-MM-DD string), end_date (YYYY-MM-DD string), parent_task_id (string|null) -->
<!-- currentProject has: project_name (string) -->

<!-- window.print() approach: -->
<!-- exportGanttPDF() injects a hidden <div id="gantt-print-frame"> into document.body, -->
<!-- populates it with the header + snapshot + table HTML, -->
<!-- calls window.print(), -->
<!-- then removes the print frame after a short timeout (500ms). -->
<!-- @media print CSS hides everything EXCEPT #gantt-print-frame. -->

<!-- destroy() pattern (lines 362-366): -->
<!-- delete window.setGanttZoom; delete window.deleteTaskNow; etc. -->
<!-- Add: delete window.exportGanttPDF; in the same block. -->

<!-- init() window registration (lines 212-222): -->
<!-- window.setGanttZoom = setGanttZoom; etc. -->
<!-- Add: window.exportGanttPDF = exportGanttPDF; -->

<!-- Status derivation for summary table: -->
<!-- const progress = t.progress ?? 0; -->
<!-- status = progress >= 100 ? 'Complete' : progress > 0 ? 'In Progress' : 'Not Started' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exportGanttPDF() function, Export button in render(), and window registration</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    - app/views/project-plan.js lines 105-146 (render() HTML — find the toolbar, locate where to insert the Export button)
    - app/views/project-plan.js lines 210-222 (init() window.* registration block — where to add window.exportGanttPDF)
    - app/views/project-plan.js lines 362-370 (destroy() delete window.* block — where to add delete window.exportGanttPDF)
    - app/views/project-plan.js lines 3065-3069 (near end of file — a good place to define exportGanttPDF before the final closing brace)
  </read_first>
  <action>
    1. In render() HTML, insert the Export button AFTER the closing `</div>` of `.zoom-pill-group` and BEFORE the `<label class="plan-toolbar-toggle"` line. The button HTML is:
       `<button type="button" class="plan-export-btn" onclick="window.exportGanttPDF()" title="Export Gantt as PDF">Export</button>`

    2. Define `exportGanttPDF()` as a module-scope function (not exported). Place it near the bottom of the file, in the toolbar-helpers section (near `setGanttZoom` or at the end before any closing comment). The function must:

       a. Build a `dateStr` using `new Date().toLocaleString()` for the export timestamp.

       b. Build `projectName` from `currentProject?.project_name || projectCode || 'Project'`.

       c. Build the task summary table rows from `tasks` filtered to leaf tasks only (tasks where no other task has `parent_task_id === t.task_id` — or simpler: include ALL tasks, parents and leaves, since the user wants a full summary). Use all tasks sorted by `row_order` ascending (or by the existing `tasks` array order which onSnapshot provides). For each task `t`:
          - `name = escapeHTML(t.task_name || '(unnamed)')`
          - `progress = t.progress ?? 0`
          - `start = t.start_date || '—'`
          - `end = t.end_date || '—'`
          - `status = progress >= 100 ? 'Complete' : progress > 0 ? 'In Progress' : 'Not Started'`
          - `indent = t.parent_task_id ? 'style="padding-left:20px;"' : ''`

       d. Capture the `innerHTML` of the Gantt viewport element: `const ganttEl = document.getElementById('ganttPane'); const ganttHtml = ganttEl ? ganttEl.innerHTML : '<p>(No Gantt chart visible)</p>';`. Wrap it in a `<div class="gantt-print-snapshot">` with `pointer-events: none`.

       e. Build the print frame HTML string — a self-contained `<div id="gantt-print-frame">` containing:
          - A header div: `<div class="pdf-header"><h2>{projectName} — Project Plan</h2><p>Exported: {dateStr}</p></div>`
          - A Gantt snapshot section: `<div class="pdf-gantt-section"><h3>Gantt Chart</h3><div class="gantt-print-snapshot">{ganttHtml}</div></div>`
          - A task summary section: `<div class="pdf-table-section"><h3>Task Completion Summary</h3><table class="pdf-summary-table">...</table></div>`
          - The summary table has columns: Task Name, Progress, Start, End, Status. Use a `<thead>` and `<tbody>`.

       f. Create the print frame: `const frame = document.createElement('div'); frame.id = 'gantt-print-frame'; frame.innerHTML = printHtml; document.body.appendChild(frame);`

       g. Call `window.print()`.

       h. Remove the frame after 800ms: `setTimeout(() => { frame.remove(); }, 800);`

       i. Show a toast: `showToast('Opening print dialog — choose "Save as PDF" to download.', 'info');`

    3. In `init()`, add `window.exportGanttPDF = exportGanttPDF;` in the window registration block (after the existing `window.toggleCriticalPath = toggleCriticalPath;` line).

    4. In `destroy()`, add `delete window.exportGanttPDF;` in the delete window.* block (after `delete window.toggleCriticalPath;`).
  </action>
  <verify>
    <automated>node --check app/views/project-plan.js && grep -c "exportGanttPDF" app/views/project-plan.js</automated>
    Expected: no syntax error; `exportGanttPDF` count >= 4 (function definition + button onclick + window registration + destroy delete).
    Also: `grep -c "window.print" app/views/project-plan.js` >= 1.
    Also: `grep -c "gantt-print-frame" app/views/project-plan.js` >= 2.
    Also: `grep -c "plan-export-btn" app/views/project-plan.js` >= 1.
    Also: `grep -c "Not Started\|In Progress\|Complete" app/views/project-plan.js` >= 1 (status derivation present).
  </verify>
  <acceptance_criteria>
    - `node --check app/views/project-plan.js` exits 0
    - `grep -c "exportGanttPDF" app/views/project-plan.js` >= 4
    - `grep -c "window\.print" app/views/project-plan.js` >= 1
    - `grep -c "gantt-print-frame" app/views/project-plan.js` >= 2
    - `grep -c "plan-export-btn" app/views/project-plan.js` >= 1
    - `grep -c "Not Started" app/views/project-plan.js` >= 1
    - Export button appears in render() HTML between .zoom-pill-group and .plan-toolbar-toggle
    - window.exportGanttPDF registered in init() and deleted in destroy()
  </acceptance_criteria>
  <done>exportGanttPDF() defined; Export button wired in render(); window.exportGanttPDF registered in init() and cleaned in destroy(). No syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add Export button styles and @media print CSS</name>
  <files>styles/views.css</files>
  <read_first>
    - styles/views.css lines 2406-2420 (current .zoom-pill and .zoom-pill.active rules — match button style to these)
    - styles/views.css — end of file (append new rules after the last block; do not insert in middle of existing blocks)
  </read_first>
  <action>
    Append two CSS blocks to the end of styles/views.css (after the last existing rule):

    Block 1 — Export button style (screen display, matches zoom-pill aesthetics):
    ```
    .plan-export-btn {
        display: inline-flex;
        align-items: center;
        padding: 5px 14px;
        border-radius: 16px;
        border: 1.5px solid var(--primary, #1a73e8);
        background: transparent;
        color: var(--primary, #1a73e8);
        font-size: 13px;
        font-weight: 500;
        cursor: pointer;
        white-space: nowrap;
        transition: background 0.15s, color 0.15s;
        margin-left: 8px;
    }
    .plan-export-btn:hover {
        background: var(--primary, #1a73e8);
        color: #ffffff;
    }
    ```

    Block 2 — @media print rules:
    ```
    @media print {
        /* Hide everything except the print frame */
        body > *:not(#gantt-print-frame) { display: none !important; }
        #gantt-print-frame { display: block !important; }

        /* Print frame layout */
        #gantt-print-frame {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            color: #1e293b;
            padding: 24px;
        }
        .pdf-header h2 {
            font-size: 18px;
            font-weight: 700;
            margin: 0 0 4px 0;
        }
        .pdf-header p {
            font-size: 12px;
            color: #64748b;
            margin: 0 0 20px 0;
        }
        .pdf-gantt-section h3,
        .pdf-table-section h3 {
            font-size: 14px;
            font-weight: 600;
            margin: 16px 0 8px 0;
            border-bottom: 1px solid #e2e8f0;
            padding-bottom: 4px;
        }
        /* Gantt snapshot: scale to fit page width */
        .gantt-print-snapshot {
            width: 100%;
            overflow: hidden;
            transform-origin: top left;
            page-break-after: always;
        }
        /* SVG inside snapshot: fit width */
        .gantt-print-snapshot svg { max-width: 100%; height: auto; }

        /* Summary table */
        .pdf-summary-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 12px;
            margin-top: 8px;
        }
        .pdf-summary-table th {
            background: #f1f5f9;
            border: 1px solid #e2e8f0;
            padding: 6px 8px;
            text-align: left;
            font-weight: 600;
        }
        .pdf-summary-table td {
            border: 1px solid #e2e8f0;
            padding: 5px 8px;
        }
        .pdf-summary-table tr:nth-child(even) td { background: #f8fafc; }
    }
    ```

    Do NOT modify any existing rule. Only append to the end of the file.
  </action>
  <verify>
    <automated>grep -c "plan-export-btn" styles/views.css && grep -c "@media print" styles/views.css</automated>
    Expected: `plan-export-btn` count >= 2 (rule definition + hover variant); `@media print` count >= 1.
    Also: `grep -c "pdf-summary-table" styles/views.css` >= 3 (th, td, and the table itself).
    Also: `grep -c "gantt-print-frame" styles/views.css` >= 2.
  </verify>
  <acceptance_criteria>
    - `grep -c "plan-export-btn" styles/views.css` >= 2
    - `grep -c "@media print" styles/views.css` >= 1
    - `grep -c "pdf-summary-table" styles/views.css` >= 3
    - `grep -c "gantt-print-frame" styles/views.css` >= 2
    - The @media print block hides `body > *:not(#gantt-print-frame)` and shows `#gantt-print-frame`
    - .plan-export-btn has border-radius, matches the zoom-pill aesthetic (blue border/text, fills on hover)
  </acceptance_criteria>
  <done>Export button CSS and @media print rules appended to views.css. Print frame is shown exclusively when printing; task summary table styled for readability.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Export button added to Gantt toolbar. Clicking it injects a print frame with project name, export timestamp, Gantt snapshot, and task summary table, then calls window.print().
  </what-built>
  <how-to-verify>
    1. Start the dev server: `python -m http.server 8000` (or `npx http-server`)
    2. Navigate to a project plan: `http://localhost:8000/#/projects/{project-code}/plan`
    3. Verify the Export button appears to the right of the Day/Week/Month zoom pills in the toolbar
    4. Click Export — verify the toast "Opening print dialog" appears and the browser print dialog opens
    5. In the print dialog, verify the print preview shows:
       a. Header: project name and export date/time
       b. A Gantt chart section (may be a raw SVG render — acceptable for prototype)
       c. A Task Completion Summary table with columns: Task Name, Progress, Start, End, Status
       d. Status values are "Not Started", "In Progress", or "Complete" (not raw numbers)
    6. Cancel the print dialog (no need to actually save a PDF)
    7. Verify the toolbar is back to normal (no lingering print-frame DOM node)
  </how-to-verify>
  <resume-signal>Type "approved" to close this plan, or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tasks[] data → innerHTML in print frame | Task names and project names are injected into innerHTML; must be escaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.9.2-01 | Injection | task_name in print frame innerHTML | mitigate | escapeHTML() wrapper (already imported from utils.js) applied to task_name before interpolation |
| T-86.9.2-02 | Information Disclosure | Gantt SVG captured via innerHTML and sent to browser print | accept | print is a local browser action; no data leaves the device; same data the user can already see on screen |
| T-86.9.2-03 | Denial of Service | Large task list (>500 tasks) building the summary table | accept | D-22 cap is 500 tasks; table build is O(N) string concatenation; no network I/O |
</threat_model>

<verification>
After both tasks complete (and before the UAT checkpoint):

Automated checks:
- `node --check app/views/project-plan.js` exits 0
- `grep -c "exportGanttPDF" app/views/project-plan.js` >= 4
- `grep -c "plan-export-btn" styles/views.css` >= 2
- `grep -c "@media print" styles/views.css` >= 1
- `grep -c "window\.print" app/views/project-plan.js` >= 1
- `grep -c "Not Started" app/views/project-plan.js` >= 1

Manual UAT (Task 3 checkpoint):
- Export button visible in toolbar next to Day/Week/Month
- Print dialog opens with project-name header, Gantt snapshot, task summary table
- Status column shows "Not Started", "In Progress", or "Complete"
- No DOM leakage after dialog dismissed
</verification>

<success_criteria>
- Export button renders in toolbar between the zoom pills and the critical-path toggle
- Clicking Export opens browser print dialog with correct content structure
- Task summary table contains all tasks with name, progress %, start date, end date, status
- Status values are "Not Started" / "In Progress" / "Complete" (not raw numbers)
- Project name and export date/time appear in the PDF header
- No JavaScript errors in console when clicking Export
- destroy() cleans up `window.exportGanttPDF` so it is not callable after navigating away
</success_criteria>

<output>
After completion, create `.planning/phases/86.9-gantt-curtain-divider-pdf-export/86.9-02-SUMMARY.md`
</output>
