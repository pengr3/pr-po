---
phase: 86.9-gantt-curtain-divider-pdf-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/project-plan.js
  - styles/views.css
autonomous: true
requirements:
  - GANTT-UX-CURTAIN-DIVIDER

must_haves:
  truths:
    - "Dragging the panel divider left does not collapse or hide task-grid columns — the .task-grid-rail DOM and its columns remain at full width at all times"
    - "The right Gantt pane slides left as a positioned overlay, covering the left rail like a curtain, rather than the grid shrinking"
    - "At any divider position, task-grid rows in the uncovered (left-of-divider) portion remain fully interactive — clicks, edits, and drags work normally"
    - "The divider can be dragged freely between 15% and 75% of the container width"
    - "On mobile breakpoints, the existing behavior (single-pane: gantt hidden) is preserved"
    - "destroy() removes all resize-related document listeners with no regressions vs. current cleanup"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "Rewritten initPanelResize() using absolute positioning; _paneDividerPct module-scope variable"
      contains: "_paneDividerPct"
    - path: "styles/views.css"
      provides: "Curtain layout CSS: .plan-split-pane (position:relative), .task-grid-rail (absolute fill), .gantt-pane (absolute overlay), .plan-divider (absolute)"
      contains: "position: absolute"
  key_links:
    - from: "initPanelResize() mousemove handler"
      to: ".gantt-pane style.left and .plan-divider style.left"
      via: "direct style mutation"
      pattern: "ganttPane\\.style\\.left"
    - from: ".plan-split-pane"
      to: ".task-grid-rail + .gantt-pane"
      via: "position:relative containing block for absolute children"
      pattern: "position:\\s*relative"
---

<objective>
Replace the CSS-grid column-shrink resize behavior with a curtain/overlay approach so dragging the divider moves only the Gantt panel, never touching the task-grid columns.

Purpose: The current approach shrinks `.task-grid-rail` as the divider moves left, eventually hiding task columns. The curtain approach keeps the task grid at full width at all times; the Gantt panel sits as an absolute-positioned overlay that slides left to reveal more of the grid or right to cover it. Column widths, sticky headers, and scroll sync are unaffected.

Output: Rewritten `initPanelResize()` in project-plan.js; rewritten split-pane CSS block in views.css.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\PROJECT.md
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\.planning\STATE.md

<!-- Key prior-phase decisions relevant to this plan: -->
<!-- Phase 86.2-03: _resizeMouseMoveHandler/_resizeMouseUpHandler attached to document; removed in destroy() -->
<!-- Phase 86.4 D-SCROLL: bindScrollSync() synchronizes scrollTop between .task-grid-rail and .gantt-container -->
<!-- The scroll sync is on rail scrollTop → gantt-container scrollTop; absolute positioning of the pane does NOT break this because the DOM elements are the same -->
<!-- Phase 86.3: _ganttScrollClampPane stores a reference to .gantt-pane for scroll-clamp cleanup -->
<!-- Phase 86.3 also stores _ganttScrollClampPane = document.querySelector('.gantt-pane') — this pane reference stays valid after the CSS change because it is the same DOM element -->

<interfaces>
<!-- Current initPanelResize() (lines 463-489 of project-plan.js): -->
<!-- modifies splitPane.style.gridTemplateColumns = pct.toFixed(1) + '% 4px 1fr' -->
<!-- New approach: modifies ganttPane.style.left and planDivider.style.left instead -->

<!-- Module-scope state vars already declared (lines 39-41): -->
<!--   let _resizeMouseMoveHandler = null; -->
<!--   let _resizeMouseUpHandler = null; -->
<!--   let _resizeDragging = false; -->
<!-- NEW: add _paneDividerPct = 35 at module scope (default percentage) -->

<!-- destroy() cleanup for resize (lines 374-378) is already correct — no changes needed there -->
<!-- The destroy block removes mousemove/mouseup from document and nulls the handlers -->

<!-- CSS current state (views.css lines ~2421-2454): -->
<!-- .plan-split-pane { display: grid; grid-template-columns: 35% 4px 1fr; flex: 1 1 auto; min-height: 0; } -->
<!-- .task-grid-rail { overflow-x: hidden; overflow-y: auto; background: #fff; border-right: 1px solid ...; } -->
<!-- .plan-divider { background: ...; cursor: col-resize; ... } -->

<!-- Mobile media query to preserve: -->
<!-- @media (max-width: 768px) — currently hides .gantt-pane and shows only the rail -->
<!-- After curtain approach, mobile should similarly hide .gantt-pane (already absolute, just set display:none) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _paneDividerPct module-scope variable and rewrite initPanelResize()</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    - app/views/project-plan.js lines 39-41 (resize state vars — where to insert _paneDividerPct)
    - app/views/project-plan.js lines 463-489 (current initPanelResize() — full body to replace)
    - app/views/project-plan.js lines 374-378 (destroy() resize cleanup — verify it still works after rewrite)
  </read_first>
  <action>
    1. In the "Plan 03 — resizable panel divider state" block (around line 39), add a fourth variable: `let _paneDividerPct = 35;` — this is the module-scope percentage (default 35, clamped 15-75 on drag).

    2. Replace the entire body of `initPanelResize()` (lines 463-489) with the new curtain implementation. The new implementation must:

       a. Query `document.getElementById('planDivider')`, `document.getElementById('planSplitPane')`, and `document.querySelector('.gantt-pane')`. If any is missing, return early.

       b. Apply the initial position on first call: set `ganttPane.style.left = _paneDividerPct + '%'` and `planDivider.style.left = 'calc(' + _paneDividerPct + '% - 2px)'` so the divider sits centered on the panel boundary. The 2px offset aligns the 4px divider visually — its center lands exactly at the percentage boundary.

       c. On `divider.addEventListener('mousedown', ...)`: set `_resizeDragging = true`, add class `'dragging'` to divider, call `e.preventDefault()`.

       d. `_resizeMouseMoveHandler` (assigned to the module-scope var, not a local let): when `_resizeDragging` is true, compute `pct = Math.max(15, Math.min(75, ((e.clientX - rect.left) / splitPane.getBoundingClientRect().width) * 100))`, then set `_paneDividerPct = pct`, `ganttPane.style.left = pct.toFixed(1) + '%'`, `planDivider.style.left = 'calc(' + pct.toFixed(1) + '% - 2px)'`.

       e. `_resizeMouseUpHandler` (module-scope var): clears `_resizeDragging = false`, removes `'dragging'` class from divider.

       f. Attach: `document.addEventListener('mousemove', _resizeMouseMoveHandler)` and `document.addEventListener('mouseup', _resizeMouseUpHandler)` — same as existing code; destroy() cleanup is unchanged.

    3. Do NOT touch destroy() — its cleanup already handles `_resizeMouseMoveHandler`, `_resizeMouseUpHandler`, and `_resizeDragging`. The `_paneDividerPct` reset is not needed in destroy() (it re-initializes to 35 on the next `initPanelResize()` call is fine, but add `_paneDividerPct = 35;` to the destroy() resize section for consistency — add it after `_resizeDragging = false` at line ~378).
  </action>
  <verify>
    <automated>node --check app/views/project-plan.js && grep -c "_paneDividerPct" app/views/project-plan.js</automated>
    Expected: no syntax error; `_paneDividerPct` count >= 4 (declaration + two style assignments in initPanelResize + destroy reset).
    Also verify: `grep -c "gridTemplateColumns" app/views/project-plan.js` — count must equal 0 (old approach fully removed from initPanelResize).
    Also verify: `grep -c "ganttPane\.style\.left" app/views/project-plan.js` — count >= 2.
  </verify>
  <acceptance_criteria>
    - `node --check app/views/project-plan.js` exits 0
    - `grep -c "_paneDividerPct" app/views/project-plan.js` >= 4
    - `grep -c "ganttPane\.style\.left" app/views/project-plan.js` >= 2
    - `grep -c "gridTemplateColumns" app/views/project-plan.js` == 0 (initPanelResize no longer uses grid columns)
    - `grep -c "initPanelResize" app/views/project-plan.js` >= 2 (definition + call site in init())
  </acceptance_criteria>
  <done>initPanelResize() rewritten to move .gantt-pane and .plan-divider left values; _paneDividerPct declared and reset in destroy(); no syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite split-pane CSS from grid to curtain overlay layout</name>
  <files>styles/views.css</files>
  <read_first>
    - styles/views.css lines 2421-2455 (current .plan-split-pane, .plan-divider, .task-grid-rail CSS — full block to replace)
    - styles/views.css — search for `@media` blocks near the plan-split-pane section to find the mobile breakpoint for .gantt-pane (preserve or update it)
  </read_first>
  <action>
    Replace the `.plan-split-pane` block with the curtain layout. The new CSS must produce these computed behaviors:

    1. `.plan-split-pane` — change from `display: grid; grid-template-columns: 35% 4px 1fr;` to `position: relative; overflow: hidden; display: flex; flex: 1 1 auto; min-height: 0;`. Remove `grid-template-columns`. Keep `border-top: 1px solid var(--gray-200, #dadce0)`. The `display: flex` + `flex: 1 1 auto` keeps the container expanding to fill vertical space (same behavior as before with the grid approach inside the plan-view-surface flex column).

    2. `.task-grid-rail` — add `position: relative; flex: 0 0 100%; min-width: 100%;`. The rail now fills the full container width (it is the "floor" element). The Gantt pane overlaps it. Keep `overflow-x: hidden; overflow-y: auto; background: #ffffff; border-right: 1px solid var(--gray-200, #dadce0)`.

    3. `.plan-divider` — change to `position: absolute; top: 0; bottom: 0; left: calc(35% - 2px); width: 4px; z-index: 20; cursor: col-resize; user-select: none; transition: background 0.15s; background: var(--gray-200, #dadce0);`. The JS sets `style.left` on drag; the CSS sets the default (35% - 2px). Keep the `:hover` and `.dragging` background rules.

    4. `.gantt-pane` (the wrapper div containing `#ganttPane`) — add `position: absolute; top: 0; right: 0; bottom: 0; left: 35%; z-index: 10; overflow: hidden; background: #fff;`. The JS sets `style.left` on drag; CSS provides the default. `z-index: 10` sits above the rail (which has no z-index / z-index: 0) so the Gantt panel paints over the covered portion of the grid.

    5. Mobile breakpoint — find the existing `@media (max-width: 768px)` block that controls plan view mobile layout. Ensure `.gantt-pane` gets `display: none` (or equivalent) on mobile, as it did before. The `.task-grid-rail` on mobile should reset to `position: relative; flex: 1 1 auto; min-width: 0;` so it fills the screen normally.

    Do NOT change any CSS that is NOT in the plan-split-pane / plan-divider / task-grid-rail / gantt-pane block. Do not touch `.task-grid`, `.task-grid thead`, or any other rules below the split-pane section.
  </action>
  <verify>
    <automated>grep -c "position: relative" styles/views.css</automated>
    Expected: count >= 1 for the new .plan-split-pane rule.
    Also: `grep -c "position: absolute" styles/views.css` >= 2 (plan-divider and .gantt-pane both absolute).
    Also: `grep -c "grid-template-columns" styles/views.css` == 0 for the split-pane block specifically — run `grep -n "grid-template-columns" styles/views.css` and verify no result is inside a `.plan-split-pane` block.
    Also: `grep -c "z-index: 10" styles/views.css` >= 1 (.gantt-pane overlay).
    Also: `grep -c "z-index: 20" styles/views.css` >= 1 (.plan-divider sits above the gantt pane so it's always clickable).
  </verify>
  <acceptance_criteria>
    - `grep -n "grid-template-columns" styles/views.css` returns no line inside a `.plan-split-pane` rule block
    - `.plan-split-pane` has `position: relative` and `overflow: hidden`
    - `.gantt-pane` has `position: absolute` and `z-index: 10`
    - `.plan-divider` has `position: absolute` and `z-index: 20` and `left: calc(35% - 2px)`
    - `.task-grid-rail` retains `overflow-x: hidden; overflow-y: auto`
    - `grep -c "z-index: 20" styles/views.css` >= 1
    - Mobile `@media` block sets `.gantt-pane { display: none }` (or equivalent) to preserve single-pane mobile behavior
  </acceptance_criteria>
  <done>Curtain CSS applied: .plan-split-pane is relative/overflow-hidden, .gantt-pane is absolute overlay with z-index:10, .plan-divider is absolute with z-index:20, mobile breakpoint preserves single-pane layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user drag input → style mutation | Mouse clientX used to compute pixel percentage for left style assignment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.9.1-01 | Tampering | _paneDividerPct clamp | accept | pct already clamped to Math.max(15, Math.min(75, ...)) in mousemove handler — no server data touched |
| T-86.9.1-02 | Denial of Service | rapid drag events firing style mutations | accept | style.left mutation is synchronous O(1); no Firestore writes; no accumulation possible |
</threat_model>

<verification>
After both tasks complete:

1. In the browser: navigate to a project plan view. Drag the panel divider slowly to the left. Task-grid column headers (Name, Duration, Start, End, Progress, Predecessors) remain visible and at full width — they do NOT get clipped or hidden as the divider moves left. The Gantt panel slides over the right portion of the grid like a curtain.

2. Click a task name in the uncovered (left) portion of the grid and verify the inline edit activates normally.

3. Drag the divider to the far right (~70-75%) — the task grid is mostly covered but the vertical row labels on the leftmost column remain visible.

4. Drag back to center (~35%) — Gantt panel retreats to the right and grid is visible again.

5. Reload the page — the divider starts at the 35% default position.

6. Resize browser to < 768px — Gantt pane is hidden, only the task grid shows (mobile behavior preserved).
</verification>

<success_criteria>
- Dragging the divider does NOT change any `grid-template-columns` style — confirmed by `grep -c "gridTemplateColumns" app/views/project-plan.js == 0`
- `.gantt-pane` is `position: absolute` at `left: 35%` (default) and `left: {pct}%` after drag
- `.plan-divider` is `position: absolute` at `left: calc(35% - 2px)` (default)
- `.plan-split-pane` is `position: relative; overflow: hidden`
- `node --check app/views/project-plan.js` exits 0
- No regressions in destroy() listener cleanup (existing `_resizeMouseMoveHandler`/`_resizeMouseUpHandler` removal blocks are unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/86.9-gantt-curtain-divider-pdf-export/86.9-01-SUMMARY.md`
</output>
