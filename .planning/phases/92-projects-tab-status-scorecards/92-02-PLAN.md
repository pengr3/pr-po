---
phase: 92-projects-tab-status-scorecards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/home.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "Home page no longer has a dedicated Firestore onSnapshot listener for the projects collection"
    - "Home page no longer renders a Projects stat card or Chart.js canvas for project status"
    - "The 3 remaining stat cards (Procurement: Pending MRFs, Pending PRs, Active POs) are unchanged"
    - "home.js does not contain the string 'projectsListener' or 'renderStatusBreakdown' calls for the projects chart"
    - "Operations/admin roles that previously saw the Projects card now see only the Procurement card"
  artifacts:
    - path: "app/views/home.js"
      provides: "Home page without Projects chart listener and card"
      exports: ["render", "init", "destroy"]
  key_links:
    - from: "home.js loadStats()"
      to: "projects onSnapshot block deleted"
      via: "lines 388-407 removed"
      pattern: "projectsCardHtml|projectsListener"
    - from: "home.js render()"
      to: "projectsCardHtml() call deleted"
      via: "mode check block updated"
      pattern: "statsContent \\+= projectsCardHtml"
---

<objective>
Remove the Projects status chart from the Home page as specified in D-10. Delete the dedicated projectsListener onSnapshot block in loadStats(), delete the projectsCardHtml() function, remove the call to projectsCardHtml() from render(), and remove the now-unused Chart.js color constants and helper functions that exist solely for the projects chart.

Purpose: The Projects tab scorecard strip (Plan 01) is now the canonical status overview surface. The Home page chart is redundant and adds an unnecessary Firestore listener on every Home visit.
Output: home.js with projects chart code removed; Home page shows only Procurement card for operations roles (same as services-only roles see only Procurement + Services).
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\.planning\phases\92-projects-tab-status-scorecards\92-CONTEXT.md

Key facts extracted from the codebase (do not re-read these sections — use these directly):

home.js loadStats() contains three onSnapshot blocks. The projects block is guarded by:
  if (mode === 'projects' || mode === 'both') {
      const projectsListener = onSnapshot(collection(db, 'projects'), ...
      statsListeners.push(projectsListener);
  }
This block spans approximately lines 388-407 and must be deleted in full (the if-block and its contents).

home.js render() builds statsContent by conditionally appending card HTML:
  let statsContent = procurementCardHtml();
  if (mode === 'projects' || mode === 'both') { statsContent += projectsCardHtml(); }
  if (mode === 'services' || mode === 'both') { statsContent += servicesCardHtml(); }
The middle block (projectsCardHtml line) must be deleted.

home.js contains projectsCardHtml() function at lines 130-140. This function and its declaration must be deleted entirely.

home.js UNIFIED_STATUS_OPTIONS local copy (lines 9-21) is still used by the services onSnapshot (for otStatus/recStatus tallying). Do NOT delete it.

home.js cachedStats object (lines 26-35) contains projectsByStatus: null. This field can remain (harmless) or be removed. Removing it is cleaner — remove the line "projectsByStatus: null," from the object literal.

home.js MONOCHROMATIC_STATUS_COLORS contains a 'Draft' entry (line 52). Since this file's color maps are only used by renderStatusBreakdown() for services charts now, the 'Draft' entry in the color map is harmless (it maps a color, not a status option). Leave it as-is to avoid unintended side effects on services rendering.

home.js buildStatusBreakdownContainer(), renderStatusBreakdown(), HIGHLIGHTED_STATUS_COLORS, MONOCHROMATIC_STATUS_COLORS, MONOCHROMATIC_FALLBACK, getBarColor(), getChartSizeClass() — these are all still used by the SERVICES chart. Do NOT delete them.

home.js destroy() tears down chartInstances (Chart.js). This still applies to services charts. No change needed there.

The services card (servicesCardHtml) and its onSnapshot listener (lines 410-438) must NOT be touched.
</context>

<interfaces>
From app/views/home.js — context the executor needs:

cachedStats object before change:
  let cachedStats = {
      activeMRFs: null,
      pendingPRs: null,
      activePOs: null,
      projectsByStatus: null,       ← delete this line
      servicesByStatusOneTime: null,
      servicesByStatusRecurring: null
  };

loadStats() structure after deletion:
  function loadStats(mode) {
      // mrfListener block (keep)
      // prListener block (keep)
      // poListener block (keep)
      // ---- Projects card (D-02) ---- block → DELETE (lines 388-407)
      // ---- Services card (D-03) ---- block (keep)
  }

render() structure after deletion:
  let statsContent = procurementCardHtml();
  // if (mode === 'projects' || mode === 'both') { statsContent += projectsCardHtml(); }  ← DELETE this block
  if (mode === 'services' || mode === 'both') { statsContent += servicesCardHtml(); }

Functions to DELETE:
  - projectsCardHtml() (lines 130-140)

Functions to KEEP (still used by services charts):
  - procurementCardHtml()
  - servicesCardHtml()
  - buildStatusBreakdownContainer()
  - renderStatusBreakdown()
  - getBarColor(), getChartSizeClass()
  - HIGHLIGHTED_STATUS_COLORS, MONOCHROMATIC_STATUS_COLORS, MONOCHROMATIC_FALLBACK
  - UNIFIED_STATUS_OPTIONS (local copy — used for services tallying)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete projects chart code from home.js</name>
  <files>app/views/home.js</files>
  <read_first>
    Read app/views/home.js in full before making any edits. The file is approximately 480 lines. Map out every occurrence of 'projectsCard', 'projectsListener', 'projectsByStatus', and 'renderStatusBreakdown' before touching anything, to avoid accidentally removing services chart code.
  </read_first>
  <action>
    Make ALL of the following changes to app/views/home.js. Apply in file-order.

    CHANGE 1 — Delete projectsByStatus from cachedStats object (lines 26-35 area):
    Remove the line: projectsByStatus: null,
    The object must still contain activeMRFs, pendingPRs, activePOs, servicesByStatusOneTime, servicesByStatusRecurring.

    CHANGE 2 — Delete the entire projectsCardHtml() function (lines 130-140 approximately). The function starts with "function projectsCardHtml() {" and returns HTML for the Projects stat card including buildStatusBreakdownContainer('stat-projects-status', ...). Delete from the JSDoc comment above it (the line "* Build Projects card HTML (D-02)...") through the closing brace.

    CHANGE 3 — In render() (lines 265-313 area): Delete the block:
      if (mode === 'projects' || mode === 'both') {
          statsContent += projectsCardHtml();
      }
    Leave the procurementCardHtml() line and the services block untouched.

    CHANGE 4 — In loadStats() (lines 338-438 area): Delete the entire projects onSnapshot block:
      // ---- Projects card (D-02) ----
      if (mode === 'projects' || mode === 'both') {
          const projectsListener = onSnapshot(
              collection(db, 'projects'),
              (snapshot) => {
                  ...
              },
              (error) => { ... }
          );
          statsListeners.push(projectsListener);
      }
    Delete from the comment line "// ---- Projects card (D-02) ----" through "statsListeners.push(projectsListener);" and the closing brace of the if-block. The services block immediately after must remain intact.

    DO NOT modify any services-related code, renderStatusBreakdown(), buildStatusBreakdownContainer(), UNIFIED_STATUS_OPTIONS, chart color constants, or destroy().
  </action>
  <verify>
    grep -c "projectsCardHtml" app/views/home.js returns 0.
    grep -c "projectsListener" app/views/home.js returns 0.
    grep -c "projectsByStatus" app/views/home.js returns 0.
    grep -c "stat-projects-status" app/views/home.js returns 0.
    grep -c "servicesCardHtml" app/views/home.js returns at least 2 (function def + call in render) — confirms services code was not deleted.
    grep -c "renderStatusBreakdown" app/views/home.js returns at least 3 — confirms services rendering still present.
  </verify>
  <acceptance_criteria>
    - home.js does NOT contain the string 'projectsCardHtml'
    - home.js does NOT contain the string 'projectsListener'
    - home.js does NOT contain the string 'projectsByStatus'
    - home.js does NOT contain the string 'stat-projects-status'
    - home.js STILL contains 'servicesCardHtml' (services chart untouched)
    - home.js STILL contains 'renderStatusBreakdown' (used for services chart)
    - home.js STILL contains 'procurementCardHtml' (procurement card untouched)
    - home.js STILL contains the UNIFIED_STATUS_OPTIONS array (used for services tallying)
    - home.js cachedStats object does not have a 'projectsByStatus' key
    - home.js render() does not call projectsCardHtml()
    - home.js loadStats() does not contain a projects onSnapshot block
    - home.js has no syntax errors (page loads without console errors)
  </acceptance_criteria>
  <done>Home page loads without JS errors; operations_admin/user roles see only the Procurement stat card (no Projects chart card); services roles still see their services chart card; super_admin/finance roles see Procurement + Services cards only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| home.js services listener | Remaining onSnapshot for services collection; not changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-02-01 | Denial of Service | Orphaned projectsListener not removed | mitigate | Deleting the entire listener block prevents it from ever being created; no orphan risk |
| T-92-02-02 | Information Disclosure | cachedStats.projectsByStatus persisting | mitigate | Field deleted from cachedStats; no stale data retained |
</threat_model>

<verification>
After task completes, open the app in a browser at http://localhost:8000/#/ (Home) and verify:
1. No JS console errors on home page load
2. Procurement stat card shows Pending MRFs, Pending PRs, Active POs (unchanged)
3. Operations roles (operations_admin, operations_user) — if testable — see only the Procurement card, no Projects status chart
4. Services chart (if visible for services role) still renders correctly
5. No "stat-projects-status" canvas element in DOM (inspect elements)
</verification>

<success_criteria>
- home.js contains zero references to projectsCardHtml, projectsListener, projectsByStatus, stat-projects-status
- Home page renders without JS errors for all roles
- Procurement stat card unchanged (MRFs, PRs, POs counts still live)
- Services chart unchanged (renders for services/super_admin roles)
- No extra Firestore projects listener on the Home page
</success_criteria>

<output>
After completion, create .planning/phases/92-projects-tab-status-scorecards/92-02-SUMMARY.md
</output>
