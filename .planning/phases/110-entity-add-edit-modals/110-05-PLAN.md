---
phase: 110-entity-add-edit-modals
plan: 05
type: execute
wave: 2
depends_on: [110-02, 110-03]
files_modified:
  - app/views/projects.js
autonomous: true
requirements: [MODAL-03, MODAL-04, MODAL-07]
must_haves:
  truths:
    - "The Projects list 'Add Project' button opens the New Engagement modal via openEngagementModal({lockedType:'project'}) — the inline #addProjectForm and toggleAddProjectForm/addProject are gone"
    - "The Add Project button is gated by canEngagements (super_admin/operations_admin/services_admin) per D-05, replacing the old canCreateProject (super_admin/operations_admin) gate"
    - "The per-row 'Edit' button opens openProjectEditModal(projectId, record) — the inline editProject/saveEdit/cancelEdit path is gone"
    - "The now-unused inline personnel-pill helpers and inline tranche-builder window-fn registrations are removed from projects.js; attach/destroy blocks updated; no dead references remain"
    - "project-detail.js is NOT touched — detail-page inline auto-save is unchanged (MODAL-07)"
  artifacts:
    - path: "app/views/projects.js"
      provides: "Projects list wired to the engagement-create modal (Add) + entity-edit-modal (Edit); inline add/edit forms removed; D-05 gate swap"
  key_links:
    - from: "app/views/projects.js"
      to: "app/engagement-create.js"
      via: "openEngagementModal({lockedType:'project'}) for Add"
      pattern: "openEngagementModal\\(\\{[^}]*lockedType: ?'project'"
    - from: "app/views/projects.js"
      to: "app/entity-edit-modal.js"
      via: "openProjectEditModal(projectId, record) for Edit"
      pattern: "openProjectEditModal"
    - from: "Add Project button"
      to: "canEngagements gate"
      via: "3-role check (D-05)"
      pattern: "super_admin.*operations_admin.*services_admin"
---

<objective>
Wire `app/views/projects.js` to the consolidated Add (Plan 02: `openEngagementModal`) and the dedicated Edit modal (Plan 03: `openProjectEditModal`), and remove the retired inline `#addProjectForm` (add+edit reuse). Swap the Add Project gate to `canEngagements` per D-05.

Per D-04: "Add Project" opens the existing New Engagement modal locked to `type='project'` (NOT a wrapper around the inline form); new projects start as Draft with no status/tranche editor at creation (accepted — set afterward via the Edit modal / detail page). Per D-05: the Add button gates to the 3 engagement roles (`super_admin`, `operations_admin`, `services_admin`) via a `canEngagements`-equivalent check, replacing the old `canCreateProject` (super_admin/operations_admin only) — accepted widen: `services_admin` gains Project-Add; non-engagement roles that had `canEditTab('projects')` lose list-Add but keep detail-page editing.

The inline personnel-pill helpers and inline tranche-builder window fns move OUT of projects.js (they now live in entity-edit-modal.js). project-detail.js is not touched.

Purpose: retire the Projects inline add/edit form; route Add→engagement modal, Edit→edit modal.
Output: modified `app/views/projects.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-entity-add-edit-modals/110-CONTEXT.md
@.planning/phases/110-entity-add-edit-modals/110-PATTERNS.md
@.planning/phases/110-entity-add-edit-modals/110-02-SUMMARY.md
@.planning/phases/110-entity-add-edit-modals/110-03-SUMMARY.md

<interfaces>
<!-- Exact regions to change in projects.js. Cited file:line. -->

- Add button + gate: projects.js:145-172 (canCreateProject = super_admin||operations_admin at :147; the Add button `onclick="window.toggleAddProjectForm()"` at :170 gated by canCreateProject). Swap gate → canEngagements (super_admin/operations_admin/services_admin).
- Inline add-form to DELETE: projects.js:175-260ish (`<div id="addProjectForm" class="add-form" ...>` incl. the UNIFIED_STATUS_OPTIONS <select> at :200 and renderTrancheBuilder([],'projectForm') at :219 and personnel pill markup).
- Row Edit button: `onclick="window.editProject('${...}')"` in renderBrowseAll/feed rows — repoint to the new Edit launcher.
- Functions to DELETE (logic now in the two modal modules): toggleAddProjectForm (614-656), addProject (659-~765), editProject (1228-1272), cancelEdit (1275-1277), saveEdit (1280-~1454). Inline personnel-pill helpers renderPills/filterPersonnelDropdown/showPersonnelDropdown/selectPersonnel/removePersonnel (486-580) — remove (now in entity-edit-modal.js) after confirming no OTHER projects.js code calls them.
- attachWindowFunctions: projects.js:107-133 (drop toggleAddProjectForm/addProject/editProject/cancelEdit/saveEdit + selectPersonnel/removePersonnel/filterPersonnelDropdown/showPersonnelDropdown + addTranche/removeTranche/recalculateTranches; add the new Add/Edit launchers).
- destroy delete block: projects.js:403-426 (mirror the same drops; keep unrelated fns).
- Imports: projects.js:10 (renderTrancheBuilder/readTranchesFromDOM/addTranche/removeTranche/recalculateTranches from '../tranche-builder.js') — drop the ones that become unused after the inline form is gone.

engagement-create.js (Plan 02): `openEngagementModal({ lockedType, onCreated })`. entity-edit-modal.js (Plan 03): `openProjectEditModal(projectId, record)`.
canEngagements pattern (home.js:63): `['super_admin','operations_admin','services_admin'].includes(role)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove the inline #addProjectForm, the add/edit functions, and the now-unused pill/tranche wiring</name>
  <files>app/views/projects.js</files>
  <read_first>
    - app/views/projects.js lines 107-133 (attachWindowFunctions), 145-260 (render gate + Add button + #addProjectForm block), 366-427 (destroy), 486-580 (personnel-pill helpers), 614-765 (toggleAddProjectForm/addProject), 1228-1455 (editProject/cancelEdit/saveEdit)
    - .planning/phases/110-entity-add-edit-modals/110-CONTEXT.md D-04 (inline add-forms + toggle fns removed; Draft-at-creation accepted) and the code_context note "check for other callers first"
  </read_first>
  <action>
    Delete the entire `<div id="addProjectForm" class="add-form" ...>` block from `render()` (projects.js ~175-260). Leave the header, Export CSV button, scorecards, filters, portfolio/browse-all rendering intact.

    Delete the functions `toggleAddProjectForm`, `addProject`, `editProject`, `cancelEdit`, and `saveEdit` entirely. Delete the inline personnel-pill helpers `renderPills`, `filterPersonnelDropdown`, `showPersonnelDropdown`, `selectPersonnel`, `removePersonnel` (486-580) — but FIRST grep projects.js to confirm nothing else calls them; if any survivor references them, stop and report before deleting. Remove the module state tied only to the inline form: `editingProject`, `editingProjectTranches`, `selectedPersonnel` (and their resets in destroy) — confirm each is unused by surviving code before removal.

    Remove from `attachWindowFunctions()` (107-133): toggleAddProjectForm, addProject, editProject, cancelEdit, saveEdit, selectPersonnel, removePersonnel, filterPersonnelDropdown, showPersonnelDropdown, and the addTranche/removeTranche/recalculateTranches registrations (the inline tranche builder is gone; entity-edit-modal.js registers its own on open). Mirror the same deletions in `destroy()` (403-426). Keep exportProjectsCSV, applyFilters/debouncedFilter, deleteProject, toggleProjectActive, handleScorecardClick, vmSwitch, toggleStageGroup.

    In the import at projects.js:10, drop `renderTrancheBuilder, readTranchesFromDOM, addTranche, removeTranche, recalculateTranches` IF none are referenced by surviving code (they were only used by the deleted add/edit paths). Likewise drop `normalizePersonnel`/`syncPersonnelToAssignments` from the utils import only if now unused. Do not remove imports still used elsewhere (e.g. escapeHTML, formatCurrency, generateProjectCode).

    Do NOT touch project-detail.js. Do NOT change loadClients/renderClientDropdown/loadProjects/loadActiveUsers unless they exist solely to feed the deleted inline form — if loadActiveUsers/renderClientDropdown become dead, remove them too (verify no survivor calls them first).
  </action>
  <acceptance_criteria>
    - `grep -c "addProjectForm" app/views/projects.js` returns 0.
    - `grep -nE "function (toggleAddProjectForm|addProject|editProject|cancelEdit|saveEdit)\b" app/views/projects.js` returns 0.
    - `grep -nE "function (renderPills|filterPersonnelDropdown|showPersonnelDropdown|selectPersonnel|removePersonnel)\b" app/views/projects.js` returns 0.
    - `grep -nE "window\.(toggleAddProjectForm|addProject|editProject|cancelEdit|saveEdit|selectPersonnel|removePersonnel|filterPersonnelDropdown|showPersonnelDropdown|addTranche|removeTranche|recalculateTranches) =" app/views/projects.js` returns 0 (attach cleaned).
    - `grep -c "editingProject\b" app/views/projects.js` returns 0.
    - projects.js loads with no ReferenceError; `grep -c "renderPortfolio\|renderBrowseAll\|handleScorecardClick" app/views/projects.js` still matches (list rendering preserved).
  </acceptance_criteria>
  <verify>Grep checks pass; projects.js loads with no console error; the Projects list still renders scorecards, portfolio, and browse-all rows.</verify>
  <done>The inline add/edit form, its five functions, the inline pill helpers, and the inline tranche-builder window fns are removed; attach/destroy/imports cleaned with no dead references.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire Add Project (locked engagement modal + D-05 gate) and Edit (project edit modal)</name>
  <files>app/views/projects.js</files>
  <read_first>
    - app/views/projects.js lines 145-172 (canCreateProject gate + Add button), the row Edit button occurrences (renderBrowseAll / buildFeedRow), 107-133 + 366-427 (attach/destroy)
    - .planning/phases/110-entity-add-edit-modals/110-02-SUMMARY.md (openEngagementModal signature + lockedType) and 110-03-SUMMARY.md (openProjectEditModal signature)
    - .planning/phases/110-entity-add-edit-modals/110-CONTEXT.md D-05 (gate to canEngagements) + the pattern-mapper note on the accepted widen for services_admin
  </read_first>
  <action>
    Add imports: `import { openEngagementModal } from '../engagement-create.js';` and `import { openProjectEditModal } from '../entity-edit-modal.js';`.

    D-05 gate swap: replace `const canCreateProject = user?.role === 'super_admin' || user?.role === 'operations_admin';` (projects.js:147) with `const canCreateProject = ['super_admin','operations_admin','services_admin'].includes(user?.role);` (the canEngagements 3-role set). Keep the variable name or rename to canEngagements — either is fine; the Add button visibility must be driven by this 3-role check. (Accepted widen: services_admin gains Project-Add; this is the documented D-05 behavior.)

    Add a window-fn wrapper `function openProjectAdd() { openEngagementModal({ lockedType: 'project', onCreated: () => showToast('Project created as Draft — set status and tranches from the project detail page or Edit.', 'success') }); }` (the onCreated is optional UX; the launcher itself auto-closes on success per Plan 02). Change the Add button (projects.js:170) `onclick` to `window.openProjectAdd()`.

    Add a window-fn wrapper `function openProjectEdit(projectId) { if (window.canEditTab?.('projects') === false) { showToast('You do not have permission to edit projects', 'error'); return; } const record = allProjects.find(p => p.id === projectId) || null; openProjectEditModal(projectId, record); }`. Repoint every row Edit button `onclick` from `window.editProject('...')` to `window.openProjectEdit('...')` (search renderBrowseAll + any feed/portfolio row that emitted an Edit button). Keep detail-page navigation (row click → detail) unchanged.

    Register `window.openProjectAdd = openProjectAdd; window.openProjectEdit = openProjectEdit;` in attachWindowFunctions and add matching deletes in destroy.
  </action>
  <acceptance_criteria>
    - `grep -n "from '../engagement-create.js'" app/views/projects.js` matches (openEngagementModal imported); `grep -n "from '../entity-edit-modal.js'" app/views/projects.js` matches (openProjectEditModal imported).
    - `grep -n "openEngagementModal({ lockedType: 'project'" app/views/projects.js` matches (Add locked to project, D-04).
    - `grep -nE "\['super_admin','operations_admin','services_admin'\]\.includes\(user\?\.role\)|canEngagements" app/views/projects.js` matches (D-05 gate swap; services_admin included).
    - `grep -c "window.editProject('" app/views/projects.js` returns 0 and `grep -c "window.openProjectEdit('" app/views/projects.js` is ≥1 (row Edit repointed).
    - `grep -nE "window\.(openProjectAdd|openProjectEdit) =" app/views/projects.js` returns 2 (attach) and the matching deletes exist in destroy.
    - Console: as super_admin the Add Project button opens the New Engagement modal with the type locked to Project and creating a Draft project auto-closes it; as services_admin the Add Project button is visible (accepted widen); as a non-engagement role with canEditTab('projects') the Add Project button is hidden but the row Edit still opens the pre-filled edit modal.
  </acceptance_criteria>
  <verify>Grep checks pass; Add opens the locked engagement modal, Edit opens the pre-filled project edit modal, and the D-05 3-role gate governs Add visibility.</verify>
  <done>Add Project launches the engagement modal locked to project behind the canEngagements gate; row Edit launches openProjectEditModal; launchers registered/cleaned up; detail-page untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (authenticated user) → engagement-create / entity-edit modals | The Add gate (canEngagements) and Edit gate (canEditTab) decide who can invoke project creates/updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-05-AUTHZ | E (Elevation of Privilege) | D-05 Add gate swap | mitigate | The Add button moves from canCreateProject (2 roles) to canEngagements (3 roles) per the LOCKED D-05 decision — an accepted, documented widen (services_admin gains Project-Add). The engagement modal it opens is itself role-scoped (renderEngagementForm(role, 'project')), and Firestore `projects` rules are unchanged, so no user can create a project they could not already create via home's + New Engagement. Edit stays behind canEditTab('projects'). No unintended widening beyond the documented D-05 change. |
| T-110-05-XSS | I (stored XSS) | row Edit onclick | accept | Only the Firestore project id is interpolated into the onclick; the record is passed by object reference to openProjectEditModal, which escapeHTMLs on render. Existing row cells' escaping is unchanged. |
| T-110-05-SC | T (supply chain) | dependencies | accept | No package installs in this plan. |
</threat_model>

<verification>
- Add Project opens the engagement modal locked to project (MODAL-03) behind the canEngagements gate (D-05).
- Row Edit opens the pre-filled project edit modal (MODAL-04).
- The inline #addProjectForm, its functions, inline pill helpers, and inline tranche window fns are removed with no dead references.
- project-detail.js is NOT modified — detail-page inline auto-save unchanged (MODAL-07).
</verification>

<success_criteria>
- Adding a project uses the shared engagement `.modal`; editing a project uses the shared edit `.modal` (MODAL-03, MODAL-04, MODAL-07).
- D-05 gate swap is in place; no inline add/edit form remains in projects.js.
</success_criteria>

<output>
Create `.planning/phases/110-entity-add-edit-modals/110-05-SUMMARY.md` when done — record the removed symbols, the D-05 gate swap (and the accepted services_admin widen), the wrapper names, and confirmation that project-detail.js was untouched.
</output>
