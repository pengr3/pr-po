---
phase: 110-entity-add-edit-modals
plan: 06
type: execute
wave: 2
depends_on: [110-02, 110-03]
files_modified:
  - app/views/services.js
autonomous: true
requirements: [MODAL-05, MODAL-06, MODAL-07]
must_haves:
  truths:
    - "The Services list 'Add Service' button opens the New Engagement modal via openEngagementModal({lockedType:'service'}) — the inline #addServiceForm and toggleAddServiceForm/addService are gone"
    - "The service lock keeps the One-time vs Recurring sub-choice user-selectable (only the project-vs-service axis is locked) — pattern-mapper flag #2 resolution"
    - "The Add Service button is gated by canEngagements (super_admin/operations_admin/services_admin) per D-05, replacing the old canCreateService gate"
    - "The per-row 'Edit' button opens openServiceEditModal(serviceId, record) — the inline editService/saveServiceEdit path is gone"
    - "service-detail.js is NOT touched — detail-page inline auto-save is unchanged (MODAL-07)"
  artifacts:
    - path: "app/views/services.js"
      provides: "Services list wired to the engagement-create modal (Add) + entity-edit-modal (Edit); inline add/edit forms removed; D-05 gate swap"
  key_links:
    - from: "app/views/services.js"
      to: "app/engagement-create.js"
      via: "openEngagementModal({lockedType:'service'}) for Add"
      pattern: "openEngagementModal\\(\\{[^}]*lockedType: ?'service'"
    - from: "app/views/services.js"
      to: "app/entity-edit-modal.js"
      via: "openServiceEditModal(serviceId, record) for Edit"
      pattern: "openServiceEditModal"
---

<objective>
Wire `app/views/services.js` to the consolidated Add (Plan 02: `openEngagementModal`) and the dedicated Edit modal (Plan 03: `openServiceEditModal`), and remove the retired inline `#addServiceForm` (add+edit reuse). Swap the Add Service gate to `canEngagements` per D-05. This is the exact mirror of Plan 05 for the Services list.

Per D-04: "Add Service" opens the existing New Engagement modal locked to the service axis (`lockedType='service'`); per pattern-mapper flag #2 the One-time vs Recurring sub-choice stays user-selectable (only Project-vs-Service is locked). New services start as Draft with no status/tranche editor at creation (accepted — set afterward via Edit / detail page). Per D-05: the Add button gates to the 3 engagement roles.

The inline service personnel-pill helpers and inline tranche-builder window fns move OUT of services.js (they now live in entity-edit-modal.js). service-detail.js is not touched.

Purpose: retire the Services inline add/edit form; route Add→engagement modal, Edit→edit modal.
Output: modified `app/views/services.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-entity-add-edit-modals/110-CONTEXT.md
@.planning/phases/110-entity-add-edit-modals/110-PATTERNS.md
@.planning/phases/110-entity-add-edit-modals/110-02-SUMMARY.md
@.planning/phases/110-entity-add-edit-modals/110-03-SUMMARY.md

<interfaces>
<!-- Exact regions to change in services.js. Cited file:line. -->

- attachWindowFunctions: services.js:98-119 (toggleAddServiceForm/addService/editService/cancelServiceEdit/saveServiceEdit at 99-103) — drop these + the service pill helpers + tranche window fns; add new launchers.
- Add button: services.js:173 `onclick="window.toggleAddServiceForm()"` (gated by the service create-role check in render — mirror of projects.js canCreateProject). Swap gate → canEngagements.
- Inline add-form to DELETE: the `<div id="addServiceForm" class="add-form" ...>` block (incl. serviceType select, UNIFIED_STATUS_OPTIONS status select, renderTrancheBuilder([],'serviceForm'), personnel pill markup, submit button at :254-255).
- Functions to DELETE: toggleAddServiceForm (632-677), addService (678-~790), editService (1272-1317), cancelServiceEdit (1320-1322), saveServiceEdit (1325-~1500). Service personnel-pill helpers renderServicePills (504-...), selectServicePersonnel (573-...), removeServicePersonnel (588-...) and any filter/show helpers — remove after confirming no other caller.
- destroy delete block: services.js:422-426 (+ pill/tranche deletes) — mirror the drops.
- Row Edit button: `onclick="window.editService('${...}')"` — repoint to the new Edit launcher.
- Imports: services.js top (renderTrancheBuilder/readTranchesFromDOM/addTranche/removeTranche/recalculateTranches from '../tranche-builder.js'; normalizePersonnel/syncServicePersonnelToAssignments from '../utils.js') — drop those that become unused after the inline form + saveServiceEdit are gone.

engagement-create.js (Plan 02): `openEngagementModal({ lockedType, onCreated })`. entity-edit-modal.js (Plan 03): `openServiceEditModal(serviceId, record)` — note the edit modal owns the service-side syncServicePersonnelToAssignments call now (Plan 03), so services.js no longer needs that import once saveServiceEdit is removed.
canEngagements pattern (home.js:63): `['super_admin','operations_admin','services_admin'].includes(role)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove the inline #addServiceForm, the add/edit functions, and the now-unused pill/tranche wiring</name>
  <files>app/views/services.js</files>
  <read_first>
    - app/views/services.js lines 98-119 (attachWindowFunctions), 136-260 (render gate + Add button + #addServiceForm block), ~410-430 (destroy), 504-613 (service personnel-pill helpers), 632-790 (toggleAddServiceForm/addService), 1272-1500 (editService/cancelServiceEdit/saveServiceEdit — note saveServiceEdit's syncServicePersonnelToAssignments at :1496 leaves with this function)
    - .planning/phases/110-entity-add-edit-modals/110-05-SUMMARY.md (how the projects.js mirror was executed — follow the same removal approach)
    - .planning/phases/110-entity-add-edit-modals/110-CONTEXT.md D-04 (inline add-forms + toggle fns removed) and the code_context "check for other callers first" note
  </read_first>
  <action>
    Delete the entire `<div id="addServiceForm" class="add-form" ...>` block from `render()`. Leave header, Export CSV, scorecards, filters, and list rendering intact.

    Delete the functions `toggleAddServiceForm`, `addService`, `editService`, `cancelServiceEdit`, and `saveServiceEdit` entirely. Delete the inline service personnel-pill helpers (`renderServicePills`, `selectServicePersonnel`, `removeServicePersonnel`, and any service filter/show dropdown helpers) — FIRST grep services.js to confirm nothing surviving calls them; if any survivor references them, stop and report. Remove the module state tied only to the inline form (`editingService`, `editingServiceTranches`, `selectedPersonnel`) after confirming each is unused by surviving code.

    Remove from `attachWindowFunctions()` (98-119): toggleAddServiceForm, addService, editService, cancelServiceEdit, saveServiceEdit, the service pill window fns, and the addTranche/removeTranche/recalculateTranches registrations. Mirror the deletions in `destroy()`. Keep exportServicesCSV/applyFilters/deleteService/toggleServiceActive/scorecard/vmSwitch/toggleStageGroup equivalents.

    Drop the now-unused imports (renderTrancheBuilder/readTranchesFromDOM/addTranche/removeTranche/recalculateTranches; normalizePersonnel/syncServicePersonnelToAssignments — the latter was only used by saveServiceEdit, which is being deleted; the edit modal (Plan 03) now owns the service-side sync) IF no surviving code references them. Keep escapeHTML/formatCurrency/getAssignedServiceCodes/generateServiceCode/etc. that survivors still use. Do NOT touch service-detail.js. If loadActiveUsers/renderServiceClientDropdown exist only to feed the deleted inline form, remove them too after verifying no survivor calls them.
  </action>
  <acceptance_criteria>
    - `grep -c "addServiceForm" app/views/services.js` returns 0.
    - `grep -nE "function (toggleAddServiceForm|addService|editService|cancelServiceEdit|saveServiceEdit)\b" app/views/services.js` returns 0.
    - `grep -nE "function (renderServicePills|selectServicePersonnel|removeServicePersonnel)\b" app/views/services.js` returns 0.
    - `grep -nE "window\.(toggleAddServiceForm|addService|editService|cancelServiceEdit|saveServiceEdit|addTranche|removeTranche|recalculateTranches) =" app/views/services.js` returns 0 (attach cleaned).
    - `grep -c "editingService\b" app/views/services.js` returns 0.
    - services.js loads with no ReferenceError; the Services list still renders its scorecards/portfolio/browse rows (`grep -c "renderPortfolio\|renderBrowseAll\|handleScorecardClick" app/views/services.js` still matches).
  </acceptance_criteria>
  <verify>Grep checks pass; services.js loads with no console error; the Services list still renders correctly.</verify>
  <done>The inline add/edit form, its five functions, inline service pill helpers, and inline tranche window fns are removed; attach/destroy/imports cleaned with no dead references.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire Add Service (service-locked engagement modal + D-05 gate) and Edit (service edit modal)</name>
  <files>app/views/services.js</files>
  <read_first>
    - app/views/services.js render() gate + Add button; the row Edit button occurrences; attach/destroy blocks
    - .planning/phases/110-entity-add-edit-modals/110-02-SUMMARY.md (openEngagementModal + lockedType='service' semantics: One-time/Recurring stay selectable) and 110-03-SUMMARY.md (openServiceEditModal signature; the modal owns the service-side syncServicePersonnelToAssignments)
    - .planning/phases/110-entity-add-edit-modals/110-CONTEXT.md D-05 (gate to canEngagements) + planning-context flag #2 (service sub-type selectable)
  </read_first>
  <action>
    Add imports: `import { openEngagementModal } from '../engagement-create.js';` and `import { openServiceEditModal } from '../entity-edit-modal.js';`.

    D-05 gate swap: replace the existing service create-role check in render() (the canCreateService/`super_admin`+`operations_admin`+... equivalent driving the Add Service button) with `['super_admin','operations_admin','services_admin'].includes(user?.role)` (the canEngagements 3-role set). Add Service button visibility must be driven by this check.

    Add a window-fn wrapper `function openServiceAdd() { openEngagementModal({ lockedType: 'service', onCreated: () => showToast('Service created as Draft — set status and tranches from the service detail page or Edit.', 'success') }); }` — lockedType 'service' locks only the project-vs-service axis; the modal still shows One-time + Recurring radios (flag #2). Change the Add Service button `onclick` (services.js:173) to `window.openServiceAdd()`.

    Add a window-fn wrapper `function openServiceEdit(serviceId) { if (window.canEditTab?.('services') === false) { showToast('You do not have permission to edit services', 'error'); return; } const record = allServices.find(s => s.id === serviceId) || null; openServiceEditModal(serviceId, record); }`. Repoint every row Edit button `onclick` from `window.editService('...')` to `window.openServiceEdit('...')`. Keep row-click→detail navigation unchanged.

    Register `window.openServiceAdd = openServiceAdd; window.openServiceEdit = openServiceEdit;` in attachWindowFunctions and add matching deletes in destroy.
  </action>
  <acceptance_criteria>
    - `grep -n "from '../engagement-create.js'" app/views/services.js` matches; `grep -n "from '../entity-edit-modal.js'" app/views/services.js` matches.
    - `grep -n "openEngagementModal({ lockedType: 'service'" app/views/services.js` matches (Add locked to service axis, D-04).
    - `grep -nE "\['super_admin','operations_admin','services_admin'\]\.includes\(user\?\.role\)|canEngagements" app/views/services.js` matches (D-05 gate swap).
    - `grep -c "window.editService('" app/views/services.js` returns 0 and `grep -c "window.openServiceEdit('" app/views/services.js` is ≥1.
    - `grep -nE "window\.(openServiceAdd|openServiceEdit) =" app/views/services.js` returns 2 (attach) and matching deletes exist in destroy.
    - Console: Add Service opens the New Engagement modal with the Project radio hidden and BOTH One-time and Recurring radios present/selectable (flag #2); creating a Draft service auto-closes the modal; row Edit opens the pre-filled service edit modal with Service Type reflecting the record; a role with canEditTab('services')===false cannot open Edit.
  </acceptance_criteria>
  <verify>Grep checks pass; Add opens the service-locked engagement modal (One-time/Recurring selectable), Edit opens the pre-filled service edit modal, and the D-05 gate governs Add visibility.</verify>
  <done>Add Service launches the engagement modal locked to the service axis behind canEngagements; row Edit launches openServiceEditModal; launchers registered/cleaned up; detail-page untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (authenticated user) → engagement-create / entity-edit modals | The Add gate (canEngagements) and Edit gate (canEditTab) decide who can invoke service creates/updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-06-AUTHZ | E (Elevation of Privilege) | D-05 Add gate swap | mitigate | Add Service moves to canEngagements per the LOCKED D-05 decision; the engagement modal is role-scoped (renderEngagementForm(role,'service')) and Firestore `services` rules are unchanged, so no user gains a create they could not already perform via home's + New Engagement. Edit stays behind canEditTab('services'). |
| T-110-06-XSS | I (stored XSS) | row Edit onclick | accept | Only the Firestore service id is interpolated into the onclick; the record is passed by object reference to openServiceEditModal, which escapeHTMLs on render. |
| T-110-06-SC | T (supply chain) | dependencies | accept | No package installs in this plan. |
</threat_model>

<verification>
- Add Service opens the engagement modal locked to the service axis with One-time/Recurring selectable (MODAL-05, flag #2) behind the canEngagements gate (D-05).
- Row Edit opens the pre-filled service edit modal (MODAL-06).
- The inline #addServiceForm, its functions, inline pill helpers, and inline tranche window fns are removed with no dead references.
- service-detail.js is NOT modified — detail-page inline auto-save unchanged (MODAL-07).
</verification>

<success_criteria>
- Adding a service uses the shared engagement `.modal`; editing a service uses the shared edit `.modal` (MODAL-05, MODAL-06, MODAL-07).
- D-05 gate swap is in place; no inline add/edit form remains in services.js.
</success_criteria>

<output>
Create `.planning/phases/110-entity-add-edit-modals/110-06-SUMMARY.md` when done — record the removed symbols, the D-05 gate swap, the service sub-type-selectable behavior (flag #2), the wrapper names, and confirmation that service-detail.js was untouched.
</output>
