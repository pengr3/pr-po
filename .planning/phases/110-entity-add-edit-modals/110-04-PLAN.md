---
phase: 110-entity-add-edit-modals
plan: 04
type: execute
wave: 2
depends_on: [110-01]
files_modified:
  - app/views/clients.js
autonomous: true
requirements: [MODAL-01, MODAL-02, MODAL-07]
must_haves:
  truths:
    - "The Clients list 'Add Client' button opens app/client-modal.js's openClientAddModal() — the inline #addClientForm and its toggleAddClientForm are gone"
    - "The per-row 'Edit' button opens openClientEditModal(clientId, record) pre-filled from clientsData — the inline-row <input> edit branch in renderClientsTable is gone"
    - "The removed window functions (toggleAddClientForm/addClient/editClient/cancelEdit/saveEdit) no longer exist in clients.js (attach + destroy blocks updated); the new launcher window fns are registered and cleaned up"
    - "Client Add/Edit gating is unchanged (still canEditTab('clients')) — D-05's canEngagements swap applies only to Project/Service, not Client"
  artifacts:
    - path: "app/views/clients.js"
      provides: "Client list wired to the modal; inline add-form + inline-row edit removed"
  key_links:
    - from: "app/views/clients.js"
      to: "app/client-modal.js"
      via: "import + window-fn wrappers for Add/Edit buttons"
      pattern: "from '\\.\\./client-modal.js'"
    - from: "Add/Edit buttons"
      to: "openClientAddModal / openClientEditModal"
      via: "onclick window fns"
      pattern: "openClient(Add|Edit)Modal"
---

<objective>
Wire `app/views/clients.js` to the new `app/client-modal.js` (Plan 01) and remove the retired inline UI (D-07). Delete the inline `#addClientForm` block, the inline-row `<input>` edit branch in `renderClientsTable`, and the now-dead window functions (`toggleAddClientForm`, `addClient`, `editClient`, `cancelEdit`, `saveEdit`). Point the "Add Client" button at `openClientAddModal()` and each row's "Edit" button at `openClientEditModal(clientId, record)`.

Client gating is UNCHANGED: the Add and Edit controls stay behind the existing `canEditTab('clients')` check (D-05's `canEngagements` swap is Project/Service-only). Detail-page / detail-modal behavior (showClientDetail) is untouched.

Purpose: retire the Client inline add-form and inline-row edit in favor of the shared modal.
Output: modified `app/views/clients.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-entity-add-edit-modals/110-CONTEXT.md
@.planning/phases/110-entity-add-edit-modals/110-PATTERNS.md
@.planning/phases/110-entity-add-edit-modals/110-01-SUMMARY.md

<interfaces>
<!-- Exact regions to change in clients.js. Cited file:line. -->

- Add button: clients.js:63 `onclick="window.toggleAddClientForm()"` → the new Add launcher.
- Inline add-form block to DELETE: clients.js:68-94 (`<div id="addClientForm" class="add-form" ...>` ... 5 fields ... form-actions).
- Inline-row edit branch to DELETE: clients.js:445-458 (`if (editingClient === client.id) { return \`<tr>...<input id="edit-code">...\`; }`) — rows must ALWAYS render the read-only branch (clients.js:460-478). The `editingClient` module var + its resets can be removed.
- Row Edit button: clients.js:469 `onclick="window.editClient('${client.id}')"` → the new Edit launcher passing the row's record.
- Functions to DELETE (logic now lives in client-modal.js): toggleAddClientForm (497-518), addClient (520-572), editClient (574-583), cancelEdit (585-588), saveEdit (590-644).
- attachWindowFunctions: clients.js:27-38 (drop toggleAddClientForm/addClient/editClient/cancelEdit/saveEdit; add the new launchers).
- destroy delete block: clients.js:167-177 (mirror the same drop/add).

client-modal.js (Plan 01) exports: `openClientAddModal()`, `openClientEditModal(clientId, record)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove the inline #addClientForm block, the inline-row edit branch, and the dead window functions</name>
  <files>app/views/clients.js</files>
  <read_first>
    - app/views/clients.js lines 27-38 (attachWindowFunctions), 42-95 (render header + #addClientForm), 440-495 (renderClientsTable both branches), 497-644 (toggleAddClientForm/addClient/editClient/cancelEdit/saveEdit), 160-186 (destroy delete block)
    - .planning/phases/110-entity-add-edit-modals/110-CONTEXT.md D-07 (Client owns a dedicated modal replacing BOTH inline add and inline-row edit)
  </read_first>
  <action>
    In `render()`, delete the entire `<div id="addClientForm" class="add-form" ...>` block (clients.js:68-94). Leave the `.card`/`suppliers-header`/filter-bar structure intact.

    In `renderClientsTable()`, delete the `if (editingClient === client.id) { ... }` edit-branch (clients.js:445-458) so `pageItems.map(...)` always returns the read-only `<tr>` (the current else-branch at 460-478). Remove the now-unused `editingClient` module variable and every reference/reset to it (declaration at :17, cancelEdit/saveEdit/editClient usages, and the reset in destroy at :164).

    Delete the functions `toggleAddClientForm`, `addClient`, `editClient`, `cancelEdit`, and `saveEdit` entirely. Keep `deleteClient`, `showClientDetail`, `closeClientDetailModal`, pagination/sort/filter helpers, and `isValidEmail` only if still referenced elsewhere in clients.js — if `isValidEmail` becomes unused after the deletions, remove it too (it now lives in client-modal.js). Keep the `import ... escapeHTML ...` and `addDoc/updateDoc/doc` imports only if still used by remaining code (e.g. deleteClient); drop any import that becomes unused.

    Remove the five deleted names from `attachWindowFunctions()` (clients.js:28-32) and from the `destroy()` delete block (clients.js:167-171). Do not remove the other window fns.
  </action>
  <acceptance_criteria>
    - `grep -c "addClientForm" app/views/clients.js` returns 0 (inline add-form gone).
    - `grep -c "editingClient" app/views/clients.js` returns 0 (inline-row edit state gone).
    - `grep -nE "function (toggleAddClientForm|addClient|editClient|cancelEdit|saveEdit)\b" app/views/clients.js` returns 0 matches (functions removed).
    - `grep -nE "window\.(toggleAddClientForm|addClient|editClient|cancelEdit|saveEdit) =" app/views/clients.js` returns 0 matches (attach cleaned); `grep -nE "delete window\.(toggleAddClientForm|addClient|editClient|cancelEdit|saveEdit)" app/views/clients.js` returns 0 matches (destroy cleaned).
    - `grep -n "id=\"edit-code\"\|id=\"edit-company\"" app/views/clients.js` returns 0 (inline-row inputs gone).
    - clients.js still loads without a ReferenceError (no dangling reference to a removed symbol); `grep -c "showClientDetail" app/views/clients.js` still matches (detail path preserved).
  </acceptance_criteria>
  <verify>Grep checks pass; clients.js loads with no console error; the clients table renders read-only rows only.</verify>
  <done>The inline add-form, inline-row edit branch, and the five dead window functions are removed; attach/destroy blocks no longer reference them.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire Add + Edit buttons to client-modal.js and register the launcher window fns</name>
  <files>app/views/clients.js</files>
  <read_first>
    - app/views/clients.js lines 42-65 (render header + Add button + showEditControls gate), 460-478 (read-only row + Edit button), 27-38 + 167-177 (attach/destroy)
    - .planning/phases/110-entity-add-edit-modals/110-01-SUMMARY.md (openClientAddModal / openClientEditModal signatures)
    - app/client-modal.js (exported openClientAddModal, openClientEditModal)
  </read_first>
  <action>
    Add `import { openClientAddModal, openClientEditModal } from '../client-modal.js';` at the top of clients.js.

    Add two thin window-fn wrappers (keeps the onclick-on-window convention) inside the module: `function openClientAdd() { if (window.canEditTab?.('clients') === false) { showToast('You do not have permission to edit clients', 'error'); return; } openClientAddModal(); }` and `function openClientEdit(clientId) { if (window.canEditTab?.('clients') === false) { showToast('You do not have permission to edit clients', 'error'); return; } const record = clientsData.find(c => c.id === clientId) || null; openClientEditModal(clientId, record); }`. (Passing the record from the live clientsData array avoids a redundant Firestore read; the modal falls back to getDoc if null.)

    Change the Add button (clients.js:63) `onclick` to `window.openClientAdd()`. Change the row Edit button (clients.js:469) `onclick` to `window.openClientEdit('${client.id}')`. Keep the Delete button and the `showEditControls`/`canEditTab('clients')` gating exactly as-is (Client gating unchanged — D-05 is Project/Service only).

    Register `window.openClientAdd = openClientAdd; window.openClientEdit = openClientEdit;` in `attachWindowFunctions()` and add matching `delete window.openClientAdd; delete window.openClientEdit;` in `destroy()`.
  </action>
  <acceptance_criteria>
    - `grep -n "from '../client-modal.js'" app/views/clients.js` matches and imports openClientAddModal + openClientEditModal.
    - `grep -n "onclick=\"window.openClientAdd()\"" app/views/clients.js` matches (Add button wired); `grep -n "window.openClientEdit('" app/views/clients.js` matches (Edit button wired).
    - `grep -nE "window\.(openClientAdd|openClientEdit) =" app/views/clients.js` returns 2 (attach) and `grep -nE "delete window\.(openClientAdd|openClientEdit)" app/views/clients.js` returns 2 (destroy).
    - `grep -n "canEditTab?.('clients')" app/views/clients.js` still matches inside the two wrappers and the render gate (Client gating unchanged).
    - Console: clicking Add Client opens the create modal; clicking a row's Edit opens the modal pre-filled with that client's Company Name equal to the row's company; a role with canEditTab('clients')===false sees no Add/Edit controls.
  </acceptance_criteria>
  <verify>Grep checks pass; Add opens an empty modal, Edit opens a pre-filled modal, and the client gate still hides controls for view-only roles.</verify>
  <done>Add and Edit buttons launch the shared Client modal via window-fn wrappers; the launchers are registered/cleaned up and Client gating is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (authenticated user) → client-modal.js | Row records pass into the edit modal; the Add/Edit gate decides who can invoke writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-04-AUTH | E (Elevation of Privilege) | Add/Edit launcher wrappers | mitigate | Both wrappers re-check `canEditTab('clients') === false` before opening, matching the deleted functions' guards; the render gate still hides controls for view-only roles. No widening — Client gating is deliberately unchanged (D-05 is Project/Service only). |
| T-110-04-XSS | I (stored XSS) | row Edit onclick + record passing | accept | Only `client.id` is interpolated into the onclick (a Firestore id, not free text); the record is passed as a JS object reference, not string-built HTML. The read-only row cells already escapeHTML company/contact/etc (unchanged). |
| T-110-04-SC | T (supply chain) | dependencies | accept | No package installs in this plan. |
</threat_model>

<verification>
- Add Client opens the modal (openClientAddModal); row Edit opens the pre-filled modal (openClientEditModal) — MODAL-01/MODAL-02.
- The inline #addClientForm and inline-row edit are fully removed; no dead window fns remain.
- Client Add/Edit gating is unchanged (canEditTab('clients')).
- clients.js is the only file modified; detail-page files untouched (MODAL-07).
</verification>

<success_criteria>
- Adding and editing a client both go through the shared `.modal` (MODAL-01, MODAL-02, MODAL-07).
- No inline add-form / inline-row edit remains in clients.js.
</success_criteria>

<output>
Create `.planning/phases/110-entity-add-edit-modals/110-04-SUMMARY.md` when done — record which functions/blocks were removed, the wrapper names, and confirmation that Client gating stayed on canEditTab('clients').
</output>
