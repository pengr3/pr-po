---
context: phase
phase: 110-entity-add-edit-modals
task: null
total_tasks: 12
status: planning-complete
last_updated: 2026-07-15T10:27:35.043Z
---

# Phase 110: Entity Add/Edit Modals — Paused at Planning Complete

**Clean phase-boundary pause.** Planning is fully done, verified, and committed
(`475f32a`). Working tree is clean. Nothing is mid-task. The next action is
execution.

<current_state>
Phase 110 is PLANNED and ready to execute — 6 plans in 2 waves, all autonomous,
0/6 executed. Pattern-map + plan + verify all complete; requirements (MODAL-01…07,
7/7) and decision coverage (D-01…D-07, 7/7) gates passed. Committed as `475f32a`.
</current_state>

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Wrong personnel-sync fn | `syncPersonnelToAssignments` (writes `assigned_project_codes`) and `syncServicePersonnelToAssignments` (writes `assigned_service_codes`) are NOT interchangeable. Calling the project fn on a service edit silently corrupts prod user data (no staging). | advisory (already fixed in plans) | Plan 03 branches the sync by `editingKind`; keep that branching — its acceptance criteria assert it. |
| Touching detail-page auto-save | MODAL-07 forbids modifying `project-detail.js` / `service-detail.js` inline auto-save. | advisory | Plans exclude these from `files_modified` + have grep acceptance checks; do not add them. |
| Decision citations misplaced (planning only) | GSD step-13a gate scans only plan frontmatter `must_haves` (+ `## must_haves/truths/tasks/objective` headings), NOT `<acceptance_criteria>`/`<objective>` XML tags; CONTEXT.md decisions need `**D-NN:**` format. | advisory (planning) | See memory `feedback_decision_coverage_gate_mechanics`. Not relevant to execution. |

<completed_work>

This session (all committed in `475f32a`):
- gsd-pattern-mapper → `110-PATTERNS.md` (7/7 analogs; flagged the `submitNewEngagement()` auto-close gap + Service type-lock ambiguity)
- gsd-planner → 6 PLAN.md files (2 waves, 12 tasks); resolved both pattern-mapper flags; caught the `UNIFIED_STATUS_OPTIONS` Draft asymmetry
- gsd-plan-checker → PASSED on revision 1 (fixed 1 BLOCKER: service-edit personnel sync corruption; + 2 warnings: missing `<files>`, Plan 02 threat-model D-05 widen)
- Requirements + decision coverage gates passed; STATE.md + ROADMAP annotated
- Fixed CONTEXT.md D-05 header format so the Add-gating decision is gate-trackable
</completed_work>

<remaining_work>

- **Execute Wave 1** (parallel): `110-01` client-modal.js · `110-02` engagement-create.js locked-type launcher + auto-close hook · `110-03` entity-edit-modal.js
- **Execute Wave 2** (parallel, after Wave 1): `110-04` clients.js · `110-05` projects.js · `110-06` services.js — wire to modals, remove inline forms, D-05 gate swap
- **Manual browser UAT** against production Firebase (no automated tests — user's established pattern)
</remaining_work>

<decisions_made>

- Skipped research + UI-SPEC (config research:false; CONTEXT.md already locks D-01…D-07; reuses existing components)
- Plan 03 personnel sync branches by `editingKind` (blocker fix)
- Shared Draft-inclusive `ENGAGEMENT_STATUS_OPTIONS` in entity-edit-modal.js
- Reformatted CONTEXT.md `**D-05 — Add gating:**` → `**D-05:**` (meaning-preserving; makes it trackable)
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/110-entity-add-edit-modals/110-CONTEXT.md` — locked decisions D-01…D-07 (authoritative over literal ROADMAP wording)
2. `.planning/phases/110-entity-add-edit-modals/110-PATTERNS.md` — per-file analogs with file:line excerpts
3. `.planning/phases/110-entity-add-edit-modals/110-0{1..6}-PLAN.md` — the executable plans
4. `Skill("spike-findings-pr-po")` — project patterns/landmines (auto-loads during execution)

## Infrastructure State
- No servers/watchers running. Background grep task `bze9itq4x` (a planning investigation) completed and can be ignored.
- Deployment is Netlify auto-deploy on push; Firebase is production (no staging).

<context>
Phase 110 converts Client/Project/Service Add + list-Edit from inline `.add-form`
toggles to modals. Key shape: Client gets its own dedicated Add+Edit modal (D-07);
Project/Service **Add** reuses the existing New Engagement modal with a locked type
(D-04); Project/Service **Edit** is a new dedicated pre-filled modal writing via
`updateDoc` (D-06). Add buttons gate to the 3 engagement roles via `canEngagements`
(D-05). Detail-page inline auto-save is untouched (MODAL-07).
</context>

<next_action>
Start with: `/clear`, then `/gsd:execute-phase 110`. Wave 1 (110-01/02/03) runs in
parallel; Wave 2 (110-04/05/06) after Wave 1 completes.
</next_action>
