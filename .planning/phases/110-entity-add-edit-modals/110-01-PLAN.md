---
phase: 110-entity-add-edit-modals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/client-modal.js
autonomous: true
requirements: [MODAL-01, MODAL-02, MODAL-07]
must_haves:
  truths:
    - "A new app/client-modal.js exports openClientAddModal() and openClientEditModal(clientId, record) — one module, create|edit mode, modeled on proposal-modal.js (D-01 modal foundation; D-07 Client gets its own dedicated Add+Edit modal replacing both #addClientForm and the inline-row edit)"
    - "Opening in edit mode pre-fills all 5 fields (Client Code, Company Name, Contact Person, Phone, Email) from the record, every value passed through escapeHTML"
    - "The modal free-closes on backdrop click, the ✕ button, and Esc; a successful save auto-closes and shows a success toast (D-03 free close, no confirm-if-dirty guard)"
    - "Validation is toast-based only (no inline field-level error UI) and matches the exact checks currently in clients.js addClient/saveEdit (D-02 keep existing toast-based validation)"
  artifacts:
    - path: "app/client-modal.js"
      provides: "Client Add/Edit modal module (open/show/validate/save/close) built on the .modal + display:flex + DOM-injection convention"
      exports: ["openClientAddModal", "openClientEditModal"]
  key_links:
    - from: "app/client-modal.js"
      to: "firestore clients collection"
      via: "addDoc on create, updateDoc on edit"
      pattern: "(addDoc\\(collection\\(db, ?'clients'\\)|updateDoc\\(doc\\(db, ?'clients')"
    - from: "app/client-modal.js"
      to: "app/utils.js"
      via: "escapeHTML/showToast/showLoading import"
      pattern: "import \\{[^}]*escapeHTML[^}]*\\} from './utils.js'"
---

<objective>
Create `app/client-modal.js` — a dedicated Client Add/Edit modal module (D-07). One module, `create | edit` mode, modeled on `app/proposal-modal.js`'s create/edit shell (mode flag, `.modal` + inline `style="display:flex"`, `document.body.insertAdjacentHTML` mount, `.remove()` on close, window-fn register/delete per open/close). It REPLACES both the inline `#addClientForm` and the inline-row `<input>` edit currently in `clients.js` (the removal + wiring happens in Plan 04, which imports this module's exports).

The 5 fields and validation are unchanged (D-07/D-02): Client Code, Company Name, Contact Person, Phone, Email; required = code/company/contact; at least one of phone/email; valid email format; duplicate client_code blocked. No inline field-level error UI — toast validation only (D-02). Free close (D-03): backdrop / ✕ / Esc all close immediately, no confirm-if-dirty; successful save auto-closes + success toast.

Purpose: give Plan 04 a drop-in modal so the Client list can retire its inline add-form and inline-row edit.
Output: `app/client-modal.js` exporting `openClientAddModal` and `openClientEditModal`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-entity-add-edit-modals/110-CONTEXT.md
@.planning/phases/110-entity-add-edit-modals/110-PATTERNS.md

<interfaces>
<!-- The chosen analog + the exact validation/write logic to reuse. Cited file:line so the executor transcribes, not re-derives. -->

Analog shell — app/proposal-modal.js:692-842 (openCreateProposalModal / openEditProposalModal / showCreateModal / closeCreateProposalModal): mode flag trio, mode-branched HTML with escapeHTML pre-fill, window-fn registration inline right after the HTML string, close removes DOM + deletes window fns + resets state + fires optional onClose callback.

Validation + write to reuse VERBATIM — app/views/clients.js:527-562 (addClient) and :597-633 (saveEdit):
```js
const client_code = ...value.trim().toUpperCase();
const company_name = ...value.trim();
const contact_person = ...value.trim();
const phone = ...value.trim();
const email = ...value.trim();
if (!client_code || !company_name || !contact_person) { showToast('Please fill in Client Code, Company Name, and Contact Person', 'error'); return; }
if (!phone && !email) { showToast('Provide at least one of Phone or Email', 'error'); return; }
if (email && !isValidEmail(email)) { showToast('Email is not a valid address', 'error'); return; }
// duplicate: edit mode excludes self (c.id !== editingId)
// create: await addDoc(collection(db,'clients'), { client_code, company_name, contact_person, phone, email, created_at: new Date().toISOString() });
// edit:   await updateDoc(doc(db,'clients',editingId), { client_code, company_name, contact_person, phone, email, updated_at: new Date().toISOString() });
```

isValidEmail — app/views/clients.js:11-13 (private, NOT exported): `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`. Duplicate this one-liner into client-modal.js (do not add an export to clients.js this plan).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create app/client-modal.js shell — open/show/close lifecycle with escapeHTML pre-fill and free close</name>
  <files>app/client-modal.js</files>
  <read_first>
    - app/proposal-modal.js lines 692-842 — the create/edit shell to copy structurally (mode flag, showCreateModal HTML + inline window-fn registration, closeCreateProposalModal DOM-remove + window-fn delete + state reset + onClose callback)
    - app/views/clients.js lines 68-94 (the #addClientForm 5-field block being replaced — field labels/placeholders to mirror), lines 445-458 (inline-row edit values being replaced)
    - app/views/home.js lines 1285-1313 — the backdrop-click-to-close pattern (`overlay.addEventListener('click', e => { if (e.target === overlay) close(); })`)
    - app/views/finance.js lines 3206-3238 — AbortController + keydown/Escape precedent for Esc-to-close
    - styles/components.css lines 764-857 — the .modal / .modal-content / .modal-header / .modal-close / .modal-body / .modal-footer contract (use inline style="display:flex", NOT the .active toggle — per D-01)
  </read_first>
  <action>
    Create `app/client-modal.js`. Import from './firebase.js': `db, collection, doc, getDoc, getDocs, addDoc, updateDoc, query, where`. Import from './utils.js': `showLoading, showToast, escapeHTML`. Add the private `isValidEmail(value)` one-liner (regex `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`) duplicated from clients.js:11-13.

    Module state: `let clientModalMode = 'create';` (`'create' | 'edit'`), `let clientModalEditingId = null;`, `let _clientModalAbort = null;` (AbortController for the Esc listener).

    Export `async function openClientAddModal()`: set `clientModalMode='create'`, `clientModalEditingId=null`, call `showClientModal(null)`.

    Export `async function openClientEditModal(clientId, record = null)`: resolve the record — if `record` is passed, use it; else `const snap = await getDoc(doc(db,'clients',clientId)); record = snap.exists() ? { id: snap.id, ...snap.data() } : null;`. If null, `showToast('Client not found.', 'error')` and return. Set `clientModalMode='edit'`, `clientModalEditingId=clientId`, call `showClientModal(record)`.

    `function showClientModal(existing)`: remove any prior `#clientModal` element first. Compute `isEdit = clientModalMode==='edit'`, `heading = isEdit ? 'Edit Client' : 'Add Client'`, `ctaLabel = isEdit ? 'Save Changes' : 'Add Client'`. Build 5 fields with ids `clientModalCode`, `clientModalCompany`, `clientModalContact`, `clientModalPhone`, `clientModalEmail`; pre-fill each with `escapeHTML(existing?.<field> || '')` in edit mode (Code uppercase, keep `text-transform:uppercase` on the code input). Mount via `document.body.insertAdjacentHTML('beforeend', ...)` a `<div id="clientModal" class="modal" style="display:flex;z-index:1001;">` with `.modal-content` (max-width:520px;margin:auto), `.modal-header` (heading + `<button class="modal-close" onclick="window.closeClientModal()">&times;</button>`), `.modal-body`, `.modal-footer` (Cancel → `window.closeClientModal()`, primary → `window.saveClient()` labelled `${ctaLabel}`). Immediately after the HTML string, register `window.closeClientModal = closeClientModal;` and `window.saveClient = saveClient;`. After insertion, attach backdrop close: look up `#clientModal` and `addEventListener('click', e => { if (e.target.id === 'clientModal') closeClientModal(); })`. Wire Esc: create `_clientModalAbort = new AbortController()` and `document.addEventListener('keydown', e => { if (e.key === 'Escape') closeClientModal(); }, { signal: _clientModalAbort.signal })`. Focus `#clientModalCode`.

    `function closeClientModal()`: `document.getElementById('clientModal')?.remove()`; `_clientModalAbort?.abort(); _clientModalAbort = null;`; `delete window.closeClientModal; delete window.saveClient;`; reset `clientModalMode='create'; clientModalEditingId=null;`. Leave `saveClient` as a stub that Task 2 fills (register it now so window wiring is complete; a no-op returning is fine until Task 2).
  </action>
  <acceptance_criteria>
    - `grep -c "export async function open" app/client-modal.js` returns 2 (openClientAddModal, openClientEditModal).
    - `grep -n "class=\"modal\" style=\"display:flex" app/client-modal.js` matches (D-01 convention — inline display:flex, not .active).
    - `grep -nE "e\.key === 'Escape'|new AbortController\(\)" app/client-modal.js` matches (Esc free-close, D-03).
    - `grep -n "e.target.id === 'clientModal'" app/client-modal.js` matches (backdrop free-close, D-03).
    - `grep -c "escapeHTML" app/client-modal.js` returns at least 5 (all 5 pre-fill values escaped).
    - `grep -n "document.body.insertAdjacentHTML('beforeend'" app/client-modal.js` matches; `grep -n "getElementById('clientModal')?.remove()" app/client-modal.js` matches in closeClientModal (DOM add/remove convention).
    - Module loads in DevTools with no console error; `openClientAddModal()` then Esc removes `#clientModal` from the DOM.
  </acceptance_criteria>
  <verify>Grep checks above pass; module imports cleanly; open→Esc/backdrop/✕ each removes the modal element and leaves no `window.closeClientModal`/`window.saveClient` behind.</verify>
  <done>app/client-modal.js exists with a working open/show/close lifecycle, escapeHTML on all pre-fill, and free close via backdrop/✕/Esc.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement saveClient — toast validation + create/edit write branch + success auto-close</name>
  <files>app/client-modal.js</files>
  <read_first>
    - app/views/clients.js lines 520-644 (addClient + saveEdit — the exact validation order, duplicate-code check, addDoc/updateDoc field sets to reuse verbatim)
    - app/proposal-modal.js lines 844-955 (saveProposal — the `if (mode==='edit' && editingId) updateDoc else addDoc` branch shape + showLoading/finally + catch→showToast error pattern; do NOT copy its inline `<div id=...Error>` per-field error UI — D-02 requires plain showToast)
  </read_first>
  <action>
    Add `async function saveClient()` to client-modal.js (replacing the Task 1 stub). Read the 5 field values by id (`clientModalCode` → `.trim().toUpperCase()`, others `.trim()`). Run validation in this exact order using `showToast(..., 'error'); return;` on failure (D-02, no inline errors): (1) required — if `!client_code || !company_name || !contact_person` → 'Please fill in Client Code, Company Name, and Contact Person'; (2) contact — if `!phone && !email` → 'Provide at least one of Phone or Email'; (3) email format — if `email && !isValidEmail(email)` → 'Email is not a valid address'; (4) duplicate code — run `getDocs(query(collection(db,'clients'), where('client_code','==', client_code)))`; if any returned doc has `doc.id !== clientModalEditingId` (edit excludes self; create matches any) → `Client code "${client_code}" already exists`.

    Then `showLoading(true)` inside a try/catch/finally. Branch on mode: if `clientModalMode === 'edit' && clientModalEditingId` → `await updateDoc(doc(db,'clients',clientModalEditingId), { client_code, company_name, contact_person, phone, email, updated_at: new Date().toISOString() })` then `showToast('Client updated successfully', 'success')`; else → `await addDoc(collection(db,'clients'), { client_code, company_name, contact_person, phone, email, created_at: new Date().toISOString() })` then `showToast(\`Client "${company_name}" added successfully!\`, 'success')`. On success call `closeClientModal()` (D-03 auto-close). In `catch (err)` log `[ClientModal]` + `showToast(err?.message || 'Failed to save client', 'error')`. In `finally` `showLoading(false)`.

    Do NOT add any permission gate inside the modal — Plan 04 keeps the `canEditTab('clients')` gate on the Add/Edit buttons (Client gating is unchanged by D-05; D-05 only affects Project/Service).
  </action>
  <acceptance_criteria>
    - `grep -nE "clientModalMode === 'edit'" app/client-modal.js` matches (mode-branched write).
    - `grep -n "addDoc(collection(db, 'clients')" app/client-modal.js` and `grep -n "updateDoc(doc(db, 'clients'" app/client-modal.js` each match (both write paths present).
    - `grep -c "showToast(" app/client-modal.js` returns at least 6 (4 validation + create + update success toasts + error) and `grep -c "id=\"...Error\"\|Error\"" app/client-modal.js` shows NO per-field inline error divs were introduced (D-02).
    - `grep -n "where('client_code', '==', client_code)" app/client-modal.js` matches (duplicate check as a targeted query) and the edit-mode self-exclusion `doc.id !== clientModalEditingId` is present.
    - `grep -n "closeClientModal()" app/client-modal.js` appears inside saveClient's success path (auto-close, D-03).
    - Console: create a client via the modal → new doc appears in `clients`; edit the same client's Company Name → `updateDoc` fires and modal auto-closes; entering a duplicate code shows the toast and does NOT write.
  </acceptance_criteria>
  <verify>Grep checks pass; a create + an edit + a duplicate-code rejection each behave correctly in the browser console; no inline error-div UI exists.</verify>
  <done>saveClient validates via toast only, writes via addDoc (create) or updateDoc (edit) to the clients collection, blocks duplicate codes, and auto-closes on success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (authenticated user) → Firestore `clients` | User-entered strings (code/company/contact/phone/email) cross into stored docs and are re-rendered into modal HTML on edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01-XSS | I (Information Disclosure via stored XSS) | showClientModal pre-fill HTML | mitigate | Every pre-fill value wrapped in `escapeHTML(existing?.field || '')` before interpolation into the modal HTML string (established pattern; ASVS L1 V5). Acceptance criterion asserts ≥5 escapeHTML calls. |
| T-110-01-AUTH | E (Elevation of Privilege) | saveClient write path | accept | Modal adds no new privilege — it writes to `clients` via the same addDoc/updateDoc paths clients.js already uses; the Add/Edit buttons remain gated by `canEditTab('clients')` in Plan 04, and Firestore `clients` rules are unchanged. No widening of who can write. |
| T-110-01-SC | T (Tampering / supply chain) | dependencies | accept | No package installs in this plan (static ES-module SPA, no build step). RESEARCH Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- app/client-modal.js loads with no console error and exports openClientAddModal + openClientEditModal.
- Free close works via backdrop, ✕, and Esc (D-03); successful save auto-closes with a success toast.
- All 5 pre-fill values pass through escapeHTML (T-110-01-XSS mitigated).
- Validation is toast-only, matching clients.js exactly; no inline field-error UI (D-02).
- No file other than app/client-modal.js is modified (project-detail.js / service-detail.js untouched — MODAL-07).
</verification>

<success_criteria>
- openClientAddModal() opens an empty create modal; openClientEditModal(id, record) opens a pre-filled edit modal.
- Create writes via addDoc, edit writes via updateDoc, duplicate codes blocked, success auto-closes + toasts (MODAL-01, MODAL-02).
- Modal uses the shared `.modal` CSS contract with backdrop + Esc + validation (MODAL-07).
</success_criteria>

<output>
Create `.planning/phases/110-entity-add-edit-modals/110-01-SUMMARY.md` when done — record the exported functions, field ids used, and confirmation that validation matches clients.js and that only app/client-modal.js was created.
</output>
