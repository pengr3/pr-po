---
phase: 99.3-collectibles-filter-bar-urgency-sort
plan: 02
subsystem: ui
tags: [finance, collectibles, accordion, group-by, portfolio-view, vanilla-js]

# Dependency graph
requires:
  - phase: 99.3-01
    provides: COLL_URG_ORDER tier map, rebuilt getDisplayedCollectibles (filtered + urgency-sorted), canonical near-due definition, filter-bar markup
provides:
  - "Group by Project" accordion toggle on the Collectibles tab (flat default ↔ grouped portfolio)
  - Grouped renderer: collapsible header per project/service (worst-urgency border, aggregate progress, urgency summary, group last payment) expanding to tranche child rows
  - Groups ordered by worst urgency tier (same COLL_URG_ORDER as the flat sort)
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Grouped accordion reuses the flat 5-column structure + coll-urgency-${tier} row-class border (no separate urg-bar cell, no second <thead>)"
    - "Intentional scorecard divergence: grouped view aggregates the PRE-hide portfolio set (fully-paid included); flat view stays on the post-hide visible set (Fix-1, documented)"

key-files:
  created: []
  modified:
    - app/views/finance.js
    - styles/views.css

key-decisions:
  - "Grouped header/child rows map to the real 5-column finance table (urgency border via coll-urgency-${tier} row class), NOT the spike's 6-col urg-bar layout — so the existing <thead> serves both views"
  - "Grouped near-due count uses getCollectibleUrgency tier === 'near-due' (Fix-2), matching the Plan-01 Near-due filter exactly; the spike's looser overdue-inclusive test was NOT ported"
  - "Grouped scorecard intentionally uses renderCollectiblesScorecard(allDisplayed) (portfolio totals, fully-paid included) — Fix-1; flat path's post-hide scorecard left untouched"
  - "Group onclick key backslash-escapes for the JS string literal so it round-trips to the raw key used by collGroupExpanded/collGroupKey (codes are safe alphanumerics)"
  - "Grouped child rows expose a single primary action (Record Payment / View History); the flat view's inline hidden history row is not reproduced in grouped view"

patterns-established:
  - "Pattern: worst-tier group ordering via COLL_URG_ORDER[collGroupWorstTier(items)] — reuses the Plan-01 tier map, no duplicated thresholds (D-08b)"

requirements-completed: [spike-027c]

# Metrics
duration: ~15min
completed: 2026-06-07
---

# Phase 99.3 Plan 02: Group-by-Project Accordion Summary

**The Collectibles tab gains a "Group by Project" toggle that switches the flat 5-column list into a portfolio accordion — one collapsible header per project/service ordered worst-urgency-first, showing aggregate collection progress, an urgency summary, and the group's most-recent payment, expanding to its individual tranche rows; flat view (default) keeps Show-N-completed + pagination unchanged.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-06-07
- **Tasks:** 2
- **Files modified:** 2 (app/views/finance.js, styles/views.css)

## Accomplishments
- Added `collGroupBy` / `collGroupExpanded` state and the grouped renderer (`renderCollectiblesGroupedRows` + `groupCollectiblesByProject` / `collGroupWorstTier` / `collGroupKey` / `collGroupName`), all keyed off the shared `getCollectibleUrgency` / `deriveCollectibleStatus` helpers (D-08b).
- Branched `renderCollectiblesTable` on `collGroupBy`: grouped path scorecards the pre-hide portfolio set (Fix-1), sorts groups by worst tier, hides pagination + Show-N-completed; flat path below untouched.
- Added the "Group by Project" toggle button + `toggleCollGroupBy` / `toggleCollGroup` handlers, registered on `window`, torn down in `destroy()`; `collGroupBy`/`collGroupExpanded` reset in `destroy()` (MD-01).
- Appended grouped-view CSS (section 8) to the COLLECTIBLES REVAMP β block.

## Task Commits

Each task was committed atomically:

1. **Task 1: Group state + grouped renderer + render-branch wiring** — `529ea74` (feat)
2. **Task 2: Toggle button + handlers + window reg/teardown + grouped CSS** — `e79071d` (feat)

## Files Created/Modified
- `app/views/finance.js` — group state, group helpers + `renderCollectiblesGroupedRows`, `if (collGroupBy)` branch in `renderCollectiblesTable`, toggle button markup, `toggleCollGroupBy`/`toggleCollGroup` + window reg/teardown, destroy resets.
- `styles/views.css` — section 8 (grouped accordion: `.coll-group-header` / `.coll-group-child` / `.coll-group-count-chip`).

## Decisions Made
- Mapped the grouped rows onto the real 5-column table (urgency border via row class) instead of the spike's 6-col `urg-bar` layout, so the existing `<thead>` serves both flat and grouped views without a second header.
- Near-due count pinned to the canonical tier (Fix-2) so a group's "near due" count and the Plan-01 Near-due filter always agree.
- Grouped scorecard intentionally diverges to portfolio totals (Fix-1) — documented inline so the Total Invoiced/Collected/Outstanding shift on toggle reads as design, not a bug.

## Deviations from Plan

### Notes (no scope change)

**1. [Comment reword] removed literal `d<=0` from a JSDoc**
- **Found during:** Task 1 verification
- **Detail:** The acceptance gate `grep -c "d<=0"` must be 0, but an explanatory JSDoc line ("NOT the spike's looser d<=0 && -d<=7") matched it. Reworded to "…looser overdue-inclusive test" — the actual near-due logic already used the canonical tier. Pure documentation; no logic change.

**2. [Simplification] grouped child rows show one primary action**
- The flat view renders Record Payment + History plus a hidden inline-history `<tr>`. Grouped child rows expose only the primary action (Record Payment / View History) and omit the hidden history row, matching the spike's single-button child. "View History" in grouped view is a graceful no-op (its target row exists only in flat view). Recorded for UAT awareness.

---

**Total deviations:** 0 functional. 2 documentation/simplification notes. No scope creep.

## Issues Encountered
None — both tasks passed `node --check` and every acceptance grep gate.

## User Setup Required
None — display/UX only. NO Firestore schema/rules change.

## Next Phase Readiness
- Phase 99.3 code complete (both plans). Completes the spike-027c filter-bar + sort layer.
- **Browser UAT pending** (zero-build SPA): toggle Group by Project → groups render worst-urgency first; expand/collapse a group; toggle off restores flat Show-N-completed + pagination; remount starts flat + collapsed; confirm the intentional scorecard shift (flat = active list paid-excluded, grouped = portfolio paid-included) and that a group's "near due" count matches the Near-due filter.

---
*Phase: 99.3-collectibles-filter-bar-urgency-sort*
*Completed: 2026-06-07*
