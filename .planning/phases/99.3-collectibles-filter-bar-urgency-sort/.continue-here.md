---
context: phase
phase: 99.3-collectibles-filter-bar-urgency-sort
task: 5
total_tasks: 5
status: awaiting_uat
last_updated: 2026-06-07T00:00:00.000Z
---

# Phase 99.3 — Paused at the browser-UAT gate

> No blocking constraints were discovered through failure this session — execution was clean.
> (Per the pause-work workflow, the BLOCKING CONSTRAINTS section is omitted because none apply.)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree/subagent path on this machine | gsd-sdk is NOT installed and 2 stale **locked** agent worktrees (`.claude/worktrees/agent-a4222795…`, `…ab5541979…`) exist — spawning gsd-executor in isolated worktrees is unsafe here. | advisory | Execute inline-sequential on `v3.3` (orchestrator IS executor), per the Phase 98/99/99.1/99.2 precedent. Already done for 99.3. |
| Acceptance-grep literalism | `grep "statusPriority"` (Plan-01 T1 gate) and `grep "d<=0"` (Plan-02 T1 gate) can match unrelated code / explanatory comments. | advisory | Read the match in context before treating a non-zero count as a failure: `statusPriority` survives only in the RFP table sort (`deriveRFPStatus`); the `d<=0` count was a JSDoc phrase, since reworded. Both intents satisfied. |

<current_state>
Phase 99.3 (Collectibles Filter Bar + Urgency Sort — completes the spike-027c filter/sort
layer) is **fully implemented and committed** on branch `v3.3`. Both plans done; verification
verdict is `human_needed`. The ONLY remaining work is **human browser-UAT** of the 10 items in
`99.3-HUMAN-UAT.md`. The phase is intentionally NOT marked complete in ROADMAP/STATE until UAT
is approved.

8 commits this session (newest → oldest):
- `632a937` docs(phase-99.3): execution + VERIFICATION (human_needed) + HUMAN-UAT
- `42deaba` docs(99.3-02): SUMMARY
- `e79071d` feat(99.3-02): toggle + handlers + window reg/teardown + grouped CSS
- `529ea74` feat(99.3-02): group state + grouped renderer + render-branch wiring
- `d585e17` docs(99.3-01): SUMMARY
- `cb151dd` feat(99.3-01): filter-bar markup + Looker date popover + popover CSS
- `af2707f` feat(99.3-01): filter pipeline — search/dept-only/near-due/presets
- `b59f77c` feat(99.3-01): urgency-tier sort
</current_state>

<completed_work>

Plan 99.3-01 (urgency sort + filter-bar rebuild) — DONE:
- Task 1: COLL_URG_ORDER urgency-tier sort replaces the status-priority sort (`b59f77c`)
- Task 2: filter pipeline — collSearchQuery/collDuePreset state, computeCollDueRange, search/dept/Near-due blocks, populateCollProjectFilter removed (`af2707f`)
- Task 3: filter-bar markup (search + status w/ Near-due + dept-only + Looker popover), 6 date handlers, window reg/teardown, section-7 CSS (`cb151dd`)

Plan 99.3-02 (group-by-Project accordion) — DONE:
- Task 1: collGroupBy/collGroupExpanded state, group helpers + renderCollectiblesGroupedRows, `if (collGroupBy)` branch in renderCollectiblesTable (`529ea74`)
- Task 2: Group-by toggle button + toggleCollGroupBy/toggleCollGroup, window reg/teardown, destroy resets, section-8 grouped CSS (`e79071d`)

Verification + tracking — DONE:
- `99.3-VERIFICATION.md` (status human_needed, 6/6 SCs static-verified)
- `99.3-HUMAN-UAT.md` (10 pending items)
- ROADMAP plan boxes checked + Status note; STATE.md status/stopped_at/Current Position updated; completed_plans 129→131
</completed_work>

<remaining_work>

- **Human browser-UAT** — the 10 items in `99.3-HUMAN-UAT.md` (no code work remaining).
- On all-pass: flip `99.3-HUMAN-UAT.md` (status → passed, results → pass) and `99.3-VERIFICATION.md` (status → passed), then mark Phase 99.3 complete in ROADMAP (Status line) + STATE.md, and update progress.
- On any failure: `/gsd-plan-phase 99.3 --gaps` (VERIFICATION.md drives the gap plans) → `/gsd-execute-phase 99.3 --gaps-only`.
</remaining_work>

<decisions_made>

- Inline-sequential execution on `v3.3` (gsd-sdk unavailable + 2 stale locked worktrees + both plans share finance.js/views.css) — Phase 98/99/99.1/99.2 precedent.
- Near-due = `getCollectibleUrgency` tier `'near-due'` (≤7 days AND not overdue), narrower than the spike's looser test, so the filter and the grouped near-due count agree (Fix-2).
- Grouped scorecard intentionally aggregates the pre-hide portfolio set (fully-paid included); flat scorecard stays on the post-hide set (Fix-1) — documented divergence.
- Grouped rows reuse the real 5-column table + `coll-urgency-${tier}` row-class border (no separate urg-bar cell / second `<thead>`).
- Plan-01 `destroy()` MD-01 state resets landed in the Task-2 commit (removing the `collProjectFilter` declaration made its lingering destroy assignment a strict-mode ReferenceError); window-fn teardown stayed in Task 3. End state matches the plan.
</decisions_made>

<blockers>
- Phase completion is browser-UAT gated (zero-build SPA, no functional harness). Not a code blocker — needs a human to load the page and confirm the 10 items.
</blockers>

## Required Reading (in order)
1. `.planning/phases/99.3-collectibles-filter-bar-urgency-sort/99.3-VERIFICATION.md` — verdict + the 6 SC static-evidence table + the 10 human items.
2. `.planning/phases/99.3-collectibles-filter-bar-urgency-sort/99.3-HUMAN-UAT.md` — the exact 10 UAT items + expected results.
3. `99.3-01-SUMMARY.md` and `99.3-02-SUMMARY.md` — what shipped per plan + the 2 documentation-note deviations (no functional deviations).
4. `.planning/spikes/027c-collectibles-table-redesign/spike.html` — the validated design contract, if a UAT item is ambiguous.

## Infrastructure State
- No dev server running. To UAT: `python -m http.server 8000` → open the app → Finance → Collectibles.
- Branch `v3.3`; working tree clean for 99.3 (all committed). Pre-existing UNRELATED changes remain uncommitted (spikes CONVENTIONS/MANIFEST, Phase 86.9 debug files, untracked spike dirs 028–031, `debug-diag-86.9.js`) — do NOT fold them into 99.3.
- Standing carry-over debt (untouched by 99.3, NOT a 99.3 blocker): dev rules deploy + v3.3→main merge (Phase 87.4/99/99.1). 99.3 changed NO firestore rules.

<context>
The phase is done bar the human gate. I stopped at UAT rather than auto-advancing because
`_auto_chain_active` is false, no `--auto` flag was passed, and the session is interactive
(not an overnight batch — the user did not say "I'll check in the morning"). All four named
fixes are honored (Fix-1..Fix-4); node --check passes; static gates all green; inline code
review found no correctness issues.
</context>

<next_action>
Start with: ask the operator for the browser-UAT result on the 10 items in `99.3-HUMAN-UAT.md`
(run `python -m http.server 8000` → Finance → Collectibles). Do NOT re-run or re-edit code.
- If "approved" → set `99.3-HUMAN-UAT.md` results to pass + status passed, set `99.3-VERIFICATION.md`
  status passed, mark Phase 99.3 complete in ROADMAP + STATE, commit, then offer the next phase.
- If issues → `/gsd-plan-phase 99.3 --gaps`.
</next_action>
