---
status: passed
phase: 99.3-collectibles-filter-bar-urgency-sort
score: 6/6 must-haves static-verified + 10/10 browser UAT passed
plans_verified: [99.3-01, 99.3-02]
requirements_verified: [spike-027c]
verified: 2026-06-08
verifier: inline (gsd-sdk unavailable; zero-build SPA — node --check + grep static gates + human browser UAT)
---

# Phase 99.3: Collectibles Filter Bar + Urgency Sort — Verification

**Verdict: `passed`.** All 6 success criteria are statically satisfied in the codebase
(`node --check app/views/finance.js` passes; every acceptance grep gate met). Browser UAT
10/10 passed 2026-06-08 (urgency order, search, dept dropdown, date presets, fixed-range
stale-clear, near-due ≤7, group-by accordion, scorecard divergence confirmed by design, CSV,
remount reset).

## Goal

Finish the spike-027c Finance Collectibles redesign by rebuilding the filter bar and row ordering
omitted from Phase 99.2 — smart date ranges + free-text search + most-urgent-first ordering.
Display/UX only; no Firestore schema/rules change.

## Success Criteria — Static Verification

| # | Criterion | Evidence (static) | Status |
|---|-----------|-------------------|--------|
| 1 | Urgency sort replaces status-priority sort; scorecard/pagination/CSV operate on the set | `COLL_URG_ORDER` map keyed on `getCollectibleUrgency(c).tier` drives `displayed.sort` in `getDisplayedCollectibles()`; old collectibles `statusPriority` map removed; CSV/scorecard/pagination still call `getDisplayedCollectibles` | ✓ static |
| 2 | Looker date-range picker (7 presets + Fixed range) replaces raw inputs; filters due_date | `#collDatePickerBtn`/`#collDpPanel`/`#collDpFixed` markup; `computeCollDueRange` + 6 handlers; `data-mode` today/yesterday/this_month/last7/last30/''/fixed; From/To reuse `#collDueFromFilter`/`#collDueToFilter` | ✓ static |
| 3 | Free-text search over name/tranche/COLL id/code; resets to page 1 | `#collSearchInput` oninput→`filterCollectiblesTable` (sets `collCurrentPage=1`); search-filter block builds lowercased haystack `[name, tranche_label, coll_id, code]` | ✓ static |
| 4 | Dept-only dropdown; project-name lookup moved to search; collProjectFilter merged away | `#collProjectFilter` markup + state + `populateCollProjectFilter` all removed; `#collDeptFilter` is the single static dept control | ✓ static |
| 5 | Group-by-Project toggle (flat ↔ grouped); flat keeps Show-N-completed; grouped sorts by worst urgency | `#collGroupByBtn`→`toggleCollGroupBy`; `renderCollectiblesGroupedRows`; groups sorted by `COLL_URG_ORDER[collGroupWorstTier(items)]`; flat path (Show-N-completed + pagination) untouched below the branch | ✓ static |
| 6 | CSV (13 cols, filtered set) + pagination recompute + ≤768px card list reflect the same rows | `exportCollectiblesCSV` byte-unchanged (13 headers, reads `getDisplayedCollectibles`); pagination over the visible set; mobile `buildCollectibleCard` over the same set | ✓ static |

## Invariants Preserved (carry-forward)

- **D-08b** — sort + all group counts key off the single `getCollectibleUrgency` helper; no second threshold source (`grep "d<=0"` → 0).
- **D-07** — `exportCollectiblesCSV` unchanged; 13 columns; reads the filtered set; T-85.6-01 `safe()` intact.
- **D-05b / D-06** — fully-paid hide (flat) + ≤768px card list intact over the new sorted set.
- **MD-01** — all new state (`collSearchQuery`, `collDuePreset`, `collGroupBy`, `collGroupExpanded`) reset in `destroy()`; all 5 new window handlers registered AND deleted (5/5 symmetric).
- **Near-due agreement (Fix-2)** — the Plan-01 Near-due filter and the Plan-02 grouped near-due summary both use `getCollectibleUrgency(c).tier === 'near-due'`.

## Requirement Traceability

- `spike-027c` — completed across 99.3-01 (sort + filter bar) and 99.3-02 (group-by accordion). Both plans' frontmatter `requirements: [spike-027c]`; both SUMMARYs `requirements-completed: [spike-027c]`. Fully accounted for.

## Human Verification Required

See `99.3-HUMAN-UAT.md`. Load `python -m http.server 8000` → Finance → Collectibles:

1. **Urgency order** — rows render most-urgent-first: critical → overdue → near-due → partial → healthy → (paid hidden by default).
2. **Search** — typing filters by project/service name, tranche label, COLL id, and code; page resets to 1.
3. **Dept dropdown** — All Departments / Projects / Services filters correctly; no code-name dropdown remains.
4. **Date presets** — Today / Yesterday / This month / Last 7 / Last 30 / Any time each re-filter; button label updates.
5. **Fixed range + stale-clear** — pick Fixed range, type From/To (filters); then pick "Any time" → From/To inputs are CLEARED (no stale dates) and filtering stops.
6. **Near due (≤7 days)** status option — shows only rows ≤7 days out and NOT overdue (overdue excluded).
7. **Group by Project** — toggle → groups render worst-urgency first; expand/collapse a group; toggle off restores flat Show-N-completed + pagination; group "near due" count matches the Near-due filter.
8. **Scorecard divergence (expected)** — Total Invoiced/Collected/Outstanding intentionally shift between flat (active list, paid excluded) and grouped (portfolio, paid included). This is by design, not a bug.
9. **CSV** — Export CSV still downloads the filtered set (13 columns), including fully-paid rows.
10. **Remount** — navigate away from Finance and back → filter bar resets (empty search, All Departments, "Due: Any time", flat view, no groups expanded).

## Notes

- No Firestore schema/rules change → no dev/prod rules deploy needed for this phase (the standing v3.3→main + 87.4/99/99.1 rules-deploy debt is unaffected).
- If any UAT item fails → `/gsd-plan-phase 99.3 --gaps`.
