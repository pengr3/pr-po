---
phase: 99.3-collectibles-filter-bar-urgency-sort
plan: 01
subsystem: ui
tags: [finance, collectibles, filter, sort, date-picker, vanilla-js]

# Dependency graph
requires:
  - phase: 99.2-collectibles-revamp-beta
    provides: getCollectibleUrgency (D-08b single-source tier helper), 5-col table + scorecard + mobile cards, COLLECTIBLES REVAMP β CSS block
provides:
  - Urgency-tier sort (COLL_URG_ORDER) replacing the 99.2 status-priority sort in getDisplayedCollectibles()
  - Rebuilt filter pipeline: free-text search box, dept-only dropdown, "Near due (≤7 days)" status option
  - Hand-rolled Looker-style date-range popover (computeCollDueRange + 6 handlers) replacing the two raw date inputs and the code-name project dropdown
affects: [99.3-02 (group-by accordion — consumes COLL_URG_ORDER + rebuilt filter bar)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Single-source urgency tier ordering (COLL_URG_ORDER keyed on getCollectibleUrgency tier — D-08b)"
    - "Effective-range date filter: presets compute a {from,to} written into the existing collDueFrom/ToFilter state; Fixed range reuses the same two inputs"
    - "Deterministic popover open-state (display 'block'/'none' only, never '')"

key-files:
  created: []
  modified:
    - app/views/finance.js
    - styles/views.css

key-decisions:
  - "Near-due filter uses the canonical getCollectibleUrgency tier === 'near-due' (≤7 days AND not overdue, finance.js:148), deliberately narrower than the spike's looser d<=0 && -d<=7 — so it agrees with Plan 02 group counts"
  - "last7/last30 presets are ROLLING windows ending today; this_month = current calendar month"
  - "Non-fixed presets unconditionally overwrite both effective-range state AND the visible From/To input .values (Fix-4), so picking Any time after typing Fixed dates leaves no stale values"
  - "Date inputs reuse the EXISTING ids collDueFromFilter/collDueToFilter so getDisplayedCollectibles + CSV need no rewiring"

patterns-established:
  - "Pattern: client-side free-text search builds a lowercased haystack [name, tranche_label, coll_id, code].join(' ') and .includes() — never interpolated into innerHTML (T-99.3-01)"

requirements-completed: [spike-027c]

# Metrics
duration: ~20min
completed: 2026-06-07
---

# Phase 99.3 Plan 01: Urgency Sort + Filter-Bar Rebuild Summary

**Collectibles now sort most-urgent-first (critical → overdue → near-due → partial → healthy → paid via the shared D-08b helper) and the filter bar matches the validated spike-027c design — free-text search, a single dept-only dropdown, a "Near due (≤7 days)" status option, and a hand-rolled Looker-style date-range popover replacing the two raw date inputs and the redundant code-name dropdown.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-06-07
- **Tasks:** 3
- **Files modified:** 2 (app/views/finance.js, styles/views.css)

## Accomplishments
- Replaced the status-priority sort with an urgency-tier sort keyed off `getCollectibleUrgency` (no second threshold source — D-08b); CSV/scorecard/pagination consume the same `getDisplayedCollectibles()` set unchanged (D-07).
- Rebuilt the filter pipeline: removed `collProjectFilter` + `populateCollProjectFilter()`, added `collSearchQuery`/`collDuePreset` state, free-text search, a `Near Due` status branch using the canonical tier, and `computeCollDueRange()`.
- Added the spike-027c date-range popover (trigger button + preset panel + Fixed-range sub-block) with 6 handlers; registered 3 on `window`, torn down in `destroy()`; MD-01 resets for the new state.
- Ported `.coll-date-picker-*` / `.coll-dp-*` CSS (section 7) into the COLLECTIBLES REVAMP β block.

## Task Commits

Each task was committed atomically:

1. **Task 1: Urgency-tier sort** — `b59f77c` (feat)
2. **Task 2: Filter pipeline rebuild** — `af2707f` (feat)
3. **Task 3: Filter-bar markup + date popover handlers + CSS** — `cb151dd` (feat)

## Files Created/Modified
- `app/views/finance.js` — COLL_URG_ORDER + urgency sort; collSearchQuery/collDuePreset state; computeCollDueRange + 6 popover handlers; filterCollectiblesTable/getDisplayedCollectibles rewrite; populateCollProjectFilter removed; filter-bar markup; window register/teardown; destroy resets.
- `styles/views.css` — section 7 (Looker date-range popover) appended to COLLECTIBLES REVAMP β.

## Decisions Made
- Near-due = canonical `getCollectibleUrgency` tier (≤7 & not overdue), NOT the spike's looser test — pins Plan 02 group counts to the same definition.
- Reused existing `collDueFromFilter`/`collDueToFilter` ids for the Fixed-range inputs so no other code path (filter, CSV) needed rewiring.
- Moved the `destroy()` MD-01 state reset of the new vars into the Task 2 commit (rather than Task 3 as the plan sketched) because removing the `collProjectFilter` declaration made its lingering `destroy()` assignment a strict-mode ReferenceError; Task 3 then only added the window-fn teardown. End state matches the plan exactly.

## Deviations from Plan

### Notes (no scope change)

**1. [Acceptance-grep false positive] `statusPriority` still matches**
- **Found during:** Task 1 verification
- **Detail:** The Task 1 acceptance gate says `grep -n "statusPriority"` must return nothing, but lines ~889–892 retain a *separate* `statusPriority` map used by the RFP table sort (`deriveRFPStatus`), unrelated to collectibles. The collectibles status-priority map was removed. The grep was written assuming a single occurrence; intent (collectibles sort replaced) is satisfied. The RFP map was correctly left untouched.

**2. [Task-boundary shuffle] destroy() state reset landed in Task 2**
- See "Decisions Made" above. Window-fn deletes remain in Task 3. No behavioral difference; all final-state grep gates pass.

---

**Total deviations:** 0 functional. 2 documentation notes (1 acceptance-grep false positive, 1 task-boundary shuffle). No scope creep.

## Issues Encountered
None — all three tasks passed `node --check` and every (corrected) acceptance grep gate.

## User Setup Required
None — display/UX only. NO Firestore schema/rules change.

## Next Phase Readiness
- `COLL_URG_ORDER` and the rebuilt filter bar are in place — Plan 02 (group-by-Project accordion) can consume both.
- **Browser UAT pending** (zero-build SPA, no functional harness): verify urgency order top-to-bottom, search, dept dropdown, the 7 date presets + Fixed range (type Fixed dates → pick "Any time" → inputs cleared), Near-due option (≤7-and-not-overdue only), CSV exports the filtered set, pagination + mobile cards reflect the same rows.

---
*Phase: 99.3-collectibles-filter-bar-urgency-sort*
*Completed: 2026-06-07*
