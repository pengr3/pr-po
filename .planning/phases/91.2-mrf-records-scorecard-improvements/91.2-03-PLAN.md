---
phase: 91.2-mrf-records-scorecard-improvements
plan: 03
type: execute
wave: 2
depends_on: [91.2-02]
subsystem: finance
tags: [is_subcon, regression-fix, scorecard, finance-js]
files_modified:
  - app/views/finance.js
  - .planning/phases/91.2-mrf-records-scorecard-improvements/91.2-HUMAN-UAT.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "approvePR's PO-creation block computes hasSubconItems from supplierItems before addDoc"
    - "is_subcon on the new PO doc is set to hasSubconItems (not hardcoded false)"
    - "procurement_status on the new PO doc is 'Pending' when hasSubconItems else 'Pending Procurement' (matches archive/finance.html:2528 behavior)"
    - "Category check uses the literal string 'SUBCON' (case-sensitive) to match archive parity and the existing items_json convention; no string-tolerance changes shipped in this plan"
    - "UAT test 4 (cross-group AND filter) is marked unblocked in 91.2-HUMAN-UAT.md with a backfill caveat noting only POs created AFTER this fix will populate the Subcon scorecard"
    - "No other call sites modified — all downstream consumers (finance.js:2844, mrf-records.js:721/1559, procurement.js:5188/5657/5840) already branch on po.is_subcon and need zero changes"
  artifacts:
    - path: "app/views/finance.js"
      provides: "hasSubconItems derivation in approvePR PO-creation loop; is_subcon + procurement_status now derived from hasSubconItems"
      contains: "hasSubconItems"
    - path: ".planning/phases/91.2-mrf-records-scorecard-improvements/91.2-HUMAN-UAT.md"
      provides: "Updated test 4 status with backfill caveat"
      contains: "Test 4"
  key_links:
    - from: "approvePR addDoc payload"
      to: "supplierItems"
      via: "supplierItems.some(item => item.category === 'SUBCON')"
      pattern: "hasSubconItems"
    - from: "is_subcon field on new PO doc"
      to: "hasSubconItems local"
      via: "direct assignment"
      pattern: "is_subcon: hasSubconItems"
    - from: "procurement_status field on new PO doc"
      to: "hasSubconItems local"
      via: "ternary"
      pattern: "hasSubconItems \\? 'Pending' : 'Pending Procurement'"
---

<objective>
Restore archive auto-detect of is_subcon at PO creation time so the Subcon scorecard group (Phase 91.2 Plan 01/02) populates correctly. The current hardcoded `is_subcon: false` at finance.js:5348 is a regression introduced during the SPA modularization — archive/finance.html:2507 derived this from `supplierItems.some(item => item.category === 'SUBCON')`. This unblocks UAT test 4 (cross-group AND filter: Materials + Subcon active simultaneously).

Output: 3-line change in approvePR's PO-creation block + UAT note update. Closes the long-pending Phase 68.1 backlog item.

Scope NOT included (deferred):
- Backfill of existing PO documents (is_subcon stays false on all historical POs)
- Case-tolerant category matching (`'subcon'` / `'Subcontract'` etc. still slip through)
- Mixed-PO line-item attribution (an entire PO becomes subcon if any item matches)
These are accepted limitations for this plan; mirror archive behavior exactly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91.2-mrf-records-scorecard-improvements/91.2-02-SUMMARY.md
@.planning/phases/91.2-mrf-records-scorecard-improvements/91.2-HUMAN-UAT.md

<interfaces>
<!-- approvePR PO-creation loop (finance.js ~5320-5360): -->
<!-- for (const supplier of suppliers) { -->
<!--     const supplierItems = itemsBySupplier[supplier]; -->
<!--     const supplierTotal = supplierItems.reduce(...); -->
<!--     ... -->
<!--     await addDoc(collection(db, 'pos'), { -->
<!--         ..., -->
<!--         procurement_status: 'Pending Procurement',  // line 5347 — HARDCODED -->
<!--         is_subcon: false,                            // line 5348 — HARDCODED -->
<!--         ... -->
<!--     }); -->
<!-- } -->

<!-- Archive reference (archive/finance.html:2506-2528): -->
<!-- // Check if this PO contains SUBCON items -->
<!-- const hasSubconItems = supplierItems.some(item => item.category === 'SUBCON'); -->
<!-- ... -->
<!-- is_subcon: hasSubconItems, -->
<!-- procurement_status: hasSubconItems ? 'Pending' : 'Pending Procurement' -->

<!-- Downstream readers already ready: -->
<!-- finance.js:2844, mrf-records.js:721, mrf-records.js:1559, -->
<!-- procurement.js:5188, procurement.js:5657, procurement.js:5840 -->
<!-- all use: const defaultStatus = isSubcon ? 'Pending' : 'Pending Procurement' -->
<!-- expense-modal.js:134 reads po.is_subcon directly for Subcon Cost scorecard -->

<!-- items_json category field: -->
<!-- Per existing convention (procurement.js item-category select), SUBCON is one of the -->
<!-- canonical category enum values written into each item before PR generation. -->
<!-- Case is uppercase by convention. -->
</interfaces>
</context>

<threat_model>
**No new threats introduced.** This plan changes two field values inside an existing addDoc — no new write paths, no new collections, no new permissions, no new user-controlled inputs surfaced. The new write payload still matches the existing `pos` collection firestore.rules predicate (Finance role required, already deployed pre-Phase 91.2).

| ID | STRIDE | Threat | Severity | Mitigation | Disposition |
|----|--------|--------|----------|------------|-------------|
| T-91.2-03-01 | T (Tampering) | Malicious item_category value could flip is_subcon | Low | Items are written upstream during PR generation by an authenticated procurement_user; same trust boundary as the rest of items_json. No new attack surface. | Accept |
| T-91.2-03-02 | I (Info disclosure) | Subcon-flagged POs appear in Subcon scorecard rather than Materials | None | This is the **intended** behavior; the regression was hiding subcon POs in the Materials bucket. | N/A |
</threat_model>

<tasks>

## Task 1: Restore is_subcon + procurement_status auto-detect in approvePR PO creation

**Type:** standard
**Scope:** app/views/finance.js (approvePR PO-creation block, ~lines 5320-5360)

**Steps:**
1. In the `for (const supplier of suppliers)` loop, immediately after the `supplierTotal` declaration (currently line 5323) and before the `firstWord`/`supplierSlug` block, insert:
   ```js
   const hasSubconItems = supplierItems.some(item => item.category === 'SUBCON');
   ```
2. Change line 5347 from `procurement_status: 'Pending Procurement',` to:
   ```js
   procurement_status: hasSubconItems ? 'Pending' : 'Pending Procurement',
   ```
3. Change line 5348 from `is_subcon: false,` to:
   ```js
   is_subcon: hasSubconItems,
   ```

**Acceptance criteria:**
- `grep -n "hasSubconItems" app/views/finance.js` returns exactly 3 hits (1 declaration + 2 usages) inside the approvePR function
- `grep -n "is_subcon: false" app/views/finance.js` returns 0 hits
- `grep -n "procurement_status: 'Pending Procurement'," app/views/finance.js` returns 0 hits inside approvePR (the bare-string default elsewhere in the file is unrelated read-side fallback logic and must stay)
- No other file modified
- Browser console: open /finance, approve a PR whose PR contains a SUBCON-category line item, verify the resulting PO doc in Firestore has `is_subcon: true` and `procurement_status: 'Pending'` (UAT browser test, not part of static acceptance)

**Commit:** `fix(91.2-03): restore is_subcon auto-detect at PO creation (archive parity)`

---

## Task 2: Update 91.2-HUMAN-UAT.md — add backfill caveat under test 4

**Type:** standard
**Scope:** .planning/phases/91.2-mrf-records-scorecard-improvements/91.2-HUMAN-UAT.md

**Steps:**
1. Locate the existing test 4 entry (cross-group AND filter) — currently shows `result: [pending]`.
2. Append a "Backfill caveat" callout immediately under the `result:` line for test 4: existing pre-fix POs still have `is_subcon: false`. To exercise the AND filter, create a fresh MRF → PR with a SUBCON-category line item, approve it through Finance to mint a new PO with `is_subcon: true`, then run test 4.
3. No structural changes to tests 1/2/3/5/6.
4. No frontmatter changes — `status: partial` stays.

**Acceptance criteria:**
- `grep -n "Backfill caveat" .planning/phases/91.2-mrf-records-scorecard-improvements/91.2-HUMAN-UAT.md` returns ≥1 hit
- Tests 1/2/3/5/6 remain byte-identical (verify with `git diff --stat` showing one file changed, additions only — no deletions outside test 4)

**Commit:** `docs(91.2-03): unblock UAT test 4 with backfill caveat`

</tasks>

<verification>
## Static verification (post-execute)

```bash
# Task 1
grep -c "hasSubconItems" app/views/finance.js           # expect: 3
grep -c "is_subcon: false" app/views/finance.js          # expect: 0
grep -c "is_subcon: hasSubconItems" app/views/finance.js # expect: 1

# Task 2
grep -c "Backfill caveat" .planning/phases/91.2-mrf-records-scorecard-improvements/91.2-HUMAN-UAT.md  # expect: ≥1
```

## Goal-backward check

**Phase 91.2 goal:** Cross-group scorecard multi-filter — Materials AND Subcon active simultaneously.
**Plan 03 contribution:** Subcon scorecard group can now actually count rows. Without this, the AND condition between groups always evaluates to "0 rows" and test 4 can never pass.

After Plan 03 ships, end-to-end UAT path is unblocked. No further code plans needed in Phase 91.2.

## Sanity assertions (no-regression checks)

1. **`'Pending'` must NOT appear in `blockedStatuses`** at `app/views/procurement.js:944`. Verified before drafting: `blockedStatuses = ['Procuring', 'Procured', 'Delivered', 'Processing', 'Processed']`. Subcon `'Pending'` POs remain cancellable (mirrors Materials `'Pending Procurement'` behavior). Plan 03 must NOT modify this list.
2. **`PROCUREMENT_STATUS_ORDER`** at `app/views/procurement.js:485–495` already maps `'Pending': 1` alongside `'Pending Procurement': 1` — subcon-default sort behaves identically to Materials post-fix. Plan 03 must NOT modify this map.
3. **No write-back of `is_subcon` anywhere.** `procurement.js:5597` and `mrf-records.js:1453` only **read** `poData.is_subcon` into local view models; they never `updateDoc`/`setDoc` it back. Backfill stays manual / future-decision territory; this plan does not introduce a write path that could be misused for backfill.
</verification>

<rollback>
If Plan 03 introduces unexpected behavior:
- `git revert <commit-sha-task-1>` — reverts the 3-line change in finance.js
- `git revert <commit-sha-task-2>` — reverts the UAT note
- POs created during the rollback window retain `is_subcon: true` / `procurement_status: 'Pending'` — this is non-breaking because downstream readers handle both values correctly; manual Firestore patch unneeded.
</rollback>
