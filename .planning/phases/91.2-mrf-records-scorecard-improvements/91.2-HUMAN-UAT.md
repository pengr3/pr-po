---
status: complete
phase: 91.2-mrf-records-scorecard-improvements
source: [91.2-VERIFICATION.md]
started: 2026-05-18T00:00:00Z
updated: 2026-05-19T00:00:00Z
---

## Current Test

[complete — all tests passed 2026-05-19]

## Tests

### 1. Compact card visual rendering
expected: Navigate to #/procurement/records — 7 scorecard cards render with compact padding/font (no large gradient backgrounds), colored left borders per status, and counts populated from Firebase on load
result: [pass]

### 2. Click-to-filter (single group)
expected: Click "Pending Procurement" (Materials) card — card gets blue active border (.project-scorecard-card--active); table narrows to show only MRFs that have at least one Pending Procurement PO
result: [pass]

### 3. Toggle-off behavior
expected: Click the already-active "Pending Procurement" card again — card deselects (active border removed); table reverts to unfiltered view
result: [pass]

### 4. Cross-group OR logic
expected: Click "Pending Procurement" (Materials) + "Processing" (Subcon) — both cards show active styling; table shows MRFs matching EITHER condition (OR, not AND) — the union of both filtered sets, so the row count is the SUM (minus any MRFs that match both) of each single-filter count
result: [pass]

> **Backfill caveat (post-Plan 03):** Existing pre-fix POs still have `is_subcon: false` in Firestore — they will never appear in the Subcon scorecard counts even if their items_json includes a SUBCON-category line. To exercise this test, create a fresh MRF whose items contain at least one SUBCON-category line, convert to PR, then approve through Finance — that new PO will be minted with `is_subcon: true` and `procurement_status: 'Pending'` (Subcon Processing). Click the resulting "Pending" Subcon card together with any active Materials card to verify the OR logic — both the new subcon MRF AND the existing materials-matched MRFs should appear simultaneously. Manual Firestore backfill of historical POs is deferred (not part of Phase 91.2).

### 5. Composed filters
expected: With a scorecard filter active, use the search box or MRF Status dropdown — both filter correctly together with the active scorecard filter
result: [pass]

### 6. Refresh respects active state
expected: With a scorecard card active, click the Refresh (🔄) button — the table re-loads via loadPRPORecords and still respects the active scorecard filter (active card remains highlighted; table remains filtered)
result: [pass]

## Follow-up Fix Re-verifications

### Fix A — Status no longer reverts on 🔄 (commit 7daa2b9)
expected: Change procurement_status via dropdown; click 🔄 — status stays on new value, does not revert
result: [pass]

### Fix B — 🔄 fetches fresh Firestore data (commit e74fc70)
expected: Changes made in another tab/session appear immediately on 🔄 click, not delayed by 5-min cache
result: [pass]

### Fix C — OR filter cross-group (commit 65e1b3c)
expected: Materials card + Subcon card active simultaneously — row count is union (≥ each single count)
result: [pass]

## Summary

total: 9
passed: 9
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
