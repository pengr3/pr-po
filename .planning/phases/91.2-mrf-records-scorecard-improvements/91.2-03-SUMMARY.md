---
phase: 91.2-mrf-records-scorecard-improvements
plan: 03
subsystem: finance
tags: [is_subcon, regression-fix, scorecard, finance-js, archive-parity]

# Dependency graph
requires:
  - phase: 91.2-02
    provides: "Subcon scorecard filter logic in filterPRPORecords (Set-based mrf_id lookup against cachedAllPOData)"
provides:
  - is_subcon flag now correctly populated at PO creation, unblocking the Subcon scorecard group counts and UAT test 4 (cross-group AND filter)
affects:
  - approvePR PO-creation block (finance.js ~lines 5320-5360) — is_subcon and procurement_status now derived from supplierItems SUBCON-category check

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Archive parity restoration — mirrors archive/finance.html:2507 SPA-modularization regression"
    - "Per-supplier derived flag computed once before addDoc payload assembly to avoid duplicate scans"

key-files:
  created: []
  modified:
    - app/views/finance.js
    - .planning/phases/91.2-mrf-records-scorecard-improvements/91.2-HUMAN-UAT.md

key-decisions:
  - "Case-sensitive 'SUBCON' literal check (not toLowerCase) — exact archive parity; tolerant matching deferred"
  - "Backfill of existing POs deferred — only new POs created via approvePR after this commit populate the Subcon scorecard; UAT path uses a fresh MRF to mint a test PO"
  - "Mixed-PO attribution unchanged from archive — a single SUBCON-category item flips the whole PO; mixed material+subcon POs treated as fully subcon"
  - "No firestore.rules touched — pos write predicate (Finance role) unchanged"

patterns-established:
  - "When restoring archive-only behavior dropped during SPA modularization, mirror exact archive logic and document the deferred-scope items (backfill, mixed attribution, case tolerance) explicitly"

requirements-completed:
  - "Closes Phase 68.1 backlog item: 'Fix subcon cost scorecard showing zero when subcon items are categorized as subcon in items_json but po is_subcon flag is not set'"

# Metrics
duration: ~15min
completed: 2026-05-18
---

# Phase 91.2 Plan 03: is_subcon Auto-Detect Regression Fix Summary

**Restored archive-parity auto-detect of `is_subcon` and `procurement_status` at PO creation in `approvePR`, unblocking the Subcon scorecard counts and UAT test 4.**

## Performance

- **Duration:** ~15 min (drafting + plan-checker + 2 atomic-commit execution)
- **Started:** 2026-05-18
- **Completed:** 2026-05-18
- **Tasks:** 2
- **Files modified:** 2 (1 code, 1 doc)
- **Commits:** 3 (PLAN doc + 2 atomic task commits)

## Accomplishments

- Added `hasSubconItems = supplierItems.some(item => item.category === 'SUBCON')` derivation in `approvePR`'s per-supplier PO-creation loop (finance.js ~line 5324)
- Replaced hardcoded `is_subcon: false` with `is_subcon: hasSubconItems` in the addDoc payload
- Replaced hardcoded `procurement_status: 'Pending Procurement'` with `procurement_status: hasSubconItems ? 'Pending' : 'Pending Procurement'`
- Added "Backfill caveat" callout under UAT test 4 documenting the post-fix verification path (fresh MRF → SUBCON-category item → Finance approval → new PO with `is_subcon: true`)

## Task Commits

Each task was committed atomically:

1. **PLAN doc** — `1b0fbe2` (docs)
2. **Task 1: Restore is_subcon + procurement_status auto-detect** — `4d5a801` (fix)
3. **Task 2: Update HUMAN-UAT with backfill caveat** — `df04e9c` (docs)

## Files Created/Modified

- `app/views/finance.js` — `hasSubconItems` derivation + flipped `is_subcon` and `procurement_status` lines in `approvePR` (3-line net change: +1 const decl, 2 field-value replacements)
- `.planning/phases/91.2-mrf-records-scorecard-improvements/91.2-HUMAN-UAT.md` — added backfill caveat blockquote under test 4 (2 lines added, no deletions)

## Decisions Made

- **Case-sensitive 'SUBCON' literal** — matches archive/finance.html:2507 exactly. Tolerant matching (`'subcon'`, `'Subcontract'`, etc.) considered but deferred — would re-open a category-string-canonicalization scope that doesn't belong in a regression-fix plan.
- **No backfill of historical POs** — manual Firestore patch deferred. UAT caveat documents the test path using a fresh MRF rather than relying on legacy data.
- **No write-back of `is_subcon` introduced** — preserves the read-only-after-create contract; future backfill, if ever wanted, will be an explicit ops task with its own audit trail.
- **No firestore.rules changes** — the `pos` collection write predicate (Finance role required) already covers this addDoc with no new attack surface.

## Deviations from Plan

None — both tasks executed exactly as written. Plan-checker recommendations (UAT wording fix, sanity-assertions block) were folded in before execution; no in-flight deviations during execute.

## Issues Encountered

None. Pre-execute Bash `&&` chain on `grep -c` exit-codes broke the verification one-liner (zero matches → nonzero exit); re-ran with `;` separators. Cosmetic only — file state was correct on first edit.

## Known Stubs

None.

## Threat Flags

None. Per the plan's threat model:
- T-91.2-03-01 (item.category tampering): accepted — items_json is written upstream by authenticated procurement_user; same trust boundary as the rest of items_json.
- T-91.2-03-02 (subcon POs visible in correct scorecard bucket): N/A — this is the intended behavior the regression was suppressing.

Sanity checks confirmed before execute (still hold):
- `'Pending'` is NOT in `app/views/procurement.js:944` `blockedStatuses` → new subcon POs remain cancellable, mirroring Materials `'Pending Procurement'` behavior
- `PROCUREMENT_STATUS_ORDER` at `procurement.js:485–495` maps both `'Pending'` and `'Pending Procurement'` to priority 1 → no sort regression
- No write-back of `po.is_subcon` exists anywhere in the codebase

## User Setup Required

**Browser UAT** — 6 tests in `91.2-HUMAN-UAT.md`. Test 4 (cross-group AND filter) requires the backfill-caveat path: create a fresh MRF with at least one SUBCON-category line item, convert to PR, approve through Finance, then exercise the AND filter against the newly-minted Subcon PO.

Tests 1/2/3/5/6 (Materials side) can run against any existing data — no fresh MRF required.

---

## Self-Check: PASSED

- `app/views/finance.js` contains all 3 expected `hasSubconItems` references (1 decl + 2 usages) — verified
- `app/views/finance.js` contains 0 `is_subcon: false` occurrences — verified
- `app/views/finance.js` contains exactly 1 `is_subcon: hasSubconItems` occurrence — verified
- `91.2-HUMAN-UAT.md` contains 1 `Backfill caveat` occurrence — verified
- UAT diff is additions-only, no deletions outside test 4 — verified (`git diff --stat` shows `1 file changed, 2 insertions(+)`)
- Three commits in linear order on `v3.3`: `1b0fbe2` → `4d5a801` → `df04e9c` — verified

---

*Phase: 91.2-mrf-records-scorecard-improvements*
*Completed: 2026-05-18*
