---
context: phase
phase: 91.2-mrf-records-scorecard-improvements
task: browser-UAT
total_tasks: 6 phase UAT tests + 2 follow-up fix re-verifications
status: paused-at-UAT
last_updated: 2026-05-18T12:45:00Z
---

<current_state>
Phase 91.2 is **code-complete on all three plans + two follow-up fixes**. All commits on `v3.3`. The remaining work is purely human browser verification.

Plans shipped today (in order):
- Plan 01 (`bc9be35`) — scorecard HTML to `.project-scorecard-card`
- Plan 02 (`ffdd733`) — filter state, click handler, cross-group filter, `loadPRPORecords` routing (originally AND, later flipped to OR — see follow-up #2)
- Plan 03 (`1b0fbe2` + `4d5a801` + `df04e9c` + `daf109f` + `b497117`) — is_subcon auto-detect regression fix at PO creation, restoring archive parity from `archive/finance.html:2507`. Closes Phase 68.1 backlog item.

Two follow-up fixes shipped during the same browser UAT session:
1. **Cache coherency fix** (`7daa2b9`) — `updatePOStatus` now mirrors the Firestore write into `cachedAllPOData` and `_prpoSubDataCache` after `updateDoc`. Surfaced when the user clicked the 🔄 toolbar button and the dropdown reverted to the pre-update value. Pre-existing bug, not a Plan 02 regression. Debug session at `.planning/debug/po-status-revert-on-refresh.md` (status: resolved).
2. **Cross-group filter flipped AND → OR** (`65e1b3c` + STATE log `3a1e222`) — spec pivot via `/gsd-fast`. User decided after passing the AND-version of test 4 that they actually want union semantics. `filterPRPORecords` now treats `materialFilterMrfIds ∪ subconFilterMrfIds` as the scorecard match when both are active; non-scorecard filters (dept/search/MRF-status) still AND with that union. UAT test 4 header + backfill caveat text updated in the same commit.
</current_state>

<completed_work>

This session (chronological commit list on `v3.3`):

| # | SHA | Type | Subject |
|---|---|---|---|
| 1 | `1b0fbe2` | docs | Plan 03 PLAN.md (is_subcon auto-detect plan) |
| 2 | `4d5a801` | fix | Restore is_subcon auto-detect at PO creation (archive parity) |
| 3 | `df04e9c` | docs | Unblock UAT test 4 with backfill caveat |
| 4 | `daf109f` | docs | Plan 03 SUMMARY |
| 5 | `b497117` | chore | Record Plan 03 in STATE.md |
| 6 | `0c60cef` | chore | Clear stale HANDOFF.json (earlier-in-session) |
| 7 | `7daa2b9` | fix | mrf-records: cache coherency in updatePOStatus |
| 8 | `65e1b3c` | fix | 91.2: flip cross-group scorecard filter AND → OR |
| 9 | `3a1e222` | chore | STATE: log /gsd-fast filter flip |

UAT progress as of pause:
- Tests 1, 2, 3, 5, 6 — verbally confirmed passed by user ("all passed but i ran thru a bug")
- Test 4 — was passed for AND semantics, then user changed spec to OR; needs a re-run against the new union behavior
- Two new fixes (commits `7daa2b9` and `65e1b3c`) need their own browser re-verification

`91.2-HUMAN-UAT.md` markers are still `[pending]` for all 6 tests — they were not flipped to `[pass]` because the spec for test 4 changed mid-UAT. **Flipping the markers is part of remaining work.**
</completed_work>

<remaining_work>

Six concrete items left, all human-driven:

1. **Verify cache coherency fix (`7daa2b9`).** On http://localhost:8000 → MRF Records → change a PO's procurement status via the dropdown → click 🔄 toolbar button. Status should STAY on the new value. Critical test cases (these are the ones that were exposed because they don't trigger the proof modal):
   - `Pending Procurement` → `Procuring` → 🔄 → stays on `Procuring`
   - `Procuring` → `Pending` → 🔄 → stays on `Pending`
   - `Pending` → `Delivered` → 🔄 → stays on `Delivered`
   - Subcon: `Pending` → `Processing` → 🔄 → stays on `Processing`
2. **Verify OR-filter flip (`65e1b3c`).** Click one Materials scorecard card + one Subcon card simultaneously. Table should show the UNION (row count ≥ each single-filter count). Compare against the pre-flip AND behavior (was strict intersection).
3. **Re-run UAT test 4** against a fresh SUBCON-category MRF (per Plan 03's backfill caveat in `91.2-HUMAN-UAT.md`). Required because existing POs are all `is_subcon: false` from before Plan 03 — need a new PO minted via Finance approval to actually populate the Subcon scorecard.
4. **Update `91.2-HUMAN-UAT.md`** — flip `[pending]` to `[pass]`/`[fail]` for all 6 tests, add result lines for the two follow-up fixes, update the Summary block (currently `passed: 0, pending: 6`).
5. **Phase 91 UAT Bug 3 re-verification** is still pending from before this session (commit `557a764` was a T4 follow-up that needs browser re-test for renderPOTrackingTable scoping). See `.planning/quick/260516-bg3-scoreboard-scoping/`.
6. **Phase 92.2 UAT** is also still pending from before this session (per STATE.md frontmatter).

Once 1-4 are done and Phase 91.2 is closed, the natural next step is `/gsd-complete-milestone` review for v3.3 → main (5 + 6 should ideally close before that).
</remaining_work>

<decisions_made>

- **Plan 03 = archive parity, no extras.** Mirrored `archive/finance.html:2507` exactly (case-sensitive `'SUBCON'`, whole-PO flag). Tolerant matching, mixed-PO attribution, and historical-data backfill explicitly deferred. Closes Phase 68.1 backlog.
- **Cache fix = in-place mutation, not TTL reset.** Mirrors the precedent at `procurement.js:1041` and `procurement.js:1930`. Surgical, ~14 LOC, no architecture change. TTL reset would have forced a full re-fetch on every status change.
- **Bug 2 routed via /gsd-fast, not /gsd-debug.** The "bug" was actually a spec pivot — Plan 02 implemented AND faithfully per the discuss-phase decision; user changed their mind after UAT. Surfacing this distinction kept /gsd-debug context focused on the actual defect.
- **No new firestore.rules / schema changes today.** All work was in-app behavior + cache coherency. No security surface changed.
</decisions_made>

<blockers>
None. Both code-side fixes are complete and committed. Only browser UAT remains.
</blockers>

## Required Reading (in order)

1. `.planning/phases/91.2-mrf-records-scorecard-improvements/91.2-HUMAN-UAT.md` — the 6 UAT tests with the post-fix test 4 (OR semantics) and backfill caveat
2. `.planning/phases/91.2-mrf-records-scorecard-improvements/91.2-03-SUMMARY.md` — Plan 03 details + sanity assertions still in force
3. `.planning/debug/po-status-revert-on-refresh.md` — Full debug evidence + fix rationale for the cache coherency bug (resolved)
4. `CLAUDE.md` — Project conventions if a fresh assistant resumes

## Critical Anti-Patterns

_None discovered this session that need structural mitigation. The session was clean: plan-checker PASS first try, both fixes single-shot, no failed deploys._

## Infrastructure State

- **Dev server:** `python -m http.server 8000` is running in this terminal as background task `b40mcqdqh`. Kill it if you want to stop serving the site.
- **Browser:** User was actively testing on http://localhost:8000 at pause time.
- **Branch:** `v3.3` (no merge to `main` yet). 9 commits made this session.
- **Working tree:** Clean for everything touched this session. Pre-existing untracked artifacts remain: `.claude/worktrees/`, `.planning/debug/86.9-row-misalign-DEBUG.md`, `.planning/phases/86.9-gantt-curtain-divider-pdf-export/86.9-03-PLAN.md`, `debug-diag-86.9.js`. **These are not mine — do not touch.**

## Pre-Execution Critique Required

_Not applicable — no design-vs-execute split here. Both fixes are post-design and post-execute._

<context>
Resumed earlier in session from an existing HANDOFF.json that documented Plan 02 paused awaiting the is_subcon decision. User picked Approach 1 (minimal archive-parity fix). Routed through proper Plan 03 path (gsd-plan-phase → gsd-plan-checker PASS → execute), then browser UAT surfaced two more items handled inline: a real defect (refresh button revert) via /gsd-debug end-to-end with root-cause confirmation, and a spec pivot (AND→OR) via /gsd-fast.

Mental model entering pause: Phase 91.2 is fully code-shipped and statically verified. The only remaining risk is that one of the two follow-up fixes regresses something not yet accounted for. The OR-flip is bounded (single function, single composition expression). The cache fix is the higher-risk one — it touches a hot path that runs on every PO status update — but it's a strict additive change matching two existing precedents in the same file, so the blast radius is well-understood.

Memory updated this session: `project_milestone_status.md` — Phase 68.1 marked CLOSED 2026-05-18.
</context>

<next_action>
On resume: ask the user to run the 4 browser checks listed under remaining_work item #1 (cache fix), then the OR-filter check (item #2), then the fresh-SUBCON test 4 path (item #3). When all green, flip the markers in `91.2-HUMAN-UAT.md` (item #4) and offer `/gsd-complete-milestone` review.

Dev server is still running at http://localhost:8000 — user can start verifying immediately without setup.
</next_action>
