---
context: phase
phase: 91.2-mrf-records-scorecard-improvements
task: UAT (post-execution)
total_tasks: UAT 6 tests
status: blocked_design
last_updated: 2026-05-18T10:25:48Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: is_subcon never set — `is_subcon` is hardcoded `false` on every PO created in `finance.js:5348`. No logic in the current codebase ever sets it to `true`. The Phase 91.2 Subcon scorecard group reads `is_subcon` to count/filter — so all subcon counts are 0 and the subcon scorecard filter never returns results. UAT tests #2–#6 can be run for Materials group, but UAT test #4 (cross-group AND logic) is fully untestable until this is resolved.

**Do not proceed into UAT for the Subcon scorecard group until the is_subcon fix is decided and implemented.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Scorecarding a broken flag | Phase 91.2 built a full scorecard + filter system on `is_subcon` without verifying the flag is ever populated. The Subcon group will always show zero. | blocking | Before building UI on a boolean flag, grep for all write sites and confirm at least one sets it to `true`. |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 91.2 is code-complete (both plans executed, committed). All 14 programmatic verifications pass. The phase is paused at human browser UAT — 6 tests pending in 91.2-HUMAN-UAT.md.

This session discovered a critical design gap: `is_subcon` is never set to `true` anywhere in the codebase. The Subcon scorecard cards (Pending / Processing / Processed) will always display "0" and clicking them will never filter any MRFs. The Materials group scorecards should work correctly because all POs are `is_subcon: false`.

The user investigated and understands the issue. A decision on the fix approach is needed before the Subcon-dependent UAT tests can run.
</current_state>

<completed_work>

- Plan 01 — Replaced old scorecard HTML with `.project-scorecard-card` components (compact style, colored left borders, data-group/data-status/onclick attributes). All 7 cards wired. Committed.
- Plan 02 — Added `activeMaterialsFilter` / `activeSubconFilter` state vars, `handleMRFScorecardClick()`, `renderMRFScorecardActive()`, updated `filterPRPORecords()` with AND logic across both groups, routed `loadPRPORecords()` through `filterPRPORecords()` on both paths, registered window function in init(), cleaned up in destroy(). Committed.
- Code review completed (91.2-REVIEW.md).
- Static verification completed: 14/14 must-haves verified (91.2-VERIFICATION.md). Status: `human_needed`.
- Human UAT file created (91.2-HUMAN-UAT.md) with 6 browser tests, all pending.
- This session: investigated `is_subcon` population gap — confirmed it's a systemic issue, not a Phase 91.2 bug. Phase 91.2 implementation itself is correct; it's the upstream data that's broken.
</completed_work>

<remaining_work>

**Before UAT can fully complete:**
1. Decide on `is_subcon` fix approach (auto-detect from item categories vs. manual toggle in Finance modal vs. hybrid)
2. Implement the chosen fix (likely a new plan — Phase 91.2 Plan 03, or a separate Phase 91.3)
3. Verify at least one PO in production has `is_subcon: true` after the fix

**UAT tests (browser):**
1. Compact card visual rendering — [pending]
2. Click-to-filter (Materials single group) — [pending, testable without is_subcon fix]
3. Toggle-off behavior — [pending, testable without is_subcon fix]
4. Cross-group AND logic (Materials + Subcon) — [blocked until is_subcon fix deployed]
5. Composed filters (scorecard + search/dropdown) — [pending, testable without is_subcon fix]
6. Refresh respects active filter state — [pending, testable without is_subcon fix]
</remaining_work>

<decisions_made>

- Phase 91.2 built scorecards on `is_subcon` flag — this is correct as the architectural intent.
- `is_subcon` fix options surfaced this session: (A) auto-detect from `category === 'SUBCON'` in items_json during PO creation in `finance.js`; (B) explicit manual toggle on Finance PR approval modal; (C) hybrid (auto-detect as default, overridable). **No decision made yet — awaiting user choice.**
- The fix scope is small: a single block in `approvePR()` in `finance.js` around line 5348 for option A.
</decisions_made>

<blockers>

- **is_subcon never populated**: All POs have `is_subcon: false`. Subcon scorecard group is non-functional. Requires a fix before full UAT can proceed. Options and analysis surfaced — user decision pending.
- **components.css has uncommitted modifications**: `styles/components.css` is modified (`M`) but not committed. Needs review before committing.
</blockers>

## Required Reading (in order)

1. `.planning/phases/91.2-mrf-records-scorecard-improvements/91.2-HUMAN-UAT.md` — 6 browser UAT tests; tests 2/3/5/6 are testable now; test 4 blocked
2. `.planning/phases/91.2-mrf-records-scorecard-improvements/91.2-VERIFICATION.md` — 14 static verifications passed; human tests still pending
3. `app/views/finance.js` lines 5320–5360 — `approvePR()` where `is_subcon: false` is hardcoded
4. `app/views/procurement.js` lines 5173–5240 — `updatePOScoreboards()` and scorecard filter logic in `filterPRPORecords()`

## Infrastructure State

- Dev server: not running (start with `python -m http.server 8000`)
- Firebase: production (`clmc-procurement`) — all writes go live
- Netlify: auto-deploy on push to main

## is_subcon Fix Options (decision needed)

| Option | Where | Effort | Trade-off |
|--------|-------|--------|-----------|
| A: Auto-detect from categories | `finance.js:5348` in `approvePR()` | 1 line check | Fully automatic; relies on item categories being set correctly in MRF |
| B: Manual toggle in Finance approval modal | Finance PR approval modal UI | Small UI addition | Explicit control; requires Finance user to consciously flag each PR |
| C: Hybrid (auto-detect + override) | finance.js + modal UI | Moderate | Best UX but more code |

**Code for Option A (single-location fix in `approvePR()`):**
```javascript
const items = JSON.parse(pr.items_json || '[]');
const isSubcon = items.some(item => (item.category || '').toUpperCase() === 'SUBCON');
// Then use: is_subcon: isSubcon
```

<context>
Both plans are fully implemented. The scorecard system is architecturally sound. The blocker is upstream: `is_subcon` was designed to be set at PO creation time but the write was never implemented. The expense modal has a workaround (category-level detection) but the scorecard reads the PO flag directly.

UAT can partially proceed (Materials group works). The Subcon group UAT test (AND logic) needs the fix first. User preference was to discuss the fix approach before executing.
</context>

<next_action>
Start with: Confirm is_subcon fix approach with user (auto-detect A vs manual B vs hybrid C), then either add a Plan 03 to Phase 91.2 or create Phase 91.3 to implement it. After fix deployed to production, resume browser UAT for all 6 tests.
</next_action>
