---
phase: 113-assignment-source-of-truth-and-project-read-enforcement
plan: 06
type: execute
wave: 4
depends_on: ["113-03"]
files_modified:
  - app/views/mrf-form.js
  - app/views/procurement.js
autonomous: true
requirements: [D-02, D-04, D-13, D-16]

must_haves:
  truths:
    - "D-02: `mrf-form.js` loadProjects and `procurement.js` loadProjects both drop their `where('active','==',true)` clause on the scoped branch and issue a bare `where('personnel_user_ids','array-contains',uid)` query, re-applying the `active` filter in the existing snapshot post-filter — no new composite index is needed for either"
    - "D-13: `mrf-form.js` loadServices and `procurement.js` loadServicesForNewMRF no longer issue `where('service_code','in',assignedCodes)` — both use the personnel array-contains shape"
    - "D-16: `procurement.js` loadProjects is treated as fully unscoped today (RESEARCH.md Contradicts-CONTEXT.md #1 disproves the `projScope` claim) and is converted like every other Shape B site, and `services_admin` reaches it as a scoped role"
    - "D-04: `isMrfInAssignedScope()` at procurement.js:2980-2988 is NOT edited — the 260615-nlj no-leak invariant keeps its exact union logic and is fed personnel-derived codes by the plan-113-03 helper repoint"
    - "D-02: the PO-Delivered activity-feed project lookup uses `getDoc(doc(db,'projects', mrfData.project_id))` instead of a `where('project_name','==',...)` list query, converting a list operation into a single-document read"
  artifacts:
    - path: "app/views/mrf-form.js"
      provides: "personnel-scoped project and service pickers for the dedicated MRF form"
      contains: "array-contains"
    - path: "app/views/procurement.js"
      provides: "personnel-scoped project/service caches + doc-ID PO-Delivered journal lookup"
      contains: "array-contains"
  key_links:
    - from: "app/views/procurement.js PO Delivered handler"
      to: "projects/{project_id}"
      via: "getDoc using the MRF's denormalized project_id"
      pattern: "getDoc\\(doc\\(db, 'projects', "
    - from: "app/views/mrf-form.js rebuildPSOptions"
      to: "window.getAssignedProjectCodes"
      via: "personnel-derived code list from plan 113-03"
      pattern: "getAssignedProjectCodes"
---

<objective>
Convert both MRF-filing surfaces — the dedicated MRF form and the Procurement tab's Create-MRF data sources — plus the PO-Delivered journal traversal.

`procurement.js:2932` deserves particular care: CONTEXT.md's preliminary audit called it "partially scoped already via `projScope` at 2981". RESEARCH.md disproves this — `projScope` is a local inside `isMrfInAssignedScope()` (`procurement.js:2980-2988`), an unrelated function that filters MRF RECORDS client-side and shares no code path or variable with `loadProjects()` (`:2926-2955`). `loadProjects()` has zero scoping of any kind and must be converted like `projects.js:848` and `mrf-form.js:1046`.

`isMrfInAssignedScope()` itself is deliberately NOT touched. D-04's no-leak invariant is expressed as a department-keyed union over code lists; plan 113-03 already made those code lists personnel-derived, so the invariant survives with its logic byte-identical. Editing it here would put the invariant at risk for no gain.

Purpose: close the Shape B (unscoped-plus-equality) and Shape E (legacy `in`) sites on the MRF path, and the last Shape D name-lookup.
Output: two files whose project and service reads are personnel-scoped or doc-ID direct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONTEXT.md
@.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-RESEARCH.md
@.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
`app/utils.js` after plan 113-03: `getAssignedProjectCodes()` / `getAssignedServiceCodes()` — `null` = see-all, personnel-derived `string[]` otherwise.

Sites in scope (current line numbers):
  `app/views/mrf-form.js:1044-1069` `loadProjects()` — `query(projectsRef, where('active','==',true))`
  `app/views/mrf-form.js:1085-1124` `loadServices()` — `where('service_code','in',assignedCodes)` / `where('active','==',true)`
  `app/views/mrf-form.js:1146-1197` `rebuildPSOptions()` — the client-side assignment filter, including the codeless-project carve-out at `:1157-1160` and the codeless-service carve-out at `:1183-1186`
  `app/views/procurement.js:2890-2924` `loadServicesForNewMRF()` — `where('service_code','in',assignedServiceCodes)` / `where('active','==',true)`, plus a `cachedServicesForNewMRF` TTL cache
  `app/views/procurement.js:2926-2955` `loadProjects()` — `where('active','==',true)`, plus a `projectsData` TTL cache
  `app/views/procurement.js:2980-2988` `isMrfInAssignedScope()` — DO NOT EDIT
  `app/views/procurement.js:7940-7994` PO-Delivered activity-feed block — `where('project_name','==',projectName)` at `:7951`, inside an outer "never block the status update — swallow" try/catch

MRF documents carry a denormalized `project_id` since Phase 78 D-04 (written at `mrf-form.js:1722`). It may be absent on pre-Phase-78 documents.

OUT OF SCOPE and explicitly deferred by CONTEXT.md: `procurement.js:3866`, the Create-MRF project picker that builds options from `projectsData.map(...)` with no role gate. Converting `loadProjects()` narrows the data that picker sees for scoped roles as a side effect, but the picker's own missing filter is a separate phase. Do not fix it here and do not add a filter to it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scope both mrf-form.js loaders</name>
  <files>app/views/mrf-form.js</files>
  <read_first>
    - app/views/mrf-form.js lines 400-415 (the `psShowProjects` / `psShowServices` role gating added by quick 260722-msg — context for which roles reach which picker), 1040-1200 (`loadProjects`, `loadServices`, `populateProjectDropdown`, `populateServiceDropdown`, `rebuildPSOptions`)
    - app/views/services.js lines 882-893 (canonical scoped-query pattern)
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-PATTERNS.md Shape B and Shape E
  </read_first>
  <action>
`loadProjects()` (`:1044-1069`): branch before building the query.
  - `window.getAssignedProjectCodes?.() === null` → keep `query(projectsRef, where('active', '==', true))` unchanged.
  - Otherwise, with `const uid = window.getCurrentUser?.()?.uid`: when there is no uid, return without attaching a listener (leave `cachedProjects` as-is / empty — fail-closed). With a uid, build `query(projectsRef, where('personnel_user_ids', 'array-contains', uid))` — a BARE array-contains with no second clause, so no composite index is required.
  - Because the scoped branch drops `where('active','==',true)`, re-apply it inside the existing `snapshot.forEach` post-filter, immediately alongside the existing `if (data.project_status === 'Draft') return;` line: skip documents whose `active !== true` when the scoped branch is in effect. Keep the Draft skip for both branches. Keep the alphabetical `project_code` sort and the `populateProjectDropdown()` call.
  - Keep the existing `onSnapshot` error callback.

`loadServices()` (`:1085-1124`): collapse to the same two branches.
  - Delete the `where('service_code', 'in', assignedCodes)` branch entirely (Shape E retirement, D-13).
  - Scoped branch: bare `query(servicesRef, where('personnel_user_ids', 'array-contains', uid))`, with the same uid guard. Delete the `if (assignedCodes.length === 0) return;` early return — it would prevent the listener from ever attaching, so a later assignment would never appear live.
  - See-all branch: keep `query(servicesRef, where('active', '==', true))`.
  - The `active` filter for the scoped branch is already handled downstream in `rebuildPSOptions` (`:1184-1185` requires `s.active === true`), so no new post-filter is needed here — but keep the existing `project_status === 'Draft'` skip in the snapshot callback for both branches.

`rebuildPSOptions()` (`:1146-1197`): the codeless carve-outs at `:1157-1160` (projects) and `:1183-1186` (services) exist because `syncPersonnelToAssignments` skipped codeless containers. Under the personnel-derived helper they are now redundant for the coded case but remain CORRECT and load-bearing for the codeless case: a codeless assigned project is returned by the array-contains query yet contributes no code to `getAssignedProjectCodes()`, so the `!p.project_code && personnel_user_ids.includes(uid)` clause is exactly what keeps it visible. KEEP both carve-outs. Update their comments to say so and to drop the now-false reference to `syncPersonnelToAssignments`.

Do not touch `psShowProjects` / `psShowServices` or the quick-260722-msg role gating at `:400-415`.
  </action>
  <verify>
    <automated>node --check app/views/mrf-form.js && node -e "const s=require('fs').readFileSync('app/views/mrf-form.js','utf8'); if(/where\('service_code', 'in'/.test(s)) throw new Error('legacy service_code IN query still present'); const n=(s.match(/where\('personnel_user_ids', 'array-contains'/g)||[]).length; if(n<2) throw new Error('expected >=2 array-contains queries, found '+n); if(!/!p\.project_code/.test(s)) throw new Error('codeless project carve-out was removed'); console.log('mrf-form.js OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/mrf-form.js` exits 0
    - Zero occurrences of `where('service_code', 'in'` in the file
    - At least 2 occurrences of `where('personnel_user_ids', 'array-contains'` (one per loader)
    - Neither array-contains query is paired with a second `where(...)` clause on the same `query(...)` call
    - The scoped `loadProjects` branch re-applies the `active` check inside `snapshot.forEach`
    - The `project_status === 'Draft'` skip is present in both loaders' snapshot callbacks
    - `loadServices` no longer contains an `assignedCodes.length === 0` early return
    - `rebuildPSOptions` still contains both codeless carve-outs (`!p.project_code` and `!s.service_code`) and their comments no longer reference `syncPersonnelToAssignments`
    - `psShowProjects` / `psShowServices` logic is unchanged
  </acceptance_criteria>
  <done>The dedicated MRF form's project and service pickers are fed by personnel-scoped queries that need no composite index and that keep codeless assigned containers visible.</done>
</task>

<task type="auto">
  <name>Task 2: Scope both procurement.js loaders without touching the no-leak predicate</name>
  <files>app/views/procurement.js</files>
  <read_first>
    - app/views/procurement.js lines 2610-2630 (the `init()` `Promise.all([loadProjects(), ...])` bootstrap that runs with no role gate), 2886-2990 (`loadServicesForNewMRF`, `loadProjects`, and `isMrfInAssignedScope` with its full quick-260627-kg0 / 260706-mco / 260615-nlj rationale comment)
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-RESEARCH.md "Contradicts CONTEXT.md" item 1
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-PATTERNS.md Shape B and Shape E
  </read_first>
  <action>
`loadProjects()` (`:2926-2955`): apply the identical Shape B conversion as Task 1.
  - Preserve the TTL cache guard at the top (`if (projectsData.length > 0 && (Date.now() - _projectsCachedAt) < CACHE_TTL_MS) return;`).
  - See-all branch (`getAssignedProjectCodes() === null`): keep `query(collection(db,'projects'), where('active','==',true))`.
  - Scoped branch: bare `query(collection(db,'projects'), where('personnel_user_ids','array-contains',uid))` with a uid guard that returns without attaching a listener.
  - Re-apply `active !== true` as a skip inside the existing `snapshot.forEach`, alongside the existing `project_status === 'Draft'` skip.
  - Keep `projectsData.sort(...)`, `_projectsCachedAt`, and `listeners.push(listener)`.
  - Replace the function's doc comment with one that states plainly: this query had NO scoping of any kind before Phase 113; the `projScope` local in `isMrfInAssignedScope` is an unrelated function and never applied here.

`loadServicesForNewMRF()` (`:2890-2924`): apply the Shape E conversion.
  - Delete the `where('service_code', 'in', assignedServiceCodes)` branch and the `if (assignedServiceCodes !== null && assignedServiceCodes.length === 0) return;` early return above it.
  - Scoped branch: bare `query(collection(db,'services'), where('personnel_user_ids','array-contains',uid))` with a uid guard.
  - See-all branch: keep `query(collection(db,'services'), where('active','==',true))`.
  - Because the scoped branch drops the `active` clause, add an `active !== true` skip inside the existing `snapshot.forEach` for the scoped case, alongside the existing `project_status === 'Draft'` skip. Preserve the `cachedServicesForNewMRF` TTL cache and its sort.

`isMrfInAssignedScope()` (`:2980-2988`) — DO NOT EDIT. Not the function body, not its 20-line rationale comment. It is the D-04 no-leak invariant and it is already correct: plan 113-03 repointed the two helpers it calls, so its department-keyed union now operates on personnel-derived codes with zero change to its logic. Append ONE comment line above it recording that Phase 113 deliberately left it untouched and why.

`procurement.js:3866`'s Create-MRF picker is OUT OF SCOPE — do not add a filter to it.
  </action>
  <verify>
    <automated>node --check app/views/procurement.js && node -e "const s=require('fs').readFileSync('app/views/procurement.js','utf8'); if(/where\('service_code', 'in'/.test(s)) throw new Error('legacy service_code IN query still present'); const n=(s.match(/where\('personnel_user_ids', 'array-contains'/g)||[]).length; if(n<2) throw new Error('expected >=2 array-contains queries, found '+n); if(!/function isMrfInAssignedScope\(mrf\) \{/.test(s)) throw new Error('isMrfInAssignedScope signature changed'); if(!/const projOk = \(projScope === null\) \? \(dept === 'projects'\) : projScope\.includes\(mrf\.project_code\);/.test(s)) throw new Error('no-leak union logic was modified'); console.log('procurement.js loaders OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/procurement.js` exits 0
    - Zero occurrences of `where('service_code', 'in'` in the file
    - At least 2 occurrences of `where('personnel_user_ids', 'array-contains'`, neither paired with a second `where(...)` on the same `query(...)` call
    - `isMrfInAssignedScope`'s body is byte-identical to its pre-task state — the exact line `const projOk = (projScope === null) ? (dept === 'projects') : projScope.includes(mrf.project_code);` is still present, as is its `svcOk` twin and the `return projOk || svcOk;`
    - Both loaders retain their TTL cache guards and their `_projectsCachedAt` / `_servicesCachedAt` assignments
    - Both loaders re-apply the `active` filter client-side on the scoped branch
    - `procurement.js:3866`'s picker is unchanged
  </acceptance_criteria>
  <done>Both Procurement-tab data sources are personnel-scoped, and D-04's no-leak predicate is provably untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the PO-Delivered project name lookup with a doc-ID read</name>
  <files>app/views/procurement.js</files>
  <read_first>
    - app/views/procurement.js lines 7930-7995 (the full PO-Delivered block: the NOTIF-18 notification, the Phase 101 D-08 project journal traversal, the Phase 104 D-12 service journal traversal, and the outer swallow try/catch)
    - app/views/mrf-form.js around line 1722 (where `project_id` is denormalized onto the MRF document, Phase 78 D-04) — confirm the exact field name
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-PATTERNS.md Shape D example 2
  </read_first>
  <action>
Inside the Phase 101 D-08 project-journal block, replace the `where('project_name','==',projectName)` list query at `:7951` with a direct document read:
  - When `mrfData.project_id` is a non-empty string, call `getDoc(doc(db, 'projects', mrfData.project_id))`, and when it exists use `projSnap.id` as `projectDocId` for the existing `addDoc(collection(db, 'projects', projectDocId, 'activity_entries'), {...})` call. The activity-entry payload is unchanged.
  - When `mrfData.project_id` is absent (pre-Phase-78 MRF), do NOT fall back to the name-based list query — skip the journal entry and log with the existing `[Procurement]` prefix. RESEARCH assumption A4 flags the legacy-coverage rate as unverified; this whole block is already best-effort inside a "never block the status update — swallow" try/catch, so skipping is the correct degradation and avoids leaving an unsatisfiable list query in the code.
  - Keep the outer `try`/`catch (journalErr)` with its "Never block the status update — swallow" comment exactly as it is.
  - Ensure `getDoc` and `doc` are in this file's `../firebase.js` import list; add them if missing.

Leave the Phase 104 D-12 SERVICE journal traversal at `:7966-7988` alone, including its `where('service_code','==',serviceCode)` query against `services` and its own inner try/catch. That query is reachable only by roles with `procurement_records` edit rights, all of which are in the `services` list rule's exempt set both before and after this phase, so it is not broken by the tightening. Add a comment recording that reasoning so a future reader does not convert it speculatively.
  </action>
  <verify>
    <automated>node --check app/views/procurement.js && node -e "const s=require('fs').readFileSync('app/views/procurement.js','utf8'); if(/where\('project_name', '==', projectName\)/.test(s)) throw new Error('PO-Delivered name lookup still present'); if(!/getDoc\(doc\(db, 'projects', /.test(s)) throw new Error('doc-ID lookup missing'); if(!/where\('service_code', '==', serviceCode\)/.test(s)) throw new Error('service journal traversal was wrongly modified'); console.log('PO-Delivered OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/procurement.js` exits 0
    - Zero occurrences of `where('project_name', '==', projectName)` in the file
    - The project-journal block contains `getDoc(doc(db, 'projects', ` keyed on the MRF's `project_id`
    - No name-based `projects` fallback query was added for the missing-`project_id` case
    - The Phase 104 D-12 service journal block still contains `where('service_code', '==', serviceCode)` and its own inner try/catch, with a new comment explaining why it was left alone
    - The outer `catch (journalErr)` and its "Never block the status update — swallow" comment are unchanged
    - `getDoc` and `doc` are present in the file's `../firebase.js` import list
  </acceptance_criteria>
  <done>The PO-Delivered journal entry resolves its project by document ID rather than by a list query, removing the last `where('project_name','==',...)` read against `projects`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MRF picker data source → what a user can file against | The set of projects/services a user can attach an MRF to is derived from these caches. Narrowing them narrows the filing surface. |
| MRF document `project_id` → journal write target | A denormalized foreign key now drives which project document receives an activity entry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-26 | Information Disclosure | `procurement.js:3866` Create-MRF picker showing all active projects | accept (deferred) | CONTEXT.md `<deferred>` scopes this to its own phase. Converting `loadProjects()` shrinks the data the picker sees for scoped roles as a side effect, so the exposure gets strictly smaller, but the picker's missing role gate is NOT fixed here and must not be silently "tidied". |
| T-113-27 | Tampering | `mrfData.project_id` steering the journal `getDoc` and subsequent `addDoc` | mitigate | The id is read from an MRF document the actor could already read, and the subsequent `addDoc` into `projects/{id}/activity_entries` is governed by that subcollection's own create rule. The change swaps one server-gated path for another; it does not add a client-chosen target. |
| T-113-28 | Repudiation | dropping the journal entry for pre-Phase-78 MRFs | accept | The entire block is already best-effort inside a swallow try/catch — before this change the same entry was silently skipped whenever the name lookup came back empty. The degradation is unchanged in kind and now logs explicitly. |
| T-113-29 | Denial of Service (self-inflicted) | editing `isMrfInAssignedScope` and breaking the 260615-nlj no-leak invariant | mitigate | The function is declared off-limits, and the acceptance criteria assert the exact union lines are still present verbatim. D-04 requires the invariant to carry over unchanged; the safest way to satisfy that is to not touch it. |
| T-113-30 | Information Disclosure | scoped branch dropping `active` and surfacing inactive containers | mitigate | Both loaders re-apply `active !== true` as a client-side skip on the scoped branch. Asserted in acceptance criteria for all three converted loaders. |
| T-113-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed. |
</threat_model>

<verification>
- `node --check` passes on both files
- Zero `where('service_code', 'in'` and zero `where('project_name', '==', projectName)` across both files
- `isMrfInAssignedScope`'s union lines present verbatim
- No array-contains query in either file is paired with a second `where(...)` clause (no new composite index needed)
</verification>

<success_criteria>
Both MRF-filing data sources are personnel-scoped without requiring a new index, the services-side legacy `in` queries are gone from both files, the PO-Delivered journal uses a doc-ID read, and D-04's no-leak predicate is provably untouched.
</success_criteria>

<output>
Create `.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-06-SUMMARY.md` when done. Record the final branch structure of all four converted loaders and confirm the `isMrfInAssignedScope` diff is comment-only.
</output>
