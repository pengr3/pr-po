---
phase: 113-assignment-source-of-truth-and-project-read-enforcement
plan: 07
type: execute
wave: 4
depends_on: ["113-03"]
files_modified:
  - app/proposal-modal.js
  - app/views/clients.js
  - app/engagement-create.js
autonomous: true
requirements: [D-02, D-08, D-11, D-12, D-13, D-16]

must_haves:
  truths:
    - "D-02: `proposal-modal.js` no longer issues a bare `getDocs(collection(db,'projects'))` — the scoped-role Start-Proposal path skips the projects fetch entirely, and any remaining fetch is personnel-scoped"
    - "D-16: `clients.js` showClientDetail treats `services_admin` as scoped for projects and `operations_admin` as scoped for services, pairing each `client_code` equality lookup with `personnel_user_ids array-contains` for scoped actors"
    - "D-13: the `clients.js` services-side lookup is converted alongside the projects-side one, so the client detail modal works identically for both departments"
    - "D-08: `engagement-create.js` invokes neither `syncPersonnelToAssignments` nor `syncServicePersonnelToAssignments`, and imports neither"
    - "D-11 and D-12: the personnel array written by `createEngagement` is part of its awaited container-document create — after removing the `onAfterCreate` sync tail, no fire-and-forget assignment write remains on any creation path"
  artifacts:
    - path: "app/proposal-modal.js"
      provides: "no unscoped projects collection fetch"
    - path: "app/views/clients.js"
      provides: "personnel-paired client detail lookups for both projects and services"
      contains: "array-contains"
    - path: "app/engagement-create.js"
      provides: "creation path with zero assignment sync calls"
  key_links:
    - from: "app/proposal-modal.js openCreateProposalModal"
      to: "_loadModalDropdownData"
      via: "skipped when preselectedProjectId locks the dropdown to one project"
      pattern: "preselectedProjectId"
    - from: "app/views/clients.js showClientDetail"
      to: "projects.personnel_user_ids / services.personnel_user_ids"
      via: "paired client_code equality + array-contains queries"
      pattern: "where\\('personnel_user_ids', 'array-contains'"
---

<objective>
Close the last three read surfaces and the last two sync call sites.

`proposal-modal.js:212` is the worst query shape in the whole audit — `getDocs(collection(db,'projects'))` with zero predicates, reachable by `operations_user`, `services_user` and `services_admin` through the "Start Proposal" button, which `project-detail.js:2486-2490` renders whenever `canDrive` is true and `personnel_user_ids.includes(uid)`. It also happens to be unnecessary on exactly that path: when `preselectedProjectId` is supplied the dropdown is immediately locked to one project (`proposal-modal.js:700-711`, `projectSelectEl.disabled = true`), so the full list is fetched and then discarded.

`clients.js:243-244` is reachable by all seven roles (the `clients` tab is `access:true` universally and every row carries an unguarded `onclick`), and its two `where('client_code','==',...)` lookups are unsatisfiable under an array-contains-only rule.

`engagement-create.js:400,403` are the last two of the eleven fire-and-forget sync invocations.

Purpose: finish the conversion set so plan 113-09's D-03 completeness check can pass.
Output: three files with no unscoped `projects` read and no assignment sync call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONTEXT.md
@.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-RESEARCH.md
@.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
Deployed in plan 113-02 and required by this plan:
  composite index `projects`: `client_code` ASC + `personnel_user_ids` CONTAINS
  composite index `services`: `client_code` ASC + `personnel_user_ids` CONTAINS

`app/utils.js` after plan 113-03: `getAssignedProjectCodes()` / `getAssignedServiceCodes()` — `null` = see-all, personnel-derived `string[]` otherwise.

`app/proposal-modal.js`:
  `:208-227` `_loadModalDropdownData()` — guarded by `_modalProjectsLoaded`, fetches `projects` and `clients` in a `Promise.all`, filters out `project_status === 'Draft'` and `active === false`, sorts, caches into `_modalProjectsData` / `_modalClientsData`
  `:694-713` the create-modal opener — calls `await _loadModalDropdownData()` then, when `preselectedProjectId` is set, inserts a synthetic option, sets the value and sets `projectSelectEl.disabled = true`
  `:165-200` `_isCallerAttachedToProposalParent()` / `canApprove` / `canDrive` — role and personnel gating, NOT edited by this plan

`app/views/clients.js:240-252` `showClientDetail()` — a `Promise.all` of two `getDocs(query(...))` calls filtered by `client_code`, feeding `linkedProjects` and `linkedServices`.

`app/engagement-create.js`:
  `:16-28` module header comment + the `syncPersonnelToAssignments` / `syncServicePersonnelToAssignments` named imports from `./utils.js`
  `:396-406` the `onAfterCreate` callback passed to `createEngagement`, containing both sync calls
  `createEngagement` itself is defined in this file and is called from 3 sites (`services.js`, `projects.js`, and this file's own Proposals-tab form).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Stop proposal-modal.js fetching the whole projects collection</name>
  <files>app/proposal-modal.js</files>
  <read_first>
    - app/proposal-modal.js lines 160-230 (`_isCallerAttachedToProposalParent`, `canApprove`/`canDrive`, `_loadModalDropdownData` with its module-scope caches and its `_modalProjectsLoaded` guard)
    - app/proposal-modal.js lines 685-715 (the create-modal opener, the `preselectedProjectId` / `lockedProjectCode` handling and the `projectSelectEl.disabled = true` line)
    - app/views/project-detail.js around line 2486-2510 (the `canDrive` gate that renders the Start Proposal button for `operations_user` / `services_user` / `services_admin`)
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-PATTERNS.md Shape A, including its note that scoping this query is the SECOND-choice fix
  </read_first>
  <action>
Two changes, in this order of preference:

  1. Skip the fetch on the preselected path. Give `_loadModalDropdownData()` a parameter such as `skipProjects` (default `false`). In the create-modal opener at `:698`, when `preselectedProjectId` is truthy, call it with projects skipped — the dropdown is locked to one project three lines later, so the list is never used. Ensure the `clients` half of the `Promise.all` still runs on that path (the modal needs `_modalClientsData`). Keep the `_modalProjectsLoaded` guard semantics coherent: a skipped-projects load must NOT mark projects as loaded, so a subsequent unpreselected open still fetches them.

  2. Scope the fetch that remains. On the path where projects ARE loaded, branch exactly like the other converted sites: `window.getAssignedProjectCodes?.() === null` keeps the existing `getDocs(collection(db, 'projects'))`; otherwise, with a resolvable uid, use `getDocs(query(collection(db, 'projects'), where('personnel_user_ids', 'array-contains', uid)))` — a bare array-contains, no second clause, no new index. With no uid, set `_modalProjectsData = []` and continue (fail-closed) rather than falling through to the unscoped fetch.
     Import `query` and `where` from `./firebase.js` if they are not already imported here.

Keep the existing post-fetch filtering (`project_status === 'Draft'` skip, `active === false` skip) and the sort, unchanged, for both branches.

Do not touch `_isCallerAttachedToProposalParent`, `canApprove`, `canDrive`, or any proposal state-transition code.
  </action>
  <verify>
    <automated>node --check app/proposal-modal.js && node -e "const s=require('fs').readFileSync('app/proposal-modal.js','utf8'); if(!/where\('personnel_user_ids', 'array-contains'/.test(s)) throw new Error('scoped fallback query missing'); const bare=/getDocs\(collection\(db, 'projects'\)\)/.test(s); const guarded=/getAssignedProjectCodes/.test(s); if(bare && !guarded) throw new Error('unscoped projects fetch is not behind a see-all guard'); console.log('proposal-modal.js OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/proposal-modal.js` exits 0
    - `_loadModalDropdownData` accepts a parameter that suppresses the projects fetch, and the create-modal opener passes it when `preselectedProjectId` is truthy
    - A skipped-projects load does not set `_modalProjectsLoaded` to a value that would prevent a later real load
    - Any surviving `getDocs(collection(db, 'projects'))` sits inside a branch gated on `getAssignedProjectCodes() === null`
    - The scoped branch uses `where('personnel_user_ids', 'array-contains', uid)` with no second `where(...)` clause
    - The `clients` fetch still runs on the preselected path
    - The `project_status === 'Draft'` and `active === false` filters and the sort are unchanged
    - `_isCallerAttachedToProposalParent`, `canApprove` and `canDrive` are unchanged
  </acceptance_criteria>
  <done>The Start-Proposal flow used by scoped roles performs no `projects` collection read at all, and the unpreselected path is personnel-scoped.</done>
</task>

<task type="auto">
  <name>Task 2: Pair both clients.js client_code lookups with the personnel predicate</name>
  <files>app/views/clients.js</files>
  <read_first>
    - app/views/clients.js lines 228-255 (`showClientDetail`'s guard, `showLoading(true)`, the `Promise.all` of the two `client_code` lookups, and how `linkedProjects` / `linkedServices` are consumed)
    - app/views/clients.js around line 455-465 (the table row `onclick="window.showClientDetail(...)"` with no role gate — the reachability evidence)
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-PATTERNS.md Shape C variant
  </read_first>
  <action>
Both lookups in the `Promise.all` at `clients.js:242-245` must be branched independently, because a single actor can be scoped on one dimension and see-all on the other (a `services_admin` is scoped for projects and see-all for services; an `operations_admin` is the mirror).

  - Projects lookup: `window.getAssignedProjectCodes?.() === null` → keep `query(collection(db,'projects'), where('client_code','==', client.client_code))`. Otherwise, with a resolvable uid, use `query(collection(db,'projects'), where('client_code','==', client.client_code), where('personnel_user_ids','array-contains', uid))` — served by the `projects` `client_code` + `personnel_user_ids` composite index deployed in 113-02.
  - Services lookup: mirror image using `window.getAssignedServiceCodes?.()` and the `services` composite index.
  - With no resolvable uid on a scoped dimension, substitute an already-resolved empty result for that half of the `Promise.all` rather than issuing a query (fail-closed). Do not let either dimension fall through to its unscoped form.
  - Keep the `Promise.all` structure, the `showLoading(true)` / `showLoading(false)` pairing, the `linkedProjects` / `linkedServices` accumulation, and every downstream render unchanged.
  - Add a comment recording the consequence for the user: a scoped actor opening a client detail now sees only the projects and services of that client that they are personnel on. That is the intended posture — a scoped user should not see a client's full roster.

Import `query` and `where` from `../firebase.js` if not already present.
  </action>
  <verify>
    <automated>node --check app/views/clients.js && node -e "const s=require('fs').readFileSync('app/views/clients.js','utf8'); const n=(s.match(/where\('personnel_user_ids', 'array-contains'/g)||[]).length; if(n<2) throw new Error('expected 2 array-contains clauses (projects + services), found '+n); if(!/getAssignedProjectCodes/.test(s)||!/getAssignedServiceCodes/.test(s)) throw new Error('both dimension helpers must be consulted'); console.log('clients.js OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/clients.js` exits 0
    - Exactly 2 `where('personnel_user_ids', 'array-contains', uid)` clauses exist, one on the `projects` query and one on the `services` query
    - Each scoped clause sits on the SAME `query(...)` call as its `where('client_code', '==', ...)` clause
    - The projects branch consults `getAssignedProjectCodes()` and the services branch consults `getAssignedServiceCodes()` — the two dimensions are decided independently
    - Neither dimension falls back to its unscoped query when uid is unresolvable
    - The `Promise.all` shape, `showLoading` pairing and all downstream rendering are unchanged
  </acceptance_criteria>
  <done>The client detail modal issues only queries a scoped actor can satisfy, on both dimensions independently.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the last two sync call sites from the shared creation path</name>
  <files>app/engagement-create.js</files>
  <read_first>
    - app/engagement-create.js lines 1-60 (module header comment explaining the `onAfterCreate` rationale, the `./utils.js` import list, and `createEngagement`'s signature and options contract)
    - app/engagement-create.js lines 375-415 (the Proposals-tab `createEngagement` call with the `onAfterCreate` callback containing both sync calls)
    - app/views/projects.js and app/views/services.js as edited by plan 113-04 (the other two `createEngagement` call sites, whose `onAfterCreate` handling must stay consistent with whatever is decided here)
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-PATTERNS.md section 5 ("Creation-time call site")
  </read_first>
  <action>
Delete the entire `onAfterCreate` callback body at `:396-406` — both the `createdType === 'project'` branch calling `syncPersonnelToAssignments` and the `(createdType === 'one-time' || createdType === 'recurring')` branch calling `syncServicePersonnelToAssignments`, plus the `const userIds = selectedPersonnel.map(...)` local that exists only to feed them.

Then decide the fate of the `onAfterCreate` option itself by reading `createEngagement`'s own implementation in this file:
  - If `onAfterCreate` has no other consumer once all three call sites have dropped their sync bodies (plan 113-04 removes the other two), remove the option from this call site AND remove its invocation and its documentation from `createEngagement`'s implementation and header comment.
  - If `createEngagement` invokes it unconditionally or another caller still supplies a meaningful body, keep the parameter and simply omit it at this call site.
  Record which branch was taken, and why, in the SUMMARY — do not guess.

Remove `syncPersonnelToAssignments` and `syncServicePersonnelToAssignments` from the `./utils.js` named import list at `:27-28`, and rewrite the module header comment at `:16-17` that currently explains the project/service sync split — it describes a mechanism that no longer exists. Replace it with a sentence recording that `personnel_user_ids` written on the container document at create time IS the assignment, per D-08.

Do not change `createEngagement`'s code-generation logic, its `generateProjectCode` / `generateServiceCode` calls, or the personnel array it writes onto the new container document. That write is the awaited, authoritative assignment record.
  </action>
  <verify>
    <automated>node --check app/engagement-create.js && node -e "const s=require('fs').readFileSync('app/engagement-create.js','utf8'); if(/syncPersonnelToAssignments|syncServicePersonnelToAssignments/.test(s)) throw new Error('sync reference still present in engagement-create.js'); if(!/personnel_user_ids/.test(s)) throw new Error('personnel write appears to have been removed'); console.log('engagement-create.js OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/engagement-create.js` exits 0
    - Zero occurrences of `syncPersonnelToAssignments` or `syncServicePersonnelToAssignments` anywhere in the file, including the import list and the header comment
    - `createEngagement` still writes `personnel_user_ids` onto the newly created container document
    - `generateProjectCode` / `generateServiceCode` invocations are unchanged
    - The header comment no longer describes a project/service assignment-sync split
    - The SUMMARY records whether `onAfterCreate` was removed from `createEngagement`'s contract or retained, with the evidence that decided it
  </acceptance_criteria>
  <done>All eleven fire-and-forget sync invocations identified in RESEARCH.md are now removed from the codebase; only the two function definitions in `app/utils.js` remain, and plan 113-09 deletes those.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Start-Proposal button → projects dropdown data | Rendered for scoped roles gated on `personnel_user_ids.includes(uid)`, but the data it loaded was unfiltered. |
| client row click → linked project/service roster | The `clients` tab is universally accessible and the row `onclick` has no role gate, so this is a genuinely all-roles surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-31 | Information Disclosure | `getDocs(collection(db,'projects'))` in the proposal modal | mitigate | Eliminated on the scoped-role path (fetch skipped when the dropdown is locked) and personnel-scoped on the remaining path. Zero-predicate collection reads are the shape the tightened rule denies outright. |
| T-113-32 | Information Disclosure | client detail exposing a client's full project/service roster to a scoped actor | mitigate | Both lookups gain the personnel clause for scoped actors. The narrowed result is the intended posture and is recorded in a code comment so it is not later "fixed" back. |
| T-113-33 | Denial of Service (self-inflicted) | independent dimensions collapsed into one branch decision | mitigate | Task 2 requires the projects and services dimensions to be decided by their own helper. Collapsing them would break `services_admin` (scoped on projects, see-all on services) and `operations_admin` (the mirror) simultaneously. |
| T-113-34 | Denial of Service (self-inflicted) | `_modalProjectsLoaded` marked true after a skipped load | mitigate | Explicitly called out in Task 1's action and asserted in its acceptance criteria; otherwise the first preselected open would poison the cache for every later unpreselected open. |
| T-113-35 | Repudiation | removing `onAfterCreate` without confirming its contract | mitigate | Task 3 forbids guessing: the executor must read `createEngagement`'s implementation and record which branch was taken and why. |
| T-113-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed. |
</threat_model>

<verification>
- `node --check` passes on all three files
- Zero `syncPersonnelToAssignments` / `syncServicePersonnelToAssignments` references in `app/engagement-create.js`
- 2 `array-contains` clauses in `clients.js`, each paired with its `client_code` equality on the same query
- No bare `getDocs(collection(db,'projects'))` outside a see-all guard in `proposal-modal.js`
</verification>

<success_criteria>
Every MUST-CONVERT read site from RESEARCH.md's audit is converted, and every fire-and-forget assignment sync invocation is removed from the codebase.
</success_criteria>

<output>
Create `.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-07-SUMMARY.md` when done. Record the `onAfterCreate` decision and its evidence, and the final `_loadModalDropdownData` signature.
</output>
