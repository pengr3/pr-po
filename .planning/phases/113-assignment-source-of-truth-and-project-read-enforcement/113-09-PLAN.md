---
phase: 113-assignment-source-of-truth-and-project-read-enforcement
plan: 09
type: execute
wave: 5
depends_on: ["113-04", "113-05", "113-06", "113-07", "113-08"]
files_modified:
  - app/utils.js
  - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONVERSION-AUDIT.md
autonomous: false
requirements: [D-03, D-04, D-08, D-11, D-12]

must_haves:
  truths:
    - "D-08: `syncPersonnelToAssignments` and `syncServicePersonnelToAssignments` no longer exist anywhere in the codebase — neither definition, nor export, nor window registration, nor import, nor call site"
    - "D-08: the `assigned_project_codes` / `assigned_service_codes` FIELDS are deliberately NOT deleted from any user document — no migration, cleanup script or field delete is performed in this phase, preserving the rollback path"
    - "D-11: retiring the syncs IS the silent-failure fix — after this plan there is exactly one write per assignment (the container document's personnel array) and it is awaited and toasted at every call site"
    - "D-12: a documented inspection sweep proves no fire-and-forget error swallow remains on any assignment-affecting write path, with the exact commands and results recorded so the claim is reproducible"
    - "D-03: a completeness audit maps every MUST-CONVERT site from 113-RESEARCH.md's table to its converted state, so no surface is left to become a hard denial when the rule tightens"
    - "D-04: production verification confirms the 260615-nlj no-leak invariant still holds in the browser — an unassigned AND codeless cross-department item stays hidden from a cross-department admin on MRF Records"
  artifacts:
    - path: "app/utils.js"
      provides: "no assignment sync helpers"
    - path: ".planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONVERSION-AUDIT.md"
      provides: "site-by-site D-03 completeness map and the D-12 sweep evidence"
  key_links:
    - from: "113-RESEARCH.md MUST CONVERT table"
      to: "113-CONVERSION-AUDIT.md"
      via: "one row per site with its converted query shape and the plan that converted it"
      pattern: "MUST CONVERT"
---

<objective>
Close the client-side half of the phase, prove it is closed, then verify in production BEFORE anything is tightened.

This plan is the hinge. Everything before it is a strict narrowing that still works correctly under today's permissive `projects` rule. Everything after it is irreversible tightening. `113-RESEARCH.md`'s Rules Mechanics section establishes that Firestore `list` denial is total, so any surface the conversion missed becomes a hard failure the instant the rule changes — not a degraded one. The audit and the browser check are how a miss gets found while it is still free to fix.

The two sync functions can only be deleted now, in this wave, because five modules imported them until plans 113-04 through 113-08 landed. Deleting them earlier would have been an ES module resolution failure that breaks the entire SPA on load.

Purpose: prove D-03 completeness and D-12 cleanliness, then confirm in the live app that the narrowed queries return correct data while the permissive rule is still in force.
Output: a sync-free `app/utils.js`, a reproducible audit document, and an operator-confirmed production baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONTEXT.md
@.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-RESEARCH.md
@.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete both sync helpers from app/utils.js</name>
  <files>app/utils.js</files>
  <read_first>
    - app/utils.js lines 700-851 (the PERSONNEL-ASSIGNMENT SYNC section banner, `syncPersonnelToAssignments`, `syncServicePersonnelToAssignments`, and the CSV EXPORT UTILITIES banner that follows)
    - app/utils.js lines 1-10 (the `./firebase.js` import list — `getDoc`, `updateDoc`, `arrayUnion`, `arrayRemove` were used by the deleted functions and may no longer be needed)
    - app/utils.js lines 620-655 (`window.utils` registry and the `window.*` assignments, including `window.syncServicePersonnelToAssignments`)
    - The four Wave-4 SUMMARY files (113-04, 113-05, 113-06, 113-07) — confirm every importer dropped its named import before the exports are deleted
  </read_first>
  <action>
Delete `syncPersonnelToAssignments` (lines 716-776) and `syncServicePersonnelToAssignments` (lines 790-849) in full, together with their JSDoc blocks and the PERSONNEL-ASSIGNMENT SYNC section banner at lines 700-702. Delete the `window.syncServicePersonnelToAssignments` registration at line 652.

Then prune the `./firebase.js` import list on line 6 to exactly what `app/utils.js` still uses. Determine this by inspection, not assumption. `arrayUnion` and `arrayRemove` were used only by the deleted functions and are likely now unused here. `getDoc`, `updateDoc`, `doc`, `getDocs`, `query`, `where`, `collection` and `onSnapshot` may still be used by `generateProjectCode`, `generateServiceCode`, `getAllSuppliers` and the plan-113-03 personnel listeners. Remove only symbols with zero remaining references in the file.

Do NOT delete or modify:
  - `getAssignedProjectCodes` / `getAssignedServiceCodes` or the personnel cache added by plan 113-03
  - `PROJECT_SEE_ALL_ROLES` / `SERVICE_SEE_ALL_ROLES`
  - `generateProjectCode` (269-309) or `generateServiceCode` (359-399), including their `projects` range-scan queries — the rules posture that keeps `generateServiceCode` working for `services_admin` is decided in plan 113-10
  - `getActiveProjects` (428) — RESEARCH.md flags it as dead code with zero call sites, but deleting it is out of scope; leave it and record it as a cleanup candidate in the SUMMARY
  - `normalizePersonnel` or anything else in the PERSONNEL UTILITIES section

Do NOT write any migration, cleanup script or field delete against `assigned_project_codes` / `assigned_service_codes` on user documents. D-08 keeps those fields as frozen, inert rollback insurance and CONTEXT.md `deferred` scopes their removal to a later phase.
  </action>
  <verify>
    <automated>node --check app/utils.js && node --check app/views/projects.js && node --check app/views/services.js && node --check app/views/project-detail.js && node --check app/views/service-detail.js && node --check app/engagement-create.js && node --check app/views/assignments.js && node --check app/views/mrf-form.js && node --check app/views/procurement.js && node --check app/proposal-modal.js && node --check app/views/clients.js && node --check app/expense-modal.js && node --check app/views/project-plan.js && node --check app/views/user-management.js && node --check app/auth.js && node -e "const s=require('fs').readFileSync('app/utils.js','utf8'); if(/syncPersonnelToAssignments|syncServicePersonnelToAssignments/.test(s)) throw new Error('sync helper still present in utils.js'); if(!/getAssignedProjectCodes/.test(s)) throw new Error('helper wrongly deleted'); console.log('utils.js sync-free');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` exits 0 for all 15 listed JS files (a stale named import anywhere in the graph is the failure this catches)
    - `app/utils.js` contains zero occurrences of `syncPersonnelToAssignments` and zero of `syncServicePersonnelToAssignments`
    - `git grep -n "syncPersonnelToAssignments\|syncServicePersonnelToAssignments" -- app scripts index.html` returns no matches (record the command output in the SUMMARY)
    - `app/utils.js` still exports `getAssignedProjectCodes`, `getAssignedServiceCodes`, `initAssignedCodesListeners`, `destroyAssignedCodesListeners`, `generateProjectCode`, `generateServiceCode`, `normalizePersonnel`
    - The `./firebase.js` import list contains no symbol with zero references in the file
    - No file in the repository performs a delete or overwrite of `assigned_project_codes` / `assigned_service_codes` on a user document
  </acceptance_criteria>
  <done>Both sync pipelines are gone from the codebase and every module in the import graph still parses.</done>
</task>

<task type="auto">
  <name>Task 2: Produce the D-03 completeness audit and the D-12 inspection sweep</name>
  <files>.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONVERSION-AUDIT.md</files>
  <read_first>
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-RESEARCH.md — the "MUST CONVERT" table (13 rows) and the "SAFE" table, in full
    - The five Wave-4 SUMMARY files plus 113-03-SUMMARY.md — what each plan actually changed
    - app/views/procurement.js around line 2980 (`isMrfInAssignedScope`) — confirm it is comment-only changed
  </read_first>
  <action>
Create `113-CONVERSION-AUDIT.md` with three sections.

Section 1 — D-03 completeness map. One row per site in RESEARCH.md's MUST CONVERT table, in the same numbering (1 through 13), with columns: site (`file:line`), original query shape, shape class (A/B/C/D/E per PATTERNS.md), converted query shape as it now stands in the code, and the plan that converted it. Every row must read CONVERTED or, for row 13 (`generateProjectCode`) and row 12 (`generateServiceCode`), NO CHANGE REQUIRED with the reason stated. Add rows for the sites this phase converted that were NOT in RESEARCH.md's table — the services-side Shape E sites (`services.js` home-department branch, `mrf-form.js` loadServices, `procurement.js` loadServicesForNewMRF), `clients.js`'s services-side lookup, and the `app/utils.js` helper repoint itself. If ANY row cannot be marked CONVERTED, stop and report it rather than writing the document with a gap.

Section 2 — D-12 inspection sweep. Run and record, verbatim, the output of each of these searches across `app/` and `scripts/`:
  (a) every occurrence of `.catch(` on a line or in a statement that also mentions `personnel`, `assign`, `Assignment` or `Personnel`
  (b) every occurrence of `updateDoc(` whose statement writes `personnel_user_ids`, together with whether it is preceded by `await`
  (c) every occurrence of `console.error` inside a `.catch(` arrow on an assignment path
For each hit, classify it as: assignment-affecting (a D-12 defect, must be fixed before this plan completes) or non-assignment (permitted — `recordEditHistory`, `last_activity_at` bumps and journal auto-entries are explicitly out of D-12's scope per CONTEXT.md D-12's wording and PATTERNS.md's Shared Patterns note). Record the classification and the reasoning for every hit. The expected result is zero assignment-affecting hits.

Section 3 — residual and deferred. State plainly, for the record:
  - the `assigned_project_codes` / `assigned_service_codes` fields still exist on user documents and are now inert (D-08), with no migration performed and none needed — project and service documents were always written correctly under the old model, so repointing reads self-heals historical assignments (CONTEXT.md `specifics`, "no migration required")
  - `procurement.js:3866`'s Create-MRF picker remains unscoped and is deferred to its own phase
  - `app/utils.js:428` `getActiveProjects` is dead code, left in place, flagged for opportunistic cleanup
  - `isMrfInAssignedScope` was changed by comment only — include the `git diff` for that function
  </action>
  <verify>
    <automated>test -f .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONVERSION-AUDIT.md && node -e "const s=require('fs').readFileSync('.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONVERSION-AUDIT.md','utf8'); const rows=(s.match(/CONVERTED/g)||[]).length; if(rows<11) throw new Error('expected at least 11 CONVERTED rows, found '+rows); if(/PENDING|TODO|UNKNOWN/i.test(s)) throw new Error('audit contains an unresolved marker'); console.log('audit OK, CONVERTED rows:',rows);"</automated>
  </verify>
  <acceptance_criteria>
    - `113-CONVERSION-AUDIT.md` exists and contains all three sections
    - Section 1 has a row for every one of RESEARCH.md's 13 MUST CONVERT sites, each marked CONVERTED or NO CHANGE REQUIRED with a stated reason — no row left blank, PENDING, TODO or UNKNOWN
    - Section 1 additionally covers the services-side Shape E sites, the `clients.js` services lookup and the `app/utils.js` helper repoint
    - Section 2 records the verbatim output of all three searches and classifies every hit
    - Section 2 reports ZERO assignment-affecting fire-and-forget hits; if any exist, they were fixed and the fix is named
    - Section 3 states the no-migration position, the deferred `procurement.js:3866` picker, the dead `getActiveProjects`, and includes the `isMrfInAssignedScope` diff showing comment-only change
    - The document contains no unresolved marker (`PENDING`, `TODO`, `UNKNOWN`)
  </acceptance_criteria>
  <done>D-03 completeness and D-12 cleanliness are documented with reproducible evidence rather than asserted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Production verification BEFORE any rule tightens</name>
  <files>(none — browser verification against production Firebase)</files>
  <read_first>
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONVERSION-AUDIT.md (produced by Task 2 — the list of surfaces to exercise)
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-RESEARCH.md "Manual browser UAT" section (the drafted acceptance narrative)
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONTEXT.md `specifics` (the operations_admin vs super_admin experiment that is the canonical acceptance narrative)
  </read_first>
  <action>
Serve the app against production Firebase and walk the 8 numbered verification steps below, in order, with DevTools Console open throughout. Record a PASS or FAIL for each step plus the full text of every console error. Change no files. Do NOT proceed to plan 113-10 unless all 8 steps pass — the rule tightening there converts any failure here into a total list denial.
  </action>
  <what-built>
Every client-side query that reads `projects` or `services` now issues a strict narrowing keyed on `personnel_user_ids`, both assignment sync pipelines are deleted, the Assignments tab writes personnel directly, and `getAssignedProjectCodes()` / `getAssignedServiceCodes()` are personnel-derived. The `projects` read rule is STILL the permissive `allow read: if isActiveUser()` — nothing has been tightened. That is deliberate: these narrowed queries must be proven correct while the permissive rule still admits any shape, so a mistake shows up as wrong DATA (recoverable) rather than as a total denial (a live outage).
  </what-built>
  <how-to-verify>
Serve the app locally against production Firebase (`python -m http.server 8000`) or use the deployed site. Keep DevTools Console open throughout and report any `permission-denied` or `The query requires an index` message with its full text.

1. **The canonical acceptance narrative (CONTEXT.md `specifics`).** As an `operations_admin`, assign a `services_user` to a project via the project-detail Personnel panel. Then, logged in separately as that `services_user`, WITHOUT any re-save, removal or re-add:
   - `#/projects` lists the project
   - the dedicated MRF form's project/service picker offers it
   This is the defect that produced this phase. Previously it worked only when a `super_admin` performed the assignment.

2. **No migration required.** Pick a user assigned BEFORE this phase shipped and confirm their existing projects and services still appear on `#/projects`, `#/services`, the MRF form picker and the Procurement tab. Repointing reads must self-heal historical data with zero backfill.

3. **Assignments tab round trip.** As `operations_admin`, open Assignments → Projects → Manage for a user, check and uncheck projects, save. Confirm: the success toast appears, the Assignment Count column updates, and the affected project's Personnel panel reflects the change. Repeat as `services_admin` on the Services sub-tab.

4. **Cross-department row visibility (D-10 site).** After assigning a `services_user` to a project via the Personnel panel only, confirm that user still appears as a manageable row in Assignments → Projects.

5. **Service creation still works (RESEARCH.md's highest-severity finding).** As `services_admin`, create a new service. It must succeed and receive a `CLMC-...` code. A failure here shows as the toast "Failed to create service".

6. **D-04 no-leak invariant.** As a cross-department admin (`services_admin` on the projects side, or `operations_admin` on the services side), open Procurement → MRF Records and confirm an unassigned AND codeless cross-department item is NOT listed. This is the 260615-nlj invariant and it must be unchanged.

7. **Escape hatch (D-09).** If any account holds `all_projects: true` or `all_services: true`, confirm it still sees everything on the corresponding list view.

8. **Sweep the converted surfaces for console errors.** Visit `#/projects`, a project detail page, its Plan page, `#/services`, a service detail page, `#/clients` → open a client detail, Procurement → MRF Records and Create MRF, the dedicated MRF form, and open a Full Breakdown modal from project detail. Report every console error.

If any step fails, do NOT proceed to plan 113-10 — the rule tightening would convert the failure into a total denial. Run `/gsd:plan-phase 113 --gaps` instead.
  </how-to-verify>
  <verify>
    <automated>MISSING — there is no browser test harness in this zero-build SPA. The emulator suite covers rules only; these are client query and UI behaviours against production data.</automated>
    <human-check>Operator confirms all 8 steps pass with zero `permission-denied` and zero missing-index console errors.</human-check>
  </verify>
  <acceptance_criteria>
    - Step 1 passes: a `services_user` assigned by an `operations_admin` sees the project immediately on `#/projects` AND in the MRF form picker, with no removal/re-add workaround
    - Step 2 passes: at least one pre-existing assignment for at least one user still resolves on all four surfaces, with no backfill performed
    - Step 3 passes on BOTH sub-tabs: save succeeds, count updates, the container's Personnel panel reflects it
    - Step 4 passes: the personnel-only-assigned cross-department user is still a manageable row
    - Step 5 passes: `services_admin` creates a service successfully and it receives a code
    - Step 6 passes: the unassigned codeless cross-department item is absent from MRF Records for the cross-department admin
    - Step 7 passes, or the operator confirms no account holds either flag
    - Step 8: zero `permission-denied` errors and zero `The query requires an index` errors across all listed surfaces; any other console error is reported with its full text
  </acceptance_criteria>
  <resume-signal>Type "verified" when all 8 steps pass, or describe exactly which step failed and paste the console output.</resume-signal>
  <done>The narrowed client queries are proven correct against production data while the permissive rule is still in force — the only safe moment to discover a miss.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → production behaviour | These conversions ship to a live SPA with no staging. Production users are the first real exercise of every narrowed query. |
| audit document → the decision to tighten | Plan 113-10's irreversible tightening is authorised by this plan's audit and browser verification. A dishonest audit converts directly into an outage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-42 | Denial of Service (self-inflicted) | a missed conversion surviving into the tightening | mitigate | Task 2's completeness map requires an explicit disposition for every RESEARCH.md MUST CONVERT row plus the services-side additions, and forbids unresolved markers. Task 3 then exercises each converted surface in the browser. Two independent checks, one static and one behavioural. |
| T-113-43 | Denial of Service (self-inflicted) | deleting the sync exports while an importer still references them | mitigate | Task 1's verify runs `node --check` across all 15 files in the import graph; a stale named import fails there rather than as a blank page in production. The wave ordering additionally guarantees every importer landed first. |
| T-113-44 | Repudiation | D-12 asserted rather than demonstrated | mitigate | Task 2 Section 2 requires verbatim search output and a per-hit classification, so the "no fire-and-forget assignment write remains" claim is reproducible by anyone re-running the commands. |
| T-113-45 | Information Disclosure | frozen legacy arrays still present on user documents | accept | D-08 deliberately retains them as inert rollback insurance. They are no longer read by any code path (proved by Task 2 Section 1) and no rule branch will read them after plan 113-10. Their deletion is a separate, explicitly deferred phase. |
| T-113-46 | Information Disclosure | D-04 no-leak invariant silently weakened | mitigate | Task 2 Section 3 must include the `isMrfInAssignedScope` diff proving comment-only change, and Task 3 step 6 exercises the invariant in the browser. |
| T-113-47 | Elevation of Privilege | operator approving the gate without running every step | accept | Mitigated only procedurally: each step has its own acceptance criterion and the resume signal requires naming the failing step. This is the inherent limit of manual UAT in a project with no browser test harness. |
| T-113-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed. |
</threat_model>

<verification>
- `node --check` exits 0 for all 15 JS files in the import graph
- Zero `syncPersonnelToAssignments` / `syncServicePersonnelToAssignments` references across `app/`, `scripts/`, `index.html`
- `113-CONVERSION-AUDIT.md` exists, has no unresolved markers, and disposes of every MUST CONVERT row
- Operator confirms all 8 browser verification steps
</verification>

<success_criteria>
The client-side conversion is complete, documented, and proven correct in production while the permissive rule is still in force. The phase is now safe to tighten.
</success_criteria>

<output>
Create `.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-09-SUMMARY.md` when done. Record: the final `./firebase.js` import list for `app/utils.js`, the git grep output proving no sync references remain, and the operator's per-step verification results.
</output>
