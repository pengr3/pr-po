---
phase: 113-assignment-source-of-truth-and-project-read-enforcement
plan: 10
type: execute
wave: 6
depends_on: ["113-09"]
files_modified:
  - firestore.rules
  - test/firestore.test.js
autonomous: false
requirements: [D-01, D-02, D-09, D-14, D-15, D-16, D-17]

must_haves:
  truths:
    - "D-15: the `projects` collection's single `allow read` is replaced by a split `allow get` / `allow list`, and BOTH are scoped on `personnel_user_ids` for scoped roles — a scoped role cannot reach an unassigned project by direct doc-ID link either"
    - "D-01: the see-all role list named in D-01 keeps unrestricted `projects` reads, so server-side enforcement binds scoped roles only and is a correctness fix, not a containment boundary against admins — stated plainly in a rules comment"
    - "D-16: `services_admin` is treated as a scoped role at the CLIENT layer (`PROJECT_SEE_ALL_ROLES` still excludes it, and every MUST-CONVERT site was converted on that basis), and the D-01-vs-D-16 tension over its RULES-layer posture is resolved by an explicit recorded decision rather than silently"
    - "D-02: every scoped branch in the tightened `projects` and `services` rules keys on `request.auth.uid in resource.data.personnel_user_ids` — no rule branch reads `assigned_project_codes` or `assigned_service_codes` any more"
    - "D-09: `all_projects` / `all_services` survive as a top-level OR term in both the `get` and the `list` branch, so the escape hatch still grants an unscoped query"
    - "D-17: the uncommitted `users.update` cross-department carve-out is dropped, and its regression tests are retained with their assertions inverted so the least-privilege posture is permanently pinned"
    - "D-14: the `projects` subcollection read rules (activity_entries, progress_updates, issues, audit_log, baselines, edit_history) are deliberately left at `allow read: if isActiveUser()`, and the residual exposure is documented in the rules file itself rather than implied to be covered"
  artifacts:
    - path: "firestore.rules"
      provides: "scoped projects get/list split; personnel-only services branches; no users.update carve-out; documented D-14 residual"
      contains: "allow list:"
    - path: "test/firestore.test.js"
      provides: "projects list/get regression suites, inverted carve-out tests, code-generation query coverage"
  key_links:
    - from: "firestore.rules projects allow list"
      to: "resource.data.personnel_user_ids"
      via: "request.auth.uid in resource.data.personnel_user_ids"
      pattern: "allow list:"
    - from: "app/views/projects.js array-contains query"
      to: "firestore.rules projects allow list"
      via: "query shape provably satisfies the per-document predicate"
      pattern: "personnel_user_ids"
---

<objective>
Tighten the server side. This is the irreversible half of the phase and it runs only after plan 113-09's production verification confirmed every narrowed client query returns correct data.

Four things happen here:
1. `projects` gains a `get` / `list` split with both branches scoped on `personnel_user_ids` (D-15).
2. Every remaining rule branch that reads a frozen legacy array is converted to the personnel predicate — the transitional `isAssignedToProject` / `isAssignedToService` alternatives added in plan 113-01 are removed, leaving only the personnel term (D-02, D-08).
3. The uncommitted `users.update` cross-department carve-out is dropped and its tests inverted (D-17).
4. The residual subcollection exposure D-14 defers is written into the rules file so nobody reads this phase as "project read scoping is complete".

One question must be settled by the user before any of it: D-01 and D-16 give contradictory answers for `services_admin`'s RULES-layer posture on `projects`. Task 1 surfaces that contradiction with its consequences instead of resolving it silently.

Purpose: make `personnel_user_ids` the enforced authority, not the advisory one.
Output: a tightened `firestore.rules` with emulator coverage, ready for the final deploy gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONTEXT.md
@.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-RESEARCH.md
@.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-PATTERNS.md
@.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONVERSION-AUDIT.md
@CLAUDE.md
</context>

<interfaces>
Rule helpers (`firestore.rules:45-90`) — reuse, do not rewrite:
`isSignedIn()`, `getUserData()`, `isActiveUser()`, `hasRole(roles)`, `isRole(role)`, `isAssignedToProject(projectCode)`, `isAssignedToService(serviceCode)`, `isLegacyOrAssigned(projectCode)`.

`isAssignedToProject` / `isAssignedToService` become UNUSED by every branch this phase touches. They must NOT be deleted — `isLegacyOrAssigned` still calls `isAssignedToProject`, and any branch outside this phase's scope that still calls them must keep working. Verify by search before deciding; if a helper genuinely has zero remaining call sites after this plan, leave it defined with a comment marking it vestigial rather than deleting it (deleting a rules helper is a wider blast radius than this phase's decisions authorise).

Blocks edited here (current line numbers):
- `users` allow update — 166-180, specifically the two carve-out branches at 170-175 and the explanatory comment at 148-165
- `projects` — the single `allow read: if isActiveUser();` at 229
- `projects` subcollections — the `allow read: if isActiveUser()` lines at 292, 304, 312, 329, 340, 353
- `services` allow list — 599-603 as widened by plan 113-01
- `services` allow update — 610-617 as widened by plan 113-01
- `project_tasks` create/update/delete and `service_tasks` create/update/delete — as widened by plan 113-01

Client contract that the tightened rule must admit (from plans 113-03 through 113-08):
- bare `where('personnel_user_ids','array-contains',uid)` on `projects` and on `services`
- `where('project_code','==',X)` + `where('personnel_user_ids','array-contains',uid)` on `projects`
- `where('client_code','==',X)` + `where('personnel_user_ids','array-contains',uid)` on `projects` and on `services`
- `getDoc(doc(db,'projects',<docId>))` for an assigned project
- unscoped `collection(db,'projects')` and `collection(db,'services')` for see-all roles and `all_*` holders
- `generateServiceCode()`'s `projects` range scan: `where('client_code','==',C)` + `where('project_code','>=',min)` + `where('project_code','<=',max)`, issued by `services_admin`
- `generateProjectCode()`'s `services` range scan, issued by `operations_admin` (already exempt at `firestore.rules:600`)

Test fixtures available in `test/firestore.test.js` `seedUsers()`: `active-super-admin`, `active-ops-admin`, `active-ops-user` (has `assigned_project_codes: ["CLMC_TEST_2026001"]`), `active-services-admin` (has `all_services: true`), `active-services-user` (has `assigned_service_codes: ["SVC-001"]`), `active-finance`, `active-procurement`, `pending-user`.
</interfaces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Settle services_admin's rules-layer posture on the projects collection</name>
  <files>(none — decision only)</files>
  <read_first>
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONTEXT.md D-01, D-15 and D-16 (read all three together — the contradiction is between their literal texts)
    - firestore.rules lines 589-603 (the `services` allow list rule, whose comment at 590-591 documents the existing precedent: `operations_admin` is in the exempt set specifically so the code-generation collision check works)
    - app/utils.js lines 311-319 (`PROJECT_SEE_ALL_ROLES` / `SERVICE_SEE_ALL_ROLES` and the comment explaining why `services_admin` is excluded from the first)
    - app/utils.js lines 359-399 (`generateServiceCode` — the `projects` range scan that throws on error)
    - app/views/services.js around line 761-785 (the Add Service caller whose catch shows "Failed to create service")
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-RESEARCH.md audit row 12 (the highest-severity finding) and "Contradicts CONTEXT.md" item 2
  </read_first>
  <action>
Present the decision below to the user with its three options and their consequences, wait for a selection, then record the selection verbatim in `113-10-SUMMARY.md` and as a comment above the `projects` block in `firestore.rules` citing D-01, D-15 and D-16 by ID. Execute Tasks 2 and 3 against the SELECTED option, not against the recommendation. Change no files before the selection is made.
  </action>
  <decision>
Should `services_admin` be in the EXEMPT role list of the tightened `projects` `allow get` / `allow list` rules, or should it be scoped on `personnel_user_ids` like `operations_user` and `services_user`?
  </decision>
  <context>
CONTEXT.md gives two contradictory answers and both are locked decisions:

- **D-01** names the exempt set literally as `super_admin` / `finance` / `procurement` / `operations_admin` / **`services_admin`**, and says this "mirrors the existing services rules".
- **D-16** says the plan must read D-01 as "`*_user` roles **and** the cross-department admin role are scoped", citing `app/utils.js:318` where `PROJECT_SEE_ALL_ROLES` deliberately excludes `services_admin`.
- **D-15** says both `get` and `list` are scoped on `personnel_user_ids`, with "the see-all roles of D-01" unrestricted — which just points back at D-01's list.

The evidence D-16 cites is the CLIENT-layer helper, and that layer is not in dispute: `PROJECT_SEE_ALL_ROLES` still excludes `services_admin`, and every MUST-CONVERT site was converted on the basis that `services_admin` is scoped, exactly as D-16 demands. The open question is only the RULES layer.

This matters because of RESEARCH.md audit row 12, its highest-severity finding: `generateServiceCode()` (`app/utils.js:359-399`) range-scans the WHOLE `projects` collection for a client/year to avoid CLMC code collisions between projects and services. It is reached by `services_admin` creating ANY new service, via three call sites of `createEngagement()`. It `throw`s on query error and the caller surfaces "Failed to create service". That query is a list operation on non-personnel fields and can NEVER satisfy an array-contains-only rule.
  </context>
  <options>
    <option id="option-a">
      <name>A — services_admin EXEMPT in the projects rule (recommended)</name>
      <pros>
        Matches D-01's literal role list. Mirrors the existing, shipped, documented precedent at `firestore.rules:589-603`, where `operations_admin` (the mirror cross-department admin) is in the `services` exempt set for exactly this code-generation reason. `generateServiceCode()` keeps working with no change. `assignments.js:192`'s unconditional unscoped projects listener — which `services_admin` reaches on the admin-only Assignments route — stays quiet instead of throwing `permission-denied` noise. The UI stays assignment-scoped for `services_admin` regardless, because `PROJECT_SEE_ALL_ROLES` excludes it. Fully reversible: one line, once a counter-document code generator exists.
      </pros>
      <cons>
        A `services_admin` can enumerate all projects via devtools. This is precisely the posture D-01 already accepts and states ("not a containment boundary against admins"), but it is a real residual and must be recorded in the threat model.
      </cons>
    </option>
    <option id="option-b">
      <name>B — services_admin SCOPED in the projects rule</name>
      <pros>
        Matches D-16's literal instruction. Server-side enforcement then binds the cross-department admin too, closing the enumeration path that Option A accepts.
      </pros>
      <cons>
        Hard-blocks service creation for `services_admin` — the exact "Failed to create service" failure RESEARCH.md calls the highest-severity finding. Fixing it in this phase requires EITHER degrading the projects half of the collision scan to empty on `permission-denied` (which risks issuing a service the same CLMC code as an existing project for that client and year — a data-integrity defect worse than the exposure being closed), OR migrating code generation to a counter document (a new collection, new rules, and a seeding migration across every existing client/year — a phase of its own). It also makes `assignments.js:192` throw `permission-denied` for `services_admin` on every Assignments-tab open. And it leaves an asymmetry unless `operations_admin` is simultaneously scoped on `services`, which would break `generateProjectCode()` the same way.
      </cons>
    </option>
    <option id="option-c">
      <name>C — Option A now, plus a queued follow-up phase for counter-document code generation</name>
      <pros>
        Ships the correct, working posture today and books the work that would make Option B safe later. Records the exposure as a conscious, time-boxed acceptance rather than a permanent one.
      </pros>
      <cons>
        Adds a backlog item. No downside to this phase.
      </cons>
    </option>
  </options>
  <verify>
    <automated>MISSING — this is a decision, not a change. Its outcome is verified by Task 2's acceptance criteria, which are written against whichever option is selected.</automated>
    <human-check>User selects an option; the selection and its rationale are recorded verbatim in the plan SUMMARY and in a comment inside `firestore.rules` above the projects block.</human-check>
  </verify>
  <acceptance_criteria>
    - The user's selection is recorded verbatim in `113-10-SUMMARY.md`
    - A comment above the `projects` block in `firestore.rules` names the selected option, cites D-01 / D-15 / D-16 by ID, and states the accepted residual in one sentence
    - Task 2 and Task 3 are executed against the selected option, not against the recommendation
    - If Option B is selected, Task 2 additionally carries the `generateServiceCode` remediation the user chooses, and the plan is re-scoped before proceeding rather than shipping a known service-creation block
  </acceptance_criteria>
  <resume-signal>Select: option-a, option-b, or option-c.</resume-signal>
  <done>The D-01 / D-16 contradiction is resolved by the user on the evidence, and the resolution is recorded in the rules file itself.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten firestore.rules — projects get/list split, personnel-only branches, carve-out removal, D-14 residual</name>
  <files>firestore.rules</files>
  <read_first>
    - firestore.rules lines 45-90 (helpers), 140-185 (users allow update with the carve-out and its 18-line explanatory comment), 224-360 (the whole projects block including all six subcollection read rules), 578-620 (services block), 755-900 (project_tasks and service_tasks as widened by plan 113-01)
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-PATTERNS.md section 3 (target projects rule shape, and the explicit warning not to copy the services hybrid list rule's CONTENT)
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-CONVERSION-AUDIT.md (the client query shapes this rule must admit)
    - The Task 1 decision outcome
  </read_first>
  <action>
**(a) Replace the `projects` `allow read` (line 229) with a split `get` / `list` pair (D-15).** Both branches take the same three-part shape:
  - `hasRole([...exempt roles...])` — the D-01 see-all list, with `services_admin` included or excluded per the Task 1 decision
  - OR `getUserData().all_projects == true` — the D-09 escape hatch, hoisted to a top-level OR term so a holder can issue an unscoped query
  - OR `(isRole('operations_user') || isRole('services_user')) && request.auth.uid in resource.data.personnel_user_ids` — the D-02 uniform scoped predicate (add `isRole('services_admin')` to this disjunction if Task 1 selected Option B)
  `allow list` additionally requires `isActiveUser() &&` as its outer conjunct, mirroring `firestore.rules:599`.
  Do NOT copy the `services` list rule's `isAssignedToService(...)` branch — PATTERNS.md section 3 flags it explicitly as the legacy shape to leave behind.
  Above the pair, write a comment that: names D-15 as the reason for the split; states D-01's consequence plainly (server-side enforcement binds scoped roles only; it is a correctness and consistency fix, not a containment boundary against admin roles); names the exact client query shapes this rule is paired with, so the rule/query pairing is documented the way `firestore.rules:597` already documents it for services.

**(b) Convert the remaining legacy branches to personnel-only (D-02, D-08).** In every branch plan 113-01 widened, DELETE the transitional `isAssignedToProject(...)` / `isAssignedToService(...)` term and keep only the personnel term:
  - `services` allow list — the `services_user` branch becomes personnel-only; merge it with the `operations_user` branch into one disjunction over both roles, exactly as done for `projects` in (a). Keep the `getUserData().all_services == true` top-level OR added in 113-01. Keep the `hasRole([...])` exempt list unchanged — `operations_admin` stays in it for the `generateProjectCode` collision check that comment 590-591 already documents.
  - `services` allow update — the three `services_user` / `operations_user` / `operations_admin` branches become personnel-only.
  - `project_tasks` create / update Tier 1 / delete — drop `isAssignedToProject(...)`, keep the guarded parent-`get()` personnel term.
  - `service_tasks` create / update Tier 1 / delete — drop `isAssignedToService(...)`, keep the guarded parent-`get()` personnel term.
  Leave every Tier-2 progress-only branch (`request.auth.uid in resource.data.assignees`) untouched — those never read a legacy array.
  Do NOT delete the `isAssignedToProject` / `isAssignedToService` helper definitions. Search for remaining call sites first; `isLegacyOrAssigned` (line 78) still calls `isAssignedToProject`. Mark any helper that ends up with zero call sites as vestigial in a comment rather than removing it.

**(c) Drop the `users.update` cross-department carve-out (D-17).** Delete the two branches at lines 170-175 (`operations_admin` writing `assigned_project_codes` + `all_projects` on a `services_user` target, and the `services_admin` mirror). Keep the super_admin branch, both same-department admin branches, and the self-update branch exactly as they are. Rewrite the 18-line explanatory comment at 148-165: it currently narrates why the carve-out was added; replace it with a record that Phase 113 D-17 removed it because every client path that constructed those cross-department writes was deleted (both sync helpers and the Assignments-tab user-doc write), so the grant became unreachable — keeping an unreachable permission grant is a least-privilege regression with no offsetting benefit. Note that plan 113-08's conditional `all_*` flag clear is now the only user-document write the Assignments tab performs, that it is same-department in practice, and that it surfaces its own error toast when denied.

**(d) Document the D-14 residual in the rules file.** Above the `projects` subcollection blocks, add a comment stating: `activity_entries`, `progress_updates`, `issues`, `audit_log`, `baselines` and `edit_history` deliberately keep `allow read: if isActiveUser()` and do NOT consult the parent's `personnel_user_ids`; therefore after Phase 113 a scoped user who knows or guesses a project's document ID can still read its journal and audit trail even though the project itself is no longer discoverable; this is perimeter-only scoping, consciously deferred by D-14 to its own phase, and is recorded here so the tightened parent rule is not mistaken for complete read scoping. Change NONE of those six `allow read` lines.

Make no other change to `firestore.rules`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('firestore.rules','utf8');const o=(t.match(/{/g)||[]).length,c=(t.match(/}/g)||[]).length;if(o!==c)throw new Error('brace imbalance '+o+'/'+c);const s=t.split('\n').filter(l=>!/^\s*\/\//.test(l)).join('\n');if(/allow read: if isActiveUser\(\);[\s\S]{0,200}Phase 78 D-01/.test(s))throw new Error('projects allow read was not replaced');if(!/allow get:/.test(s)||!/allow list:/.test(s))throw new Error('projects get/list split missing');if(/hasOnly\(\['assigned_project_codes', 'all_projects'\]\)/.test(s))throw new Error('users carve-out still present');if(/hasOnly\(\['assigned_service_codes', 'all_services'\]\)/.test(s))throw new Error('users carve-out mirror still present');const legacy=(s.match(/isAssignedToService\(resource\.data\.service_code\)/g)||[]).length;if(legacy>0)throw new Error('legacy isAssignedToService branches remain: '+legacy);console.log('rules tightened OK, braces',o,c);"</automated>
  </verify>
  <acceptance_criteria>
    - `firestore.rules` brace count is balanced
    - `match /projects/{projectId}` contains an `allow get:` and an `allow list:` and no longer contains a unified `allow read:`
    - Both projects branches contain `request.auth.uid in resource.data.personnel_user_ids`
    - Both projects branches contain `getUserData().all_projects == true` as a top-level OR term
    - `allow list` for projects has `isActiveUser() &&` as its outer conjunct
    - The projects exempt `hasRole([...])` list matches the Task 1 decision exactly, and a comment above the block names the selected option and cites D-01, D-15 and D-16
    - Comment-stripped `firestore.rules` contains zero occurrences of `isAssignedToService(resource.data.service_code)` and zero of `isAssignedToProject(resource.data.project_code)`
    - Comment-stripped `firestore.rules` contains zero occurrences of `hasOnly(['assigned_project_codes', 'all_projects'])` and zero of `hasOnly(['assigned_service_codes', 'all_services'])`
    - The `users` allow update rule still contains the super_admin branch, both same-department admin branches, and the self-update branch with its `role` / `status` / `invitation_code` exclusions
    - All six `projects` subcollection `allow read: if isActiveUser()` lines are unchanged, and a comment above them records the D-14 residual
    - `isAssignedToProject` and `isAssignedToService` helper definitions still exist
    - `git diff firestore.rules` shows no change to the `mrfs`, `prs`, `pos`, `transport_requests`, `proposals`, `notifications`, `billing_requests` or `collectibles` blocks
  </acceptance_criteria>
  <done>`personnel_user_ids` is the sole assignment predicate in every rule branch this phase touches, `projects` reads are scoped on both `get` and `list`, the unreachable carve-out is gone, and the deferred residual is documented where a rules reader will find it.</done>
</task>

<task type="auto">
  <name>Task 3: Emulator coverage for the tightened rules, including the inverted carve-out tests</name>
  <files>test/firestore.test.js</files>
  <read_first>
    - test/firestore.test.js lines 1-60 (harness and `seedUsers()` fixtures), 370-430 (the `operations_admin project assignments` suite that seeds its own `projects` fixtures — the per-suite seeding pattern to copy), 600-700 (existing services list/get suites and the `assertFails(getDocs(query(...)))` shape), 690-799 (the three cross-department carve-out suites added in the working tree)
    - firestore.rules as tightened by Task 2
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-PATTERNS.md section 4 (test patterns, including the content-assertion shape and the note that no existing test asserts query RESULT content)
    - .planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-RESEARCH.md "Recommended new tests for Phase 113" (items 1-8)
  </read_first>
  <action>
**(a) Invert the carve-out tests (D-17).** The working tree's three cross-department suites contain 9 tests, of which exactly 4 currently use `assertSucceeds` and must become `assertFails` now that the carve-out is gone. Identify them by their titles:
  - "operations_admin can update assigned_project_codes on a services_user document"
  - "services_admin can update assigned_service_codes on an operations_user document"
  - "operations_admin can update assigned_project_codes + all_projects together on a services_user document"
  - "services_admin can update assigned_service_codes + all_services together on an operations_user document"
Change only the assertion wrapper and rewrite each title to state the new expectation (they now prove the write is DENIED). Keep the same fixture IDs and the same `updateDoc` payloads — that is what makes them permanent regression coverage against the rules-drifts-from-the-assignment-model bug class this investigation chain kept rediscovering. The other 5 tests in those suites already use `assertFails` and stay as they are. Update each suite's header comment: it currently narrates the services-user-project-hidden fix; it must now record that Phase 113 D-17 removed the carve-out because no client path constructs these writes any more, and that these tests exist to keep it removed.
  Note for the executor: RESEARCH.md says "8 tests"; the actual working-tree count is 9. Verify by reading before editing and record the true count in the SUMMARY.

**(b) Add a `projects` read-rule suite.** There is currently NO test anywhere in the file asserting `allow read` / `allow list` behaviour on `projects`, so this is net-new. Seed, in a per-suite `beforeEach` using `testEnv.withSecurityRulesDisabled`: `projects/proj-assigned` with `project_code: "CLMC_TEST_2026001"`, `client_code: "TEST"`, `personnel_user_ids: ["active-ops-user"]`; and `projects/proj-unassigned` with `project_code: "CLMC_TEST_2026002"`, `client_code: "TEST"`, `personnel_user_ids: []`. Cover:
  1. Each exempt role from the Task 1 decision succeeds on an unscoped `getDocs(collection(db,'projects'))`.
  2. `active-ops-user` FAILS on the unscoped `getDocs(collection(db,'projects'))` — this is the query shape the whole phase exists to eliminate.
  3. `active-ops-user` SUCCEEDS on `getDocs(query(collection(db,'projects'), where('personnel_user_ids','array-contains','active-ops-user')))` AND the returned snapshot contains exactly `["proj-assigned"]`. Assert on result content, not just pass/fail — PATTERNS.md notes no existing test does this, so use `snap.docs.map(d => d.id)` and match the assertion library the file already uses.
  4. `active-ops-user` SUCCEEDS on the paired `where('project_code','==','CLMC_TEST_2026001') + array-contains` query (the `project-detail.js` / `project-plan.js` shape).
  5. `active-ops-user` SUCCEEDS on the paired `where('client_code','==','TEST') + array-contains` query (the `clients.js` shape).
  6. `active-ops-user` SUCCEEDS on `getDoc(doc(db,'projects','proj-assigned'))` — the D-15 scoped `get`.
  7. `active-ops-user` FAILS on `getDoc(doc(db,'projects','proj-unassigned'))` — the direct-doc-ID link closure D-15 asks for.
  8. A fixture user carrying `all_projects: true` SUCCEEDS on the unscoped collection query — the D-09 escape hatch. Seed this user in the suite's own `beforeEach` if `seedUsers()` has no such fixture.
  9. `services_admin` executing `generateServiceCode`'s exact range-scan shape against `projects` — `where('client_code','==','TEST')` + `where('project_code','>=',min)` + `where('project_code','<=',max)` — succeeds or fails per the Task 1 decision, with the test title stating which and why. This test is what stops a future edit silently re-breaking service creation.

**(c) Add a `services` tightening regression.** `active-services-user` FAILS on `getDocs(query(collection(db,'services'), where('service_code','in',['SVC-001'])))` — the legacy query shape, now denied because the `isAssignedToService` branch is gone — and SUCCEEDS on the `personnel_user_ids array-contains` query against a service seeded with its uid.

**(d) Keep the plan-113-01 transitional suite passing.** Its personnel-predicate assertions must still pass after the legacy alternatives are removed. Its two legacy-predicate assertions (services `service_code in` succeeding, and any test relying on `isAssignedToService`) will now fail and must be updated to the new expectation or folded into (c). Do not delete coverage; convert it.

Run the suite and report both the passing count and the delta from the pre-task count.
  </action>
  <verify>
    <automated>firebase emulators:exec --only firestore --project clmc-procurement "npx mocha test/firestore.test.js --exit --timeout 30000"</automated>
  </verify>
  <acceptance_criteria>
    - The emulator command exits 0 with zero failing tests
    - The three cross-department carve-out suites contain zero `assertSucceeds` calls — all their assertions are now `assertFails`
    - The suite titles of the 4 inverted tests state the DENIED expectation
    - A `projects` read-rule suite exists covering all 9 listed cases
    - Case 3 asserts on returned document IDs, not just on promise resolution
    - Case 7 (`getDoc` on an unassigned project by a scoped role) asserts failure
    - Case 9's title states the Task 1 decision and its rationale
    - The `services` legacy `service_code in` query is asserted to FAIL for `active-services-user`
    - The passing count increased by at least 11 versus the pre-task count (record both numbers)
    - No test was deleted — any test whose expectation changed was converted, not removed
  </acceptance_criteria>
  <done>The tightened rules are pinned by emulator tests covering the exempt path, the denied unscoped path, the admitted scoped path with result-content assertions, both direct-doc-ID outcomes, the escape hatch, the code-generation query, and the removed carve-out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → `projects` / `services` reads | After this plan this boundary is enforced server-side for scoped roles for the first time. Before it, it was cosmetic client-side filtering. |
| actor's own user document → rule evaluation | `getUserData().all_projects` / `all_services` remain authorization inputs. The `assigned_*_codes` fields cease to be. |
| admin → another user's document | Narrowed: the cross-department carve-out is removed, restoring the Phase 49 audit's department-containment posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-48 | Denial of Service (self-inflicted) | tightening a rule while an unconverted client query still exists | mitigate | This plan depends on 113-09, whose completeness audit disposes of every MUST-CONVERT site and whose browser checkpoint exercised each converted surface in production against the still-permissive rule. The wave graph makes the reverse order impossible. |
| T-113-49 | Information Disclosure | `services_admin` (Option A) able to enumerate all projects server-side | accept | Explicitly the posture D-01 states and accepts: server-side enforcement binds scoped roles only; it is a correctness and consistency fix, not a containment boundary against admins. Recorded in a rules comment, decided by the user in Task 1, and reversible in one line once code generation no longer needs a cross-collection range scan. CONTEXT.md `deferred` already books "revisit whether admins should be scoped at all" as a separate security decision. |
| T-113-50 | Information Disclosure | residual subcollection exposure (D-14) | accept (documented) | The six `projects` subcollections keep `allow read: if isActiveUser()`. A scoped user who knows or guesses a project's document ID can still read its journal and audit trail even though the project is no longer discoverable. Task 2(d) writes this into `firestore.rules` itself so the tightened parent rule cannot be mistaken for complete read scoping, and CONTEXT.md `deferred` books the fix as its own phase. |
| T-113-51 | Spoofing | fail-open when `personnel_user_ids` is missing or malformed | mitigate | `request.auth.uid in resource.data.personnel_user_ids` fails closed by construction: a document lacking the field, or where it is not a list, never satisfies the predicate, and `array-contains` never matches it on the query side either. This is a mechanical property of the operator, asserted by the `proj-unassigned` fixture (empty array) in Task 3 cases 3 and 7. |
| T-113-52 | Elevation of Privilege | unreachable carve-out left in place | mitigate | D-17 removes it. Task 3(a) pins the removal with 4 inverted tests using the original payloads, so a future re-addition fails CI-equivalent verification immediately. |
| T-113-53 | Denial of Service (self-inflicted) | `getDoc` fallback paths broken by a `get` rule that is narrower than expected | mitigate | Task 3 cases 6 and 7 assert both outcomes explicitly (assigned succeeds, unassigned fails), covering `project-detail.js:225`'s clientless doc-ID fallback and `procurement.js`'s PO-Delivered `getDoc`. |
| T-113-54 | Repudiation | deleting a rules helper with call sites outside this phase's scope | mitigate | Task 2 forbids deleting `isAssignedToProject` / `isAssignedToService`, requires a call-site search first, and permits only a vestigial comment. `isLegacyOrAssigned` at line 78 still calls one of them. |
| T-113-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed. |
</threat_model>

<verification>
- `firestore.rules` braces balanced
- Comment-stripped rules contain zero `isAssignedToService(resource.data.service_code)`, zero `isAssignedToProject(resource.data.project_code)`, zero `hasOnly(['assigned_project_codes', 'all_projects'])`, zero `hasOnly(['assigned_service_codes', 'all_services'])`
- `match /projects/{projectId}` has both `allow get:` and `allow list:`, each carrying the personnel predicate and the `all_projects` escape hatch
- All six projects subcollection `allow read` lines unchanged, with a D-14 residual comment above them
- Emulator suite exits 0 with at least 11 net-new passing tests and zero `assertSucceeds` remaining in the carve-out suites
</verification>

<success_criteria>
`personnel_user_ids` is the enforced read authority for scoped roles on `projects` and `services`, the frozen legacy arrays are read by no rule branch, the unreachable cross-department carve-out is gone with its removal pinned by tests, and the deliberately-deferred subcollection residual is documented in the rules file.
</success_criteria>

<output>
Create `.planning/phases/113-assignment-source-of-truth-and-project-read-enforcement/113-10-SUMMARY.md` when done. Record: the Task 1 decision verbatim with its rationale; the true count of carve-out tests (RESEARCH.md says 8, verify); the pre- and post-task mocha passing counts; and the final text of the projects `allow get` / `allow list` pair.
</output>
