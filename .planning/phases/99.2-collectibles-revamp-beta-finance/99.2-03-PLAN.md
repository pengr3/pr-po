---
phase: 99.2-collectibles-revamp-beta-finance
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - app/views/finance.js
autonomous: true
requirements: [SC2, D-03, D-04, D-09]
user_setup: []

must_haves:
  truths:
    - "One collapsible banner shows two independent sub-sections: 'Awaiting Your Review' (pending billing requests) and 'Approved — File as Collectible' (approved requests with no matching collectible)"
    - "An approved billing request whose project_code+tranche_index has no collectibles doc ALWAYS appears in sub-section 2 (the only recovery path for the Phase 99 orphan state — never silently absent)"
    - "The 'File COLL-xxx' button opens the existing prefilled create modal via openCreateCollectibleModal(preselectKey) — no new modal path is invented"
  artifacts:
    - path: "app/views/finance.js"
      provides: "Widened billing_requests listener (pending + approved), getOrphanApprovedBillingRequests() reconciliation, two-section renderPendingBillingBanner, fileCollectibleFromBanner handler"
      contains: "function getOrphanApprovedBillingRequests"
  key_links:
    - from: "approved billing_requests"
      to: "collectiblesData reconciliation on project_code+tranche_index"
      via: "client-side .some() match; show when no match (D-04 — orphan never silently absent)"
      pattern: "tranche_index"
    - from: "'File COLL-xxx' button"
      to: "openCreateCollectibleModal(preselectKey)"
      via: "reuse the existing preselectKey 3-segment shape from approveBillingRequest L1655-1658"
      pattern: "openCreateCollectibleModal"
    - from: "brUnsub billing_requests listener L1592"
      to: "pending + approved split"
      via: "widened query (drop the status=='pending' filter) + client-side split into pendingBillingRequests + approvedBillingRequests"
      pattern: "approvedBillingRequests"
---

<objective>
Deliver the **unified approved-not-filed recovery banner** (SC2/027b): restructure the existing single-section `renderPendingBillingBanner()` into ONE collapsible banner with TWO independent sub-sections — "Awaiting Your Review" (pending) and "Approved — File as Collectible" (approved orphans) — and widen the `billing_requests` listener so the approved orphans are loaded and reconciled against `collectiblesData`. This is the ONLY recovery path for the Phase 99 silent-orphan state (D-04), so reconciliation correctness is a must-have.

Purpose: SC2 (two-section banner with the approved-not-filed recovery section) + D-03 (banner owned in this phase) + D-04 (recovery-by-reconciliation, NOT atomic approve — an orphan must NEVER be silently absent).
Output: a widened `brUnsub` query + `approvedBillingRequests` state, `getOrphanApprovedBillingRequests()` reconciliation, a two-sub-section `renderPendingBillingBanner()`, and `window.fileCollectibleFromBanner` + a second sub-section collapse toggle (both registered AND torn down).

Runs in Wave 3 (after Plan 02) so it never edits `attachWindowFunctions()` / `destroy()` / the collectibles section markup in the same wave as the table-surface plan — zero shared-region contention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99.2-collectibles-revamp-beta-finance/99.2-CONTEXT.md
@.planning/phases/99.2-collectibles-revamp-beta-finance/99.2-PATTERNS.md
@.planning/spikes/027b-collectibles-billing-pipeline/README.md

<interfaces>
<!-- Existing finance.js anchors to MODIFY (from PATTERNS.md §2): -->
brUnsub listener L1592-1601 — currently `query(collection(db,'billing_requests'), where('status','==','pending'))` → WIDEN
pendingBillingRequests (module var) — keep; ADD a sibling `approvedBillingRequests`
collectiblesData (module var) — the reconciliation source
billingBannerCollapsed (module var L187) + window.toggleBillingBanner (L396) — existing collapse for section 1
renderPendingBillingBanner() L1607-1644 — restructure to two sub-sections
approveBillingRequest() L1649-1675 — DO NOT change its logic (D-04: stays a simple status flip)
  — but COPY its preselectKey shape (L1655-1658) for the File button:
    isService ? 'services:'+(service_code||'')+':'+tranche_index : 'projects:'+(project_code||'')+':'+tranche_index
openCreateCollectibleModal(preselectKey) L1755 — the File-COLL target (parses the 3-segment key at L1766-1771)
Banner host markup: <div id="pendingBillingBanner"></div> at L3702 (KEEP — fills both sub-sections)
window register L373-399 ; teardown L4653-4675
Reconciliation key (dept-aware, mirrors getDisplayedCollectibles L1339 + approveBillingRequest L1655):
  code = (department === 'services') ? service_code : project_code ; plus tranche_index
escapeHTML / formatCurrency / formatTimestamp — available

<!-- From Plan 01/02 (already shipped) — banner does not need them, but the section markup must stay consistent: -->
The collectibles section now also has #collectiblesScorecard, #collectiblesCardList, the Show-completed host — do NOT remove or reorder them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the billing_requests listener + add reconciliation for approved orphans</name>
  <read_first>
    - app/views/finance.js L1590-1602 (brUnsub listener — the `where('status','==','pending')` to widen + the snapshot handler)
    - app/views/finance.js L182-190 (module vars region — where pendingBillingRequests/billingBannerCollapsed live; add approvedBillingRequests here)
    - app/views/finance.js L1328-1342 (getDisplayedCollectibles dept-aware code selection — the reconciliation key pattern to mirror)
    - app/views/finance.js L1649-1660 (approveBillingRequest preselectKey shape — the reconciliation key + the File-button key)
    - app/views/finance.js L1538-1545 (collectibles listener — re-render the banner when collectiblesData changes so a freshly-filed COLL removes its orphan)
    - .planning/spikes/027b-collectibles-billing-pipeline/README.md L26-33, L77-79 (reconciliation source + orphan note)
    - .planning/phases/99.2-collectibles-revamp-beta-finance/99.2-PATTERNS.md §2 (widen query + reconcile .some() + LANDMINE D-04)
  </read_first>
  <action>
    1. **Add module state**: near the existing `pendingBillingRequests` declaration (~L182-190) add `let approvedBillingRequests = [];` and a second collapse flag `let approvedBannerCollapsed = false;` (independent collapse for sub-section 2, D-03 "two independently-collapsible sub-sections").

    2. **Widen the `brUnsub` query** (L1592-1601). Drop the `where('status','==','pending')` filter and split client-side (PATTERNS.md prefers one un-filtered query + client split to keep a single listener in `listeners[]`):
       ```javascript
       const brUnsub = onSnapshot(
           collection(db, 'billing_requests'),
           (snapshot) => {
               pendingBillingRequests = [];
               approvedBillingRequests = [];
               snapshot.forEach(d => {
                   const r = { id: d.id, ...d.data() };
                   if (r.status === 'pending') pendingBillingRequests.push(r);          // case-sensitive (CLAUDE.md)
                   else if (r.status === 'approved') approvedBillingRequests.push(r);
                   // rejected/other: ignored
               });
               renderPendingBillingBanner();
           },
           (err) => { console.error('[Finance/BillingReq] snapshot error:', err); }
       );
       listeners.push(brUnsub);
       ```
       (If document volume is a concern, `where('status','in',['pending','approved'])` is acceptable — but the un-filtered single-query+split is the chosen default. Keep exactly ONE listener pushed to `listeners[]`.)

    3. **Re-render the banner when collectibles change** so filing a COLL immediately clears its orphan: in the `collectibles` onSnapshot handler (L1539-1545), after `renderCollectiblesTable();` add `renderPendingBillingBanner();`. (collectiblesData is the reconciliation source — when a new COLL doc lands, the matching approved request must drop out of sub-section 2.)

    4. **Add `getOrphanApprovedBillingRequests()`** — the reconciliation (the D-04 must-have). An approved request is an orphan (show in sub-section 2) when NO collectibles doc matches its dept-aware code + tranche_index:
       ```javascript
       function getOrphanApprovedBillingRequests() {
           return (approvedBillingRequests || []).filter(req => {
               const reqCode = req.department === 'services' ? (req.service_code || '') : (req.project_code || '');
               const hasColl = (collectiblesData || []).some(c => {
                   const code = c.department === 'services' ? (c.service_code || '') : (c.project_code || '');
                   return code === reqCode && c.tranche_index === req.tranche_index;
               });
               return !hasColl;   // orphan when no matching collectible
           });
       }
       ```
       LANDMINE (D-04): this must be correct so an abandoned-create orphan is NEVER silently absent. Do NOT make approve atomic (explicitly deferred). The `tranche_index` comparison is strict-equality on the stored value (both sides store the same integer index — mirror approveBillingRequest's use at L1657-1658).

    Do NOT alter approveBillingRequest() / rejectBillingRequest() logic — they stay simple status flips (D-04).
  </action>
  <verify>
    <automated>node --check app/views/finance.js</automated>
  </verify>
  <acceptance_criteria>
    - The narrow query is gone: `grep -c "where('status', '==', 'pending')" app/views/finance.js` → 0 (the brUnsub no longer filters to pending-only).
    - `grep -c "approvedBillingRequests" app/views/finance.js` → ≥3 (decl + push in listener + use in reconciliation).
    - `grep -c "function getOrphanApprovedBillingRequests" app/views/finance.js` → 1.
    - Reconciliation matches on BOTH code and tranche_index: read getOrphanApprovedBillingRequests and confirm `code === reqCode && c.tranche_index === req.tranche_index`.
    - Dept-aware code selection present on BOTH sides (req and collectible): the `department === 'services' ? service_code : project_code` pattern appears for req and for c.
    - Exactly ONE listener still pushed for billing_requests: `grep -c "listeners.push(brUnsub)" app/views/finance.js` → 1.
    - Banner re-renders on collectibles change: `renderPendingBillingBanner()` is called inside the collectibles onSnapshot handler (read L1539-1546 region).
    - approveBillingRequest still flips status only (NOT atomic create): `grep -c "status: 'approved'" app/views/finance.js` ≥1 and no addDoc/setDoc to 'collectibles' added inside approveBillingRequest.
    - `node --check app/views/finance.js` exits 0.
  </acceptance_criteria>
  <done>The billing_requests listener loads both pending and approved into separate arrays via a single subscription; getOrphanApprovedBillingRequests reconciles approved requests against collectiblesData on dept-aware code + tranche_index; the banner re-renders when collectibles change; approve stays a simple status flip; node --check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure the banner into two sub-sections + File-COLL handler (register + teardown)</name>
  <read_first>
    - app/views/finance.js L1604-1644 (current single-section renderPendingBillingBanner — the chevron/label/count/body markup to restructure; the existing escapeHTML discipline at L1619-1627)
    - app/views/finance.js L1616-1633 (the pending-request row template — reuse for sub-section 1)
    - app/views/finance.js L1649-1660 (approveBillingRequest preselectKey — copy the exact shape for the File button)
    - app/views/finance.js L373-399 (window register) + L4653-4675 (teardown) — add the new handlers to BOTH
    - .planning/spikes/027b-collectibles-billing-pipeline/README.md L60-79 (two-section unified banner: amber "Awaiting Your Review" + blue "Approved — File as Collectible")
    - .planning/phases/99.2-collectibles-revamp-beta-finance/99.2-PATTERNS.md §2 (banner markup to extend + window register/teardown rule)
  </read_first>
  <action>
    Restructure `renderPendingBillingBanner()` (L1607-1644) into ONE outer banner container with TWO independently-collapsible sub-sections. The host `<div id="pendingBillingBanner">` (L3702) is unchanged.

    1. **Compute both datasets at the top**:
       ```javascript
       const pending = Array.isArray(pendingBillingRequests) ? pendingBillingRequests : [];
       const orphans = getOrphanApprovedBillingRequests();   // Task 1
       const host = document.getElementById('pendingBillingBanner');
       if (!host) return;
       if (pending.length === 0 && orphans.length === 0) { host.innerHTML = ''; return; } // auto-disappear when both empty
       ```

    2. **Sub-section 1 — "Awaiting Your Review" (amber, pending)**: reuse the EXISTING pending-row template (L1616-1633) verbatim (Approve/Reject buttons → window.approveBillingRequest / window.rejectBillingRequest, unchanged). Wrap it in a collapsible header using the existing `billingBannerCollapsed` flag + `window.toggleBillingBanner` (L396). Label "Awaiting Your Review", amber accent (`background:#fffbeb;border:1px solid #fde68a;` header `color:#92400e`). Render this sub-section ONLY when `pending.length > 0`.

    3. **Sub-section 2 — "Approved — File as Collectible" (blue, orphans)**: render ONLY when `orphans.length > 0`. Header uses the new `approvedBannerCollapsed` flag + `window.toggleApprovedBanner`. Blue accent (`background:#eff6ff;border:1px solid #bfdbfe;` header `color:#1e40af`). Each orphan row shows the project/service name (escaped), tranche label + % + amount, submitter/approver context, and a **"File COLL-xxx" button** wired to `window.fileCollectibleFromBanner('${escapeHTML(req.id)}')`. Mirror the pending-row layout (L1621-1633) for visual consistency; all user strings through escapeHTML (T-99.2-08).

    4. **Add `fileCollectibleFromBanner(reqId)`** — builds the preselectKey EXACTLY like approveBillingRequest L1655-1658 and calls the existing modal (NO new modal path, D-03):
       ```javascript
       function fileCollectibleFromBanner(reqId) {
           const req = (approvedBillingRequests || []).find(r => r.id === reqId);
           if (!req) { showToast('Approved request not found. Refresh and try again.', 'error'); return; }
           const isService = req.department === 'services';
           const preselectKey = isService
               ? 'services:' + (req.service_code || '') + ':' + req.tranche_index
               : 'projects:' + (req.project_code || '') + ':' + req.tranche_index;
           window.openCreateCollectibleModal(preselectKey);
       }
       ```
       (After Finance submits the create modal, the new collectibles doc triggers the onSnapshot → renderPendingBillingBanner re-runs → reconciliation drops this orphan automatically. No manual state cleanup needed.)

    5. **Register + tear down the new handlers** (CLAUDE.md SPA contract — BOTH lists):
       - attachWindowFunctions() (after L398): `window.toggleApprovedBanner = () => { approvedBannerCollapsed = !approvedBannerCollapsed; renderPendingBillingBanner(); };` and `window.fileCollectibleFromBanner = fileCollectibleFromBanner;`.
       - destroy() (after L4675): `delete window.toggleApprovedBanner;` and `delete window.fileCollectibleFromBanner;`.

    6. **Outer container**: wrap both sub-sections in a single `<div>` (small gap between them) so they read as ONE banner with two parts (spike 027b WINNER = unified banner, not two separate banners). Keep `margin-bottom:1rem` so it sits cleanly above the scorecard.

    Preserve the existing count-pill pattern (a small rounded badge with the sub-section's item count) on each sub-section header.
  </action>
  <verify>
    <automated>node --check app/views/finance.js</automated>
  </verify>
  <acceptance_criteria>
    - Both sub-section labels present verbatim: `grep -c "Awaiting Your Review" app/views/finance.js` ≥1 AND `grep -c "Approved — File as Collectible" app/views/finance.js` ≥1.
    - `grep -c "function fileCollectibleFromBanner" app/views/finance.js` → 1.
    - The File button calls the existing modal with the preselectKey: `grep -c "openCreateCollectibleModal(preselectKey)" app/views/finance.js` → ≥2 (one in approveBillingRequest unchanged, one in fileCollectibleFromBanner).
    - The preselectKey 3-segment shape is reused (services:/projects: + code + ':' + tranche_index) in fileCollectibleFromBanner — read to confirm it matches approveBillingRequest's shape.
    - New window fns registered AND torn down: `window.toggleApprovedBanner` and `window.fileCollectibleFromBanner` each appear in BOTH attachWindowFunctions() and destroy() (`grep -c "toggleApprovedBanner" app/views/finance.js` ≥3; `grep -c "fileCollectibleFromBanner" app/views/finance.js` ≥4 counting def+button+register+teardown).
    - Banner auto-disappears only when BOTH empty: `grep -n "pending.length === 0 && orphans.length === 0" app/views/finance.js` → ≥1.
    - Sub-section 2 sources from getOrphanApprovedBillingRequests (the reconciled set, not raw approvedBillingRequests): `grep -c "getOrphanApprovedBillingRequests()" app/views/finance.js` → ≥1 inside renderPendingBillingBanner.
    - approveBillingRequest/rejectBillingRequest logic unchanged (status-flip only — no new collectible write inside approve).
    - All orphan-row user strings escaped: read sub-section 2 markup, confirm project/service name, tranche_label, submitter all wrapped in escapeHTML.
    - `node --check app/views/finance.js` exits 0.
  </acceptance_criteria>
  <done>renderPendingBillingBanner renders one unified banner with two independently-collapsible sub-sections; sub-section 2 lists ONLY reconciled orphans; the File-COLL button reuses openCreateCollectibleModal(preselectKey) with the exact existing key shape; both new handlers are registered and torn down; the banner auto-hides only when both sub-sections are empty; node --check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore `billing_requests` doc fields → DOM (banner innerHTML) | project/service name, tranche_label, requester name, notes render into both banner sub-sections |
| Approved billing request → Create-Collectible modal | preselectKey (built from service_code/project_code/tranche_index) passed to the existing modal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99.2-08 | Tampering (Stored XSS) | banner sub-section row rendering | mitigate | Every dynamic billing_requests field (project_name, service_name, tranche_label, requested_by_name, notes, req.id) rendered via `escapeHTML(...)`, amounts via `formatCurrency(...)` — mirrors the existing pending-banner discipline (L1619-1627). req.id is escaped before interpolation into the onclick attribute. |
| T-99.2-09 | Spoofing/Auth bypass | File-COLL → create modal | accept | openCreateCollectibleModal already gates on hasCollectibleWriteAuthority() (Finance-only, per approveBillingRequest L1653 comment); the banner adds no new privilege — it reuses the same authorized entry point. |
| T-99.2-10 | Information Disclosure | orphan reconciliation | accept | Reconciles only over data the Finance user already subscribes to (billing_requests + collectibles); no new collection/field exposed; no schema/rules change. |
| T-99.2-11 | Tampering (silent data loss) | reconciliation correctness | mitigate | The D-04 must-have: getOrphanApprovedBillingRequests returns EVERY approved request lacking a matching collectible; banner re-renders on both billing_requests and collectibles snapshots so the orphan set is always current — an abandoned-create orphan can never be silently absent. UAT step 3 explicitly verifies this. |

No HIGH-severity threats: no schema/rules/auth change, the only write path (create modal) is the pre-existing authorized one. Approve flow is unchanged (D-04).
</threat_model>

<verification>
- `node --check app/views/finance.js` passes.
- brUnsub no longer filters to pending-only; exactly one billing_requests listener in `listeners[]`.
- getOrphanApprovedBillingRequests reconciles on dept-aware code + tranche_index.
- `window.toggleApprovedBanner` and `window.fileCollectibleFromBanner` present in BOTH attachWindowFunctions() and destroy().
- approveBillingRequest/rejectBillingRequest logic byte-unchanged (D-04 — recovery, not atomic prevention).
- Banner re-renders on both billing_requests and collectibles snapshot updates.
</verification>

<success_criteria>
- SC2: one collapsible banner, two independent sub-sections (pending review + approved-not-filed); "File COLL-xxx" → openCreateCollectibleModal(preselectKey).
- D-03: banner owned in this phase; sub-section 2 is the only recovery path for the orphan state.
- D-04: recovery-by-reconciliation (approve stays a status flip); orphan never silently absent.
- D-09: reconciliation key is dept-aware project_code/service_code + tranche_index.
</success_criteria>

<uat_steps>
Browser UAT (zero-build, no test harness) — `python -m http.server 8000`, open `#/finance` → Collectibles:
1. **Two-section banner (SC2):** With ≥1 pending billing request, the banner shows "Awaiting Your Review" (amber) with Approve/Reject. Each sub-section collapses/expands independently via its chevron.
2. **Pending → approved flow:** Approve a pending request (the existing prefilled modal opens). DISMISS the create modal WITHOUT submitting (simulate the abandoned-create orphan). The request leaves "Awaiting Your Review" and now appears under "Approved — File as Collectible" (blue) with a "File COLL-xxx" button — it is NOT silently lost (D-04, the core recovery guarantee).
3. **Reconciliation correctness (D-04 must-have):** Click "File COLL-xxx" on the orphan → the prefilled Create-Collectible modal opens with the correct project/service + tranche preselected. Submit it. The orphan immediately disappears from "Approved — File as Collectible" (the new collectibles doc reconciles it out). Confirm it does NOT reappear.
4. **Auto-hide:** With no pending requests and no approved orphans, the entire banner is absent (host empty).
5. **Service-side parity:** Repeat step 2–3 for a service (department='services') billing request → the orphan shows, the File button preselects the service + tranche correctly, and filing clears it.
6. **No regression:** Existing Approve/Reject behavior on pending requests is unchanged; rejecting still requires a reason.
</uat_steps>

<output>
After completion, create `.planning/phases/99.2-collectibles-revamp-beta-finance/99.2-03-SUMMARY.md`
</output>
