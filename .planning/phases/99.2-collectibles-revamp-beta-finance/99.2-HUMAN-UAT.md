---
status: passed
phase: 99.2-collectibles-revamp-beta-finance
source: [99.2-VERIFICATION.md]
started: 2026-06-06T00:00:00Z
updated: 2026-06-06T00:00:00Z
approved: 2026-06-06
approved_by: operator ("all approved")
---

## Current Test

[complete — operator approved all 7 items 2026-06-06]

## Tests

### 1. Scorecard reactivity (SC1/D-02)
expected: 4 chips above the table show Total Invoiced / Cash Collected / Outstanding (and Overdue only when > ₱0). Applying a status/project filter recomputes the chips to match ONLY the visible rows. Filtering to "Fully Paid" makes Outstanding/Overdue read ₱0 and the Overdue chip disappears.
result: PASS

### 2. 5-column table + urgency borders (SC3/D-05/D-05a)
expected: Exactly 5 columns (Project·Tranche / Collection Progress / Due·Urgency / Last Payment / Actions). Col 1 merges name + tranche(%) + dept chip + monospace COLL id; a 6px progress bar shows "₱X of ₱Y · Z%"; a relative-due string renders ("N days overdue ‼" / "Due in 5 days ⚠" / "Due in 40 days"). Overdue rows show red/orange 4px left border, near-due amber, fully-paid green + dimmed.
result: PASS

### 3. Show-completed toggle + pagination recompute (SC3/D-05b/D-08c)
expected: Fully-paid rows hidden by default; a "Show N completed" toggle appears when N>0; clicking reveals them and the pagination count/pages grow to include them; clicking again hides them and the count drops. Page indicator never lands out of range. (Regression check for the destroy() fix: toggle ON → switch to another view → return to Finance → fully-paid rows should be HIDDEN again by default.)
result: PASS

### 4. Last Payment void exclusion (SC5/D-08a)
expected: A row with a recorded payment shows "₱amt · date · method"; a row with none shows italic "No payments yet". Voiding a payment in the history modal makes that row's Last Payment / Cash Collected stop counting the voided amount.
result: PASS

### 5. Mobile ≤768px card layout (SC4/D-06)
expected: Narrowing to ≤768px (or DevTools device mode) replaces the table with a card stack; each card shows the same 5-col content + the urgency left-border; Record Payment / View History buttons render full-width.
result: PASS

### 6. CSV non-regression (SC5/D-07)
expected: Export CSV downloads a file with all 13 columns and includes fully-paid rows even while they are hidden in the table view.
result: PASS

### 7. Two-section recovery banner + orphan flow (SC2/D-03/D-04)
expected: With ≥1 pending billing request the banner shows "Awaiting Your Review" (amber, Approve/Reject), each sub-section collapsing independently. Approve a pending request then DISMISS the create modal without submitting (simulate abandoned-create orphan) → the request moves to "Approved — File as Collectible" (blue) with a "File as Collectible" button and is NOT silently lost. Click File → the prefilled Create-Collectible modal opens with the correct project/service + tranche preselected; submitting it removes the orphan immediately and it does not reappear. Repeat for a service (department='services') request. With no pending and no approved orphans the whole banner is absent.
result: PASS

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

None blocking. NOTE — scope gap surfaced by operator after approval (spike 027c filter-bar/sort items dropped at the 99.2 plan-now scoping stage, not in CONTEXT D-01..D-09): Looker Studio date-range presets, search box, dept-only dropdown, group-by-Project toggle, sortByUrgency(). These are NEW scope (candidate Phase 99.3), not 99.2 defects — tracked in ROADMAP under "Deferred from spike 027c".
