---
phase: 99.2-collectibles-revamp-beta-finance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/finance.js
  - styles/views.css
autonomous: true
requirements: [SC5, D-08a, D-08b, D-05a, D-06]
user_setup: []

must_haves:
  truths:
    - "A single shared helper classifies every collectible into one of 6 urgency tiers and is the only place the overdue threshold is defined"
    - "A single shared helper returns the newest non-voided payment for a collectible (used by the Last Payment column)"
    - "A dedicated CSS block exists for the collectibles scorecard chips, 6px progress bars, 6 urgency left-borders, and the ≤768px collectible card layout — no rich visuals are inline"
  artifacts:
    - path: "app/views/finance.js"
      provides: "getCollectibleUrgency() single-source urgency classifier + getCollectibleLastPayment() helper at module scope"
      contains: "function getCollectibleUrgency"
    - path: "styles/views.css"
      provides: "Collectibles dedicated CSS block: scorecard chips, .coll-progress* bars, .coll-urgency-* left-border tiers, #collectibles-section card mobile rules"
      contains: ".coll-urgency-critical"
  key_links:
    - from: "deriveCollectibleStatus() L110"
      to: "getCollectibleUrgency()"
      via: "isOverdue branch consumes the shared helper so status and tier never disagree"
      pattern: "getCollectibleUrgency"
    - from: "styles/views.css ≤768px hide list (L2044-2047)"
      to: "#collectibles-section .table-scroll-container"
      via: "collectibles table hidden on mobile so the card list shows"
      pattern: "#collectibles-section .table-scroll-container"
---

<objective>
Build the SHARED FOUNDATION every other Phase 99.2 plan depends on: (a) the single-source urgency/overdue classifier + the last-payment helper in `finance.js`, and (b) the dedicated collectibles CSS block (scorecard chips, progress bars, 6 urgency left-borders, ≤768px card layout). This plan is FOUNDATION-ONLY — it adds helpers and CSS but does NOT yet rewrite the table, scorecard, or banner (those consume this plan's output in Waves 2–3).

Purpose: Satisfies D-08b (ONE overdue definition shared by the scorecard Overdue chip AND the 6 row tiers — never duplicate the threshold) and D-08a (Last Payment filters non-voided). Pre-staging the CSS here means Waves 2–3 add markup that references classes that already exist, and prevents two later plans from both editing `views.css` in the same wave.
Output: `getCollectibleUrgency()` + `getCollectibleLastPayment()` at module scope in `finance.js`; a new `/* COLLECTIBLES REVAMP β (Phase 99.2) */` CSS block in `styles/views.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99.2-collectibles-revamp-beta-finance/99.2-CONTEXT.md
@.planning/phases/99.2-collectibles-revamp-beta-finance/99.2-PATTERNS.md
@.planning/spikes/027c-collectibles-table-redesign/README.md

<interfaces>
<!-- Extracted from app/views/finance.js — executor uses these directly, no exploration needed. -->

Existing inline overdue check to centralize (finance.js L114, inside deriveCollectibleStatus):
```javascript
const isOverdue = coll.due_date && new Date(coll.due_date) < new Date();
```

Existing non-voided money rule (repeated at L111-113, L1409-1411, L2632-2634):
```javascript
(coll.payment_records || []).filter(r => r.status !== 'voided')
    .reduce((s, r) => s + (parseFloat(r.amount) || 0), 0)
```

deriveCollectibleStatus() returns one of: 'Fully Paid' | 'Overdue' | 'Partially Paid' | 'Pending' (finance.js L110-119).

views.css existing dual-mode landmarks:
- `.fc-card-list { display: none; }` (L2040 — desktop hides cards)
- ≤768px hide-tables list (L2044-2047): #approvals-section / #pos-section / #projects-section / #payables-section .table-scroll-container — **#collectibles-section is ABSENT (the gap this plan fills)**
- `.fc-card` / `.fc-card-header` / `.fc-card-id` / `.fc-card-body` / `.fc-card-row` / `.fc-label` / `.fc-value` / `.fc-card-actions` block (L2057-2124) — the established card anatomy to reuse
- em-scorecard grids `.em-scorecard-2col` / `.em-scorecard-3col` (L2179-2197) — chip-grid collapse model
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared urgency classifier + last-payment helper at finance.js module scope</name>
  <read_first>
    - app/views/finance.js L108-119 (deriveCollectibleStatus — the inline isOverdue at L114 to centralize)
    - app/views/finance.js L1409-1412 (the non-voided totalPaid rule to reuse)
    - .planning/phases/99.2-collectibles-revamp-beta-finance/99.2-PATTERNS.md "Shared Patterns → Overdue / urgency helper" (the 6-tier spec + D-08b invariant)
    - .planning/spikes/027c-collectibles-table-redesign/README.md L52-58 (the 6 urgency tiers + thresholds + colors)
  </read_first>
  <behavior>
    - getCollectibleUrgency(coll) returns { tier, daysOverdue, daysUntilDue, isOverdue } where tier ∈ {'critical','overdue','near-due','partial','healthy','paid'}.
    - Tier precedence (evaluate in this order): paid (totalPaid >= amount_requested && amount_requested > 0) → critical (daysOverdue >= 30) → overdue (daysOverdue 1–29) → near-due (0 < daysUntilDue <= 7) → partial (totalPaid > 0, not overdue/near) → healthy (default).
    - isOverdue is TRUE iff due_date present AND new Date(due_date) < now AND not fully paid — this is the ONE overdue definition (D-08b).
    - A collectible with no due_date is never overdue/critical/near-due (returns healthy or partial).
    - getCollectibleLastPayment(coll) returns the newest non-voided payment_records entry (max by `date`) or null; never returns a voided record (D-08a).
  </behavior>
  <action>
    Add TWO module-scope functions in finance.js, placed immediately AFTER deriveCollectibleStatus() (after L119) so all collectibles helpers sit together.

    1. `getCollectibleUrgency(coll)` — single source of truth (D-08b). Concrete logic:
       ```javascript
       function getCollectibleUrgency(coll) {
           const totalPaid = (coll.payment_records || [])
               .filter(r => r.status !== 'voided')
               .reduce((s, r) => s + (parseFloat(r.amount) || 0), 0);
           const amt = parseFloat(coll.amount_requested) || 0;
           const fullyPaid = totalPaid >= amt && amt > 0;
           // ONE overdue definition (D-08b). Day-granularity diff from today (local midnight).
           let daysOverdue = 0, daysUntilDue = null, isOverdue = false;
           if (coll.due_date) {
               const today = new Date(); today.setHours(0, 0, 0, 0);
               const due = new Date(coll.due_date + 'T00:00:00');
               const diffDays = Math.round((due - today) / 86400000);
               if (diffDays < 0) { isOverdue = !fullyPaid; daysOverdue = -diffDays; }
               else { daysUntilDue = diffDays; }
           }
           let tier;
           if (fullyPaid) tier = 'paid';
           else if (isOverdue && daysOverdue >= 30) tier = 'critical';
           else if (isOverdue) tier = 'overdue';                 // 1–29
           else if (daysUntilDue !== null && daysUntilDue <= 7) tier = 'near-due';
           else if (totalPaid > 0) tier = 'partial';
           else tier = 'healthy';
           return { tier, daysOverdue, daysUntilDue, isOverdue };
       }
       ```
       Tier→CSS-class mapping (consumed in Wave 2/3): `coll-urgency-${tier}` (i.e. `coll-urgency-critical`, `coll-urgency-overdue`, `coll-urgency-near-due`, `coll-urgency-partial`, `coll-urgency-healthy`, `coll-urgency-paid`).

    2. `getCollectibleLastPayment(coll)` (D-08a):
       ```javascript
       function getCollectibleLastPayment(coll) {
           const recs = (coll.payment_records || []).filter(r => r.status !== 'voided');
           if (recs.length === 0) return null;
           return recs.reduce((latest, r) =>
               (!latest || (r.date || '') > (latest.date || '')) ? r : latest, null);
       }
       ```

    3. Refactor deriveCollectibleStatus() (L110-119) so its isOverdue branch consumes the shared helper instead of the inline L114 check — replace `const isOverdue = coll.due_date && new Date(coll.due_date) < new Date();` with `const isOverdue = getCollectibleUrgency(coll).isOverdue;`. Keep the existing return values ('Fully Paid'/'Overdue'/'Partially Paid'/'Pending') byte-identical so no downstream consumer breaks. This proves the single-source invariant: status and tier can never disagree.

    Do NOT register these on window (they are internal helpers, not onclick handlers). Do NOT touch any render function in this plan.
  </action>
  <verify>
    <automated>node --check app/views/finance.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function getCollectibleUrgency" app/views/finance.js` → exactly 1 match.
    - `grep -n "function getCollectibleLastPayment" app/views/finance.js` → exactly 1 match.
    - `grep -c "new Date(coll.due_date) < new Date()" app/views/finance.js` → 0 (the inline L114 check is gone — centralized).
    - `grep -n "getCollectibleUrgency(coll).isOverdue" app/views/finance.js` → ≥1 (deriveCollectibleStatus consumes the shared helper).
    - getCollectibleUrgency returns an object literal containing all of: tier, daysOverdue, daysUntilDue, isOverdue (read the function body to confirm).
    - The six tier strings appear verbatim: critical, overdue, near-due, partial, healthy, paid.
    - getCollectibleLastPayment filters `r.status !== 'voided'` before selecting (read body to confirm D-08a).
    - `node --check app/views/finance.js` exits 0.
  </acceptance_criteria>
  <done>Both helpers exist at module scope; deriveCollectibleStatus delegates its overdue branch to getCollectibleUrgency; node --check passes; no inline due_date threshold remains.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dedicated collectibles CSS block (chips, progress bars, 6 urgency borders, mobile cards)</name>
  <read_first>
    - styles/views.css L2032-2169 (the entire Phase 73.1 `.fc-card*` block — the card anatomy + the ≤768px hide-tables list at L2044-2047 to extend)
    - styles/views.css L2179-2197 (em-scorecard chip grids — the chip-grid collapse model)
    - .planning/spikes/027a-collectibles-page-scorecard/README.md L26-40 (chip colors: light bg + colored border + dark text, NOT filled pills; chip color hex values)
    - .planning/spikes/027c-collectibles-table-redesign/README.md L52-68 (4px left-border accents + tier colors + 6px progress bar + fully-paid de-emphasis)
  </read_first>
  <action>
    Append ONE new CSS block at the END of the Phase 73.1 finance-card section (after L2169, OUTSIDE the existing `@media (max-width: 768px)` block, so desktop rules are global and only the mobile rules sit in a media query). Header comment: `/* ======================================== COLLECTIBLES REVAMP β (Phase 99.2) — scorecard chips, progress bars, urgency borders, mobile cards ======================================== */`.

    Include (CSS only — NO JS, NO markup here):

    1. **Scorecard chips** (D-02 / spike 027a — light bg + colored border + dark text):
       - `.coll-scorecard` { display:flex; gap:0.75rem; flex-wrap:wrap; margin-bottom:1rem; }
       - `.coll-chip` { flex:1; min-width:160px; border-radius:8px; padding:0.6rem 0.85rem; border:1px solid; background:#fff; }
       - `.coll-chip-label` { font-size:0.7rem; font-weight:700; text-transform:uppercase; letter-spacing:0.03em; color:#64748b; }
       - `.coll-chip-value` { font-size:1.25rem; font-weight:700; color:#1e293b; margin-top:0.15rem; }
       - `.coll-chip-sub` { font-size:0.7rem; color:#94a3b8; margin-top:0.1rem; }
       - Per-chip accent border colors (left-border 3px + matching value tint allowed): `.coll-chip-invoiced { border-left:3px solid #059669; }`, `.coll-chip-collected { border-left:3px solid #0369a1; }`, `.coll-chip-outstanding { border-left:3px solid #c2410c; }`, `.coll-chip-overdue { border-left:3px solid #991b1b; }` and give `.coll-chip-overdue .coll-chip-value { color:#991b1b; }`.

    2. **Collection progress bar** (D-05 — 6px height):
       - `.coll-progress-track` { height:6px; border-radius:999px; background:#e5e7eb; overflow:hidden; margin-top:0.3rem; }
       - `.coll-progress-fill` { height:100%; border-radius:999px; }
       - Color-by-urgency fill modifiers: `.coll-progress-fill.is-overdue { background:#ef4444; }`, `.coll-progress-fill.is-paid { background:#059669; }`, `.coll-progress-fill.is-normal { background:#3b82f6; }`.
       - `.coll-progress-text` { font-size:0.72rem; color:#64748b; margin-top:0.15rem; }

    3. **4px urgency left-borders — 6 tiers** (D-05a — applied to `<tr>` AND `.fc-card`). Per spike 027c colors:
       - `.coll-urgency-critical { border-left:4px solid #ef4444; background:#fef2f2; }`
       - `.coll-urgency-overdue { border-left:4px solid #f97316; background:#fff7ed; }`
       - `.coll-urgency-near-due { border-left:4px solid #f59e0b; background:#fffbeb; }`
       - `.coll-urgency-partial { border-left:4px solid #3b82f6; }`
       - `.coll-urgency-healthy { border-left:4px solid #e5e7eb; }`
       - `.coll-urgency-paid { border-left:4px solid #059669; opacity:0.72; }` (de-emphasized, D-05a)
       (Define these once; both the desktop `<tr>` and the mobile `.fc-card` reuse them.)

    4. **Relative-due / last-payment text helpers**:
       - `.coll-due-critical { color:#991b1b; font-weight:700; }`, `.coll-due-overdue { color:#c2410c; font-weight:600; }`, `.coll-due-near { color:#b45309; font-weight:600; }`, `.coll-due-normal { color:#64748b; }`
       - `.coll-no-payment { color:#94a3b8; font-style:italic; }`

    5. **"Show N completed" toggle**:
       - `.coll-show-completed-toggle { display:inline-flex; align-items:center; gap:0.4rem; margin-top:0.6rem; font-size:0.8rem; color:#1a73e8; cursor:pointer; background:none; border:none; padding:0.2rem 0; }`

    6. **Mobile card layout (≤768px)** — ADD to the existing media query at L2042 (do not create a second 768px block; insert the new rules inside the existing one OR add a clearly-commented second block — the file already has multiple media blocks per Phase 91.3-04 precedent, so a new appended `@media (max-width:768px)` block is acceptable). Required rules:
       - Add `#collectibles-section .table-scroll-container { display: none; }` (the missing entry — collectibles is the ONE Finance table 73.1 skipped).
       - `#collectibles-section .fc-card-list { display:flex; flex-direction:column; gap:0.75rem; }` (it inherits `.fc-card` anatomy from L2057+).
       - `.fc-card.coll-urgency-critical, .fc-card.coll-urgency-overdue, .fc-card.coll-urgency-near-due, .fc-card.coll-urgency-partial, .fc-card.coll-urgency-healthy, .fc-card.coll-urgency-paid { /* left border already defined globally — confirm it applies on cards */ }` (no override needed; the global rules in #3 already match `.fc-card`).
       - `.coll-card-progress` { padding-top:0.3rem; } (slot for the in-card progress bar).

    Use ONLY hex values from CLAUDE.md design system / the spikes. No `!important`. Keep `.fc-card-list { display:none; }` global default (already at L2040) authoritative for desktop.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('styles/views.css','utf8'); const open=(c.match(/\{/g)||[]).length, close=(c.match(/\}/g)||[]).length; if(open!==close){console.error('Brace mismatch',open,close);process.exit(1)} console.log('braces balanced',open)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "COLLECTIBLES REVAMP β (Phase 99.2)" styles/views.css` → ≥1 (block header present).
    - All 6 urgency tier classes present: `grep -c "coll-urgency-critical\|coll-urgency-overdue\|coll-urgency-near-due\|coll-urgency-partial\|coll-urgency-healthy\|coll-urgency-paid" styles/views.css` → ≥6.
    - `grep -n "coll-progress-track" styles/views.css` and `grep -n "coll-progress-fill" styles/views.css` → each ≥1; progress track height is `6px`.
    - `grep -n "#collectibles-section .table-scroll-container" styles/views.css` → ≥1 with `display: none` inside a `max-width: 768px` media query (collectibles now hides on mobile).
    - `grep -n "#collectibles-section .fc-card-list" styles/views.css` → ≥1 (mobile card container shown).
    - Chip classes present: `.coll-chip-invoiced`, `.coll-chip-collected`, `.coll-chip-outstanding`, `.coll-chip-overdue` all found; `.coll-chip-overdue` uses border color `#991b1b`.
    - `grep -c "coll-show-completed-toggle" styles/views.css` → ≥1.
    - No `!important` introduced in the new block.
    - CSS braces balanced (verify command exits 0).
  </acceptance_criteria>
  <done>The dedicated collectibles CSS block exists with all chip/progress/6-tier-urgency/mobile-card rules; #collectibles-section now in the ≤768px hide list; braces balanced; no inline rich visuals required by later plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore `collectibles` doc → render helpers | Field values (`amount_requested`, `payment_records`, `due_date`) read into pure computation; no DOM write in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99.2-01 | Tampering | getCollectibleUrgency numeric parse | mitigate | All amounts coerced via `parseFloat(...) || 0`; malformed due_date yields no overdue tier (guarded by `if (coll.due_date)`); no NaN propagates to a tier decision |
| T-99.2-02 | Information Disclosure | CSS-only block | accept | No data rendered in this plan; pure styling. No PII surface. |
| T-99.2-03 | Denial of Service | reduce over payment_records | accept | Bounded by per-doc payment_records length (small); no recursion |

No XSS surface in this plan — no helper writes to innerHTML. (XSS is addressed in Waves 2–3 where dynamic values render.)
</threat_model>

<verification>
- `node --check app/views/finance.js` passes.
- views.css braces balanced.
- Grep gates in both tasks' acceptance_criteria pass.
- No window functions added (no onclick handlers in this plan) — `attachWindowFunctions()` and `destroy()` are UNTOUCHED by this plan (confirm `git diff` shows no change to L373-399 / L4653-4675).
</verification>

<success_criteria>
- SC5 foundation: ONE shared overdue definition exists (D-08b) and Last Payment helper filters non-voided (D-08a).
- D-05a foundation: 6 urgency-tier CSS classes exist.
- D-06 foundation: collectibles table now hides at ≤768px and a `.fc-card-list` container rule exists for it.
- Zero behavior change visible in the browser yet (helpers unused, CSS classes unreferenced until Waves 2–3) — this is intentional foundation staging.
</success_criteria>

<uat_steps>
This plan ships no user-visible change (foundation only). UAT is deferred to Waves 2–3. Sanity check only:
1. `python -m http.server 8000`, open `#/finance` → Collectibles tab. Confirm the page still loads and the existing 10-col table renders unchanged (no regression from the deriveCollectibleStatus refactor).
2. Browser console: no new errors on Collectibles tab mount.
</uat_steps>

<output>
After completion, create `.planning/phases/99.2-collectibles-revamp-beta-finance/99.2-01-SUMMARY.md`
</output>
