---
phase: 99.2-collectibles-revamp-beta-finance
verified: 2026-06-06T00:00:00Z
status: passed
uat_approved: 2026-06-06
score: 5/5 must-haves verified statically; 7/7 browser UAT approved by operator
overrides_applied: 0
human_verification:
  - test: "Scorecard reactivity (SC1/D-02)"
    expected: "4 chips above the table show Total Invoiced / Cash Collected / Outstanding (and Overdue when >₱0). Applying a status/project filter recomputes the chips to match ONLY the visible rows. Filtering to 'Fully Paid' makes Outstanding/Overdue read ₱0 and the Overdue chip disappears."
    why_human: "Live Firestore aggregation + DOM re-render reflecting filtered rows cannot be exercised without a browser + real collectibles data."
  - test: "5-column table + urgency borders (SC3/D-05/D-05a)"
    expected: "Exactly 5 columns (Project·Tranche / Collection Progress / Due·Urgency / Last Payment / Actions). Col 1 merges name + tranche(%) + dept chip + monospace COLL id; a 6px progress bar shows '₱X of ₱Y · Z%'; a relative-due string renders ('N days overdue ‼' / 'Due in 5 days ⚠' / 'Due in 40 days'). Overdue rows show red/orange 4px left border, near-due amber, fully-paid green + dimmed."
    why_human: "Visual rendering of borders, progress bars, dept chips and merged-cell layout requires a browser; color/visual correctness is not statically assertable."
  - test: "Show-completed toggle + pagination recompute (SC3/D-05b/D-08c)"
    expected: "Fully-paid rows hidden by default; a 'Show N completed' toggle appears when N>0; clicking reveals them and the pagination count/pages grow to include them; clicking again hides them and the count drops. Page indicator never lands out of range."
    why_human: "Interactive toggle state + live pagination math over the visible set needs runtime clicks and a populated dataset."
  - test: "Last Payment void exclusion (SC5/D-08a)"
    expected: "A row with a recorded payment shows '₱amt · date · method'; a row with none shows italic 'No payments yet'. Voiding a payment in the history modal makes that row's Last Payment / Cash Collected stop counting the voided amount."
    why_human: "Requires the void action through the payment-history modal against live data; the non-voided filter logic is unit-verified but the end-to-end void→recompute path is runtime."
  - test: "Mobile ≤768px card layout (SC4/D-06)"
    expected: "Narrowing to ≤768px replaces the table with a card stack; each card shows the same 5-col content + the urgency left-border; Record Payment / View History buttons render full-width."
    why_human: "Responsive breakpoint behavior (table hidden / fc-card-list shown) is a CSS media-query effect only observable in a browser viewport."
  - test: "CSV non-regression (SC5/D-07)"
    expected: "Export CSV downloads a file with all 13 columns and includes fully-paid rows even while they are hidden in the table view."
    why_human: "File download + content inspection is a runtime artifact; the code path (getDisplayedCollectibles, 13 headers) is statically verified but the produced file should be eyeballed once."
  - test: "Two-section recovery banner + orphan flow (SC2/D-03/D-04)"
    expected: "With ≥1 pending billing request the banner shows 'Awaiting Your Review' (amber, Approve/Reject), each sub-section collapsing independently. Approve a pending request then DISMISS the create modal without submitting (simulate abandoned-create orphan) → the request moves to 'Approved — File as Collectible' (blue) with a 'File as Collectible' button and is NOT silently lost. Click File → the prefilled Create-Collectible modal opens with the correct project/service + tranche preselected; submitting it removes the orphan immediately and it does not reappear. Repeat for a service (department='services') request. With no pending and no approved orphans the whole banner is absent."
    why_human: "The core D-04 recovery guarantee involves live billing_requests + collectibles reconciliation across two onSnapshot streams and a modal round-trip — only verifiable end-to-end in a browser."
---

# Phase 99.2: Collectibles Revamp β — Finance Collectibles Page Verification Report

**Phase Goal:** Overhaul the standalone Finance Collectibles tab — add an aggregate scorecard, surface the approved-but-not-filed billing pipeline, redesign the table from 10 flat columns to 5 rich ones, and add the missing mobile card layout — so Finance identifies actionable receivables in seconds. Owns the approved-not-filed orphan-state fix deferred from Phase 99.
**Verified:** 2026-06-06
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| SC1 | 4-chip reactive scorecard (Total Invoiced / Cash Collected / Outstanding / Overdue — Overdue hidden at ₱0) above the table, reflecting currently-filtered rows | ✓ VERIFIED (static) | `renderCollectiblesScorecard(displayed)` L1434 aggregates over the SAME post-hide `displayed` array passed from `renderCollectiblesTable` L1502; 4 chip labels present verbatim; Overdue gated by `if (overdueOutstanding > 0)` L1454; uses `getCollectibleUrgency(coll).isOverdue` L1445 (no `collectiblesData.forEach` inside). Host `#collectiblesScorecard` at markup L4026. |
| SC2 | Unified collapsible banner, two independent sub-sections; sub-section 2 = approved orphans (dept-aware code + tranche_index), File button calls openCreateCollectibleModal(preselectKey) | ✓ VERIFIED (static) | `renderPendingBillingBanner` L1831 renders one outer container + "Awaiting Your Review" (L1867) + "Approved — File as Collectible" (L1898) with independent collapse flags (`billingBannerCollapsed` / `approvedBannerCollapsed`). Auto-hides only when both empty (L1836). Sub-section 2 sources `getOrphanApprovedBillingRequests()` (L1835). `fileCollectibleFromBanner` L1916 builds the 3-segment preselectKey and calls `openCreateCollectibleModal(preselectKey)` L1923 — identical key shape to `approveBillingRequest` L1936-1939. |
| SC3 | Table 10→5 cols with 4px urgency left-borders (6 tiers) and fully-paid rows hidden behind "Show N completed" toggle | ✓ VERIFIED (static) | thead = exactly 5 `<th>` (L4031-4035); row uses `<tr class="coll-urgency-${urg.tier}">` L1565; 6 tiers in CSS L2198-2203; `grep -c 'colspan="10"'` = 0; hide applied in render path (L1494) with `collShowCompleted` flag; toggle rendered via `renderCollShowCompletedToggle` only when `completedCount > 0` (L1474); fully-paid Actions render View History only (L1557). |
| SC4 | ≤768px card layout via dual-mode CSS; rich visuals in a real CSS block, not inline | ✓ VERIFIED (static) | `buildCollectibleCard(coll)` L1659 mirrors `.fc-card` anatomy with `coll-urgency-${urg.tier}` border; `#collectiblesCardList` host (class `fc-card-list`) at markup L4044, populated L1591; CSS `@media (max-width:768px)` (L2216-2220) hides `#collectibles-section .table-scroll-container` and shows `.fc-card-list`. All urgency/progress/chip visuals live in the dedicated views.css block L2175-2220. |
| SC5 | Build correctness: Last Payment !=='voided'; ONE shared overdue def; hide recomputes pagination; CSV keeps 13 cols + fully-paid rows | ✓ VERIFIED (static) | `getCollectibleLastPayment` L159 filters `r.status !== 'voided'` before max-by-date; `getCollectibleUrgency` L129 is the only overdue definition (`grep -c "new Date(coll.due_date) < new Date()"` = 0; `deriveCollectibleStatus` delegates via `.isOverdue` L114); `renderCollectiblesTable`/`changeCollectiblesPage`/scorecard all recompute over the visible set (L1497-1498, L1634-1636); `exportCollectiblesCSV` L2883 calls `getDisplayedCollectibles()` directly (NOT post-hide) with 13 headers (L2895-2909) and CSV-injection mitigation preserved (L2893). |

**Score:** 5/5 must-haves verified statically. All require browser UAT to confirm runtime/visual behavior (see Human Verification).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/views/finance.js` :: getCollectibleUrgency | Single-source 6-tier urgency/overdue classifier (D-08b) | ✓ VERIFIED | L129; returns `{tier,daysOverdue,daysUntilDue,isOverdue}`; 6 tiers verbatim; unit-tested 8/8 logic paths PASS (incl. fully-paid override, voided exclusion, no-due-date, due-today-not-overdue). |
| `app/views/finance.js` :: getCollectibleLastPayment | Newest non-voided payment (D-08a) | ✓ VERIFIED | L159; filters voided before reduce-by-date; unit-tested PASS (skips voided, null on empty/all-voided). |
| `app/views/finance.js` :: renderCollectiblesScorecard | 4-chip reactive scorecard | ✓ VERIFIED | L1434; aggregates passed `displayed`, Overdue gated at ₱0, shared helper. |
| `app/views/finance.js` :: renderCollectiblesTable | 5-col body + hide toggle + pagination recompute + card-list populate | ✓ VERIFIED | L1488; substantive 5-col row map, urgency class on `<tr>`, progress bar, relative-due, last-payment. |
| `app/views/finance.js` :: buildCollectibleCard | Mobile card mirroring 5-col content | ✓ VERIFIED | L1659; `.fc-card` anatomy + urgency border; not on window. |
| `app/views/finance.js` :: getOrphanApprovedBillingRequests | Dept-aware code + tranche_index reconciliation | ✓ VERIFIED | L1816; `.some()` on `code === reqCode && c.tranche_index === req.tranche_index`; unit-tested PASS (matched/diff-tranche/service-side). |
| `app/views/finance.js` :: renderPendingBillingBanner | Two-section unified banner | ✓ VERIFIED | L1831; two independently-collapsible sub-sections, auto-hide when both empty, all strings escaped. |
| `app/views/finance.js` :: fileCollectibleFromBanner | Bridge to existing prefilled modal | ✓ VERIFIED | L1916; reuses `openCreateCollectibleModal(preselectKey)`, exact key shape. |
| `styles/views.css` :: COLLECTIBLES REVAMP β block | Chips, 6px bars, 6 urgency borders, mobile cards | ✓ VERIFIED | L2175-2220; all classes present; no `!important` in block; braces balanced (661). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| deriveCollectibleStatus L114 | getCollectibleUrgency | `.isOverdue` branch | ✓ WIRED | L114 `const isOverdue = getCollectibleUrgency(coll).isOverdue;` — status & tier cannot disagree. |
| renderCollectiblesTable | renderCollectiblesScorecard(displayed) | same post-hide array | ✓ WIRED | L1502 called with the same `displayed` the table paginates (D-02). |
| renderCollectiblesTable / changeCollectiblesPage | fully-paid hide | `collShowCompleted`, applied OUTSIDE getDisplayedCollectibles | ✓ WIRED | Hide in render (L1494) + page-change (L1634); `getDisplayedCollectibles` L1381-1426 has no `collShowCompleted`/hide → CSV preserved (D-07). |
| row map + buildCollectibleCard | getCollectibleUrgency / getCollectibleLastPayment | shared helpers | ✓ WIRED | L1531-1532 (table), L1664-1665 (card). |
| brUnsub billing_requests listener | pending + approved split | widened query (no status filter) | ✓ WIRED | L1791-1806; un-filtered query, client split, exactly ONE `listeners.push(brUnsub)`. |
| approved billing_requests | collectiblesData reconciliation | `.some()` on dept-aware code + tranche_index | ✓ WIRED | getOrphanApprovedBillingRequests L1816-1825. |
| 'File as Collectible' button | openCreateCollectibleModal(preselectKey) | reuse existing key shape | ✓ WIRED | fileCollectibleFromBanner L1916-1924. |
| collectibles onSnapshot | renderPendingBillingBanner | re-render so filed COLL drops its orphan | ✓ WIRED | L1744 inside collectibles snapshot handler. |
| window.toggleCollShowCompleted / toggleApprovedBanner / fileCollectibleFromBanner | attachWindowFunctions + destroy | register + teardown (CLAUDE.md SPA contract) | ✓ WIRED | Register L424/L450/L451 (in attachWindowFunctions @344); teardown L4936/L4958/L4959 (in destroy @4826). All three symmetric. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| finance.js parses | `node --check app/views/finance.js` | exit 0 | ✓ PASS |
| views.css braces balanced | brace-count node script | 661 balanced | ✓ PASS |
| getCollectibleUrgency tier logic | isolated node unit test, 8 cases | 8/8 PASS (paid override, 40d→critical, 10d→overdue, 5d→near-due, partial, no-due→healthy, voided ignored, due-today→not overdue) | ✓ PASS |
| getCollectibleLastPayment void exclusion | isolated node unit test, 3 cases | 3/3 PASS (newest non-voided, null on empty, null on all-voided) | ✓ PASS |
| getOrphanApprovedBillingRequests reconciliation | isolated node unit test, 5 cases | 4/5 PASS — cross-dept-code collision (project code "X" vs service code "X" same tranche) suppresses an orphan; matches the planned PATTERNS.md spec exactly (see Anti-Patterns / INFO) | ✓ PASS (matches spec) |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| app/views/finance.js | L1816-1825 | Reconciliation matches on dept-aware code value WITHOUT also comparing `department`; a project_code equal to a service_code at the same tranche_index would suppress an orphan | ℹ️ Info | Matches the authoritative PATTERNS.md §reconciliation spec (L91-97) byte-for-byte and mirrors two existing sites (`getDisplayedCollectibles` L1339, `approveBillingRequest` key). The create-modal preselectKey IS department-namespaced (`projects:` vs `services:`), so the write side is safe. Collision requires identical code strings across a project and a service simultaneously sharing a tranche index — a domain-level edge, not a regression introduced by this phase. No must-have violated. Noted for human awareness only. |

No TODO/FIXME/placeholder/stub markers found in any touched region (L100-170, L1380-1960, L2880-2950). The single "Loading collectibles..." static row is the legitimate pre-render placeholder, now correctly `colspan="5"`.

### Decisions Coverage (D-02..D-09)

| Decision | Status | Evidence |
|----------|--------|----------|
| D-02 scorecard reflects filtered rows, Overdue hidden at ₱0 | ✓ | L1434/L1454/L1502 |
| D-03 unified banner, two independent sub-sections, File→existing modal | ✓ | L1831/L1916 |
| D-04 recovery-not-prevention; approve stays status flip; orphan never silently absent | ✓ | approveBillingRequest L1944 is `updateDoc status:'approved'` only, no `addDoc`/`setDoc` to collectibles inside approve/reject (the only collectibles `addDoc` L2318 is the create-modal submit); reconciliation L1816 |
| D-05 5 composite columns | ✓ | thead L4031-4035 + row map L1564-1580 |
| D-05a 4px urgency left-borders, 6 tiers | ✓ | CSS L2198-2203, `<tr>` L1565 |
| D-05b fully-paid hidden behind Show-N-completed; pagination recomputes | ✓ | L1494/L1497/L1634 |
| D-06 ≤768px card layout via dual-mode CSS | ✓ | CSS L2216-2220, buildCollectibleCard L1659 |
| D-07 CSV keeps all 13 cols + fully-paid rows | ✓ | exportCollectiblesCSV L2883 calls getDisplayedCollectibles directly, 13 headers |
| D-08a Last Payment filters non-voided | ✓ | getCollectibleLastPayment L160 |
| D-08b ONE shared overdue definition | ✓ | getCollectibleUrgency L129; `grep -c "new Date(coll.due_date) < new Date()"` = 0 |
| D-08c hide recomputes pagination | ✓ | L1497-1498, L1635-1636 |
| D-09 dept-aware reconciliation key (project_code/service_code + tranche_index), money-in vocabulary | ✓ | getOrphanApprovedBillingRequests L1818-1821; scorecard labels Invoiced/Collected/Outstanding |

### Requirements Coverage

REQUIREMENTS.md maps no IDs for this phase ("None mapped yet" — ROADMAP L1071). The SC/D codes in each PLAN's `requirements:` frontmatter are the contract and are all accounted for:

| Plan | requirements: | Status |
|------|---------------|--------|
| 99.2-01 | SC5, D-08a, D-08b, D-05a, D-06 | ✓ all verified (helpers + CSS foundation) |
| 99.2-02 | SC1, SC3, SC4, SC5, D-02, D-05, D-05a, D-05b, D-06, D-07, D-08c, D-09 | ✓ all verified (scorecard + 5-col table + mobile + pagination + CSV) |
| 99.2-03 | SC2, D-03, D-04, D-09 | ✓ all verified (two-section banner + reconciliation + File bridge) |

No orphaned requirements.

### Deferred Items

None. Phase 99.2 is the highest-numbered phase in the milestone roadmap; no later phase exists to defer to.

### Human Verification Required

7 browser-UAT items (zero-build SPA, no UI test harness — runtime/visual behavior cannot be asserted statically). See the `human_verification` frontmatter for full test/expected/why. Summary:

1. **Scorecard reactivity (SC1/D-02)** — chips recompute to the filtered set; Overdue disappears at ₱0.
2. **5-col table + urgency borders (SC3/D-05/D-05a)** — 5 columns, progress bars, relative-due strings, colored 4px borders.
3. **Show-completed toggle + pagination (SC3/D-05b/D-08c)** — fully-paid hidden by default; toggle grows/shrinks pagination; page never out of range.
4. **Last Payment void exclusion (SC5/D-08a)** — voiding a payment drops it from Last Payment + Cash Collected.
5. **Mobile ≤768px cards (SC4/D-06)** — table replaced by card stack with urgency borders.
6. **CSV non-regression (SC5/D-07)** — exported file has 13 columns + fully-paid rows even while hidden.
7. **Two-section recovery banner + orphan flow (SC2/D-03/D-04)** — abandoned-create orphan surfaces under "Approved — File as Collectible", File preselects correctly, filing clears it; service-side parity; auto-hide when empty.

### Gaps Summary

No gaps. Every static check passes: `node --check` clean, CSS braces balanced, all 5 ROADMAP Success Criteria implemented in substantive (non-stub) code, all key links wired, all 12 decision codes (D-02..D-09) honored, window-function register/teardown symmetry confirmed for all three new handlers, exactly one billing_requests listener, reconciliation correct per the planned spec, CSV-preserving hide applied outside getDisplayedCollectibles, single-source overdue definition with zero inline duplicates. Isolated unit tests of the three pure helpers pass. The status is `human_needed` (not `passed`) solely because this is a zero-build SPA with no automated UI harness — the 7 runtime/visual behaviors above require manual browser UAT to fully confirm goal achievement. One INFO-level note (cross-dept reconciliation key) matches the authoritative plan spec and is not a defect.

---

_Verified: 2026-06-06_
_Verifier: Claude (gsd-verifier)_
