---
phase: 99.1-collectibles-revamp-alpha-detail
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/expense-modal.js
autonomous: true
requirements: [CDR-05]
tags: [collectibles, billing, expense-modal, scorecard, firestore, ghost-row]

must_haves:
  truths:
    - "The Full Breakdown modal header strip shows 5 chips: Expense / Rem. Payable / Billed to Client / Invoiced (COLLs) / Cash Received (CDR-05 / D-17)"
    - "'Billed to Client' is computed from a NEW billing_requests fetch (approved amount_requested, D-01) keyed by project_code (project mode) or service_code (service mode) — the modal did not fetch billing_requests before (D-17)"
    - "The Collectibles tab shows a 3-chip summary (Total Billed / Cash Collected / Outstanding) plus a formula note spelling out Billed (billing_requests) vs Invoiced (collectibles) (CDR-05 / D-18)"
    - "The Collectibles tab shows muted 'awaiting COLL-xxx' ghost rows for approved billing_requests with no matching collectible at the same tranche_index (CDR-05 / D-19)"
    - "All cash sums reuse the canonical non-voided payment_records[].amount formula already at expense-modal.js:24/585-589 — no divergent denominator (D-13)"
  artifacts:
    - path: "app/expense-modal.js"
      provides: "billing_requests fetch (approved) parallel to the collectibles fetch, mode-aware key"
      contains: "billing_requests"
    - path: "app/expense-modal.js"
      provides: "5-chip money-in header strip + 3-chip Collectibles-tab summary + formula note + ghost rows"
      contains: "Billed to Client"
  key_links:
    - from: "expense-modal.js header strip"
      to: "approved billing_requests + collectibles totals"
      via: "5-chip render block"
      pattern: "Billed to Client"
    - from: "expense-modal.js Collectibles tab"
      to: "approved billing_requests with no collectible"
      via: "ghost-row reconcile by tranche_index"
      pattern: "awaiting COLL"
---

<objective>
Revamp the shared Full Breakdown modal (`expense-modal.js`, used by both project and service detail) per spike 026 (CDR-05): add a 5-chip money-in header strip, fetch approved `billing_requests` (new — the modal never queried them), surface a 3-chip Collectibles-tab summary with a Billed-vs-Invoiced formula note, and render muted "awaiting COLL-xxx" ghost rows for approved-but-not-yet-invoiced tranches.

Purpose: Make the Billed → Invoiced → Collected → Outstanding distinction explicit in the modal, including the currently-invisible "approved but not yet invoiced" state (the exact bug spike 026 flagged).
Output: Modified `app/expense-modal.js`. Works in both `mode:'project'` and `mode:'service'`; the new billing_requests fetch keys off project_code/service_code per mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99.1-collectibles-revamp-alpha-detail/99.1-CONTEXT.md
@.planning/phases/99.1-collectibles-revamp-alpha-detail/99.1-PATTERNS.md

<interfaces>
<!-- Vocabulary (D-01..D-04) — keep exact: -->
<!-- Billed (to client) = approved billing_requests.amount_requested (advisory). -->
<!-- Invoiced = collectibles.amount_requested (frozen COLL amount). -->
<!-- Cash Received / Collected = non-voided payment_records[].amount. -->
<!-- Outstanding = Invoiced − Collected. -->

In-scope values ALREADY computed in showExpenseBreakdownModal:
  totalCost            (expense-modal.js:666)  → "Expense"
  remainingPayable     (expense-modal.js:667)  → "Rem. Payable"
  collTotalRequested   (expense-modal.js:584)  → "Invoiced (COLLs)"
  collTotalCollected   (expense-modal.js:585-589) → "Cash Received"
  collRemaining        (expense-modal.js:590)  → Outstanding (3-chip)

MISSING value needing a NEW fetch:
  "Billed to Client"   = sum of approved billing_requests.amount_requested (D-01)

Mode key: project mode re-fetches the project doc to get project_code (expense-modal.js:101-105 →
  projectCodeForColl); service mode identifier IS service_code. Reuse projectCodeForColl for the
  new billing_requests query so no extra projects fetch is added.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fetch approved billing_requests (mode-aware) + 5-chip money-in header strip (CDR-05 / D-17)</name>
  <files>app/expense-modal.js</files>
  <read_first>
    - app/expense-modal.js:17-58 (signature + mode branching, identifier semantics), :89-112 (collectibles fetch — clone its mode branching + reuse projectCodeForColl), :662-668 (totalCost/remainingPayable), :584-590 (coll totals), :796-842 (scorecard area to extend)
    - 99.1-PATTERNS.md §5 (5-chip strip + the new billing_requests fetch + the projectCodeForColl reuse caveat)
  </read_first>
  <behavior>
    - billingReqsForModal = approved billing_requests for this project/service.
      - service mode: where('service_code','==', identifier)
      - project mode: where('project_code','==', projectCodeForColl) — reuse the code already resolved at expense-modal.js:105; skip the query entirely if projectCodeForColl is empty.
      - filter to status === 'approved' (in code or via a where clause; either is acceptable).
    - billedToClient = sum of billingReqsForModal[].amount_requested.
    - The 5-chip strip renders: Expense (totalCost) / Rem. Payable (remainingPayable) / Billed to Client (billedToClient) / Invoiced (collTotalRequested) / Cash Received (collTotalCollected).
  </behavior>
  <action>
    Add a billing_requests fetch parallel to the existing collectibles fetch (expense-modal.js:92-112). Inside each mode branch, after the collectibles fetch, query approved billing_requests:
      - service mode: `const brSnap = await getDocs(query(collection(db, 'billing_requests'), where('service_code', '==', identifier)));`
      - project mode: reuse `projectCodeForColl` — `if (projectCodeForColl) { const brSnap = await getDocs(query(collection(db, 'billing_requests'), where('project_code', '==', projectCodeForColl))); ... }`
    Accumulate into a module-local `let billingReqsForModal = [];` (declared near collectiblesForTab at :92), pushing `{ id, ...d.data() }`. Keep only approved: filter `r => r.status === 'approved'` either at push time or when summing.
    Compute `const billedToClient = billingReqsForModal.filter(r => r.status === 'approved').reduce((s, r) => s + (parseFloat(r.amount_requested) || 0), 0);` near the other coll totals (after expense-modal.js:590).

    Add a 5-chip money-in strip in the scorecard area. Insert a new `<div class="em-scorecard-3col">`-style block (or two rows) AFTER the existing Row-3 block (after expense-modal.js:842, before the tab bar at :844). Each chip uses the existing chip markup (label div + ₱ value div). Order: Expense / Rem. Payable / Billed to Client / Invoiced (COLLs) / Cash Received. Use `formatCurrency(...)` and the `&#8369;` peso glyph already used in the file. Give the strip a section sublabel like "Money-In" so it reads distinctly from the cost scorecards. Distinct color families per the existing convention (green #f0fdf4/#166534, blue #eff6ff/#1d4ed8). Service-mode "Billed to Client" may legitimately be 0 until service billing requests exist (Plan 03) — render it the same regardless.
  </action>
  <verify>
    <automated>node --check app/expense-modal.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/expense-modal.js` exits 0
    - expense-modal.js now queries billing_requests: `grep -c "collection(db, 'billing_requests')" app/expense-modal.js` returns >= 1
    - The service-mode key uses service_code and project-mode reuses projectCodeForColl: grep finds `where('service_code', '==', identifier)` in a billing_requests query AND `where('project_code', '==', projectCodeForColl)` in a billing_requests query
    - expense-modal.js contains `billedToClient` and the chip label `Billed to Client`
    - The strip renders all five labels: grep finds `Rem. Payable`, `Billed to Client`, and `Cash Received` (Expense/Invoiced labels may reuse existing wording — confirm `Cash Received` present)
    - billedToClient sum uses approved filter: grep finds `r.status === 'approved'` near the billedToClient reduce
  </acceptance_criteria>
  <done>
    The modal fetches approved billing_requests (mode-aware, no extra projects fetch) and renders a 5-chip money-in header strip.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 3-chip Collectibles-tab summary + formula note + ghost rows (CDR-05 / D-18 / D-19)</name>
  <files>app/expense-modal.js</files>
  <read_first>
    - app/expense-modal.js:562-660 (Collectibles tab markup), :584-590 (totals reused for 3 chips), :617-655 (the per-collectible row .map to augment with ghost rows), :642-654 (existing row markup to mirror style)
    - billingReqsForModal added in Task 1
    - 99.1-PATTERNS.md §6 (3-chip summary + formula note + ghost-row reconcile by tranche_index)
  </read_first>
  <behavior>
    - 3-chip summary above the collectibles table: Total Billed (billedToClient, from billing_requests) / Cash Collected (collTotalCollected) / Outstanding (collRemaining).
    - A formula note distinguishing: "Billed = what ops requested (billing_requests). Invoiced = what Finance issued as COLL-xxx (collectibles). Outstanding = Invoiced − Collected."
    - Ghost rows: for each approved billing_request whose tranche_index has NO matching collectible in collectiblesForTab, render a muted row with "awaiting COLL-xxx" in the ID column, the tranche label, and the billing_request.amount_requested. (D-05 in-page reconcile = tranche_index.)
  </behavior>
  <action>
    Inside the Collectibles tab markup (expense-modal.js:592-660), ABOVE the `.category-card`/table, add a 3-chip summary block (reuse the chip markup from Task 1): Total Billed = `billedToClient`, Cash Collected = `collTotalCollected`, Outstanding = `collRemaining` (color red #ef4444 when > 0). Directly under the chips add a formula note `<div style="font-size:0.72rem;color:#64748b;...">` spelling out the Billed vs Invoiced vs Outstanding distinction (D-18 exact wording — surface BOTH numbers; never conflate Billed with Invoiced).

    Compute ghost rows before the table: `const ghosts = billingReqsForModal.filter(br => br.status === 'approved' && !collectiblesForTab.some(c => c.tranche_index === br.tranche_index));`
    Render ghost rows in the SAME `<table class="modal-items-table">` AFTER the existing `collectiblesForTab.map(...)` rows. Each ghost `<tr>` styled muted (e.g. `opacity:0.6;font-style:italic;color:#94a3b8;`): ID cell = `awaiting COLL-xxx`, TRANCHE cell = `escapeHTML(br.tranche_label || '')` + `(${pct}%)`, STATUS cell = "Approved — awaiting invoice", AMOUNT cell = `formatCurrency(br.amount_requested || 0)`, PAID/BALANCE cells blank or "—". escapeHTML() all interpolated strings.

    Guard: the existing tab uses the ternary `collectiblesForTab.length > 0 ? (...) : '<p>No collectibles recorded.</p>'`. Update so that when there are ghosts but no collectibles, the table still renders (i.e. condition becomes `collectiblesForTab.length > 0 || ghosts.length > 0`), and the 3-chip summary + formula note render whenever there is any money-in data.
  </action>
  <verify>
    <automated>node --check app/expense-modal.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/expense-modal.js` exits 0
    - expense-modal.js contains the ghost-row reconcile: `grep -F "!collectiblesForTab.some(c => c.tranche_index === br.tranche_index)" app/expense-modal.js` returns >= 1
    - expense-modal.js contains `awaiting COLL` (ghost ID placeholder)
    - The 3-chip summary labels present: grep finds `Total Billed` and `Cash Collected` and `Outstanding`
    - A formula note distinguishing Billed vs Invoiced exists: grep finds both the words `Billed` and `Invoiced` within the Collectibles tab block (the note text)
    - The empty-tab guard now considers ghosts: grep finds `ghosts.length > 0` in the collectiblesHTML condition
    - All ghost-row interpolations escaped: grep confirms `escapeHTML(br.tranche_label` appears
  </acceptance_criteria>
  <done>
    The Collectibles tab shows a 3-chip Billed/Collected/Outstanding summary with a Billed-vs-Invoiced formula note, plus muted ghost rows for approved-but-not-yet-invoiced tranches.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore (read billing_requests + collectibles) | the modal renders tranche_label, coll_id, doc data — user-authored strings — into the breakdown table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99.1-04 | Information disclosure / XSS | ghost rows + 3-chip note (tranche_label, coll_id) | mitigate | escapeHTML() on every interpolated string in the new ghost-row + chip markup (matches existing modal rows at :643-647). |
| T-99.1-05 | Spoofing / data integrity | "Billed to Client" sourced from advisory billing_requests | accept | Read-only display; the chip is explicitly labeled Billed (advisory) and distinguished from Invoiced (authoritative) by the formula note (D-18). No write path. |
</threat_model>

<verification>
- `node --check app/expense-modal.js` passes after each task.
- All acceptance grep gates pass.
- Manual sanity: open Full Breakdown on a project with an approved-but-not-invoiced tranche → header shows Billed > Invoiced, Collectibles tab shows a ghost row; once invoiced, the ghost row disappears.
</verification>

<success_criteria>
- 5-chip money-in header strip with a new approved-billing_requests fetch (CDR-05 / D-17).
- 3-chip Collectibles-tab summary + Billed-vs-Invoiced formula note (CDR-05 / D-18).
- "awaiting COLL-xxx" ghost rows reconciled by tranche_index (CDR-05 / D-19).
- All cash sums reuse the canonical voided-excluded formula (D-13).
</success_criteria>

<output>
After completion, create `.planning/phases/99.1-collectibles-revamp-alpha-detail/99.1-02-SUMMARY.md`. Record: the chip markup helper/snippet used (so Plan 03's service-mode call reuses the same modal), and the exact ghost-row condition.
</output>
