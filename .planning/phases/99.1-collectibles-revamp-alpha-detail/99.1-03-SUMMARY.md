---
phase: 99.1-collectibles-revamp-alpha-detail
plan: 03
subsystem: ui
tags: [collectibles, billing, service-detail, port, lifecycle, firestore, notifications, department]

requires:
  - phase: 99.1-01
    provides: computeTrancheLifecycle contract, renderTrancheLifecycleRows pattern, department discriminator convention
  - phase: 99.1-02
    provides: shared expense modal money-in revamp (renders for mode:'service' automatically)
  - phase: 99-billing-request-flow
    provides: the full project-side billing modal stack (port source)
provides:
  - "service-detail.js full Phase 99 billing capability: modal + submitBillingRequest(department:'services') + own-requests listener + Finance notif"
  - "service collectibles listener + computeTrancheLifecycle (independent copy) + per-tranche lifecycle footer"
  - "service 2-chip Collected+Outstanding + Full Breakdown header button + window-split collapse"
affects: [99.1-04-finance]

tech-stack:
  added: []
  patterns:
    - "Cross-file net-new port (project-detail.js billing stack -> service-detail.js) adapted to currentService/service_code"

key-files:
  created: []
  modified: [app/views/service-detail.js]

key-decisions:
  - "service billing_requests carry department:'services' + service_code + service_name (the field Plan 04's finance reader keys on)"
  - "Independent copy of computeTrancheLifecycle (no shared module exists for these views — CONTEXT-sanctioned)"
  - "Full Breakdown button placed beside Export CSV in the existing flex header; Initiate Billing entry placed in a new Collection Lifecycle footer block"

patterns-established:
  - "Service money-in parity = project parity: same helper, same stage->color map, same modal (now mode:'service')"

requirements-completed: [CDR-06, CDR-07]

duration: ~35min
completed: 2026-06-06
---

# Phase 99.1 Plan 03: Service Detail Billing Port + Display Revamp Summary

**Full Phase 99 billing flow ported to service-detail.js (modal + submit with department:'services' + own-requests/collectibles listeners + Finance notification) plus the 025/026 display revamp (lifecycle footer, 2-chip Collected/Outstanding, Full Breakdown header button).**

## Performance
- **Duration:** ~35 min
- **Tasks:** 3
- **Files modified:** 1 (`app/views/service-detail.js`)

## Accomplishments
- **CONFIRMED for Plan 04:** service `submitBillingRequest()` writes `billing_requests` docs carrying `department: 'services'` + `service_code` + `service_name` (plus the frozen D-04 fields, `status:'pending'`, `requested_by_*`, `requested_at: serverTimestamp()`). This is the exact field Plan 04's finance reader keys on.
- Extended imports (`addDoc`, `serverTimestamp`; `createNotificationForRoles`).
- Added `ensureServiceBillingRequestsListener` + `ensureServiceCollectiblesListener` (both scoped to `currentService.service_code`), called in the services snapshot handler, torn down in `destroy()`.
- Copied `computeTrancheLifecycle` verbatim from project-detail.js (independent copy).
- Ported the full billing modal stack (`BILLING_DOCS`, `_hintBillingType`, `openBillingRequestModal`, `_onBillingTrancheChange`, `_selectBillingType`, `_renderBillingDocFields`, `_validateBillingForm`, `submitBillingRequest`) adapted to `currentService`; amount = `(tranchePct/100)*currentService.contract_cost`.
- Finance `BILLING_REQUEST_SUBMITTED` fan-out (fire-and-forget, own try/catch — submit never breaks on notif failure).
- Display revamp: `renderServiceTrancheLifecycle()` footer with the new "↑ Initiate Billing →" entry; relabeled "Remaining Collectible" → "Outstanding"; "Full Breakdown →" header button (`window.openServiceFullBreakdown`); removed the buried Projected-Cost click + 🔄 + hint; collapsed the show/refresh window split.

## Task Commits
1. **Task 1: imports + listeners + computeTrancheLifecycle** - `386cb32` (feat)
2. **Task 2: billing modal port + submit(department:'services') + Finance notif** - `5fac454` (feat)
3. **Task 3: lifecycle footer + 2-chip + Full Breakdown + window collapse** - `d3bea16` (feat)

## Files Created/Modified
- `app/views/service-detail.js` - imports, module vars, 2 listeners, computeTrancheLifecycle, full billing modal stack, submitBillingRequest, Finance notif, lifecycle footer, scorecard relabel, header button, window-split collapse.

## Decisions Made
- Billing window-fn teardown (`delete window.openBillingRequestModal` etc. + `delete window.openServiceFullBreakdown`) was placed in `destroy()` during Task 1 so the listener/modal cleanup lives in one block; the registrations were added in Tasks 2 & 3.

## Adaptation deltas from the project port
- `currentProject` → `currentService`; `project_code/project_name` → `service_code/service_name`; amount sourced from `currentService.contract_cost`.
- Service expense agg was ALREADY collectible-doc-sourced (service-detail.js:986) — no D-04 landmine fix needed; only the label changed.
- Modal reuse: `window.openServiceFullBreakdown` calls the shared `showExpenseBreakdownModal(..., {mode:'service'})`, so Plan 02's money-in chips + ghost rows render for services automatically.

## Deviations from Plan
None - plan executed as written.

## Issues Encountered
None. `node --check` passes; all per-task acceptance greps pass.

## Next Phase Readiness
- `department:'services'` writer is live → Plan 04's finance reader (dept-aware preselectKey + banner + notify link) can now be wired and exercised end-to-end.

---
*Phase: 99.1-collectibles-revamp-alpha-detail*
*Completed: 2026-06-06*
