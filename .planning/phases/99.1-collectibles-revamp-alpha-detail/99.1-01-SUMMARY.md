---
phase: 99.1-collectibles-revamp-alpha-detail
plan: 01
subsystem: ui
tags: [collectibles, billing, project-detail, lifecycle, scorecard, firestore, onsnapshot]

requires:
  - phase: 99-billing-request-flow
    provides: ensureBillingRequestsListener pattern, submitBillingRequest, billing_requests schema
provides:
  - "ensureCollectiblesListener() + currentCollectibleDocs raw-doc array scoped to project_code"
  - "computeTrancheLifecycle(tranche, idx, billingReqs, collectibleDocs) canonical stage helper (Plan 03 mirrors verbatim)"
  - "renderTrancheLifecycleRows() per-tranche lifecycle footer (replaces renderOwnBillingRequests)"
  - "2-chip Collected + Outstanding scorecard sourced from collectible docs (D-04 landmine fixed)"
  - "window.openFullBreakdown single always-refresh modal entry"
  - "department:'projects' written on submitBillingRequest (D-22 symmetry)"
affects: [99.1-03-service-detail, 99.1-04-finance]

tech-stack:
  added: []
  patterns:
    - "Sibling raw-doc onSnapshot listener (collectibles by project_code) beside ensureBillingRequestsListener"
    - "Single computeTrancheLifecycle helper = one source of truth for stage + cash %"

key-files:
  created: []
  modified: [app/views/project-detail.js]

key-decisions:
  - "Footer renderer RENAMED renderOwnBillingRequests -> renderTrancheLifecycleRows (single call-site updated)"
  - "2 card chips computed inline from currentCollectibleDocs (live) so card + listener agree, not from currentCollectibles aggregate"
  - "refreshExpense now sources the Invoiced figure from collectibles.amount_requested, dropping the approved-billing_requests query"

patterns-established:
  - "computeTrancheLifecycle return shape: { stage, badgeLabel, badgeColor, opacity, pct, totalPaid, amountRequested, note }"
  - "Stage->color map: not-filed #94a3b8 (opacity 0.45, dashed) / pending #f59e0b / rejected #ef4444 / approved-not-invoiced #4f46e5 / invoiced-awaiting #0d9488 / collecting #0d9488 (+note) / fully-collected #059669"

requirements-completed: [CDR-01, CDR-02, CDR-03, CDR-04]

duration: ~25min
completed: 2026-06-06
---

# Phase 99.1 Plan 01: Project Detail Collectibles Revamp Summary

**Per-tranche lifecycle footer + raw-collectibles listener + 2-chip Collected/Outstanding scorecard (D-04 fixed) + single Full Breakdown header button on project-detail.js, plus the canonical computeTrancheLifecycle helper Plan 03 mirrors.**

## Performance
- **Duration:** ~25 min
- **Tasks:** 3
- **Files modified:** 1 (`app/views/project-detail.js`)

## Accomplishments
- Added `ensureCollectiblesListener()` streaming raw collectible docs (with `payment_records`) scoped to `currentProject.project_code` into a new `currentCollectibleDocs` array; attached beside both `ensureBillingRequestsListener()` call-sites; torn down in `destroy()`.
- Added the canonical `computeTrancheLifecycle(tranche, idx, billingReqs, collectibleDocs)` helper deriving stage + cash % via the voided-excluded `payment_records[].amount` formula (D-13). **This is the reference Plan 03 copies verbatim.**
- Replaced the status-only `renderOwnBillingRequests()` with `renderTrancheLifecycleRows()` iterating EVERY `collection_tranche` (unfiled rows at 45% opacity + dashed badge, collecting rows with a "₱X of ₱Y · Z%" note).
- Relabeled the 2 card chips to **Collected + Outstanding**, both computed live from `currentCollectibleDocs`; fixed the `refreshExpense` D-04 landmine (Invoiced now sourced from `collectibles.amount_requested`, not approved billing_requests).
- Replaced the buried click-the-number + 🔄 affordance with one **Full Breakdown →** header button (`window.openFullBreakdown`, always refresh-then-open); collapsed the `showExpenseModal`/`refreshAndShowExpenseModal` split.
- Added `department: 'projects'` to the `submitBillingRequest` addDoc payload (D-22 symmetry).

## Canonical computeTrancheLifecycle contract (Plan 03 MUST mirror)

Returns `{ stage, badgeLabel, badgeColor, opacity, pct, totalPaid, amountRequested, note }`:

| Inputs (matched by tranche_index) | stage | badgeLabel | badgeColor | opacity | note |
|---|---|---|---|---|---|
| no billing_request AND no collectible | `not-filed` | `— Not Filed` | `#94a3b8` | `0.45` | — (dashed badge) |
| billing status `pending` | `pending` | `Pending Review` | `#f59e0b` | 1 | — |
| billing status `rejected` | `rejected` | `Rejected` | `#ef4444` | 1 | — |
| approved + no collectible | `approved-not-invoiced` | `Approved — Not Yet Invoiced` | `#4f46e5` | 1 | — |
| collectible + pct === 0 | `invoiced-awaiting` | `Invoiced — Awaiting Payment` | `#0d9488` | 1 | — |
| collectible + 0 < pct < 100 | `collecting` | `Billed / Collecting` | `#0d9488` | 1 | `₱{paid} of ₱{req} · {pct}%` |
| collectible + pct >= 100 | `fully-collected` | `Fully Collected ✓` | `#059669` | 1 | — |

`pct = Math.round(totalPaid / amountRequested * 100)` with `amountRequested` guarded `>0`; `totalPaid` = non-voided `payment_records[].amount` over the FROZEN `amount_requested` (D-13).

## Task Commits
1. **Task 1: Collectibles listener + computeTrancheLifecycle helper** - `207ef22` (feat)
2. **Task 2: Lifecycle footer + 2-chip scorecard + refreshExpense D-04 fix** - `1572191` (feat)
3. **Task 3: Full Breakdown header button + window collapse + department:'projects'** - `d8424ad` (feat)

## Files Created/Modified
- `app/views/project-detail.js` - listener, helper, footer rewrite, scorecard relabel, refreshExpense fix, header button, window-fn collapse, department field.

## Decisions Made
- Footer fn renamed `renderOwnBillingRequests` → `renderTrancheLifecycleRows` (clearer; single call-site at the footer updated). Plan 03's service equivalent uses `renderServiceTrancheLifecycle`.
- 2 chips compute inline from `currentCollectibleDocs` (live via the listener) rather than the `currentCollectibles` aggregate, so a recorded payment updates the card immediately.

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None. `node --check` passes; all per-task acceptance greps pass.

## Next Phase Readiness
- `department:'projects'` is now written by the project submit — Plan 04's finance reader (treat missing as projects) will match.
- The computeTrancheLifecycle contract above is the exact shape Plan 03 ports into service-detail.js.

---
*Phase: 99.1-collectibles-revamp-alpha-detail*
*Completed: 2026-06-06*
