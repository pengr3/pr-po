---
phase: 99.1-collectibles-revamp-alpha-detail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/project-detail.js
autonomous: true
requirements: [CDR-01, CDR-02, CDR-03, CDR-04]
tags: [collectibles, billing, project-detail, lifecycle, scorecard, firestore]

must_haves:
  truths:
    - "On the project detail Financial Summary card, EVERY collection tranche is always shown as a lifecycle row — even tranches with no billing request (CDR-01 / D-06)"
    - "Unfiled tranches render at 45% opacity with a dashed '— Not Filed' badge; partial-payment tranches show a '₱X of ₱Y · Z%' note under the Billed badge, not a separate stage (D-08 / D-09)"
    - "A collectibles onSnapshot scoped to currentProject.project_code feeds the footer with raw collectible docs (payment_records) and is unsubscribed in destroy() (CDR-02 / D-12)"
    - "Stage + cash % derive from the exact deriveCollectibleStatus formula (non-voided payment_records[].amount over frozen amount_requested) via a single computeTrancheLifecycle helper — no third divergent copy (D-13)"
    - "The card shows a 2-chip scorecard 'Collected' + 'Outstanding', both sourced from collectible docs (NOT approved billing_requests); the refreshExpense D-04 landmine is fixed (CDR-03 / D-10)"
    - "'Initiate Billing →' is retained at footer right and submitBillingRequest now writes department:'projects' on the doc (D-11 / D-22 project-side symmetry)"
    - "A single 'Full Breakdown →' button in the Financial Summary card header always refreshes-then-opens; the hidden click-the-number affordance + 🔄 button are removed and the showExpenseModal/refreshAndShowExpenseModal split is collapsed to one window fn (CDR-04 / D-14..D-16)"
  artifacts:
    - path: "app/views/project-detail.js"
      provides: "ensureCollectiblesListener + collectiblesListenerUnsub + currentCollectibleDocs raw-doc array"
      contains: "ensureCollectiblesListener"
    - path: "app/views/project-detail.js"
      provides: "computeTrancheLifecycle(tranche, idx, billingReqs, collectibleDocs) shared stage helper"
      contains: "function computeTrancheLifecycle"
  key_links:
    - from: "project-detail.js renderProjectDetail footer"
      to: "currentProject.collection_tranches"
      via: "iterate every tranche → computeTrancheLifecycle"
      pattern: "collection_tranches"
    - from: "project-detail.js ensureCollectiblesListener"
      to: "collectibles collection"
      via: "onSnapshot scoped to project_code"
      pattern: "where\\('project_code', '==', code\\)"
---

<objective>
Revamp the money-in (collectibles) view on the **project** detail Financial Summary card per spikes 025 + 026 (CDR-01..CDR-04). Replace the status-only own-requests list with per-tranche lifecycle rows, add a `collectibles`-by-`project_code` listener feeding raw docs, fix the D-04 scorecard landmine (2 chips sourced from collectible docs), and replace the buried "click the number" modal entry with a single "Full Breakdown →" header button. Also add `department:'projects'` to the project billing submit for symmetry with the service port (D-22).

Purpose: Users can read billing health (Not Filed → Pending → Approved-Not-Yet-Invoiced → Invoiced → Collecting → Fully Collected) at a glance, including the currently-invisible "approved but not yet invoiced" state.
Output: Modified `app/views/project-detail.js`. The `computeTrancheLifecycle` helper shape defined here is the canonical reference that Plan 03 (service-detail.js) mirrors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99.1-collectibles-revamp-alpha-detail/99.1-CONTEXT.md
@.planning/phases/99.1-collectibles-revamp-alpha-detail/99.1-PATTERNS.md

<interfaces>
<!-- Canonical formula (DO NOT re-derive with a different denominator/voided rule — D-13). -->
<!-- finance.js:110 === expense-modal.js:24 (byte-for-byte). -->
deriveCollectibleStatus(coll):
  totalPaid = (coll.payment_records || []).filter(r => r.status !== 'voided').reduce((s,r)=>s+(parseFloat(r.amount)||0),0)
  pct = totalPaid / coll.amount_requested   // amount_requested = FROZEN "Invoiced" (D-02)

collection_tranches (on currentProject) = array of { label, percentage }
billing_requests doc = { project_code, tranche_index, tranche_label, tranche_percentage,
                         amount_requested (=Billed, D-01), status ('pending'|'approved'|'rejected' lowercase),
                         rejection_reason?, documents[], notes, requested_by_* }
collectibles doc = { coll_id, department, project_code, tranche_index, tranche_label,
                     tranche_percentage, amount_requested (=Invoiced, FROZEN), due_date, payment_records[] }

Design-system colors: success #059669, warning #f59e0b, danger #ef4444, primary #1a73e8,
  text #1e293b/#475569/#64748b, borders #e2e8f0. Spike-025 stage extras: indigo, teal, green ✓.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Collectibles listener + computeTrancheLifecycle helper (CDR-02 / D-12 / D-13)</name>
  <files>app/views/project-detail.js</files>
  <read_first>
    - app/views/project-detail.js:29-39 (existing billing module vars), :255-275 (ensureBillingRequestsListener analog to clone), :180-227 (the two listener call-sites at :188 and :226), :290-345 (destroy teardown block — :314-318 is the billing teardown to mirror)
    - 99.1-PATTERNS.md §1 (listener clone) + the "Stage-Derivation Logic to Reuse" section (the D-07 table)
    - app/expense-modal.js:24-33 (canonical deriveCollectibleStatus formula — replicate, do not import)
  </read_first>
  <behavior>
    - computeTrancheLifecycle(tranche, idx, billingReqs, collectibleDocs) returns { stage, badgeLabel, badgeColor, opacity, pct, totalPaid, amountRequested, note } where:
      - no billing_request AND no collectible at idx → stage 'not-filed', badgeColor gray #94a3b8, opacity 0.45, dashed "— Not Filed" badge
      - billing status 'pending' → stage 'pending', amber #f59e0b "Pending Review"
      - billing status 'rejected' → stage 'rejected', red #ef4444 "Rejected"
      - 'approved' + no collectible → stage 'approved-not-invoiced', indigo (#4f46e5) "Approved — Not Yet Invoiced"
      - collectible exists + pct === 0 → stage 'invoiced-awaiting', teal (#0d9488) "Invoiced — Awaiting Payment"
      - collectible exists + 0 < pct < 100 → stage 'collecting', progress note "₱{totalPaid} of ₱{amountRequested} · {pct}%"
      - collectible exists + pct >= 100 → stage 'fully-collected', green #059669 "Fully Collected ✓"
    - pct = Math.round(totalPaid / amountRequested * 100) with amountRequested = collectible.amount_requested (frozen), totalPaid = non-voided payment_records sum. amountRequested guarded (>0) to avoid divide-by-zero.
  </behavior>
  <action>
    Add two module vars near project-detail.js:30-31 (beside currentBillingRequests/billingRequestsListenerUnsub):
      `let currentCollectibleDocs = [];` and `let collectiblesListenerUnsub = null;`
    (Distinct name — `currentCollectibles` at :22 is the AGGREGATE object, not the raw doc array.)

    Add `ensureCollectiblesListener()` immediately after `ensureBillingRequestsListener()` (after project-detail.js:275), cloning its shape exactly:
      - idempotent guard: `if (collectiblesListenerUnsub) return;`
      - `const code = currentProject?.project_code; if (!code) return;` (scope by the DOC FIELD, not the URL `projectCode` param — Phase 78 doc-id fallback)
      - `collectiblesListenerUnsub = onSnapshot(query(collection(db, 'collectibles'), where('project_code', '==', code)), (snap) => { currentCollectibleDocs = []; snap.forEach(d => currentCollectibleDocs.push({ id: d.id, ...d.data() })); if (currentProject) renderProjectDetail(); }, (err) => { console.error('[ProjectDetail/Collectibles] snapshot error:', err); });`

    Call `ensureCollectiblesListener();` directly beside BOTH existing `ensureBillingRequestsListener();` call-sites (project-detail.js:188 and :226).

    In destroy(), immediately after the billing teardown (after :318), add:
      `if (collectiblesListenerUnsub) { try { collectiblesListenerUnsub(); } catch (e) { /* swallow */ } }`
      `collectiblesListenerUnsub = null;`
      `currentCollectibleDocs = [];`

    Add the shared helper `function computeTrancheLifecycle(tranche, idx, billingReqs, collectibleDocs) { ... }` at module scope (place it just above renderOwnBillingRequests, ~project-detail.js:629). Implement the <behavior> stage table EXACTLY. Compute totalPaid with the canonical formula: `(coll.payment_records || []).filter(r => r.status !== 'voided').reduce((s,r)=>s+(parseFloat(r.amount)||0),0)`. Match billing req by `billingReqs.find(r => r.tranche_index === idx)` and collectible by `collectibleDocs.find(c => c.tranche_index === idx)` (D-05 in-page key = tranche_index). Use stage colors: gray #94a3b8, amber #f59e0b, red #ef4444, indigo #4f46e5, teal #0d9488, green #059669.
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/project-detail.js` exits 0
    - project-detail.js contains `let currentCollectibleDocs = [];` and `let collectiblesListenerUnsub = null;`
    - project-detail.js contains `function ensureCollectiblesListener` AND `where('project_code', '==', code)` inside it (grep both)
    - project-detail.js contains `function computeTrancheLifecycle`
    - The string `ensureCollectiblesListener();` appears at least twice (both call-sites) — `grep -c "ensureCollectiblesListener();" app/views/project-detail.js` >= 2
    - destroy() contains `collectiblesListenerUnsub = null;` AND `currentCollectibleDocs = [];`
    - computeTrancheLifecycle body contains the literal voided filter `r.status !== 'voided'` (canonical formula reused, D-13)
    - computeTrancheLifecycle body contains the indigo color `#4f46e5` and teal `#0d9488` (the two new lifecycle states)
  </acceptance_criteria>
  <done>
    Raw collectible docs stream in scoped to project_code and tear down in destroy(); a single computeTrancheLifecycle helper computes every stage using the canonical voided-excluded formula.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Per-tranche lifecycle footer + 2-chip scorecard + refreshExpense D-04 fix (CDR-01 / CDR-03)</name>
  <files>app/views/project-detail.js</files>
  <read_first>
    - app/views/project-detail.js:577-594 (current Collectibles group markup + Initiate Billing link + renderOwnBillingRequests call), :629-648 (renderOwnBillingRequests being replaced), :1230-1253 (refreshExpense D-04 landmine — collTotalFiled sourced from approved billing_requests)
    - 99.1-PATTERNS.md §2 (footer replacement) + §3 (2-chip scorecard + D-04 landmine) + "Stage-Derivation Logic to Reuse"
    - The computeTrancheLifecycle helper added in Task 1
  </read_first>
  <action>
    REPLACE `renderOwnBillingRequests()` (project-detail.js:629-648) with a per-tranche lifecycle renderer (keep the same function name so the existing call-site at :594 still resolves, OR rename to `renderTrancheLifecycleRows` and update the call-site — either is fine; document which). It MUST:
      - read `const tranches = Array.isArray(currentProject?.collection_tranches) ? currentProject.collection_tranches : [];`
      - return '' when there are no tranches
      - iterate EVERY tranche with index i, call `computeTrancheLifecycle(tranche, i, currentBillingRequests, currentCollectibleDocs)`
      - render one row per tranche showing: escaped tranche label, the stage badge (badgeLabel + badgeColor), and — for the 'collecting' stage — the "₱X of ₱Y · Z%" note UNDER the badge (D-09, not a separate row)
      - apply `opacity:0.45` + a dashed "— Not Filed" badge for the 'not-filed' stage (D-08)
      - escapeHTML() every interpolated user string (tranche_label, rejection_reason)
      - for 'rejected' rows, show the rejection_reason (escaped) from the matching billing request

    REPLACE the 2 collectibles chips (project-detail.js:581-588): relabel to "Collected" and "Outstanding". Source BOTH from `currentCollectibleDocs` (NOT currentCollectibles.totalRequested):
      - Collected = sum over currentCollectibleDocs of non-voided payment_records[].amount
      - Invoiced = sum over currentCollectibleDocs of amount_requested
      - Outstanding = Invoiced − Collected (D-04). Color Outstanding red #ef4444 when > 0 else #059669.
    Compute these inline at render (a small reduce) so the card and the new listener agree.

    FIX the refreshExpense D-04 landmine (project-detail.js:1233-1253): change `collTotalFiled` to source from **collectible docs** (`coll.amount_requested`), matching the service side (service-detail.js:984-990). Remove the `where('status', '==', 'approved')` billing_requests query for the "filed" figure; compute `totalRequested` from the `collectibles` snapshot's `amount_requested`. Keep `totalCollected` from non-voided payment_records. Set `currentCollectibles = { totalRequested: <coll amount_requested sum>, totalCollected, remainingCollectible: totalRequested - totalCollected }`. Add an inline comment: "D-04 fix per 99.1 D-10 — Outstanding sourced from collectible docs, NOT approved billing_requests".

    KEEP the "↑ Initiate Billing →" footer link verbatim (project-detail.js:590-593, D-11).
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/project-detail.js` exits 0
    - The footer renderer iterates tranches and calls computeTrancheLifecycle: grep finds `computeTrancheLifecycle(` invoked with `currentBillingRequests` and `currentCollectibleDocs` as args
    - project-detail.js contains the chip labels `>Collected<` and `>Outstanding<` (the 2-chip scorecard relabel) — `grep -F "Outstanding" app/views/project-detail.js` returns >= 1
    - refreshExpense no longer queries approved billing_requests for the filed figure: `grep -v '^\s*//' app/views/project-detail.js | grep -c "where('status', '==', 'approved')"` returns 0 inside refreshExpense (the line at 1237 is removed/repurposed)
    - refreshExpense computes totalRequested from `amount_requested` on collectible docs: grep finds `coll.amount_requested` OR `.data().amount_requested` inside the collectibles forEach
    - "↑ Initiate Billing →" still present: `grep -F "Initiate Billing" app/views/project-detail.js` returns >= 1
    - No "Not Filed" dashed badge regression: grep finds the literal `Not Filed` and `0.45` (opacity) in the footer renderer
  </acceptance_criteria>
  <done>
    Footer shows all tranches as lifecycle rows; the card's 2 chips (Collected + Outstanding) and refreshExpense both source from collectible docs, eliminating the D-04 landmine.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: "Full Breakdown →" header button + collapse window-fn split + department:'projects' (CDR-04 / D-14..D-16 / D-22)</name>
  <files>app/views/project-detail.js</files>
  <read_first>
    - app/views/project-detail.js:525-549 (Financial card label at :528 + Projected Expense onclick/🔄 at :544-547), :1897-1929 (attachWindowFunctions — :1902-1907 is the show/refresh split), :340-345 (destroy window deletes), :848-862 (submitBillingRequest addDoc — where department:'projects' is added)
    - 99.1-PATTERNS.md §4 (header button + window collapse) — note service-detail.js:391-398 is the flex-header visual template
  </read_first>
  <action>
    CONVERT the bare "Financial Summary" label (project-detail.js:528) into a flex header using the service-detail.js:391-398 template:
      `<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.6rem;">
         <div style="font-size:0.7rem;font-weight:700;color:#94a3b8;text-transform:uppercase;letter-spacing:0.06em;">Financial Summary</div>
         <button class="btn btn-sm btn-secondary" onclick="window.openFullBreakdown()" style="font-size:0.7rem;padding:0.2rem 0.6rem;">Full Breakdown →</button>
       </div>`

    REMOVE the hidden click affordance (D-15): in the Projected Expense block (project-detail.js:543-547) drop the `onclick="window.showExpenseModal()"` from the amount `<span>` (leave the number as plain text) AND remove the adjacent 🔄 `<button onclick="window.refreshAndShowExpenseModal()">` entirely.

    COLLAPSE the window-fn split (D-16): in attachWindowFunctions (project-detail.js:1902-1907) delete both `window.showExpenseModal` and `window.refreshAndShowExpenseModal`; add ONE always-refresh fn:
      `window.openFullBreakdown = async () => { if (!currentProject) return; await refreshExpense(true); showExpenseBreakdownModal(currentProject.project_name, { mode: 'project' }); };`
    In destroy() (project-detail.js:344-345) delete the two obsolete deletes and add `delete window.openFullBreakdown;` (clean removal — no thin alias).

    ADD `department: 'projects',` to the submitBillingRequest addDoc payload (project-detail.js:849, beside `project_code`). One-line backward-safe symmetry per D-22. Add inline comment "D-22 — department discriminator (finance treats missing as 'projects')".
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/project-detail.js` exits 0
    - project-detail.js contains `Full Breakdown →` and `window.openFullBreakdown`
    - The obsolete window fns are GONE: `grep -c "window.showExpenseModal" app/views/project-detail.js` returns 0 AND `grep -c "window.refreshAndShowExpenseModal" app/views/project-detail.js` returns 0
    - destroy() contains `delete window.openFullBreakdown;` and NO `delete window.showExpenseModal;` / `delete window.refreshAndShowExpenseModal;`
    - The Projected Expense amount span no longer carries an expense-modal onclick (grep the Projected Expense block has no `onclick="window.showExpenseModal()"`)
    - submitBillingRequest addDoc payload contains `department: 'projects'` — `grep -F "department: 'projects'" app/views/project-detail.js` returns >= 1
  </acceptance_criteria>
  <done>
    One "Full Breakdown →" header button always refresh-then-opens the modal; the buried click + dual window fns are gone; project billing docs now carry department:'projects'.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore (billing_requests/collectibles read) | listener reads docs others wrote; tranche_label/rejection_reason/notes are user-authored strings rendered into the card |
| client → Firestore (billing_requests write via submitBillingRequest) | operations_user submits an advisory amount + doc-link URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99.1-01 | Tampering | submitBillingRequest amount_requested | accept | Advisory only; Finance re-derives the authoritative amount at approval (D-01, Phase 99 T-99-05). No new exposure — department:'projects' is a non-privileged discriminator. |
| T-99.1-02 | Information disclosure / XSS | lifecycle footer rows (tranche_label, rejection_reason) | mitigate | escapeHTML() on every interpolated user string in the new footer renderer (already universal in this file at :643). |
| T-99.1-03 | Elevation of privilege | unauthorized status transitions | mitigate | firestore.rules billing_requests update = finance roles only (firestore.rules:549, unchanged — D-27). |
</threat_model>

<verification>
- `node --check app/views/project-detail.js` passes after each task.
- grep gates in each task's acceptance_criteria all pass.
- Manual sanity: open a project detail with ≥1 tranche; confirm every tranche shows a row, unfiled ones grayed/dashed, and "Full Breakdown →" opens the modal.
</verification>

<success_criteria>
- Per-tranche lifecycle footer rendering all tranches with correct stages/badges (CDR-01).
- collectibles listener scoped to project_code, torn down in destroy() (CDR-02).
- 2-chip Collected + Outstanding sourced from collectible docs; refreshExpense D-04 landmine fixed; Initiate Billing retained (CDR-03).
- Single Full Breakdown header button; hidden click removed; window split collapsed; department:'projects' written (CDR-04 / D-22).
</success_criteria>

<output>
After completion, create `.planning/phases/99.1-collectibles-revamp-alpha-detail/99.1-01-SUMMARY.md`. Record: the final name of the footer renderer (kept vs renamed), the exact computeTrancheLifecycle return shape (so Plan 03 mirrors it), and the stage→color map used.
</output>
