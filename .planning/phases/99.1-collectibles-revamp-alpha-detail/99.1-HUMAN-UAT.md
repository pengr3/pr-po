---
status: passed
phase: 99.1-collectibles-revamp-alpha-detail
source: [99.1-04-PLAN.md Task 3, 99.1-VERIFICATION.md]
started: 2026-06-06
updated: 2026-06-06
---

## Current Test

[complete — operator approved all 8 items 2026-06-06 ("approved all")]

## Prerequisite

Deploy Phase 99's billing_requests rules to dev before testing (no NEW rules this phase, D-27):

```
firebase deploy --only firestore:rules --project dev
```

(CLI active project is PROD — the `--project dev` flag is required.)
Use a SERVICE that has `collection_tranches` + a positive `contract_cost`.

## Tests

### 1. Service billing submit
expected: operations user → SERVICE detail → "↑ Initiate Billing →" → pick tranche → auto-hinted type + url doc field(s) + notes → Submit → success toast; footer own-request row shows the tranche as pending.
result: passed

### 2. Finance pending banner shows Service
expected: `#/finance/collectibles` → "Pending Billing Requests" shows the SERVICE name with a "Service" chip; doc links open in a new tab.
result: passed

### 3. Approve pre-fills Services dept
expected: Approve → Create-Collectible opens PRE-FILLED with Department=Services, the correct service, and the requested tranche pre-selected → set due date → Submit the collectible.
result: passed

### 4. Service footer lifecycle advances
expected: footer advances Pending → Approved-Not-Yet-Invoiced (indigo) → Invoiced — Awaiting Payment (teal) → record a payment → Collecting "₱X of ₱Y · Z%" → Fully Collected ✓ at 100%.
result: passed

### 5. Full Breakdown ghost row clears once invoiced
expected: before invoicing, service Full Breakdown → Collectibles tab shows an "awaiting COLL-xxx" ghost row + Billed to Client > 0; after the COLL exists, the ghost row clears and the real COLL row appears.
result: passed

### 6. Service decision notification routes to service detail
expected: submitter receives a BILLING_REQUEST_DECIDED notification that, when clicked, lands on `#/services/detail/...` (NOT #/projects).
result: passed

### 7. Project regression (lifecycle + scorecard + Full Breakdown)
expected: a PROJECT detail still shows lifecycle rows + the D-04-correct 2-chip Collected/Outstanding (sourced from collectible docs) + the single Full Breakdown header button; Initiate Billing still works.
result: passed

### 8. Project billing regression (Finance side)
expected: submit on a PROJECT → banner shows "Project", Approve pre-fills Projects dept + correct tranche, decision notification lands on the project detail.
result: passed

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
