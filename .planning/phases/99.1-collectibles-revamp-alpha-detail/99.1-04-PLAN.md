---
phase: 99.1-collectibles-revamp-alpha-detail
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - app/views/finance.js
  - app/notifications.js
autonomous: false
requirements: [CDR-08]
tags: [collectibles, billing, finance, notifications, department, generalization]

must_haves:
  truths:
    - "renderPendingBillingBanner shows req.project_name || req.service_name and a department indicator so Finance can tell a service request from a project one (CDR-08 / D-25)"
    - "approveBillingRequest builds the preselectKey from req.department: 'services:' + req.service_code + ':' + req.tranche_index when department === 'services', else 'projects:' + req.project_code + ':' + req.tranche_index — the hardcoded 'projects:' at finance.js:1655 is removed (CDR-08 / D-25)"
    - "finance treats a MISSING department as 'projects' (backward-compatible with existing Phase 99 docs) (CDR-08 / D-22)"
    - "_notifyBillingDecision routes the submitter link by department: '#/services/detail/' + req.service_code for services, else '#/projects/detail/' + req.project_code — service decisions no longer dead-end on the projects route (CDR-08 / D-26)"
    - "firestore.rules is NOT modified (the billing_requests block is already department-agnostic) (D-27)"
    - "BROWSER-UAT (cross-surface service flow): Initiate Billing on a SERVICE → Finance pending banner shows the service name + dept → Approve pre-fills Create-Collectible with the services dept + tranche → after the COLL exists the service footer advances Pending → Approved → Invoiced → Collected and the service Full Breakdown ghost row clears once invoiced"
  artifacts:
    - path: "app/views/finance.js"
      provides: "dept-aware preselectKey + banner project_name||service_name + dept-aware notify link"
      contains: "req.department === 'services'"
    - path: "app/notifications.js"
      provides: "confirmed no functional change required (per-notification link override is honored at handleNotificationClick:448)"
      contains: "BILLING_REQUEST_DECIDED"
  key_links:
    - from: "finance.js approveBillingRequest"
      to: "openCreateCollectibleModal preselectKey parser (finance.js:1756 — already supports services: + 3rd tranche segment)"
      via: "dept-derived preselectKey"
      pattern: "'services:' \\+ req.service_code"
    - from: "finance.js _notifyBillingDecision"
      to: "service detail route"
      via: "per-notification link override (honored over TYPE_META fallback at notifications.js:448)"
      pattern: "#/services/detail/"
---

<objective>
Minimally generalize the Finance review side (CDR-08 / D-25, D-26) so service billing requests are reviewable/approvable end-to-end — WITHOUT the larger Phase 99.2 finance revamp. The dept-agnostic pending listener (finance.js:1592) already picks up service requests; this plan makes the banner, the approve bridge, and the decision notification department-aware. The `department` discriminator is written by Plan 01 (projects) and Plan 03 (services); this plan is its consumer.

Purpose: Complete the service billing loop — submit (Plan 03) → review/approve (this plan) → footer advances (Plan 01/03 listeners).
Output: Modified `app/views/finance.js` + a confirmation pass on `app/notifications.js`. The `department` reader here MUST stay consistent with the writers in Plans 01/03 (treat missing as 'projects').
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99.1-collectibles-revamp-alpha-detail/99.1-CONTEXT.md
@.planning/phases/99.1-collectibles-revamp-alpha-detail/99.1-PATTERNS.md
@.planning/phases/99.1-collectibles-revamp-alpha-detail/99.1-03-SUMMARY.md
@.planning/phases/99-billing-request-flow/99-03-SUMMARY.md

<interfaces>
<!-- openCreateCollectibleModal ALREADY supports the dept + 3rd tranche segment (finance.js:1756): -->
<!--   'projects:CLMC-ACME-001:2' | 'services:SVC-CLMC-001:0' — DO NOT change the parser. -->
preselectKey shapes (finance.js:1751-1761):
  const [d, c, t] = preselectKey.split(':');  // d=dept, c=code, t=trancheIndex (optional)

<!-- Per-notification link override is honored over TYPE_META fallback (notifications.js:448): -->
const link = cached?.link || TYPE_META[cached?.type]?.target_route || '';
<!-- So passing link:'#/services/detail/'+service_code in _notifyBillingDecision routes correctly with NO notifications.js TYPE_META change. -->

billing_requests doc carries (Plan 01/03): department ('projects'|'services'|undefined),
  project_code|service_code, project_name|service_name, tranche_index, tranche_label, amount_requested.
Backward-compat rule (D-22): a doc with NO department field is treated as 'projects'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Dept-aware pending banner + approve preselectKey + decision-notify link (CDR-08 / D-25/D-26)</name>
  <files>app/views/finance.js</files>
  <read_first>
    - app/views/finance.js:1590-1644 (pending listener + renderPendingBillingBanner — the row at :1623 hardcodes req.project_name), :1646-1671 (approveBillingRequest — hardcoded 'projects:' at :1655), :1673-1716 (rejectBillingRequest + _notifyBillingDecision — link '#/projects/detail/' at :1707), :1745-1761 (openCreateCollectibleModal preselectKey parser — READ-ONLY, already dept-aware)
    - 99-03-SUMMARY.md (the existing project-side approve/reject behavior being generalized)
    - 99.1-03-SUMMARY.md (confirm service billing_requests carry department:'services')
  </read_first>
  <behavior>
    - Banner row label = `req.project_name || req.service_name`. Add a small dept chip/text: "Service" when `req.department === 'services'`, else "Project" (missing department → Project).
    - approveBillingRequest preselectKey:
      - if `req.department === 'services'` → `'services:' + (req.service_code || '') + ':' + req.tranche_index`
      - else → `'projects:' + (req.project_code || '') + ':' + req.tranche_index`
    - _notifyBillingDecision link + message object:
      - services → link `'#/services/detail/' + (req.service_code || '')`, message uses `req.service_name`, source_id = service_code, object_name = service_name
      - else → link `'#/projects/detail/' + (req.project_code || '')`, message uses `req.project_name` (existing behavior preserved)
  </behavior>
  <action>
    In `renderPendingBillingBanner()` (finance.js:1623): replace `${escapeHTML(req.project_name || '')}` with `${escapeHTML(req.project_name || req.service_name || '')}` and append a department indicator near the title, e.g. `<span style="font-size:0.62rem;font-weight:700;color:#64748b;background:#f1f5f9;border-radius:999px;padding:0.05rem 0.45rem;margin-left:0.4rem;">${req.department === 'services' ? 'Service' : 'Project'}</span>`. Keep the rest of the row layout + escaping unchanged.

    In `approveBillingRequest()` (finance.js:1655): REPLACE the hardcoded `'projects:' + (req.project_code || '') + ':' + req.tranche_index` (and delete the "Billing requests are projects-only this phase" comment) with a dept-derived key:
      ```
      const isService = req.department === 'services';
      const preselectKey = isService
        ? 'services:' + (req.service_code || '') + ':' + req.tranche_index
        : 'projects:' + (req.project_code || '') + ':' + req.tranche_index;  // missing department → projects (D-22)
      window.openCreateCollectibleModal(preselectKey);
      ```
    Do NOT touch openCreateCollectibleModal's parser (already supports services: + 3rd segment).

    In `_notifyBillingDecision()` (finance.js:1700-1716): make link/message/source dept-aware:
      ```
      const isService = req.department === 'services';
      const name = isService ? (req.service_name || '') : (req.project_name || '');
      const link = isService ? ('#/services/detail/' + (req.service_code || '')) : ('#/projects/detail/' + (req.project_code || ''));
      const sourceId = isService ? (req.service_code || '') : (req.project_code || '');
      ```
      Use `name` in the message + object_name, `link` for link, `sourceId` for source_id. Preserve the fire-and-forget OWN try/catch.

    rejectBillingRequest (finance.js:1674) needs no change beyond passing the same `req` to `_notifyBillingDecision` (already does) — the link logic now lives in the helper.
  </action>
  <verify>
    <automated>node --check app/views/finance.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/finance.js` exits 0
    - approveBillingRequest is dept-aware: `grep -F "req.department === 'services'" app/views/finance.js` returns >= 1
    - The dept-derived service key is built: `grep -F "'services:' + (req.service_code" app/views/finance.js` returns >= 1
    - The hardcoded projects-only approve key is gone: the literal `'projects:' + (req.project_code || '') + ':' + req.tranche_index` no longer appears as an UNCONDITIONAL statement directly inside approveBillingRequest (it may appear only as the else-branch of the isService ternary). Verify by reading approveBillingRequest — the preselectKey assignment is a ternary on `isService`.
    - Banner shows service fallback: `grep -F "req.project_name || req.service_name" app/views/finance.js` returns >= 1
    - A department indicator string exists in the banner: grep finds `'Service'` and `'Project'` near req.department in renderPendingBillingBanner
    - Decision notify routes services: `grep -F "#/services/detail/" app/views/finance.js` returns >= 1 inside _notifyBillingDecision
    - openCreateCollectibleModal parser unchanged: `grep -c "preselectKey.split(':')" app/views/finance.js` returns >= 1 (still present, untouched)
  </acceptance_criteria>
  <done>
    Finance banner, approve bridge, and decision notification are department-aware; service requests route to the services dept + service detail; missing-department docs still behave as projects.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Confirm notifications.js needs no functional change (D-26 / D-27 confirmation)</name>
  <files>app/notifications.js</files>
  <read_first>
    - app/notifications.js:95-96 (BILLING_REQUEST_SUBMITTED/DECIDED TYPE_META), :443-455 (handleNotificationClick — per-notification cached.link takes precedence over TYPE_META[type].target_route), :608-657 (createNotificationForUsers signature — accepts link)
  </read_first>
  <action>
    VERIFY (no code change expected): the per-notification `link` set by finance.js `_notifyBillingDecision` (Task 1) is honored over `TYPE_META.BILLING_REQUEST_DECIDED.target_route` ('#/projects') at notifications.js:448 (`const link = cached?.link || TYPE_META[...].target_route`). Because finance.js now passes `'#/services/detail/' + service_code` for service decisions, the submitter lands on the service detail — D-26 satisfied with NO notifications.js change.
    Do NOT change TYPE_META.BILLING_REQUEST_DECIDED.target_route (it is only the fallback for legacy/linkless docs; changing it would mis-route project decisions).
    If — and only if — reading reveals the per-notification link is NOT persisted/honored (it IS, per :448), THEN add the minimal fix to persist `link`; otherwise leave notifications.js unchanged and record the confirmation in the SUMMARY.
  </action>
  <verify>
    <automated>node --check app/notifications.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/notifications.js` exits 0
    - notifications.js handleNotificationClick still honors the per-notification link first: `grep -F "cached?.link || TYPE_META" app/notifications.js` returns >= 1
    - TYPE_META.BILLING_REQUEST_DECIDED is unchanged (still present): `grep -c "BILLING_REQUEST_DECIDED" app/notifications.js` returns >= 2 (the NOTIFICATION_TYPES entry + TYPE_META entry)
    - SUMMARY records the D-26 mechanism (per-notification link override via finance.js, notifications.js unchanged)
  </acceptance_criteria>
  <done>
    Confirmed: D-26 is satisfied by finance.js's per-notification link override; notifications.js requires no functional change (or the minimal persist-link fix applied if verification shows otherwise).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser UAT — end-to-end SERVICE billing flow</name>
  <what-built>
    Service-side billing flow (Plan 03) + Finance generalization (this plan): a service user can Initiate Billing; Finance sees the request labeled as a Service; Approve pre-fills Create-Collectible with the services dept + tranche; the service detail footer advances through the lifecycle; the Full Breakdown ghost row clears once invoiced.
  </what-built>
  <how-to-verify>
    Prereq: Phase 99's billing_requests rules are deployed to dev — run `firebase deploy --only firestore:rules --project dev` (CLI active project is PROD; use --project dev). Use a service that has collection_tranches + a positive contract_cost.
    1. As an operations user, open a SERVICE detail page → click "↑ Initiate Billing →" → pick a tranche → confirm the auto-hinted type + the per-type url doc field(s) → fill the doc link(s) + notes → Submit. Expect: success toast, the service footer own-request row shows "pending".
    2. As Finance, open #/finance/collectibles → the "Pending Billing Requests" banner shows the SERVICE name with a "Service" indicator + the doc links open in a new tab.
    3. Click Approve → the Create-Collectible modal opens PRE-FILLED with Department=Services, the correct service, and the requested tranche pre-selected. Set a due date → Submit the collectible.
    4. Back on the SERVICE detail footer: the tranche advances Pending → Approved-Not-Yet-Invoiced (indigo) → after the COLL exists, Invoiced — Awaiting Payment (teal). Record a payment in Finance → footer shows Collecting "₱X of ₱Y · Z%" then Fully Collected ✓ at 100%.
    5. Open the service Full Breakdown modal: before invoicing, the Collectibles tab shows an "awaiting COLL-xxx" ghost row for the approved tranche; after the COLL exists, the ghost row is gone and the real COLL row appears. Header strip shows Billed to Client > 0.
    6. Confirm the submitter receives a BILLING_REQUEST_DECIDED notification that, when clicked, lands on the SERVICE detail (#/services/detail/...), NOT #/projects.
    Regression: repeat step 1-3 once on a PROJECT to confirm the project flow still works (banner shows "Project", Approve pre-fills Projects dept, decision notification lands on the project detail).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing step(s). If the user is unavailable (overnight/batch), mark this UAT pending-in-browser in the SUMMARY and emit a punch list per the project's overnight-batch convention.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore (billing_requests update: approve/reject) | Finance flips status + writes reviewer fields; preselectKey is built from doc-controlled department/code |
| Finance → submitter (decision notification) | link is built from the request's department/code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99.1-10 | Elevation of privilege | approve/reject status transition | mitigate | firestore.rules billing_requests update = finance roles only (dept-agnostic, firestore.rules:549 — unchanged, D-27); approveBillingRequest also gated by hasCollectibleWriteAuthority() inside openCreateCollectibleModal. |
| T-99.1-11 | Tampering | preselectKey built from doc-controlled department/service_code | accept | preselectKey only pre-fills the Create-Collectible form; Finance reviews + re-derives the authoritative amount before submitting the collectible (D-01). No privileged action is taken purely from the request fields. |
| T-99.1-12 | Information disclosure / XSS | banner project_name||service_name + dept chip | mitigate | escapeHTML() retained on the name; the dept chip text is a fixed literal ('Service'/'Project'), not user input. |
| T-99.1-13 | Open redirect / routing | per-notification link from req.service_code/project_code | mitigate | link is a fixed in-app hash prefix ('#/services/detail/'+code); code is a Firestore-controlled identifier, not free-form URL — no external redirect surface. |
</threat_model>

<verification>
- `node --check app/views/finance.js` and `node --check app/notifications.js` pass.
- All acceptance grep gates pass.
- Browser UAT (Task 3) is the authoritative end-to-end gate (zero-build SPA, no test harness).
</verification>

<success_criteria>
- Dept-aware banner + approve preselectKey + decision-notify link (CDR-08 / D-25/D-26).
- Missing-department docs treated as projects (backward-compatible, D-22).
- notifications.js confirmed needing no functional change (D-26 via per-notification link; D-27 rules unchanged).
- End-to-end service flow verified in-browser (or recorded pending-in-browser per overnight-batch convention).
</success_criteria>

<output>
After completion, create `.planning/phases/99.1-collectibles-revamp-alpha-detail/99.1-04-SUMMARY.md`. Record: the dept-resolution rule used (missing → projects), the D-26 mechanism (per-notification link, notifications.js unchanged), the rules-deploy reminder, and the UAT outcome (passed / pending-in-browser + punch list).
</output>
