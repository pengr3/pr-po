---
phase: 93.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/home.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "Clicking anywhere on a queue row (except action buttons) opens the proposal modal"
    - "Approve and Reject buttons fire their respective actions without opening the modal"
    - "Queue rows show a pointer cursor and a subtle background highlight on hover"
  artifacts:
    - path: "app/views/home.js"
      provides: "_renderHomeApprovalQueueHtml with row-click handler and button stopPropagation"
      contains: "onclick=\"window.openProposalModal"
  key_links:
    - from: "_renderHomeApprovalQueueHtml <tr>"
      to: "window.openProposalModal"
      via: "onclick on <tr> element"
      pattern: "window\\.openProposalModal && window\\.openProposalModal"
    - from: "Approve/Reject <button>"
      to: "event.stopPropagation()"
      via: "onclick handler prefix"
      pattern: "event\\.stopPropagation\\(\\)"
---

<objective>
Add full-row click behavior to the Proposal Approval Queue table in `_renderHomeApprovalQueueHtml`. Every `<tr>` becomes clickable, opening the proposal detail modal via `window.openProposalModal(p.id)`. The Approve and Reject buttons stop click propagation so they remain independent.

Purpose: Approvers can open the proposal detail to read attachments and context before deciding, without having to navigate to a different view.
Output: Modified `_renderHomeApprovalQueueHtml` function in `app/views/home.js`.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\PROJECT.md
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\.planning\phases\93.1-proposal-queue-ux-row-click-modal-unified-status-table\93.1-CONTEXT.md

<interfaces>
<!-- Key signatures extracted from app/views/home.js (read before editing) -->

Function being modified:
  _renderHomeApprovalQueueHtml(pending)   // line 227 — builds the queue card HTML

Existing pattern on the <tr> rows (lines 250–266):
  Each row currently has no onclick. The last cell contains two <button> elements with:
    onclick="window.homeQueueOpenApproveModal('${escapeHTML(p.id)}')"
    onclick="window.homeQueueOpenRejectModal('${escapeHTML(p.id)}')"

window.openProposalModal is already registered in init() (line 556):
  window.openProposalModal = openProposalModal;

Guard pattern used elsewhere in proposals.js renderStageGroupCard (line 179):
  onclick="event.preventDefault();window.openProposalModal && window.openProposalModal('${escapeHTML(p.id)}')"

escapeHTML() is imported from utils.js (line 20).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add row-click handler and hover affordance to queue table rows</name>
  <files>app/views/home.js</files>

  <read_first>
    - app/views/home.js — read _renderHomeApprovalQueueHtml (lines 227–294) in full before making any edits. Understand the exact <tr> and <button> structure.
    - app/views/proposals.js — read renderStageGroupCard (lines 165–218) for the established onclick guard pattern: `window.openProposalModal && window.openProposalModal('${escapeHTML(p.id)}')`.
  </read_first>

  <action>
  In `_renderHomeApprovalQueueHtml` (home.js), modify each `<tr>` in the `rows` map to:

  1. Add onclick to the `<tr>` element (per D-01):
     `onclick="window.openProposalModal && window.openProposalModal('${escapeHTML(p.id)}')"`

  2. Add cursor and hover style to the `<tr>` element (per D-01). Use inline onmouseenter/onmouseleave toggling style.background — this matches the template-literal pattern already used throughout home.js for per-row styles:
     `style="cursor:pointer;"` on the `<tr>`
     `onmouseenter="this.style.background='#f8fafc'"` on the `<tr>`
     `onmouseleave="this.style.background=''"` on the `<tr>`

  3. Prefix BOTH button onclick handlers with `event.stopPropagation();` (per D-02) so the Approve/Reject buttons do not trigger the row-click modal:
     `onclick="event.stopPropagation(); window.homeQueueOpenApproveModal('${escapeHTML(p.id)}')"`
     `onclick="event.stopPropagation(); window.homeQueueOpenRejectModal('${escapeHTML(p.id)}')"`

  No other functions are touched. Do not change the queue card outer structure, column headers, or the empty-state branch.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('app/views/home.js','utf8');const hasRowClick=src.includes('window.openProposalModal && window.openProposalModal');const hasStopProp=(src.match(/event\.stopPropagation\(\)/g)||[]).length>=2;const hasCursor=src.includes('cursor:pointer');console.log('rowClick:',hasRowClick,'stopProp(x2+):',hasStopProp,'cursor:',hasCursor);if(!hasRowClick||!hasStopProp||!hasCursor)process.exit(1);"</automated>
  </verify>

  <done>
    - Each queue `<tr>` has `onclick`, `onmouseenter`, `onmouseleave`, and `style="cursor:pointer;"` attributes.
    - Both Approve and Reject button onclick handlers begin with `event.stopPropagation();`.
    - `window.openProposalModal` guard pattern matches the form used in `renderStageGroupCard`.
    - No other functions in home.js are modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore → home.js template literals | proposal.id and proposal.title are user-supplied strings interpolated into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93.1-01 | Injection | `<tr onclick>` interpolating `p.id` | mitigate | `escapeHTML(p.id)` is already applied in the existing button handlers; the new `<tr onclick>` must use the same `escapeHTML(p.id)` guard |
| T-93.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Manual browser steps (after executor completes):
1. Navigate to Home → Proposals sub-tab (as super_admin or operations_admin with pending_internal proposals).
2. Hover over a queue row — cursor changes to pointer, background lightens to #f8fafc.
3. Click the row body (not buttons) — proposal detail modal opens.
4. Click Approve button — approve mini-modal opens, proposal modal does NOT open.
5. Click Reject button — reject mini-modal opens, proposal modal does NOT open.
</verification>

<success_criteria>
- Row click opens `openProposalModal` with the correct proposal id.
- Approve/Reject buttons work independently (stopPropagation confirmed).
- Hover highlight and pointer cursor are visible.
- No regressions to the queue card structure or action logic.
</success_criteria>

<output>
Create `.planning/phases/93.1-proposal-queue-ux-row-click-modal-unified-status-table/93.1-01-SUMMARY.md` when done.
</output>
