---
phase: 93.1-proposal-queue-ux-row-click-modal-unified-status-table
verified: 2026-05-25T00:00:00Z
status: human_needed
score: 10/10 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Navigate to Home -> Proposals sub-tab (as super_admin or operations_admin with at least one pending_internal proposal). Hover over a queue row."
    expected: "Cursor changes to pointer; background lightens to #f8fafc on hover."
    why_human: "CSS cursor and background transitions cannot be verified programmatically via grep."
  - test: "Click anywhere on a queue row body (not the Approve or Reject buttons)."
    expected: "Proposal detail modal opens for the correct proposal."
    why_human: "Modal open behavior requires browser DOM interaction and window function dispatch."
  - test: "Click the Approve button on a queue row."
    expected: "Approve mini-modal opens; proposal detail modal does NOT open."
    why_human: "stopPropagation behavior requires live event bubbling in a browser."
  - test: "Click the Reject button on a queue row."
    expected: "Reject mini-modal opens; proposal detail modal does NOT open."
    why_human: "Same as above — event propagation cannot be simulated via static analysis."
  - test: "Navigate to Home -> Proposals sub-tab (as super_admin). Verify five scorecard tiles appear above the unified proposals table."
    expected: "Five tiles in order: Pending Internal Approval (amber left border), Pending Client Review (blue), For Revision (red), Client Approved (green), Loss (gray). Each tile shows a count matching proposals in that status."
    why_human: "Visual tile rendering, border colors, and live counts require browser rendering + Firestore data."
  - test: "Click the 'Pending Internal Approval' scorecard tile."
    expected: "Table filters to only pending_internal rows; tile gains active border/background (project-scorecard-card--active)."
    why_human: "Filter state and active-class toggle require live DOM interaction."
  - test: "Click the same active tile again."
    expected: "Filter clears; all proposals are shown; tile returns to default (non-active) appearance."
    why_human: "Toggle-off behavior requires live browser interaction."
  - test: "Click any row in the unified proposals table."
    expected: "Proposal detail modal opens for that proposal."
    why_human: "Modal dispatch requires live browser DOM."
  - test: "Navigate away from Home (e.g., to Procurement) and then back to Home -> Proposals sub-tab."
    expected: "Scorecards reset to 'all' state (no active tile), full table shows all proposals."
    why_human: "destroy()/init() lifecycle and filter reset require navigation in a live browser session."
  - test: "Log in as a non-approver role (e.g., services_admin). Navigate to Home -> Proposals sub-tab."
    expected: "No Approval Queue card appears; only scorecard tiles + unified table are shown."
    why_human: "Role-gated rendering requires a live session with the correct Firestore user record."
---

# Phase 93.1: Proposal Queue UX Verification Report

**Phase Goal:** Two UX improvements to the Home -> Proposals sub-tab: (1) make each row in the Proposal Approval Queue clickable to open the proposal detail modal; (2) collapse the separate per-status proposal cards into a single unified table with a Status column backed by filterable scorecard tiles.
**Verified:** 2026-05-25
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Clicking anywhere on a queue row (except action buttons) opens the proposal modal | VERIFIED | Line 251: `onclick="window.openProposalModal && window.openProposalModal('${escapeHTML(p.id)}')"` on `<tr>` in `_renderHomeApprovalQueueHtml` |
| 2 | Approve and Reject buttons fire their respective actions without opening the modal | VERIFIED | Lines 265/267: both button onclick handlers begin with `event.stopPropagation();` |
| 3 | Queue rows show a pointer cursor and subtle background highlight on hover | VERIFIED | Line 250: `style="cursor:pointer;"` on `<tr>`; lines 252-253: `onmouseenter`/`onmouseleave` toggling `style.background` to `#f8fafc` |
| 4 | Five scorecard tiles appear above the proposals table, each showing a count per proposal status | VERIFIED | `_renderHomeProposalScorecards` (lines 451-472) maps over `STAGE_ORDER` (5 entries), computes per-status counts, renders one tile per stage with `scorecard-label` and `scorecard-count` spans |
| 5 | Clicking a scorecard tile filters the table to that status; clicking the active tile again shows all | VERIFIED | `window.handleHomeProposalScorecardClick` (line 652-655): toggles `_homeProposalStatusFilter` (null if same key, else statusKey) then calls `_rerenderProposalTable()` |
| 6 | The unified table shows all proposals from filterProposalsForUser across all five statuses | VERIFIED | `_loadHomeProposalsTab` (line 564): `const scoped = filterProposalsForUser(all)` then passes `scoped` directly to `_renderHomeProposalTable(scoped)` with no active-only pre-filter |
| 7 | Each table row is clickable and opens the proposal detail modal | VERIFIED | Line 492: `onclick="window.openProposalModal && window.openProposalModal('${escapeHTML(p.id)}')"` on every `<tr>` in `_renderHomeProposalTable` |
| 8 | Status column uses getProposalStatusBadge(); Amount column uses formatCurrency(); all user-data fields use escapeHTML() | VERIFIED | Line 495: `${getProposalStatusBadge(p.status)}`; line 489: `'₱' + formatCurrency(p.amount)`; lines 486-487, 496, 498-499: all user fields wrapped in `escapeHTML()` |
| 9 | window.handleHomeProposalScorecardClick is registered in init() and deleted in destroy() | VERIFIED | Line 652: registered in `init()`; line 743: `delete window.handleHomeProposalScorecardClick` in `destroy()` |
| 10 | Filter state resets to null on each fresh _loadHomeProposalsTab call | VERIFIED | Line 557: `_homeProposalStatusFilter = null;` — third statement in `_loadHomeProposalsTab` body, before the getDocs fetch |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/views/home.js` | Queue row-click + unified table + scorecards | VERIFIED | File exists, 763 lines, substantive implementation. All five required functions present: `_renderHomeApprovalQueueHtml` (modified), `_renderHomeProposalScorecards` (new, line 451), `_renderHomeProposalTable` (new, line 480), `_rerenderProposalTable` (new, line 526), `_loadHomeProposalsTab` (modified, line 553) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `_renderHomeApprovalQueueHtml <tr>` | `window.openProposalModal` | `onclick` on `<tr>` element | WIRED | Line 251 — guard pattern matches `window.openProposalModal && window.openProposalModal(...)` |
| `Approve/Reject <button>` | `event.stopPropagation()` | `onclick` handler prefix | WIRED | Lines 265/267 — both buttons prefix with `event.stopPropagation();` |
| `window.handleHomeProposalScorecardClick` | `_homeProposalStatusFilter + _rerenderProposalTable` | scorecard tile onclick | WIRED | Line 652-655 in `init()`; tiles call `window.handleHomeProposalScorecardClick('${stage.key}')` at line 466 |
| `_loadHomeProposalsTab` | `_homeProposalsCache` | assigns scoped proposals before rendering | WIRED | Line 565: `_homeProposalsCache = scoped` after `filterProposalsForUser` |
| `_renderHomeProposalTable` | `window.openProposalModal` | `<tr onclick>` on each row | WIRED | Line 492 — same guard pattern as queue rows |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `_renderHomeProposalTable` | `proposals` parameter | `_homeProposalsCache` populated from `getDocs(collection(db, 'proposals'))` at line 560 | Yes — real Firestore query, not static | FLOWING |
| `_renderHomeProposalScorecards` | `proposals` parameter | Same cache path | Yes | FLOWING |
| `_rerenderProposalTable` | `_homeProposalsCache` | Pre-scoped from Firestore fetch; no Firestore round-trip on filter toggle | Yes — cache set from real query | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED — no runnable entry points (zero-build SPA; requires browser + live Firebase).

### Probe Execution

Step 7c: SKIPPED — no `scripts/*/tests/probe-*.sh` files present and phase does not declare any probes.

### Requirements Coverage

No requirement IDs were mapped to this phase (ROADMAP: "Requirements: None mapped (INSERTED UX improvement)"). Step 6 not applicable.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/views/home.js` | 326, 346 | `placeholder` string variable used in textarea HTML | Info | Not a stub — it is a legitimate UX label for the mini-modal textarea, interpolated via `escapeHTML(placeholder)`. No impact. |

No `TBD`, `FIXME`, or `XXX` markers found. No empty return stubs in phase-modified code paths. The `return []` at line 82 is the correct default branch of `filterProposalsForUser` for unknown roles, not a stub.

### Human Verification Required

All ten automated truths VERIFIED. The following items require a live browser session because they depend on real DOM event dispatch, CSS rendering, role-based Firestore auth, and visual appearance.

#### 1. Queue Row Hover Affordance

**Test:** Navigate to Home -> Proposals sub-tab as super_admin or operations_admin (ensure at least one pending_internal proposal exists). Hover slowly over queue table rows.
**Expected:** Cursor changes to a pointer hand; row background shifts to a light blue-gray (#f8fafc).
**Why human:** CSS transitions and cursor changes are not verifiable via static code analysis.

#### 2. Queue Row-Click Opens Modal

**Test:** Click on any cell in a queue row that is NOT the Approve or Reject button.
**Expected:** The proposal detail modal opens showing the correct proposal content.
**Why human:** `window.openProposalModal` dispatch and modal DOM insertion require live browser execution.

#### 3. Approve Button Stops Propagation

**Test:** Click the Approve button on a queue row.
**Expected:** The home-local approve mini-modal opens. The proposal detail modal does NOT open.
**Why human:** Event bubbling stopPropagation behavior requires a live browser to verify the event tree.

#### 4. Reject Button Stops Propagation

**Test:** Click the Reject button on a queue row.
**Expected:** The home-local reject mini-modal opens. The proposal detail modal does NOT open.
**Why human:** Same reason as #3.

#### 5. Five Scorecard Tiles Render Correctly

**Test:** Navigate to Home -> Proposals sub-tab as super_admin. Observe the area above the proposals table.
**Expected:** Five tiles appear in STAGE_ORDER sequence with correct border-left colors (amber, blue, red, green, gray) and counts matching the number of proposals per status in the database.
**Why human:** Visual rendering and live count accuracy require browser + Firestore.

#### 6. Scorecard Filter — Activate

**Test:** Click the "Pending Internal Approval" scorecard tile.
**Expected:** The table below updates to show only pending_internal proposals. The clicked tile gains a highlighted border/background (project-scorecard-card--active style).
**Why human:** DOM state changes and visual active state require live browser interaction.

#### 7. Scorecard Filter — Toggle Off

**Test:** Click the same already-active tile again.
**Expected:** The table returns to showing all proposals. The tile returns to its default (non-active) appearance.
**Why human:** Toggle-off state change requires live browser interaction.

#### 8. Unified Table Row-Click Opens Modal

**Test:** Click any row in the unified proposals table.
**Expected:** The proposal detail modal opens showing data for that row's proposal.
**Why human:** Modal dispatch requires live DOM.

#### 9. Filter Reset on Re-navigation

**Test:** Navigate away from Home (e.g., click Procurement in the nav), then navigate back and open the Proposals sub-tab.
**Expected:** Scorecard tiles appear with no active tile (filter reset to null); full unfiltered proposals table is shown.
**Why human:** destroy()/init() lifecycle and _homeProposalStatusFilter reset require browser navigation tracking.

#### 10. Non-Approver Role View

**Test:** Log in as services_admin (or any role where canApproveQueue = false). Navigate to Home -> Proposals sub-tab.
**Expected:** No Approval Queue card is visible. Only scorecard tiles + unified table are shown.
**Why human:** Role-gating requires a live browser session with an authenticated Firestore user.

### Gaps Summary

No gaps found. All 10 must-have truths are verified in the codebase with substantive implementations, correct wiring, and real data flowing from Firestore. The only items pending are browser-level behavioral checks that cannot be confirmed via static analysis.

---

_Verified: 2026-05-25_
_Verifier: Claude (gsd-verifier)_
