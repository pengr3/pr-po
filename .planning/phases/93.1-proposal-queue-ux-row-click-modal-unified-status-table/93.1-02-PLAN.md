---
phase: 93.1
plan: 02
type: execute
wave: 2
depends_on:
  - 93.1-01
files_modified:
  - app/views/home.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "Five scorecard tiles appear above the proposals table, each showing a count per proposal status"
    - "Clicking a scorecard tile filters the table to that status; clicking the active tile again shows all"
    - "The unified table shows all proposals from filterProposalsForUser across all five statuses"
    - "Each table row is clickable and opens the proposal detail modal"
    - "Status column shows a badge from getProposalStatusBadge(); Amount column uses formatCurrency(); all user-data fields use escapeHTML()"
    - "window.handleHomeProposalScorecardClick is registered in init() and deleted in destroy()"
    - "Filter state resets to null on each fresh _loadHomeProposalsTab call"
  artifacts:
    - path: "app/views/home.js"
      provides: "_renderHomeProposalTable, _renderHomeProposalScorecards, module-level _homeProposalStatusFilter, window.handleHomeProposalScorecardClick lifecycle"
      contains: "_homeProposalStatusFilter"
  key_links:
    - from: "window.handleHomeProposalScorecardClick"
      to: "_homeProposalStatusFilter + _rerenderProposalTable"
      via: "scorecard tile onclick"
      pattern: "handleHomeProposalScorecardClick"
    - from: "_loadHomeProposalsTab"
      to: "_homeProposalsCache"
      via: "assigns scoped proposals before rendering"
      pattern: "_homeProposalsCache = scoped"
    - from: "_renderHomeProposalTable"
      to: "window.openProposalModal"
      via: "<tr onclick> on each row"
      pattern: "window\\.openProposalModal && window\\.openProposalModal"
---

<objective>
Replace the `renderStageGroupCard` loop in `_loadHomeProposalsTab` with a unified proposals table preceded by five filterable scorecard tiles. Add the private helper functions `_renderHomeProposalScorecards`, `_renderHomeProposalTable`, and `_rerenderProposalTable`; add `_homeProposalStatusFilter` module-level state; register/delete `window.handleHomeProposalScorecardClick` in `init()`/`destroy()`.

Purpose: Users see all proposals (including terminal statuses client_approved and loss) in one scannable table. Scorecard tiles provide one-click status filtering without a Firestore round-trip.
Output: Modified `app/views/home.js` with new helper functions and updated `_loadHomeProposalsTab`, `init()`, and `destroy()`.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\PROJECT.md
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\.planning\phases\93.1-proposal-queue-ux-row-click-modal-unified-status-table\93.1-CONTEXT.md
@C:\Users\Admin\Roaming\pr-po\.planning\phases\93.1-proposal-queue-ux-row-click-modal-unified-status-table\93.1-01-SUMMARY.md

<interfaces>
<!-- Key signatures extracted from source files — executor uses these directly -->

From app/views/home.js (primary edit target):

Module-level state to ADD (line ~36–38 area, alongside _homeProposalsCache):
  let _homeProposalStatusFilter = null;  // null = show all; string = active status key

Existing module-level state to KEEP unchanged:
  let _homeProposalsCache = [];
  let _homeCanApproveQueue = false;

_loadHomeProposalsTab(canApproveQueue) — lines 451–511:
  - Currently ends dashboardHtml with the renderStageGroupCard loop (lines 480–494)
  - REPLACE the loop (and its empty-state) with: scorecards HTML + table HTML
  - KEEP the queueHtml section unchanged (lines 467–477)
  - ADD: _homeProposalStatusFilter = null; at the top of the function body (reset on fresh load)
  - The full scoped list (NOT filtered by active only) feeds the table. Remove the existing
    `const active = scoped.filter(...)` line — the unified table uses scoped directly via STAGE_ORDER.

init() — line 516:
  - ADD: window.handleHomeProposalScorecardClick = (statusKey) => { ... }
    Handler body: toggle _homeProposalStatusFilter (null if same key, else statusKey),
    then call _rerenderProposalTable().
  - Existing registrations (switchHomeTab, openProposalModal, homeQueue*) unchanged.

destroy() — line 638:
  - ADD: delete window.handleHomeProposalScorecardClick;
  - Existing deletions unchanged.

From app/views/proposals.js (read-only):

STAGE_ORDER (already imported into home.js, line 10):
  [
    { key: 'pending_internal', label: 'Pending Internal Approval' },
    { key: 'pending_client',   label: 'Pending Client Review' },
    { key: 'for_revision',     label: 'For Revision' },
    { key: 'client_approved',  label: 'Client Approved' },
    { key: 'loss',             label: 'Loss' }
  ]

getProposalStatusBadge(status) — proposals.js line 106:
  Returns an HTML <span> string. Already imported in home.js (line 13).
  Example: getProposalStatusBadge('pending_internal') → '<span class="status-badge pending">Pending Internal Approval</span>'

renderAgeBadge(proposal) — proposals.js line 140:
  Returns an HTML <span> string. Already imported in home.js (line 15).

From app/views/procurement.js (pattern reference):

Scorecard tile HTML structure (lines 2168–2183):
  <div class="project-scorecard-card" data-group="materials" data-status="Pending Procurement"
       style="border-left: 3px solid #dc2626"
       onclick="window.handleMRFScorecardClick('materials', 'Pending Procurement'); event.stopPropagation()">
    <span class="scorecard-label">Pending</span>
    <span class="scorecard-count" id="scoreMaterialsPending">0</span>
  </div>

Active toggle pattern (procurement.js ~line 448 equivalent, for proposal scorecards):
  card.classList.toggle('project-scorecard-card--active', card.dataset.status === activeFilter)

From styles/views.css (CSS classes — DO NOT add new CSS):
  .project-scorecard-card        — base card (background:#fff; border:1px solid #e5e7eb; cursor:pointer; padding:0.75rem; border-radius:6px)
  .project-scorecard-card--active — active state (border-color:#1a73e8; background:#e8f0fe)
  .project-scorecard-card:hover  — hover state
  .scorecard-label               — small label text (0.68rem)
  .scorecard-count               — large count number (1.5rem, font-weight:700)
  .project-scorecards            — grid container (grid-template-columns: repeat(6, 1fr))
    NOTE: .project-scorecards is a 6-column grid designed for projects tab.
    For 5 proposal tiles use inline flex instead:
    style="display:flex;flex-wrap:wrap;gap:0.5rem;margin-bottom:1rem;"
    Each tile: style="flex:1;min-width:140px;"

From utils.js (already imported in home.js line 20):
  formatCurrency(amount) — formats number to locale string
  escapeHTML(str)        — escapes HTML special characters
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add module-level filter state and new private helper functions</name>
  <files>app/views/home.js</files>

  <read_first>
    - app/views/home.js — read the full file before editing. Focus on: module-level state block (~lines 24–38), the imports block (~lines 7–21), and the existing _renderHomeApprovalQueueHtml function (lines 227–294) for the established row HTML pattern (column structure, escapeHTML usage, formatCurrency usage, renderAgeBadge usage).
    - app/views/proposals.js — read renderStageGroupCard (lines 165–218) for the exact field names used: p.proposal_id, p.id, p.title, p.project_code, p.target_client_name, p.amount, and the renderAgeBadge call. The unified table replicates this column content.
    - styles/views.css — read the scorecard CSS block around lines 2955–3010 to confirm class names (.project-scorecard-card, .project-scorecard-card--active, .scorecard-label, .scorecard-count) and that no new CSS needs to be added.
  </read_first>

  <action>
  Make the following additions to app/views/home.js. All additions are isolated inserts — do not alter any existing function.

  **1. Module-level filter variable** — add immediately after `let _homeCanApproveQueue = false;` (line ~38):
    ```
    let _homeProposalStatusFilter = null; // null = all stages; string = active status key (single-select)
    ```

  **2. `_renderHomeProposalScorecards(proposals, activeFilter)` private function** — add before `_loadHomeProposalsTab`. Builds the five scorecard tiles:
    - Container: `<div style="display:flex;flex-wrap:wrap;gap:0.5rem;margin-bottom:1rem;">`
    - One tile per entry in STAGE_ORDER in order.
    - Per-tile color map (border-left) per D-12:
        pending_internal → `#f59e0b`
        pending_client   → `#3b82f6`
        for_revision     → `#ef4444`
        client_approved  → `#059669`
        loss             → `#6b7280`
    - Each tile: `<div class="project-scorecard-card${activeFilter === stage.key ? ' project-scorecard-card--active' : ''}"
        data-status="${stage.key}"
        style="flex:1;min-width:140px;border-left:3px solid ${color};"
        onclick="window.handleHomeProposalScorecardClick('${stage.key}')">`
    - Inside tile: `<span class="scorecard-label">${escapeHTML(stage.label)}</span>` and
      `<span class="scorecard-count">${count}</span>` where count = proposals.filter(p => p.status === stage.key).length.
    - Count 0 renders (tile still shown, count text is "0").

  **3. `_renderHomeProposalTable(proposals)` private function** — add after _renderHomeProposalScorecards. Builds the unified proposals table:
    - If proposals.length === 0: return a card with message "No proposals match the selected filter. Click the active tile to show all." (used when filter active + no matches).
    - Table columns (left → right): Status | PROP-ID | Title | Project | Client | Amount | Age
    - Each `<tr>` is clickable (per D-09):
        `style="cursor:pointer;"` on `<tr>`
        `onmouseenter="this.style.background='#f8fafc'"`
        `onmouseleave="this.style.background=''"`
        `onclick="window.openProposalModal && window.openProposalModal('${escapeHTML(p.id)}')"`
    - Status cell: `getProposalStatusBadge(p.status)` (no escapeHTML needed — badge function escapes internally).
    - PROP-ID cell: `escapeHTML(p.proposal_id || p.id)`
    - Title cell: truncate at 40 chars same as renderStageGroupCard: `(p.title||'').length > 40 ? escapeHTML((p.title||'').slice(0,40))+'…' : escapeHTML(p.title||'—')`
    - Project cell: `escapeHTML(p.project_code || '—')`
    - Client cell: `escapeHTML(p.target_client_name || '(none)')`
    - Amount cell (right-aligned): `p.amount != null && p.amount !== '' ? '₱'+formatCurrency(p.amount) : '—'`  — formatCurrency output is numeric, no escapeHTML needed.
    - Age cell: `renderAgeBadge(p)` — badge function handles escaping internally.
    - Wrap table in `<div class="card" style="margin-bottom:1rem;"><div style="overflow-x:auto;">...</div></div>`.
    - Table header row: `background:#f8f9fa; border-bottom:1px solid #e5e7eb;` — match queue table header style (font-size:0.8125rem; color:#64748b; font-weight:600; padding:0.6rem 1rem).

  **4. `_rerenderProposalTable()` private function** — add after _renderHomeProposalTable. Used by the scorecard click handler to re-render from cache without Firestore:
    - const mount = document.getElementById('homeProposalsContent'); if (!mount) return;
    - const tableContainerId = 'homeProposalTableSection';
    - const existing = document.getElementById(tableContainerId); if (!existing) return;
    - const filtered = _homeProposalStatusFilter
        ? _homeProposalsCache.filter(p => p.status === _homeProposalStatusFilter)
        : _homeProposalsCache;
    - existing.innerHTML = _renderHomeProposalScorecards(_homeProposalsCache, _homeProposalStatusFilter)
        + _renderHomeProposalTable(filtered);
    - After re-render, update active class on scorecard tiles:
        document.querySelectorAll('#homeProposalTableSection .project-scorecard-card').forEach(card => {
          card.classList.toggle('project-scorecard-card--active', card.dataset.status === _homeProposalStatusFilter);
        });
      NOTE: The class is already set via template literal in _renderHomeProposalScorecards — the classList.toggle call after innerHTML assignment provides a safety-net sync but is not strictly required if the template sets the class correctly. Include it.

  No existing functions are touched in this task.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('app/views/home.js','utf8');const checks={filterVar:src.includes('_homeProposalStatusFilter = null'),scorecardFn:src.includes('function _renderHomeProposalScorecards'),tableFn:src.includes('function _renderHomeProposalTable'),rerenderFn:src.includes('function _rerenderProposalTable'),stageOrder:src.includes('STAGE_ORDER'),escapeInTile:src.includes('escapeHTML(stage.label)'),getStatusBadge:src.includes('getProposalStatusBadge(p.status)'),formatCurrencyUsed:src.includes('formatCurrency(p.amount)'),renderAgeBadgeUsed:src.includes('renderAgeBadge(p)'),rowClick:src.includes('openProposalModal && window.openProposalModal'),cursor:src.includes('cursor:pointer')};const failed=Object.entries(checks).filter(([k,v])=>!v).map(([k])=>k);if(failed.length){console.error('MISSING:',failed);process.exit(1);}else{console.log('All checks passed');}"</automated>
  </verify>

  <done>
    - `_homeProposalStatusFilter = null` declared at module scope.
    - `_renderHomeProposalScorecards(proposals, activeFilter)` function exists and renders five tiles with correct data-status, border-left colors, scorecard-label, scorecard-count, and active class.
    - `_renderHomeProposalTable(proposals)` function exists and renders a table with 7 columns; each row has onclick, onmouseenter, onmouseleave, cursor:pointer; all user-data fields use escapeHTML() or the badge/currency helpers that escape internally.
    - `_rerenderProposalTable()` function exists and reads from `_homeProposalsCache` without Firestore.
    - No existing function is modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire helpers into _loadHomeProposalsTab, init(), and destroy()</name>
  <files>app/views/home.js</files>

  <read_first>
    - app/views/home.js — re-read _loadHomeProposalsTab (lines 451–511), init() (lines 516–564), and destroy() (lines 638–666) after Task 1 edits are applied. All wiring targets live in these three functions.
  </read_first>

  <action>
  Make three surgical edits to existing functions. Do not alter any code outside the described change points.

  **Edit A — `_loadHomeProposalsTab`** (per D-06, D-07, D-13, D-14):

  At the very top of the function body (first statement after the `if (!mount) return;` guard and `_homeCanApproveQueue = !!canApproveQueue;` assignment), add:
    `_homeProposalStatusFilter = null;`
  This resets the filter on every fresh load (D-14).

  Remove the existing line:
    `const active = scoped.filter(p => p.status !== 'client_approved' && p.status !== 'loss');`
  The unified table uses `scoped` directly.

  Replace the entire `for (const stage of STAGE_ORDER) { ... }` loop and its empty-state block (the `if (!dashboardHtml) { ... }` branch) with:
    ```
    const tableSection = `<div id="homeProposalTableSection">
      ${_renderHomeProposalScorecards(scoped, null)}
      ${_renderHomeProposalTable(scoped)}
    </div>`;
    const dashboardHtml = tableSection;
    ```
  The empty-state when scoped is zero-length is handled inside `_renderHomeProposalTable` itself (renders "No proposals in your scope." message when called with an empty array — add this empty state: `if (proposals.length === 0) return '<div class="card" style="margin-bottom:1rem;"><div class="card-body" style="padding:1.25rem 1.5rem;"><p style="color:#64748b;margin:0;font-size:0.9375rem;">No proposals in your scope.</p></div></div>';`).

  Keep the `queueHtml` section (canApproveQueue block, lines 467–477) completely unchanged.
  Keep the final `mount.innerHTML = ...` block unchanged (it already interpolates `${queueHtml}${dashboardHtml}`).

  NOTE: `renderStageGroupCard` import from proposals.js (line 11 of home.js) becomes unused after this edit. Remove it from the import statement to keep the module clean: change `renderStageGroupCard,` out of the named imports on line ~11. Do not remove any other import.

  **Edit B — `init()`** (per D-15):

  After the existing line `window.homeQueueOpenRejectModal = (id) => _openHomeQueueModal(id, 'reject');`, add:
    ```
    window.handleHomeProposalScorecardClick = (statusKey) => {
        _homeProposalStatusFilter = (_homeProposalStatusFilter === statusKey) ? null : statusKey;
        _rerenderProposalTable();
    };
    ```

  **Edit C — `destroy()`** (per D-15):

  After the existing line `delete window.homeQueueOpenRejectModal;`, add:
    `delete window.handleHomeProposalScorecardClick;`
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('app/views/home.js','utf8');const checks={filterReset:src.includes('_homeProposalStatusFilter = null'),tableSection:src.includes('homeProposalTableSection'),scorecardCallInLoad:src.includes('_renderHomeProposalScorecards(scoped'),tableCallInLoad:src.includes('_renderHomeProposalTable(scoped'),windowReg:src.includes('window.handleHomeProposalScorecardClick = '),windowDel:src.includes('delete window.handleHomeProposalScorecardClick'),noStageLoop:!src.includes('for (const stage of STAGE_ORDER)'),noRenderStageGroupCard:!src.includes('renderStageGroupCard(')};const failed=Object.entries(checks).filter(([k,v])=>!v).map(([k])=>k);if(failed.length){console.error('MISSING or UNEXPECTED:',failed);process.exit(1);}else{console.log('All wiring checks passed');}"</automated>
  </verify>

  <done>
    - `_homeProposalStatusFilter = null` is the first reset in `_loadHomeProposalsTab` body.
    - The `renderStageGroupCard` loop is gone; `homeProposalTableSection` div is injected instead.
    - `window.handleHomeProposalScorecardClick` is registered in `init()` with toggle logic.
    - `delete window.handleHomeProposalScorecardClick` is in `destroy()`.
    - `renderStageGroupCard` is removed from the proposals.js import statement (no unused import).
    - Browser: Home → Proposals sub-tab renders five scorecard tiles above a single unified table. Clicking a tile filters the table. Clicking the active tile clears the filter. Each table row click opens the proposal modal.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore → template literals in _renderHomeProposalTable | p.title, p.proposal_id, p.project_code, p.target_client_name, p.id are user-supplied strings interpolated into HTML |
| Scorecard onclick → statusKey parameter | statusKey is a string from STAGE_ORDER.key — enum-bounded, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93.1-02 | Injection | _renderHomeProposalTable row interpolation | mitigate | escapeHTML() required on p.title, p.proposal_id, p.project_code, p.target_client_name, p.id in all <tr> and <td> contexts. Amount uses formatCurrency (numeric). Badge functions escape internally. |
| T-93.1-03 | Injection | scorecard tile label interpolation | mitigate | escapeHTML(stage.label) — stage.label comes from STAGE_ORDER constant (not Firestore), but escapeHTML is still applied per project convention |
| T-93.1-04 | Elevation of Privilege | _rerenderProposalTable bypasses filterProposalsForUser | accept | _homeProposalsCache is already the scoped (role-filtered) result from filterProposalsForUser set during _loadHomeProposalsTab. The re-render reads from this pre-scoped cache — no bypass occurs. |
| T-93.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Manual browser steps (after both tasks complete):
1. Navigate to Home → Proposals sub-tab (as super_admin — sees all proposals).
2. Verify five scorecard tiles appear: Pending Internal Approval (amber left border), Pending Client Review (blue), For Revision (red), Client Approved (green), Loss (gray). Counts match number of proposals in each status.
3. Click the "Pending Internal Approval" tile — table filters to only pending_internal rows. Tile gains active border/background.
4. Click the same tile again — filter clears, all proposals show. Tile returns to default state.
5. Click any table row — proposal modal opens for that proposal.
6. Navigate away from Home (e.g., to Procurement), then back — scorecards are reset to "all" state (no active tile), full table shows.
7. If approver: confirm Approval Queue card still appears above scorecards.
8. If non-approver: confirm only scorecards + table appear (no queue card).
</verification>

<success_criteria>
- Five scorecard tiles render with correct colors, labels, and live counts.
- Single-select toggle filter works without Firestore round-trip.
- Unified table covers all five proposal stages (active + terminal).
- All user-data fields in table rows pass through escapeHTML() or a helper that escapes internally.
- window.handleHomeProposalScorecardClick lifecycle is clean (registered in init, deleted in destroy).
- renderStageGroupCard is no longer imported or called.
- No regressions to queue card, queue row-click (Plan 01), or any other home.js surface.
</success_criteria>

<output>
Create `.planning/phases/93.1-proposal-queue-ux-row-click-modal-unified-status-table/93.1-02-SUMMARY.md` when done.
</output>
