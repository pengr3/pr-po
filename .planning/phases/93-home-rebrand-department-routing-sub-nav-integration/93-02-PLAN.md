---
phase: 93
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/home.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "Home page shows 5 department tiles: Clients, Projects, Services, Procurement, Finance"
    - "The 3 legacy nav-cards (Material Request, Procurement, Finance Dashboard) are gone from the DOM"
    - "Clicking any tile changes location.hash to the correct route (#/clients, #/projects, #/services, #/procurement, #/finance)"
    - "The Phase 87.1 sub-nav (Overview | Engagements | Proposals) renders identically to before"
    - "Overview sub-tab shows #homeOverviewContent (tile grid is above it; stats card is inside it)"
    - "switchHomeTab('overview') shows #homeOverviewContent, not .quick-stats"
    - "switchHomeTab('engagements') and switchHomeTab('proposals') are unchanged in behavior"
    - "getHomeSubTabConfig() is completely unchanged"
    - "No new window.* functions are registered for tile clicks"
  artifacts:
    - path: "app/views/home.js"
      provides: "Updated render() with 5-tile .dept-cards grid + #homeOverviewContent wrapper; updated switchHomeTab()"
      contains: "dept-cards"
  key_links:
    - from: "home.js render() #homeOverviewContent"
      to: "switchHomeTab() overviewEl selector"
      via: "document.getElementById('homeOverviewContent')"
      pattern: "homeOverviewContent"
    - from: "home.js .nav-card tiles"
      to: "router.js route protection"
      via: "onclick=\"location.hash='#/...'\""
      pattern: "location.hash='#/"
---

<objective>
Rewrite the render() function in app/views/home.js to replace the 3 legacy nav-cards with a 5-tile departmental grid, wrap the Overview sub-tab content in a new #homeOverviewContent div, and update switchHomeTab() to toggle #homeOverviewContent instead of .quick-stats.

Purpose: Deliver the Phase 93 visual rebrand — 5 department tiles with correct routes, descriptions, and icons; Procurement stats card repositioned inside the Overview wrapper below the tiles; Phase 87.1 sub-nav fully preserved.
Output: Modified app/views/home.js (render() + switchHomeTab() only — no other functions touched).
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-home-rebrand-department-routing-sub-nav-integration/93-CONTEXT.md
@.planning/phases/93-home-rebrand-department-routing-sub-nav-integration/93-UI-SPEC.md
</context>

<interfaces>
<!-- Current home.js structure — read the full file before editing -->

Imports (lines 1–22): unchanged — do not modify.
Module-scope state (lines 24–37): unchanged.
getDashboardMode() (lines 43–48): unchanged.
getHomeSubTabConfig() (lines 55–64): unchanged — D-04 locked, must not be touched.
filterProposalsForUser() (lines 73–83): unchanged.
procurementCardHtml() (lines 90–110): unchanged — renders .hs-stat-card Procurement block.

render() — CURRENT structure (lines 116–173):
  - calls getDashboardMode() and procurementCardHtml()
  - emits <div class="navigation-cards"> with 3 .nav-card divs
  - emits <div class="home-sub-nav" id="homeSubNav" style="display:none;">
  - emits <div id="homeEngagementsContent" style="display:none;">
  - emits <div id="homeProposalsContent" style="display:none;">
  - emits <div class="quick-stats"> containing ${statsContent}

switchHomeTab() — CURRENT (lines 180–200):
  - const overviewEl = document.querySelector('.quick-stats');   ← THIS MUST CHANGE
  - hides/shows overviewEl, engEl, propEl
  - toggles active class on the three tab buttons

init() (lines 499–547): unchanged — reads #homeSubNav, #homeEngagementsContent, #homeProposalsContent, homeTabOverview, homeTabEngagements, homeTabProposals. None of these IDs change.
loadStats(), updateStatDisplay(), destroy() (lines 553–649): completely unchanged.

CSS class names supplied by Plan 01:
  .dept-cards           — flex column wrapper, max-width 1200px, margin-bottom 2rem
  .dept-cards-row       — grid row
  .dept-cards-row--top  — 3 columns on desktop
  .dept-cards-row--bottom — 2 columns centered on desktop
  .nav-card             — existing tile style (unchanged)
  .nav-card-icon        — existing icon style (unchanged)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace nav-cards block in render() with 5-tile .dept-cards grid</name>
  <files>app/views/home.js</files>

  <read_first>
    app/views/home.js — read in full before making any edits. The two edit sites are render() (lines ~116–173) and switchHomeTab() (lines ~180–200). All other code is off-limits.
  </read_first>

  <action>
    Make exactly two edits to app/views/home.js. No other lines are touched.

    EDIT 1 — Inside render(), replace the entire <div class="navigation-cards">…</div> block (the 3 legacy nav-cards) and the <div class="quick-stats">…</div> block at the bottom with the new structure below. The sub-nav elements (#homeSubNav, #homeEngagementsContent, #homeProposalsContent) are positioned identically to today — between the .dept-cards grid and the #homeOverviewContent wrapper.

    New render() return value (full template literal body — replace only the interior of the backtick template):

        <div class="hero-section">
            <h1 class="hero-title">🏗️ CLMC</h1>
            <p class="hero-subtitle">Management System Portal</p>

            <div class="dept-cards">
                <div class="dept-cards-row dept-cards-row--top">
                    <div class="nav-card" onclick="location.hash='#/clients'">
                        <div class="nav-card-icon">📋</div>
                        <h3>Clients</h3>
                        <p>Manage client records, contacts, and engagement history</p>
                        <button class="btn btn-primary">Enter →</button>
                    </div>
                    <div class="nav-card" onclick="location.hash='#/projects'">
                        <div class="nav-card-icon">🏗️</div>
                        <h3>Projects</h3>
                        <p>Track projects, budgets, Gantt schedules, and financials</p>
                        <button class="btn btn-primary">Enter →</button>
                    </div>
                    <div class="nav-card" onclick="location.hash='#/services'">
                        <div class="nav-card-icon">🔧</div>
                        <h3>Services</h3>
                        <p>Manage recurring service contracts and work tracking</p>
                        <button class="btn btn-primary">Enter →</button>
                    </div>
                </div>
                <div class="dept-cards-row dept-cards-row--bottom">
                    <div class="nav-card" onclick="location.hash='#/procurement'">
                        <div class="nav-card-icon">🛒</div>
                        <h3>Procurement</h3>
                        <p>Submit MRFs, manage suppliers, track orders and RFPs</p>
                        <button class="btn btn-primary">Enter →</button>
                    </div>
                    <div class="nav-card" onclick="location.hash='#/finance'">
                        <div class="nav-card-icon">💰</div>
                        <h3>Finance</h3>
                        <p>Approve PRs, manage payables, collectibles, and RFPs</p>
                        <button class="btn btn-primary">Enter →</button>
                    </div>
                </div>
            </div>

            <!-- Phase 87.1 D-01 — Home sub-nav; init() reveals it for eligible roles -->
            <div class="home-sub-nav" id="homeSubNav" style="display:none;">
                <div class="home-sub-nav-tabs">
                    <button class="home-sub-nav-tab home-sub-nav-tab--active" id="homeTabOverview"
                            onclick="window.switchHomeTab('overview')">Overview</button>
                    <button class="home-sub-nav-tab" id="homeTabEngagements" style="display:none;"
                            onclick="window.switchHomeTab('engagements')">Engagements</button>
                    <button class="home-sub-nav-tab" id="homeTabProposals" style="display:none;"
                            onclick="window.switchHomeTab('proposals')">Proposals</button>
                </div>
            </div>
            <div id="homeEngagementsContent" style="display:none;"></div>
            <div id="homeProposalsContent" style="display:none;"></div>

            <!-- Phase 93: Overview wrapper — tiles shown above (always visible); stats card shown/hidden by switchHomeTab -->
            <div id="homeOverviewContent">
                <div class="quick-stats">
                    ${statsContent}
                </div>
            </div>
        </div>

    Note: the mode variable and statsContent assignment at the top of render() are unchanged. The getDashboardMode() call and procurementCardHtml() call stay exactly as today.

    EDIT 2 — Inside switchHomeTab(), change the overviewEl selector from:
        const overviewEl = document.querySelector('.quick-stats');
    to:
        const overviewEl = document.getElementById('homeOverviewContent');

    No other line in switchHomeTab() changes. The hide-all loop and the three branch conditions (engagements / proposals / else) are identical to today.
  </action>

  <verify>
    <automated>grep -c "dept-cards" app/views/home.js</automated>
    Expected: 4 matches (class="dept-cards", class="dept-cards-row dept-cards-row--top", class="dept-cards-row dept-cards-row--bottom", and the closing div comment area).

    <automated>grep -c "navigation-cards" app/views/home.js</automated>
    Expected: 0 — the old .navigation-cards div is completely removed.

    <automated>grep -c "mrf-form" app/views/home.js</automated>
    Expected: 0 — the Material Request nav-card referencing #/mrf-form is removed.

    <automated>grep "homeOverviewContent" app/views/home.js</automated>
    Expected: lines containing id="homeOverviewContent" (in render()) and document.getElementById('homeOverviewContent') (in switchHomeTab()).

    <automated>grep "document.querySelector('.quick-stats')" app/views/home.js</automated>
    Expected: 0 matches — old selector replaced.

    <automated>grep "location.hash='#/clients'" app/views/home.js</automated>
    Expected: 1 match (Clients tile onclick).

    <automated>grep "location.hash='#/projects'" app/views/home.js</automated>
    Expected: 1 match (Projects tile onclick).

    <automated>grep "location.hash='#/services'" app/views/home.js</automated>
    Expected: 1 match (Services tile onclick).

    <automated>grep "getHomeSubTabConfig" app/views/home.js</automated>
    Expected: same count as before (function body unchanged — verify by diff that only render() and switchHomeTab() changed).
  </verify>

  <done>
    app/views/home.js render() emits .dept-cards with 5 .nav-card tiles (Clients/Projects/Services in top row, Procurement/Finance in bottom row). The 3 legacy nav-cards (Material Request, old Procurement, old Finance Dashboard) are absent. The .quick-stats block is wrapped in #homeOverviewContent. switchHomeTab() targets document.getElementById('homeOverviewContent'). All other functions (init, destroy, loadStats, getHomeSubTabConfig, etc.) are byte-for-byte identical to before.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tile onclick handlers | onclick="location.hash='#/...'" — no user input, hardcoded hash strings only. Router handles auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-02-01 | Spoofing | tile onclick routes | accept | Routes are hardcoded string literals; no user input. router.js enforces auth on navigate. |
| T-93-02-02 | Tampering | switchHomeTab overviewEl selector | mitigate | getElementById('homeOverviewContent') is specific; cannot accidentally target an unrelated element. Old querySelector('.quick-stats') was less specific. |
| T-93-02-03 | Tampering | Phase 87.1 sub-nav preserved | accept | getHomeSubTabConfig() is untouched; no regression on role gating possible from this edit. |
</threat_model>

<verification>
1. grep -c "dept-cards" app/views/home.js returns 4+
2. grep -c "navigation-cards" app/views/home.js returns 0
3. grep -c "mrf-form" app/views/home.js returns 0
4. grep "homeOverviewContent" app/views/home.js shows 2 hits: render() id= and switchHomeTab() getElementById()
5. Browser smoke-test: home page loads 5 tiles; clicking Clients navigates to #/clients; clicking Procurement navigates to #/procurement; Overview sub-tab shows stats card; Engagements and Proposals sub-tabs work
6. Roles that see the sub-nav (operations_admin, super_admin) — switchHomeTab('engagements') shows engagements content, hides #homeOverviewContent; switchHomeTab('overview') shows #homeOverviewContent
</verification>

<success_criteria>
The home page shows 5 department tiles (Clients, Projects, Services, Procurement, Finance) in a 3+2 grid. The 3 legacy nav-cards are removed. Phase 87.1 sub-nav (Overview | Engagements | Proposals) works identically to before. Overview sub-tab shows the Procurement stats card. All tab switching is correct.
</success_criteria>

<output>
Create .planning/phases/93-home-rebrand-department-routing-sub-nav-integration/93-02-SUMMARY.md when done
</output>
