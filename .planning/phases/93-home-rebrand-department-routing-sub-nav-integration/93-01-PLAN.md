---
phase: 93
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - styles/hero.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Department tiles render in a 3-on-top / 2-centered-below grid on desktop"
    - "Bottom 2-tile row (Procurement, Finance) is visually centered relative to the 3-tile top row"
    - "At ≤1024px both rows become 2-column grids; the lone bottom-row tile (Finance) spans full width centered"
    - "At ≤768px all rows collapse to 1-column stacks"
    - "Existing .nav-card styles are unchanged — tile visual appearance is identical to current cards"
  artifacts:
    - path: "styles/hero.css"
      provides: ".dept-cards, .dept-cards-row, responsive overrides for dept grid"
      contains: ".dept-cards"
  key_links:
    - from: "styles/hero.css .dept-cards-row"
      to: "home.js render() .dept-cards-row--top / .dept-cards-row--bottom"
      via: "class names used in Plan 02 HTML template"
      pattern: "dept-cards-row"
---

<objective>
Add the Phase 93 department-tile grid CSS to styles/hero.css — the .dept-cards wrapper and .dept-cards-row inner-row classes with three breakpoints. This CSS is consumed by Plan 02 which rewrites home.js render().

Purpose: Establish the 3+2 centered layout contract before the JS template is written, so Plan 02 can reference exact class names without discovery.
Output: Three new CSS rule blocks appended to styles/hero.css (desktop, ≤1024px, ≤768px). No existing rules are modified.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-home-rebrand-department-routing-sub-nav-integration/93-CONTEXT.md
@.planning/phases/93-home-rebrand-department-routing-sub-nav-integration/93-UI-SPEC.md
</context>

<interfaces>
<!-- Key existing CSS in styles/hero.css that executor must not regress -->
<!-- Read the file fully before editing. Key selectors to preserve: -->

From styles/hero.css (existing, unchanged):
  .navigation-cards { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; max-width: 1200px; width: 100%; margin-bottom: 4rem; }
  .nav-card { background: white; border-radius: 12px; padding: 2.5rem 2rem; text-align: center; box-shadow: ...; border: 2px solid transparent; }
  .nav-card:hover { transform: translateY(-8px); box-shadow: ...; border-color: var(--primary); }
  .nav-card-icon { font-size: 4rem; margin-bottom: 1.5rem; display: block; }
  .nav-card h3 { font-size: 1.5rem; font-weight: 600; color: var(--gray-900); margin-bottom: 1rem; }
  .nav-card p { font-size: 1rem; color: var(--gray-700); margin-bottom: 2rem; line-height: 1.6; }
  .nav-card .btn { width: 100%; padding: 0.75rem 1.5rem; font-size: 1rem; font-weight: 600; }

Existing @media (max-width: 1024px) block contains .navigation-cards overrides.
Existing @media (max-width: 768px) block contains .navigation-cards and .nav-card overrides.
These existing media query blocks must NOT be touched — add new dept-* rules inside them.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add .dept-cards and .dept-cards-row CSS to hero.css</name>
  <files>styles/hero.css</files>

  <read_first>
    styles/hero.css — read in full before editing. Locate the end of the existing "/* Navigation Cards */" block and the two existing @media blocks (≤1024px, ≤768px). All new rules are appended/inserted without modifying existing selectors.
  </read_first>

  <action>
    Append three new CSS blocks to styles/hero.css immediately after the existing "/* Navigation Cards */" section (before the "/* Quick Stats */" section). Do NOT modify any existing rule.

    Block 1 — Desktop (no media query — base styles):

    /* Department Tiles Grid (Phase 93) — 5-tile 3+2 layout */
    .dept-cards {
        display: flex;
        flex-direction: column;
        gap: 2rem;
        max-width: 1200px;
        width: 100%;
        margin-bottom: 2rem;
    }

    .dept-cards-row {
        display: grid;
        gap: 2rem;
    }

    .dept-cards-row--top {
        grid-template-columns: repeat(3, 1fr);
    }

    .dept-cards-row--bottom {
        grid-template-columns: repeat(2, 1fr);
        max-width: calc((100% - 2rem) * 2 / 3 + 2rem);
        margin: 0 auto;
    }

    Block 2 — Append inside the existing @media (max-width: 1024px) block (after the .navigation-cards rules already in that block):

    .dept-cards-row--top {
        grid-template-columns: repeat(2, 1fr);
    }

    .dept-cards-row--bottom {
        grid-template-columns: repeat(2, 1fr);
        max-width: 100%;
    }

    .dept-cards-row--bottom .nav-card:last-child {
        grid-column: 1 / -1;
        max-width: 500px;
        margin: 0 auto;
    }

    Block 3 — Append inside the existing @media (max-width: 768px) block (after the .navigation-cards and .nav-card rules already in that block):

    .dept-cards-row--top,
    .dept-cards-row--bottom {
        grid-template-columns: 1fr;
        max-width: 100%;
    }

    .dept-cards-row--bottom .nav-card:last-child {
        grid-column: 1;
        max-width: 100%;
        margin: 0;
    }

    The .navigation-cards CSS remains in the file as-is (no delete, no comment-out) per UI-SPEC.md note: "leave it if there is any risk of regression."
  </action>

  <verify>
    <automated>grep -c "dept-cards" styles/hero.css</automated>
    Expected: 8 or more matches (dept-cards, dept-cards-row, dept-cards-row--top, dept-cards-row--bottom appear multiple times across base + 2 media blocks).

    Visual check: open styles/hero.css and confirm:
    - .dept-cards block exists at base level with display:flex, flex-direction:column, gap:2rem, max-width:1200px
    - .dept-cards-row--top at base has grid-template-columns:repeat(3,1fr)
    - .dept-cards-row--bottom at base has max-width:calc(...) and margin:0 auto
    - At ≤1024px: .dept-cards-row--top becomes repeat(2,1fr); .dept-cards-row--bottom max-width:100%; last .nav-card gets grid-column:1/-1
    - At ≤768px: both rows become grid-template-columns:1fr; last-child overrides removed
    - No existing .navigation-cards, .nav-card, .nav-card:hover, .hs-* rules have been modified
  </verify>

  <done>
    styles/hero.css contains .dept-cards, .dept-cards-row, .dept-cards-row--top, .dept-cards-row--bottom at base level and their responsive overrides inside the existing ≤1024px and ≤768px media blocks. The existing .navigation-cards and .nav-card rules are untouched. No other CSS file is modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS only | No user input, no data flow. Pure style addition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01-01 | Tampering | hero.css existing rules | accept | Read file before editing; use append-only approach; no existing selectors touched |
</threat_model>

<verification>
1. grep -c "dept-cards" styles/hero.css returns ≥ 8
2. grep -c "navigation-cards" styles/hero.css returns same count as before (existing rules not removed)
3. grep "dept-cards-row--bottom" styles/hero.css shows the calc() centering expression at base level and max-width:100% inside ≤1024px block
4. grep "max-width: 1024px" styles/hero.css confirms rules are inside the correct media query block
</verification>

<success_criteria>
CSS additions landed in hero.css. The .dept-cards / .dept-cards-row contract is available for Plan 02 to reference. No visual regression on existing page areas (no existing selectors modified).
</success_criteria>

<output>
Create .planning/phases/93-home-rebrand-department-routing-sub-nav-integration/93-01-SUMMARY.md when done
</output>
