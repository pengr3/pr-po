---
context: phase
phase: 93-home-rebrand-department-routing-sub-nav-integration
stage: discuss-complete
task: null
total_tasks: null
status: paused
last_updated: 2026-05-25T17:37:10.979Z
---

<current_state>
Phase 93 discuss-phase is complete. CONTEXT.md (10 decisions) and DISCUSSION-LOG.md are written and committed as `00d9ae8`. The phase has not yet been planned — `/gsd-plan-phase 93` is the next workflow step. No code edits have been made.

The discussion was originally `--batch` mode but switched to default interactive mode mid-flow at the user's request (after Area 1). All 4 selected gray areas were covered: department identity, sub-nav fate, tile layout, click behavior.
</current_state>

<completed_work>
Completed Tasks:
- Area 1: 5th department identity — Done (Clients selected; tiles mirror top-nav 1:1; everybody sees everything; Management stays in top nav)
- Area 2: Sub-nav fate — Done (Phase 87.1 sub-nav retained verbatim; Overview tab restructured to host tiles + stats card; Management NOT added to sub-nav)
- Area 3: Tile layout & visual treatment — Done (3-on-top + 2-centered-below grid; match current .nav-card style verbatim; descriptions revised per current functionality)
- Area 4: Per-role visibility & click behavior — Done (existing router.js route protection handles denied clicks; tile click routes to top-level department landing 1:1 with top-nav)
- 93-CONTEXT.md + 93-DISCUSSION-LOG.md written and committed (`00d9ae8`)
</completed_work>

<remaining_work>

- Run `/gsd-plan-phase 93` to break Phase 93 into executable plans
- Optionally run `/gsd-ui-phase 93` first (UI hint: yes) for a design contract before planning
- After plan: `/gsd-execute-phase 93`
- Carry-over from Phase 87.4 (separate concern): merge v3.3 → main and run `firebase deploy --only firestore:rules` for the Phase 87.4 D-03/D-04 attachment-presence server gate
</remaining_work>

<decisions_made>

- 5 tiles = Clients, Projects, Services, Procurement, Finance (1:1 with top-nav links)
- All tiles visible to every role; permission enforced at the click destination by existing router.js
- Phase 87.1 sub-nav (Overview | Engagements | Proposals) retained as-is — Management stays in top nav (NOT a tile, NOT in sub-nav)
- Overview sub-tab restructured: 5 tiles on top, existing Procurement stats card below
- Tile visual = current .nav-card style (icon + title + description + Enter button)
- Tile layout = 3-on-top + 2-centered-below; mobile reflow falls out naturally
- Tile descriptions revised to reflect current functionality post v3.2 + v4.0
- Tile click routes to top-level department landing only (no sub-action quick links this phase)
- Pure UI rebrand — no firestore.rules edits, no new collection, no new view-module
</decisions_made>

<blockers>
- None for Phase 93. (Pre-existing Phase 87.4 follow-up: prod firestore.rules deploy pending merge of v3.3 to main — does not block planning of Phase 93.)
</blockers>

## Required Reading (in order)
1. `.planning/phases/93-home-rebrand-department-routing-sub-nav-integration/93-CONTEXT.md` — locked decisions (D-01..D-10) downstream agents must respect
2. `.planning/phases/93-home-rebrand-department-routing-sub-nav-integration/93-DISCUSSION-LOG.md` — alternatives considered (audit trail; not required for planning agents but useful for sanity-checks)
3. `.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-CONTEXT.md` — sub-nav structure being preserved (D-01, D-04, D-07, D-08)
4. `.planning/phases/91-navigation-restructuring-mrf-into-procurement-my-requests-fi/91-CONTEXT.md` — why the existing Material Request nav-card is dead (legacy redirect)
5. `app/views/home.js` — primary edit target (~650 lines; preserve all Phase 87.1 sub-nav infrastructure)
6. `styles/hero.css` — current `.nav-card` / `.navigation-cards` styles; primary CSS target for the 3+2 grid
7. `.planning/ROADMAP.md` §"Phase 93" — roadmap framing and original open-questions list

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Importing proposals.js renderApprovalQueue from home.js | Phase 87.1 RESEARCH Pitfall 7 — depends on proposalsData module state home.js does not maintain | advisory | home.js owns its local queue via `_renderHomeApprovalQueueHtml` + `_openHomeQueueModal` + `_homeQueueConfirmAction`. Do not import the proposals.js queue renderer. |
| Modifying getHomeSubTabConfig role gating | Phase 87.1 D-08 model is locked; Phase 93 explicitly retains it | advisory | Treat `getHomeSubTabConfig()` as read-only for this phase. |
| Adding Management to home (tile or sub-nav) | Explicitly out of scope per D-06; user said "remove management" from sub-nav discussion | advisory | Management stays in top nav unchanged. No new home-page Management surface. |
| Per-role tile visibility filtering | Locked: every role sees all 5 tiles (D-02) | advisory | Do not introduce a tile-visibility helper. Role-gating is at the click destination only. |
| Soft-disabling tiles for roles without access | Locked: existing router.js protection handles denied clicks (D-10) | advisory | Do not add greyed-out / lock-icon state to tiles. |

## Infrastructure State

- Git branch: `v3.3`
- Last commit on branch: `00d9ae8 docs(93): capture phase context for home page rebrand`
- Pre-existing uncommitted files (NOT part of this session): `.planning/ROADMAP.md` (modified), `.planning/STATE.md` (modified), `.planning/debug/86.9-row-misalign-DEBUG.md` (untracked), `.planning/phases/86.9-gantt-curtain-divider-pdf-export/86.9-03-PLAN.md` (untracked), `debug-diag-86.9.js` (untracked), `.claude/worktrees/` (untracked)
- No running background processes
- No environment-specific setup required for Phase 93 planning

## Pre-Execution Critique Required

N/A — no implementation in progress. Phase 93 is paused between discuss-phase (complete) and plan-phase (not started).

<context>
The discuss-phase resolved every selected gray area. Phase 93 is a focused UI rebrand of `app/views/home.js` plus CSS work in `styles/hero.css`. There is no schema change, no Firestore rules change, no new view module, no new route. The sub-nav infrastructure from Phase 87.1 must be preserved verbatim (Engagements, Proposals tabs, role gating, queue helpers). The only structural change inside the Overview sub-tab is the addition of the 5-tile department grid above the existing Procurement stats card.

The user iteratively shaped the answers — they switched the discussion mode from `--batch` to interactive mid-flow, and used a clarification ("retain tiles in overview. Tiles first then stats") to redirect a layout question. The CONTEXT.md captures the final resolved positions; the DISCUSSION-LOG.md preserves the alternatives offered at each step.

The user explicitly asked for `/gsd-pause-work` followed by `git push` after CONTEXT.md was written. The push happens AFTER this handoff commit lands.
</context>

<next_action>
Start with: `/gsd-plan-phase 93` (or `/gsd-ui-phase 93` first if you want a UI design contract before planning — phase has UI hint: yes). Read `93-CONTEXT.md` first to load the 10 locked decisions before doing anything else.
</next_action>
