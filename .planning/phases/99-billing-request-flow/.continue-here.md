---
context: phase
phase: 99-billing-request-flow
task: 8
total_tasks: 8
status: executed_awaiting_uat
last_updated: 2026-06-04T16:16:52Z
---

# Phase 99: Billing Request Flow — Paused (EXECUTED, awaiting UAT)

_No `blocking` constraints were discovered through failure this session — section omitted intentionally.
The two `human_action` blockers below gate UAT, not code resumption._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| gsd-sdk assumed available | This machine has NO `gsd-sdk` (only legacy gsd-tools.cjs). Workflows that call `gsd-sdk query ...` silently do nothing. | advisory | Run all state/commit/verify steps INLINE; do not rely on `gsd-sdk` commands. |
| worktree/subagent execution | 2 stale LOCKED agent worktrees (`agent-a4222795...`, `agent-ab5541...`) make `isolation="worktree"` / parallel-subagent dispatch unsafe. | advisory | Execute inline sequentially on the working tree (matches Phase 98/91.3). |
| forgetting the rules deploy | The `billing_requests` rules block is committed but NOT deployed → every write hits `permission-denied` in the browser. | advisory | Deploy to dev FIRST (`firebase deploy --only firestore:rules --project dev`) before any UAT. |

<current_state>
Phase 99 is **fully executed and committed** on branch `v3.3`. All 8 tasks across 3 plans (2 waves)
are done with atomic commits; all 3 SUMMARYs written; VERIFICATION.md verdict = `human_needed`;
99-HUMAN-UAT.md persisted with 6 pending items. ROADMAP plan checkboxes marked `[x]`; STATE +
frontmatter updated to "EXECUTED — awaiting UAT". **The phase is NOT marked complete.** No coding
work remains — the only remaining work is the human browser UAT (after a rules deploy) and then
marking the phase complete.

HEAD at pause: `e687432`. Phase 99 commit range: `0064849`..`e687432` (14 commits).
</current_state>

<completed_work>

All 8 implementation tasks DONE (atomic commits):
- 99-01 (Wave 1, firestore.rules + app/notifications.js)
  - T1: `billing_requests` Security Rules block (create=isActiveUser; update/delete=finance) — `0064849`
  - T2: `BILLING_REQUEST_SUBMITTED` / `BILLING_REQUEST_DECIDED` enum + TYPE_META — `de68d76`
  - SUMMARY — `adb6de0`
- 99-02 (Wave 2, app/views/project-detail.js)
  - T1: footer link (unconditional) + own-requests onSnapshot + status list — `52483ad`
  - T2: billing-request modal (tranche → 3 auto-hinted pills → doc-link fields → notes → validation) — `2fc481b`
  - T3: `submitBillingRequest` frozen D-04 doc + amount math + Finance fan-out + window wiring — `9efc6f5`
  - SUMMARY — `7a69fdb`
- 99-03 (Wave 2, app/views/finance.js)
  - T1: pending-requests listener + collapsible "Pending Billing Requests" banner — `2fe9bea`
  - T2: approve bridge (preselectKey 3rd `:TRANCHE_INDEX` segment + D-11 already-billed hint + mark approved) — `1a9800d`
  - T3: reject (required reason) + `_notifyBillingDecision` to submitter + window wiring — `9d54e17`
  - SUMMARY — `cb68feb`
- Verification + 99-HUMAN-UAT.md — `d506d79`
- ROADMAP/STATE tracking (plans `[x]`, phase awaiting UAT) — `e687432`

Automated checks ALL PASS: `node --check` on all 3 JS modules; `firestore.rules` braces 61/61;
cross-plan contract intact; every per-task `<acceptance_criteria>` verified during execution.
</completed_work>

<remaining_work>

NO code remains. Remaining = human + tracking:
1. **Deploy rules to dev** (blocking for UAT): `firebase deploy --only firestore:rules --project dev`
2. **Run the 6 browser-UAT items** in `99-HUMAN-UAT.md`.
3. **If all 6 pass** → mark Phase 99 complete in `.planning/ROADMAP.md` (phase header/progress) +
   `.planning/STATE.md` (status → complete, advance counters: completed_phases 29→30), and flip
   `99-HUMAN-UAT.md` frontmatter `status: partial` → `complete`.
4. **If any fail** → `/gsd-plan-phase 99 --gaps` (reads VERIFICATION.md → gap plans → `/gsd-execute-phase 99 --gaps-only`).
</remaining_work>

<decisions_made>

- Inline sequential execution on v3.3 (no subagents/worktrees) — gsd-sdk unavailable + stale locked worktrees; matches Phase 98/91.3.
- Own-requests listener scoped to `currentProject.project_code` (not the URL `projectCode` param) via idempotent `ensureBillingRequestsListener()` — correct for the Phase-78 doc-id fallback; plan-sanctioned alternative.
- Pills inlined as literal `_selectBillingType('progress'|'completion'|'other')` calls (style-only helper) to satisfy the acceptance contract and keep intent grep-able.
- Verification done inline (verdict `human_needed`); `gsd-code-review` skill skipped (gsd-sdk-dependent, advisory) — replaced by inline goal-backward + cross-plan contract review.
- Approve marks-approved separately from collectible creation (D-12); advisory amount re-derived by Finance at approval (T-99-01/05/09 accepted).
</decisions_made>

<blockers>
- Rules not yet deployed → `billing_requests` reads/writes will `permission-denied` in the browser until `firebase deploy --only firestore:rules --project dev` runs. (human_action, blocking for UAT only.)
</blockers>

## Required Reading (in order)
1. `.planning/phases/99-billing-request-flow/99-VERIFICATION.md` — verdict + per-criterion evidence table.
2. `.planning/phases/99-billing-request-flow/99-HUMAN-UAT.md` — the 6 items to run in-browser.
3. `.planning/phases/99-billing-request-flow/99-CONTEXT.md` — locked decisions D-01..D-21 (if any UAT defect needs root-causing).
4. `.planning/phases/99-billing-request-flow/99-0{1,2,3}-SUMMARY.md` — what each plan shipped + the 2 plan-sanctioned adjustments in 99-02.

## Critical Anti-Patterns (do NOT repeat these)
- DO NOT mark Phase 99 complete until the 6 UAT items pass — verification is `human_needed`, not `passed`.
- DO NOT run UAT before the rules deploy — you'll get spurious permission-denied failures and misdiagnose them.
- DO NOT re-run via subagents/worktrees on this machine — stale locked worktrees + no gsd-sdk.

## Infrastructure State
- Branch: `v3.3` (HEAD `e687432`). Working tree clean except pre-existing untracked items (`.claude/worktrees/`, `debug-diag-86.9.js`, `.planning/debug/86.9-row-misalign-DEBUG.md`, `.planning/phases/86.9-.../86.9-03-PLAN.md`, deleted `HANDOFF.json` — now re-created by this pause).
- Firestore rules: `billing_requests` block committed, NOT deployed (dev or prod). Same `firestore.rules` also carries the still-undeployed Phase 87.4 attachment-gate change → both ship together on the next deploy.
- No running servers/watchers from this session.

<context>
Phase 99 was a clean, well-specified 3-plan phase (plan-checker had PASSED with 0 blockers). Execution
was smooth — the only friction was mis-calibrated verification regexes I wrote on the fly (JS `[^]]*`
gotcha + too-tight windows); the CODE was correct each time, re-verified with corrected assertions.
Two scoped adjustments (listener scope, literal pills) were within plan guidance, documented in the
99-02 SUMMARY. The hardest correctness question — Finance banner listener lifecycle — resolves cleanly
because it mirrors the proven Phase-85 collectibles-listener pattern (same `listeners[]` teardown,
same null-guarded render, attached once in `initCollectiblesTab`).
</context>

<next_action>
Start with: `firebase deploy --only firestore:rules --project dev`, then open the app as an
operations_user on a project that has collection_tranches + a positive contract_cost and walk
`99-HUMAN-UAT.md` items 1→6. Reply "approved" to mark complete, or report failures for gap closure.
</next_action>
