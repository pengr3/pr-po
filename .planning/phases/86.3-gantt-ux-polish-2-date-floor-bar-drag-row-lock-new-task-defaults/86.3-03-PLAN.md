---
phase: 86.3-gantt-ux-polish-2-date-floor-bar-drag-row-lock-new-task-defaults
plan: 03
type: execute
wave: 3
depends_on: [86.3-01, 86.3-02]
files_modified:
  - app/views/project-plan.js
  - styles/views.css
autonomous: false  # has UAT checkpoint
requirements: []  # INSERTED phase — Phase 86.3 covers 9 UX defects with no REQ-ID mapping
must_haves:
  truths:
    - "A newly added task in the inline grid renders immediately as a 1-day Gantt bar without requiring the user to type a Duration first"
    - "New task start_date is anchored to the start_date of the row immediately above; falls back to today if no row above OR the row above has no start_date"
    - "End_date = start_date (1 day inclusive — matches Phase 86.1 D-03 addDays(start, days-1) convention with days=1)"
    - "No stray scrollbar appears in the middle of the project plan view — only the intended scroll containers (timeline horizontal, grid vertical, gantt-pane horizontal) show scrollbars"
    - "Existing Phase 86.2 sortedForInherit pattern is reused (no double-sort, no new array allocation)"
    - "All 86.2 verified UAT items remain green (parent inheritance via sortedForInherit, # cell drag, resizable panel divider)"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "handleNewRowCommit writes start_date and end_date with anchor-on-row-above + fallback-to-today"
      contains: "rowAbove?.start_date || formatDateISO(new Date())"
    - path: "styles/views.css"
      provides: "stray-scrollbar root cause patched (specific overflow rule on the diagnosed wrapper)"
  key_links:
    - from: "handleNewRowCommit (project-plan.js line 565+)"
      to: "Firestore project_tasks setDoc with start_date + end_date"
      via: "anchorStart = rowAbove?.start_date || today; newEndDate = anchorStart"
      pattern: "anchorStart"
    - from: "next snapshot → renderTaskGrid + renderGantt"
      to: "1-day Gantt bar rendered immediately on the new row"
      via: "frappeTasks now includes start/end for the new task; appendNode no longer skips it"
      pattern: "if \\(!start \\|\\| !end\\) return"
---

<objective>
Two concerns in this plan:

(1) **D-04 / D-05 — New-task default 1-day duration anchored on row above (CONTEXT-locked).** Extend `handleNewRowCommit` to write `start_date` and `end_date` at insert time. Anchor: start_date of the row immediately above (using the `sortedForInherit[last]` pattern already computed for parent inheritance — no second sort). Fallback: today (`formatDateISO(new Date())`) when (a) tasks list is empty (first row in project) OR (b) row above has no `start_date`. End_date = start_date (1 day inclusive, days=1 → end = start). Result: new bar renders immediately on the next snapshot without requiring Duration entry.

(2) **D-10 — Stray middle-of-screen scrollbar (CONTEXT explicitly says "diagnose first").** Use Chrome DevTools to identify which element is rendering the unintended scrollbar between the resizable divider and the Gantt pane. Then patch ONLY that element's overflow rule in `styles/views.css`. Per PATTERNS.md §D-10, the most likely candidates are `.task-grid-rail` (overflow: auto + min-width: 640px on .task-grid forces a horizontal scrollbar at the bottom of the rail when rail is narrow) or a wrapper between `.plan-divider` and `.gantt-pane`. Do NOT add a global `overflow: hidden` — that would break legitimate timeline horizontal scroll and grid vertical scroll.

Purpose: Closes phase success criteria #6 (no stray scrollbar) and #7 (new task = 1-day bar immediately). D-10 is diagnose-first; D-04/D-05 are PATTERNS-locked exact mirrors of the Phase 86.2 sortedForInherit pattern.

Output: Modified `handleNewRowCommit` with anchor logic; targeted CSS overflow patch; manual UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86.3-gantt-ux-polish-2-date-floor-bar-drag-row-lock-new-task-defaults/86.3-CONTEXT.md
@.planning/phases/86.3-gantt-ux-polish-2-date-floor-bar-drag-row-lock-new-task-defaults/86.3-PATTERNS.md
@.planning/phases/86.2-gantt-ux-polish-row-alignment-drag-indent-panels/86.2-03-PLAN.md
@app/views/project-plan.js
@styles/views.css

<interfaces>
<!-- Existing sortedForInherit pattern (Phase 86.2 DEFECT-6) — REUSE, do not re-sort: -->
```javascript
// project-plan.js:565-567
const sortedForInherit = tasks.slice().sort((a, b) => (a.row_order ?? Infinity) - (b.row_order ?? Infinity));
const inheritedParentId = sortedForInherit[sortedForInherit.length - 1]?.parent_task_id ?? null;
```

<!-- Existing 1-day-inclusive convention (project-plan.js:484): -->
```javascript
const endDate = addDays(effectiveStart, days - 1); // 1d => same day
```

<!-- formatDateISO helper (project-plan.js:1130-1137 region): -->
```javascript
function formatDateISO(d) { /* returns 'YYYY-MM-DD' */ }
```

<!-- Plan-view CSS (styles/views.css:2375-2602 — read this region during diagnosis): -->
<!-- Key candidates from PATTERNS.md §D-10: -->
<!-- - .task-grid-rail { overflow: auto } + .task-grid { min-width: 640px } -->
<!-- - .gantt-pane { overflow: auto } -->
<!-- - .plan-view-surface { height: calc(100vh - 64px) } -->
<!-- - .plan-split-pane (CSS Grid 35% / 4px / 1fr) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-04/D-05 — Anchor new-task start_date on row above; default end_date = start_date</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    - app/views/project-plan.js (lines 541–596 for handleNewRowCommit; lines 478–488 for the existing 1-day-inclusive convention; line 1130 region for formatDateISO)
    - .planning/phases/86.3-gantt-ux-polish-2-date-floor-bar-drag-row-lock-new-task-defaults/86.3-CONTEXT.md (D-04, D-05 — locked decisions)
    - .planning/phases/86.3-gantt-ux-polish-2-date-floor-bar-drag-row-lock-new-task-defaults/86.3-PATTERNS.md (§D-04, §D-05 — exact mirror of sortedForInherit pattern)
  </read_first>
  <action>
At `app/views/project-plan.js`, modify `handleNewRowCommit` (around lines 565–584).

Step A — RIGHT AFTER line 567 (`const inheritedParentId = sortedForInherit[...].parent_task_id ?? null;`), ADD these two new lines:

```javascript
        // Phase 86.3 D-05: anchor new task's start_date on the row immediately above (reuse sortedForInherit, no re-sort).
        // Fallback to today if (a) tasks is empty OR (b) row above has no start_date. Phase 86.3 D-04: 1-day inclusive → end = start.
        const rowAbove = sortedForInherit[sortedForInherit.length - 1];
        const anchorStart = rowAbove?.start_date || formatDateISO(new Date());
```

Step B — In the `docData` object (lines 569–584), ADD `start_date` and `end_date` keys. The current docData object has these keys in this order:
- task_id, project_id, project_code, parent_task_id, name, description, progress, is_milestone, dependencies, assignees, resources, row_order, created_at, updated_at

ADD `start_date` and `end_date` directly after `name` (before `description`). The modified block:

OLD:
```javascript
        const docData = {
            task_id: taskId,
            project_id: currentProject.id,
            project_code: currentProject.project_code,
            parent_task_id: inheritedParentId,
            name,
            description: '',
            progress: 0,
            ...
        };
```

NEW:
```javascript
        const docData = {
            task_id: taskId,
            project_id: currentProject.id,
            project_code: currentProject.project_code,
            parent_task_id: inheritedParentId,
            name,
            // Phase 86.3 D-04/D-05: default 1-day bar anchored on row above (or today)
            start_date: anchorStart,
            end_date: anchorStart, // 1-day inclusive: addDays(anchorStart, 1-1) === anchorStart
            description: '',
            progress: 0,
            ...
        };
```

(Keep all other docData keys unchanged — dependencies, assignees, resources, row_order, created_at, updated_at remain in their current order.)

Do NOT modify:
- The `sortedForInherit` line (line 566) — same array is reused.
- `inheritedParentId` (line 567) — unchanged.
- `row_order: newRowOrder` (line 581) — unchanged; D-06 is owned by Plan 01.
- `setDoc(doc(db, 'project_tasks', taskId), docData)` — same call, the new fields ride along atomically.
- `handleGridCellBlur` (lines 467–539) — the duration branch's existing addDays logic is untouched.

Edge case behaviors (verify these manually after the change):
- First task in empty project: `sortedForInherit` is empty → `rowAbove` is `undefined` → `rowAbove?.start_date` is `undefined` (falsy) → fallback to `formatDateISO(new Date())` (today).
- Row above has no start_date (legacy task created pre-86.3): `rowAbove?.start_date` is `undefined` → fallback to today.
- Row above has start_date: new task gets the SAME start_date and end_date (overlapping 1-day bar — that's intentional, the user can drag or set Duration to extend).
  </action>
  <verify>
    <automated>grep -n "anchorStart" app/views/project-plan.js</automated>
    Expected: at least 3 matches — declaration, start_date assignment, end_date assignment, all within handleNewRowCommit.
    
    Additional checks:
    `grep -n "rowAbove?.start_date" app/views/project-plan.js` returns 1 match.
    `grep -n "Phase 86.3 D-05" app/views/project-plan.js` returns ≥ 1 match.
    `grep -nE "start_date: anchorStart" app/views/project-plan.js` returns 1 match.
    `grep -nE "end_date: anchorStart" app/views/project-plan.js` returns 1 match.
  </verify>
  <acceptance_criteria>
    - `grep -c "rowAbove?.start_date \|\| formatDateISO" app/views/project-plan.js` returns ≥ 1.
    - `grep -c "start_date: anchorStart" app/views/project-plan.js` returns 1.
    - `grep -c "end_date: anchorStart" app/views/project-plan.js` returns 1.
    - The new lines reuse the existing `sortedForInherit` array (same line 566 reference) — no second sort introduced. Verify by `grep -c "tasks.slice().sort" app/views/project-plan.js` returns its prior value (no new occurrences).
    - The order of inserted keys matches the spec: `start_date` and `end_date` appear directly after `name` in the docData literal.
  </acceptance_criteria>
  <done>handleNewRowCommit writes start_date + end_date; reuses sortedForInherit; falls back to today on empty tasks or missing rowAbove.start_date.</done>
</task>

<task type="auto">
  <name>Task 2: D-10 — Diagnose stray scrollbar; patch the specific overflow rule</name>
  <files>styles/views.css</files>
  <read_first>
    - styles/views.css (lines 2375–2610 — full plan-view CSS block; read all of it, the diagnosis is which selector to patch)
    - .planning/phases/86.3-gantt-ux-polish-2-date-floor-bar-drag-row-lock-new-task-defaults/86.3-CONTEXT.md (D-10 — locked: "diagnose first; investigate and fix at source")
    - .planning/phases/86.3-gantt-ux-polish-2-date-floor-bar-drag-row-lock-new-task-defaults/86.3-PATTERNS.md (§D-10 — explains likely candidates, scope of safe fixes)
    - .planning/phases/86.2-gantt-ux-polish-row-alignment-drag-indent-panels/86.2-03-PLAN.md (Phase 86.2 panel divider — must not regress)
  </read_first>
  <action>
**Step 1 — DIAGNOSE (do this BEFORE any CSS edit; record findings in the SUMMARY.md):**

1. Run `python -m http.server 8000` and open `http://localhost:8000/#/projects` in Chrome.
2. Open a project in the plan view.
3. Open Chrome DevTools → Elements panel.
4. Locate the stray middle-of-screen scrollbar visually. Identify:
   - Which axis: horizontal or vertical?
   - Approximate screen position relative to the divider (left of divider = inside `.task-grid-rail`; right of divider = inside `.gantt-pane` or a wrapper)?
5. In DevTools, click the scrollbar's host element to select it. Read its computed `overflow-x` and `overflow-y` properties.
6. Confirm the LEGITIMATE scrollbars are still present:
   - `.task-grid-rail` may show vertical scrollbar when there are many tasks (legitimate — preserve).
   - `.gantt-pane` shows horizontal scrollbar when timeline is wider than pane (legitimate — preserve).
   - Per PATTERNS.md, the stray bar is most likely either:
     - **Candidate A:** `.task-grid-rail { overflow: auto }` combined with `.task-grid { min-width: 640px }` causes a horizontal scrollbar at the BOTTOM of the rail when the rail is narrower than 640px (default 35% × 1440px ≈ 504px). This visually appears just left of the divider.
     - **Candidate B:** A wrapper element (e.g., between `.plan-split-pane` and `.gantt-pane`) inadvertently has overflow set.
     - **Candidate C:** `.plan-view-surface { height: calc(100vh - 64px) }` produces overflow on a container if descendant heights exceed it.

**Step 2 — PATCH the diagnosed selector ONLY (CSS edit):**

Apply the smallest possible fix. Examples (the executor must pick the one that matches the diagnosis):

- **If Candidate A is confirmed:** Either reduce `.task-grid` `min-width: 640px` (e.g., to a value that fits at 35% rail width on a 1024px viewport: `min-width: 360px`) OR change `.task-grid-rail` overflow-x to `hidden` while keeping `overflow-y: auto`. Recommended: 
  ```css
  .task-grid-rail {
      overflow-x: hidden;   /* Phase 86.3 D-10: kill stray horizontal scrollbar at bottom of rail */
      overflow-y: auto;     /* Vertical scroll preserved for many-tasks projects */
      background: #ffffff;
      border-right: 1px solid var(--gray-200, #dadce0);
  }
  ```
  AND tighten `.task-grid { min-width: ... }` if necessary so columns still fit at narrower rail widths. PATTERNS.md notes 360px as a safe lower bound.

- **If Candidate B is confirmed:** Add `overflow: hidden` to ONLY the offending wrapper. Document the wrapper selector in the SUMMARY.

- **If Candidate C is confirmed:** Adjust the height calc on the offending container.

**Critical constraints (regardless of which candidate is the cause):**
- Do NOT add `overflow: hidden` to `.gantt-pane` — that breaks the legitimate timeline horizontal scroll (Phase 86 success criterion).
- Do NOT add `overflow: hidden` to `.plan-split-pane` or `.plan-view-surface` — would kill all scrolling.
- Do NOT modify `.plan-divider` — Phase 86.2 Plan 03 already verified the divider rendering.

**Step 3 — Document the diagnosis in the eventual 86.3-03-SUMMARY.md:**
Record (a) which DOM element was rendering the stray bar, (b) which axis, (c) which CSS rule was patched, (d) the exact before/after CSS. The orchestrator/checker will read this when verifying.
  </action>
  <verify>
    <automated>grep -nE "overflow-x|overflow-y|overflow: " styles/views.css | head -50</automated>
    Manual: confirm the diagnosed wrapper's overflow rule was added/changed and no other selector was touched. After running the dev server, the stray bar must be gone while legitimate timeline + grid scrollbars still work.
    
    Additional check: `grep -c "Phase 86.3 D-10" styles/views.css` returns ≥ 1 (the patch must include a comment marking the change).
  </verify>
  <acceptance_criteria>
    - The diagnosed selector and patched CSS rule are documented in the SUMMARY.
    - Legitimate scrollbars work: `.gantt-pane` shows horizontal scroll on wide timelines (test by zooming to Day mode); `.task-grid-rail` shows vertical scroll on many tasks.
    - Stray scrollbar is no longer visible mid-screen.
    - `git diff styles/views.css` shows changes only inside the plan-view block (lines ~2375–2610).
    - No `overflow: hidden` was added globally (`.plan-split-pane`, `.plan-view-surface`, `.gantt-pane`).
  </acceptance_criteria>
  <done>Stray scrollbar diagnosed and removed via targeted CSS patch; legitimate scrollbars preserved; diagnosis recorded in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — new task default 1d bar + no stray scrollbar</name>
  <what-built>
- D-04/D-05: New tasks now write start_date + end_date (1-day inclusive bar) anchored on the row above (or today for empty/legacy projects).
- D-10: Stray middle-of-screen scrollbar removed via targeted CSS patch on the diagnosed selector.
  </what-built>
  <how-to-verify>
1. Start the dev server: `python -m http.server 8000`
2. Open `http://localhost:8000/#/projects` in Chrome.
3. **Test D-04/D-05 (empty project fallback to today):**
   - Open or create a project with NO tasks.
   - Type a name in the empty trailing row and press Enter.
   - **Expected:** A 1-day Gantt bar appears on the new row, anchored at TODAY's date (you should see it under the today line / today pill).
   - **Fail signal:** No bar visible in the Gantt pane (means start_date / end_date didn't write).
4. **Test D-04/D-05 (anchor on row above):**
   - In the same project, click an existing task's Start cell and set it to a past date (e.g., 2026-04-15). Press Tab to commit.
   - In the empty trailing row, type a name and press Enter.
   - **Expected:** The new task's bar appears at 2026-04-15 (1-day bar), anchored on the row above's start_date — NOT today.
5. **Test D-04/D-05 (legacy task above without start_date):**
   - If you have a task created before this fix that lacks dates, set it as the last sorted row and add a new task below.
   - **Expected:** Fallback to today (since rowAbove.start_date is undefined).
6. **Test D-04 (1-day inclusive bar):**
   - Inspect the new task's End column in the grid. It should equal the Start column (same date).
   - The Gantt bar should be exactly 1 day wide.
7. **Test D-10 (no stray scrollbar):**
   - In the plan view, scan the entire viewport. Confirm:
     - NO scrollbar appears in the middle of the screen between the divider and the Gantt pane.
     - NO scrollbar appears at the bottom of the left rail (the candidate-A spot).
   - **Test legitimate scrollbars preserved:**
     - Scroll the Gantt timeline horizontally — should work smoothly.
     - Add many tasks (or open a project with many) — the left rail should scroll vertically.
     - Resize the divider via drag (Phase 86.2) — should still work, no new scrollbars appear.
8. **Regression — Duration column still works:**
   - Pick a new task. Click its Duration cell, type "5d", press Enter.
   - **Expected:** End date recomputes to start + 4 days; Gantt bar extends to 5 days.
   - This confirms `handleGridCellBlur` duration branch still fires; the new docData fields don't conflict.

Type "approved" if all eight tests pass. Otherwise describe which test failed and what you observed.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <acceptance_criteria>
    - Test 3 (empty project): 1-day bar at today.
    - Test 4 (anchor on row above): 1-day bar at row-above's start_date.
    - Test 5 (legacy fallback): 1-day bar at today.
    - Test 6 (1-day inclusive): Start === End.
    - Test 7 (no stray scrollbar): viewport clean; legitimate scrollbars work.
    - Test 8 (Duration regression): Duration commit still works.
    - User responded "approved".
  </acceptance_criteria>
  <done>User confirms eight tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Firestore (project_tasks setDoc) | New fields start_date and end_date added to the new-task docData literal. Existing Security Rules already permit these fields (project_tasks already accepts start_date / end_date writes — verified by the Phase 86.1 inline grid Start/End columns and Phase 86 D-19 base rules). No rules change needed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.3.03-01 | Tampering | handleNewRowCommit docData | accept | start_date and end_date are derived from in-memory tasks[] (which is already source-controlled by the Firestore project_tasks listener with WHERE project_id == currentProject.id). User cannot inject foreign data via this path. |
| T-86.3.03-02 | Information disclosure | rowAbove leak | accept | rowAbove is read from the user's already-authorized tasks[] array — no new data exposure. |
| T-86.3.03-03 | DoS | sortedForInherit reuse | accept | Reuses the existing sort (line 566); no additional O(n log n) cost. |
| T-86.3.03-04 | Tampering | CSS overflow patch | accept | Pure visual change. No data write paths or auth-relevant code paths are modified. |
</threat_model>

<verification>
- `grep -c "rowAbove?.start_date \|\| formatDateISO" app/views/project-plan.js` returns ≥ 1
- `grep -c "start_date: anchorStart" app/views/project-plan.js` returns 1
- `grep -c "end_date: anchorStart" app/views/project-plan.js` returns 1
- `grep -c "Phase 86.3 D-10" styles/views.css` returns ≥ 1
- All eight UAT subtests in Task 3 pass.
</verification>

<success_criteria>
- Phase success criterion #6 (no stray scrollbar) covered.
- Phase success criterion #7 (newly added task = 1-day bar immediately) covered.
- Phase 86.2 panel divider rendering NOT regressed.
- Phase 86.2 sortedForInherit pattern reused (no double-sort).
</success_criteria>

<output>
After completion, create `.planning/phases/86.3-gantt-ux-polish-2-date-floor-bar-drag-row-lock-new-task-defaults/86.3-03-SUMMARY.md`.
The SUMMARY must include the diagnosis details for D-10 (which DOM element, which axis, which CSS rule patched, before/after CSS).
</output>
