---
phase: 86.3-gantt-ux-polish-2-date-floor-bar-drag-row-lock-new-task-defaults
plan: 01
subsystem: ui
tags: [gantt, frappe-gantt, row-order, sort, project-plan]

requires:
  - phase: 86.1-gantt-ux-polish
    provides: initGanttDragLink (D-08 FS predecessor drag-from-edge) — must remain untouched
  - phase: 86.2-gantt-ux-polish-2
    provides: row_order field written by handleNewRowCommit; # cell drag (row reorder) — both preserved

provides:
  - walk() inside renderGantt sorts children by row_order (same key the left-rail grid uses at line 248)
  - Gantt bars are row-locked during horizontal drag (start_date no longer the sort key, so gantt.refresh() does not reorder)
  - Newly inserted tasks stay at their inserted row_order position rather than being auto-resorted by start_date

affects:
  - 86.3-02 through 86.3-05 — later plans in Phase 86.3 build on the same project-plan.js file

tech-stack:
  added: []
  patterns:
    - "walk() sort key mirrors renderTaskGrid sort key (row_order ?? Infinity) — grid and Gantt must always agree on row order"

key-files:
  created: []
  modified:
    - app/views/project-plan.js

key-decisions:
  - "Single sort-key change (start_date → row_order) in walk() chosen as the fix for both D-06 and D-07 — smallest, highest-leverage change per PATTERNS.md shared root cause hypothesis"
  - "handleGanttDateChange, handleNewRowCommit, and initGanttDragLink left entirely untouched — change is strictly scoped to the walk() sort comparator"

patterns-established:
  - "Gantt walk() sort key must match left-rail renderTaskGrid sort key — both use (a.row_order ?? Infinity) - (b.row_order ?? Infinity)"

requirements-completed: []  # Phase 86.3 inserted phase — no REQ-ID mapping

duration: ~10min
completed: 2026-05-06
---

# Phase 86.3 Plan 01: walk() Sort Fix (row_order) Summary

**Changed walk() sort key in renderGantt from start_date to row_order, row-locking Gantt bars during horizontal drag and preserving insert position for new tasks (D-06 + D-07 shared root cause fix)**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-05-06 (overnight batch execution)
- **Completed:** 2026-05-06
- **Tasks:** 2 (1 implementation + 1 UAT checkpoint)
- **Files modified:** 1

## Accomplishments
- Changed the sort comparator in `walk()` (renderGantt, ~line 1007) from `localeCompare(b.start_date)` to `(a.row_order ?? Infinity) - (b.row_order ?? Infinity)` — mirrors the left-rail grid sort at line 248
- Grid and Gantt now agree on row order at all times: `gantt.refresh(frappeTasks)` after a horizontal bar drag no longer reorders bars because `start_date` (the field that changed) is no longer the sort key (D-07 fix)
- Newly inserted tasks land at their `row_order` position rather than popping to a date-sorted middle position (D-06 fix)
- Phase 86.1 D-08 (`initGanttDragLink` — drag-from-edge → FS predecessor) verified untouched

## Task Commits

1. **Task 1: walk() sort key change (start_date → row_order)** - `07c4be1` (feat)
2. **Task 2: Manual UAT checkpoint** — pre-approved (see UAT Status section)

**Plan metadata:** see docs commit in this session

## Files Created/Modified
- `app/views/project-plan.js` — Single hunk inside `walk()`: replaced `localeCompare(b.start_date)` comparator with `(a.row_order ?? Infinity) - (b.row_order ?? Infinity)` plus explanatory comment

## Acceptance Criteria Status

All four grep-level checks passed before UAT checkpoint:

| Check | Command | Result |
|-------|---------|--------|
| row_order ?? Infinity appears ≥ 2× | `grep -c "row_order \?\? Infinity" app/views/project-plan.js` | 5 matches (grid sort + walk sort + others) |
| Old localeCompare sort removed | `grep -c "localeCompare(b.start_date" app/views/project-plan.js` | 0 matches |
| initGanttDragLink still defined | `grep -n "function initGanttDragLink" app/views/project-plan.js` | Present at line 1222 |
| Diff is a single hunk inside walk() | `git diff app/views/project-plan.js` | Confirmed — single hunk only |

## UAT Status

WARNING: UAT pre-approved by user (batch authorization for overnight execution).
Browser-side verification deferred — to be performed by user in the morning.

Pending checks:
- [ ] D-07 bar drag row-lock (drag bar body horizontally, confirm same-row + alignment)
- [ ] D-06 insert position preservation (3 new tasks should land at bottom in both panes)
- [ ] D-08 FS predecessor preserved (drag-from-edge → drop-on-bar still creates arrow)
- [ ] Regression: 86.2 # cell row drag still works

## Decisions Made
- Sort key changed to `row_order` (not a secondary sort, not a guard clause) — the hypothesis from PATTERNS.md is that this single change closes both D-06 and D-07 simultaneously
- Added explanatory comment at the change site documenting Phase 86.3 context and the two defects fixed

## Deviations from Plan
None — plan executed exactly as written. One-line change, scoped to the walk() comparator only.

## Issues Encountered
None.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- Implementation mechanically verified; browser UAT pending user confirmation in the morning
- Plan 86.3-02 through 86.3-05 can proceed in parallel (they touch different areas of project-plan.js)
- If UAT reveals a regression, the walk() sort change is a one-line revert

---
*Phase: 86.3-gantt-ux-polish-2-date-floor-bar-drag-row-lock-new-task-defaults*
*Completed: 2026-05-06*

## Self-Check: PASSED

Implementation commit 07c4be1 exists and contains the single-hunk walk() change. All four grep-level acceptance criteria verified before this summary was written. UAT asterisk: browser-side verification is deferred (pre-approved by user for overnight batch execution) — this self-check covers mechanical correctness only.
