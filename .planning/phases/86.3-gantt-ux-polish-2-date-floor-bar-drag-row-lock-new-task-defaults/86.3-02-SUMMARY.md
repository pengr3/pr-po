---
phase: "86.3"
plan: "02"
subsystem: "gantt"
status: "complete"
tags: ["gantt", "keyboard", "enter-commit", "predecessors", "D-09"]
dependency_graph:
  requires: ["86.3-01"]
  provides: ["86.3-03"]
  affects: ["app/views/project-plan.js"]
tech_stack:
  added: []
  patterns: ["Enter-commit allowlist extension"]
key_files:
  created: []
  modified:
    - "app/views/project-plan.js"
decisions:
  - "D-09: Predecessors cell added to Enter-commit allowlist alongside Duration and Resources (Phase 86.3 locked decision)"
metrics:
  duration: "< 5 minutes"
  completed: "2026-05-07"
  tasks_completed: 2
  files_modified: 1
---

# Phase 86.3 Plan 02: Predecessors Enter Commit Summary

Add the `predecessors` column to the `_gridKeydownHandler` Enter-commit allowlist so that pressing Enter in a Predecessors cell delegates to the existing `handleGridCellBlur` path — the same mechanism used for Duration (DEFECT-10, Phase 86.2) and Resources.

## What Was Built

A single-line edit at `project-plan.js` line 460 extends the `_gridKeydownHandler` guard condition from:

```js
if (col !== 'duration' && col !== 'resources') return; // only Duration and Resources per spec
```

to:

```js
if (col !== 'duration' && col !== 'resources' && col !== 'predecessors') return; // Phase 86.3 D-09: Duration, Resources, Predecessors
```

This is the minimal, complete fix for D-09: Enter in a Predecessors cell now calls `input.blur()`, which fires the existing capture-phase blur listener → `handleGridCellBlur('predecessors', ...)` → parses predecessor IDs and draws arrows. No other behaviour changes.

## Files Modified

| File | Change |
|------|--------|
| `app/views/project-plan.js` | Line 460 — extend Enter-commit allowlist guard + updated comment |

## Commits

| Hash | Message |
|------|---------|
| `813f79b` | `feat(86.3-02): extend Enter-commit allowlist to Predecessors cell` |

## Acceptance Criteria (Automated Grep — PASSED)

| Check | Expected | Result |
|-------|----------|--------|
| `grep -c "col !== 'predecessors'"` | 1 | 1 PASSED |
| `grep -c "only Duration and Resources per spec"` | 0 | 0 PASSED |
| `grep -c "Phase 86.3 D-09"` | >= 1 | 1 PASSED |

## UAT Status

WARNING: UAT pre-approved by user (batch authorization for overnight execution).
Browser-side verification deferred — to be performed by user in the morning.

Pending checks (Plan 02 — Predecessors Enter commit):

- [ ] D-09 clean Enter: type "1, 2" in Predecessors cell of row #3, press Enter — arrow drawn from #1 to #3 AND #2 to #3, no toast
- [ ] D-09 cycle detect: enter a predecessor that would form a cycle, press Enter — toast appears, cell reverts
- [ ] Regression: Tab still commits Predecessors
- [ ] Regression: Enter still commits Duration / Resources (86.2 DEFECT-10)
- [ ] Regression: Enter still does NOT commit Name / Start / End (Tab only)
- [ ] No JavaScript errors in browser console

## Deviations from Plan

None — plan executed exactly as written. Single-line edit applied at project-plan.js:460 with no scope creep.

## Self-Check: PASSED

- [x] `app/views/project-plan.js` exists and contains the new guard condition (grep confirmed)
- [x] Commit `813f79b` exists (`feat(86.3-02): extend Enter-commit allowlist to Predecessors cell`)
- [x] Old comment "only Duration and Resources per spec" removed (grep count 0)
- [x] New comment references "Phase 86.3 D-09" (grep count 1)
- [ ] UAT: Mechanically verified via grep; in-browser confirmation deferred to user
