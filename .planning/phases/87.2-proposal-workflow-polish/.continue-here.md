---
context: phase
phase: 87.2-proposal-workflow-polish
plan: 0
task: 0
total_tasks: 0
status: planned_ready_to_execute
last_updated: 2026-05-22
branch: v3.3
last_commit: b24a617
---

<current_state>
Phase 87.2 (Proposal Workflow Polish) is fully PLANNED and committed (b24a617). No execution has started. The five PLAN.md files cover all 26 D-NN locked decisions from CONTEXT.md and the 5 Roadmap success criteria (re-interpreted per the CONTEXT.md correctives for SC#1 and SC#3). Plan-checker verified after 2 targeted revision iterations; no BLOCKERs or WARNINGs remain on disk.

Working tree is clean for tracked files. Untracked files unrelated to this phase: `.claude/worktrees/`, `.planning/debug/86.9-row-misalign-DEBUG.md`, `.planning/phases/86.9-gantt-curtain-divider-pdf-export/86.9-03-PLAN.md`, `debug-diag-86.9.js`.

The phase is sitting at the gate between planning and execution. The next session can either run `/gsd-execute-phase 87.2` directly or `/clear` first for a fresh executor context window.
</current_state>

<completed_work>

Completed (this session):
- `/gsd-discuss-phase 87.2` artifact verified — CONTEXT.md (26 decisions D-01..D-26) already on disk
- `/gsd-plan-phase 87.2` — orchestrated end-to-end:
  - Skipped UI-SPEC.md (CONTEXT.md D-25 already captured the visual contract) per user choice
  - Skipped RESEARCH.md (CONTEXT.md already line-number precise) per user choice
  - Spawned gsd-planner → 5 plans across 4 waves produced
  - Spawned gsd-plan-checker iter 1 → 2 WARNINGs found (W1 Plan 01 undercount; W2 Plan 04 grep count off-by-one)
  - Spawned gsd-planner revision iter 1 → Plan 01 expanded to 4 sites; Plan 04 grep split into 2 precise greps
  - Spawned gsd-plan-checker iter 2 → 1 WARNING (Plan 01 still missing 2 sites at line 160 sort comparator) + 1 INFO (cosmetic 528→529)
  - Spawned gsd-planner revision iter 2 → Plan 01 expanded to 6 sites total; cosmetic line numbers fixed
  - Manually verified iter-2 revision content on disk; accepted (would have been iter-3 of checker — diminishing returns + 3-iteration cap)
- Decision coverage gate: 26/26 D-NN traceable to plans (verified by grep)
- STATE.md updated: status=Phase 87.2 READY TO EXECUTE; Current Position rewritten
- ROADMAP.md annotated with wave-grouping headers + cross-cutting constraints + manual deploy note
- Committed all planning artifacts as `b24a617`
</completed_work>

<remaining_work>

1. Wave 1 (parallel — disjoint files): Execute `87.2-01-PLAN.md` (proposal-modal.js: Target Client auto-select + 6-site `company_name` bug fix) + `87.2-02-PLAN.md` (firestore.rules: new proposals update branch).
2. **MANUAL DEPLOY between waves** — `firebase deploy --only firestore:rules` REQUIRED after Wave 1 commits. Otherwise Wave 2's operations_user smoke-test will fail at the rules layer with `Missing or insufficient permissions`.
3. Wave 2: Execute `87.2-03-PLAN.md` (renderProposalActionButtons dual canApprove + canDrive flags; parent_collection-aware department gating; `_parentDocCache` preload via getDoc at openProposalModal).
4. Wave 3: Execute `87.2-04-PLAN.md` (Request Revision button + sub-modal mirroring Mark as Loss; extended Mark Sent to Client gate at `for_revision`; `REVISION_REQUESTED` audit action + amber dot color; window function registration).
5. Wave 4: Execute `87.2-05-PLAN.md` (renderAuditTrail merges comms_log as indented children under preceding SENT_TO_CLIENT or under CREATED for pre-send comms; left-column Client Communications kept as input surface).
6. In-browser UAT walkthrough on PROP-2026-001 (CLMC-SPI-2026004) — the same proposal that surfaced the original 5 gaps.
7. `/gsd-verify-phase 87.2` — goal-backward verification against the 5 Roadmap success criteria (with the corrective re-interpretations from D-10/D-11/D-12/D-15 for SC#3 and D-03 for SC#1).
</remaining_work>

<decisions_made>

- **CONTEXT.md supersedes ROADMAP.md on SC#3** — comms entries stay PURE LOGS; revision cycle uses explicit Request Revision + extended Mark Sent action buttons. NEVER reintroduce auto-status-from-comms.
- **No legacy backfill (D-03 overrides Roadmap SC#1)** — existing prod proposals with `target_client_name=null` stay until manually corrected via Edit modal. NO read-time fallback. NO migration script.
- **Plan 01 bundles all SIX `client_name` sites** — 1 write (line 632) + 3 dropdown render (lines 529-531) + 2 sort comparator (line 160). Same root cause; atomic fix.
- **Wave structure prioritizes shared-file safety** — only Wave 1 has parallel plans (01 + 02, disjoint files). Waves 2/3/4 serialize the remaining proposal-modal.js work to avoid orphan-ownership / merge conflict on the shared file. (Per project memory `feedback_orphan_ownership_parallel_plans.md`.)
- **Skipped UI-SPEC and RESEARCH** — CONTEXT.md D-25 already captured visual contract; CONTEXT.md cited every line number. No duplication.
- **Accepted iter-2 INFOs without further revision** — cosmetic only; manually verified iter-2 disk content matched revision spec; iter-3 cap reached anyway.
</decisions_made>

<blockers>
None at the planning stage. One human gate exists between Wave 1 and Wave 2 (see Infrastructure State).
</blockers>

## Required Reading (in order)
1. `.planning/phases/87.2-proposal-workflow-polish/87.2-CONTEXT.md` — the 26 locked decisions D-01..D-26. This is the authoritative contract. ROADMAP.md SC#1 and SC#3 are SUPERSEDED by D-03 and D-10/D-11/D-12/D-15 respectively.
2. `.planning/phases/87.2-proposal-workflow-polish/87.2-01-PLAN.md` through `87.2-05-PLAN.md` — execution blueprints.
3. `CLAUDE.md` — window function registration pattern (Plan 04 needs `openRequestRevisionModal` and `confirmRequestRevision` registered in `openProposalModal` and deleted in `closeProposalModal`).
4. `.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-CONTEXT.md` D-04, D-06, D-08 — `proposal-modal.js` is the canonical owner; `parent_collection` discriminator on proposal docs; Proposals sub-tab role-gates.
5. `.planning/phases/87-proposal-lifecycle/87-CONTEXT.md` D-02, D-04, D-08, D-10 — proposal schema (audit_log + comms_log embedded arrays); project_status state machine; Firestore rules.
6. `.planning/phases/84-notifications-triggers/84-CONTEXT.md` D-12 — Phase 84 NOTIF-12 field-mask pattern is the canonical template that Plan 02's new proposals update branch mirrors verbatim.

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Auto-status-from-comms | User explicitly course-corrected mid-discussion: comms entries DO NOT trigger any `proposal.status` or `parent.project_status` change. Roadmap SC#3 wording is OBSOLETE. | blocking | If any executor or auditor sees logic that flips status based on a comms_log append, REJECT. Use the new Request Revision action button instead. |
| Legacy target_client backfill | User explicitly opted out of any read-time fallback from parent.client_id. Roadmap SC#1 wording is OBSOLETE. | blocking | Display reads whatever is stored in target_client_name. User repairs via Edit modal once the save-side fix lands. |
| Parallel plans on proposal-modal.js | Plans 01, 03, 04, 05 all touch this file. Only sequential waves are safe. Per `feedback_orphan_ownership_parallel_plans.md`. | blocking | The wave dependencies in ROADMAP.md enforce this (03→01, 04→03, 05→04). Do not re-parallelize them. |
| Skipping firestore.rules deploy | Plan 87.2-02 modifies rules; Netlify does not auto-deploy them. Skipping the manual `firebase deploy` step makes Wave 2 smoke-test fail in a confusing way (looks like UI bug). | blocking | After Wave 1 commits and before Wave 2 starts: run `firebase deploy --only firestore:rules` and verify. |
| `c.client_name` / `a.client_name` / `client.client_name` anywhere | The `clients` collection has NO `client_name` field. The canonical field is `company_name`. Plan 01 fixes ALL 6 sites; future code should never reintroduce. | advisory | Plan 01 grep gate `! grep -nE "\.client_name" app/proposal-modal.js` enforces it for the modal; project-wide is up to reviewer vigilance. |

**Severity values:** `blocking` — resuming agent must demonstrate understanding before proceeding. `advisory` — important context, does not gate resumption.

## Infrastructure State

- **Branch:** `v3.3` (current)
- **Main branch:** `main`
- **Last commit (this work):** `b24a617 docs(87.2): plan Proposal Workflow Polish — 5 plans across 4 waves`
- **Firebase project:** `clmc-procurement` (production — no staging environment)
- **Firestore rules:** current rules in repo at `firestore.rules`; live rules in Firebase Console reflect the LAST manual deploy. Plan 02 will need `firebase deploy --only firestore:rules` between Wave 1 and Wave 2.
- **Netlify:** auto-deploys SPA assets (HTML/JS/CSS) on push to v3.3 / main. Does NOT deploy Firestore rules.
- **Dev server:** not running. To smoke-test locally: `python -m http.server 8000` from project root.
- **Untracked files (unrelated to 87.2):**
  - `.claude/worktrees/`
  - `.planning/debug/86.9-row-misalign-DEBUG.md`
  - `.planning/phases/86.9-gantt-curtain-divider-pdf-export/86.9-03-PLAN.md`
  - `debug-diag-86.9.js`
  These are leftover from Phase 86.9 work; leave them alone unless triaging that phase.

<context>
This phase was inserted 2026-05-21 after a super_admin walkthrough of PROP-2026-001 on CLMC-SPI-2026004 surfaced 5 integration gaps in the proposal workflow built across Phase 87 and Phase 87.1. The gaps are real (Target Client field-name bug at 6 sites; operations_user blocked from the actions they own; the original "auto-status from comms" idea ergonomically wrong → replaced with explicit Request Revision button mid-discussion; firestore.rules too restrictive for the role that drives the proposal; audit trail bouncy because comms log is in a separate column).

The phase is POLISH — no new lifecycle stages, no schema migrations, no new collections. All work is targeted edits to two files: `app/proposal-modal.js` (4 plans worth of edits, serialized across waves) and `firestore.rules` (one plan, parallel-safe).

Plans are commit-ready. Next session should run `/clear` (fresh executor context) then `/gsd-execute-phase 87.2`.
</context>

<next_action>
Run `/clear` to free the planning context, then `/gsd-execute-phase 87.2` to start Wave 1 (parallel: 87.2-01 + 87.2-02). After Wave 1's commits land, pause and run `firebase deploy --only firestore:rules` from the project root before letting the executor proceed to Wave 2.
</next_action>
