---
phase: 87.2-proposal-workflow-polish
plan: 05
subsystem: ui
tags: [firebase, proposal-modal, audit-trail, comms-log, javascript]

requires:
  - phase: 87.2-04
    provides: REVISION_REQUESTED audit action + AUDIT_ACTION_DOT_COLORS/LABELS entries added
  - phase: 87.2-01
    provides: proposal-modal.js base structure, COMMS_TYPE_META, buildCommsLogSection

provides:
  - "_buildAuditCommsGroups: groups comms_log entries under nearest preceding SENT_TO_CLIENT audit entry"
  - "_renderNestedCommsChild: renders one comms entry as indented child (24px, 12px font, gray dot, type pill)"
  - "renderAuditTrail: extended to render merged audit+comms unified timeline"

affects:
  - 87.2-verification
  - any future audit trail changes in proposal-modal.js

tech-stack:
  added: []
  patterns:
    - "Comms-under-audit nesting: comms_log entries grouped under nearest SENT_TO_CLIENT (or CREATED fallback) using oldest-first scan of audit timeline"
    - "Private helper naming: underscore-prefixed functions (_buildAuditCommsGroups, _renderNestedCommsChild) are module-private; no new exports"

key-files:
  created: []
  modified:
    - app/proposal-modal.js

key-decisions:
  - "D-21: comms_log entries appear in the right-column audit trail as nested children, not a separate section"
  - "D-22: parent selection algorithm uses CREATED (idx 0) as fallback bucket; only CREATED + SENT_TO_CLIENT are group parents"
  - "D-23: trail-level order newest-first; within-group children oldest-first (conversation reading direction)"
  - "D-24: buildCommsLogSection (left-column input surface) unchanged — input form co-location preserved"
  - "D-25: visual treatment — 24px indent, 12px font, #94a3b8 gray dot, type pill via COMMS_TYPE_META.cls"
  - "D-26: no new exports; _buildAuditCommsGroups and _renderNestedCommsChild are private to module"

requirements-completed: []

duration: 12min
completed: 2026-05-22
---

# Phase 87.2 Plan 05: Audit Trail Comms Nesting Summary

**Comms_log entries now render as nested children inside the audit trail — each entry grouped under the nearest preceding SENT_TO_CLIENT audit event (or under CREATED as fallback), using _buildAuditCommsGroups + _renderNestedCommsChild private helpers.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-22T00:00:00Z
- **Completed:** 2026-05-22T00:12:00Z
- **Tasks:** 1 (two sub-edits: 1A insert helpers, 1B replace renderAuditTrail body)
- **Files modified:** 1

## Accomplishments

- Inserted `_buildAuditCommsGroups` — sorts audit oldest-first, scans to assign each comms entry to its parent (CREATED idx 0 or nearest preceding SENT_TO_CLIENT), sorts children oldest-first within each group, reverses back to newest-first for display; handles empty audit_log edge case with synthetic CREATED placeholder
- Inserted `_renderNestedCommsChild` — renders a comms entry with 24px left-padding, 12px font, #94a3b8 gray dot, type pill via COMMS_TYPE_META.cls (badge-primary / status-badge pending / status-badge rejected), description truncated at 120 chars with escapeHTML applied
- Replaced `renderAuditTrail` body to call `_buildAuditCommsGroups` and append `childrenHtml` inside each audit row div; preserved all existing audit row HTML shape byte-for-byte
- `buildCommsLogSection` (left-column input surface) left completely unchanged per D-24

## Task Commits

1. **Task 1: Add private helpers + extend renderAuditTrail** - `b6b3f12` (feat)

**Plan metadata:** (see final docs commit)

## Files Created/Modified

- `app/proposal-modal.js` — inserted 103 lines of new code (two helpers + doc comments), replaced 8-line renderAuditTrail body with 19-line grouped version; net +111 lines

## Decisions Made

- Preserved `·` (middle dot U+00B7) and `—` (em dash U+2014) separators from the existing `renderAuditTrail` rather than switching to `. ` and `-` as shown in plan's text spec — the HTML shape and visual result are the same; the actual file used the Unicode characters and they must match for correct diffing
- Applied same separator style inside `_renderNestedCommsChild` for visual consistency with the parent audit entry rows

## Deviations from Plan

None — plan executed exactly as written. The only minor note: the plan's INTERFACES section showed `. ` and `-` as separator characters in the example HTML, but the actual file already used `·` and `—`. The new helpers were written to match the file's existing style, which is the correct behavior per the "preserve byte-for-byte where unchanged" invariant.

## Issues Encountered

None.

## Threat Surface Scan

- T-87.2-05-01 (XSS): `escapeHTML` applied to all user-provided strings in `_renderNestedCommsChild` — `descShort`, `logged_by_name`, `meta.label`, `meta.cls`. No raw HTML injection possible.
- T-87.2-05-02 (Information Disclosure): comms entries now surface in both columns; both have same read-access control (isActiveUser() per firestore.rules). No new disclosure boundary.

## Known Stubs

None.

## Automated Grep Verification (all passing)

```
function _buildAuditCommsGroups  → 1 match (line 261)
function _renderNestedCommsChild → 1 match (line 325)
const grouped = _buildAuditCommsGroups → 1 match (line 344)
#94a3b8 → 2 matches (comment + style attribute)
padding-left:24px → 1 match
export.*_buildAuditCommsGroups → 0 matches (PASS — private)
export.*_renderNestedCommsChild → 0 matches (PASS — private)
renderAuditTrail count → 2 (definition + call site)
buildCommsLogSection → 1 definition, "Client Communications" header present
```

## Next Phase Readiness

- Phase 87.2 is now code-complete (all 5 plans executed). Ready for UAT verification.
- Gap G5 closed: reviewers can follow the full proposal conversation in a single right-column unified timeline.
- Left-column add-entry form still co-located with the comms list (D-24 preserved).

---
*Phase: 87.2-proposal-workflow-polish*
*Completed: 2026-05-22*
