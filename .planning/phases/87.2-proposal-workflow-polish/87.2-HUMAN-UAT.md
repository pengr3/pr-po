---
status: complete
phase: 87.2-proposal-workflow-polish
source: [87.2-VERIFICATION.md]
started: 2026-05-22T00:00:00.000Z
updated: 2026-05-22T03:50:00.000Z
---

## Current Test

[testing complete]

## Tests

### 1. G1 — Target Client auto-fill + company name labels + persistence
expected: Create a new proposal from a project that has a client assigned. On project dropdown selection, Target Client dropdown auto-fills with the project's parent client. Dropdown option labels show company names (not blank). Saving the proposal persists a non-null `target_client_name` in Firestore.
result: blocked
blocked_by: other
reason: "i cannot seem to test this as there is no way to create a proposal."

### 2. G2 — operations_user sees correct button set
expected: Log in as an operations_user who is in `personnel_user_ids` of the parent project. Open a proposal in `pending_client` status. Action buttons show: Mark Sent to Client, Client Approved, Mark as Loss, Request Revision. No Approve Proposal / Reject Proposal buttons.
result: pass

### 3. G3 — Full revision cycle
expected: (a) Click Request Revision → sub-modal appears with optional comment. (b) Confirm → proposal status flips to `for_revision`, toast confirms. (c) Action column now shows Submit for Internal Approval + Edit Proposal + Mark Sent to Client. (d) Click Mark Sent to Client → toast says "Resent to client. Proposal back under client review." → status flips to `pending_client`.
result: pass
note: initially failed with Firestore permissions error; fixed in commit aa18750 (operations_user branch added to projects allow update rule)

### 4. G4 — operations_user comms entry saves without permission error
expected: Assigned operations_user opens a proposal and adds a comms entry via the Client Communications section. No "Missing or insufficient permissions" error. Entry appears in the comms log.
result: pass

### 5. G5 — Audit trail shows comms as nested children
expected: Open a proposal that has both audit log entries and comms log entries. In the right-column Audit Trail, comms entries appear as indented children (24px indent, gray dot, type pill, 12px font) under the nearest preceding SENT_TO_CLIENT audit entry. Comms logged before any SENT_TO_CLIENT appear under the CREATED entry at the bottom.
result: pass

## Summary

total: 5
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 1

## Gaps

- truth: "operations_user can Request Revision — proposal flips to for_revision and parent project_status updates atomically"
  status: fixed
  reason: "User reported: FirebaseError: Missing or insufficient permissions at confirmRequestRevision (proposal-modal.js:1256)"
  severity: major
  test: 3
  root_cause: "_applyProposalStateTransition uses a writeBatch that updates BOTH the proposal doc (allowed by BRANCH 2) AND the parent projects doc (project_status). The projects collection update rule only granted super_admin/operations_admin/finance — operations_user was excluded."
  fix: "Added operations_user narrow branch to projects allow update rule (commit aa18750) — field-mask hasOnly(['project_status','updated_at']) + personnel_user_ids membership check."
  verified: true
