---
phase: 87.2
plan: 03
type: execute
wave: 2
depends_on: [87.2-01]
files_modified:
  - app/proposal-modal.js
autonomous: true
requirements: []
decisions_addressed: [D-05, D-06, D-07, D-08, D-09]
must_haves:
  truths:
    - "super_admin sees every action button regardless of parent_collection"
    - "operations_admin sees every action button on projects-parent proposals (Approve/Reject/Submit/Edit/Mark Sent/Client Approved/Mark as Loss)"
    - "services_admin sees every action button on services-parent proposals"
    - "operations_user assigned to the parent project sees Submit/Edit/Mark Sent/Client Approved/Mark as Loss but NOT Approve/Reject"
    - "services_user assigned to the parent service sees the same set"
    - "Non-assigned operations_user / services_user, and cross-department users (services_admin on a project proposal, etc.) see NO action buttons"
  artifacts:
    - path: "app/proposal-modal.js"
      provides: "renderProposalActionButtons refactored to dual canApprove + canDrive flags with parent_collection-aware department gating"
      contains: "canDrive"
  key_links:
    - from: "renderProposalActionButtons"
      to: "_isCallerAttachedToProposalParent(proposal)"
      via: "synchronous role + assignment lookup using cached parent doc"
      pattern: "_isCallerAttachedToProposalParent"
    - from: "openProposalModal"
      to: "module-state cached parent doc"
      via: "one extra getDoc per modal open"
      pattern: "_parentDocCache"
---

OBJECTIVE

Close Gap G2 — Operations role permissions UI refactor in renderProposalActionButtons.

Purpose: An operations_user on a project they own opens the proposal modal from the inline card on project-detail.js, and the Action Buttons column shows "No further actions." — even though the proposal sits in pending_client and the operations_user is the one driving it. Today every action button is gated behind canApprove = ['super_admin','operations_admin'].includes(cu?.role) (line 211). The fix: refactor to TWO flags per D-05 — canApprove (admins only, Approve/Reject) and canDrive (admins + assigned operations_user/services_user, the rest of the lifecycle).

Per D-06 the role-vs-parent_collection asymmetry must hold: services_admin / services_user do NOT see action buttons on a parent_collection === 'projects' proposal even if they otherwise hold the role; operations_admin / operations_user do NOT see action buttons on a parent_collection === 'services' proposal. Super_admin sees everything.

Assignment check (D-07) reuses the existing getAssignedProjectCodes / getAssignedServiceCodes utilities — but those check against project_code / service_code, not against doc IDs. Since proposals store project_code but not service_code, the cleanest approach is to preload the parent doc once at modal open time (one extra getDoc per open, cached in module state) and read the parent's personnel_user_ids array directly. This mirrors the same approach the firestore.rules new branch (Plan 02) uses.

Output: four sub-edits to app/proposal-modal.js:
  1. Add module-state variable _parentDocCache near currentProposal (line 70).
  2. Add private helper _isCallerAttachedToProposalParent() near _fetchProposalDoc (after line 127).
  3. Refactor renderProposalActionButtons (lines 209-239) to use the dual flags.
  4. In openProposalModal (line 1293) preload the parent doc into _parentDocCache. In closeProposalModal (line 1341) clear it.

This plan runs in Wave 2 (after Plan 01 lands G1 bug fix) to keep proposal-modal.js edits serialized.

EXECUTION CONTEXT

- @$HOME/.claude/get-shit-done/workflows/execute-plan.md
- @$HOME/.claude/get-shit-done/templates/summary.md

CONTEXT

- @.planning/PROJECT.md
- @.planning/ROADMAP.md
- @.planning/STATE.md
- @.planning/phases/87.2-proposal-workflow-polish/87.2-CONTEXT.md
- @.planning/phases/87.2-proposal-workflow-polish/87.2-01-SUMMARY.md

INTERFACES

Current renderProposalActionButtons (proposal-modal.js lines 209-239) — to be refactored:

  function renderProposalActionButtons(proposal) {
      const cu = (typeof window.getCurrentUser === 'function') ? window.getCurrentUser() : null;
      const canApprove = ['super_admin', 'operations_admin'].includes(cu?.role);
      const status = proposal.status || 'draft';
      const docId = escapeHTML(proposal.id);
      let buttons = [];
      if (status === 'draft' || status === 'for_revision') {
          if (canApprove) buttons.push('button: Submit for Internal Approval (window.submitProposalForApproval)');
          buttons.push('button: Edit Proposal (window.openEditProposalModal)');
      } else if (status === 'pending_internal') {
          if (canApprove) {
              buttons.push('button: Approve Proposal (window.openApproveModal)');
              buttons.push('button: Reject Proposal (window.openRejectModal)');
          }
      } else if (status === 'pending_client') {
          if (canApprove) {
              buttons.push('button: Mark Sent to Client (window.submitMarkSentToClient)');
              buttons.push('button: Client Approved (window.openClientApprovedModal)');
              buttons.push('button: Mark as Loss (window.openLossModal)');
          }
      }
      ...
  }

Existing getAssignedProjectCodes shape (utils.js lines 264-273) — returns null for non-operations_user, returns array of codes (or empty array) for operations_user. Cannot answer "is this admin attached" — admins are inherently attached.

Existing getAssignedServiceCodes shape (utils.js lines 340-348) — symmetric to the above for services_user.

Parent doc shape (relevant fields, both projects and services collections): every doc has personnel_user_ids (array of uid strings) and active (bool) plus collection-specific code field (project_code OR service_code).

Existing openProposalModal extension point (proposal-modal.js lines 1293-1335): after the line currentProposal = proposal; (line 1304) and before the line const existing = document.getElementById('proposalDetailModal'); (line 1308) is the correct insertion point for the parent-doc preload. The preload is awaited so renderProposalActionButtons (called synchronously inside buildProposalDetailModalHtml) can read the cache without async.

_refreshDetailModalAfterTransition (lines 465-478) rebuilds the modal HTML after every transition. The _parentDocCache lives in module scope and persists across re-renders within the same modal session.

TASKS

Task 1: Add _parentDocCache module state + _isCallerAttachedToProposalParent helper + parent-doc preload in openProposalModal + clear in closeProposalModal

  files: app/proposal-modal.js
  type: auto

  read_first:
  - Read app/proposal-modal.js lines 67-76 (module state declarations: currentProposal, createModalMode, createModalEditingId, _modalProjectsData, _modalClientsData, _modalProjectsLoaded). Confirm _parentDocCache is not already declared.
  - Read app/proposal-modal.js lines 113-127 (_fetchProposalDoc helper). Mirror its shape (try/catch + console.error logging).
  - Read app/proposal-modal.js lines 1293-1335 (openProposalModal) to identify the exact insertion point between currentProposal = proposal; (line 1304) and the existing pre-existing modal removal block (line 1308).
  - Read app/proposal-modal.js lines 1341-1367 (closeProposalModal) to identify where to clear _parentDocCache (immediately after currentProposal = null; on line 1344).

  action:

  Make FOUR sub-edits in app/proposal-modal.js.

  Sub-edit 1A — Add module state (insert as a new line after line 75, before the blank line that precedes the constants section starting at line 78):

      // Phase 87.2 D-06/D-07: cache the parent project/service doc so the synchronous
      // renderProposalActionButtons gate can do role + assignment checks without an
      // async lookup on every render. Populated in openProposalModal, cleared in
      // closeProposalModal. Shape: { collection: 'projects'|'services', doc: {id, personnel_user_ids, ...} } | null
      let _parentDocCache = null;

  Sub-edit 1B — Add the helper function. Insert as a new block AFTER the closing brace of _fetchProposalDoc (after line 127) and BEFORE the JSDoc for _loadModalDropdownData (line 129). Use this exact JSDoc + function body, do not paraphrase:

      /**
       * Phase 87.2 D-05/D-06/D-07 — Synchronous role + assignment check for the
       * proposal modal's Action Buttons gate. Returns { canApprove, canDrive }.
       *
       * canApprove  - may invoke Approve / Reject sub-modals.
       *   super_admin: always true
       *   operations_admin: true when parent_collection === 'projects' (D-09)
       *   services_admin: true when parent_collection === 'services' (D-09)
       *
       * canDrive    - may invoke Submit / Edit / Mark Sent / Client Approved /
       *               Mark as Loss / Request Revision.
       *   super_admin: always true
       *   operations_admin: true when parent_collection === 'projects' (D-06)
       *   services_admin: true when parent_collection === 'services' (D-06)
       *   operations_user: true when parent_collection === 'projects' AND caller uid
       *                    is in _parentDocCache.doc.personnel_user_ids (D-07)
       *   services_user: true when parent_collection === 'services' AND caller uid
       *                  is in _parentDocCache.doc.personnel_user_ids (D-07)
       *
       * canApprove implies canDrive (admins can do everything drivers can).
       *
       * Returns { canApprove: false, canDrive: false } if _parentDocCache is empty
       * (e.g., modal opened against a deleted parent doc - defensive default-deny).
       */
      function _isCallerAttachedToProposalParent(proposal) {
          const cu = (typeof window.getCurrentUser === 'function') ? window.getCurrentUser() : null;
          const role = cu?.role || null;
          const parentCollection = proposal?.parent_collection || 'projects';
          const assigned = Array.isArray(_parentDocCache?.doc?.personnel_user_ids)
              ? _parentDocCache.doc.personnel_user_ids
              : [];
          const isAssigned = !!cu?.uid && assigned.includes(cu.uid);

          let canApprove = false;
          let canDrive   = false;

          if (role === 'super_admin') {
              canApprove = true;
              canDrive   = true;
          } else if (parentCollection === 'projects') {
              if (role === 'operations_admin') {
                  canApprove = true;
                  canDrive   = true;
              } else if (role === 'operations_user' && isAssigned) {
                  canApprove = false;
                  canDrive   = true;
              }
          } else if (parentCollection === 'services') {
              if (role === 'services_admin') {
                  canApprove = true;
                  canDrive   = true;
              } else if (role === 'services_user' && isAssigned) {
                  canApprove = false;
                  canDrive   = true;
              }
          }

          return { canApprove, canDrive };
      }

  Sub-edit 1C — Insert parent-doc preload in openProposalModal. Add this block as new lines AFTER the line currentProposal = proposal; (line 1304) and BEFORE the comment // Remove any pre-existing instance of the modal (line 1306):

      // Phase 87.2 D-06/D-07 - Preload the parent project/service doc so the
      // synchronous renderProposalActionButtons gate can check assignment without
      // an async lookup on every render. Single getDoc per modal open.
      try {
          const parentColl = proposal.parent_collection || 'projects';
          const parentSnap = await getDoc(doc(db, parentColl, proposal.project_id));
          if (parentSnap.exists()) {
              _parentDocCache = { collection: parentColl, doc: { id: parentSnap.id, ...parentSnap.data() } };
          } else {
              _parentDocCache = null;
          }
      } catch (err) {
          console.error('[ProposalModal] parent-doc preload failed:', err);
          _parentDocCache = null;
      }

  Sub-edit 1D — Clear cache in closeProposalModal. Add this single line AFTER currentProposal = null; (line 1344), BEFORE the // Delete every window function comment (line 1346):

      _parentDocCache = null;

  Do NOT change anything else in openProposalModal or closeProposalModal. Window function registrations and cleanups stay intact (Plan 04 will add openRequestRevisionModal and confirmRequestRevision to both lists in its own task).

  verify automated:
      cd /c/Users/franc/dev/projects/pr-po && grep -nE "^let _parentDocCache" app/proposal-modal.js && grep -nE "function _isCallerAttachedToProposalParent" app/proposal-modal.js && grep -nE "parent-doc preload" app/proposal-modal.js && [ $(grep -c "_parentDocCache = null" app/proposal-modal.js) -ge 3 ]

  acceptance_criteria:
  - grep -nE "^let _parentDocCache" app/proposal-modal.js returns exactly 1 match.
  - grep -nE "function _isCallerAttachedToProposalParent" app/proposal-modal.js returns exactly 1 match.
  - grep -nE "parent-doc preload" app/proposal-modal.js returns at least 1 match (the comment in openProposalModal).
  - grep -c "_parentDocCache = null" app/proposal-modal.js returns at least 3 (the initial declaration; the else-branch in the preload; the closeProposalModal clear; possibly one more in the catch block).
  - grep -nE "personnel_user_ids" app/proposal-modal.js returns at least 1 match (inside the new helper).
  - The helper returns the shape { canApprove, canDrive } (both booleans).
  - No async keyword on the new helper - it must be synchronously callable from renderProposalActionButtons.

  done: Module state + helper + preload + cache-clear are in place. The helper can be called synchronously from renderProposalActionButtons in Task 2. A console.log({canApprove, canDrive}) sanity check while opening a proposal as different roles confirms the matrix.

Task 2: Refactor renderProposalActionButtons to use dual canApprove + canDrive flags

  files: app/proposal-modal.js
  type: auto

  read_first:
  - Read app/proposal-modal.js lines 209-239 to confirm renderProposalActionButtons exact current shape (status branches, button push calls, fallback "No further actions." string).
  - Confirm Task 1 has already landed _isCallerAttachedToProposalParent earlier in the file. The grep `grep -nE "function _isCallerAttachedToProposalParent" app/proposal-modal.js` must return a match BEFORE starting Task 2.

  action:

  Replace the ENTIRE body of renderProposalActionButtons (lines 209-239 inclusive) with this exact implementation. Preserve the function signature unchanged. Preserve all existing onclick window.* function names (no renames in this plan; Plan 04 adds the new Request Revision button).

      function renderProposalActionButtons(proposal) {
          // Phase 87.2 D-05/D-06/D-07/D-08/D-09 — Dual-flag gating.
          const { canApprove, canDrive } = _isCallerAttachedToProposalParent(proposal);
          const status = proposal.status || 'draft';
          const docId = escapeHTML(proposal.id);

          let buttons = [];

          if (status === 'draft' || status === 'for_revision') {
              if (canDrive) {
                  buttons.push(`<button class="btn btn-primary" style="width:100%;" onclick="window.submitProposalForApproval('${docId}')">Submit for Internal Approval</button>`);
                  buttons.push(`<button class="btn btn-outline" style="width:100%;" onclick="window.openEditProposalModal('${docId}')">Edit Proposal</button>`);
              }
          } else if (status === 'pending_internal') {
              if (canApprove) {
                  buttons.push(`<button class="btn btn-success" style="width:100%;" onclick="window.openApproveModal('${docId}')">Approve Proposal</button>`);
                  buttons.push(`<button class="btn btn-danger" style="width:100%;" onclick="window.openRejectModal('${docId}')">Reject Proposal</button>`);
              }
          } else if (status === 'pending_client') {
              if (canDrive) {
                  buttons.push(`<button class="btn btn-outline" style="width:100%;" onclick="window.submitMarkSentToClient('${docId}')">Mark Sent to Client</button>`);
                  buttons.push(`<button class="btn btn-success" style="width:100%;" onclick="window.openClientApprovedModal('${docId}')">Client Approved</button>`);
                  buttons.push(`<button class="btn btn-danger" style="width:100%;" onclick="window.openLossModal('${docId}')">Mark as Loss</button>`);
              }
          }
          // client_approved + loss: no further actions

          if (buttons.length === 0) {
              return `<div style="font-size:13px;color:#64748b;">No further actions.</div>`;
          }
          return `<div style="display:flex;flex-direction:column;gap:8px;">${buttons.join('')}</div>`;
      }

  KEY BEHAVIOR CHANGES vs. the pre-refactor code:
  - draft / for_revision: previously the Edit button was visible to EVERYONE regardless of canApprove. After refactor, Edit is gated to canDrive (D-08 says operations_user / services_user can edit their own proposals; non-attached/non-admin users no longer see Edit). This is intentional per D-08: "Edit Proposal is included in the operations_user/services_user driver set"; finance + procurement users never had a reason to see Edit, and they were not the audience for the prior unconditional display.
  - draft / for_revision: Submit for Internal Approval was previously gated to canApprove (admins). After refactor, Submit is gated to canDrive — D-05 explicitly says "Submit for Internal Approval" belongs to the driver set, not the approver set. The original gate was overly restrictive.
  - pending_client: Mark Sent / Client Approved / Mark as Loss were previously gated to canApprove. After refactor they're gated to canDrive (admins + assigned ops/services users). This is the core G2 fix.
  - pending_internal: Approve / Reject still gated to canApprove — internal approval gate stays admin-only per D-09.
  - The "No further actions." fallback is preserved verbatim, including the inline style string. The container div style string is preserved verbatim.

  Do NOT add the Request Revision button in this task — Plan 04 owns that wiring. Do NOT touch the underlying handlers (submitProposalForApproval, openEditProposalModal, submitMarkSentToClient, openClientApprovedModal, openLossModal) — they read status from a fresh _fetchProposalDoc and self-validate, so they tolerate a wider caller set without rule changes.

  verify automated:
      cd /c/Users/franc/dev/projects/pr-po && grep -nE "const \{ canApprove, canDrive \} = _isCallerAttachedToProposalParent" app/proposal-modal.js && grep -cE "if \(canDrive\)" app/proposal-modal.js && grep -cE "if \(canApprove\)" app/proposal-modal.js

  acceptance_criteria:
  - grep -nE "const \{ canApprove, canDrive \} = _isCallerAttachedToProposalParent" app/proposal-modal.js returns exactly 1 match (in renderProposalActionButtons).
  - grep -cE "if \(canDrive\)" app/proposal-modal.js returns at least 2 (draft/for_revision branch + pending_client branch).
  - grep -cE "if \(canApprove\)" app/proposal-modal.js returns at least 1 (pending_internal branch).
  - The old line `const canApprove = ['super_admin', 'operations_admin'].includes(cu?.role);` is REMOVED — grep -nE "\\['super_admin', 'operations_admin'\\]\\.includes\\(cu\\?\\.role\\)" app/proposal-modal.js returns 0 matches.
  - "Submit for Internal Approval" string is still present in the file.
  - "Mark Sent to Client" string is still present.
  - "Mark as Loss" string is still present.
  - "Edit Proposal" string is still present.
  - "Approve Proposal" string is still present.
  - "Reject Proposal" string is still present.

  done: renderProposalActionButtons returns the correct button set per role-and-status matrix. Manual smoke test: log in as an operations_user assigned to CLMC-SPI-2026004, open PROP-2026-001 (status pending_client), expect to see Mark Sent / Client Approved / Mark as Loss (NO Approve/Reject). Log in as services_admin opening the same project-parent proposal, expect "No further actions." Log in as super_admin, expect every button visible for the current status.

THREAT MODEL

Trust Boundaries

  - browser UI gate (renderProposalActionButtons) — surface only; not a security boundary. The real security boundary is Firestore rules (Plan 02 handles that).

STRIDE Threat Register

  - T-87.2-03-01 (Elevation): an operations_user could spoof getCurrentUser().role via DevTools to render Approve/Reject buttons. Disposition: accept. The buttons only invoke window.openApproveModal / window.openRejectModal which call _applyProposalStateTransition; Firestore rules block the actual write for any non-admin. UI-side spoofing is cosmetic.
  - T-87.2-03-02 (Tampering): an attacker could rewrite _parentDocCache.doc.personnel_user_ids in DevTools to include their own uid. Disposition: accept. Same residual risk as T-87.2-03-01 — the UI gate is cosmetic; Firestore rules enforce the real assignment check.
  - T-87.2-03-03 (Information Disclosure): the new module variable _parentDocCache holds the parent's personnel_user_ids in the browser. Disposition: accept. Both projects and services have allow read: if isActiveUser() — any logged-in user can already read these arrays. No new disclosure.
  - T-87.2-03-04 (DoS): one extra getDoc per modal open. Disposition: accept. Same cost shape as the existing _fetchProposalDoc.

VERIFICATION

Manual smoke-test matrix (post-Plan-02-deploy of firestore.rules):
  1. super_admin opens PROP-2026-001 (project parent, pending_client). Expect: Mark Sent / Client Approved / Mark as Loss visible.
  2. operations_admin opens same proposal. Expect: same as super_admin.
  3. operations_user IN personnel_user_ids opens same proposal. Expect: Mark Sent / Client Approved / Mark as Loss visible. NO Approve/Reject.
  4. operations_user NOT IN personnel_user_ids opens same proposal. Expect: "No further actions." text.
  5. services_admin opens same (project-parent) proposal. Expect: "No further actions." (cross-department block per D-06).
  6. super_admin opens a services-parent proposal in pending_client. Expect: full set.
  7. services_user assigned to that service opens same. Expect: full set minus Approve/Reject.

Automated grep gate:
  grep -nE "^let _parentDocCache" app/proposal-modal.js
  grep -nE "function _isCallerAttachedToProposalParent" app/proposal-modal.js
  grep -nE "const \{ canApprove, canDrive \} = _isCallerAttachedToProposalParent" app/proposal-modal.js
  grep -nE "parent-doc preload" app/proposal-modal.js
  ! grep -nE "\\['super_admin', 'operations_admin'\\]\\.includes\\(cu\\?\\.role\\)" app/proposal-modal.js

SUCCESS CRITERIA

  1. _parentDocCache module state declared at top-of-file.
  2. _isCallerAttachedToProposalParent helper defined, returns { canApprove, canDrive }.
  3. openProposalModal preloads parent doc before insertAdjacentHTML; closeProposalModal clears _parentDocCache.
  4. renderProposalActionButtons uses canDrive for draft/for_revision/pending_client driver actions and canApprove for pending_internal Approve/Reject only.
  5. All seven smoke-test rows pass against the running app (after Plan 02's firestore.rules deploy).

OUTPUT

After completion, create .planning/phases/87.2-proposal-workflow-polish/87.2-03-SUMMARY.md covering:
  - Diff of renderProposalActionButtons (before/after).
  - The new helper signature and the role+parent_collection truth table it implements.
  - Smoke-test matrix results (all 7 rows).
  - Decisions covered: D-05, D-06, D-07, D-08, D-09.
