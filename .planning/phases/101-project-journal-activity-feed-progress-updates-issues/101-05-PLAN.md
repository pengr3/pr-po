---
phase: 101-project-journal-activity-feed-progress-updates-issues
plan: 05
type: execute
wave: 4
depends_on: ["101-03", "101-04"]
files_modified: [app/views/project-detail.js, app/views/procurement.js]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Each of the 4 Phase 100 lifecycle gate transitions auto-posts a system Feed entry after its Firestore update"
    - "A contract_cost or budget edit in saveField auto-posts a system Feed entry with the before/after delta"
    - "Marking a PO Delivered (non-subcon) auto-posts a system Feed entry to its project referencing PO ID + supplier"
  artifacts:
    - path: "app/views/project-detail.js"
      provides: "Auto-entry calls in 4 gate functions + saveField cost-delta block"
      contains: "_addActivityEntry(currentProject.id"
    - path: "app/views/procurement.js"
      provides: "PO Delivered auto-entry write to projects/{id}/activity_entries"
      contains: "activity_entries"
  key_links:
    - from: "procurement.js Delivered handler"
      to: "projects/{project_id}/activity_entries"
      via: "addDoc with is_system:true"
      pattern: "activity_entries"
    - from: "project-detail.js saveField cost block"
      to: "_addActivityEntry"
      via: "fire-and-forget .catch()"
      pattern: "_addActivityEntry"
---

<objective>
Wire the four auto-system-entry triggers (D-05–D-09) so the Activity Feed becomes the unified edit/event history: (1) all four Phase 100 lifecycle gate functions post a system entry after their Firestore update; (2) the saveField contract_cost/budget delta block posts a system entry with before→after values; (3) the procurement.js PO Delivered handler posts a system entry to the target project's Feed. Personnel/billing/RFP-Paid events are explicitly out of scope (D-10).

Purpose: Edit history folds into the Feed — no separate edit-history UI (D-09). This is the integration that makes the journal authoritative.
Output: 4 gate functions + saveField cost block updated in project-detail.js; Delivered handler updated in procurement.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-CONTEXT.md
@.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-PATTERNS.md

<interfaces>
project-detail.js (from Plan 03): `_addActivityEntry(projectId, { type, text, is_system })` exists at module scope. Call with type:'system', is_system:true.

Gate functions (project-detail.js):
- window.lcAdvanceToForProposal (line 2489) — after `await addProjectAuditEntry(...)`, in the same try: add `await _addActivityEntry(projectId, { type:'system', is_system:true, text:'Status advanced to For Proposal by '+(cu?.full_name||'Unknown') })`.
- window.lcStartMobilization (line 2499) — analogous text "Mobilization started by ...".
- window.lcStartProject (line 2510) — "Project started by ...".
- window.lcMarkProjectComplete (line 2520) — "Project marked Completed by ...".
Each already has `const cu = window.getCurrentUser?.();` and a try/catch wrapping updateDoc + addProjectAuditEntry.

saveField NOTIF-19 block (project-detail.js lines 1265-1287): after the existing `createNotificationForUsers(...).catch(...)` block (still inside `if (NOTIF19_COST_FIELDS.includes(fieldName) && normalizedOld !== valueToSave)`), add a fire-and-forget `_addActivityEntry(currentProject.id, {...}).catch(...)`. fieldLabel/oldDisplay/newDisplay already computed in that block — reuse them. Use formatCurrency (already imported).

procurement.js Delivered handler (lines ~7921-7961): inside `if (newStatus === 'Delivered' && !isSubcon)`, after the NOTIF-18 notification block, add a separate try/catch. procurement.js already imports addDoc, serverTimestamp, escapeHTML, collection, db (confirmed line 7-8). `poRef` is the PO doc ref in scope; a fresh `getDoc(poRef)` gives `d` with `d.project_id`, `d.po_id`, `d.supplier_name`. window.getCurrentUser?.() available.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: project-detail.js — 4 gate auto-entries + saveField cost-delta auto-entry</name>
  <read_first>
    - app/views/project-detail.js lines 2489-2531 (the four lc* gate functions — read each try block to insert _addActivityEntry after addProjectAuditEntry)
    - app/views/project-detail.js lines 1265-1290 (the NOTIF-19 cost-change block in saveField — fieldLabel/oldDisplay/newDisplay computed at 1272-1274; insert the journal auto-entry after the createNotificationForUsers().catch() call, still inside the same if)
    - .planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-PATTERNS.md ("Lifecycle gate auto-entry pattern", "Delta detection pattern for budget/contract_cost auto-entries", "Fire-and-forget pattern")
    - .planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-CONTEXT.md (D-05/D-06/D-07 auto-entry scope, D-10 explicit exclusions)
  </read_first>
  <action>
    1. In each of the four gate functions, inside the existing try block, immediately AFTER the `await addProjectAuditEntry(...)` line, add `await _addActivityEntry(projectId, { type: 'system', is_system: true, text: '<message>' });` with messages:
       - lcAdvanceToForProposal: `Status advanced to For Proposal by ${cu?.full_name || 'Unknown'}`
       - lcStartMobilization: `Mobilization started by ${cu?.full_name || 'Unknown'}`
       - lcStartProject: `Project started by ${cu?.full_name || 'Unknown'}`
       - lcMarkProjectComplete: `Project marked Completed by ${cu?.full_name || 'Unknown'}`
       Place it inside the try so a journal failure is caught by the existing catch (gate transition already succeeded via updateDoc; the auto-entry is a best-effort side effect — acceptable to log and move on). Do NOT add awaits before updateDoc; order is updateDoc → addProjectAuditEntry → _addActivityEntry.

    2. In saveField's NOTIF-19 block (after the `createNotificationForUsers({...}).catch(...)` call but still inside `if (NOTIF19_COST_FIELDS.includes(fieldName) && normalizedOld !== valueToSave) { ... }`), add a fire-and-forget journal auto-entry. Reuse the in-scope `fieldLabel`, `oldDisplay`, `newDisplay`, and `cu`:
       `_addActivityEntry(currentProject.id, { type: 'system', is_system: true, text: \`${fieldLabel} changed: ${oldDisplay} → ${newDisplay} by ${cu?.full_name || 'Unknown'}\` }).catch(err => console.error('[ProjectDetail] Journal cost-change auto-entry failed:', err));`
       NOTE: the existing block computes `oldDisplay`/`newDisplay` INSIDE the `if (recipients.length > 0)` sub-block. The journal entry must fire regardless of recipients — so compute the display strings (or move/duplicate the fieldLabel/oldDisplay/newDisplay computation) at the top of the `if (NOTIF19_COST_FIELDS...)` block so both the notification and the journal entry can use them. The journal entry is fire-and-forget (`.catch()`, never `await`) so it never blocks saveField (return true at line ~1288 must remain reachable and unchanged).
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js && node -e "const c=require('fs').readFileSync('app/views/project-detail.js','utf8'); const need=['Status advanced to For Proposal by','Mobilization started by','Project started by','Project marked Completed by','Journal cost-change auto-entry failed']; const miss=need.filter(s=>!c.includes(s)); if(miss.length){console.error('MISSING:',miss);process.exit(1);} const sysCalls=(c.match(/_addActivityEntry\\((currentProject\\.id|projectId), \\{ type: 'system'/g)||[]).length; if(sysCalls<5){console.error('expected >=5 system auto-entry calls (4 gates + 1 cost), found:',sysCalls);process.exit(1);} console.log('PASS', sysCalls);"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/project-detail.js` exits 0
    - Each of the four gate functions contains an `_addActivityEntry(projectId, { type: 'system', is_system: true, ...})` call placed after `addProjectAuditEntry`
    - The four gate messages contain `Status advanced to For Proposal by`, `Mobilization started by`, `Project started by`, `Project marked Completed by`
    - saveField's cost block calls `_addActivityEntry(currentProject.id, ...)` as fire-and-forget with `.catch(` and the text `${fieldLabel} changed:` (before→after delta), and this call is NOT gated behind `if (recipients.length > 0)`
    - `return true;` at the end of saveField remains present and reachable (cost-block is not awaited)
  </acceptance_criteria>
  <done>4 gate functions + saveField cost block auto-post system Feed entries; node --check passes; saveField still returns true non-blocking.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: procurement.js — PO Delivered auto-entry to the project Feed</name>
  <read_first>
    - app/views/procurement.js lines 7-8 (confirm addDoc, serverTimestamp, collection, db, escapeHTML imports present — they are)
    - app/views/procurement.js lines ~7900-7965 (the updatePOStatus Delivered handler: `if (newStatus === 'Delivered' && !isSubcon)` and the NOTIF-18 block; `poRef` in scope; `poId` param)
    - .planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-PATTERNS.md ("procurement.js — PO Delivered auto-entry" — exact insert shape)
    - .planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-CONTEXT.md (D-08 PO Delivered references PO ID + supplier)
  </read_first>
  <action>
    Inside the existing `if (newStatus === 'Delivered' && !isSubcon) { ... }` block in updatePOStatus, AFTER the NOTIF-18 notification try/catch, add a SEPARATE try/catch so journal failure is independent of notification failure:
    - `const poDocFresh2 = await getDoc(poRef);` `const d = poDocFresh2.data() || {};`
    - if `d.project_id`: `await addDoc(collection(db, 'projects', d.project_id, 'activity_entries'), { type: 'system', is_system: true, text: \`PO ${escapeHTML(d.po_id || poId)} from ${escapeHTML(d.supplier_name || 'Unknown Supplier')} marked Delivered\`, created_by_uid: window.getCurrentUser?.()?.uid ?? '', created_by_name: window.getCurrentUser?.()?.full_name || 'System', created_at: serverTimestamp() });`
    - catch logs `[Procurement] Phase 101 PO Delivered journal entry failed:` and swallows (never block the status update).
    If a fresh `poDocFresh` already exists in scope from the NOTIF-18 block, reuse it instead of a second getDoc — but ensure the field used to scope the project is the PO's `project_id`. If POs store the project linkage under a different field name (verify by reading the PO doc shape in the surrounding code — e.g. `project_id` vs `mrf_id`), use the correct field; the Feed must land on the project that owns the PO. If no project linkage field exists on the PO, skip the write (guard with the `if (d.project_id)`).
  </action>
  <verify>
    <automated>node --check app/views/procurement.js && node -e "const c=require('fs').readFileSync('app/views/procurement.js','utf8'); const need=[\"'activity_entries'\",'Phase 101 PO Delivered journal entry failed','marked Delivered',\"type: 'system'\",'is_system: true']; const miss=need.filter(s=>!c.includes(s)); if(miss.length){console.error('MISSING:',miss);process.exit(1);} if(!/newStatus === 'Delivered'/.test(c)){console.error('Delivered guard not found');process.exit(1);} console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/procurement.js` exits 0
    - procurement.js contains an `addDoc(collection(db, 'projects', <projectField>, 'activity_entries'), {...})` call inside the `if (newStatus === 'Delivered' && !isSubcon)` block
    - the journal entry text contains `marked Delivered` and references the PO id and supplier name (both via `escapeHTML(`)
    - the entry sets `type: 'system'`, `is_system: true`, `created_at: serverTimestamp()`
    - the journal write is in its own try/catch that logs `[Procurement] Phase 101 PO Delivered journal entry failed:` and does not rethrow (swallows)
    - the write is guarded so it is skipped when the PO has no project linkage field
  </acceptance_criteria>
  <done>PO Delivered (non-subcon) auto-posts a system entry to the owning project's Feed in its own try/catch; node --check passes.</done>
</task>

</tasks>

<verification>
- `node --check` passes on both project-detail.js and procurement.js.
- Static greps confirm 4 gate messages, cost-delta entry, and the Delivered journal write.
- Functional (browser UAT): advance a project through each gate → a system entry appears in the Feed for each transition; edit contract_cost/budget → a "changed: X → Y" system entry appears; mark a non-subcon PO Delivered → a "PO ... marked Delivered" system entry appears in that project's Feed.
</verification>

<success_criteria>
All four auto-entry sources (4 gates + cost delta + PO Delivered) post system Feed entries; saveField and updatePOStatus remain non-blocking on journal failure; personnel/billing/RFP-Paid events untouched (D-10).
</success_criteria>

<output>
Create `.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-05-SUMMARY.md` when done.
</output>
