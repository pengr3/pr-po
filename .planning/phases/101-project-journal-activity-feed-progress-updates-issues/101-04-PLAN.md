---
phase: 101-project-journal-activity-feed-progress-updates-issues
plan: 04
type: execute
wave: 3
depends_on: ["101-03"]
files_modified: [app/views/project-detail.js]
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can submit a Progress Update (pct_complete, summary, blockers, next_milestone) which persists and lists newest-first"
    - "User can log an Issue (type, title, description) which persists in the punch list"
    - "Issues can be filtered by All / Open / Resolved via filter chips"
    - "Resolving an issue requires resolution notes, flips status to resolved, and auto-posts a system Feed entry"
    - "Resolved issues can be re-opened, clearing resolution fields and auto-posting a re-open Feed entry"
    - "Progress form and issue write controls are hidden when the project is Completed (read-only)"
  artifacts:
    - path: "app/views/project-detail.js"
      provides: "Progress Updates tab + Issues tab (filter chips, log form, resolve/reopen workflow)"
      contains: "window.resolveIssue"
  key_links:
    - from: "project-detail.js resolveIssue()"
      to: "_addActivityEntry (Plan 03) system entry"
      via: "await _addActivityEntry after updateDoc resolves issue"
      pattern: "resolveIssue"
---

<objective>
Fill the two remaining Journal tabs in `project-detail.js`, replacing the Plan 03 placeholders: the **Progress Updates** tab (structured check-in form + history list) and the **Issues** tab (filter chips + log form + punch list + open/resolved workflow). Issue resolution requires notes (D-11) and auto-posts a system Feed entry via the Plan 03 `_addActivityEntry` primitive (D-12); resolved issues re-open and auto-post too (D-13). All write surfaces respect the read-only gate from Plan 03.

Purpose: Complete the 3-tab journal so all manual user surfaces work end-to-end.
Output: Progress + Issues tab builders, submit handlers, filter state, resolve/reopen handlers added and window-registered; Plan 03 placeholders replaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-CONTEXT.md
@.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-PATTERNS.md

<interfaces>
From Plan 03 (now in project-detail.js):
- `_addActivityEntry(projectId, { type, text, is_system })` — shared write primitive. Call with type:'system', is_system:true for resolution/reopen auto-entries.
- `_buildJournalPanelHtml(project)` — builds the panel; this plan replaces the `<div data-journal-placeholder="progress">` and `<div data-journal-placeholder="issues">` inner content with real builders `_buildProgressTabHtml(project, isReadOnly)` and `_buildIssuesTabHtml(project, isReadOnly)`.
- `_renderJournalPanelInPlace()` — re-renders the panel; called by the issues/progress snapshot listeners (already wired in Plan 03 ensureJournalListeners).
- Module state: `journalProgressUpdates[]`, `journalIssues[]`, `journalIssueFilter` (declared in Plan 03), `_activeJournalTab`.
- Read-only gate: `isReadOnly = project.project_status === 'Completed'`.

Issue schema (D-17): issue_type:'delay'|'change_order'|'site_issue'|'client_request'; title; description; status:'open'|'resolved'; resolution_notes:string|null; resolved_at:Timestamp|null; resolved_by_uid:string|null; created_at; created_by_uid; created_by_name.
Progress schema (D-17): pct_complete:number; summary; blockers; next_milestone; created_at; created_by_uid; created_by_name.

Required-notes pattern analog: finance.js rejectBillingRequest uses `const reason=(window.prompt('...required:')||'').trim(); if(!reason){showToast(...);return;}`.
CSS classes available (Plan 01): .journal-progress-form, .journal-progress-row, .journal-pct-input, .journal-progress-card, .journal-issue-filters, .journal-filter-chip[.active], .journal-issue-form, .journal-issue, .journal-issue-type-chip[--delay|--change_order|--site_issue|--client_request], .journal-issue-status--open, .journal-issue-status--resolved, .journal-issue-resolve-btn, .journal-issue-reopen-btn, .journal-resolution-notes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Progress Updates tab — form, submit handler, history list</name>
  <read_first>
    - app/views/project-detail.js (the Plan 03 additions: _buildJournalPanelHtml with the `data-journal-placeholder="progress"` slot, _addActivityEntry, ensureJournalListeners, attachWindowFunctions, destroy window-delete block)
    - .planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-CONTEXT.md (D-17 progress_updates schema, D-19 manual-only no Gantt)
    - .planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-PATTERNS.md (escapeHTML usage, optimistic append, window registration patterns)
  </read_first>
  <action>
    1. Add `_buildProgressTabHtml(project, isReadOnly)`:
       - When `!isReadOnly`, render `.journal-progress-form` with: a `.journal-pct-input` number input id `journalProgPct` (min 0, max 100, step 1), a summary textarea id `journalProgSummary`, a blockers textarea id `journalProgBlockers`, a next-milestone text input id `journalProgNext`, and a `.journal-post-btn` `onclick="window.submitProgressUpdate()"`.
       - Then a history list rendering `journalProgressUpdates` (newest-first, already ordered by the listener) via `_renderProgressCard(u)` → `.journal-progress-card`: a pct badge (`${Number(u.pct_complete)||0}%`), a meta line (timestamp + `escapeHTML(u.created_by_name||'Unknown')`), and `escapeHTML` on summary / blockers / next_milestone (each labeled).
       - Wire it into `_buildJournalPanelHtml` by replacing the `data-journal-placeholder="progress"` slot with `${_buildProgressTabHtml(project, isReadOnly)}`.

    2. Add `submitProgressUpdate()` (async): read the four fields. Require at least `summary` non-empty (trim) — else `showToast('Add a progress summary.','error')` return. Parse pct via `Math.max(0, Math.min(100, parseInt(value,10) || 0))`. `await addDoc(collection(db,'projects',currentProject.id,'progress_updates'), { pct_complete, summary, blockers, next_milestone, created_by_uid: cu?.uid??'', created_by_name: cu?.full_name||cu?.email||'Unknown', created_at: serverTimestamp() })`. On success showToast success; the snapshot listener re-renders. On error showToast failure + console.error `[ProjectDetail/Journal] submitProgressUpdate failed:`.

    3. Register `window.submitProgressUpdate = submitProgressUpdate;` in attachWindowFunctions; `delete window.submitProgressUpdate;` in destroy().
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js && node -e "const c=require('fs').readFileSync('app/views/project-detail.js','utf8'); const need=['function _buildProgressTabHtml','function _renderProgressCard','function submitProgressUpdate','journalProgPct','journalProgSummary',\"'progress_updates'\",'pct_complete','window.submitProgressUpdate','delete window.submitProgressUpdate','_buildProgressTabHtml(project']; const miss=need.filter(s=>!c.includes(s)); if(miss.length){console.error('MISSING:',miss);process.exit(1);} console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/project-detail.js` exits 0
    - `_buildProgressTabHtml` is wired into `_buildJournalPanelHtml` (replacing the progress placeholder)
    - `submitProgressUpdate` writes to `collection(db, 'projects', currentProject.id, 'progress_updates')` with fields `pct_complete`, `summary`, `blockers`, `next_milestone`, `created_at: serverTimestamp()`
    - pct is clamped to 0..100
    - `_renderProgressCard` escapes `summary`, `blockers`, `next_milestone`, `created_by_name` via `escapeHTML(`
    - `submitProgressUpdate` registered on window and deleted in destroy()
    - When isReadOnly, `_buildProgressTabHtml` omits `.journal-progress-form`
  </acceptance_criteria>
  <done>Progress tab form submits and persists; history lists newest-first; read-only hides the form; node --check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Issues tab — filter chips, log form, punch list, resolve/reopen workflow</name>
  <read_first>
    - app/views/project-detail.js (Plan 03 additions + Task 1 additions: _addActivityEntry, journalIssues, journalIssueFilter, _buildJournalPanelHtml issues placeholder, attachWindowFunctions, destroy)
    - .planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-CONTEXT.md (D-11 resolve requires notes, D-12 resolve auto-posts, D-13 reopen clears + auto-posts, D-14 open<->resolved, D-16 read-only)
    - .planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-PATTERNS.md ("Issue resolution — required-notes pattern" — _issueSeqNum + resolveIssue shape; escapeHTML usage)
  </read_first>
  <action>
    1. Add `_issueSeqNum(issueId)`: derive a stable "#N" display number. Since issues list is ordered created_at desc, compute N by ranking issueId within `journalIssues` sorted by created_at ASCENDING (oldest = #1). Return the 1-based index; fall back to a short id slice if not found.

    2. Add `_buildIssuesTabHtml(project, isReadOnly)`:
       - `.journal-issue-filters` with three `.journal-filter-chip` buttons (All / Open / Resolved), the one matching `journalIssueFilter` gets `active`, each `onclick="window.setIssueFilter('all'|'open'|'resolved')"`.
       - When `!isReadOnly`: `.journal-issue-form` with an issue-type `<select>` id `journalIssueType` (options delay/change_order/site_issue/client_request), title input id `journalIssueTitle`, description textarea id `journalIssueDesc`, and a `.journal-post-btn` `onclick="window.submitNewIssue()"`.
       - Punch list: filter `journalIssues` by `journalIssueFilter` (all → no filter; open → status==='open'; resolved → status==='resolved'), render each via `_renderIssueRow(issue, isReadOnly)`.

    3. Add `_renderIssueRow(issue, isReadOnly)` → `.journal-issue`: `#${_issueSeqNum(issue.id)}` label; `.journal-issue-type-chip journal-issue-type-chip--${issue.issue_type}` with a human label; `escapeHTML(issue.title)`; `escapeHTML(issue.description)`; a `.journal-issue-status--open` or `--resolved` badge from `issue.status`. If resolved, show `.journal-resolution-notes` with `escapeHTML(issue.resolution_notes||'')` and resolver name. Action buttons (only when `!isReadOnly`): if open → `.journal-issue-resolve-btn` `onclick="window.resolveIssue('${escapeHTML(issue.id)}')"`; if resolved → `.journal-issue-reopen-btn` `onclick="window.reopenIssue('${escapeHTML(issue.id)}')"`.

    4. Wire `_buildIssuesTabHtml` into `_buildJournalPanelHtml` (replace the `data-journal-placeholder="issues"` slot).

    5. Add `setIssueFilter(f)`: set `journalIssueFilter = f;` then `_renderJournalPanelInPlace();`. Register on window + delete in destroy.

    6. Add `submitNewIssue()` (async): read type/title/desc; require title non-empty (`showToast('Add an issue title.','error')` return). `await addDoc(collection(db,'projects',currentProject.id,'issues'), { issue_type, title, description, status:'open', resolution_notes:null, resolved_at:null, resolved_by_uid:null, created_by_uid:cu?.uid??'', created_by_name:cu?.full_name||cu?.email||'Unknown', created_at:serverTimestamp() })`. showToast on success/failure.

    7. Add `resolveIssue(issueId)` (async) per PATTERNS: `const notes=(window.prompt('Resolution notes (required):')||'').trim(); if(!notes){showToast('Resolution notes are required.','error');return;}`. `await updateDoc(doc(db,'projects',currentProject.id,'issues',issueId), { status:'resolved', resolution_notes:notes, resolved_at:serverTimestamp(), resolved_by_uid:cu?.uid??'' })`. Then D-12 auto-entry: find the issue in `journalIssues`, `await _addActivityEntry(currentProject.id, { type:'system', is_system:true, text:\`Issue #${_issueSeqNum(issueId)} (${issue.issue_type} — ${issue.title}) resolved by ${cu?.full_name||'Unknown'}\` })`. showToast success; on error console.error + showToast failure.

    8. Add `reopenIssue(issueId)` (async) per D-13: `await updateDoc(doc(db,'projects',currentProject.id,'issues',issueId), { status:'open', resolution_notes:null, resolved_at:null, resolved_by_uid:null })`. Then auto-entry: `await _addActivityEntry(currentProject.id, { type:'system', is_system:true, text:\`Issue #${_issueSeqNum(issueId)} re-opened by ${cu?.full_name||'Unknown'}\` })`. showToast success/failure.

    9. Register `window.setIssueFilter`, `window.submitNewIssue`, `window.resolveIssue`, `window.reopenIssue` in attachWindowFunctions; delete all four in destroy().
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js && node -e "const c=require('fs').readFileSync('app/views/project-detail.js','utf8'); const need=['function _buildIssuesTabHtml','function _renderIssueRow','function _issueSeqNum','function setIssueFilter','function submitNewIssue','window.resolveIssue','window.reopenIssue','Resolution notes (required)',\"status: 'resolved'\",\"status: 'open'\",'resolution_notes: null','_buildIssuesTabHtml(project','delete window.resolveIssue','delete window.reopenIssue','delete window.submitNewIssue','delete window.setIssueFilter']; const miss=need.filter(s=>!c.includes(s)); if(miss.length){console.error('MISSING:',miss);process.exit(1);} const r=(c.match(/_addActivityEntry/g)||[]).length; if(r<4){console.error('expected _addActivityEntry called for resolve+reopen (>=4 total refs incl def):',r);process.exit(1);} console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/project-detail.js` exits 0
    - `_buildIssuesTabHtml` is wired into `_buildJournalPanelHtml` (replacing the issues placeholder) and renders three `.journal-filter-chip` buttons
    - `submitNewIssue` writes status `'open'` with `resolution_notes: null`, `resolved_at: null`, `resolved_by_uid: null`
    - `resolveIssue` blocks on empty notes via `window.prompt('Resolution notes (required):')` + early return, sets `status: 'resolved'` with `resolved_at: serverTimestamp()`, and calls `_addActivityEntry` with `type: 'system'` (D-12)
    - `reopenIssue` sets `status: 'open'`, clears `resolution_notes`/`resolved_at`/`resolved_by_uid` to null, and calls `_addActivityEntry` with `type: 'system'` (D-13)
    - `_renderIssueRow` escapes `issue.title`, `issue.description`, `issue.resolution_notes`, and `issue.id` via `escapeHTML(`
    - punch list filters by `journalIssueFilter` (all/open/resolved)
    - resolve/reopen buttons are omitted when isReadOnly
    - `setIssueFilter`, `submitNewIssue`, `resolveIssue`, `reopenIssue` all registered on window and deleted in destroy()
  </acceptance_criteria>
  <done>Issues tab: log/filter/resolve/reopen all work; resolution requires notes; resolve + reopen auto-post system Feed entries; read-only hides write controls; node --check passes.</done>
</task>

</tasks>

<verification>
- `node --check app/views/project-detail.js` passes.
- Static greps confirm both tab builders wired, schemas correct, required-notes guard, auto-entry calls, escapeHTML, window symmetry.
- Functional (browser UAT): submit a progress update (persists, newest-first); log an issue (appears open); filter chips narrow the list; resolve an issue (prompts for notes, blocks empty, flips badge, a system entry appears in the Feed); re-open (clears notes, re-opens, Feed entry); Completed project hides all write controls.
</verification>

<success_criteria>
Progress Updates and Issues tabs are fully functional with the open/resolved workflow and auto-Feed integration; all journal write surfaces respect read-only.
</success_criteria>

<output>
Create `.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-04-SUMMARY.md` when done.
</output>
