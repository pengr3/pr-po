---
phase: 101-project-journal-activity-feed-progress-updates-issues
plan: 03
type: execute
wave: 2
depends_on: ["101-01", "101-02"]
files_modified: [app/views/project-detail.js]
autonomous: true
requirements: []
must_haves:
  truths:
    - "On For Mobilization / On-going / Completed projects, a Journal panel renders below the Info+Financial+Plan bottom row"
    - "Panel is hidden entirely for For Inspection, For Proposal, Loss, and all pre-mobilization statuses"
    - "Completed projects show the panel in read-only mode (no Feed composer)"
    - "User can pick a tag type, type a note, click Post, and the entry appears (optimistically then reconciled) and persists across refresh"
    - "Three real-time listeners (activity/progress/issues) attach on init and tear down in destroy()"
    - "Switching tabs (Activity/Progress/Issues) is DOM-only and updates the active tab visual"
  artifacts:
    - path: "app/views/project-detail.js"
      provides: "Journal panel render + listeners + tab switching + Activity Feed composer/CRUD"
      contains: "function _addActivityEntry"
  key_links:
    - from: "project-detail.js renderProjectDetail()"
      to: "#projectJournalPanel (Plan 01 CSS)"
      via: "_buildJournalPanelHtml injected after #projectDetailBottomRow"
      pattern: "projectJournalPanel"
    - from: "project-detail.js ensureJournalListeners()"
      to: "projects/{id}/activity_entries onSnapshot"
      via: "query(collection(...), orderBy('created_at','desc'), limit(50))"
      pattern: "activity_entries"
---

<objective>
Build the Project Journal panel shell in `project-detail.js`: status-gated render injection, the three real-time subcollection listeners, DOM-only tab switching, and the fully working **Activity Feed** tab (tag-type composer + Post + optimistic append + system-vs-manual entry rendering). The `_addActivityEntry(projectId, {type, text, is_system})` helper created here is the shared write primitive that Plan 04 (issue resolution) and Plan 05 (auto-entries) call.

Purpose: Stand up the panel and the most-active tab plus the write helper, so Plan 04 (Progress/Issues) and Plan 05 (auto-entries) build against a live contract.
Output: Journal panel + Feed tab live in project-detail.js; `_addActivityEntry`, `ensureJournalListeners`, `_buildJournalPanelHtml`, `_renderJournalPanelInPlace`, `switchJournalTab`, `postActivityEntry` defined; listeners + window functions symmetric across init/destroy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-CONTEXT.md
@.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-PATTERNS.md

<interfaces>
project-detail.js firebase.js import (line 6) currently exports:
db, collection, doc, getDoc, updateDoc, deleteDoc, onSnapshot, query, where, getDocs, writeBatch, getAggregateFromServer, sum, count, addDoc, serverTimestamp
— MUST add `orderBy` and `limit` (both exported by app/firebase.js, confirmed). escapeHTML + formatCurrency already imported from utils.js.

Current-user accessor (used throughout): `const cu = window.getCurrentUser?.();` → cu.uid, cu.full_name, cu.email.

Module-scope `currentProject` (line 15) holds the loaded project; `currentProject.project_status` is the status string ('For Mobilization' | 'On-going' | 'Completed' | 'For Inspection' | 'For Proposal' | 'Loss' | ...).

Render injection point: in renderProjectDetail() (line 491), the bottom row closes at:
  <div id="projectDetailBottomRow" style="margin-bottom:0.75rem;"> ... </div>   (lines 669-672)
The Delete button block follows at line 674. Inject the journal panel BETWEEN them.

Idempotent listener analog: ensureBillingRequestsListener (line 298). Destroy teardown analog: lines 372-374. addDoc write analog: addProjectAuditEntry (line 2343). attachWindowFunctions at line 2365; destroy delete-window block at lines 402-414.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Imports, module state, _addActivityEntry helper, and the listener attach/teardown</name>
  <read_first>
    - app/views/project-detail.js lines 6-13 (import block), 15-43 (module-scope listener vars), 298-345 (ensureBillingRequestsListener idempotent pattern), 347-414 (destroy teardown + window-delete), 2343-2362 (addProjectAuditEntry addDoc shape)
    - .planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-PATTERNS.md (sections: "Imports pattern", "Module-scope listener variables", "Idempotent listener attach pattern", "Destroy teardown pattern", "Subcollection addDoc write pattern")
  </read_first>
  <action>
    1. Add `orderBy` and `limit` to the firebase.js import on line 6 (append to the existing destructured list; both are exported by app/firebase.js).

    2. Add module-scope state alongside the existing listener vars (near line 15-43):
       `let journalActivityUnsub = null;` `let journalProgressUnsub = null;` `let journalIssuesUnsub = null;`
       `let journalActivityEntries = [];` `let journalProgressUpdates = [];` `let journalIssues = [];`
       `let _activeJournalTab = 'activity';`  // default tab per CONTEXT Claude's Discretion
       `let journalIssueFilter = 'all';`  // Plan 04 uses this; declare here so destroy() resets it in one place

    3. Add the shared write helper `_addActivityEntry(projectId, { type, text, is_system = false })` mirroring addProjectAuditEntry: `await addDoc(collection(db, 'projects', projectId, 'activity_entries'), { type, text, is_system, created_by_uid: cu?.uid ?? '', created_by_name: cu?.full_name || cu?.email || 'Unknown', created_at: serverTimestamp() })` wrapped in try/catch that logs `[ProjectDetail/Journal] _addActivityEntry failed:`. cu = `window.getCurrentUser?.()`.

    4. Add `ensureJournalListeners()` mirroring ensureBillingRequestsListener: guard `if (!currentProject?.id) return;`. For each of the three subcollections, if the unsub var is null, set it via `onSnapshot(query(collection(db,'projects',projectId,'<sub>'), orderBy('created_at','desc'), limit(50)), snap => { <arr>=[]; snap.forEach(d => <arr>.push({id:d.id,...d.data()})); _renderJournalPanelInPlace(); }, err => console.error('[ProjectDetail/Journal] <sub> snapshot error:', err))`. Subcollections: `activity_entries`→journalActivityEntries, `progress_updates`→journalProgressUpdates, `issues`→journalIssues. (All three listeners run simultaneously so tab switching is DOM-only — no per-switch listener churn.)

    5. Call `ensureJournalListeners()` from inside the existing init()/render flow at the same place ensureBillingRequestsListener is invoked — but ONLY when the panel is visible (status in ['For Mobilization','On-going','Completed']). Guard the call so listeners do not attach for hidden-panel statuses.

    6. In destroy(): add the three-line teardown per listener (`if (journalActivityUnsub){ try{journalActivityUnsub();}catch(e){} } journalActivityUnsub=null; journalActivityEntries=[];`) for activity/progress/issues, and reset `_activeJournalTab='activity'; journalIssueFilter='all';`.
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js && node -e "const c=require('fs').readFileSync('app/views/project-detail.js','utf8'); const need=['orderBy','limit','journalActivityUnsub','journalProgressUnsub','journalIssuesUnsub','function _addActivityEntry','function ensureJournalListeners',\"orderBy('created_at', 'desc')\"]; const miss=need.filter(s=>!c.includes(s)); if(miss.length){console.error('MISSING:',miss);process.exit(1);} const a=(c.match(/journalActivityUnsub/g)||[]).length; if(a<3){console.error('journalActivityUnsub not attached+torn-down (need set, null in destroy, guard):',a);process.exit(1);} console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/project-detail.js` exits 0
    - import line 6 includes `orderBy` and `limit`
    - `project-detail.js` contains `function _addActivityEntry(` and its addDoc targets `collection(db, 'projects', projectId, 'activity_entries')` with `created_at: serverTimestamp()`
    - `project-detail.js` contains `function ensureJournalListeners(` using `orderBy('created_at', 'desc')` and `limit(50)`
    - `journalActivityUnsub`, `journalProgressUnsub`, `journalIssuesUnsub` each appear in both an onSnapshot assignment AND a destroy() `= null` teardown
    - destroy() resets `_activeJournalTab = 'activity'` and `journalIssueFilter = 'all'`
  </acceptance_criteria>
  <done>Imports extended, journal state + _addActivityEntry + ensureJournalListeners added, listeners torn down in destroy(); node --check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Panel builders, status gating, tab switching, and the Activity Feed composer + CRUD</name>
  <read_first>
    - app/views/project-detail.js lines 491-512 (renderProjectDetail head: canEdit, user), 668-681 (bottom-row + Delete button injection region), 284-296 (in-place partial re-render pattern for projectPlanCard), 2365-2420 (attachWindowFunctions registration), 2478-2487 (lcSwitchTab DOM-only switch analog)
    - .planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-PATTERNS.md (sections: "In-place partial re-render pattern", "DOM-only tab switching pattern", "Read-only mode gate pattern", "escapeHTML usage pattern", "Optimistic DOM append", "Window function registration pattern")
    - styles/views.css (the Plan 01 journal block — use its exact class names: .project-journal-panel, .project-journal-panel--readonly, .journal-tab-bar, .journal-tab-btn[.active], .journal-tab-panel, .journal-composer, .journal-tag-select, .journal-composer-textarea, .journal-post-btn, .journal-feed-list, .journal-entry, .journal-entry--system, .journal-entry-meta, .journal-entry-text, .journal-entry-tag[--update|--milestone|--client|--system])
  </read_first>
  <action>
    1. Add `_buildJournalPanelHtml(project)`:
       - Compute gating: `const JOURNAL_WRITE_STATUSES = ['For Mobilization', 'On-going'];` `const isVisible = JOURNAL_WRITE_STATUSES.includes(project.project_status) || project.project_status === 'Completed';` `const isReadOnly = project.project_status === 'Completed';`
       - If `!isVisible`, return `''` (panel hidden entirely — D-03).
       - Else return a root `<div id="projectJournalPanel" class="project-journal-panel${isReadOnly ? ' project-journal-panel--readonly' : ''}">` containing: a `.journal-panel-title` heading ("Project Journal"); a `.journal-tab-bar` with three `.journal-tab-btn` buttons (id `journalTabBtn-activity|progress|issues`, the active one gets `active`, each `onclick="window.switchJournalTab('activity'|'progress'|'issues')"`); three `.journal-tab-panel` wrappers (id `journalTab-activity|progress|issues`) with inactive ones `style="display:none"` based on `_activeJournalTab`.
       - In the activity panel: render the composer (`.journal-composer`) ONLY when `!isReadOnly` — a `.journal-tag-select` with id `journalTagSelect` and options update/milestone/issue/client (values matching D-17 type enum: 'update','milestone','issue','client'), a `.journal-composer-textarea` id `journalComposerText`, and a `.journal-post-btn` `onclick="window.postActivityEntry()"`. Then a `.journal-feed-list` rendering `journalActivityEntries` via a `_renderFeedEntry(entry)` helper.
       - Plan 04 fills the progress/issues panel bodies; for THIS plan, render those two `.journal-tab-panel` wrappers with a placeholder `<div data-journal-placeholder="progress|issues"></div>` so the panel structure exists and Plan 04 replaces only the inner content.

    2. Add `_renderFeedEntry(entry)` returning a `.journal-entry` (add `journal-entry--system` when `entry.is_system`): a `.journal-entry-meta` line with the formatted timestamp (handle Firestore Timestamp via `entry.created_at?.seconds` → `new Date(seconds*1000)`, and the optimistic-entry shape) and `escapeHTML(entry.created_by_name || 'Unknown')`; a `.journal-entry-tag journal-entry-tag--<type>` pill; and `.journal-entry-text` with `escapeHTML(entry.text)`. ALL user text through escapeHTML.

    3. Add `_renderJournalPanelInPlace()`: get `#projectJournalPanel`; if absent or no currentProject, return; build `_buildJournalPanelHtml(currentProject)` into a temp div and `el.replaceWith(tmp.firstElementChild)`. Guard: if the builder returns '' (panel hidden) but the element exists, remove it; if the panel was hidden and is now visible, this in-place path won't find an anchor — that case only changes on a full status-change re-render which already calls renderProjectDetail().

    4. Inject the panel into renderProjectDetail(): immediately after the `#projectDetailBottomRow` closing `</div>` (line 672) and before the Delete button block (line 674), add `${_buildJournalPanelHtml(currentProject)}`.

    5. Add `switchJournalTab(tab)` (DOM-only, per lcSwitchTab analog): set `_activeJournalTab = tab`; for each of ['activity','progress','issues'] toggle `#journalTab-<t>` display and `#journalTabBtn-<t>` active class.

    6. Add `postActivityEntry()` (async): read `#journalTagSelect` value and `#journalComposerText` value (trimmed); if text empty, `showToast('Enter a note before posting.','error')` and return. Optimistic append: `journalActivityEntries.unshift({ id:'_optimistic', type, text, is_system:false, created_by_name: cu?.full_name||cu?.email||'Unknown', created_at:{ seconds: Date.now()/1000 } })`, then `_renderJournalPanelInPlace()`, then clear the textarea. Then `await _addActivityEntry(currentProject.id, { type, text, is_system:false })` — the onSnapshot reconciles the real doc (replaces the `_optimistic` placeholder on next snapshot). On error showToast failure.

    7. Register in attachWindowFunctions(): `window.switchJournalTab = switchJournalTab; window.postActivityEntry = postActivityEntry;`. Delete both in destroy().
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js && node -e "const c=require('fs').readFileSync('app/views/project-detail.js','utf8'); const need=['function _buildJournalPanelHtml','function _renderFeedEntry','function _renderJournalPanelInPlace','function switchJournalTab','function postActivityEntry','id=\"projectJournalPanel\"','project-journal-panel--readonly','journalTagSelect','journalComposerText','window.switchJournalTab','window.postActivityEntry','_buildJournalPanelHtml(currentProject)']; const miss=need.filter(s=>!c.includes(s)); if(miss.length){console.error('MISSING:',miss);process.exit(1);} const reg=(c.match(/window\\.postActivityEntry/g)||[]).length, del=(c.match(/delete window\\.postActivityEntry/g)||[]).length; if(reg<2||del<1){console.error('postActivityEntry register/delete asymmetry',reg,del);process.exit(1);} console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/project-detail.js` exits 0
    - `_buildJournalPanelHtml` returns `''` when status is not in ['For Mobilization','On-going','Completed'] (panel hidden — D-03)
    - `_buildJournalPanelHtml` adds `project-journal-panel--readonly` class and omits the `.journal-composer` when status === 'Completed' (D-02/D-16)
    - renderProjectDetail() contains `${_buildJournalPanelHtml(currentProject)}` injected after `#projectDetailBottomRow`
    - `_renderFeedEntry` wraps `entry.text`, `entry.created_by_name` in `escapeHTML(`
    - `postActivityEntry` performs optimistic `journalActivityEntries.unshift(` before `await _addActivityEntry`
    - `switchJournalTab` and `postActivityEntry` are registered on window in attachWindowFunctions and `delete`d in destroy() (register/delete symmetric)
  </acceptance_criteria>
  <done>Panel renders status-gated below the bottom row; Feed composer posts (optimistic + persist); tab switching works DOM-only; window functions symmetric; node --check passes.</done>
</task>

</tasks>

<verification>
- `node --check app/views/project-detail.js` passes.
- Static greps confirm builders, escapeHTML usage, optimistic append, window register/delete symmetry.
- Functional (browser UAT, zero-build): panel appears on For Mobilization/On-going; hidden on For Inspection/For Proposal/Loss; read-only on Completed; posting a tagged note appears and persists across refresh; tab buttons switch panels.
</verification>

<success_criteria>
Journal panel + Activity Feed tab are live and status-gated; _addActivityEntry write primitive exists for Plan 04/05; three listeners attach on visible statuses and tear down in destroy().
</success_criteria>

<output>
Create `.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-03-SUMMARY.md` when done.
</output>
