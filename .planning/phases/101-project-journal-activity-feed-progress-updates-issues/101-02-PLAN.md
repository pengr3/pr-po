---
phase: 101-project-journal-activity-feed-progress-updates-issues
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [firestore.rules]
autonomous: false
requirements: []
must_haves:
  truths:
    - "activity_entries and progress_updates subcollections are create-only for any active user; delete is admin-only (super_admin/operations_admin) per spike-032 contract"
    - "issues subcollection allows create and update (for open<->resolved) for any active user; delete is admin-only (super_admin/operations_admin) per spike-032 contract"
    - "All three subcollections allow read for any active user"
  artifacts:
    - path: "firestore.rules"
      provides: "Three Phase 101 subcollection rule blocks inside match /projects/{projectId}"
      contains: "match /activity_entries/{entryId}"
  key_links:
    - from: "firestore.rules activity_entries create rule"
      to: "_addActivityEntry addDoc in project-detail.js + procurement.js (Plan 03/05)"
      via: "allow create: if isActiveUser()"
      pattern: "match /activity_entries"
---

<objective>
Add three new Firestore subcollection rule blocks — `activity_entries`, `progress_updates`, `issues` — inside the existing `match /projects/{projectId}` block in `firestore.rules`, then deploy. Per D-15 any active user with project access may post (no role-gating within the journal); per the append-only audit philosophy, `activity_entries` and `progress_updates` are create-only, `issues` allows update for open↔resolved transitions. Per the spike-032 validated design contract, **delete is permitted for admins only** — mirroring the `baselines` block's admin-delete pattern.

Purpose: Without these rules, every journal write (Plan 03/04/05) throws "Missing or insufficient permissions" — even for Super Admin. Rules must land and deploy before any panel testing.
Output: Three rule blocks appended after the `audit_log` block (firestore.rules line ~267); rules deployed to production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-CONTEXT.md
@.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-PATTERNS.md
</context>

<notes>
**Admin-delete helper:** This codebase has NO `isAdmin()` function in firestore.rules. The validated spike-032 "admin delete" contract is expressed here using `hasRole(['super_admin', 'operations_admin'])` — the SAME expression the existing `baselines` block (line ~256) uses for its `allow delete`. Do NOT introduce a new `isAdmin()` helper; reuse `hasRole([...])`. The available helpers are `isActiveUser()`, `hasRole(roles)`, and `isRole(role)` (firestore.rules lines 56–68).
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Add three subcollection rule blocks inside match /projects/{projectId}</name>
  <read_first>
    - firestore.rules lines 56-68 (the `isActiveUser()` / `hasRole(roles)` helper definitions — confirm `hasRole(['super_admin', 'operations_admin'])` is the admin expression to reuse)
    - firestore.rules lines 200-268 (the `match /projects/{projectId}` block, the `baselines` block at ~252 — note its `allow delete: if hasRole([...])` admin-delete line — and the `audit_log` block at ~259-267; the new blocks insert immediately AFTER audit_log, still inside the projects match, before the closing brace at line 268)
    - .planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-PATTERNS.md (the "firestore.rules — Three new subcollection blocks" section; NOTE: the pattern doc shows `allow delete: if false;` — OVERRIDE that with `allow delete: if hasRole(['super_admin', 'operations_admin']);` per the spike-032 admin-delete contract)
    - .planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-CONTEXT.md (D-15 no role-gating for create/update; D-17 schemas; D-14 issue state machine open<->resolved)
  </read_first>
  <action>
    Insert these three `match` blocks immediately after the closing brace of the `audit_log` subcollection block (after firestore.rules line 267) and BEFORE the closing `}` of `match /projects/{projectId}` (line 268). Use `isActiveUser()` — the same helper used by the baselines/audit_log blocks. For the admin-delete line use `hasRole(['super_admin', 'operations_admin'])` — the exact expression the baselines block uses for its delete rule (there is no `isAdmin()` helper in this file). Match the existing comment style (`// <name> subcollection (Phase 101 — ...)`).

    Block 1 — activity_entries (append-only, admin-delete):
    `match /activity_entries/{entryId}` with `allow read: if isActiveUser();`, `allow create: if isActiveUser();`, `allow update: if false;`, `allow delete: if hasRole(['super_admin', 'operations_admin']);`

    Block 2 — progress_updates (append-only, admin-delete):
    `match /progress_updates/{updateId}` with `allow read: if isActiveUser();`, `allow create: if isActiveUser();`, `allow update: if false;`, `allow delete: if hasRole(['super_admin', 'operations_admin']);`

    Block 3 — issues (create + update for open<->resolved, admin-delete):
    `match /issues/{issueId}` with `allow read: if isActiveUser();`, `allow create: if isActiveUser();`, `allow update: if isActiveUser();` (comment: open↔resolved transitions per D-13/D-14), `allow delete: if hasRole(['super_admin', 'operations_admin']);`

    Do NOT modify the existing `match /projects/{projectId}` update rule, the baselines block, the audit_log block, or any other rule. The three new blocks are purely additive.
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('firestore.rules','utf8'); const need=['match /activity_entries/{entryId}','match /progress_updates/{updateId}','match /issues/{issueId}']; const miss=need.filter(s=>!r.includes(s)); if(miss.length){console.error('MISSING:',miss);process.exit(1);} const adminDeletes=(r.match(/allow delete: if hasRole\(\['super_admin', 'operations_admin'\]\);/g)||[]).length; if(adminDeletes<4){console.error('expected >=4 admin-delete lines (baselines + 3 new blocks), found:',adminDeletes);process.exit(1);} const o=(r.match(/{/g)||[]).length,cl=(r.match(/}/g)||[]).length; if(o!==cl){console.error('BRACE IMBALANCE',o,cl);process.exit(1);} console.log('PASS: 3 blocks present, admin-delete lines present, braces balanced',o);"</automated>
  </verify>
  <acceptance_criteria>
    - `firestore.rules` contains `match /activity_entries/{entryId}` with `allow create: if isActiveUser();`, `allow update: if false;`, and `allow delete: if hasRole(['super_admin', 'operations_admin']);`
    - `firestore.rules` contains `match /progress_updates/{updateId}` with `allow create: if isActiveUser();`, `allow update: if false;`, `allow delete: if hasRole(['super_admin', 'operations_admin']);`
    - `firestore.rules` contains `match /issues/{issueId}` with `allow create: if isActiveUser();`, `allow update: if isActiveUser();`, `allow delete: if hasRole(['super_admin', 'operations_admin']);`
    - All three blocks appear between the `audit_log` block and the closing brace of `match /projects/{projectId}` (i.e. they are nested subcollections of projects, not top-level)
    - No new `isAdmin()` helper is introduced; admin-delete uses the existing `hasRole(['super_admin', 'operations_admin'])` expression
    - `firestore.rules` braces are balanced (open count === close count)
  </acceptance_criteria>
  <done>The three subcollection blocks exist inside the projects match with admin-delete rules, static verify passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Three new Firestore subcollection rule blocks (activity_entries, progress_updates, issues) inside match /projects/{projectId}, each with admin-only delete per the spike-032 contract. These must be deployed before any journal write will succeed — even for Super Admin.</what-built>
  <how-to-verify>
    1. Deploy the rules. The CLI's active project is PRODUCTION (clmc-procurement). Run from the repo root:
       `firebase deploy --only firestore:rules`
       (If a dev/staging project alias exists and you want to test there first, use `firebase deploy --only firestore:rules --project dev`.)
    2. Confirm the deploy output reports success with no compile errors.
    3. Note: this deploy also carries the standing Phase 87.4/99/100 rules-deploy debt — that is expected and fine.
  </how-to-verify>
  <resume-signal>Type "deployed" once `firebase deploy --only firestore:rules` reports success, or paste the error output if it fails.</resume-signal>
</task>

</tasks>

<verification>
- Static: 3 blocks present, admin-delete lines present, braces balanced.
- Deploy: `firebase deploy --only firestore:rules` succeeds (human checkpoint).
</verification>

<success_criteria>
Three subcollection rules added inside the projects match (create-only/update-where-allowed for active users, admin-only delete), statically verified, and deployed to Firestore.
</success_criteria>

<output>
Create `.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
