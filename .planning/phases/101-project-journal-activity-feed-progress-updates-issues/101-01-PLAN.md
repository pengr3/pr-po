---
phase: 101-project-journal-activity-feed-progress-updates-issues
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [styles/views.css]
autonomous: true
requirements: []
must_haves:
  truths:
    - "A 3-tab Journal panel renders with a tab bar (Activity / Progress / Issues) and active-tab visual state"
    - "Feed composer, progress form, issue punch list, filter chips, and resolution badges have dedicated styling"
    - "Read-only mode hides composer/forms via a CSS hook without breaking layout"
  artifacts:
    - path: "styles/views.css"
      provides: "Project Journal panel CSS block (tab bar, Feed composer, progress form, issue list, filter chips, badges)"
      contains: ".project-journal-panel"
  key_links:
    - from: "styles/views.css .journal-tab-btn.active"
      to: "project-detail.js switchJournalTab DOM toggle (Plan 03)"
      via: "classList.toggle('active')"
      pattern: "\\.journal-tab-btn\\.active"
---

<objective>
Add the complete CSS block for the Project Journal panel to `styles/views.css`. This is a CSS-only plan — no JS. It defines every class the Plan 03/04 panel builders will reference: the panel shell, the 3-tab bar, the Feed composer (tag selector + textarea + Post button), the Feed entry list (with a distinct system-entry treatment), the Progress Update form, the Issues punch list, filter chips (All/Open/Resolved), issue-type chips, the open/resolved status badge, and a read-only state.

Purpose: Establish the visual contract first so Plan 03/04 executors emit HTML against known class names with no styling guesswork.
Output: One appended CSS block in `styles/views.css`, namespaced under `.project-journal-*` and `.journal-*`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-CONTEXT.md
@.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-PATTERNS.md
@.planning/spikes/032-ongoing-activity-panel/spike.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Project Journal CSS block to views.css</name>
  <read_first>
    - styles/views.css (read the END of the file to find the append point and to match the existing CSS variable usage, e.g. var(--gray-200), var(--primary), and the spacing/radius conventions already in the file)
    - .planning/spikes/032-ongoing-activity-panel/spike.html (the visual reference — Feed composer with tag-type selector, Progress form, Issues punch list with filter chips and resolve button)
    - .planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-CONTEXT.md (D-01/D-02/D-03 status gating, D-16 read-only = no composer, "Specific Ideas" section: tag selector + filter chips)
    - CLAUDE.md (UI Design System color tokens: #1a73e8 primary, #059669 success, #f59e0b warning, #ef4444 danger, border #e5e7eb)
  </read_first>
  <action>
    Append a new CSS section at the end of `styles/views.css` titled with a comment `/* ===== Phase 101 — Project Journal Panel ===== */`. Define these rules (all class names are the contract Plan 03/04 will use verbatim):

    Panel shell:
    - `.project-journal-panel` — card container (white bg, 1px solid #e5e7eb border, border-radius matching the existing project cards, padding ~1rem, margin-bottom 0.75rem). This is the element with `id="projectJournalPanel"` in Plan 03.
    - `.journal-panel-title` — section heading style ("Project Journal").

    Tab bar:
    - `.journal-tab-bar` — horizontal flex row, gap ~0.25rem, bottom border 1px solid #e5e7eb.
    - `.journal-tab-btn` — tab button: transparent bg, no border, padding 0.5rem 0.9rem, color #64748b, cursor pointer, font-weight 600, 2px transparent bottom border for the active underline anchor.
    - `.journal-tab-btn.active` — active tab: color #1a73e8, border-bottom 2px solid #1a73e8.
    - `.journal-tab-panel` — per-tab content wrapper (default visible; Plan 03 toggles display:none via inline style on inactive tabs).

    Feed composer (Activity tab):
    - `.journal-composer` — bordered box (1px solid #e5e7eb, border-radius, padding 0.75rem, margin-bottom 0.75rem).
    - `.journal-tag-select` — the Update/Milestone/Issue/Client Comm `<select>` styling (match existing `.detail-field` select look).
    - `.journal-composer-textarea` — full-width textarea, min-height ~64px, resize vertical.
    - `.journal-post-btn` — primary blue button (#1a73e8, white text, hover #1557b0) for the Post action.

    Feed list:
    - `.journal-feed-list` — vertical list container.
    - `.journal-entry` — single entry row: padding 0.5rem 0, border-top 1px solid #f1f5f9, flex column.
    - `.journal-entry-meta` — small muted line (timestamp · author), font-size 0.7rem, color #94a3b8.
    - `.journal-entry-text` — entry body text, color #1e293b, font-size 0.85rem.
    - `.journal-entry--system` — distinct treatment for `is_system` entries (left accent border 3px solid #94a3b8, slightly muted/italic text, subtle #f8fafc background) so auto-entries read differently from manual posts.
    - `.journal-entry-tag` — small inline pill for the entry's tag type (reuse the pill shape: border-radius 999px, padding 0.1rem 0.5rem, font-size 0.6rem). Provide tag-color variants: `.journal-entry-tag--update`, `--milestone`, `--client`, `--system`.

    Progress Updates tab:
    - `.journal-progress-form` — bordered form box.
    - `.journal-progress-row` — label+input grid row.
    - `.journal-pct-input` — number/slider input for % complete.
    - `.journal-progress-card` — a single historical progress update card in the list (pct badge + summary + blockers + next milestone).

    Issues tab:
    - `.journal-issue-filters` — flex row of filter chips above the punch list.
    - `.journal-filter-chip` — chip button (border 1px solid #e5e7eb, border-radius 999px, padding 0.2rem 0.7rem, cursor pointer, color #64748b).
    - `.journal-filter-chip.active` — active chip (#1a73e8 bg, white text, border-color #1a73e8).
    - `.journal-issue-form` — new-issue form box (issue-type select + title + description).
    - `.journal-issue` — single issue row in the punch list.
    - `.journal-issue-type-chip` — categorized type pill with variants `--delay`, `--change_order`, `--site_issue`, `--client_request` (distinct colors).
    - `.journal-issue-status--open` / `.journal-issue-status--resolved` — status badge (open = amber #f59e0b, resolved = green #059669).
    - `.journal-issue-resolve-btn` — amber button (#f59e0b) for Resolve.
    - `.journal-issue-reopen-btn` — neutral/outline button for Re-open.
    - `.journal-resolution-notes` — muted block showing resolution notes under a resolved issue.

    Read-only state:
    - `.project-journal-panel--readonly` — when this modifier is on the panel, hide composer/forms. Add rules: `.project-journal-panel--readonly .journal-composer`, `.project-journal-panel--readonly .journal-progress-form`, `.project-journal-panel--readonly .journal-issue-form`, `.project-journal-panel--readonly .journal-issue-resolve-btn`, `.project-journal-panel--readonly .journal-issue-reopen-btn` → `display: none;`. (Plan 03/04 may also gate these in HTML, but the CSS hook guarantees read-only never shows a write surface.)

    Use existing CSS variables where the file already defines them (var(--primary), var(--gray-200), etc.) and fall back to the literal hex tokens from CLAUDE.md's UI Design System where no variable exists. Do NOT use `!important`. Do NOT modify any rule outside this new appended block.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('styles/views.css','utf8'); const need=['.project-journal-panel','.journal-tab-bar','.journal-tab-btn.active','.journal-composer','.journal-tag-select','.journal-post-btn','.journal-entry--system','.journal-progress-form','.journal-issue-filters','.journal-filter-chip.active','.journal-issue-type-chip','.journal-issue-status--open','.journal-issue-status--resolved','.project-journal-panel--readonly']; const miss=need.filter(s=>!c.includes(s)); if(miss.length){console.error('MISSING:',miss);process.exit(1);} const o=(c.match(/{/g)||[]).length,cl=(c.match(/}/g)||[]).length; if(o!==cl){console.error('BRACE IMBALANCE',o,cl);process.exit(1);} console.log('PASS: all selectors present, braces balanced',o);"</automated>
  </verify>
  <acceptance_criteria>
    - `styles/views.css` contains the comment `/* ===== Phase 101 — Project Journal Panel ===== */`
    - `styles/views.css` contains every selector in the verify list: `.project-journal-panel`, `.journal-tab-bar`, `.journal-tab-btn.active`, `.journal-composer`, `.journal-tag-select`, `.journal-post-btn`, `.journal-entry--system`, `.journal-progress-form`, `.journal-issue-filters`, `.journal-filter-chip.active`, `.journal-issue-type-chip`, `.journal-issue-status--open`, `.journal-issue-status--resolved`, `.project-journal-panel--readonly`
    - CSS braces in `styles/views.css` are balanced (open count === close count)
    - `.project-journal-panel--readonly .journal-composer` rule sets `display: none`
    - No `!important` appears in the new block
  </acceptance_criteria>
  <done>views.css has the full Project Journal CSS block; the static verify passes (all selectors present, braces balanced).</done>
</task>

</tasks>

<verification>
- `node -e` brace/selector gate passes (see Task 1 verify).
- Visual verification deferred to browser UAT after Plan 03/04 wire the HTML (zero-build SPA).
</verification>

<success_criteria>
All required journal selectors exist in views.css, braces balanced, read-only hook present, no `!important`.
</success_criteria>

<output>
Create `.planning/phases/101-project-journal-activity-feed-progress-updates-issues/101-01-SUMMARY.md` when done.
</output>
