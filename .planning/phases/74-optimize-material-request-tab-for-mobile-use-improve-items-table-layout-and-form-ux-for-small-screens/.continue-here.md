---
phase: 74-optimize-material-request-tab-for-mobile-use
task: 4
total_tasks: 4
status: in_progress
last_updated: "2026-04-17T10:47:30.138Z"
---

<current_state>
Phase 74 Plan 03 — all 3 code tasks committed. Currently paused at Task 4 (blocking human-verify
UAT checkpoint). There are also uncommitted changes to mrf-form.js (edit item card refactor) that
need a commit before or alongside presenting the UAT checklist.

This is the FINAL plan of the FINAL phase (74) of the v3.2 milestone.
After UAT passes → write 74-03-SUMMARY.md → /gsd:complete-milestone.
</current_state>

<completed_work>

**Plan 74-01** — MRF Sub-Nav Pill Bar (commits 0daf107 → bbe7203, DONE)
- Sticky scroll-hide/show pill bar matching Finance 73.3 pattern

**Plan 74-02** — MRF Items Table Mobile Card Layout (commits 101035b, ca17c60, DONE)
- CSS dual-mode: hides items table at <=768px, shows .mrf-item-card stack
- addItem/removeItem keep both DOM trees in sync via data-item-index
- Scoped sync handlers on .mrf-items-section; destroy() cleanup

**Plan 74-03 Task 1** — .mrf-req-card CSS + dual-mode hide/show (commit 376d177, DONE)
- New .mrf-req-card* block in views.css
- #myRequestsContainer .table-scroll-container hidden at <=768px; .mrf-req-card-list shown
- .position:fixed dropdown menu for 3-dot actions

**Plan 74-03 Task 2** — onMobileAction + mapMRFToDisplayData + buildMRFRequestCard in mrf-records.js (commit 356b2e9, DONE)
- onMobileAction option added to createMRFRecordsController
- mapMRFToDisplayData() shared helper (REVIEWS [MEDIUM]) computes display strings ONCE per MRF
- buildMRFRequestCard() accepts pre-computed displayData (no re-computation)
- Single-pass Promise.all returns {rowHtml, cardHtml} pairs
- render() emits both .table-scroll-container and .mrf-req-card-list
- _mrfRecordsMobileAction_{containerId} window function registered + destroy() cleanup

**Plan 74-03 Task 3** — Wire onMobileAction in mrf-form.js (commit cd049b0 + fix commits f9f1aa4, 557c29e, DONE)
- initMyRequests passes onMobileAction callback
- Dropdown: Edit MRF (blue) / Cancel MRF (red) for Pending/In Progress; "No actions available" otherwise
- Reuses existing window._myRequestsEditMRF / window._myRequestsCancelMRF
- scroll listener auto-closes dropdown (REVIEWS [LOW]: no floating menu)
- closeMyRequestsMobileMenu() centralized close; destroy() cleanup

**Uncommitted refactor in mrf-form.js** (NOT YET COMMITTED):
- buildEditItemRow now returns `<div class="edit-item-card">` instead of `<tr>` (native card layout)
- Removed inline `<style>` block from editRequestorMRF modal HTML (was a hack for @media table-row stacking)
- Fixed `e.target.closest('td')` → `e.target.parentElement` (no longer inside a <td>)
- Add Item button now creates a div wrapper instead of a temp tbody
- querySelectorAll('#editMRFItemsBody tr') → querySelectorAll('#editMRFItemsBody .edit-item-card') in save handler
- #editMRFItemsBody is now a plain div (not a tbody) — layout is flex column
</completed_work>

<remaining_work>

1. **Commit the uncommitted mrf-form.js edit item card refactor**
   - Files: app/views/mrf-form.js (104 line diff), .planning/STATE.md, .planning/debug/knowledge-base.md
   - Commit message: `fix(74-03): refactor edit MRF modal to native card layout (remove table-row hack)`

2. **Task 4 — Human UAT** (BLOCKING)
   - Present the how-to-verify checklist from 74-03-PLAN.md Task 4
   - Wait for user to respond "approved" or report issues
   - Full checklist includes: mobile cards visible at 375px, 3-dot menu, scroll-close, edit/cancel flows, desktop parity, data parity (REVIEWS [MEDIUM] check)

3. **Write 74-03-SUMMARY.md** after UAT passes

4. **Update STATE.md** — mark Phase 74 complete, all plans done

5. **Run /gsd:complete-milestone** — v3.2 is the last milestone; this closes it out
</remaining_work>

<decisions_made>

- Phase 74-03 fix commits (f9f1aa4, 557c29e) addressed mobile search bar overflow and edit modal zoom
- buildEditItemRow refactored from <tr> to <div class="edit-item-card"> to eliminate the @media inline style hack and fix the 'closest("td")' selector bug that would break unit dropdown on mobile
- mapMRFToDisplayData() single-source-of-truth approach (REVIEWS [MEDIUM]) was kept — single Promise.all pass, no duplicate computation
- Desktop behavior (onContextMenu right-click) preserved throughout all changes
</decisions_made>

<blockers>
None — all code is working. Only blocker is the human UAT gate (Task 4).
</blockers>

<context>
Phase 74 is the FINAL phase of v3.2 milestone ("Supplier Search, Proof of Procurement & Payables Tracking"). After UAT passes, /gsd:complete-milestone closes out v3.2. The uncommitted mrf-form.js changes are a clean final improvement: the @media inline style block that forced table rows to render as cards at <=640px was a brittle hack. The refactor makes the edit item card layout native (div-based from the start), which also fixes the closest('td') selector bug in the unit 'others' toggle handler that would have been invisible on desktop but broken on mobile.
</context>

<next_action>
Start with:
1. Commit the uncommitted files: `git add app/views/mrf-form.js .planning/STATE.md .planning/debug/knowledge-base.md && git commit -m "fix(74-03): refactor edit MRF modal to native card layout (remove table-row hack)"`
2. Present Task 4 UAT checklist to user (from 74-03-PLAN.md <how-to-verify>)
3. Await "approved" signal
4. Write .planning/phases/74-.../74-03-SUMMARY.md
5. /gsd:complete-milestone
</next_action>
