---
phase: "97.1"
plan: "02"
type: execute
wave: 2
depends_on:
  - "97.1-01"
files_modified:
  - app/views/project-plan.js
  - styles/views.css
autonomous: true
requirements:
  - Fix-97.1-bug3-toolbar-ux

must_haves:
  truths:
    - "Toolbar controls are visually grouped into four labeled sections with dividers between them: Zoom (Day/Week/Month), View (Critical Path + Export), Baseline (dropdown + Set/Clear button), and Iterations (Save Iteration + History)"
    - "All existing onclick handlers and element IDs are preserved — no functional regressions from the HTML restructure"
    - "The search input remains rightmost in the toolbar, separated from the Iterations group"
    - "Critical Path checkbox and Export button share the same group, reducing the total number of standalone floating controls"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "Redesigned render() toolbar HTML with .plan-toolbar-group wrappers and .plan-toolbar-group-label spans"
      contains: "plan-toolbar-group"
    - path: "styles/views.css"
      provides: "CSS for .plan-toolbar-group, .plan-toolbar-divider, and .plan-toolbar-group-label"
      contains: "plan-toolbar-group"
  key_links:
    - from: "render() toolbar HTML"
      to: "styles/views.css .plan-toolbar-group"
      via: "class='plan-toolbar-group'"
      pattern: "plan-toolbar-group"
    - from: "baselineToggleBtn"
      to: "window.toggleBaseline"
      via: "id='baselineToggleBtn' onclick preserved"
      pattern: "baselineToggleBtn"
    - from: "iterHistoryBtn"
      to: "window.toggleIterRail"
      via: "id='iterHistoryBtn' onclick preserved"
      pattern: "iterHistoryBtn"
---

<objective>
Redesign the project plan toolbar so that the 8 controls (zoom pills, export, critical path, baseline dropdown, baseline toggle, save iteration, history, search) are visually grouped into labeled sections with thin dividers between them, reducing visual clutter and making each control's purpose immediately scannable.

Purpose: The flat toolbar is hard to scan when all 8 controls share the same visual weight and gap. Grouping by function (Zoom / View / Baseline / Iterations) makes each cluster self-explanatory.

Output: Modified render() HTML using .plan-toolbar-group wrappers, plus new CSS in views.css for the group container, divider, and label styles. All IDs, onclicks, and aria attributes from the original HTML are preserved exactly.
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\97-project-plan-iterations\97-VERIFICATION.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\CLAUDE.md
@C:\Users\Admin\Roaming\pr-po\app\views\project-plan.js
@C:\Users\Admin\Roaming\pr-po\styles\views.css

<interfaces>
<!-- Current toolbar HTML structure (lines 141–180 of project-plan.js) -->
<!-- The executor must preserve all id=, onclick=, data-zoom=, type=, class= values exactly as they appear in the original -->

Current flat controls after .plan-toolbar-spacer:
  1. .zoom-pill-group#zoomPillGroup: three .zoom-pill buttons (Day/Week/Month)
  2. .plan-export-btn: Export PDF button (onclick="window.exportGanttPDF()")
  3. .plan-toolbar-toggle label: Critical path checkbox (id="cpToggle", onclick="window.toggleCriticalPath(this.checked)")
  4. #baselineSelect.plan-baseline-select: dropdown (onchange="window.selectBaseline(this.value)")
  5. #baselineToggleBtn.plan-export-btn.plan-baseline-btn: Set/Clear Baseline button
  6. .plan-export-btn.plan-iter-save-btn: Save Iteration button (onclick="window.saveIteration()")
  7. #iterHistoryBtn.plan-export-btn.plan-iter-history-btn: History button (onclick="window.toggleIterRail()")
  8. .plan-toolbar-search div: search input + clear button

Target grouping:
  Group "Zoom":       zoom-pill-group
  Group "View":       Export PDF + Critical Path toggle
  Group "Baseline":   baseline select + Set/Clear button
  Group "Iterations": Save Iteration + History
  (no group):         search input — stays ungrouped, floats to far right

Existing CSS classes to preserve (no renames):
  .plan-toolbar-toggle, .plan-export-btn, .plan-baseline-select,
  .plan-baseline-btn, .plan-iter-save-btn, .plan-iter-history-btn,
  .zoom-pill-group, .zoom-pill, .plan-toolbar-search, .tg-search-input, .tg-search-clear

New CSS classes to add (must be in views.css under Phase 97.1 comment block):
  .plan-toolbar-group        — flex container for a logical control cluster
  .plan-toolbar-group-label  — small uppercase label above/below the cluster (or visually implied)
  .plan-toolbar-divider      — thin vertical line between groups
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add toolbar group CSS to views.css</name>
  <files>styles/views.css</files>
  <read_first>
    - styles/views.css lines 2286–2325: existing .plan-toolbar, .zoom-pill-group, .zoom-pill rules — understand the flex layout (.plan-toolbar uses display:flex, align-items:center, gap:8px)
    - styles/views.css lines 2713–2722: existing Phase 97 iteration button CSS block — insert new rules after this block, under a new "Phase 97.1" comment header
    - styles/views.css lines 3275–3311: .plan-export-btn and .plan-baseline-select rules — understand existing button sizing (padding: 5px 14px, font-size:13px, border-radius:16px) so group labels match
  </read_first>
  <action>
    Append a new CSS block in styles/views.css immediately after the existing `.plan-iter-history-btn.active` rule (line ~2722). Insert under a comment header "Phase 97.1 — Toolbar groups":

    .plan-toolbar-group: display:flex; align-items:center; gap:6px; position:relative

    .plan-toolbar-divider: width:1px; height:28px; background:var(--gray-200, #dadce0); margin:0 4px; flex-shrink:0

    .plan-toolbar-group-label: display:block; font-size:10px; font-weight:600; letter-spacing:0.04em; text-transform:uppercase; color:#94a3b8; margin-bottom:2px; white-space:nowrap; line-height:1

    Add a wrapping flex-column structure for groups that have labels. The group itself becomes a column flex container when it has a label:
    .plan-toolbar-group--labeled: flex-direction:column; align-items:flex-start; gap:2px

    The inner row of controls in a labeled group:
    .plan-toolbar-group--labeled .plan-toolbar-group-controls: display:flex; align-items:center; gap:6px

    Note: The zoom pills and search box do not need labels — only the three functional groups (View, Baseline, Iterations) get labels. Design choice: use .plan-toolbar-group (horizontal, no label) for the zoom group and search; use .plan-toolbar-group--labeled (column with label row) for View, Baseline, and Iterations.

    Alternative simpler approach (preferred for this project's pattern of inline styles): Skip .plan-toolbar-group-label and .plan-toolbar-group--labeled entirely. Use only:
    - .plan-toolbar-group (flex row container)
    - .plan-toolbar-divider (separator)

    And rely on the divider alone for visual grouping without text labels — this matches the simple toolbar style of this SPA. Use this simpler approach.

    Final rules to add:
    - .plan-toolbar-group: display:flex; align-items:center; gap:6px
    - .plan-toolbar-divider: width:1px; height:24px; background:var(--gray-200, #dadce0); margin:0 2px; flex-shrink:0; align-self:center

    Do NOT modify any existing CSS rules. Only append the new block.
  </action>
  <verify>
    Grep views.css for `.plan-toolbar-group` — must return at least one line.
    Grep views.css for `.plan-toolbar-divider` — must return at least one line.
    Grep views.css for `Plan 97.1` or `97.1` as a comment marker — must be present to identify the new block.
  </verify>
  <acceptance_criteria>
    - styles/views.css contains `.plan-toolbar-group` rule with `display: flex` and `align-items: center`
    - styles/views.css contains `.plan-toolbar-divider` rule with `width: 1px` and a background referencing the gray border color
    - The new rules are appended AFTER the `.plan-iter-history-btn.active` rule, not interspersed in existing rules
    - No existing CSS rule is modified or removed
  </acceptance_criteria>
  <done>views.css has .plan-toolbar-group and .plan-toolbar-divider CSS rules that the HTML in Task 2 will reference</done>
</task>

<task type="auto">
  <name>Task 2: Restructure render() toolbar HTML into logical groups</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    - app/views/project-plan.js lines 139–180: the complete current render() return value toolbar section — read all 40 lines carefully before touching anything; note every id=, onclick=, class=, and data-zoom= attribute exactly as written
    - app/views/project-plan.js lines 264–265: init() calls loadBaselines() and then uses updateBaselineToolbarUI() which queries document.getElementById('baselineSelect') and document.getElementById('baselineToggleBtn') — these IDs must be preserved exactly
    - app/views/project-plan.js lines 3248–3268: updateBaselineToolbarUI() queries #baselineSelect and #baselineToggleBtn — confirm the IDs match what is in render()
    - app/views/project-plan.js lines 3380–3401: toggleIterRail() and closeIterRail() query #iterHistoryBtn and #iterRail — confirm the IDs match
    - styles/views.css lines 2713–2722: .plan-iter-history-btn.active rule — confirm the class name used by the History button
  </read_first>
  <action>
    Replace the toolbar HTML in render() (the section between `<div class="plan-toolbar-spacer"></div>` and the closing `</div>` of .plan-toolbar) with the following restructured version. The controls are identical — only the wrapper divs change.

    Structure after the spacer:

    Group 1 — Zoom (no divider before it, no label):
    Wrap the existing .zoom-pill-group div in a .plan-toolbar-group div. No other change to the zoom pills.

    Divider 1: `<div class="plan-toolbar-divider" aria-hidden="true"></div>`

    Group 2 — View (Export + Critical Path):
    Wrap the Export button and the Critical Path label together in a .plan-toolbar-group div. The Export button stays first, then the Critical Path label — same order as current flat layout.

    Divider 2: `<div class="plan-toolbar-divider" aria-hidden="true"></div>`

    Group 3 — Baseline:
    Wrap the #baselineSelect and #baselineToggleBtn together in a .plan-toolbar-group div. No other change to their attributes.

    Divider 3: `<div class="plan-toolbar-divider" aria-hidden="true"></div>`

    Group 4 — Iterations:
    Wrap the Save Iteration button and History button together in a .plan-toolbar-group div. No other change to their attributes.

    The search .plan-toolbar-search div remains OUTSIDE any group wrapper — it stays as a direct child of .plan-toolbar after Group 4, same as current.

    Preserve VERBATIM (character-for-character) for every interactive element:
    - All id= attributes: zoomPillGroup, cpToggle, baselineSelect, baselineToggleBtn, iterHistoryBtn
    - All onclick= attributes: window.setGanttZoom('Day'), window.exportGanttPDF(), window.toggleCriticalPath(this.checked), window.selectBaseline(this.value), window.toggleBaseline(), window.saveIteration(), window.toggleIterRail()
    - All class= attributes on the original elements (do NOT add or remove classes from existing elements)
    - All data-zoom= attributes on the zoom pills
    - All title= attributes on buttons
    - The full multi-line title= text on the Critical Path label (the tooltip with &#10; line breaks)
    - The aria-label="Zoom" on the zoom-pill-group
    - The checked attribute on #cpToggle

    Do NOT remove the Phase 86.8 Feature 6 comment above the search div — preserve it. Do NOT remove the Phase 97 comment above the iteration buttons — preserve it within the new group wrapper.

    The only structural change is adding four .plan-toolbar-group wrappers and three .plan-toolbar-divider separators. Nothing else changes.
  </action>
  <verify>
    Grep project-plan.js for:
    - `plan-toolbar-group` — must appear at least 4 times (one per group div opening tag)
    - `plan-toolbar-divider` — must appear exactly 3 times
    - `id="baselineToggleBtn"` — must still be present (unchanged)
    - `id="iterHistoryBtn"` — must still be present (unchanged)
    - `id="cpToggle"` — must still be present (unchanged)
    - `id="baselineSelect"` — must still be present (unchanged)
    - `window.toggleBaseline()` — must still be present as an onclick value
    - `window.toggleIterRail()` — must still be present as an onclick value
    - `window.exportGanttPDF()` — must still be present as an onclick value
    - `window.toggleCriticalPath(this.checked)` — must still be present as an onclick value
  </verify>
  <acceptance_criteria>
    - project-plan.js render() toolbar contains exactly 4 `.plan-toolbar-group` wrapper divs (Zoom, View, Baseline, Iterations)
    - project-plan.js render() toolbar contains exactly 3 `.plan-toolbar-divider` elements between the groups
    - All 5 element IDs (zoomPillGroup, cpToggle, baselineSelect, baselineToggleBtn, iterHistoryBtn) are present and unchanged
    - All 7 window.* onclick handlers are present and unchanged (setGanttZoom, exportGanttPDF, toggleCriticalPath, selectBaseline, toggleBaseline, saveIteration, toggleIterRail)
    - .plan-toolbar-search div remains a direct child of .plan-toolbar (not wrapped in any group div)
    - The Critical Path label and Export button share the same .plan-toolbar-group div (Group 2 — View)
    - The #baselineSelect and #baselineToggleBtn share the same .plan-toolbar-group div (Group 3 — Baseline)
    - The Save Iteration and History buttons share the same .plan-toolbar-group div (Group 4 — Iterations)
    - The zoom-pill-group is wrapped alone in Group 1's .plan-toolbar-group div
    - Visual behavior: opening the browser shows 4 visually distinct control clusters separated by thin gray lines
  </acceptance_criteria>
  <done>render() toolbar HTML uses .plan-toolbar-group wrappers with .plan-toolbar-divider separators; all existing IDs and onclick attributes are preserved; toolbar is visually organized into Zoom / View / Baseline / Iterations clusters</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML render() → DOM | Toolbar restructure is purely structural; no new event surfaces, no new data flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97.1-03 | Tampering | render() HTML restructure | accept | Changes are purely presentational (div wrappers). All onclick handlers preserved verbatim. No new input surface. |
| T-97.1-04 | Information Disclosure | CSS new classes | accept | .plan-toolbar-group and .plan-toolbar-divider are presentation-only; no data or auth implications. |
| T-97.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- Grep project-plan.js: `plan-toolbar-group` count >= 4
- Grep project-plan.js: `plan-toolbar-divider` count = 3
- Grep project-plan.js: `id="baselineToggleBtn"` present
- Grep project-plan.js: `id="iterHistoryBtn"` present
- Grep project-plan.js: `id="cpToggle"` present
- Grep project-plan.js: `id="baselineSelect"` present
- Grep views.css: `.plan-toolbar-group` rule present
- Grep views.css: `.plan-toolbar-divider` rule present
- Browser visual check: toolbar shows 4 control groups with thin separator lines between them; all existing functionality (zoom, export, critical path, baseline, save iteration, history, search) still works
</verification>

<success_criteria>
- Toolbar renders 4 visually grouped clusters (Zoom / View / Baseline / Iterations) with gray divider lines between them
- All existing toolbar controls retain their IDs, onclicks, classes, and behavior
- No console errors introduced by the restructure
- The search input remains rightmost and ungrouped
</success_criteria>

<output>
Create `.planning/phases/97.1-project-plan-iterations-ux-fixes/97.1-02-SUMMARY.md` when done
</output>
