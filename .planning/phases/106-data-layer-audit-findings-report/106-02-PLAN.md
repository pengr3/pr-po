---
phase: 106-data-layer-audit-findings-report
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/106-data-layer-audit-findings-report/106-INVENTORY.md
autonomous: true
requirements: [AUDIT-01]
must_haves:
  truths:
    - "106-INVENTORY.md lists every Firestore SDK call site (read/write/listener/query) across all 35 app JS files with file:line anchors (AUDIT-01, D-09)"
    - "diagnostics.js is included even though it imports firebase.js via a dynamic import() a static grep misses (D-09)"
    - "Subcollection / dynamic-path accesses (activity_entries, progress_updates, issues, baselines, edit_history under a parent doc) are captured, not just literal collection(db,'X') (D-10)"
    - "The inventory records runTransaction = 0 across app/ (the sequential-ID race lead) and the where(183)-vs-limit(12) imbalance"
    - "archive/, .claude/worktrees/, and node scripts/ are excluded from the app-code inventory (D-11)"
  artifacts:
    - path: ".planning/phases/106-data-layer-audit-findings-report/106-INVENTORY.md"
      provides: "the file:line SDK call-site index that Plans 03-06 read to target their audits (the shared anchor map)"
      contains: "runTransaction"
  key_links:
    - from: "106-INVENTORY.md"
      to: "Plans 03/04/05/06"
      via: "file:line anchors the dimension audits read instead of re-scanning"
      pattern: "\\.js:[0-9]"
---

<objective>
Produce the exhaustive file:line inventory of every Firestore SDK call site across the whole `app/` data layer (D-09) — the shared anchor map that Plans 03-06 read so they target specific lines instead of re-scanning 18k+ lines of view code. This is the AUDIT-01 "inventoried" half (Plan 07 turns it into the report's inventory summary). Grep-based only — do NOT full-read the large view files (procurement.js is 9,549 lines, finance.js 6,851).

Purpose: One authoritative call-site map, categorized by operation and collection, so the dimension audits are surgical and stay within budget.
Output: `106-INVENTORY.md` — per-operation, per-file, per-collection cross-tabs with file:line anchors, plus the standing leads (runTransaction=0, where-vs-limit, subcollection paths).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/106-data-layer-audit-findings-report/106-CONTEXT.md

<interfaces>
<!-- The 35-file audit surface (D-09). 34 statically import from firebase.js; diagnostics.js imports it via dynamic import(). firestore.rules is audited by Plan 06, not inventoried here. -->
Root (15): auth.js, coll-id.js, diagnostics.js, edit-history.js, engagement-create.js, expense-modal.js, notifications.js, permissions.js, proof-modal.js, proposal-id.js, proposal-modal.js, seed-roles.js, service-task-id.js, task-id.js, utils.js  (+ firebase.js itself defines db)
Views (20): assignments, clients, finance, home, login, mrf-form, mrf-records, notifications, pending, procurement, project-detail, project-plan, projects, proposals, register, role-config, service-detail, service-plan, services, user-management  (all .js under app/views/)

<!-- Recon baseline (call-site counts, pattern "op(" — reproduce and itemize these; treat as MINIMUMS, exhaustive scan may find a few more): -->
onSnapshot 61 · getDoc 54 · getDocs 136 · updateDoc 137 · addDoc 35 · setDoc 8 · deleteDoc 28 · writeBatch 18 · runTransaction 0 · where 183 · limit 12
<!-- NOTE: raw substring counts (no paren) inflate to onSnapshot 144 / getDoc 251 because import lines and getDocs contain the substring. Inventory the CALL SITES (with "("), and record this substring-vs-callsite gap as an inventory note. -->

<!-- 21 literal collections in code: billing_requests, client_errors, clients, collectibles, deleted_mrfs, invitation_codes, mrfs, notifications, pos, project_iterations, project_tasks, projects, proposals, prs, rfps, role_templates, service_tasks, services, suppliers, transport_requests, users -->
<!-- Subcollections a naive grep misses (parent/{id}/sub): activity_entries + progress_updates + issues under projects & services; baselines under projects; edit_history under {collectionName}/{docId}/edit_history -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the raw per-operation call-site index</name>
  <files>.planning/phases/106-data-layer-audit-findings-report/106-INVENTORY.md</files>
  <read_first>
    - .planning/phases/106-data-layer-audit-findings-report/106-CONTEXT.md D-09, D-10, D-11 (audit surface, subcollections, exclusions)
    - CLAUDE.md § "Firebase Firestore Schema" (collection names to key the cross-tab on)
    - Do NOT full-read procurement.js / finance.js / mrf-form.js — grep with -n only.
  </read_first>
  <action>
    Build the raw call-site index using grep -n over `app/**/*.js` ONLY (exclude archive/, .claude/worktrees/, scripts/ per D-11). For EACH operation run `grep -rn "\bOP(" app --include=*.js` and record every hit as `file:line — <trimmed line>`:
    - Reads: `getDoc`, `getDocs`, `onSnapshot`
    - Writes: `addDoc`, `setDoc`, `updateDoc`, `deleteDoc`, `writeBatch`, `runTransaction`
    - Query modifiers: `where`, `limit`, `orderBy`
    - Collection refs: `collection(db,` AND subcollection form `collection(db, <parent>, <id>, '<sub>')`
    Include `app/diagnostics.js` explicitly — it accesses `client_errors` via `await import('./firebase.js')` then `addDoc(collection(db,'client_errors'), ...)`; a `from './firebase.js'` importer grep misses it (D-09). Grep it directly for `collection(` / `addDoc(`.
    Capture the subcollection/dynamic paths separately (D-10): `grep -rnE "collection\([^)]*(activity_entries|progress_updates|issues|baselines|edit_history)" app --include=*.js`.
    Write all of this into `106-INVENTORY.md` under a `## Raw Call-Site Index` section, grouped by operation, each group listing its `file:line` hits. Put the total count next to each operation heading.
  </action>
  <verify>
    <automated>grep -cE "\.js:[0-9]+" .planning/phases/106-data-layer-audit-findings-report/106-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\.js:[0-9]+" 106-INVENTORY.md` returns >= 400 (call sites carry real file:line anchors; recon baseline sums to ~700 op-hits, so >=400 is a safe exhaustiveness floor).
    - The index has an `onSnapshot` group with >= 55 hits, a `getDocs` group with >= 125 hits, and an `updateDoc` group with >= 125 hits (within a few of the recon baseline).
    - `grep -c "diagnostics.js" 106-INVENTORY.md` returns >= 1 (the dynamic-import file is included, D-09).
    - `grep -cE "activity_entries|progress_updates|issues|baselines|edit_history" 106-INVENTORY.md` returns >= 5 (subcollection paths captured, D-10).
    - `grep -c "runTransaction" 106-INVENTORY.md` returns >= 1 and states the count is 0.
  </acceptance_criteria>
  <done>Raw per-operation call-site index with file:line anchors written, including diagnostics.js and subcollection paths.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-tab the inventory by file and by collection + record standing leads</name>
  <files>.planning/phases/106-data-layer-audit-findings-report/106-INVENTORY.md</files>
  <read_first>
    - The `## Raw Call-Site Index` section written in Task 1 (the source of the cross-tabs)
    - CLAUDE.md (the 21 collection names + the listener/destroy pattern, to key the per-collection and per-file tables)
  </read_first>
  <action>
    Add three summary sections to `106-INVENTORY.md`:
    1. `## Per-Operation Totals` — a table: operation · call-site count · one-line note. Include the row `runTransaction · 0 · sequential-ID generation uses max-scan, no transaction → concurrent-submit race is a High-severity integrity LEAD for Plan 03`. Add a `where · 183` and `limit · 12` row noting the imbalance is an efficiency LEAD for Plan 05. Add a note that substring counts inflate (onSnapshot 144 / getDoc 251) vs call-sites — so the report must count call-sites.
    2. `## Per-File Matrix` — one row per app JS file (all 35), columns: reads (getDoc+getDocs+onSnapshot) · writes (add+set+update+delete+batch) · listeners (onSnapshot) · has destroy() (yes/no). This is the surface map Plans 03-06 slice. Flag files where onSnapshot > 0 but no `listeners`/`unsubscribe` tracking array is visible (project-detail, service-detail, home, mrf-form, engagement-create, auth, notifications, permissions) as `LISTENER-LIFECYCLE LEAD → Plan 04`.
    3. `## Per-Collection Access Map` — one row per collection (the 21 literals + the subcollections activity_entries/progress_updates/issues/baselines/edit_history): collection · reads · writes · accessed-as (top-level | subcollection under X) · files touching it. This is the map Plan 06 reconciles against firestore.rules (D-10).
    Add a final `## Scope & Exclusions` note: archive/, .claude/worktrees/, and node scripts/ (incl. verify-integrity.js as tooling) are excluded from this inventory per D-11.
  </action>
  <verify>
    <automated>grep -cE "## Per-Operation Totals|## Per-File Matrix|## Per-Collection Access Map" .planning/phases/106-data-layer-audit-findings-report/106-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "## Per-Operation Totals|## Per-File Matrix|## Per-Collection Access Map" 106-INVENTORY.md` returns 3.
    - The Per-File Matrix references >= 30 distinct `.js` filenames (near-complete surface; recon surface is 35).
    - `grep -cE "LISTENER-LIFECYCLE LEAD|Plan 04" 106-INVENTORY.md` returns >= 1 (untracked-listener files flagged).
    - The Per-Collection Access Map contains rows for `activity_entries` and `edit_history` marked as subcollection access (D-10).
    - `grep -cE "archive/|worktrees|D-11" 106-INVENTORY.md` returns >= 1 (exclusions recorded).
  </acceptance_criteria>
  <done>Per-operation, per-file, and per-collection cross-tabs plus standing leads and exclusions recorded in 106-INVENTORY.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact → repo | 106-INVENTORY.md is a static analysis artifact; no runtime, no DB, no credentials involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-04 | Information Disclosure | inventory listing internal file:line structure | accept | Only source paths + line numbers already in the repo are recorded; no secrets, no data values; internal .planning artifact |
| T-106-05 | Spoofing | inventory missing call sites (false completeness) | mitigate | Grep-based exhaustiveness floors in acceptance criteria (>=400 anchors) + explicit inclusion of diagnostics.js dynamic import + subcollection paths a naive grep misses |
| T-106-SC | Tampering | dependency installs | accept | Read-only grep over existing files; no dependencies, no installs (zero-build SPA, N/A) |
</threat_model>

<verification>
- 106-INVENTORY.md has the raw call-site index (>=400 file:line anchors) + three cross-tab sections.
- diagnostics.js and subcollection paths included.
- runTransaction=0 and where-vs-limit leads recorded.
</verification>

<success_criteria>
- Exhaustive file:line SDK call-site inventory across all 35 app JS files (AUDIT-01, D-09).
- Subcollection/dynamic paths captured (D-10); exclusions honored (D-11).
- Standing leads (ID race, listener lifecycle, where-vs-limit) flagged for the dimension plans.
</success_criteria>

<output>
Create `.planning/phases/106-data-layer-audit-findings-report/106-02-SUMMARY.md` when done.
</output>
