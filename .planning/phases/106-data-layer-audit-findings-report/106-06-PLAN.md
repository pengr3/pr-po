---
phase: 106-data-layer-audit-findings-report
plan: 06
type: execute
wave: 2
depends_on: [02]
files_modified:
  - .planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-security.md
autonomous: true
requirements: [AUDIT-03]
must_haves:
  truths:
    - "Every firestore.rules match block is reconciled against actual code access from 106-INVENTORY.md — a per-collection table of rule-gate vs code-access (AUDIT-03, D-10)"
    - "Over-permissioning is flagged, including invitation_codes allow read/update: if true (firestore.rules:185,191)"
    - "Under-permissioning is flagged — any collection/subcollection accessed in code with no governing rule (blocked legit access)"
    - "Dead/unreferenced rule blocks (a rule for a collection no code touches, e.g. deleted_users / audit_log) are flagged for confirmation"
    - "Subcollection rule nesting (activity_entries/progress_updates/issues/baselines/edit_history/audit_log under projects & services) is confirmed to match the code's subcollection access paths (D-10)"
  artifacts:
    - path: ".planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-security.md"
      provides: "security-rule findings (temp IDs S-0N) + the rule-vs-access reconciliation table for Plan 07"
      contains: "category: security-rules"
  key_links:
    - from: "106-SCRATCH-security.md"
      to: "106-FINDINGS.md (Plan 07)"
      via: "S-0N findings merged and re-IDed to F-00N by severity"
      pattern: "S-0[0-9]"
---

<objective>
Perform the security-rule coverage audit (AUDIT-03): reconcile every `firestore.rules` match block against the actual code access recorded in 106-INVENTORY.md, and flag over-permissioning, under-permissioning, dead rules, and subcollection-nesting mismatches. Emit findings in the canonical D-07 schema for Plan 07. This phase does NOT edit firestore.rules (AUDIT-03 only READS them — no rules-test tasks).

Purpose: Confirm the data layer the new UI reads/writes is neither exposing data nor blocking legitimate access, before 107-111 build on it.
Output: `106-SCRATCH-security.md` — a rule-vs-access reconciliation table + security-rule findings (temp IDs S-0N) with rule line anchors, handling, and target phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/106-data-layer-audit-findings-report/106-CONTEXT.md
@.planning/phases/106-data-layer-audit-findings-report/106-INVENTORY.md
@firestore.rules

<finding_schema>
<!-- CANONICAL D-07 schema (identical across all dimension plans). This plan's TEMPID prefix is S- (S-01, S-02, ...). Anchors here are firestore.rules:line (plus the code file:line the access comes from). -->
### <TEMPID> — <one-line title>
- severity: High | Medium | Low
- category: security-rules
- collection: <affected collection(s)>
- anchor: firestore.rules:<line>[, <code file.js:line>]
- impact: <exposure or blocked-access consequence>
- recommendation: <concrete fix direction>
- handling: code-fix | backfill-script | defer
- target_phase: 112

<!-- D-08 rubric: over/under-permissioned rules = High (data exposure or blocked legit access). Dead rules / cosmetic = Low. -->

<!-- Confirmed rule structure (from recon — verify, don't trust blindly):
Top-level collection blocks: users(113), role_templates(163), invitation_codes(183), projects(200), project_iterations(341), clients(367), mrfs(384), prs(411), pos(443), transport_requests(474), suppliers(505), deleted_mrfs(518), deleted_users(533), services(555), rfps(650), collectibles(674), billing_requests(699), project_tasks(734), service_tasks(817), proposals(920), notifications(1021), client_errors(1058).
Nested subcollections under projects(200): edit_history(263), baselines(276), audit_log(284), activity_entries(301), progress_updates(312), issues(325). Under services(555): edit_history(596), audit_log(606), activity_entries(620), progress_updates(628), issues(639).
OVER-PERMISSION LEAD: invitation_codes allow read: if true (185) + allow update: if true (191).
DEAD-RULE / DYNAMIC-ACCESS LEADS: deleted_users(533) and audit_log(284,606) do NOT appear in the code's literal collection(db,'X') grep — resolve whether accessed dynamically or dead.
Code touches 21 literal collections + client_errors (diagnostics dynamic import) + subcollections activity_entries/progress_updates/issues/baselines/edit_history. -->
</finding_schema>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the rule-vs-access reconciliation table</name>
  <files>.planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-security.md</files>
  <read_first>
    - firestore.rules (full file, 1,066 lines — read it; you need every match block's allow read/create/update/delete conditions)
    - 106-INVENTORY.md § Per-Collection Access Map (which collections/subcollections the code reads and writes, and from where)
    - CLAUDE.md § "Add New Collection" (the "Security Rules first" convention — context for why coverage matters)
  </read_first>
  <action>
    Write a `## Rule ↔ Access Reconciliation` table in 106-SCRATCH-security.md with one row per collection/subcollection (union of the firestore.rules match blocks AND the code-access collections from 106-INVENTORY.md). Columns:
    - collection (mark subcollections as `projects/*/activity_entries` etc.)
    - rule block? (firestore.rules:line, or NONE)
    - read gate (summarize the allow read condition)
    - write gate (summarize allow create/update/delete)
    - code access (read / write / listener — and which files, from the inventory; or NONE)
    - verdict placeholder (OK / over / under / dead / confirm-nesting — filled in Task 2)
    Confirm the subcollection NESTING: for each of activity_entries/progress_updates/issues/baselines/edit_history/audit_log, verify the rule is nested under `projects`/`services` (recon says it is, at the lines above) AND the code accesses it as `collection(db,'projects',id,'<sub>')` (per 106-INVENTORY.md) — a top-level rule would NOT govern a nested path, so record whether nesting matches access. Include the `databases/{database}/documents` meta-match as a note (it is the wrapper, not a collection).
  </action>
  <verify>
    <automated>grep -c "## Rule ↔ Access Reconciliation" .planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-security.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Rule ↔ Access Reconciliation" 106-SCRATCH-security.md` returns 1.
    - The table references >= 22 collections (all top-level rule blocks) — `grep -cE "mrfs|prs|pos|transport_requests|suppliers|projects|services|clients|users|role_templates|invitation_codes|rfps|collectibles|billing_requests|proposals|notifications|project_tasks|service_tasks|project_iterations|deleted_mrfs|deleted_users|client_errors" 106-SCRATCH-security.md` returns >= 20.
    - `grep -cE "activity_entries|progress_updates|issues|baselines|edit_history|audit_log" 106-SCRATCH-security.md` returns >= 5 (subcollection nesting rows present).
    - The table cites `firestore.rules:` line anchors — `grep -c "firestore.rules:" 106-SCRATCH-security.md` returns >= 15.
  </acceptance_criteria>
  <done>The rule-vs-access reconciliation table is written, covering every rule block and every code-accessed collection/subcollection with line anchors.</done>
</task>

<task type="auto">
  <name>Task 2: Flag over/under-permissioning, dead rules, and nesting mismatches</name>
  <files>.planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-security.md</files>
  <read_first>
    - The reconciliation table written in Task 1
    - firestore.rules:183-199 (the invitation_codes block — the allow read/update: if true lead) and any other block whose gate looks broad
  </read_first>
  <action>
    Walk the reconciliation table and, for each row with a problem, write an S-0N finding in the canonical schema:
    1. OVER-PERMISSIONING — a rule broader than the code needs or that exposes data. Confirm and write up `invitation_codes allow read: if true (firestore.rules:185)` and especially `allow update: if true (firestore.rules:191)`: unauthenticated clients can read AND mutate invitation codes. Judge whether the pre-auth register flow genuinely needs public READ (it likely does — code is read before an account exists) but public UPDATE is almost certainly over-permissioned (an attacker could tamper with codes). Severity: over-permissioned rule = High (D-08). Scan the other blocks for any similarly broad `if true` / missing-auth gate. NOTE the existing documented ACCEPTED RISK at firestore.rules:102 — frame the invitation_codes S-finding as "flagged despite documented acceptance — re-evaluate in Phase 112 (public UPDATE in particular)" so Phase 112 does not treat a deliberate, recorded decision as a fresh bug; the public READ may be legitimately required by the pre-auth register flow.
    2. UNDER-PERMISSIONING — any collection/subcollection the code READS or WRITES that has NO governing rule (or a rule stricter than the access requires), which would deny legitimate access. Severity: blocked legit access = High.
    3. DEAD / UNREFERENCED RULES — rule blocks whose collection no code touches (recon leads: deleted_users, audit_log). Resolve each: is it accessed dynamically (a path a naive grep missed) or genuinely dead? Dead rule = Low (cosmetic surface to trim in 112); if it turns out to be dynamic access, downgrade to OK with a note.
    4. NESTING MISMATCH — if any subcollection rule is NOT nested under the parent the code accesses it through, that is under/over-permissioning; write it up (recon suggests nesting is correct, so most likely these are OK — record the confirmation).
    Set handling=code-fix (rule change) + target_phase=112 for actionable findings; Low dead-rule items flow onto the AUDIT-06 deferral list.
  </action>
  <verify>
    <automated>grep -c "category: security-rules" .planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-security.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "category: security-rules" 106-SCRATCH-security.md` returns >= 2 (findings present).
    - `grep -ciE "invitation_codes" 106-SCRATCH-security.md` returns >= 1 AND a finding cites `firestore.rules:191` or `firestore.rules:185` (the if-true lead assessed).
    - `grep -c "severity: High" 106-SCRATCH-security.md` returns >= 1 (over/under-permissioning is High per D-08).
    - `grep -ciE "dead|unreferenced|deleted_users|audit_log" 106-SCRATCH-security.md` returns >= 1 (dead-rule leads resolved).
    - Every finding block has an `anchor:` line containing `firestore.rules:`.
    - `grep -cE "handling: (code-fix|backfill-script|defer)" 106-SCRATCH-security.md` >= 2 (schema complete); `grep -cE "target_phase: 112|defer" 106-SCRATCH-security.md` >= 1.
    - The invitation_codes finding acknowledges the prior documented acceptance — `grep -ciE "accepted risk|firestore.rules:102" 106-SCRATCH-security.md` returns >= 1 (a deliberate recorded decision is flagged for re-evaluation, not as a fresh bug).
  </acceptance_criteria>
  <done>Over/under-permissioning, dead rules, and nesting are assessed and written as S-0N findings with firestore.rules line anchors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → Firestore rules | Rules are the ONLY server-side authorization; a gap = direct data exposure or lockout |
| unauthenticated client → invitation_codes | Pre-auth register flow reads codes before an account exists; write access must not be public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-12 | Information Disclosure / Elevation | invitation_codes allow read/update: if true | mitigate | Audit flags firestore.rules:185,191 as over-permissioned (public update) → High finding for Phase 112 rule fix; this plan documents, does not edit |
| T-106-13 | Elevation of Privilege | collection accessed in code with no governing rule | mitigate | Reconciliation table surfaces any un-ruled access path as an under-permissioning High finding |
| T-106-14 | Repudiation | security finding without a rule line anchor | mitigate | Acceptance requires firestore.rules:line on every finding (D-07) |
| T-106-SC | Tampering | dependency installs | accept | Read-only grep + rules read; no installs, no rules edits, no rules-test suite (zero-build SPA, N/A) |
</threat_model>

<verification>
- 106-SCRATCH-security.md has the reconciliation table (>=15 firestore.rules line anchors) + >= 2 security-rule findings.
- invitation_codes if-true, under-permissioning, dead rules, and subcollection nesting all assessed.
- No edit to firestore.rules (read-only audit).
</verification>

<success_criteria>
- Per-collection rule-vs-access reconciliation complete; over/under-permissioning, dead rules, and nesting mismatches documented as S-0N findings (AUDIT-03, D-10).
- Findings use the D-07 schema with D-08 severities; actionable rule fixes targeted to Phase 112.
</success_criteria>

<output>
Create `.planning/phases/106-data-layer-audit-findings-report/106-06-SUMMARY.md` when done.
</output>
