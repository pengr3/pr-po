---
phase: 106-data-layer-audit-findings-report
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - .planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-correctness.md
autonomous: true
requirements: [AUDIT-04]
must_haves:
  truths:
    - "Every onSnapshot listener is checked for lifecycle correctness — tracked and unsubscribed in destroy(); untracked listeners and re-subscribe-without-teardown (tab-switch) leaks are flagged (AUDIT-04)"
    - "Read paths missing error handling and WRITE paths that can fail silently (no try/catch, no .catch) are flagged, with silent write failures rated High (AUDIT-04, D-08)"
    - "Legacy-unsafe field reads — JSON.parse(items_json) without guard, case-sensitive status assumptions, reads of fields absent on legacy docs — are flagged (AUDIT-04, success criterion 4)"
    - "Findings use the canonical D-07 schema with D-08 severities; v4.0 collections covered statically, live checks deferred to 112 (D-04)"
    - "A per-listener coverage ledger enumerates every onSnapshot call-site (~61) with a clean/flagged verdict, proving exhaustive lifecycle coverage — not just a sample (AUDIT-04, exhaustive-depth expectation)"
  artifacts:
    - path: ".planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-correctness.md"
      provides: "correctness findings (temp IDs C-0N) in canonical schema for Plan 07"
      contains: "category: correctness"
  key_links:
    - from: "106-SCRATCH-correctness.md"
      to: "106-FINDINGS.md (Plan 07)"
      via: "C-0N findings merged and re-IDed to F-00N by severity"
      pattern: "C-0[0-9]"
---

<objective>
Perform the static correctness audit (AUDIT-04) over the data layer using 106-INVENTORY.md as the anchor map: verify listener lifecycle (no leaks), read/write error handling, and legacy-safe field handling. Emit findings in the canonical D-07 schema for Plan 07 to merge.

Purpose: Prevent the new UI (107-111) from being built on leaky listeners, silently-failing writes, or reads that crash on legacy documents.
Output: `106-SCRATCH-correctness.md` — correctness findings (temp IDs C-0N) with file:line anchors, handling, and target phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/106-data-layer-audit-findings-report/106-CONTEXT.md
@.planning/phases/106-data-layer-audit-findings-report/106-INVENTORY.md

<finding_schema>
<!-- CANONICAL D-07 schema (identical across all dimension plans). This plan's TEMPID prefix is C- (C-01, C-02, ...). -->
### <TEMPID> — <one-line title>
- severity: High | Medium | Low
- category: correctness
- collection: <affected collection(s)>
- anchor: <file.js:line>[, ...]
- impact: <what goes wrong>
- recommendation: <concrete fix direction>
- handling: code-fix | backfill-script | defer
- target_phase: 112

<!-- D-08 rubric (correctness-relevant): listener leaks = Medium · missing error handling on non-critical reads = Medium · SILENT WRITE FAILURE on a write path that matters = High · a legacy read that shows wrong data = High, one that just log-warns = Low. -->

<!-- Recon leads (from 106-INVENTORY.md § Per-File Matrix): onSnapshot present but NO visible tracking array → project-detail.js(9), service-detail.js(8), home.js(4), mrf-form.js(2), engagement-create.js(2), auth.js(1, no destroy), notifications.js(1), permissions.js(1). Files WITH arrays but heavy re-init: finance.js(9), procurement.js(6), projects.js(4), user-management.js(4). Router does NOT call destroy() on same-view tab switches (CLAUDE.md) → re-init can double-subscribe. -->
</finding_schema>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit listener lifecycle (leaks + tab-switch re-subscribe)</name>
  <files>.planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-correctness.md</files>
  <read_first>
    - 106-INVENTORY.md § Per-File Matrix (the LISTENER-LIFECYCLE LEAD flags) + the onSnapshot group anchors
    - CLAUDE.md § "Firebase Listener Management" + "CRITICAL - Tab Navigation" (router does NOT call destroy() on same-view tab switches)
    - For each module with onSnapshot: read ONLY its init()/render() onSnapshot sites and its destroy() body (offset/limit reads around the anchors) — do NOT full-read the giant files
  </read_first>
  <action>
    For every module that calls onSnapshot (per 106-INVENTORY.md), verify the full lifecycle:
    1. Is each onSnapshot's unsubscribe return value CAPTURED (pushed to a listeners array or a named variable)?
    2. Is it CLEARED in destroy() (array iterated + unsubscribed, or the named var called)?
    3. TAB-SWITCH LEAK: since the router does NOT call destroy() on same-view tab switches (CLAUDE.md), does init() re-run and create NEW listeners without first clearing the old ones? Modules with multiple sub-tabs (procurement, finance, home) are the prime suspects — a re-init that pushes onto a non-cleared array accumulates duplicate listeners.
    For each gap write a C-0N finding: untracked listener (never unsubscribed) or re-subscribe-without-teardown. Name the module + onSnapshot line(s) in the anchor. Severity: listener leak = Medium (D-08). Call out the arr=0 modules (project-detail 9, service-detail 8, home 4) explicitly — confirm whether they use named-var cleanup or genuinely leak.

    COVERAGE LEDGER (exhaustiveness gate, per the D-09 exhaustive-depth expectation): before writing findings, build a `## Coverage Ledger — listeners` table in 106-SCRATCH-correctness.md with ONE ROW PER onSnapshot call-site from 106-INVENTORY.md (all ~61). Columns: `| onSnapshot site (file.js:line) | verdict | note |` where the verdict cell is EXACTLY `clean` or `flagged` (put "tracked+unsubscribed / untracked / re-subscribe-leak → C-0N" in the note cell). Copy every onSnapshot anchor verbatim from the inventory and classify each (surgical read of the module's destroy() where needed). This proves every listener was examined, not only the ones flagged.
  </action>
  <verify>
    <automated>grep -c "category: correctness" .planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-correctness.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "category: correctness" 106-SCRATCH-correctness.md` returns >= 1 after this task.
    - At least one listener finding names a specific module (e.g., project-detail.js / service-detail.js / home.js / finance.js / procurement.js) with an onSnapshot `file.js:line` anchor.
    - `grep -ciE "listener|onSnapshot|unsubscribe|destroy" 106-SCRATCH-correctness.md` returns >= 2.
    - `grep -c "severity: Medium" 106-SCRATCH-correctness.md` returns >= 1 (leaks rated Medium per D-08).
    - COVERAGE: the `## Coverage Ledger — listeners` table enumerates every onSnapshot site with a per-site verdict — `grep -cE "\|[[:space:]]*(clean|flagged)[[:space:]]*\|" 106-SCRATCH-correctness.md` returns >= 55 (near the 61 onSnapshot recon total; a shallow audit emitting only findings cannot satisfy this floor).
  </acceptance_criteria>
  <done>Listener lifecycle audited across all onSnapshot modules; leaks and tab-switch re-subscribe gaps written as C-0N findings.</done>
</task>

<task type="auto">
  <name>Task 2: Audit error handling + legacy-unsafe field reads</name>
  <files>.planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-correctness.md</files>
  <read_first>
    - 106-INVENTORY.md § the write groups (addDoc/setDoc/updateDoc/deleteDoc/writeBatch) and read groups (getDoc/getDocs)
    - CLAUDE.md § "Items Data Structure" (JSON.parse(items_json)), "Status Matching (Case-Sensitive)", and the suppliers `categories` note (absent/empty on legacy docs)
    - Surgical reads around the write/parse/status anchors only
  </read_first>
  <action>
    Two sweeps appended as more C-0N findings:
    1. ERROR HANDLING — for the write sites (updateDoc/addDoc/setDoc/deleteDoc/writeBatch) and read sites (getDoc/getDocs) from the inventory, check whether each is wrapped in try/catch or has a `.catch(...)` and surfaces failure to the user. A WRITE with no error handling where the user believes their change saved = SILENT WRITE FAILURE = High (D-08). A read with no handling that just renders empty = Medium. `grep -rnE "await (updateDoc|addDoc|setDoc|deleteDoc)\(" app --include=*.js` then check each for an enclosing try/catch. onSnapshot error callbacks: flag listeners with no error handler (second arg) as Medium if the collection can be permission-denied.
    2. LEGACY-UNSAFE READS — (a) `grep -rn "JSON.parse(" app --include=*.js` → every items_json parse NOT inside try/catch is a crash risk on malformed legacy data (cross-ref Plan 01's items_json warning count); (b) case-sensitive status reads that assume a specific casing/presence; (c) reads of fields absent on legacy docs (suppliers.categories, project_code/service_code on old MRFs) without a null/undefined guard. Severity: a read that shows WRONG data = High; one that throws-and-is-caught or log-warns = Low/Medium.
    Static coverage for v4.0 collections; defer any live verification to 112 (D-04).
  </action>
  <verify>
    <automated>grep -cE "JSON.parse|silent|error handling|try/catch|catch" .planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-correctness.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ciE "JSON.parse|items_json" 106-SCRATCH-correctness.md` returns >= 1 (the parse-guard risk is captured).
    - `grep -ciE "silent write|error handling|no try/catch|unguarded" 106-SCRATCH-correctness.md` returns >= 1.
    - `grep -c "category: correctness" 106-SCRATCH-correctness.md` returns >= 4 total across the file (exhaustive).
    - Every finding block has an `anchor:` line with a `.js:` reference.
    - `grep -cE "handling: (code-fix|backfill-script|defer)" 106-SCRATCH-correctness.md` >= 4 (schema complete on every finding).
    - `grep -cE "target_phase: 112|defer" 106-SCRATCH-correctness.md` >= 1 (Phase 112 hand-off encoded).
  </acceptance_criteria>
  <done>Error-handling gaps (incl. silent write failures) and legacy-unsafe reads written as C-0N findings with anchors, severities, handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static audit → repo | Read-only source analysis; no runtime, no DB, no credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-08 | Repudiation | correctness finding without a traceable anchor | mitigate | Acceptance requires a file:line anchor on every finding so Phase 112 can locate it (D-07) |
| T-106-09 | Denial of Service | undetected listener leak degrading the running app | mitigate | This audit's whole purpose — every onSnapshot lifecycle is checked and leaks flagged for Phase 112 remediation |
| T-106-SC | Tampering | dependency installs | accept | Read-only grep + surgical reads; no installs (zero-build SPA, N/A) |
</threat_model>

<verification>
- 106-SCRATCH-correctness.md has >= 4 correctness findings, each in canonical schema with a file:line anchor.
- Listener lifecycle, error handling (incl. silent write failure=High), and legacy-unsafe reads all covered.
</verification>

<success_criteria>
- Listener leaks, missing error handling / silent write failures, and legacy-unsafe reads documented as C-0N findings (AUDIT-04).
- Findings use the D-07 schema with D-08 severities; v4.0 live work deferred to 112 (D-04).
</success_criteria>

<output>
Create `.planning/phases/106-data-layer-audit-findings-report/106-04-SUMMARY.md` when done.
</output>
