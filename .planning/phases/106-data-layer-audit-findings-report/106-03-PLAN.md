---
phase: 106-data-layer-audit-findings-report
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - .planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-integrity.md
autonomous: true
requirements: [AUDIT-02]
must_haves:
  truths:
    - "The denormalized-field write paths (project_code / project_name / department copied into PR/PO/TR/RFP) are traced and any that can drift from the projects/services source of truth are flagged (AUDIT-02, D-02 alignment)"
    - "Orphan-creating code paths (deletes/cascades that leave dangling references) are identified with file:line anchors (AUDIT-02)"
    - "Status-derivation and case-sensitive status handling errors are flagged (AUDIT-02, success criterion 2)"
    - "The sequential-ID max-scan race (runTransaction = 0) is recorded as a High integrity finding"
    - "Every finding carries the D-07 rich schema (id/severity/category/collection/anchor/impact/recommendation/handling/target_phase) with severity per the D-08 rubric; v4.0 collections get static coverage with live measurement flagged to Phase 112 (D-04)"
    - "A coverage ledger enumerates every denormalized write-site and delete/cascade site (~46+) with a clean/flagged verdict, proving the whole integrity surface was examined — not just the flagged sites (AUDIT-02, exhaustive-depth expectation)"
  artifacts:
    - path: ".planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-integrity.md"
      provides: "integrity findings (temp IDs I-0N) in the canonical schema for Plan 07 to renumber and merge"
      contains: "category: integrity"
  key_links:
    - from: "106-SCRATCH-integrity.md"
      to: "106-FINDINGS.md (Plan 07)"
      via: "I-0N findings merged and re-IDed to F-00N by severity"
      pattern: "I-0[0-9]"
---

<objective>
Perform the static integrity audit (AUDIT-02, code side) over the data layer using 106-INVENTORY.md as the anchor map: trace denormalized-field write paths for drift, find orphan-creating delete/cascade paths, and flag status-derivation / case-sensitivity errors. Emit findings in the canonical D-07 schema so Plan 07 can merge them. This plan pairs with Plan 01's real-data drift numbers — this side finds the CODE mechanisms; Plan 01 measures them.

Purpose: Know exactly where the code can produce drifted, orphaned, or mis-derived records before the new UI (107-111) is built on top.
Output: `106-SCRATCH-integrity.md` — integrity findings (temp IDs I-0N) with file:line anchors, handling, and suggested target phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/106-data-layer-audit-findings-report/106-CONTEXT.md
@.planning/phases/106-data-layer-audit-findings-report/106-INVENTORY.md
@.planning/phases/106-data-layer-audit-findings-report/106-DATA-RESULTS.md

<finding_schema>
<!-- CANONICAL D-07 schema — every finding in every dimension scratch file uses this exact block. Plan 07 renumbers temp IDs to global F-00N by severity. -->
### <TEMPID> — <one-line title>
- severity: High | Medium | Low        (per the D-08 rubric below)
- category: integrity | correctness | efficiency | security-rules
- collection: <affected collection(s)>
- anchor: <file.js:line>[, <file.js:line> ...]
- impact: <what goes wrong for the user / data>
- recommendation: <concrete fix direction>
- handling: code-fix | backfill-script | defer
- target_phase: 112            (or a later phase; Low+defer flow onto the AUDIT-06 deferral list)

This plan's TEMPID prefix is I- (I-01, I-02, ...).

<!-- D-08 severity rubric -->
High   = wrong data shown; denormalized DRIFT misrepresenting a record; ORPHANED refs breaking a flow; over/under-permissioned rules; SILENT write failures.
Medium = listener leaks; missing error handling on non-critical reads; N+1; redundant listeners/reads; client-side filtering that should be a query; missing limit().
Low    = caching opportunities; micro-optimizations; scale-only concerns; cosmetic inconsistencies.
</finding_schema>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace denormalized-field write paths for drift (project_code / project_name / department)</name>
  <files>.planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-integrity.md</files>
  <read_first>
    - 106-INVENTORY.md § Per-Collection Access Map + the addDoc/setDoc/updateDoc/writeBatch groups (the write sites to inspect)
    - CLAUDE.md § "Firebase Firestore Schema" (which fields are denormalized copies vs source of truth in projects/services)
    - The specific write-site line ranges only — Read with offset/limit around each anchor; do NOT full-read procurement.js / finance.js
  </read_first>
  <action>
    Find every place the denormalized fields are COPIED onto a downstream doc and judge whether that copy can drift from the `projects`/`services` source of truth. Concretely:
    - `grep -rnE "project_code|project_name|department" app --include=*.js` and intersect with the write-site anchors from 106-INVENTORY.md (addDoc/setDoc/updateDoc/writeBatch). These are the copy points (MRF, PR, PO, TR, RFP creation and edit).
    - For each copy point, read the surrounding lines and determine the SOURCE of the copied value: is it re-fetched from the current project/service doc, or copied from a possibly-stale in-memory object / an upstream doc that itself may be stale? A copy from a stale upstream = drift mechanism.
    - Check the project/service EDIT paths: when a project_name or department is edited on the source doc, is there ANY propagation to existing downstream MRF/PR/PO/TR/RFP copies? (Almost certainly not — record the "no back-propagation → historical copies drift on rename" mechanism as a finding, cross-referenced to Plan 01's measured drift count in 106-DATA-RESULTS.md.)
    - v4.0 collections (proposals, rfps, billing_requests, collectibles): cover their denorm copies STATICALLY here; if live measurement is needed, set handling=defer, target_phase=112 (D-04). Do NOT run live queries.
    Write each mechanism as an I-0N finding in the canonical schema. Severity: drift that misrepresents a record = High (D-08).
  </action>
  <verify>
    <automated>grep -c "category: integrity" .planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-integrity.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "category: integrity" 106-SCRATCH-integrity.md` returns >= 2 after this task (drift findings present).
    - At least one finding names `project_code`, `project_name`, or `department` in its collection/impact and cites a write-site `file.js:line` anchor.
    - At least one finding references the projects/services rename → no-back-propagation drift mechanism.
    - `grep -cE "handling: (code-fix|backfill-script|defer)" 106-SCRATCH-integrity.md` >= 2 (schema handling field present).
  </acceptance_criteria>
  <done>Denormalized write-path drift mechanisms traced and written as I-0N integrity findings with anchors.</done>
</task>

<task type="auto">
  <name>Task 2: Find orphan-creating paths, status-derivation errors, and the ID-race finding</name>
  <files>.planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-integrity.md</files>
  <read_first>
    - 106-INVENTORY.md § the deleteDoc / writeBatch groups (cascade/delete sites) and the Per-Operation Totals row for runTransaction
    - CLAUDE.md § "Procurement Workflow", the `deleted_mrfs` soft-delete-with-cascade note, and the "Status Matching (Case-Sensitive)" + "Sequential ID Generation" sections
    - The specific delete/status/ID-gen line ranges only (offset/limit reads)
  </read_first>
  <action>
    Three integrity sweeps, appended as more I-0N findings:
    1. ORPHAN CREATION — for each deleteDoc / writeBatch cascade site: does deleting a parent (project, MRF, supplier, proposal) leave dangling child references? Check the soft-delete cascade (deleted_mrfs cascading PRs/POs/TRs per CLAUDE.md) for completeness — does it also handle RFPs, notifications, edit_history, activity_entries? A cascade that misses a child collection = orphan-creation finding. Severity: orphaned refs that break a flow = High; orphans that are merely cosmetic = Low.
    2. STATUS DERIVATION — `grep -rnE "=== '(Pending|Approved|Rejected|Procuring|Procured|Delivered|Pending Procurement)'|status ===|\.status\b|finance_status|procurement_status" app --include=*.js`. Flag: (a) any lowercase/mismatched status literal that will silently never match (case-sensitive per CLAUDE.md); (b) any place a parent status is DERIVED from children (e.g., MRF status from its PRs) where the derivation can disagree with the stored value. Severity: wrong status shown = High; a defensive-but-unreachable branch = Low.
    3. SEQUENTIAL-ID RACE — record the confirmed `runTransaction = 0` (from 106-INVENTORY.md): all *_id generation is max-scan of existing docs with no transaction, so two concurrent submits can mint the same ID. Write as a High integrity finding (collection: mrfs/prs/pos/transport_requests + the *-id.js generators), handling=code-fix, target_phase=112.
    Static coverage for v4.0 collections; flag any live-measurement need as defer→112 (D-04).

    COVERAGE LEDGER (exhaustiveness gate, per the D-09 exhaustive-depth expectation): build a `## Coverage Ledger — integrity surface` table in 106-SCRATCH-integrity.md with ONE ROW PER integrity-relevant site from 106-INVENTORY.md — every denormalized-field write/copy site (from Task 1) PLUS every deleteDoc and writeBatch cascade site (Task 2). Columns: `| site (file.js:line) | verdict | note |` where the verdict cell is EXACTLY `clean` or `flagged` (put "can-drift / creates-orphan / safe → I-0N" in the note cell). Copy each anchor verbatim from the inventory and classify it. This proves the audit examined the whole write/delete surface, not just the sites it flagged.
  </action>
  <verify>
    <automated>grep -cE "runTransaction|race|orphan" .planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-integrity.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "runTransaction|race" 106-SCRATCH-integrity.md` returns >= 1 (the ID-race finding is present).
    - `grep -ci "orphan" 106-SCRATCH-integrity.md` returns >= 1 (an orphan-creation finding is present).
    - `grep -c "severity: High" 106-SCRATCH-integrity.md` returns >= 1 (at least one High integrity finding).
    - `grep -c "category: integrity" 106-SCRATCH-integrity.md` returns >= 4 total across the file (exhaustive, not a single lead).
    - Every finding block has an `anchor:` line containing a `.js:` reference (or `verify-integrity.js`/generator anchor for the ID-race finding).
    - `grep -cE "target_phase: 112|defer" 106-SCRATCH-integrity.md` >= 1 (Phase 112 hand-off encoded, D-04).
    - COVERAGE: the `## Coverage Ledger — integrity surface` table enumerates every denorm write-site + delete/cascade site with a per-site verdict — `grep -cE "\|[[:space:]]*(clean|flagged)[[:space:]]*\|" 106-SCRATCH-integrity.md` returns >= 25 (the delete/cascade surface alone is ~46 sites; a shallow audit emitting only findings cannot satisfy this floor).
  </acceptance_criteria>
  <done>Orphan, status-derivation, and ID-race integrity findings written as I-0N with anchors, severities, and handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static audit → repo | Read-only source analysis; no runtime, no DB writes, no credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-06 | Repudiation | integrity finding without a traceable anchor | mitigate | Acceptance requires every finding to carry a file:line anchor so Phase 112 can locate and act on it (D-07) |
| T-106-07 | Information Disclosure | scratch file citing data mechanisms | accept | Only code paths and field names are described, no live data values; internal .planning artifact |
| T-106-SC | Tampering | dependency installs | accept | Read-only grep + surgical file reads; no installs (zero-build SPA, N/A) |
</threat_model>

<verification>
- 106-SCRATCH-integrity.md has >= 4 integrity findings, each in the canonical schema with a file:line anchor.
- Drift write-path, orphan-creation, status-derivation, and ID-race mechanisms all covered.
- v4.0 live measurement flagged defer→112.
</verification>

<success_criteria>
- Denormalized-field drift mechanisms, orphan-creating paths, status-derivation errors, and the ID race are documented as I-0N findings (AUDIT-02).
- Findings use the D-07 schema with D-08 severities; v4.0 live work deferred to 112 (D-04).
</success_criteria>

<output>
Create `.planning/phases/106-data-layer-audit-findings-report/106-03-SUMMARY.md` when done.
</output>
