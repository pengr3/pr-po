---
phase: 106-data-layer-audit-findings-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-integrity.js
  - .planning/phases/106-data-layer-audit-findings-report/106-DATA-RESULTS.md
autonomous: false
requirements: [AUDIT-02]
must_haves:
  truths:
    - "The extended verify-integrity.js adds ONLY a denormalized drift-across-chain check over MRF → PR → PO → TR → RFP (D-02) — no other new checks"
    - "Every Firestore operation in the extended script is read-only .get() — no set/update/add/delete/writeBatch anywhere (D-02)"
    - "The script can target clmc-procurement-dev (validation) and clmc-procurement (prod numbers) without editing the hardcoded default (D-05)"
    - "106-DATA-RESULTS.md records the real prod numbers from the existing live checks (per-collection counts, errors, warnings, orphans) plus the new drift results (D-03)"
    - "The script's existing 13-collection live checks still run unchanged (D-03) and v4.0-collection live checks are NOT added (D-04)"
  artifacts:
    - path: "scripts/verify-integrity.js"
      provides: "checkDenormDrift() over MRF→PR→PO→TR→RFP + a --project targeting flag; existing checks untouched"
      contains: "checkDenormDrift"
    - path: ".planning/phases/106-data-layer-audit-findings-report/106-DATA-RESULTS.md"
      provides: "real read-only data-pass numbers (dev validation confirmation + prod counts + drift findings) that AUDIT-02 findings cite"
      contains: "Denormalization Drift"
  key_links:
    - from: "scripts/verify-integrity.js checkDenormDrift"
      to: "rfps collection"
      via: "read-only fetch joined by po_id / tr_id"
      pattern: "rfps"
    - from: "106-DATA-RESULTS.md"
      to: "106-FINDINGS.md (Plan 07)"
      via: "real counts feeding integrity findings"
      pattern: "drift|orphan"
---

<objective>
Perform the read-only real-data pass that makes AUDIT-02 "verified against data", not merely code-reasoned (D-01). Extend `scripts/verify-integrity.js` MINIMALLY — add exactly one new capability, the denormalized drift-across-chain check over the MRF → PR → PO → TR → RFP chain (D-02) — plus the minimal `--project` targeting flag required to validate on dev before running prod (D-05). Then run it read-only (dev first, then prod) and capture the real numbers into `106-DATA-RESULTS.md` for Plan 07 to cite.

Purpose: The static audit (Plans 02-06) is backed by actual data so drift/orphan/integrity findings carry real counts, not guesses (D-01, D-03).
Output: Extended `scripts/verify-integrity.js` (drift check + `--project` flag, all read-only) and `106-DATA-RESULTS.md` (dev validation + prod numbers + drift results).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/106-data-layer-audit-findings-report/106-CONTEXT.md
@scripts/verify-integrity.js

<interfaces>
<!-- Existing structures in verify-integrity.js the new check reuses. Do NOT redefine. -->
fetchCollection(name) -> { docs, count, error }   // read-only db.collection(name).get()
COLLECTIONS = [mrfs, prs, pos, transport_requests, suppliers, projects, services, users, clients, role_templates, invitation_codes, deleted_mrfs, deleted_users]   // 13 existing
mrfToPrs   // { mrf_id: [pr_id,...] }  already built
prToPos    // { pr_id:  [po_id,...] }  already built
results = { collections{}, errors[], warnings[], info[], summary{...} }   // add: results.drift[]
admin.initializeApp({ ..., projectId: 'clmc-procurement' })   // line ~90 — hardcoded prod default to make overridable

<!-- RFP linkage (project memory: RFP.po_doc_id/tr_doc_id are unreliable) -->
Join RFP → PO by business id rfp.po_id == po.po_id ; RFP → TR by rfp.tr_id == tr.tr_id. NEVER trust rfp.po_doc_id / rfp.tr_doc_id.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add checkDenormDrift() + a --project targeting flag (read-only)</name>
  <files>scripts/verify-integrity.js</files>
  <read_first>
    - scripts/verify-integrity.js (full file, 564 lines — you MUST read fetchCollection, the COLLECTIONS array, the mrfToPrs/prToPos maps, the results object, and BOTH output formatters before editing)
    - CLAUDE.md § "Firebase Firestore Schema" (the denormalized project_code / project_name / department fields flow MRF → PR → PO → TR → RFP)
    - .planning/phases/106-data-layer-audit-findings-report/106-CONTEXT.md D-02, D-04, D-05 (scope of the ONE new check; do not scope-creep)
  </read_first>
  <action>
    Make exactly two minimal edits to scripts/verify-integrity.js (per D-02 "extend minimally — add ONLY the drift check" + D-05 dev-targeting). Everything read-only.

    EDIT A — `--project` targeting flag (D-05, required because the script hardcodes prod projectId):
    - In the CLI-flag block (near `const JSON_MODE = args.includes('--json')`), parse `--project <id>`: `const projIdx = args.indexOf('--project'); const PROJECT_ID = projIdx !== -1 ? args[projIdx + 1] : (process.env.GCLOUD_PROJECT || 'clmc-procurement');`
    - Change `admin.initializeApp({ credential: ..., projectId: 'clmc-procurement' })` to use `projectId: PROJECT_ID`. Default stays prod so existing behavior is unchanged.
    - Add `--project <id>` to the `--help` text (OPTIONS section). Do NOT change any check logic here.

    EDIT B — the drift-across-chain check (D-02, the ONE new capability):
    - Add `'rfps'` to the COLLECTIONS array so RFPs are fetched read-only (needed for the RFP tail of the chain). Do NOT add any other collection and do NOT add any new orphan/schema/live check for v4.0 collections — that is Phase 112 (D-04).
    - Add `results.drift = []` to the results object and `results.summary.drift = 0`.
    - Add `function checkDenormDrift(data, results)` and call it in Step 3 (after the existing referential-integrity checks). Algorithm (all in-memory over already-fetched docs — NO extra reads):
      - For each MRF, treat the MRF as the head-of-chain reference for the three fields `project_code`, `project_name`, `department`.
      - Gather its downstream docs: PRs where `pr.mrf_id === mrf.mrf_id`; POs where `po.mrf_id === mrf.mrf_id` (also POs reachable via those PRs' pr_id); TRs where `tr.mrf_id === mrf.mrf_id`; RFPs where `rfp.po_id` matches any of those POs' `po_id` OR `rfp.tr_id` matches any of those TRs' `tr_id` (join by business id per project memory — NEVER use rfp.po_doc_id / rfp.tr_doc_id).
      - For each of the three fields, for each downstream doc: if the field is PRESENT on both the MRF and the downstream doc AND they disagree, push a drift record: `` `DRIFT ${field}: MRF ${mrf.mrf_id}="${mrfVal}" vs ${downstreamType} ${downstreamId}="${downstreamVal}"` ``. If a downstream doc is MISSING the field entirely, do NOT count it as drift (legacy-absent is a separate schema concern) — record it under warnings only if you choose, but drift = present-but-disagrees.
    - In `runVerification` Step 6, set `results.summary.drift = results.drift.length`.
    - In `printHumanReadable`, add a "Denormalization Drift (MRF → PR → PO → TR → RFP)" section printing each `results.drift` line as `[DRIFT]`, and add `Drift: ${results.summary.drift}` to the Summary block. JSON mode needs no change (it serializes the whole results object).

    HARD CONSTRAINT (D-02, threat T-106-01): the extended script performs NO writes. Use only `db.collection(name).get()` reads. Do NOT introduce `.set(`, `.update(`, `.delete(`, `.add(`, `.create(`, or `writeBatch(` anywhere.
  </action>
  <verify>
    <automated>node --check scripts/verify-integrity.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/verify-integrity.js` exits 0.
    - `grep -c "checkDenormDrift" scripts/verify-integrity.js` returns >= 2 (definition + call).
    - `grep -cE "'rfps'|\"rfps\"" scripts/verify-integrity.js` returns >= 1 (RFPs added to the read-only fetch).
    - `grep -cE "project_code|project_name|department" scripts/verify-integrity.js` returns >= 3 (the three drift fields referenced in the new check).
    - `grep -cE "\.set\(|\.update\(|\.delete\(|\.add\(|\.create\(|writeBatch\(" scripts/verify-integrity.js` returns 0 (read-only only — T-106-01).
    - `grep -cE "po_doc_id|tr_doc_id" scripts/verify-integrity.js` returns 0 (RFP joined by business id, not the unreliable doc-id fields).
    - `grep -c "\-\-project" scripts/verify-integrity.js` returns >= 2 (flag parsed + documented in --help).
    - No new orphan/live check for v4.0 collections was added: `grep -cE "collectibles|billing_requests|baselines|progress_updates|issues|proposals" scripts/verify-integrity.js` returns 0 (D-04 — v4.0 live checks stay in Phase 112).
  </acceptance_criteria>
  <done>Extended script parses, adds only checkDenormDrift + --project flag, remains read-only, does not add v4.0 live checks.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The extended read-only `verify-integrity.js` (Task 1). It now needs to actually RUN against Firestore to produce the numbers for the report. Running requires firebase-admin + a service-account key, which the executor may not hold — so this is a gated human-action step. D-05: validate on dev FIRST, then run read-only against prod for the numbers that go into the report.</what-built>
  <how-to-verify>
    The executor should ATTEMPT these itself first; if it fails with a missing-credentials / missing-key error, the human runs them and pastes the output.

    1. DEV validation run (proves the extended script is valid against a real DB):
       `GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.dev.json node scripts/verify-integrity.js --project clmc-procurement-dev --json`
       (Use whatever dev service-account key file exists; the point is projectId must be the dev project.) Expect it to complete without a stack trace. Dev may be sparse/stale — that is fine, this run only proves validity.
    2. PROD numbers run (the real system of record, read-only):
       `node scripts/verify-integrity.js --json`  (defaults to projectId clmc-procurement, uses ./serviceAccountKey.json)
    3. Paste BOTH JSON outputs (or at minimum: the `collections` counts, `summary` block, and the full `errors`, `warnings`, `info`, and `drift` arrays from the PROD run; plus a one-line "dev ran clean" confirmation).

    Note: this is READ-ONLY (`.get()` only). It writes NOTHING to either database.
  </how-to-verify>
  <acceptance_criteria>
    - Dev run completed without a fatal stack trace (script validity confirmed against a real DB).
    - Prod run produced a JSON object containing `collections`, `summary`, `errors`, `warnings`, `info`, and `drift`.
  </acceptance_criteria>
  <resume-signal>Paste the dev "ran clean" confirmation and the prod --json output (at least collections + summary + errors + warnings + info + drift), or paste the credential error if neither could run.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Capture real numbers into 106-DATA-RESULTS.md</name>
  <files>.planning/phases/106-data-layer-audit-findings-report/106-DATA-RESULTS.md</files>
  <read_first>
    - The pasted dev + prod output from the checkpoint above (the source of every number recorded here)
    - .planning/phases/106-data-layer-audit-findings-report/106-CONTEXT.md D-03 (existing live-check counts go into the report) and D-04 (v4.0 live measurement is flagged, not run)
  </read_first>
  <action>
    Write `106-DATA-RESULTS.md` capturing the read-only data pass so Plan 07 can cite hard numbers. Sections:
    1. `## Run Metadata` — date, dev project id + "validation: ran clean" (D-05), prod project id (clmc-procurement), script version note ("read-only .get() only").
    2. `## Collection Document Counts (prod)` — a table of every collection in the prod `collections` block with its doc count (D-03).
    3. `## Referential Integrity Errors` — the prod `errors` array verbatim, with the count. If 0, state "0 — no dangling PR→MRF / PO→PR / TR→MRF references".
    4. `## Schema & Reference Warnings` — the prod `warnings` count and a categorized digest (missing-field vs invalid-status vs supplier/project_code reference warnings).
    5. `## Orphan Detection (info)` — the prod `info` array (Approved MRFs with no PRs; finance-Approved PRs with no POs) with counts.
    6. `## Denormalization Drift (MRF → PR → PO → TR → RFP)` — the prod `drift` array verbatim with the count; per-field breakdown (project_code / project_name / department). This is the D-02 new-capability result.
    7. `## Phase 112 hand-off note` — one line: live drift/orphan measurement for the v4.0 collections (proposals, collectibles, billing_requests, rfps orphans, baselines) is DEFERRED to Phase 112 (D-04); this pass covered the existing 13-collection checks + the drift chain only.
    Every number in this file MUST come from the pasted run output — do not invent values. If the prod run could not execute, write the file with an explicit "PROD RUN PENDING — numbers to be filled from `node scripts/verify-integrity.js --json`" banner at the top and leave placeholders, so the gap is visible rather than fabricated.
  </action>
  <verify>
    <automated>grep -c "Denormalization Drift" .planning/phases/106-data-layer-audit-findings-report/106-DATA-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Collection Document Counts" 106-DATA-RESULTS.md` returns 1.
    - `grep -c "## Denormalization Drift" 106-DATA-RESULTS.md` returns 1.
    - `grep -c "## Referential Integrity Errors" 106-DATA-RESULTS.md` returns 1.
    - `grep -cE "Phase 112|D-04" 106-DATA-RESULTS.md` returns >= 1 (the deferral hand-off is recorded).
    - The file contains a metadata line naming both `clmc-procurement-dev` and `clmc-procurement` (D-05 dev-first-then-prod).
  </acceptance_criteria>
  <done>106-DATA-RESULTS.md records dev validation + prod counts + drift results, or an explicit PENDING banner if the run was blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit script → prod Firestore | The script connects to the production system of record; it must only ever READ |
| service-account key → executor | The privileged admin key grants full DB access; the executor may not hold it, so runs are gated to a human-action checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Tampering | extended verify-integrity.js writing to prod | mitigate | New code uses ONLY `db.collection(name).get()`; acceptance asserts zero `.set(`/`.update(`/`.delete(`/`.add(`/`.create(`/`writeBatch(`; dev-first validation before prod (D-05) |
| T-106-02 | Information Disclosure | 106-DATA-RESULTS.md storing prod data | accept | Only aggregate counts + integrity messages (IDs like MRF-2026-003) are recorded, no financial totals or PII beyond record IDs already in .planning docs; low sensitivity, internal repo |
| T-106-03 | Denial of Service | full-collection .get() scans on prod | accept | Read-only full scans match the script's existing behavior; run frequency is one-off; current data volumes are small (single-org procurement) |
| T-106-SC | Tampering | dependency installs | accept | No new dependency — firebase-admin is already the script's declared prerequisite; zero-build SPA (N/A) |
</threat_model>

<verification>
- `node --check scripts/verify-integrity.js` exits 0.
- checkDenormDrift present; rfps added to fetch; zero write ops in the script.
- `--project` flag parsed and documented.
- 106-DATA-RESULTS.md contains the drift + integrity + collection-count sections (or an explicit PENDING banner).
</verification>

<success_criteria>
- verify-integrity.js extended with ONLY the drift-across-chain check + a dev-targeting flag, fully read-only (D-02, D-05).
- Existing 13-collection live checks unchanged; no v4.0 live checks added (D-03, D-04).
- Real prod numbers (or an explicit pending banner) captured in 106-DATA-RESULTS.md for Plan 07 (D-01, D-03).
</success_criteria>

<output>
Create `.planning/phases/106-data-layer-audit-findings-report/106-01-SUMMARY.md` when done.
</output>
