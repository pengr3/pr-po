---
phase: 106-data-layer-audit-findings-report
plan: 07
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - .planning/phases/106-data-layer-audit-findings-report/106-FINDINGS.md
autonomous: true
requirements: [AUDIT-01]
must_haves:
  truths:
    - "106-FINDINGS.md is the single severity-ranked report ordered High → Low, each finding tagged with its category (D-06)"
    - "A summary table at the top (ID · severity · category · collection · one-line) doubles as the index Phase 112 consumes (D-06)"
    - "Every finding has a stable F-00N id and the full D-07 schema (severity, category, collection, file:line anchor, impact, recommendation, handling, suggested target phase)"
    - "Every temp finding from all four dimension scratch files (I-/C-/E-/S-) is merged into the report with no drops, each carrying a 'was:' trace back to its temp id"
    - "The report includes the inventory summary (AUDIT-01) and the real data-pass numbers from 106-DATA-RESULTS.md (D-03), so integrity findings cite measured drift/orphan counts"
  artifacts:
    - path: ".planning/phases/106-data-layer-audit-findings-report/106-FINDINGS.md"
      provides: "the single ranked findings report — the AUDIT-01 deliverable and the Phase 112 hand-off contract"
      contains: "| F-0"
  key_links:
    - from: "106-FINDINGS.md summary table"
      to: "Phase 112 (AUDIT-06/07)"
      via: "stable F-00N IDs + handling + target_phase fields cited by remediation"
      pattern: "F-0[0-9][0-9]"
---

<objective>
Assemble the single deliverable: `106-FINDINGS.md`, the severity-ranked findings report (D-06/D-07/D-08). Merge the four dimension scratch files (integrity, correctness, efficiency, security-rules) and the real data-pass numbers into one report — assign stable F-00N IDs, sort High → Low, build the summary-table index, and confirm no finding is dropped. This is the AUDIT-01 deliverable and the Phase 112 hand-off contract.

Purpose: One ranked report that guides the new UI (107-111) away from broken data-layer patterns and gives Phase 112 a directly-citable fix/defer list.
Output: `106-FINDINGS.md` — summary table + High/Medium/Low sections + inventory summary + data-pass summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/106-data-layer-audit-findings-report/106-CONTEXT.md
@.planning/phases/106-data-layer-audit-findings-report/106-INVENTORY.md
@.planning/phases/106-data-layer-audit-findings-report/106-DATA-RESULTS.md
@.planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-integrity.md
@.planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-correctness.md
@.planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-efficiency.md
@.planning/phases/106-data-layer-audit-findings-report/106-SCRATCH-security.md

<report_contract>
<!-- D-06 report structure -->
# Data-Layer Audit — Findings Report (Phase 106)
## Summary Table            <- the Phase 112 index; columns: ID | Severity | Category | Collection | One-line
## High Severity            <- full finding blocks (D-07 schema), High first (D-06 High→Low)
## Medium Severity
## Low Severity
## Inventory Summary         <- AUDIT-01: totals from 106-INVENTORY.md (reads/writes/listeners/queries)
## Data-Pass Results         <- D-03: real prod numbers from 106-DATA-RESULTS.md (counts, errors, orphans, drift)
## Phase 112 Hand-off        <- how the deferral list (Low+defer) and fix list (High/Med) are consumed (AUDIT-06/07)

<!-- D-07 per-finding block (final, with global id + trace) -->
### F-00N — <title>
- severity / category / collection / anchor / impact / recommendation / handling / target_phase
- was: <temp id, e.g. I-03>
</report_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge, re-ID, and rank all findings into 106-FINDINGS.md</name>
  <files>.planning/phases/106-data-layer-audit-findings-report/106-FINDINGS.md</files>
  <read_first>
    - 106-SCRATCH-integrity.md, 106-SCRATCH-correctness.md, 106-SCRATCH-efficiency.md, 106-SCRATCH-security.md (all four — the source findings)
    - 106-CONTEXT.md D-06 (one ranked report + summary table), D-07 (rich schema + stable IDs), D-08 (severity rubric)
  </read_first>
  <action>
    Collect every finding block from the four scratch files (temp IDs I-/C-/E-/S-). Assign global stable IDs F-001, F-002, ... in severity order: all High findings first (F-001..), then Medium, then Low (D-06 High→Low). Within a severity band, group by category for readability. For each finding, carry the full D-07 schema and add a `- was: <temp id>` trace line so Phase 112 (and this plan's own completeness check) can map every F-00N back to its origin. Do NOT drop or silently merge findings — if two dimension findings are genuinely the same issue, merge into one F-00N and list BOTH temp IDs in the `was:` line (so the count still reconciles).
    Then write:
    1. `# Data-Layer Audit — Findings Report (Phase 106)` title + a one-paragraph scope line (whole app/ Firestore layer, 35 files + firestore.rules; report-not-fixes; fixes are Phase 112).
    2. `## Summary Table` — a markdown table with columns `ID | Severity | Category | Collection | One-line`, one row per F-00N, ordered High→Low. This IS the Phase 112 index (D-06).
    3. `## High Severity`, `## Medium Severity`, `## Low Severity` sections, each containing the full finding blocks for that band.
    Every finding block MUST keep its `anchor:` (file:line or firestore.rules:line), `handling:` (code-fix / backfill-script / defer), and `target_phase:` fields (D-07).
  </action>
  <verify>
    <automated>grep -c "^| F-0" .planning/phases/106-data-layer-audit-findings-report/106-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### F-0" 106-FINDINGS.md` returns >= 12 (all dimension findings merged; scratch minimums sum to ~14).
    - `grep -c "^| F-0" 106-FINDINGS.md` (summary-table rows) equals `grep -c "^### F-0" 106-FINDINGS.md` (every finding has exactly one table row — no orphaned rows, no unlisted findings).
    - `grep -cE "## High Severity|## Medium Severity|## Low Severity" 106-FINDINGS.md` returns 3, and `## High Severity` appears at an earlier line than `## Low Severity` (High→Low order, D-06).
    - The summary table header contains `ID`, `Severity`, `Category`, `Collection` (D-06 columns).
    - All four categories appear: `grep -cE "integrity|correctness|efficiency|security-rules" 106-FINDINGS.md` returns >= 4.
    - Every finding carries the schema: `grep -c "handling:" 106-FINDINGS.md` and `grep -c "target_phase:" 106-FINDINGS.md` each return >= `grep -c "^### F-0"` count.
    - `grep -c "was:" 106-FINDINGS.md` >= `grep -c "^### F-0"` count (every F-00N traces to a temp id).
  </acceptance_criteria>
  <done>106-FINDINGS.md has the summary-table index + High/Medium/Low finding blocks, every finding re-IDed to F-00N with full schema and a temp-id trace.</done>
</task>

<task type="auto">
  <name>Task 2: Add inventory + data-pass summaries and run the completeness reconciliation</name>
  <files>.planning/phases/106-data-layer-audit-findings-report/106-FINDINGS.md</files>
  <read_first>
    - 106-INVENTORY.md § Per-Operation Totals (reads/writes/listeners/queries) and Per-Collection Access Map
    - 106-DATA-RESULTS.md (real prod counts, errors, orphans, drift — or its PENDING banner)
  </read_first>
  <action>
    Append three sections to 106-FINDINGS.md:
    1. `## Inventory Summary` — the AUDIT-01 inventory half: the per-operation call-site totals (onSnapshot/getDoc/getDocs/updateDoc/addDoc/setDoc/deleteDoc/writeBatch/runTransaction/where/limit) and the collection surface, lifted from 106-INVENTORY.md. State runTransaction=0 and the where-vs-limit imbalance as the two headline inventory facts. Link out to 106-INVENTORY.md for the full file:line index.
    2. `## Data-Pass Results` — the D-03 real numbers from 106-DATA-RESULTS.md: per-collection doc counts, referential-integrity error count, schema-warning count, orphan count, and the measured denormalization-drift count across MRF→PR→PO→TR→RFP. Where an integrity finding (F-00N) has a measured number, cross-reference it here (e.g., "F-003 drift — N records measured, see 106-DATA-RESULTS.md"). If 106-DATA-RESULTS.md is PENDING (prod run blocked), carry that banner forward so the gap is visible.
    3. `## Phase 112 Hand-off` — state that the summary table is the fix/defer index for AUDIT-06: High/Medium findings (handling=code-fix/backfill-script) are the remediation queue behind the review gate; Low findings (handling=defer) are the tracked deferral list; backfill-script findings build on the drift-aware verify-integrity.js (AUDIT-07). Note (per D-04) that live drift/orphan measurement for the v4.0 collections is a Phase 112 task.
    Then RECONCILE completeness: count the temp IDs across the four scratch files and confirm every one is represented in a `was:` trace in 106-FINDINGS.md. Record the reconciliation result as a one-line `<!-- reconciliation: N temp findings → M F-IDs, 0 dropped -->` comment near the top.
  </action>
  <verify>
    <automated>grep -cE "## Inventory Summary|## Data-Pass Results|## Phase 112 Hand-off" .planning/phases/106-data-layer-audit-findings-report/106-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "## Inventory Summary|## Data-Pass Results|## Phase 112 Hand-off" 106-FINDINGS.md` returns 3.
    - `grep -cE "runTransaction|where|limit" 106-FINDINGS.md` returns >= 2 (inventory headline facts present).
    - `grep -ciE "drift|orphan" 106-FINDINGS.md` returns >= 1 (data-pass results carried in).
    - `grep -cE "reconciliation" 106-FINDINGS.md` returns >= 1 (the no-drops check is recorded).
    - `grep -ciE "deferral|defer" 106-FINDINGS.md` returns >= 1 AND `grep -ci "AUDIT-06\|AUDIT-07\|Phase 112" 106-FINDINGS.md` returns >= 1 (hand-off contract present).
    - Reconciliation holds: the number of distinct temp IDs across the four scratch files equals the number of temp IDs referenced in `was:` lines (no finding dropped).
  </acceptance_criteria>
  <done>Inventory summary, data-pass results, and Phase 112 hand-off appended; completeness reconciliation confirms no dropped findings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report artifact → Phase 112 | 106-FINDINGS.md is the authorization surface for what gets fixed; a dropped or mis-ranked finding = an unaddressed data-layer risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-15 | Repudiation / Tampering | a dimension finding silently lost in the merge | mitigate | Every F-00N carries a `was:` trace; Task 2 reconciles temp-ID count vs was-reference count and records "0 dropped" |
| T-106-16 | Information Disclosure | report aggregating rule gaps + data counts | accept | Internal .planning artifact; contains code anchors + aggregate counts, no secrets/PII beyond record IDs already in-repo |
| T-106-SC | Tampering | dependency installs | accept | Read + write of markdown only; no installs (zero-build SPA, N/A) |
</threat_model>

<verification>
- 106-FINDINGS.md has a summary table whose row count equals the finding-block count.
- Findings ordered High→Low; all four categories present; every finding has anchor + handling + target_phase + was-trace.
- Inventory summary, data-pass results, and Phase 112 hand-off present; reconciliation shows 0 dropped.
</verification>

<success_criteria>
- Single ranked report 106-FINDINGS.md with the summary-table index and full D-07 finding schema (AUDIT-01, D-06, D-07, D-08).
- Real data-pass numbers (D-03) and inventory totals (AUDIT-01) folded in; Phase 112 hand-off (AUDIT-06/07) explicit.
- No dimension finding dropped (reconciliation recorded).
</success_criteria>

<output>
Create `.planning/phases/106-data-layer-audit-findings-report/106-07-SUMMARY.md` when done.
</output>
