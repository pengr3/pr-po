---
phase: 83-notification-system-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01
  - 02
  - 03
  - 04
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05
  - NOTIF-06
  - NOTIF-13
must_haves:
  truths:
    - "All 7 NOTIF requirements pass UAT against the dev Firebase project (clmc-procurement-dev)"
    - "Firestore Security Rules + composite indexes are deployed to the dev project before any production-like UAT"
    - "Test harness (`test/firestore.test.js`) — IF Plan 01 added rule tests — passes via `npm test`"
    - "Mobile UAT pass at 375px viewport: bell visible alongside brand and hamburger"
    - "Cross-user permission test: user A cannot read user B's notifications (manual or rule-test)"
    - "Production Firebase project (clmc-procurement) has the rules + indexes deployed"
  artifacts:
    - path: ".planning/phases/83-notification-system-foundation/83-05-SUMMARY.md"
      provides: "Final UAT results table with pass/fail per NOTIF requirement"
    - path: ".planning/phases/83-notification-system-foundation/83-HUMAN-UAT.md"
      provides: "Captured UAT execution log"
  key_links:
    - from: "Local app at localhost:8000"
      to: "Dev Firebase project (clmc-procurement-dev)"
      via: "isLocal env detection in app/firebase.js"
      pattern: "clmc-procurement-dev"
    - from: "Phase 83 deploy"
      to: "Production Firebase (clmc-procurement)"
      via: "firebase deploy --only firestore:rules,firestore:indexes (deferred until UAT pass)"
      pattern: "firebase deploy"
---

<objective>
Validate the entire Phase 83 plumbing end-to-end via the structured UAT script in `83-VALIDATION.md`, deploy `firestore.rules` + `firestore.indexes.json` (rules-first per CLAUDE.md), and confirm production parity. This plan is the GATE between Phase 83 implementation and milestone completion — no code changes here, only verification, deployment, and STATE/ROADMAP updates.

Purpose: Plans 01-04 ship code. Plan 05 confirms the code actually delivers the seven success criteria from ROADMAP.md and the seven `must_haves` listed in this phase. Without this plan, Phase 83 is "code shipped, behavior unverified" — the same trap that produced the v3.2 73.2 / 79 process gaps.

Output:
- `.planning/phases/83-notification-system-foundation/83-HUMAN-UAT.md` — captured UAT execution log
- `.planning/phases/83-notification-system-foundation/83-05-SUMMARY.md` — final pass/fail per requirement
- Production deploy of rules + indexes (after dev UAT passes)
- Updated STATE.md + ROADMAP.md reflecting Phase 83 complete
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/83-notification-system-foundation/83-CONTEXT.md
@.planning/phases/83-notification-system-foundation/83-RESEARCH.md
@.planning/phases/83-notification-system-foundation/83-VALIDATION.md
@.planning/phases/83-notification-system-foundation/83-01-SUMMARY.md
@.planning/phases/83-notification-system-foundation/83-02-SUMMARY.md
@.planning/phases/83-notification-system-foundation/83-03-SUMMARY.md
@.planning/phases/83-notification-system-foundation/83-04-SUMMARY.md

@CLAUDE.md
@firestore.rules
@firestore.indexes.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Deploy firestore.rules and firestore.indexes.json to DEV project, then verify</name>
  <files>(no source code changes — deploy step only; reads firestore.rules and firestore.indexes.json from disk)</files>
  <read_first>
    - firestore.rules (verify the notifications block from Plan 01 is in place)
    - firestore.indexes.json (verify the two new notifications indexes are in place)
    - .planning/phases/83-notification-system-foundation/83-01-SUMMARY.md (verify Plan 01 conclusion)
    - .planning/phases/83-notification-system-foundation/83-RESEARCH.md "Environment Availability" section — Firebase CLI assumed available
  </read_first>
  <action>
    **Pre-deploy sanity check:**
    1. Run `node -e "JSON.parse(require('fs').readFileSync('firestore.indexes.json','utf8'))"` — must exit 0 (valid JSON).
    2. Run `grep -c "match /notifications/{docId}" firestore.rules` — must return `1`.
    3. Run `grep -c '"collectionGroup": "notifications"' firestore.indexes.json` — must return `2`.

    **Deploy to DEV project:**
    Project ID: `clmc-procurement-dev` (per `app/firebase.js` devConfig).

    Run:
    ```bash
    firebase use clmc-procurement-dev
    firebase deploy --only firestore:rules,firestore:indexes
    ```

    Wait for the index build to complete (composite indexes can take 1-5 minutes for empty collections). Confirm in Firebase Console → Firestore → Indexes that both new notifications indexes are listed and enabled.

    **Verification of deploy:**
    - Run `firebase firestore:indexes` — output should include the two notifications indexes
    - Open Firebase Console → Firestore → Rules tab → confirm the `notifications` block matches local source
    - Note the deploy timestamp into the SUMMARY

    **Production deploy is DEFERRED to Task 4 (after UAT passes).** Do NOT deploy to `clmc-procurement` (prod) yet.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('firestore.indexes.json','utf8')); const c=require('fs').readFileSync('firestore.rules','utf8'); const ok=c.includes('match /notifications/{docId}'); const j=JSON.parse(require('fs').readFileSync('firestore.indexes.json','utf8')); const i=j.indexes.filter(x=>x.collectionGroup==='notifications').length; console.log('rules-source-ok:', ok, 'indexes-count:', i); process.exit(ok && i===2 ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - Pre-deploy sanity checks all pass (rules block present, indexes JSON valid, two notifications indexes present)
    - `firebase deploy --only firestore:rules,firestore:indexes` completes without error
    - Firebase Console confirms both new notifications indexes are enabled (Built status)
    - Deploy timestamp is captured in 83-05-SUMMARY.md
    - Production deploy is NOT yet executed (gated by Task 4)
  </acceptance_criteria>
  <done>
    Dev Firebase project has the new rules and indexes. UAT in Tasks 2-3 will run against this deployed state.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HUMAN UAT — Run all 7 NOTIF requirement tests against localhost:8000 + dev project</name>
  <files>.planning/phases/83-notification-system-foundation/83-HUMAN-UAT.md (created by this task to record results)</files>
  <read_first>
    - .planning/phases/83-notification-system-foundation/83-VALIDATION.md (Per-Task Verification Map — full procedure for NOTIF-01..06, NOTIF-13)
    - .planning/phases/83-notification-system-foundation/83-CONTEXT.md (D-01..D-20 — every observed behavior must match a locked decision)
    - .planning/phases/83-notification-system-foundation/83-02-SUMMARY.md (verify __createTestNotification API signature)
    - .planning/phases/83-notification-system-foundation/83-03-SUMMARY.md (verify which DOM IDs to query: #notifBell, #notifBadge, #notifDropdownMenu, etc.)
  </read_first>
  <action>
    This is a **blocking human checkpoint**. Claude SHOULD NOT mark complete autonomously — the user must observe the bell behavior live and confirm or describe failures.

    **What was built (Plans 01-04):**
    - Bell icon + unread badge in top nav (visible on desktop and mobile)
    - Dropdown showing last 10 notifications with type, message, time, source link
    - Click row → mark read + navigate + close dropdown (atomic)
    - Per-row mark-read button + "Mark all as read" header button
    - Full history page at #/notifications, paginated 20/page, includes read + unread
    - `notifications` Firestore collection with per-user Security Rules
    - Dev-only `window.__createTestNotification()` for seeding test data

    **HUMAN UAT PROCEDURE — execute each block and record PASS/FAIL in `.planning/phases/83-notification-system-foundation/83-HUMAN-UAT.md`:**

    **Setup (one-time):**
    1. Start local server: `python -m http.server 8000` (in repo root)
    2. Open http://localhost:8000 in TWO browsers / TWO tabs (User A and User B for cross-user tests)
    3. Open DevTools Console + Network tab in both
    4. Verify the orange "DEV ENVIRONMENT — clmc-procurement-dev" banner appears at the bottom (confirms dev project routing)
    5. Log in as two different active users (one per tab/browser)

    **NOTIF-01 — Bell icon + real-time unread badge (~2 min):**
    1. In tab A: confirm `document.querySelector('#notifBell')` returns a non-null element
    2. In tab A: confirm `document.querySelector('#notifBadge')` exists; should have `display: none` if zero unreads
    3. In tab B: open Console, get user_id from `getCurrentUser().uid` and copy it
    4. In tab A: run `await __createTestNotification({ user_id: '<user_B_uid>', type: 'MRF_APPROVED', message: 'Test for B', link: '#/' })` — pass user_id override to target B
    5. In tab B: within ~2s the badge should appear and show "1" without page refresh
    6. Network tab in B: see the `Listen` request streaming updates from Firestore
    - PASS criteria: Badge appears live in tab B without refresh

    **NOTIF-02 — Bell click → dropdown shows last 10 (~2 min):**
    1. Seed at least 3 notifications for the active user via `__createTestNotification()`
    2. Click `#notifBell`
    3. Confirm `#notifDropdownMenu` toggles `.open` class
    4. Confirm up to 10 rows render: type indicator, message text, relative time, clickable behavior
    5. Confirm "Mark all as read" header button visible
    6. Confirm "View all notifications" footer link visible (`href="#/notifications"`)
    - PASS criteria: Dropdown opens; ≤10 rows; header + footer visible

    **NOTIF-03 — Click row → navigate + mark read + close (~2 min):**
    1. Seed: `await __createTestNotification({ link: '#/procurement/records', message: 'Test deep link', type: 'MRF_APPROVED' })`
    2. Click bell, then click the row
    3. Confirm: URL hash changes to `#/procurement/records`
    4. Confirm: Network tab shows ONE `updateDoc` request to `notifications/{id}` with read=true and read_at
    5. Confirm: dropdown closes (`.open` class removed)
    6. Confirm: badge count decrements by 1
    - PASS criteria: All four observable signals occur

    **NOTIF-04 — Mark single notification as read (~1 min):**
    1. With ≥1 unread, click the per-row checkmark button (NOT the row body)
    2. Confirm: row visually flips to read state (loses `.notif-row--unread`)
    3. Confirm: dropdown stays open; URL hash unchanged
    4. Confirm: Network tab shows ONE `updateDoc`
    - PASS criteria: Row updates, dropdown stays open, no navigation

    **NOTIF-05 — Mark all as read (~1 min):**
    1. Seed ≥5 unread notifications via repeated `__createTestNotification()` calls
    2. Open dropdown — confirm "Mark all as read" button is ENABLED
    3. Click it
    4. Confirm: Network tab shows ONE batch commit (writeBatch atomic)
    5. Confirm: badge drops to zero within ~1s; badge hides
    6. Confirm: button becomes DISABLED
    7. Reload page; with zero unreads, confirm button is disabled on first dropdown open
    - PASS criteria: Single batch commit, badge → 0, button disables, persists

    **NOTIF-06 — History page paginated 20/page (~3 min):**
    1. Seed ≥25 notifications mixing read and unread
    2. Open dropdown → click "View all notifications" footer link
    3. Confirm URL hash = `#/notifications`
    4. Confirm browser tab title = "Notifications | CLMC Operations"
    5. Confirm 20 rows render
    6. Confirm "Older" button enabled, "Newer" button disabled (first page)
    7. Click "Older" — next page renders, "Newer" now enabled
    8. Click "Newer" — back to first 20 rows
    9. Confirm BOTH read and unread rows appear (visually distinct via `.notif-row--unread`)
    - PASS criteria: Pagination both directions; read+unread both visible; correct page title

    **NOTIF-13 — Per-user Security Rules + persisted schema (~3 min):**

    Schema check:
    1. Run `await __createTestNotification()` once
    2. In Firebase Console → Firestore → notifications collection → open the new doc
    3. Confirm all 10 fields exist: `user_id`, `type`, `message`, `link`, `source_collection`, `source_id`, `actor_id`, `read` (false), `read_at` (null), `created_at` (timestamp)
    - PASS criteria: All 10 fields present with correct types

    Cross-user permission check (manual fallback if Plan 01 deferred rule-tests):
    - In tab B console, attempt to read tab A's notification doc id via `getDoc(doc(window.db, 'notifications', '<tabA_doc_id>'))` — must throw `permission-denied`
    - In tab B console, attempt to write a notification with `actor_id: '<wrong_uid>'` — must throw `permission-denied` (actor_id != request.auth.uid guard)
    - PASS criteria: Cross-user reads denied; impersonation denied

    **MOBILE UAT (D-02) (~1 min):**
    1. Resize viewport to 375px (or use DevTools device toolbar — iPhone SE)
    2. Confirm: bell + badge remain visible alongside brand and hamburger
    3. Confirm: `.nav-links` is hidden (verify Elements tab — `display: none`)
    4. Confirm: clicking the bell opens the dropdown at near-full viewport width
    - PASS criteria: Bell visible at 375px, dropdown opens

    **STALE STATE TEST (Pitfall 3) (~1 min):**
    1. Log out from tab A
    2. Log in as a DIFFERENT user in tab A
    3. Confirm: bell badge count reflects ONLY the new user's unreads (NOT the previous user's count even momentarily)
    - PASS criteria: No stale state across re-login

    **Capture results in `.planning/phases/83-notification-system-foundation/83-HUMAN-UAT.md`** — one row per requirement with PASS/FAIL/notes. Include the page reload + cross-tab seeding evidence (Console snippets, Network tab observations).

    **If any UAT block fails:** Do NOT mark this task done. Surface the failure to the user; trigger `/gsd:plan-phase 83 --gaps` to plan gap closure. Resume only after fixes ship.

    **resume-signal:** User types "approved" once all 7 requirements PASS in 83-HUMAN-UAT.md. Without explicit approval, downstream tasks (Task 4 production deploy) MUST NOT execute.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/83-notification-system-foundation/83-HUMAN-UAT.md'; if(!fs.existsSync(p)){console.log('UAT-file-missing — checkpoint not yet completed by human');process.exit(0);} const s=fs.readFileSync(p,'utf8'); const reqs=['NOTIF-01','NOTIF-02','NOTIF-03','NOTIF-04','NOTIF-05','NOTIF-06','NOTIF-13']; const allPresent=reqs.every(r=>s.includes(r)); const passCount=(s.match(/PASS/g)||[]).length; console.log('all-requirements-listed:',allPresent,'pass-count:',passCount);"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 NOTIF requirements have a PASS verdict in 83-HUMAN-UAT.md
    - Mobile UAT (D-02) passes
    - Stale state test (Pitfall 3) passes
    - Schema check confirms all 10 fields persist
    - Cross-user permission test confirms denial (rule-test OR manual)
    - 83-HUMAN-UAT.md exists in the phase directory with timestamps
    - User has explicitly approved (resume-signal)
  </acceptance_criteria>
  <done>
    User has signed off on Phase 83's behavior against the dev environment. Production deploy can proceed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: (Conditional) Run rule-unit-tests if Plan 01 added them</name>
  <files>(test/firestore.test.js — read-only; test execution only)</files>
  <read_first>
    - test/firestore.test.js (head of file — confirm whether Plan 01 added the notifications describe block)
    - .planning/phases/83-notification-system-foundation/83-01-SUMMARY.md (Plan 01 verdict — ADD or DEFER)
  </read_first>
  <action>
    **PRE-CHECK:** Read `83-01-SUMMARY.md`. If verdict was DEFER, skip this task and document in SUMMARY: "Rule-unit-tests deferred per Plan 01 verdict; manual UAT in Task 2 covers cross-user permission denial."

    **IF Plan 01 verdict was ADD:**

    1. Ensure Firebase emulator is installed and accessible (per existing `test/package.json` script).
    2. Run the test suite:
       ```bash
       cd test
       npm test
       ```
    3. All 10+ new notification rule cases must pass (along with all pre-existing tests; do NOT regress).
    4. Capture the test output in 83-05-SUMMARY.md (count of passing tests, count of failing tests, failure messages if any).

    **If tests fail:** Document failures in SUMMARY and trigger `/gsd:plan-phase 83 --gaps` for gap closure. Do NOT proceed to deploy (Task 4).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); if(!fs.existsSync('.planning/phases/83-notification-system-foundation/83-01-SUMMARY.md')){console.log('Plan 01 SUMMARY missing — skipping');process.exit(0);} const s=fs.readFileSync('.planning/phases/83-notification-system-foundation/83-01-SUMMARY.md','utf8'); const verdict=/Verdict.*ADD/i.test(s)?'ADD':/Verdict.*DEFER/i.test(s)?'DEFER':'UNKNOWN'; console.log('Plan-01-verdict:', verdict);"</automated>
  </verify>
  <acceptance_criteria>
    - IF DEFER: SUMMARY documents the skip rationale
    - IF ADD: `cd test && npm test` exits 0 with all rule tests passing
    - IF ADD: at least 10 new notification-related test cases pass
    - No regression in pre-existing rule tests
  </acceptance_criteria>
  <done>
    Either rule-unit-tests pass against the new rules (case ADD) or the task is explicitly skipped (case DEFER).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: PRODUCTION DEPLOY — firestore.rules + firestore.indexes.json to clmc-procurement</name>
  <files>(no source code changes — Firebase CLI deploy step; reads firestore.rules and firestore.indexes.json)</files>
  <read_first>
    - firestore.rules (final state to be deployed)
    - firestore.indexes.json (final state to be deployed)
    - .planning/phases/83-notification-system-foundation/83-HUMAN-UAT.md (Task 2 result — must show all PASS)
  </read_first>
  <action>
    This is a **blocking human checkpoint** — production deployment requires explicit user approval. Per CLAUDE.md "Important Notes / Testing & Deployment": "No staging - writes to production Firebase".

    **What is being deployed:**
    - `firestore.rules` — adds the `match /notifications/{docId}` block from Plan 01 (D-17/D-18/D-19 predicates)
    - `firestore.indexes.json` — adds two composite indexes for notifications: `(user_id, created_at desc)` and `(user_id, read, created_at desc)`

    **Pre-deploy gates (Claude verifies, then asks user to approve):**
    1. Confirm Task 2 UAT was approved — read `83-HUMAN-UAT.md` and verify all 7 NOTIF requirements show PASS
    2. Confirm `git status` is clean OR all Phase 83 commits have been pushed to the working branch
    3. Confirm production Firebase project ID: `clmc-procurement` (per `app/firebase.js` prodConfig)

    **Deploy commands (executed only after user approves):**
    ```bash
    firebase use clmc-procurement
    firebase deploy --only firestore:rules,firestore:indexes
    ```

    Wait for the index build to complete (1-5 minutes for empty collections; longer if populated).

    **Post-deploy verification:**
    1. Open Firebase Console → Firestore → Rules tab on the PRODUCTION project — confirm the `notifications` block matches local `firestore.rules`
    2. Open Firebase Console → Firestore → Indexes tab — confirm the two notifications indexes show "Enabled"
    3. Verify production app on Netlify (the deployed URL): log in as an active user, confirm bell renders. NO Firestore writes from this verification step — just confirm the bell mounts (the JS bundle deploys via Netlify pushes earlier in Plans 02-04 commits)
    4. Capture deploy timestamp + Console screenshot location in 83-05-SUMMARY.md

    **If anything fails:** STOP. Do NOT push more code. Triage; if the fix requires code changes trigger gap closure. If the fix is a missed step (e.g., wrong project alias), redo this task.

    **resume-signal:** User types "approved" once production deploy is confirmed and verified — OR describes failure for triage.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/83-notification-system-foundation/83-HUMAN-UAT.md'; if(!fs.existsSync(p)){console.log('UAT-file-missing — Task 2 not complete; deploy blocked');process.exit(1);} const s=fs.readFileSync(p,'utf8'); const reqs=['NOTIF-01','NOTIF-02','NOTIF-03','NOTIF-04','NOTIF-05','NOTIF-06','NOTIF-13']; const failHits=(s.match(/FAIL/g)||[]).length; console.log('Task-2-UAT-file-present:true','fail-mentions:',failHits); if(failHits>0)console.log('WARN: FAIL string in UAT file — verify those are not active failures');"</automated>
  </verify>
  <acceptance_criteria>
    - 83-HUMAN-UAT.md exists and shows PASS for all 7 NOTIF requirements before deploy
    - Production Firebase project (`clmc-procurement`) has the notifications rules block deployed (verifiable in Firebase Console)
    - Both production indexes show "Enabled" status
    - Production app loads with bell visible for active users
    - Deploy timestamps captured in 83-05-SUMMARY.md
    - User has approved the deploy (resume-signal)
  </acceptance_criteria>
  <done>
    Phase 83 plumbing is live in production. Phase 84 can begin wiring real triggers.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Update STATE.md and ROADMAP.md to reflect Phase 83 shipped</name>
  <files>
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/STATE.md (full — capture position, last_activity, blockers/concerns)
    - .planning/ROADMAP.md (full — confirm Phase 83 entry shape; this lives under "v4.0 Procurement → Full Management Portal")
    - .planning/phases/83-notification-system-foundation/83-05-SUMMARY.md (this plan's summary — confirm UAT pass + deploy success)
  </read_first>
  <action>
    **Edit .planning/STATE.md:**
    1. Update frontmatter:
       - `stopped_at`: "Phase 83 complete; ready for Phase 84"
       - `last_updated`: today's ISO timestamp
       - `progress.completed_phases`: increment by 1 (0 → 1)
       - `progress.total_plans`: add 5 (Phase 83's plan count, 0 → 5)
       - `progress.completed_plans`: add 5 (0 → 5)
       - `progress.percent`: recompute (1 / 7 * 100 = ~14)
    2. Update Current Position section: Phase number → 2 of 7 (Phase 84 — Notification Triggers — Existing Events); Status: Ready to plan
    3. Update Last activity: today, "Phase 83 shipped: bell + dropdown + history page + per-user Security Rules + composite indexes deployed to production"
    4. Update Progress bar visualization (use ▰ for filled, ░ for empty)
    5. Remove Phase 83 design blockers from "Blockers/Concerns" (notifications schema + rules are now LOCKED + DEPLOYED). Keep Phase 86, 87, 88 design notes.
    6. In "Roadmap Evolution": append "2026-04-29: Phase 83 shipped (5 plans). Plumbing now available for Phase 84 trigger sites."

    **Edit .planning/ROADMAP.md:**
    1. Find the Phase 83 entry under "v4.0 Procurement → Full Management Portal" (line ~220)
    2. Update the checkbox: `[ ] **Phase 83: ...**` → `[x] **Phase 83: ...** — completed YYYY-MM-DD (5 plans)`
    3. In the Progress table at the bottom (line ~334), change Phase 83 row: Plans column to `5/5`, Status to `Complete`, Completed to today's date

    **Do NOT edit .planning/PROJECT.md in this task** — milestone-level evolution happens at v4.0 milestone close, not per-phase.

    **Commit message format** (matches existing pattern from prior phases):
    ```
    docs(83): mark Phase 83 complete in STATE and ROADMAP

    Phase 83 (Notification System Foundation) shipped 5 plans, all 7 NOTIF
    requirements pass UAT. Rules + indexes deployed to production. Phase 84
    can begin wiring triggers.
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/STATE.md','utf8'); const r=fs.readFileSync('.planning/ROADMAP.md','utf8'); console.log('STATE-phase-83-complete:', s.includes('Phase 83') && (s.includes('complete') || s.includes('Complete') || s.includes('shipped'))); console.log('ROADMAP-phase-83-checked:', /\\[x\\][^\\n]*Phase 83/i.test(r) || /Phase 83[^\\n]*complete/i.test(r)); console.log('ROADMAP-table-updated:', /83[^\\n]*v4\\.0[^\\n]*5\\/5|83[^\\n]*Complete/i.test(r));"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` reflects Phase 83 as complete; `progress.completed_phases` = 1
    - `.planning/ROADMAP.md` Phase 83 entry has `[x]` checkbox AND completion date
    - `.planning/ROADMAP.md` Progress table row for Phase 83 shows `5/5`, `Complete`, today's date
    - Both files still parse as valid Markdown (no broken frontmatter)
    - `git log --oneline -1` shows the docs commit with the correct message format
  </acceptance_criteria>
  <done>
    Project state reflects Phase 83 completion. STATE.md guides the next session to Phase 84 planning.
  </done>
</task>

</tasks>

<verification>
- All 7 NOTIF requirements have PASS verdicts in `83-HUMAN-UAT.md`
- Dev Firebase project has rules + indexes deployed
- Production Firebase project has rules + indexes deployed
- (If Plan 01 added) Rule-unit-tests pass via `npm test`
- STATE.md + ROADMAP.md updated to reflect Phase 83 shipped
- No regressions in existing v3.2 functionality (manual smoke: load Procurement, Finance, Projects views — all still work)
</verification>

<success_criteria>
1. User sees a bell icon in the top navigation with an unread count badge that updates in real time as notifications are created. (NOTIF-01)
2. User can click the bell to open a dropdown listing the last 10 notifications with type, message, source link, and time. (NOTIF-02)
3. User can click any notification (in dropdown or history page) and is navigated to its source record via deep link. (NOTIF-03)
4. User can mark a single notification read or mark-all-as-read in one click, and unread count updates immediately. (NOTIF-04 + NOTIF-05)
5. User can open a full notification history page that paginates 20 per page including read items. (NOTIF-06)
6. Notifications persist in Firestore with locked schema; per-user Security Rules verified. (NOTIF-13)
7. Plumbing is ready for Phase 84 to wire actual triggers without re-asking schema or UX questions.
</success_criteria>

<output>
After completion, create `.planning/phases/83-notification-system-foundation/83-05-SUMMARY.md` containing:
- Pre-deploy sanity check results
- Dev deploy timestamp + outcome
- UAT results table: 7 rows (NOTIF-01..06, NOTIF-13) × columns (Procedure, Observed, Pass/Fail, Notes)
- Mobile UAT result (D-02)
- Stale state test result (Pitfall 3)
- Rule-unit-tests result (PASS / SKIP per Plan 01 verdict / FAIL with details)
- Production deploy timestamp + outcome
- Final phase status: SHIPPED
- Pointers for Phase 84: which trigger sites to wire next, which helpers to call (`createNotification` for direct + `createNotificationForRoles` for fan-out), the locked NOTIFICATION_TYPES enum
</output>
