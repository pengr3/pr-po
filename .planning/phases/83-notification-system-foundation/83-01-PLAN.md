---
phase: 83-notification-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestore.rules
  - firestore.indexes.json
  - test/firestore.test.js
autonomous: true
requirements:
  - NOTIF-13
must_haves:
  truths:
    - "firestore.rules contains a `match /notifications/{docId}` block"
    - "firestore.indexes.json contains two composite indexes for the notifications collection"
    - "Test infrastructure investigation has produced a written go/no-go decision recorded in this plan's SUMMARY"
    - "If go: test/firestore.test.js contains rule-unit-tests for D-17/D-18/D-19 covering cross-user read denial, actor_id impersonation block, immutable-field guard on update"
  artifacts:
    - path: "firestore.rules"
      provides: "Per-user Security Rules for notifications collection"
      contains: "match /notifications/{docId}"
    - path: "firestore.indexes.json"
      provides: "Composite indexes for unread badge query and history page query"
      contains: '"collectionGroup": "notifications"'
  key_links:
    - from: "firestore.rules notifications block"
      to: "isActiveUser() helper"
      via: "rule body uses existing helper at firestore.rules:56-58"
      pattern: "isActiveUser\\(\\)"
    - from: "firestore.indexes.json notifications indexes"
      to: "Bell badge query (Plan 02 listener)"
      via: "(user_id, read, created_at desc) composite index"
      pattern: "user_id.*read.*created_at"
---

<objective>
Lock down the storage layer for the notifications collection: ship the per-user Security Rules block, the two composite indexes the bell-badge and history-page queries will require, and (conditionally) the rule-unit-tests that prove the rules are correct. This MUST land in the same commit as the notifications module that will write to the collection (Plan 02), but it ships in Wave 1 because rules-first is mandatory per CLAUDE.md "Add New View" checklist — without rules, even Super Admin gets blocked.

Purpose: Satisfy NOTIF-13 (per-user persistence + Security Rules) and prevent Pitfall 5 (composite-index-missing → silent query failure). Honor CLAUDE.md "Add Security Rules first" rule and Phase 11 lesson.

Output:
- `firestore.rules` with `match /notifications/{docId}` block following the file's stated template (lines 6-39)
- `firestore.indexes.json` with two new composite index entries
- `test/firestore.test.js` with new notification rule cases IF the harness is current (decision documented in SUMMARY)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/83-notification-system-foundation/83-CONTEXT.md
@.planning/phases/83-notification-system-foundation/83-RESEARCH.md
@.planning/phases/83-notification-system-foundation/83-VALIDATION.md

@CLAUDE.md
@firestore.rules
@firestore.indexes.json
@test/firestore.test.js

<interfaces>
<!-- Existing helper functions in firestore.rules (DO NOT redefine) -->

From firestore.rules:46-89:
```
function isSignedIn() { return request.auth != null; }
function getUserData() { return get(/databases/$(database)/documents/users/$(request.auth.uid)).data; }
function isActiveUser() { return isSignedIn() && getUserData().status == 'active'; }
function hasRole(roles) { return isActiveUser() && getUserData().role in roles; }
```

From firestore.rules:430-445 (the rfps block — the most recent collection added in v3.2; use as a structural precedent for placement and comment style):
```
// =============================================
// rfps collection - Request for Payment
// =============================================
match /rfps/{rfpId} {
  allow read: if isActiveUser();
  ...
}
```

From firestore.indexes.json existing structure (use this object shape when appending the notifications indexes):
```json
{
  "collectionGroup": "<name>",
  "queryScope": "COLLECTION",
  "fields": [
    { "fieldPath": "<field>", "order": "ASCENDING|DESCENDING" }
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate test/firestore.test.js currency and decide ADD vs DEFER</name>
  <files>test/firestore.test.js (READ ONLY in this task; the decision drives Task 3)</files>
  <read_first>
    - test/firestore.test.js (full file — count tests, identify which collections are covered, check whether v3.2 collections like `rfps` have rule tests)
    - .planning/phases/83-notification-system-foundation/83-RESEARCH.md (Open Question 1, lines 644-648)
    - test/package.json (check `npm test` command, dependencies)
  </read_first>
  <action>
    Read `test/firestore.test.js` end to end. Determine:
    1. Does it test the v3.2 collections `rfps`, `services`, `assignments`, `deleted_users`? List the collections currently covered.
    2. Does the harness still run? Look for syntax compatible with `@firebase/rules-unit-testing` v2+ (`initializeTestEnvironment`, `assertSucceeds`, `assertFails`).
    3. Is there a working `npm test` script in `test/package.json`? Read it; do not run it (Wave 0 verification only — the actual test add-or-defer happens in Task 3).

    Record the verdict in this format inside the eventual SUMMARY:
    - **Verdict:** ADD or DEFER
    - **Evidence:** "Harness covers collections X, Y, Z; last update visible at line N; npm test command is `<cmd>`"
    - **If DEFER:** Document the carry-over as a follow-up in `.planning/STATE.md` Pending Todos so v4.1 picks it up. Do NOT add tests in Task 3.
    - **If ADD:** Proceed to Task 3 with full vigour.

    No file writes in this task (other than scratch notes that go into the SUMMARY at end of plan).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('test/firestore.test.js','utf8'); console.log('lines:', c.split('\\n').length, 'has-rfps:', c.includes('rfps'), 'has-rules-unit-testing:', c.includes('rules-unit-testing'));"</automated>
  </verify>
  <acceptance_criteria>
    - Investigation conclusion (ADD or DEFER) is written into the executor's working notes (committed in SUMMARY at end of plan)
    - The notes cite specific line numbers from `test/firestore.test.js` as evidence
    - If DEFER, an entry is appended to `.planning/STATE.md` "Pending Todos" — verifiable by `grep "test/firestore.test.js" .planning/STATE.md` returning a new line
  </acceptance_criteria>
  <done>
    A documented ADD-or-DEFER decision exists. Task 3's behavior is determined.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add notifications block to firestore.rules + composite indexes to firestore.indexes.json</name>
  <files>
    firestore.rules,
    firestore.indexes.json
  </files>
  <read_first>
    - firestore.rules (FULL FILE — needed to find the correct insertion point right after the `rfps` block at lines 430-445 and BEFORE the closing braces at 446-447)
    - firestore.indexes.json (full file — to append to the `indexes` array, before the closing `]` and the `fieldOverrides` key)
    - .planning/phases/83-notification-system-foundation/83-CONTEXT.md (D-05 schema, D-17, D-18, D-19, D-20 — quote them in the file comments)
    - .planning/phases/83-notification-system-foundation/83-RESEARCH.md Pattern 6 lines 357-401, Pattern 7 lines 404-431 (use these EXACT predicates)
  </read_first>
  <behavior>
    - Active user can read their own notification (D-17)
    - Active user CANNOT read another user's notification (cross-user read denied)
    - Active user can list their own notifications when query includes `where('user_id','==',uid)`
    - Active user can create a notification for any user, but `actor_id` MUST equal `request.auth.uid` (prevents impersonation)
    - Create requires `read == false` and string types on `user_id`, `type`, `message`, `link` (D-18)
    - Active user can update only their own notification, and only `read` and `read_at` fields are mutable (D-19); `user_id`, `type`, `message`, `link`, `created_at`, `actor_id` are pinned via `request.resource.data.X == resource.data.X`
    - Active user can delete only their own notification (D-17)
    - Inactive user is denied all access
    - Two composite indexes are created: `(user_id ASC, created_at DESC)` for history page; `(user_id ASC, read ASC, created_at DESC)` for unread badge / Mark-all queries
  </behavior>
  <action>
    **Edit `firestore.rules`** — insert the following block AFTER the existing `match /rfps/{rfpId}` block (line ~445 close brace) and BEFORE the closing `}` of `documents` (line ~446) and the closing `}` of `service` (line ~447). Verify by reading the surrounding lines first.

    Append exactly this block (use the exact predicates from D-17/D-18/D-19 — copied here verbatim from RESEARCH.md Pattern 6):

    ```
        // =============================================
        // notifications collection (Phase 83 — NOTIF-13)
        // =============================================
        // Per-user in-app notifications. Each user only touches their own.
        // Schema (D-05): user_id, type, message, link, source_collection, source_id,
        //                actor_id, read (bool), read_at, created_at.
        // Create rule (D-18): any active user can write to ANY user's feed (the actor
        //                     writes the recipient's notification — needed because we
        //                     have no Cloud Functions). actor_id pinned to request.auth.uid
        //                     prevents impersonation.
        // Update rule (D-19): only `read` and `read_at` are mutable post-create.
        match /notifications/{docId} {
          allow read: if isActiveUser() && resource.data.user_id == request.auth.uid;

          allow list: if isActiveUser() && resource.data.user_id == request.auth.uid;

          allow create: if isActiveUser()
            && request.resource.data.user_id is string
            && request.resource.data.type is string
            && request.resource.data.message is string
            && request.resource.data.link is string
            && request.resource.data.read == false
            && request.resource.data.actor_id == request.auth.uid;

          allow update: if isActiveUser()
            && resource.data.user_id == request.auth.uid
            && request.resource.data.user_id == resource.data.user_id
            && request.resource.data.type == resource.data.type
            && request.resource.data.message == resource.data.message
            && request.resource.data.link == resource.data.link
            && request.resource.data.created_at == resource.data.created_at
            && request.resource.data.actor_id == resource.data.actor_id;

          allow delete: if isActiveUser() && resource.data.user_id == request.auth.uid;
        }
    ```

    **Edit `firestore.indexes.json`** — append the following two index objects to the `indexes` array (NOT `fieldOverrides`). Add a comma after the previous last item if needed. Final structure of the file should still be valid JSON.

    Append exactly these two entries:

    ```json
        {
          "collectionGroup": "notifications",
          "queryScope": "COLLECTION",
          "fields": [
            { "fieldPath": "user_id", "order": "ASCENDING" },
            { "fieldPath": "created_at", "order": "DESCENDING" }
          ]
        },
        {
          "collectionGroup": "notifications",
          "queryScope": "COLLECTION",
          "fields": [
            { "fieldPath": "user_id", "order": "ASCENDING" },
            { "fieldPath": "read", "order": "ASCENDING" },
            { "fieldPath": "created_at", "order": "DESCENDING" }
          ]
        }
    ```

    **Critical:** Do NOT delete or reorder the existing four indexes (`users`, `pos` x2, `prs`). Append only.

    Per D-20 and Phase 11 lesson — these MUST deploy in the same commit as the first `addDoc` to `notifications`. Plan 05 owns the actual `firebase deploy` step; Plans 02-04 ship the writer code. This plan ONLY edits the source files; deployment is gated by Plan 05 checkpoint.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('firestore.rules','utf8'); const r=c.includes('match /notifications/{docId}') && c.includes(\"actor_id == request.auth.uid\") && c.includes('request.resource.data.created_at == resource.data.created_at'); const j=JSON.parse(require('fs').readFileSync('firestore.indexes.json','utf8')); const i=j.indexes.filter(x=>x.collectionGroup==='notifications'); console.log('rules-block-ok:', r, 'index-count:', i.length, 'has-2-field-index:', i.some(x=>x.fields.length===2), 'has-3-field-index:', i.some(x=>x.fields.length===3));"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "match /notifications/{docId}" firestore.rules` returns `1`
    - `grep -c "actor_id == request.auth.uid" firestore.rules` returns at least `1` (in the create rule)
    - `grep -c "request.resource.data.created_at == resource.data.created_at" firestore.rules` returns at least `1` (in the update rule, immutability guard)
    - `grep -c '"collectionGroup": "notifications"' firestore.indexes.json` returns `2`
    - `firestore.indexes.json` parses as valid JSON: `node -e "JSON.parse(require('fs').readFileSync('firestore.indexes.json','utf8'))"` exits 0
    - The original four indexes (`users`, two `pos`, `prs`) still present — `node -e "const j=JSON.parse(require('fs').readFileSync('firestore.indexes.json','utf8')); console.log(j.indexes.length)"` prints `6`
    - `firestore.rules` still parses as valid syntax (lines balanced — count of `{` == count of `}`)
  </acceptance_criteria>
  <done>
    Source files updated. Notifications rules block follows the file's stated template; indexes follow existing object shape; deployment deferred to Plan 05.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: (Conditional on Task 1 verdict = ADD) Add rule-unit-tests for notifications</name>
  <files>test/firestore.test.js</files>
  <read_first>
    - test/firestore.test.js (the full file — copy patterns from existing collection tests; preserve the existing imports, helpers, and `before`/`after` setup)
    - test/package.json (npm test command + dependencies)
    - .planning/phases/83-notification-system-foundation/83-CONTEXT.md D-17, D-18, D-19
    - .planning/phases/83-notification-system-foundation/83-RESEARCH.md Pattern 6 (the rule predicates being tested)
  </read_first>
  <behavior>
    Test cases (each must pass against the new rules block from Task 2):
    1. **Cross-user read denied:** User A creates a notification with user_id=A; User B attempts `getDoc(notifications/<id>)` → assertFails (`permission-denied`)
    2. **Same-user read allowed:** User A creates a notification with user_id=A; User A `getDoc(notifications/<id>)` → assertSucceeds
    3. **List with where-clause succeeds:** User A queries `where('user_id','==','A')` → assertSucceeds
    4. **List without where-clause denied:** User A queries `collection('notifications')` (no where) → assertFails
    5. **Create requires actor_id == auth.uid:** User A attempts to create with actor_id=B → assertFails
    6. **Create with actor_id == auth.uid succeeds:** User A creates with actor_id=A and required string fields → assertSucceeds
    7. **Update flips read=true succeeds for owner:** User A flips read on own notification → assertSucceeds
    8. **Update by non-owner denied:** User B attempts to flip read on User A's notification → assertFails
    9. **Update changing immutable field denied:** User A attempts update with type=different → assertFails
    10. **Inactive user denied all:** User with status='deactivated' attempts read/create/update → assertFails on all
  </behavior>
  <action>
    **PRE-CHECK:** If Task 1's verdict was DEFER, skip this task entirely. Document "Skipped per Task 1 verdict (DEFER)" in SUMMARY and proceed to plan SUMMARY.

    **IF Task 1 verdict was ADD:**
    1. Read `test/firestore.test.js` end to end. Identify the existing test pattern (likely `describe('collection', () => { it('test name', async () => { ... }) })` blocks using `assertSucceeds(...)` / `assertFails(...)` from `@firebase/rules-unit-testing`).
    2. Identify the existing setup helpers — most likely a `getAuthedDb(uid, role, status)` or similar that returns a Firestore reference scoped to a test identity. REUSE these helpers; do NOT create parallel ones.
    3. Append a new `describe('notifications collection', () => { ... })` block with the 10 cases from `<behavior>`. Use the exact rule predicates (`actor_id == request.auth.uid`, etc.) as the expected boundaries.
    4. For each test case, set up the prerequisite docs via the privileged admin context (the existing test bootstrap pattern), then attempt the operation under the user-under-test context.
    5. Do NOT modify any existing test cases.

    **Test execution is OUT OF SCOPE for this plan** — Plan 05 will run `npm test` (or equivalent) as the final verification gate before deploy.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('test/firestore.test.js','utf8'); console.log('has-notifications-describe:', c.includes(\"'notifications\") || c.includes('\"notifications'), 'has-cross-user-test:', /cross.*user|user.*B.*read.*A|deny.*other.*user/i.test(c), 'line-count:', c.split('\\n').length);"</automated>
  </verify>
  <acceptance_criteria>
    - IF DEFER: `test/firestore.test.js` is unchanged from main; SUMMARY documents "deferred per Task 1"
    - IF ADD: `grep -c "notifications" test/firestore.test.js` returns at least 10 (one per test case)
    - IF ADD: `grep -c "assertFails\|assertSucceeds" test/firestore.test.js` increased by at least 10 over the pre-task baseline (verify by `git diff --stat test/firestore.test.js`)
    - IF ADD: existing tests untouched: `git diff test/firestore.test.js` shows only ADDITIONS (no `-` lines that aren't `\ No newline at end of file` markers)
    - IF ADD: file still parses as valid JS: `node --check test/firestore.test.js` exits 0
  </acceptance_criteria>
  <done>
    Either tests added (case ADD) with 10 new assertions covering D-17/D-18/D-19, or task explicitly skipped (case DEFER) with SUMMARY note.
  </done>
</task>

</tasks>

<verification>
- `firestore.rules` contains the notifications block with D-17/D-18/D-19 predicates verbatim
- `firestore.indexes.json` is valid JSON and contains exactly two new notifications indexes
- The investigation outcome (ADD or DEFER) is recorded in this plan's SUMMARY
- If ADD: `test/firestore.test.js` has new test cases for the rules block
- No syntax breakage in any modified file
- No deployment yet — Plan 05 owns deploy
</verification>

<success_criteria>
- NOTIF-13 storage layer: rules + indexes locked into source. Plans 02-04 can reference the schema without ambiguity.
- Pitfall 5 (composite-index-missing) is pre-empted: indexes are in source before any code queries the collection.
- Pitfall 7 (list rule without where clause) is enforceable: list rule pattern matches existing `prs` precedent.
- Test infra decision documented; carry-over recorded in STATE.md if DEFER.
</success_criteria>

<output>
After completion, create `.planning/phases/83-notification-system-foundation/83-01-SUMMARY.md` containing:
- Tasks completed
- Test infra verdict (ADD or DEFER) with evidence
- Exact line ranges where the rules block was inserted in firestore.rules
- Exact JSON path of the new index entries
- Deferred items (e.g., DEFER verdict → STATE.md update)
- Cross-references for downstream plans (Plan 02 listener can rely on these indexes; Plan 05 will deploy)
</output>
