---
phase: 83-notification-system-foundation
plan: null
task: null
total_tasks: 15
total_plans: 5
total_waves: 3
status: planning_complete
last_updated: 2026-04-30T00:04:54.060Z
---

<current_state>
Phase 83 (Notification System Foundation) planning is fully complete and verified. No execution started yet — all 5 plans, 15 tasks, 3 waves are queued for `/gsd:execute-phase 83`. Goal: ship in-app notification plumbing (bell + badge in top nav, dropdown of last 10, history page at `#/notifications`, `notifications` Firestore collection + Security Rules + indexes, `app/notifications.js` shared module with helpers + locked enum) — without wiring any production triggers (Phase 84 wires triggers).
</current_state>

<completed_work>

- CONTEXT.md gathered earlier (D-01..D-20 locked) — schema, UX, security rules, helpers all locked
- RESEARCH.md generated (HIGH confidence, 9 pitfalls documented; all CONTEXT.md code references verified accurate)
- VALIDATION.md materialized from RESEARCH §Validation Architecture — covers all 7 NOTIF requirements
- 5 PLAN.md files created (01-05) covering NOTIF-01..06, NOTIF-13 across 3 waves
- 3 verify-*.cjs helper scripts created (Windows-bash-safe replacements for inline `node -e` checks)
- gsd-plan-checker iteration 1 found 3 BLOCKERs + 6 MAJORs + 4 MINORs
- Planner revision applied surgical fixes
- gsd-plan-checker iteration 2: VERIFICATION PASSED, no new regressions
- ROADMAP.md updated: Phase 83 plan list TBD → 5 plans; progress row 0/TBD → 0/5

</completed_work>

<remaining_work>

- **Wave 1 (parallel):**
  - Plan 01: `firestore.rules` `notifications` block + `firestore.indexes.json` composite indexes + `test/firestore.test.js` currency audit (3 tasks)
  - Plan 02: `app/firebase.js` startAfter re-export + `app/notifications.js` shared module (helpers, NOTIFICATION_TYPES enum, dev test writer, dropdown rendering, mark-read flows) (2 tasks)
- **Wave 2 (parallel, after Wave 1):**
  - Plan 03: bell markup in `index.html` (sibling of `.nav-links`) + CSS classes in `styles/components.css` + `app/auth.js` lifecycle hooks (3 destroy hook points per D-13) + **mandatory** `import './app/notifications.js'` in index.html bootstrap module (3 tasks)
  - Plan 04: `app/views/notifications.js` history page (cursor-paginated 20/page) + `app/router.js` route registration (`/notifications` in routes AND routePermissionMap) (2 tasks)
- **Wave 3 (after Wave 2):**
  - Plan 05: dev deploy of rules+indexes → manual UAT (Task 2, human checkpoint, ~10 min) → production deploy (Task 4, human checkpoint) → STATE/ROADMAP updates (5 tasks total: 3 checkpoints + 2 auto)

</remaining_work>

<decisions_made>

- **Run research before planning** despite `research_enabled: false` in config — user explicitly chose "Research first" at the gate; surfaced 3 critical pitfalls
- **Skip UI-SPEC generation** — CONTEXT.md already locks bell placement (D-01/D-02), dropdown behavior (D-03/D-04), route (D-09), mobile rules. Bell+dropdown closely mirrors existing Admin `.nav-dropdown` pattern
- **Static-import `app/notifications.js` in index.html bootstrap** — `window.*` registrations happen on module load; without explicit import every bell `onclick` would throw `ReferenceError` and `app/auth.js`'s `if (window.initNotifications)` guard would silently no-op
- **Bell wrapper as sibling of `.nav-links`, NOT child** — at ≤768px `.nav-links { display: none }`, so bell must live at `.top-nav-content` flex level to remain visible per D-02
- **`destroyNotifications()` hooked at 3 sites in `app/auth.js`** (NOT in any view destroy) — bell persists across all views per D-13; cleanup ties to logout/auth-clear, not view navigation
- **Verify scripts extracted to `.cjs` files** — Windows-bash-fragile multi-layer shell escaping replaced; project's `package.json` has `type: module` so `.cjs` extension is required for `require()` calls
- **Mobile flex tweak in `styles/components.css` only** — `.top-nav-content` rule lives there (4 occurrences); splitting across components.css + views.css would create duplicate @media blocks
- **`NOTIFICATION_TYPES` enum ships 9 keys including `PROPOSAL_SUBMITTED` + `PROPOSAL_DECIDED`** — placeholders so Phase 87 doesn't churn the constants module (D-06)

</decisions_made>

<blockers>
None. Planning is complete and verified. Execution is unblocked.
</blockers>

<context>
This was a /gsd:plan-phase 83 session — research → plan → verify → revise → re-verify → done. The phase context (CONTEXT.md, DISCUSSION-LOG.md) had been gathered before this session — they were already on disk when planning kicked off. The session only produced research, validation, plans, verify scripts, and the ROADMAP.md edit.

The biggest risk catch was BLOCKER-3 from the first verification pass: the original Plan 03 made `app/notifications.js` loading conditional ("If the bootstrap loads modules... otherwise..."), but inspection of `index.html:192-219` showed the existing bootstrap is a single inline `<script type="module">` that does NOT import notifications.js. Without the import, every `window.*` registration in app/notifications.js never runs, and the entire bell UI would have been dead-on-arrival. The revision pass made this a numbered MANDATORY edit in Plan 03 Task 3.

`commit_docs: false` in config means none of the 17 new/modified files are auto-committed. The pause-work commit will package them as a single WIP commit on the v3.3 branch.

Everything is ready to execute. The next session should run `/gsd:execute-phase 83` after `/clear`. Wave 1 plans have no dependencies and can run in parallel; Plan 05 will require user attention at 2 human-verify checkpoints (UAT and production deploy).
</context>

<next_action>
1. `/clear` to free context window
2. `/gsd:execute-phase 83` — launches Wave 1 (Plans 01 + 02 in parallel)
3. Review Wave 1 outputs, then continue through Wave 2, then Plan 05 (with manual UAT and production deploy checkpoints)

Alternative entry points if needed:
- `cat .planning/phases/83-notification-system-foundation/83-*-PLAN.md` — review plans before execution
- `/gsd:review --phase 83 --all` — peer-review plans with external AIs
- `/gsd:plan-phase 83 --reviews` — replan incorporating review feedback
</next_action>
