---
phase: 86.10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/project-plan.js
autonomous: true
requirements:
  - REQ-86.10-1
must_haves:
  truths:
    - "When Enter is pressed in the new-row input while the row above is an indented child, the newly committed task inherits the same parent_task_id as the row above"
    - "When the row above is a root-level task (no parent), the new row is also created at root level"
    - "No other create-task path is affected by this change"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "handleNewRowCommit() with smart indent inheritance"
      contains: "inheritedParentId = rowAbove?.parent_task_id ?? null"
  key_links:
    - from: "handleNewRowCommit()"
      to: "flattenTreeDepthFirst(tasks)"
      via: "rowAbove lookup"
      pattern: "flattenTreeDepthFirst"
    - from: "inheritedParentId"
      to: "Firestore docData.parent_task_id"
      via: "setDoc() or addDoc() call"
      pattern: "parent_task_id"
---

<objective>
Fix handleNewRowCommit() so a new task inherits the indent depth of the row directly above it, rather than always defaulting to root level.

Purpose: When a user is entering a sequence of child tasks (pressing Enter repeatedly), every new row should continue at the same indent level — not snap back to root. This is the standard spreadsheet/outline behavior.

Output: One-line change inside handleNewRowCommit() in app/views/project-plan.js. No schema change, no new functions.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\PROJECT.md
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\.planning\STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inherit parent_task_id from row above in handleNewRowCommit</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    Read app/views/project-plan.js lines 1064–1120 (handleNewRowCommit function).
    Key identifiers:
    - Line ~1092: `const inheritedParentId = null;` — this is the target line
    - `flattenTreeDepthFirst(tasks)` — returns visual task order; used just above line 1092 to obtain `rowAbove`
    - `rowAbove` — the last task in flattenTreeDepthFirst output, representing the visually preceding row
    - `parent_task_id` — Firestore field on task documents; null means root level
    - `docData` object (line ~1098): contains `parent_task_id: inheritedParentId`
  </read_first>
  <action>
    In handleNewRowCommit() at the line `const inheritedParentId = null;` (approximately line 1092), replace the right-hand side so it reads:

    `const inheritedParentId = rowAbove?.parent_task_id ?? null;`

    This single expression reads the parent_task_id of the visually preceding row (already computed as `rowAbove` via flattenTreeDepthFirst). If rowAbove is undefined (empty task list) or has no parent_task_id, it falls back to null (root level), preserving all existing behavior for the first row.

    Do not change any other line in this function. The `rowAbove` variable must already be in scope at the point of this assignment — confirm it is computed from flattenTreeDepthFirst above line 1092; if not, read the surrounding lines to locate where rowAbove is assigned and place the new line immediately after.

    Per REQ-86.10-1.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const src = fs.readFileSync('app/views/project-plan.js','utf8'); const ok = /inheritedParentId\s*=\s*rowAbove\?\.parent_task_id\s*\?\?\s*null/.test(src); process.exit(ok ? 0 : 1);"</automated>
  </verify>
  <done>handleNewRowCommit() contains `inheritedParentId = rowAbove?.parent_task_id ?? null` and the old `= null` assignment is gone. grep confirms the pattern.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Single-line fix: new tasks created via Enter now inherit the indent depth (parent_task_id) of the row directly above them in the grid.</what-built>
  <how-to-verify>
    1. Open http://localhost:8000 → navigate to any project's plan view.
    2. Create a root task (e.g. "Phase A"). Press Enter.
    3. Indent the resulting new row (right-click → Indent, or use the Indent button) so it becomes a child of "Phase A".
    4. Press Enter again to commit that child task and create another new row.
    5. Verify: the second new row is created at the same indent level as the first child — NOT at root.
    6. Confirm in Firestore (DevTools → Network or Firebase Console) that the newly created task has `parent_task_id` matching "Phase A"'s task ID.
    7. Also verify: creating a row when the row above is a root task → new row is root (parent_task_id = null).
  </how-to-verify>
  <resume-signal>Type "approved" if both behaviors are correct, or describe the defect.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client JS → Firestore | parent_task_id is derived from in-memory task data already fetched via onSnapshot; no new untrusted input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.10-01-01 | Tampering | handleNewRowCommit — parent_task_id inheritance | accept | rowAbove is sourced from the authenticated onSnapshot task list; an attacker would need write access to Firestore to forge a parent_task_id, which is already governed by existing security rules |
| T-86.10-01-02 | Elevation of Privilege | Inheriting parent of an out-of-project task | accept | flattenTreeDepthFirst only contains tasks for the current project (filtered at load time); cross-project task ID injection is prevented by existing Firestore query scope |
</threat_model>

<verification>
- [ ] `grep -n "rowAbove?.parent_task_id" app/views/project-plan.js` returns one match inside handleNewRowCommit
- [ ] `grep -n "inheritedParentId = null" app/views/project-plan.js` returns zero matches (old assignment removed)
- [ ] Browser UAT: new row after indented child inherits parent_task_id (approved at checkpoint)
- [ ] Browser UAT: new row after root task is root (parent_task_id = null)
</verification>

<success_criteria>
- handleNewRowCommit uses `rowAbove?.parent_task_id ?? null` for inheritedParentId
- No regression in new-row creation for root-level rows
- Checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/86.10-left-pane-polish-smart-indent-auto-group-shift-click-multi-s/86.10-01-SUMMARY.md` following the summary template.
</output>
