---
phase: 86.10
plan: 02
type: execute
wave: 2
depends_on:
  - 86.10-01
files_modified:
  - app/views/project-plan.js
  - styles/views.css
autonomous: false
requirements:
  - REQ-86.10-2
must_haves:
  truths:
    - "Shift+clicking a row while another row is already selected highlights all rows between them (contiguous range) using a distinct visual state"
    - "A plain click on any row clears the multi-selection and selects only that row"
    - "Dragging a row that is part of a multi-selection moves all selected rows together as a contiguous block to the drop target position, preserving their relative order"
    - "Dragging a row that is NOT part of the multi-selection behaves as the existing single-row drag (no regression)"
    - "Multi-select state is cleared on destroy()"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "Multi-select state vars, shift+click logic, group drag/drop"
      contains: "_selectedRowIds"
    - path: "styles/views.css"
      provides: ".tg-row.tg-multi-selected highlight rule"
      contains: "tg-multi-selected"
  key_links:
    - from: "_gridRowClickHandler"
      to: "_selectedRowIds Set"
      via: "shift key detection + flattenTreeDepthFirst range slice"
      pattern: "_selectedRowIds"
    - from: "_gridDragStartHandler / _gridDropHandler"
      to: "handleGroupDrop()"
      via: "_selectedRowIds.size > 1 branch"
      pattern: "handleGroupDrop"
    - from: "handleGroupDrop()"
      to: "commitRowOrderReorder()"
      via: "reordered task ID array"
      pattern: "commitRowOrderReorder"
---

<objective>
Add shift+click multi-row selection to the task grid and wire selected rows to move together during drag-and-drop.

Purpose: Lets users select a contiguous group of tasks and reorder them as a unit — critical for restructuring sections of a plan without drag-dropping each row individually.

Output: New module-scope state vars (`_selectedRowIds`, `_lastClickedRowId`), modified click handler, new `handleGroupDrop()` function, updated CSS highlight class, and cleanup in destroy(). No Firestore schema change — only `row_order` fields are written (same as single-row drag).
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\PROJECT.md
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\.planning\STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add multi-select state vars, shift+click handler, and CSS highlight</name>
  <files>app/views/project-plan.js, styles/views.css</files>
  <read_first>
    Read app/views/project-plan.js lines 12–100 (module-scope state vars) to find the insertion point for new vars.
    Read lines 956–1010 (_gridRowClickHandler inside bindGridEvents) — this is where shift+click logic is added.
    Read lines 1958–1990 (selectRow and clearSelection functions) — both need updates to clear multi-select.
    Read lines 350–465 (destroy function) — add cleanup for new vars.
    Read styles/views.css — search for `.tg-selected` rule to find insertion point for `.tg-multi-selected`.

    Key identifiers to know before editing:
    - `_selectedTaskId` — existing single-select var (line ~84)
    - `selectRow(taskId)` — existing function, clears `.tg-selected` and sets `_selectedTaskId` (line ~1958)
    - `clearSelection()` — existing function (line ~1981)
    - `flattenTreeDepthFirst(tasks)` — returns task objects in visual top-to-bottom order; task objects have `.task_id` property
    - `_gridRowClickHandler` — delegated click handler var assigned inside bindGridEvents
    - `.tg-row` — class on each grid row; `data-task-id` attribute holds the task ID
    - `gridContainer` — the `.task-grid-rail` element that receives delegated events
  </read_first>
  <action>
    Step 1 — Add module-scope state vars near the existing single-select vars (around line 84):
    - `let _selectedRowIds = new Set();` — holds task IDs of all currently multi-selected rows
    - `let _lastClickedRowId = null;` — anchor for shift+click range calculation

    Step 2 — Modify selectRow(taskId): after the existing logic that clears `.tg-selected` and sets `_selectedTaskId`, also clear `_selectedRowIds` (call `_selectedRowIds.clear()`) and set `_lastClickedRowId = taskId`. This ensures a plain click always collapses multi-select.

    Step 3 — Modify clearSelection(): after the existing logic, also call `_selectedRowIds.clear()` and set `_lastClickedRowId = null`.

    Step 4 — Modify _gridRowClickHandler (inside bindGridEvents, currently starts at the delegated click event section ~line 956):
    - Before the existing `selectRow(id)` call, detect `event.shiftKey`.
    - If shiftKey is true AND `_lastClickedRowId` is set AND `_lastClickedRowId !== id`:
      a. Call `flattenTreeDepthFirst(tasks)` to get the ordered task list. Extract `.task_id` from each item.
      b. Find the indices of `_lastClickedRowId` and `id` in that list. If either is not found, fall through to normal selectRow.
      c. Slice the range between the two indices (inclusive, direction-agnostic — use Math.min/Math.max).
      d. Call `selectRow(null)` to clear the single-select highlight (sets `_selectedTaskId = null`, removes `.tg-selected`).
      e. Populate `_selectedRowIds` with all task IDs in the range (do NOT update `_lastClickedRowId` — the anchor stays fixed during shift+click extension).
      f. For each task ID in `_selectedRowIds`, add class `tg-multi-selected` to the `.tg-row[data-task-id="..."]` element.
    - If shiftKey is false (normal click): first remove `tg-multi-selected` from all rows that have it, clear `_selectedRowIds`, then call `selectRow(id)` as before (which also sets `_lastClickedRowId = id`).
    - Note: `selectRow(null)` in step d should NOT clobber `_lastClickedRowId` — ensure clearSelection (called internally by selectRow when taskId is null) does not set it; the anchor must persist for subsequent shift+clicks. Alternatively, save `_lastClickedRowId` before calling selectRow(null) and restore it after.

    Step 5 — Add destroy() cleanup: inside the destroy() function, after the existing `_selectedTaskId = null;` line, add:
    - `_selectedRowIds.clear();`
    - `_lastClickedRowId = null;`

    Step 6 — CSS: in styles/views.css, find the `.tg-selected` rule (used for single-select highlight). Immediately after it, add a `.tg-row.tg-multi-selected` rule with the same background/border highlight style as `.tg-selected` (use the same color or a slightly lighter tint — match the existing `.tg-selected` treatment so both single and multi selection look cohesive).

    Per REQ-86.10-2.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const src = fs.readFileSync('app/views/project-plan.js','utf8'); const checks = [/_selectedRowIds\s*=\s*new Set\(\)/.test(src), /_lastClickedRowId\s*=\s*null/.test(src), /tg-multi-selected/.test(src), /_selectedRowIds\.clear\(\)/.test(src)]; process.exit(checks.every(Boolean) ? 0 : 1);"</automated>
    <automated>node -e "const fs = require('fs'); const css = fs.readFileSync('styles/views.css','utf8'); process.exit(/tg-multi-selected/.test(css) ? 0 : 1);"</automated>
  </verify>
  <done>_selectedRowIds Set and _lastClickedRowId are declared at module scope; selectRow() and clearSelection() clear them; _gridRowClickHandler handles shift+click with range selection; destroy() resets both vars; .tg-row.tg-multi-selected CSS rule exists in views.css.</done>
</task>

<task type="auto">
  <name>Task 2: Group drag — handleGroupDrop and modified drag handlers</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    Read app/views/project-plan.js lines 880–960 (_gridDragStartHandler and _gridDropHandler inside bindGridEvents).
    Read lines 1386–1430 (handleRowDrop and commitRowOrderReorder) to understand the existing single-row move pattern.

    Key identifiers:
    - `_draggedTaskId` — module-scope var holding the task ID being dragged (set in dragstart)
    - `handleRowDrop(draggedTaskId, targetTaskId)` — existing single-row move function (line ~1386)
    - `commitRowOrderReorder(reorderedTaskIds)` — writes new row_order values via writeBatch (line ~1423); accepts a full ordered array of all task IDs and assigns sequential row_order integers
    - `flattenTreeDepthFirst(tasks)` — returns current visual task order
    - `.task_id` — property on task objects returned by flattenTreeDepthFirst
    - `_gridDragStartHandler` — set inside bindGridEvents; only allows drag from `.tg-rn` cell
    - `_gridDropHandler` — set inside bindGridEvents; currently calls handleRowDrop
  </read_first>
  <action>
    Step 1 — Declare a new function `handleGroupDrop(selectedIds, targetTaskId)` near the existing handleRowDrop function (around line 1386):
    - Get the current visual order: `const ordered = flattenTreeDepthFirst(tasks).map(t => t.task_id);`
    - Convert selectedIds to a Set for fast lookup: `const selSet = new Set(selectedIds);`
    - Build `nonSelected` array: all task IDs in `ordered` that are NOT in `selSet`.
    - Build `selected` array: all task IDs in `ordered` that ARE in `selSet` (this preserves their relative order).
    - Find the insertion index in `nonSelected`: the position AFTER `targetTaskId`. If `targetTaskId` is itself in `selSet`, find the nearest non-selected task below it as the insertion target (walk down `ordered` from targetTaskId's index until a non-selected task is found or end of list). If no non-selected task is found below, insert at end.
    - Insert the `selected` block into `nonSelected` at the computed position to form `reorderedAll`.
    - Call `commitRowOrderReorder(reorderedAll)`.

    Step 2 — Modify _gridDropHandler (inside bindGridEvents, the drop event handler):
    - After computing `targetTaskId` from the drop target row (existing logic), check: `if (_selectedRowIds.size > 1 && _selectedRowIds.has(_draggedTaskId))`.
    - If true: call `handleGroupDrop([..._selectedRowIds], targetTaskId)` instead of `handleRowDrop`.
    - If false: call the existing `handleRowDrop(_draggedTaskId, targetTaskId)` (no regression for single-row drag).

    Step 3 — Modify _gridDragStartHandler: no logic change needed. The dragstart handler already sets `_draggedTaskId`. The drop handler will read `_selectedRowIds` at drop time to determine group vs single mode.

    Expose handleGroupDrop on window is NOT required — it is an internal function called only by _gridDropHandler. Do not attach it to window.

    Per REQ-86.10-2.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const src = fs.readFileSync('app/views/project-plan.js','utf8'); const checks = [/function handleGroupDrop/.test(src) || /handleGroupDrop\s*=\s*(?:async\s+)?function/.test(src) || /(?:async\s+)?function handleGroupDrop/.test(src), /_selectedRowIds\.has\(_draggedTaskId\)/.test(src)]; process.exit(checks.every(Boolean) ? 0 : 1);"</automated>
  </verify>
  <done>handleGroupDrop() function exists and is called from _gridDropHandler when _selectedRowIds.size > 1 and the dragged row is part of the selection. Single-row drag path is unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Multi-row selection via shift+click with group drag.
    - Shift+click selects contiguous rows highlighted with .tg-multi-selected style
    - Plain click collapses multi-select back to single row
    - Dragging a row that is part of a multi-selection moves all selected rows as a block
    - Dragging a row outside the selection moves only that row (no regression)
  </what-built>
  <how-to-verify>
    1. Open http://localhost:8000 → navigate to any project with 5+ tasks.
    2. Click row 2. Shift+click row 5. Verify rows 2–5 are highlighted.
    3. Click row 1 (no shift). Verify only row 1 is highlighted; rows 2–5 lose highlight.
    4. Click row 3. Shift+click row 6. Drag row 4 (drag handle / row number) to below row 8. Verify rows 3–6 all moved together as a block, maintaining their internal order.
    5. Drag a row that is NOT selected (no shift+click done first). Verify only that one row moves (single-row drag still works).
    6. Reload the page and verify the reordered positions persisted (row_order was written to Firestore).
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors are correct, or describe any defects.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client JS → Firestore | row_order values written via commitRowOrderReorder; source data comes from authenticated onSnapshot |
| DOM events → JS state | shift+click reads event.shiftKey and row data-task-id attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.10-02-01 | Tampering | handleGroupDrop — reordering task IDs | accept | All task IDs in _selectedRowIds originate from the authenticated Firestore snapshot; no user-supplied string bypasses the existing writeBatch security rules |
| T-86.10-02-02 | Denial of Service | shift+click selecting all rows then drag | accept | flattenTreeDepthFirst is O(n) over task count; project task lists are small (<500 rows); no unbounded loop risk |
| T-86.10-02-03 | Information Disclosure | _selectedRowIds leaking cross-project data | accept | _selectedRowIds holds only task IDs for the current project's onSnapshot; cleared in destroy() when navigating away |
</threat_model>

<verification>
- [ ] `grep -n "_selectedRowIds" app/views/project-plan.js` returns matches at: declaration, selectRow, clearSelection, _gridRowClickHandler, _gridDropHandler, destroy
- [ ] `grep -n "tg-multi-selected" app/views/project-plan.js` returns at least one match (class added/removed in click handler)
- [ ] `grep -n "tg-multi-selected" styles/views.css` returns one match (the CSS rule)
- [ ] `grep -n "handleGroupDrop" app/views/project-plan.js` returns at least two matches (definition + call site)
- [ ] Browser UAT: shift+click highlights contiguous rows (approved at checkpoint)
- [ ] Browser UAT: group drag moves all selected rows (approved at checkpoint)
- [ ] Browser UAT: single-row drag is unaffected (approved at checkpoint)
</verification>

<success_criteria>
- _selectedRowIds Set declared at module scope, cleared on plain click, destroy, and clearSelection
- Shift+click populates _selectedRowIds and applies .tg-multi-selected CSS class to each row in range
- handleGroupDrop() reorders selected rows as a block via commitRowOrderReorder
- _gridDropHandler routes to handleGroupDrop when selection size > 1 and dragged row is in selection
- Checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/86.10-left-pane-polish-smart-indent-auto-group-shift-click-multi-s/86.10-02-SUMMARY.md` following the summary template.
</output>
