---
phase: 60.2-match-my-requests-table-size-and-layout-to-mrf-records-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/mrf-form.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "My Requests table fills the same horizontal width as the MRF Records tab table — no narrow 1200px box visible"
    - "Column widths, row heights, font sizes, and table density in My Requests visually match MRF Records"
  artifacts:
    - path: "app/views/mrf-form.js"
      provides: "renderMyRequestsView() with 1600px max-width container matching procurement Records tab"
      contains: "max-width: 1600px"
  key_links:
    - from: "renderMyRequestsView() container div"
      to: "procurement.js records-section content wrapper"
      via: "max-width: 1600px"
      pattern: "max-width: 1600px"
---

<objective>
Match the My Requests table outer container width to the MRF Records tab container width so the two views look identical in size and layout.

Purpose: Users switching between My Requests and MRF Records see the same table density and column widths. Currently My Requests is capped at max-width: 1200px while MRF Records uses 1600px — producing a noticeably narrower, cramped table in My Requests.
Output: app/views/mrf-form.js updated so renderMyRequestsView() wraps content in a 1600px container instead of 1200px.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key layout facts extracted from codebase: -->

From app/views/mrf-form.js renderMyRequestsView() line 53:
```javascript
<div class="container" style="max-width: 1200px; margin: 0 auto; padding: 2rem;">
```
This is the outer content wrapper for My Requests — currently 1200px max-width.

From app/views/procurement.js line 173 (Records tab content wrapper):
```javascript
<div style="max-width: 1600px; margin: 0 auto; padding: 0 2rem;">
```
This is the outer content wrapper for MRF Records — 1600px max-width.

The table column structure and row styling in mrf-records.js render() (the live controller) are already identical to procurement.js renderPRPORecords(). The ONLY structural layout difference that produces the visual width mismatch is the outer container max-width: 1200px vs 1600px.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Widen My Requests container from 1200px to 1600px</name>
  <files>app/views/mrf-form.js</files>
  <action>
In renderMyRequestsView() (approximately line 53), change the outer container div from:
  `<div class="container" style="max-width: 1200px; margin: 0 auto; padding: 2rem;">`
to:
  `<div class="container" style="max-width: 1600px; margin: 0 auto; padding: 2rem;">`

This is a single string replacement. No other changes needed — the table structure, row styling, column headers, and font sizes in the live-rendered controller output (mrf-records.js createMRFRecordsController render()) are already identical to the MRF Records tab in procurement.js.
  </action>
  <verify>grep -n "max-width" C:/Users/Admin/Roaming/pr-po/app/views/mrf-form.js | grep -E "1[0-9][0-9][0-9]px" — should show only 1600px (or none), not 1200px</verify>
  <done>renderMyRequestsView() outer container uses max-width: 1600px; My Requests table visually matches MRF Records tab width when loaded in the browser at the same viewport</done>
</task>

</tasks>

<verification>
Open the app at http://localhost:8000, navigate to Material Request > My Requests tab, and compare the table width to Procurement > Records tab. Both tables should span the same horizontal width with identical column proportions and row density.
</verification>

<success_criteria>
My Requests table occupies the same horizontal space as the MRF Records tab — no narrow 1200px constraint visible, columns have the same widths, table fills the available card area at 1600px max-width.
</success_criteria>

<output>
After completion, create `.planning/phases/60.2-match-my-requests-table-size-and-layout-to-mrf-records-tab/60.2-01-SUMMARY.md`
</output>
