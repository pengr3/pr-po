---
phase: 44-responsive-layouts
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - app/views/projects.js
  - app/views/services.js
  - app/views/mrf-records.js
autonomous: true
requirements:
  - RES-02
  - RES-05

must_haves:
  truths:
    - "The Projects list table scrolls horizontally at narrow viewports instead of clipping"
    - "The Services list table scrolls horizontally at narrow viewports instead of clipping"
    - "The MRF Records table (in both My Requests and MRF Records views) scrolls horizontally at narrow viewports"
  artifacts:
    - path: "app/views/projects.js"
      provides: "Projects list table wrapped in .table-scroll-container"
      contains: "table-scroll-container"
    - path: "app/views/services.js"
      provides: "Services list table wrapped in .table-scroll-container"
      contains: "table-scroll-container"
    - path: "app/views/mrf-records.js"
      provides: "MRF records main table wrapped in .table-scroll-container"
      contains: "table-scroll-container"
  key_links:
    - from: "app/views/projects.js"
      to: "styles/components.css .table-scroll-container"
      via: "HTML class applied in render()"
      pattern: "table-scroll-container"
    - from: "app/views/mrf-records.js"
      to: "styles/components.css .table-scroll-container"
      via: "HTML class applied in renderTable()"
      pattern: "table-scroll-container"
---

<objective>
Apply the `.table-scroll-container` div wrapper (created in Plan 01) to all bare `<table>` elements in projects.js, services.js, and mrf-records.js.

Purpose: These views have list tables that overflow on mobile. Wrapping them in the scroll container fixes horizontal overflow without any visual change on desktop.
Output: Three JS view files updated — each list table wrapped in `.table-scroll-container`.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-responsive-layouts/44-CONTEXT.md
@.planning/phases/44-responsive-layouts/44-01-SUMMARY.md

<interfaces>
<!-- The class to apply (from Plan 01) -->
```css
/* Wrap any <table> in this div */
<div class="table-scroll-container">
    <table>...</table>
</div>
```

<!-- Current table rendering patterns in each file -->

projects.js line ~222: bare `<table>` (no wrapper) — this is the Projects list table
services.js line ~251: bare `<table>` (no wrapper) — this is the Services list table
mrf-records.js line ~106: bare `<table>` (no wrapper) — this is the MRF Records list table

mrf-records.js also has inline tables at lines ~656, ~750, ~1232 (these are sub-row detail tables
inside modals or expanded rows — wrap these too if they are plain <table> without overflow-x wrapper).
The inline modal tables at ~656 and ~750 already have `style="overflow-x: auto"` on their parent div
— leave those alone. Only wrap the main list table (~106).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap Projects and Services list tables</name>
  <files>app/views/projects.js, app/views/services.js</files>
  <action>
    In each file, find the main list `<table>` element in the `render()` function's returned HTML string and wrap it in a `<div class="table-scroll-container">` wrapper.

    **projects.js pattern (around line 222):**
    Before:
    ```javascript
    <table>
        <thead>...</thead>
        <tbody id="projectsTableBody">...</tbody>
    </table>
    ```
    After:
    ```javascript
    <div class="table-scroll-container">
        <table>
            <thead>...</thead>
            <tbody id="projectsTableBody">...</tbody>
        </table>
    </div>
    ```

    **services.js pattern (around line 251):**
    Same treatment — find the `<table>` in the services list render and wrap it identically.

    Read the full file first to confirm the exact surrounding context before editing. Do not wrap modal tables or sub-row tables — only the primary list table in the card body.

    No JS logic changes — only template HTML string changes.
  </action>
  <verify>
    Search both files: `grep -n "table-scroll-container" app/views/projects.js app/views/services.js`
    Each file must return at least 1 match. Confirm the table tag is immediately inside the wrapper div (no intermediate divs).
  </verify>
  <done>projects.js and services.js each have the main list table wrapped in .table-scroll-container.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap MRF Records main list table</name>
  <files>app/views/mrf-records.js</files>
  <action>
    In mrf-records.js, find the main MRF Records list `<table>` (around line 106 — this is the primary table rendered in the card body, NOT the inline sub-row detail tables at ~656, ~750, ~1232).

    Wrap only the primary list table:
    Before:
    ```javascript
    <table>
        <thead>...</thead>
        <tbody id="mrfRecordsTableBody">...</tbody>
    </table>
    ```
    After:
    ```javascript
    <div class="table-scroll-container">
        <table>
            <thead>...</thead>
            <tbody id="mrfRecordsTableBody">...</tbody>
        </table>
    </div>
    ```

    The inline sub-row tables inside modals or expanded rows (~656, ~750) already have `style="overflow-x: auto"` on their parent divs — do not modify those.

    Also check line ~1232 for the `<table style="...">` — if it does NOT already have an overflow-x container parent, wrap it too. Read the surrounding context to confirm before deciding.

    No JS logic changes — template string only.
  </action>
  <verify>
    `grep -n "table-scroll-container" app/views/mrf-records.js`
    Must return at least 1 match (the primary list table). Confirm it wraps the correct table.
  </verify>
  <done>mrf-records.js main list table is wrapped in .table-scroll-container. Sub-row/modal inline tables unchanged.</done>
</task>

</tasks>

<verification>
Load the app in browser at 375px viewport width:
- Navigate to Projects tab: table should scroll horizontally rather than overflow
- Navigate to Services tab: table should scroll horizontally
- Navigate to Material Request > MRF Records sub-tab: table should scroll horizontally
In all cases, a right-edge gradient fade should be visible when the table is wider than the viewport.
Desktop (>768px): no visual change — tables render normally.
</verification>

<success_criteria>
- `grep "table-scroll-container" app/views/projects.js` returns a match
- `grep "table-scroll-container" app/views/services.js` returns a match
- `grep "table-scroll-container" app/views/mrf-records.js` returns a match
- No existing JS logic or IDs changed — only HTML template string wrapping
</success_criteria>

<output>
After completion, create `.planning/phases/44-responsive-layouts/44-02-SUMMARY.md`
</output>
