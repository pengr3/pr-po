---
context: phase
phase: 86.7-gantt-phantom-drag
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-05-08T20:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: GANTT-OVERLAY-PANE-PARITY — When changing `#ganttHeaderOverlay` height in project-plan.js, you MUST also update `.tg-header` height in views.css to the same value, or the left and right panels will be misaligned.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| One-sided height change | Changing overlay height in project-plan.js without updating .tg-header in views.css causes left/right panel misalignment — happened in commit 8cb60f3 | blocking | Always grep for tg-header height in views.css and update it to match when changing overlay height |
| Wrong element investigation | Multiple debug sessions targeted the parent bar dark color and initGanttDragLink SVG elements — the actual bug was a CSS coverage gap in #ganttHeaderOverlay | advisory | Read renderCustomGanttHeader() first before assuming SVG element bugs |

<current_state>
Fixing phantom space/gap between Gantt date header and first bar row.

Root cause is CONFIRMED and CORRECT (commit 8cb60f3 has right diagnosis):
- `#ganttHeaderOverlay` div covers the SVG date headers with a white background
- Overlay was 85px tall (gantt.config.header_height)  
- Frappe positions first bar at y = header_height + padding/2 = 85 + 9 = 94px
- The 9px strip at y=85-94 showed SVG grid background through = the "phantom space"

The fix in 8cb60f3 is CORRECT in isolation but INCOMPLETE:
- It changed overlay height to 94px in project-plan.js (renderCustomGanttHeader, line 1635)
- But did NOT update `.tg-header` CSS height in views.css
- Result: left pane header (85px) is shorter than Gantt header overlay (94px) → visible misalignment

There is also an uncommitted change in styles/views.css: `border-bottom: 1px solid #e2e8f0` added to `#ganttHeaderOverlay`. Evaluate whether to keep this.
</current_state>

<completed_work>

- Bug 2 (fast-typing duplication): FIXED and confirmed foolproof — 7 independent `__new__` guards, Enter-only commit path (commit 31b5f73)
- Frappe v1.2.2 parent bar guard + stale focus timer cancellation (commit 063a195)
- isConnected guard + parent-summary-bar class propagation (commit 4c3ecc5)
- initGanttDragLink now called from setGanttZoom after every zoom-triggered SVG rebuild (commit b428b79)
- Root cause of phantom space identified correctly: 9px gap between overlay height (85px) and bar start y (94px) — commit 8cb60f3 has right formula but is incomplete
</completed_work>

<remaining_work>

- Task 1: Complete the phantom space fix CORRECTLY — update BOTH sides:
  1. Keep overlay height = 94px in project-plan.js:1635 (already done in 8cb60f3)
  2. Update `.tg-header` CSS height in views.css to also be 94px (MISSING — this is what caused the screenshot regression)
  3. Decide whether to keep the border-bottom on #ganttHeaderOverlay (currently in views.css unstaged)
  4. Commit both files together

- Task 2: Browser UAT — verify all 3 zoom modes (Day/Week/Month):
  - No phantom space/gap between header and first bar
  - Left pane header row aligns perfectly with Gantt header row
  - Initial load clean AND after hover/click/drag still clean
</remaining_work>

<decisions_made>

- Root cause confirmed: `#ganttHeaderOverlay` coverage gap — not an SVG element, not a line, not a dot
- The gap is constant 9px in all zoom modes; appears larger in Month only because wide columns have fewer vertical gridlines breaking up the visible strip
- Frappe padding default = 18px; bar y = header_height + padding/2 = 85 + 9 = 94px
- Both project-plan.js (JS height) and views.css (CSS .tg-header height) must be updated together
</decisions_made>

<blockers>

- Commit 8cb60f3 overcorrected without updating the left pane CSS → Browser shows misalignment (Image 6)
- styles/views.css has uncommitted border-bottom change — needs decision (keep or revert)
</blockers>

## Required Reading (in order)

1. `app/views/project-plan.js` lines 1628-1650 — `renderCustomGanttHeader()` where overlay height is set
2. `styles/views.css` — search for `.tg-header` and `ganttHeaderOverlay` to see current CSS values
3. This file — blocking constraints above

## Infrastructure State

- Dev server: `python -m http.server 8000` or `npx http-server` — not running, start before UAT
- Firebase: production (clmc-procurement) — all writes go to production

<context>
Session spent ~3 hours on this bug, chasing it through multiple wrong hypotheses (SVG element injection, zoom-triggered SVG rebuild, parent bar height) before correctly landing on the CSS coverage gap in #ganttHeaderOverlay. The formula fix in 8cb60f3 is correct — we just need to also update the left pane CSS to match.

The fix should take <5 minutes: find .tg-header height in views.css, update to 94px, commit both files. Then UAT.
</context>

<next_action>
Start with: grep -n "tg-header" styles/views.css to find the left pane header height CSS rule. Update it to match the 94px overlay height (or whatever formula resolves to). Then commit styles/views.css together with any remaining project-plan.js changes. Then browser UAT all 3 zoom modes.
</next_action>
