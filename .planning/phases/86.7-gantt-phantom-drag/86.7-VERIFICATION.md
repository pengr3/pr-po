---
phase: 86.7-gantt-phantom-drag
verified: 2026-05-08T08:00:00Z
status: human_needed
score: 6/6 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 4/6
  gaps_closed:
    - "setGanttZoom() now calls mountGanttBarDragGuard() at line 1297 — phantom guard re-attached after every SVG rebuild triggered by zoom switch (commit c550e07)"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Open a project plan. Switch zoom to Day or Month. Drag a Gantt bar horizontally."
    expected: "Bar moves smoothly with no position resets mid-drag. Network tab shows exactly one Firestore write on mouseup, not on every pixel of drag."
    why_human: "Cannot simulate mouse drag events programmatically in this environment."
  - test: "After a zoom switch, drag the progress indicator on a Gantt bar."
    expected: "Same single-write-on-mouseup behavior. No rubber-banding. Single Firestore write on release."
    why_human: "Requires browser interaction with Frappe Gantt SVG events."
  - test: "Begin dragging a bar. Before releasing mouse, click a nav link to navigate to another view. Confirm no uncaught promise errors in DevTools console. Return to the plan view and drag again."
    expected: "destroy() cleans up document mouseup listener. No orphaned handlers. Fresh drag works normally."
    why_human: "Requires timed browser interaction to trigger destroy() mid-drag."
---

# Phase 86.7: Gantt Phantom Drag — Verification Report

**Phase Goal:** Implement phantom drag for Frappe Gantt bars — suppress Firestore writes and re-renders during drag, commit a single write on mouseup to eliminate rubber-banding.
**Verified:** 2026-05-08T08:00:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (commit c550e07 adds mountGanttBarDragGuard() to setGanttZoom())

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Dragging a Gantt bar produces no rubber-banding or mid-drag position resets | VERIFIED | Gap closed. `setGanttZoom()` now calls `mountGanttBarDragGuard()` at line 1297 after `fixGanttContainerScroll()`. SVG mousedown handler is re-attached on every zoom switch. `_ganttBarDragging` is correctly set to true on bar mousedown (line 1219) for all subsequent drags regardless of zoom state. |
| 2 | Firestore receives exactly one write per drag gesture (on mouseup), not one per mousemove callback | VERIFIED | Gap closed by same fix. With `_ganttBarDragging` now correctly set post-zoom, `handleGanttDateChange` (line 1321) and `handleGanttProgressChange` (line 1355) both defer to their respective `_pending*Write` stores and return early. `_ganttDragMouseupHandler` flushes exactly once per gesture at lines 1244–1263. |
| 3 | onSnapshot-triggered renderGantt() calls are skipped while _ganttBarDragging is true | VERIFIED | Line 147: `if (!_ganttBarDragging) renderGantt();` — guard present and correctly positioned inside the tasksUnsub onSnapshot callback. Now fully effective post-zoom since _ganttBarDragging is correctly set. |
| 4 | On mouseup the pending dates are committed to Firestore and the grid updates correctly | VERIFIED | `_ganttDragMouseupHandler` (lines 1238–1282): flushes `_pendingDragWrite` via `updateDoc` at line 1247, calls `recomputeParentDates(parentId)` in `.then()` at line 1255. `document.addEventListener('mouseup', _ganttDragMouseupHandler)` at line 1282. |
| 5 | If the Firestore write fails the bar reverts to its pre-drag position (existing error path) | VERIFIED | `.catch()` at line 1257 calls `renderGantt()` for date write failure. `.catch()` at line 1273 calls `renderGantt()` for progress write failure. Non-drag path `.catch()` at line 1336 also calls `renderGantt()`. All error paths revert correctly. |
| 6 | Progress-bar scrub is equally smooth — same phantom gate applied to handleGanttProgressChange | VERIFIED | Lines 1354–1358: `if (_ganttBarDragging)` sets `_pendingProgressWrite = { taskId, progress }` and returns. Mouseup handler flushes at lines 1267–1280 via `updateDoc`. Same re-attach fix at line 1297 ensures this gate is also effective post-zoom. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/views/project-plan.js` | Phantom drag — state vars, mountGanttBarDragGuard(), gated handlers, destroy() cleanup | VERIFIED | 6 state vars at lines 54–60. `mountGanttBarDragGuard()` defined at lines 1205–1283, called from `renderGantt()` at line 1202 and from `setGanttZoom()` at line 1297. Gates in `handleGanttDateChange` (line 1321) and `handleGanttProgressChange` (line 1355). onSnapshot guard at line 147. destroy() cleanup at lines 255–267. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| SVG mousedown on .bar-wrapper | _ganttBarDragging = true | mountGanttBarDragGuard() svgMousedownHandler | VERIFIED | Wired from both `renderGantt()` (line 1202) and `setGanttZoom()` (line 1297) → `mountGanttBarDragGuard()`. Handler at line 1218 guards with `e.target.closest('.bar-wrapper')` before setting flag at line 1219. |
| document mouseup | updateDoc (single Firestore write) | _ganttDragMouseupHandler flushing _pendingDragWrite | VERIFIED | Lines 1238–1282: handler attached at line 1282, flushes `_pendingDragWrite` at lines 1244–1263 with `updateDoc` call and `recomputeParentDates` in `.then()`. |
| onSnapshot renderGantt() call | suppressed while dragging | if (!_ganttBarDragging) renderGantt() | VERIFIED | Line 147: guard coded correctly inside tasksUnsub callback. |

### Data-Flow Trace (Level 4)

Not applicable — this phase is a write-path guard implementation, not a data-rendering component. No dynamic data display artifact to trace.

### Behavioral Spot-Checks

Step 7b: SKIPPED — phantom drag guard is a browser event interaction; cannot verify programmatically without running the app and simulating mouse events. Route to human verification.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| GANTT-PHANTOM-DRAG | 86.7-01-PLAN.md | Phantom drag implementation eliminating rubber-banding via single mouseup write | SATISFIED | All 6 must-have truths verified. State vars, mountGanttBarDragGuard(), gated handlers, onSnapshot guard, and destroy() cleanup all confirmed in code. |

Note: GANTT-PHANTOM-DRAG is a phase-specific requirement. The parent REQUIREMENTS.md requirement is PM-08 (Phase 86 scope, still Pending overall).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| app/views/project-plan.js | 1224–1228 | Safety timer discards _pendingDragWrite/_pendingProgressWrite without flushing | WARNING | User's drag position silently lost if mouse is released >10s after drag start or after Alt+Tab loses mouse events. No user feedback. Acceptable per T-86.7-02 disposition (10s window is generous; worst case is one snapshot re-render). |

No blockers found.

### Human Verification Required

All automated checks pass. The following require browser testing.

#### 1. Post-zoom drag smoothness (critical — was the blocker in v1)

**Test:** Open a project plan with at least 2 leaf tasks. Switch zoom to "Day" or "Month". Drag a Gantt bar horizontally.
**Expected:** Bar moves smoothly with no position resets. DevTools Network tab shows exactly one Firestore write on mouseup, not one per pixel of drag movement.
**Why human:** Cannot simulate mouse drag events programmatically in this environment.

#### 2. Post-zoom progress scrub

**Test:** After a zoom switch, drag the progress indicator on a Gantt bar.
**Expected:** Same single-write-on-mouseup behavior. No rubber-banding. One Firestore write on release.
**Why human:** Requires browser interaction with Frappe Gantt SVG events.

#### 3. Navigate away mid-drag

**Test:** Begin dragging a bar. Before releasing mouse, click a nav link to navigate to another view (e.g. Home). Confirm no uncaught promise errors in DevTools console. Return to the plan view and drag again.
**Expected:** destroy() cleans up document mouseup listener. No orphaned handlers. Fresh drag works normally.
**Why human:** Requires timed browser interaction to trigger destroy() during an active drag gesture.

### Gaps Summary

No automated gaps remain. The single blocker from the initial verification — `setGanttZoom()` not calling `mountGanttBarDragGuard()` after Frappe rebuilds the SVG via `change_view_mode()` — was fixed in commit c550e07. Line 1297 now reads:

```javascript
mountGanttBarDragGuard(); // Phase 86.7 — re-attach after Frappe SVG rebuild on zoom change
```

All 6 must-have truths are VERIFIED in the static code. Phase goal is achieved pending browser UAT of the 3 human verification items above.

---

_Verified: 2026-05-08T08:00:00Z_
_Verifier: Claude (gsd-verifier)_
