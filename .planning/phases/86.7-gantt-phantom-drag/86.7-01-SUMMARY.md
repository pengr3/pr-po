---
phase: 86.7-gantt-phantom-drag
plan: 01
subsystem: ui
tags: [frappe-gantt, firestore, drag-events, phantom-state, event-listeners]

# Dependency graph
requires:
  - phase: 86.6-gantt-grid-vertical-alignment
    provides: Gantt scroll equalization — stable maxScrollTop before drag guard added
  - phase: 86.4-gantt-ux-improvements
    provides: renderGantt() with fixGanttContainerScroll, bindScrollSync, installGanttScrollClamp
  - phase: 86.3-gantt-fs-violations
    provides: handleGanttDateChange and handleGanttProgressChange — write paths being gated
provides:
  - Phantom drag implementation — _ganttBarDragging flag, mountGanttBarDragGuard(), gated handlers
  - Single Firestore write per drag gesture (flushed on mouseup, not per mousemove)
  - Suppressed onSnapshot renderGantt() re-render during drag (prevents rubber-banding)
  - destroy() cleanup removes document mouseup listener and nulls all phantom drag state
affects:
  - Any future plan modifying renderGantt() must preserve mountGanttBarDragGuard() as last step
  - Any future plan modifying handleGanttDateChange or handleGanttProgressChange must respect _ganttBarDragging gate

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Phantom drag pattern: suppress writes + re-renders during gesture, flush exactly once on mouseup"
    - "SVG mousedown guard: e.target.closest('.bar-wrapper') restricts flag to actual bar drags"
    - "Safety timer (10s) auto-clears _ganttBarDragging if browser loses focus during drag"
    - "Idempotent re-attach: remove old listener before adding new one on every renderGantt() call"

key-files:
  created: []
  modified:
    - app/views/project-plan.js

key-decisions:
  - "Phase 86.7: _ganttBarDragging flag uses SVG mousedown (not Frappe callback) so we detect drag start before Frappe fires on_date_change mid-gesture"
  - "Phase 86.7: mountGanttBarDragGuard() called as step 11 of renderGantt() — Frappe rebuilds SVG on every gantt.refresh() so listener must re-attach each time"
  - "Phase 86.7: non-drag path in handleGanttDateChange (programmatic updates without preceding bar mousedown) continues to write directly via updateDoc — _ganttBarDragging=false means the existing .then()/.catch() path is unchanged"
  - "Phase 86.7: destroy() removes document mouseup listener before nulling state — prevents phantom write after view is destroyed (T-86.7-04 mitigate)"

patterns-established:
  - "Phantom drag pattern: set flag on SVG mousedown (.bar-wrapper guard), defer writes to _pendingDragWrite/_pendingProgressWrite, flush exactly once in document mouseup handler"
  - "Safety timer pattern: clearTimeout on each mousedown, set 10s auto-clear so missed mouseup cannot leave _ganttBarDragging stuck as true permanently"

requirements-completed:
  - GANTT-PHANTOM-DRAG

# Metrics
duration: 2min
completed: 2026-05-08
---

# Phase 86.7 Plan 01: Gantt Phantom Drag Summary

**Phantom drag guard added to project-plan.js: _ganttBarDragging flag, mountGanttBarDragGuard() with mouseup flush, gated handleGanttDateChange/handleGanttProgressChange, suppressed mid-drag onSnapshot renderGantt() call**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-08T06:05:58Z
- **Completed:** 2026-05-08T06:08:00Z
- **Tasks:** 3
- **Files modified:** 1

## Accomplishments
- Added 6 phantom drag state variables to the module-scope state block (Task 1)
- Implemented mountGanttBarDragGuard() with SVG mousedown + document mouseup listener that flushes pending writes exactly once per drag gesture (Task 2)
- Gated handleGanttDateChange and handleGanttProgressChange to defer writes when _ganttBarDragging is true; suppressed onSnapshot renderGantt() call during drag; added destroy() cleanup removing all phantom drag listeners and state (Task 3)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add phantom drag state variables to module-scope block** - `809a416` (feat)
2. **Task 2: Add mountGanttBarDragGuard() and wire into renderGantt()** - `8f64888` (feat)
3. **Task 3: Gate handlers, onSnapshot guard, destroy() cleanup** - `e557db8` (feat)

## Files Created/Modified
- `app/views/project-plan.js` - Added 118 lines: 6 state vars, mountGanttBarDragGuard() (83 lines), 4 gate edits in handlers/onSnapshot/destroy()

## Decisions Made
- mountGanttBarDragGuard() must be the last step in renderGantt() because Frappe rebuilds the SVG node on every refresh and the mousedown listener must be re-attached each time
- The non-drag path in handleGanttDateChange (programmatic) is preserved unchanged — _ganttBarDragging=false means the existing updateDoc path runs normally
- The `!_ganttBarDragging` early-return guard in the document mouseup handler (`if (!_ganttBarDragging) return;`) is the correct entry guard; the two `!_ganttBarDragging` occurrences found by grep (onSnapshot guard at line 147 and mouseup early-return at line 1239) are both correct behavior

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phantom drag is fully implemented and ready for browser UAT
- User should drag a Gantt bar and confirm: (1) no rubber-banding during drag, (2) single Firestore write on mouseup, (3) progress scrub equally smooth
- No blockers for next plan

## Self-Check: PASSED

- `app/views/project-plan.js` modified — confirmed present
- Commit `809a416` exists — confirmed in git log
- Commit `8f64888` exists — confirmed in git log
- Commit `e557db8` exists — confirmed in git log
- `_ganttBarDragging` appears in 8 locations (declaration, mousedown set, mouseup clear, safety timer clear, 2 handler guards, onSnapshot guard, destroy reset)
- `mountGanttBarDragGuard` appears in 2 locations (function declaration + call inside renderGantt)
- No file deletions in any commit

---
*Phase: 86.7-gantt-phantom-drag*
*Completed: 2026-05-08*
