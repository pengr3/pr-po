---
phase: 103-portfolio-table-redesign
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified: [app/views/projects.js]
autonomous: false
requirements: [SC-5, SC-6, SC-7, SC-9, D-02, D-03]
must_haves:
  truths:
    - "Browse All renders collapsible stage groups (On-going, Contracted & Mobilizing, Proposal Stage, For Inspection, Completed, Loss) each with a header count"
    - "Every one of the 10 UNIFIED_STATUS_OPTIONS maps to exactly one stage group (no project lost, none duplicated)"
    - "Group collapse state persists in localStorage 'browse-collapse'; Completed AND Loss collapsed by default"
    - "Browse All respects the SAME filtered+scoped pool as the Feed (client/search/scorecard/Phase-7 scope all apply); scorecards remain above both modes"
  artifacts:
    - path: "app/views/projects.js"
      provides: "STAGE_GROUPS map, collapse persistence (getCollapseState/setCollapseState/toggleStageGroup), real renderBrowseAll grouped renderer"
      contains: "const STAGE_GROUPS"
  key_links:
    - from: "projects.js renderBrowseAll()"
      to: "filteredProjects + STAGE_GROUPS"
      via: "partition the already-filtered pool by status→group"
      pattern: "STAGE_GROUPS"
    - from: "projects.js toggleStageGroup()"
      to: "localStorage 'browse-collapse'"
      via: "setCollapseState persistence"
      pattern: "browse-collapse"
---

<objective>
Replace the Plan-02 placeholder `renderBrowseAll()` in `app/views/projects.js` with the real
Option B stage-grouped collapsible renderer, add the `STAGE_GROUPS` map (covering ALL 10 statuses
incl. For Revision + Loss) and the collapse-persistence helpers, and register the group-toggle
window function. This completes the projects.js D+B hybrid (D-03).

Purpose: Finish the projects portfolio so both modes (Feed from Plan 02, Browse All here) work over
the identical filtered pool, with scorecards above both (D-02), filters/scope preserved (SC-7).
Output: projects.js Browse All fully functional; ready for Plan 04 to mirror into services.js.

OWNERSHIP (orphan-ownership rule — same file as Plan 02): Plan 03 OWNS only `STAGE_GROUPS`,
`getCollapseState`, `setCollapseState`, `toggleStageGroup`, the real `renderBrowseAll`, and the
`window.toggleStageGroup` registration/teardown. It MUST NOT alter the Plan-02-owned symbols
(vmSwitch, renderPortfolio, renderPriorityFeed, getProjectSignal, renderFinancial, normalizeUpdatedAt,
URGENCY_THRESHOLDS) except to delete the placeholder body of renderBrowseAll and replace it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-portfolio-table-redesign/103-CONTEXT.md
@.planning/phases/103-portfolio-table-redesign/103-02-SUMMARY.md
@.claude/skills/spike-findings-pr-po/references/project-portfolio-view.md
@app/views/projects.js

<interfaces>
<!-- Symbols introduced by Plan 02 that Plan 03 reuses (read 103-02-SUMMARY.md for exact names): -->
- `filteredProjects` (module state) — the already-scoped+filtered array to partition by stage.
- `clientsData` (module state) — client-name lookup per row.
- `getDlpState(p)` — for Completed-group DLP accent/tag (reuse, same as Feed rows).
- `renderFinancial(p)` — reuse for each browse row's finance cell.
- `escapeHTML` (utils import).
- A shared `buildFeedRow(p)` (or equivalent row builder) was added in Plan 02 for Feed rows —
  REUSE it for browse rows so feed/browse rows are visually identical (Plan 01 `.feed-row` styles).
  If Plan 02 named it differently, call that name (see 103-02-SUMMARY.md).
- `renderBrowseAll()` — Plan 02 left a placeholder body + the seam comment
  `// Plan 03: Browse All (stage-grouped collapsible) renderer goes here`. Replace the placeholder body.
- `vmSwitch('browse')` calls renderBrowseAll() — already wired in Plan 02.

D-03 group mapping (every one of the 10 statuses maps to EXACTLY one group):
- On-going → ['On-going']
- Contracted & Mobilizing → ['Client Approved','For Mobilization']
- Proposal Stage → ['For Proposal','Proposal for Internal Approval','Proposal Under Client Review','For Revision']
- For Inspection → ['For Inspection']
- Completed → ['Completed']
- Loss → ['Loss']
Collapsed-by-default: Completed AND Loss (terminal). Persist per-group in localStorage 'browse-collapse'.

Per-group spine color (Plan-01 `.stage-group-color` takes an inline color): On-going #1a73e8,
Contracted & Mobilizing #f59e0b, Proposal Stage #7c3aed, For Inspection #94a3b8, Completed #059669, Loss #64748b.
(These are inline-style hex values, NOT CSS vars.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add STAGE_GROUPS + collapse-persistence helpers + toggleStageGroup; register/teardown window fn</name>
  <read_first>
    - app/views/projects.js (the Plan-02 placeholder renderBrowseAll + the seam comment; attachWindowFunctions; destroy)
    - .planning/phases/103-portfolio-table-redesign/103-02-SUMMARY.md (exact Plan-02 symbol names — buildFeedRow, renderFinancial, getDlpState reuse)
    - .claude/skills/spike-findings-pr-po/references/project-portfolio-view.md lines 128-152 (STAGE_GROUPS + getCollapseState/setCollapseState blueprint)
  </read_first>
  <action>
    Edit app/views/projects.js.

    (a) Add the STAGE_GROUPS constant near URGENCY_THRESHOLDS (module top). Use literal hex colors
    (NO var(--*)). Include ALL SIX groups covering all 10 statuses (see D-03 mapping above):
    ```js
    // Phase 103 D-03 — Browse All stage groups (every UNIFIED_STATUS_OPTIONS value maps to exactly one).
    const STAGE_GROUPS = [
        { key: 'ongoing',     label: 'On-going',                statuses: ['On-going'],                                         color: '#1a73e8' },
        { key: 'contracted',  label: 'Contracted & Mobilizing', statuses: ['Client Approved','For Mobilization'],              color: '#f59e0b' },
        { key: 'proposal',    label: 'Proposal Stage',          statuses: ['For Proposal','Proposal for Internal Approval','Proposal Under Client Review','For Revision'], color: '#7c3aed' },
        { key: 'inspection',  label: 'For Inspection',          statuses: ['For Inspection'],                                  color: '#94a3b8' },
        { key: 'completed',   label: 'Completed',               statuses: ['Completed'],                                       color: '#059669' },
        { key: 'loss',        label: 'Loss',                    statuses: ['Loss'],                                            color: '#64748b' }
    ];
    ```

    (b) Add collapse-persistence helpers (Completed + Loss default-collapsed):
    ```js
    function getCollapseState(key) {
        let saved = {};
        try { saved = JSON.parse(localStorage.getItem('browse-collapse') || '{}'); } catch (_) {}
        return saved[key] ?? (key === 'completed' || key === 'loss');
    }
    function setCollapseState(key, collapsed) {
        let saved = {};
        try { saved = JSON.parse(localStorage.getItem('browse-collapse') || '{}'); } catch (_) {}
        saved[key] = collapsed;
        localStorage.setItem('browse-collapse', JSON.stringify(saved));
    }
    ```

    (c) Add toggleStageGroup(key): flip the persisted state then re-render just that group (or
    re-call renderBrowseAll() for simplicity), e.g.:
    ```js
    function toggleStageGroup(key) {
        setCollapseState(key, !getCollapseState(key));
        renderBrowseAll();
    }
    ```

    (d) Register `window.toggleStageGroup = toggleStageGroup;` in attachWindowFunctions() (next to
    window.vmSwitch). Add `delete window.toggleStageGroup;` in destroy() (next to delete window.vmSwitch).
  </action>
  <verify>
    <automated>cd "C:/Users/franc/dev/projects/pr-po" && node --check app/views/projects.js && node -e "const c=require('fs').readFileSync('app/views/projects.js','utf8'); const need=['const STAGE_GROUPS','function getCollapseState(','function setCollapseState(','function toggleStageGroup(',\"localStorage.getItem('browse-collapse'\",'window.toggleStageGroup = toggleStageGroup','delete window.toggleStageGroup']; const miss=need.filter(s=>!c.includes(s)); if(miss.length){console.error('MISSING',miss);process.exit(1);} const ALL=['For Inspection','For Proposal','Proposal for Internal Approval','Proposal Under Client Review','For Revision','Client Approved','For Mobilization','On-going','Completed','Loss']; const sg=c.slice(c.indexOf('const STAGE_GROUPS'), c.indexOf('const STAGE_GROUPS')+900); const missStatus=ALL.filter(s=>!sg.includes(s)); if(missStatus.length){console.error('STAGE_GROUPS missing statuses',missStatus);process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/projects.js` exits 0
    - projects.js contains `const STAGE_GROUPS`, `function getCollapseState(`, `function setCollapseState(`, `function toggleStageGroup(`
    - The STAGE_GROUPS literal references ALL 10 statuses from UNIFIED_STATUS_OPTIONS (verify gate checks each)
    - getCollapseState defaults `completed` AND `loss` to collapsed (`key === 'completed' || key === 'loss'`)
    - attachWindowFunctions contains `window.toggleStageGroup = toggleStageGroup` AND destroy() contains `delete window.toggleStageGroup` (symmetric)
    - projects.js references `localStorage.getItem('browse-collapse')` and `localStorage.setItem('browse-collapse'`
  </acceptance_criteria>
  <done>STAGE_GROUPS covers all 10 statuses; collapse persistence works with Completed+Loss default-collapsed; toggle window fn registered+deleted symmetrically.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement the real renderBrowseAll grouped+collapsible renderer over the filtered pool</name>
  <read_first>
    - app/views/projects.js (the Plan-02 placeholder renderBrowseAll to replace; buildFeedRow/renderFinancial reuse)
    - .claude/skills/spike-findings-pr-po/references/project-portfolio-view.md lines 128-152
    - Plan 01 classes: .stage-group, .stage-group-header, .stage-group-chevron, .stage-group-color, .stage-group-count, .stage-group-body, .stage-group-empty (and .feed-row for the rows)
  </read_first>
  <action>
    Edit app/views/projects.js — REPLACE the placeholder renderBrowseAll() body with the real renderer:

    - Target `#pdb-browse`; if absent, return.
    - For each group in STAGE_GROUPS:
      - `const rows = filteredProjects.filter(p => group.statuses.includes(p.project_status));`
        (Use the same module `filteredProjects` the Feed uses → all filters/scope already applied, SC-7.)
      - `const collapsed = getCollapseState(group.key);`
      - Emit a `.stage-group${collapsed ? ' collapsed' : ''}` block:
        - `.stage-group-header` with `onclick="window.toggleStageGroup('${group.key}')"` containing:
          a `.stage-group-color` span with `style="background:${group.color}"`, a `.stage-group-chevron`
          (e.g. ▾), the group.label, and a `.stage-group-count` showing `rows.length`.
        - `.stage-group-body` containing either the rows (reuse buildFeedRow(p) from Plan 02 so they
          match Feed rows exactly — including the Phase 102 dlp accent + renderFinancial finance cell)
          or a `.stage-group-empty` "No projects in this stage" line when rows.length === 0.
    - Join all groups and set `el.innerHTML`. ALWAYS render all 6 groups even when empty (D-03 — groups
      are the browse skeleton). For very long groups, DO NOT paginate (CONTEXT discretion → confirmed
      drop pagination in both new modes; if a group exceeds, say, 50 rows it still renders fully — acceptable
      for this dataset size).
    - Sort rows within a group by project_code (or project_name) ascending for a stable browse order.

    Ensure renderBrowseAll() works whether or not #pdb-browse is currently visible (it's called from
    renderPortfolio() and from vmSwitch('browse')).
  </action>
  <verify>
    <automated>cd "C:/Users/franc/dev/projects/pr-po" && node --check app/views/projects.js && node -e "const c=require('fs').readFileSync('app/views/projects.js','utf8'); const rb=c.slice(c.indexOf('function renderBrowseAll(')); const need=['stage-group','stage-group-header','stage-group-chevron','stage-group-color','stage-group-count','stage-group-body',\"toggleStageGroup('\",'STAGE_GROUPS','filteredProjects']; const miss=need.filter(s=>!rb.slice(0,2500).includes(s)); if(miss.length){console.error('renderBrowseAll MISSING',miss);process.exit(1);} if(rb.includes('implemented in Plan 03')){console.error('placeholder still present');process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/projects.js` exits 0
    - The renderBrowseAll body contains `STAGE_GROUPS`, `filteredProjects`, `stage-group-header`, `stage-group-chevron`, `stage-group-count`, `stage-group-body`, and `toggleStageGroup('`
    - The Plan-02 placeholder string `implemented in Plan 03` no longer appears in projects.js
    - renderBrowseAll reuses the Plan-02 row builder (buildFeedRow or named equivalent) and renderFinancial (not a new row markup)
    - Each `.stage-group-header` carries `style="background:${group.color}"` on its `.stage-group-color` span (inline hex, no CSS var)
  </acceptance_criteria>
  <done>Browse All renders all 6 collapsible stage groups over the filtered pool with per-group counts, default-collapsed Completed+Loss, rows identical to Feed rows; placeholder gone; node --check passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>projects.js Browse All mode: collapsible stage groups (On-going / Contracted & Mobilizing / Proposal Stage / For Inspection / Completed / Loss) with per-group counts, collapse persisted in localStorage, Completed+Loss collapsed by default, rows identical to Feed rows (DLP accents + stage-aware finance), all over the same filtered+scoped pool with scorecards retained.</what-built>
  <how-to-verify>
    1. `python -m http.server 8000` → Projects view → click [≡ Browse All].
    2. Confirm six group headers appear with counts; Completed and Loss are collapsed on first load; the others expanded. Each header click toggles collapse; reload page → collapse state persists per group.
    3. Confirm a project of EVERY status lands in exactly one group (e.g. a For Revision project under Proposal Stage; a Loss project under Loss). Group counts sum to the visible total.
    4. Confirm browse rows look identical to Feed rows (same finance cell, same DLP left-accent/tag on Completed-in-DLP rows).
    5. Apply a Client filter / type in Search / click a scorecard → Browse All re-filters too (groups shrink). Switch to Feed and back → both reflect the same filter. Phase-7: if testing as operations_user, only assigned projects appear in both modes.
    6. Click a browse row → navigates to the project detail. DevTools Console: zero errors; Network: no new Firestore listener.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Plan 04 (services.js mirror), or describe issues for a gap plan.</resume-signal>
</task>

</tasks>

<verification>
- node --check app/views/projects.js exits 0
- STAGE_GROUPS covers all 10 statuses (Task 1 grep gate)
- Placeholder removed; renderBrowseAll real (Task 2 grep gate)
- toggleStageGroup register/teardown symmetric
- Collapse persistence + filter sharing confirmed in browser UAT
</verification>

<success_criteria>
projects.js Browse All shows all six collapsible stage groups over the same filtered pool as the
Feed, with persisted collapse (Completed+Loss default-collapsed), every status mapped to exactly
one group, scorecards above both modes, and no new Firestore reads — completing the projects D+B hybrid.
</success_criteria>

<output>
After completion, create `.planning/phases/103-portfolio-table-redesign/103-03-SUMMARY.md`.
Note the final projects.js helper/symbol names so Plan 04 mirrors them into services.js.
</output>
