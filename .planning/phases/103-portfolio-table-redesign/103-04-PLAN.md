---
phase: 103-portfolio-table-redesign
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified: [app/views/services.js]
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6, SC-7, SC-8, SC-9, D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "services.js renders the same [🔥 Priority Feed] [≡ Browse All] toggle, persisted to localStorage 'services-view-mode' (first-time default Feed)"
    - "Priority Feed + Browse All render INSIDE the active service_type pool (one-time vs recurring sub-tab is the OUTER filter, view-mode is inner — D-01)"
    - "Stage-aware finance (4 states) + Phase 102 DLP reuse + urgency signals mirror projects.js exactly via shared CSS classes"
    - "services.js has its own getDlpState (mirrored, no new listener) OR reuses the identical no-listener contract; updated_at normalized for both shapes (D-08)"
    - "Phase 92.2 service scorecards stay above both modes; client filter + search + CSV + services_user assignment scope (ASSIGN-04) + sub-tab all still work; no new Firestore listener"
  artifacts:
    - path: "app/views/services.js"
      provides: "view-mode toggle, urgency/finance/DLP helpers, Priority Feed + Browse All grouped renderers mirrored from projects.js, coexisting with service_type sub-tab"
      contains: "function getServiceSignal("
  key_links:
    - from: "services.js applyServiceFilters()"
      to: "renderServicePortfolio() (feed + browse)"
      via: "dispatcher replaces renderServicesTable; runs AFTER service_type + scope + filter narrowing"
      pattern: "renderServicePortfolio"
    - from: "services.js renderServiceBrowseAll()"
      to: "filteredServices + SERVICE_STAGE_GROUPS"
      via: "partition the already-filtered (sub-tab + scope + filters) pool"
      pattern: "SERVICE_STAGE_GROUPS"
---

<objective>
Mirror the projects.js D+B hybrid (Plans 02+03) into `app/views/services.js`, coexisting with the
one-time/recurring sub-tab. Same shared CSS classes (Plan 01), same urgency thresholds, same
stage-aware finance, same Phase 102 DLP reuse, same collapse persistence — but scoped per-service
and keyed to the `services-view-mode` / service-specific localStorage keys.

Purpose: D-01 — services get the identical redesign. The KEY difference: the service_type sub-tab
(one-time vs recurring) is an OUTER filter that runs BEFORE view-mode rendering (the feed/groups
render within the already service_type-filtered pool). applyServiceFilters() already builds that
narrowed `filteredServices` pool — the new renderers consume it directly.
Output: services.js Feed + Browse All live, mirroring projects.js; completes the phase.

Use 103-02-SUMMARY.md and 103-03-SUMMARY.md as the authoritative source for the projects.js symbol
shapes to mirror. Prefix new service symbols to avoid window-name collisions with projects.js
(e.g. window.vmSwitchService, window.toggleServiceStageGroup) — services.js already namespaces its
window fns (handleServiceScorecardClick, etc.). Internal (non-window) helpers may share names since
modules are isolated, but mirror the projects naming with a Service suffix for clarity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-portfolio-table-redesign/103-CONTEXT.md
@.planning/phases/103-portfolio-table-redesign/103-02-SUMMARY.md
@.planning/phases/103-portfolio-table-redesign/103-03-SUMMARY.md
@app/views/services.js

<interfaces>
<!-- services.js current symbols to integrate with: -->
- utils import line (line 9) imports `{ showLoading, showToast, generateServiceCode, normalizePersonnel, syncServicePersonnelToAssignments, getAssignedServiceCodes, downloadCSV, escapeHTML }`. ADD `formatCurrency`.
- `UNIFIED_STATUS_OPTIONS` (lines 39-51) — 11 values incl. 'Draft' (Phase 88). NOTE: 'Draft' is NOT
  in any Spike-033 stage group. Browse All groups must add a 'Draft' handling: either a leading
  "Draft / Pre-proposal" group OR fold Draft into Proposal Stage. CONFIRM-IN-PLAN resolution: add a
  separate leading group `{ key:'draft', label:'Draft', statuses:['Draft'], color:'#94a3b8' }`
  collapsed-by-default, so all 11 services statuses map to exactly one group (services parity for D-03).
- `SCORECARD_STATUS_OPTIONS` (line 54) = options minus 'Draft' — leave scorecards untouched (D-02).
- `filteredServices` (module state) — already narrowed by service_type (sub-tab) + assignment scope
  + client/search/scorecard in applyServiceFilters() (lines 809-854). The Feed/Browse renderers
  consume THIS — the sub-tab outer filter is therefore already applied (D-01).
- `currentActiveTab` ('services' | 'recurring') — the sub-tab; do not change its handling.
- `clientsData` (module state) — client-name lookup.
- `applyServiceFilters()` (line 809) — currently ends with `renderServicesTable(); renderServiceScorecards(scopedServicesForTab);`. Replace `renderServicesTable()` with `renderServicePortfolio()`; KEEP renderServiceScorecards.
- `renderServicesTable()` (line 973) renders `#servicesTableBody` — replace flat table with toggle + #pdb-feed-svc/#pdb-browse-svc containers (use service-specific ids to avoid clashing with projects' #pdb-feed if both ever in DOM — they won't be simultaneously, but be safe).
- attachWindowFunctions() (line 70) / destroy() (line 370) — register/delete new window fns symmetrically.
- service rows deep-link to `#/services/detail/${service_code}` (line 1004) — preserve.

CONSTRAINTS (identical to projects):
- No new Firestore listener/read (the services onSnapshot at line 954 is the only data source; D-05).
- billed% unavailable at render time (no collectibles loaded) → computeServiceBillingPct returns null;
  On-going finance shows contract value + tranche-count, empty mini-bar (no fake %).
- updated_at normalized for BOTH Timestamp + ISO string; missing → On Track (D-08).
- getDlpState mirrored from projects.js (no isRetentionCollected branch — honor retention_released_at +
  expiry only; render-only). Services DLP fields may be sparse — getDlpState returns 'active' when
  dlp_months absent or status !== 'Completed', so behaves safely.
- LANDMINE: no `var(--*)` and no `fmt()` — use formatCurrency + literal hex.
- Localstorage keys: 'services-view-mode' (D-04) and 'browse-collapse-services' (separate from
  projects' 'browse-collapse' so the two views' collapse states don't collide).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mirror helpers into services.js — thresholds, normalizeUpdatedAt, getDlpState, getServiceSignal, computeServiceUrgencySignals, renderServiceFinancial, SERVICE_STAGE_GROUPS, collapse helpers</name>
  <read_first>
    - app/views/services.js lines 1-54 (imports, UNIFIED_STATUS_OPTIONS, SCORECARD_STATUS_OPTIONS)
    - app/views/services.js lines 998-1041 (renderServicesTable row markup + deep link to mirror)
    - .planning/phases/103-02-SUMMARY.md + 103-03-SUMMARY.md (final projects.js helper bodies to copy)
    - app/views/projects.js (the shipped getProjectSignal / renderFinancial / normalizeUpdatedAt / getDlpState / STAGE_GROUPS / getCollapseState — copy and adapt)
  </read_first>
  <action>
    Edit app/views/services.js — add a Phase 103 helpers section (mirror projects.js):

    (a) Add `formatCurrency` to the utils import (line 9).

    (b) Copy `URGENCY_THRESHOLDS` constant verbatim (same 5 day thresholds, D-07).

    (c) Copy `normalizeUpdatedAt(v)` verbatim (D-08 — Timestamp + ISO string + millis + null).

    (d) Copy `getDlpState(project)` from projects.js (lines 908-913) verbatim — mirrored render-only
    DLP state (no isRetentionCollected branch). It returns 'active' when dlp_months absent or status
    !== 'Completed', so sparse-DLP services are safe.

    (e) Add `getServiceSignal(s, now)` mirroring getProjectSignal exactly (same status strings, same
    threshold order, same DLP-expired/in-dlp branches). CONTEXT discretion: "default reuse project
    signals unchanged" for recurring services — so DO NOT special-case recurring; one signal function.
    Add `getServiceDefaultOkSignal(s)` mirroring getDefaultOkSignal.

    (f) Add `computeServiceUrgencySignals(services)` → { urgent, watch, ok } (mirror).

    (g) Add `computeServiceBillingPct(service)` → returns null (no billed data without listener; mirror).

    (h) Add `renderServiceFinancial(service)` mirroring renderFinancial's 4-state matrix (D-06):
    pre-contract (For Inspection→For Revision) → .fin-pre; contracted (Client Approved/For Mobilization)
    → .fin-ready; On-going → .fin-active (empty mini-bar + "{N} tranches defined"); Completed → getDlpState
    branch (.portfolio-dlp-tag amber/red/green) or .fin-done green 100%; Loss → .fin-loss. Draft (and any
    other/legacy) → .fin-pre '—'. Use formatCurrency + service.budget / service.contract_cost.

    (i) Add `SERVICE_STAGE_GROUPS` (D-03 mirror PLUS a leading Draft group so all 11 services statuses map):
    ```js
    const SERVICE_STAGE_GROUPS = [
        { key: 'draft',       label: 'Draft',                   statuses: ['Draft'],                                          color: '#94a3b8' },
        { key: 'ongoing',     label: 'On-going',                statuses: ['On-going'],                                       color: '#1a73e8' },
        { key: 'contracted',  label: 'Contracted & Mobilizing', statuses: ['Client Approved','For Mobilization'],            color: '#f59e0b' },
        { key: 'proposal',    label: 'Proposal Stage',          statuses: ['For Proposal','Proposal for Internal Approval','Proposal Under Client Review','For Revision'], color: '#7c3aed' },
        { key: 'inspection',  label: 'For Inspection',          statuses: ['For Inspection'],                                color: '#94a3b8' },
        { key: 'completed',   label: 'Completed',               statuses: ['Completed'],                                      color: '#059669' },
        { key: 'loss',        label: 'Loss',                    statuses: ['Loss'],                                           color: '#64748b' }
    ];
    ```

    (j) Add collapse helpers keyed to 'browse-collapse-services' (Completed + Loss + Draft default-collapsed):
    getServiceCollapseState(key) → default true for key in ('completed','loss','draft'); setServiceCollapseState(key, collapsed).
  </action>
  <verify>
    <automated>cd "C:/Users/franc/dev/projects/pr-po" && node --check app/views/services.js && node -e "const c=require('fs').readFileSync('app/views/services.js','utf8'); const need=['URGENCY_THRESHOLDS','function normalizeUpdatedAt(','function getDlpState(','function getServiceSignal(','function computeServiceUrgencySignals(','function renderServiceFinancial(','const SERVICE_STAGE_GROUPS','function getServiceCollapseState(','function setServiceCollapseState(','formatCurrency',\"'browse-collapse-services'\"]; const miss=need.filter(s=>!c.includes(s)); if(miss.length){console.error('MISSING',miss);process.exit(1);} const ALL=['Draft','For Inspection','For Proposal','Proposal for Internal Approval','Proposal Under Client Review','For Revision','Client Approved','For Mobilization','On-going','Completed','Loss']; const sg=c.slice(c.indexOf('const SERVICE_STAGE_GROUPS'), c.indexOf('const SERVICE_STAGE_GROUPS')+1000); const missS=ALL.filter(s=>!sg.includes(s)); if(missS.length){console.error('SERVICE_STAGE_GROUPS missing',missS);process.exit(1);} if(/var\\(--(blue|amber|purple|green)\\)/.test(c)){console.error('forbidden css var');process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/services.js` exits 0
    - services.js contains `URGENCY_THRESHOLDS`, `function normalizeUpdatedAt(`, `function getDlpState(`, `function getServiceSignal(`, `function computeServiceUrgencySignals(`, `function renderServiceFinancial(`, `const SERVICE_STAGE_GROUPS`, `function getServiceCollapseState(`, `function setServiceCollapseState(`
    - services.js utils import line contains `formatCurrency`
    - SERVICE_STAGE_GROUPS maps ALL 11 services statuses incl. 'Draft' (verify gate checks each)
    - getServiceCollapseState defaults completed, loss, AND draft to collapsed
    - normalizeUpdatedAt references `.toDate`, `.seconds`, and `new Date(`
    - renderServiceFinancial contains `.fin-pre`, `.fin-ready`, `.fin-active`, `.fin-done`, `.fin-loss`, `portfolio-dlp-tag`
    - collapse helpers use the key `'browse-collapse-services'` (distinct from projects)
    - No `var(--...)` anywhere in services.js
  </acceptance_criteria>
  <done>All projects-mirrored helpers present in services.js with the Draft group added; node --check passes; no new listener, no forbidden CSS vars.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace services flat table with toggle + dual containers; add vmSwitchService, renderServicePortfolio, renderServiceFeed, renderServiceBrowseAll, buildServiceRow; wire applyServiceFilters; window symmetry</name>
  <read_first>
    - app/views/services.js lines 116-288 (render() — scorecards ~224, filter bar ~238, flat table ~257 to replace; sub-tab tab-bar ~128 stays)
    - app/views/services.js lines 70-92 (attachWindowFunctions) + 370-432 (destroy)
    - app/views/services.js lines 808-854 (applyServiceFilters — sub-tab + scope + filter pipeline; the call to renderServicesTable + renderServiceScorecards to preserve)
    - app/views/services.js lines 998-1041 (renderServicesTable — row markup + deep link to mirror into buildServiceRow)
    - .planning/phases/103-02-SUMMARY.md + 103-03-SUMMARY.md (projects render/vmSwitch/renderPortfolio/renderPriorityFeed/renderBrowseAll shapes)
  </read_first>
  <action>
    Edit app/views/services.js.

    (a) In render(), REPLACE the `<!-- Services Table -->` block (lines 257-284, incl. #servicesTableBody)
    with the toggle + dual service-specific containers (keep the sub-tab tab-bar ~128, scorecards ~224,
    and filter bar ~238 untouched ABOVE — D-01/D-02):
    ```html
    <div class="vm-toggle">
      <button class="vm-btn" id="vm-feed-svc" onclick="window.vmSwitchService('feed')">🔥 Priority Feed</button>
      <button class="vm-btn" id="vm-browse-svc" onclick="window.vmSwitchService('browse')">≡ Browse All</button>
    </div>
    <div id="pdb-feed-svc" class="pdb-mode"></div>
    <div id="pdb-browse-svc" class="pdb-mode" style="display:none;"></div>
    ```

    (b) Add vmSwitchService(mode) — mirror projects vmSwitch, targeting #pdb-feed-svc/#pdb-browse-svc,
    toggling #vm-feed-svc/#vm-browse-svc `.vm-on`, persisting to localStorage 'services-view-mode',
    calling renderServiceBrowseAll() on browse.

    (c) Add renderServicePortfolio() — dispatcher: calls renderServiceFeed() + renderServiceBrowseAll(),
    then restores `localStorage.getItem('services-view-mode') || 'feed'` via vmSwitchService (D-04).

    (d) Add buildServiceRow(s) — a `.feed-row tier-{level}` clickable row (onclick →
    `#/services/detail/${escapeHTML(s.service_code)}`) mirroring projects buildFeedRow: title = code+name,
    sub = client name + project_status (legacy-italic per lines 1010-1016), signal text+hint,
    renderServiceFinancial(s) on the right, Phase 102 dlp accent class from getDlpState(s). Reuse Plan 01
    .feed-row* classes (shared CSS — identical look, D-01).

    (e) Add renderServiceFeed() — render into #pdb-feed-svc from `filteredServices` (already sub-tab +
    scope + filter narrowed): computeServiceUrgencySignals(filteredServices) → 3 .feed-section blocks
    (hide empty urgent/watch, always show ok — mirror projects), rows via buildServiceRow. No pagination.

    (f) Add renderServiceBrowseAll() — render into #pdb-browse-svc: for each group in SERVICE_STAGE_GROUPS,
    filter `filteredServices` by group.statuses, build a collapsible .stage-group (header with
    onclick="window.toggleServiceStageGroup('${group.key}')", .stage-group-color inline group.color,
    chevron, label, .stage-group-count = count; body = buildServiceRow rows or .stage-group-empty).
    Always render all 7 groups (incl. Draft). Collapse via getServiceCollapseState.

    (g) Add toggleServiceStageGroup(key): setServiceCollapseState(key, !getServiceCollapseState(key)); renderServiceBrowseAll();

    (h) In applyServiceFilters() (line 852) replace `renderServicesTable();` with `renderServicePortfolio();`.
    KEEP `renderServiceScorecards(scopedServicesForTab);` (line 853) and the entire sub-tab + ASSIGN-04
    scope + filter pipeline (lines 810-844) intact. Also update the permissionsChanged handler (line 301)
    from renderServicesTable() to renderServicePortfolio().

    (i) Remove dead flat-table machinery (mirror Plan 02 cleanup, including the W-3 dangling-call
    discipline — an all-or-nothing removal; never delete a function while leaving its call site, or the
    Feed throws a runtime ReferenceError that `node --check` cannot catch):
      - `renderServicesTable` (973-1041), `updateServiceSortIndicators` (921-932),
        `sortServices` AND `sortFilteredServices` (857-918), `changeServicesPage` (1349-1361),
        `updateServicePaginationControls` (1364-1409)
      - **the `sortFilteredServices();` CALL inside applyServiceFilters()** (mirror of projects line 802) —
        delete it; the renderers impose their own ordering (Feed by urgency, Browse by stage)
      - the `sortColumn`/`sortDirection` (service equivalents) module state + any destroy() resets of them
      - window registrations (sortServices line 82, changeServicesPage line 78) + matching destroy() deletes (lines 417, 420)
      - EVERY remaining call site of `renderServicesTable()` must be REPLACED with `renderServicePortfolio()`
        (not just deleted — live re-render triggers). Grep `renderServicesTable` to find them ALL (line numbers
        drift): at minimum the `permissionsChanged` handler (~line 301, see step h) AND the
        `toggleAddServiceForm()` close/else branch (mirror of projects.js `toggleAddProjectForm` line 621) which
        re-renders the list after the add/edit form closes.
    After this, grep must show ZERO references to `sortFilteredServices`, ZERO to `renderServicesTable`, and
    ZERO to `#servicesTableBody` anywhere in services.js. (D-03 drops per-column sort.)

    (j) Register window.vmSwitchService + window.toggleServiceStageGroup in attachWindowFunctions();
    add matching `delete window.vmSwitchService;` + `delete window.toggleServiceStageGroup;` in destroy().
  </action>
  <verify>
    <automated>cd "C:/Users/franc/dev/projects/pr-po" && node --check app/views/services.js && node -e "const c=require('fs').readFileSync('app/views/services.js','utf8'); const need=['function vmSwitchService(','function renderServicePortfolio(','function renderServiceFeed(','function renderServiceBrowseAll(','function buildServiceRow(','function toggleServiceStageGroup(','pdb-feed-svc','pdb-browse-svc','renderServicePortfolio()',\"localStorage.setItem('services-view-mode'\",'window.vmSwitchService','window.toggleServiceStageGroup']; const miss=need.filter(s=>!c.includes(s)); if(miss.length){console.error('MISSING',miss);process.exit(1);} if(c.includes('servicesTableBody')){console.error('flat table id still referenced');process.exit(1);} if(c.includes('sortFilteredServices')){console.error('W-3 dangling: sortFilteredServices still referenced (definition or call) — remove fully incl. the call in applyServiceFilters');process.exit(1);} if(c.includes('renderServicesTable')){console.error('renderServicesTable still referenced (definition or call) — replace call sites with renderServicePortfolio');process.exit(1);} const pairs=[['window.vmSwitchService','delete window.vmSwitchService'],['window.toggleServiceStageGroup','delete window.toggleServiceStageGroup']]; for(const [r,d] of pairs){ if(!c.includes(r)||!c.includes(d)){console.error('asymmetric',r);process.exit(1);} } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/services.js` exits 0
    - services.js contains `function vmSwitchService(`, `function renderServicePortfolio(`, `function renderServiceFeed(`, `function renderServiceBrowseAll(`, `function buildServiceRow(`, `function toggleServiceStageGroup(`
    - render() emits `vm-toggle`, `id="pdb-feed-svc"`, `id="pdb-browse-svc"`
    - services.js contains `localStorage.setItem('services-view-mode'` and `localStorage.getItem('services-view-mode')`
    - applyServiceFilters() calls `renderServicePortfolio()` and still calls `renderServiceScorecards(`
    - The string `servicesTableBody` no longer appears in services.js
    - The string `sortFilteredServices` no longer appears in services.js — neither definition NOR the call in applyServiceFilters() (W-3 runtime-ReferenceError landmine node --check cannot detect)
    - The string `renderServicesTable` no longer appears in services.js (definition + all call sites incl. permissionsChanged + toggleAddServiceForm close branch → renderServicePortfolio())
    - Window pairs symmetric: `window.vmSwitchService`↔`delete window.vmSwitchService` AND `window.toggleServiceStageGroup`↔`delete window.toggleServiceStageGroup`
    - The sub-tab pipeline (`currentActiveTab`, `service_type`, `getAssignedServiceCodes`) in applyServiceFilters is unchanged
  </acceptance_criteria>
  <done>Services flat table replaced by toggle + Feed + Browse All, rendering within the active service_type pool; scorecards/filters/sub-tab/scope preserved; window symmetry intact; node --check passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>services.js full D+B hybrid mirror: view-mode toggle (persisted to 'services-view-mode'), Priority Feed + Browse All (7 groups incl. Draft) rendering WITHIN the active one-time/recurring sub-tab, stage-aware finance, Phase 102 DLP reuse, Phase 92.2 scorecards retained, client/search/CSV/services_user scope all preserved.</what-built>
  <how-to-verify>
    1. `python -m http.server 8000` → Services view.
    2. Confirm the Services / Recurring sub-tab bar + scorecard strip + filter bar all still render ABOVE the new [🔥 Priority Feed][≡ Browse All] toggle (Feed active on first load).
    3. Confirm Feed splits services into Needs Attention / Worth Watching / On Track with signal text+hint; finance cells match the projects 4-state behavior (pre-contract no bar, contracted "billing not started", On-going contract value + empty mini-bar + tranche count, Completed green 100% or DLP tag).
    4. Switch the sub-tab to Recurring → the Feed/Browse now show ONLY recurring services (sub-tab is the outer filter); switch back to Services → only one-time. The view-mode (Feed/Browse) is remembered across the sub-tab switch.
    5. Click [≡ Browse All] → seven collapsible groups (Draft, On-going, Contracted & Mobilizing, Proposal Stage, For Inspection, Completed, Loss); Draft+Completed+Loss collapsed by default; collapse persists across reload (independent of the projects view's collapse state). A Draft service appears under Draft.
    6. Client filter / Search / scorecard click narrow BOTH modes within the current sub-tab. As a services_user, only assigned services appear. Click a row → service detail. DevTools Console: zero errors; Network: no new Firestore listener.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 103 complete, or describe issues for a gap plan.</resume-signal>
</task>

</tasks>

<verification>
- node --check app/views/services.js exits 0 (both task verifies)
- SERVICE_STAGE_GROUPS maps all 11 statuses incl. Draft (Task 1 grep gate)
- No #servicesTableBody references; window pairs symmetric (Task 2 grep gate)
- Sub-tab outer / view-mode inner behavior + no new listener confirmed in browser UAT
</verification>

<success_criteria>
services.js renders the identical D+B hybrid as projects.js (shared CSS), rendering Feed + Browse All
within the active service_type sub-tab pool, with stage-aware finance, reused DLP visuals, persisted
toggle + collapse (own localStorage keys), retained scorecards/filters/scope, and no new Firestore reads.
</success_criteria>

<output>
After completion, create `.planning/phases/103-portfolio-table-redesign/103-04-SUMMARY.md`.
</output>
