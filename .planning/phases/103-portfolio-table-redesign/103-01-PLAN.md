---
phase: 103-portfolio-table-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [styles/views.css]
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-5, SC-6, D-02, D-03, D-04, D-06]
must_haves:
  truths:
    - "A view-mode toggle (.vm-toggle / .vm-btn / .vm-on) is styled and visually distinguishes the active mode"
    - "Priority Feed urgency sections (Needs Attention red / Worth Watching amber / On Track green) have distinct section headers and row accents"
    - "Stage-aware finance cells (pre-contract muted / contracted / active mini-bar / done) are styled"
    - "Collapsible stage-group headers (.stage-group-header) with a count chip and chevron rotate-on-collapse are styled"
    - "All new classes reuse the existing literal hex palette (NOT undefined CSS vars like var(--blue))"
    - "The Phase 102 DLP left-accent is preserved on the new div rows via .feed-row.dlp-amber/red/green rules placed after .feed-row.tier-* (the shipped tr.dlp-* selectors do not match div rows)"
  artifacts:
    - path: "styles/views.css"
      provides: "Phase 103 portfolio-redesign CSS block (toggle, feed sections, urgency rows, stage groups, stage-aware finance)"
      contains: ".vm-toggle"
  key_links:
    - from: "styles/views.css .vm-btn.vm-on"
      to: "projects.js/services.js vmSwitch()"
      via: "class toggled by JS to mark active mode"
      pattern: "vm-on"
---

<objective>
Append a single shared CSS block to `styles/views.css` that styles the entire Phase 103
portfolio redesign for BOTH `projects.js` and `services.js`. This is the foundation wave —
every JS plan (02/03/04) references these class names, so this must land first.

Purpose: One shared stylesheet block so projects and services render identically (D-01) and
the JS plans can be written against known class names with zero CSS guesswork.
Output: A `/* ===== Phase 103 — Portfolio Table Redesign ===== */` block appended after the
existing Phase 102 block (currently the file ends at line 5040 with `/* ===== End Phase 102 — DLP & Retention ===== */`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-portfolio-table-redesign/103-CONTEXT.md
@.claude/skills/spike-findings-pr-po/references/project-portfolio-view.md

<interfaces>
<!-- Existing reusable classes the new block must NOT duplicate or override (extend only): -->
From styles/views.css (already shipped):
- `.project-scorecards`, `.project-scorecard-card`, `.project-scorecard-card--active`, `.project-scorecard-card--total` (Phase 92/92.1 — scorecard strip; DO NOT touch, must stay above both modes)
- `.portfolio-dlp-tag` (Phase 102) — REUSED verbatim by the new modes; do not redefine. The DLP **tag** span is emitted by the JS plans (02/03/04) and styled by this class.
- `tr.dlp-amber` / `tr.dlp-red` / `tr.dlp-green` (left-accent borders, Phase 102) — these target `<tr>` / `.portfolio-row` ONLY (see styles/views.css ~5017). The new modes render `<div class="feed-row …">`, NOT table rows, so these selectors DO NOT match the new rows and the DLP left-accent border would be silently lost. This plan therefore adds NEW `.feed-row.dlp-amber/red/green` override rules (Task 1 group 7) so the DLP accent renders on div rows — and they must sit AFTER the `.feed-row.tier-*` rules to win the equal-specificity tie (a Completed in-DLP project is tier `ok` (green border) but must show amber).
- `.finance-bar`, `.finance-bar.state-amber/.state-red/.state-green` (Phase 102 DLP bar) — leave intact

LANDMINE — the blueprint (`project-portfolio-view.md`) uses CSS variables `var(--blue)`,
`var(--amber)`, `var(--purple)`, `var(--green)`, `var(--slate-300)`. THESE DO NOT EXIST in
this codebase (confirmed: zero matches in main.css). Use the literal hex palette from
CLAUDE.md instead:
- blue #1a73e8 (dark #1557b0), green/success #059669, amber/warning #f59e0b, danger/red #ef4444
- borders #e5e7eb / #e2e8f0, text #1e293b / #475569 / #64748b, muted bg #f8fafc
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the Phase 103 portfolio-redesign CSS block</name>
  <read_first>
    - styles/views.css lines 5013-5040 (the Phase 102 DLP block + its `/* ===== End Phase 102 — DLP & Retention ===== */` terminator — append AFTER this line)
    - styles/views.css lines 3597-3720 (existing `.project-scorecard*` rules — match border-radius / shadow conventions)
    - .claude/skills/spike-findings-pr-po/references/project-portfolio-view.md (DOM shapes the classes wrap: `.vm-toggle`/`.vm-btn`, `#pdb-feed`/`#pdb-browse`, feed sections, `STAGE_GROUPS`, stage-aware finance `.fin-pre`/`.fin-ready`/`.fin-active`/`.fin-done`)
  </read_first>
  <action>
    Append ONE new block to the END of styles/views.css (after line 5040). Wrap it in
    `/* ===== Phase 103 — Portfolio Table Redesign (Attention Feed + Browse All) ===== */`
    … `/* ===== End Phase 103 — Portfolio Table Redesign ===== */`.

    Define exactly these class groups (use the literal hex palette — NO `var(--*)`):

    1. View-mode toggle:
       - `.vm-toggle` — flex row, gap 0; inline-flex container with border 1px solid #e2e8f0, border-radius 8px, overflow hidden, margin-bottom 1rem.
       - `.vm-btn` — padding 8px 16px, background #fff, color #475569, border none, cursor pointer, font-weight 600, font-size 0.875rem, white-space nowrap.
       - `.vm-btn:not(:last-child)` — border-right 1px solid #e2e8f0.
       - `.vm-btn.vm-on` — background #1a73e8, color #fff.

    2. Mode containers:
       - `.pdb-mode` — base wrapper (the JS sets inline `display`); no display rule here (JS owns it). Add `margin-top: 0.5rem;` only.

    3. Priority Feed sections (3 urgency tiers):
       - `.feed-section` — margin-bottom 1.5rem.
       - `.feed-section-header` — flex, align-items center, gap 8px, font-weight 700, font-size 0.95rem, padding 8px 4px, border-bottom 2px solid (overridden per-tier below).
       - `.feed-section.tier-urgent .feed-section-header` — color #991b1b, border-bottom-color #ef4444.
       - `.feed-section.tier-watch .feed-section-header` — color #92400e, border-bottom-color #f59e0b.
       - `.feed-section.tier-ok .feed-section-header` — color #166534, border-bottom-color #059669.
       - `.feed-section-count` — font-size 0.8rem, font-weight 600, color #64748b, background #f1f5f9, border-radius 10px, padding 2px 8px.
       - `.feed-empty` — color #94a3b8, font-style italic, font-size 0.85rem, padding 8px 4px.

    4. Feed rows (card-like list rows, NOT a table):
       - `.feed-row` — display flex, align-items center, gap 12px, padding 10px 12px, border 1px solid #e5e7eb, border-radius 8px, margin-bottom 8px, cursor pointer, background #fff, transition background .15s.
       - `.feed-row:hover` — background #f8fafc.
       - `.feed-row.tier-urgent` — border-left 4px solid #ef4444.
       - `.feed-row.tier-watch` — border-left 4px solid #f59e0b.
       - `.feed-row.tier-ok` — border-left 4px solid #059669.
       - `.feed-row-main` — flex 1, min-width 0 (allow truncation).
       - `.feed-row-title` — font-weight 600, color #1e293b.
       - `.feed-row-sub` — font-size 0.8rem, color #64748b, margin-top 2px.
       - `.feed-row-signal` — font-size 0.8rem, font-weight 600; `.feed-row-signal.tier-urgent` color #991b1b; `.tier-watch` color #92400e; `.tier-ok` color #166534.
       - `.feed-row-hint` — font-size 0.72rem, color #94a3b8, margin-top 1px.
       - `.feed-row-fin` — flex-shrink 0, min-width 140px, text-align right.

    5. Stage-aware finance cells (used in BOTH feed rows and browse rows):
       - `.fin-pre` — text-align right; `.fin-pre-amount` color #475569, font-weight 600; `.fin-pre-label` font-size 0.7rem, color #94a3b8, text-transform uppercase, letter-spacing 0.03em.
       - `.fin-ready` — text-align right; `.fin-ready-amount` color #1e293b, font-weight 700; `.fin-ready-label` font-size 0.7rem, color #f59e0b.
       - `.fin-active` — text-align right, min-width 140px; `.fin-active-top` flex justify-content space-between gap 8px font-size 0.8rem color #1e293b font-weight 600; `.mini-bar` height 6px background #e5e7eb border-radius 3px overflow hidden margin 3px 0; `.mini-fill` height 100% border-radius 3px; `.mini-fill.fill-blue` background #1a73e8; `.mini-fill.fill-orange` background #f59e0b; `.fin-active-sub` font-size 0.7rem color #64748b.
       - `.fin-done` — text-align right; `.fin-done-amount` color #1e293b font-weight 700; `.fin-done-label` font-size 0.7rem color #059669 font-weight 600.
       - `.fin-loss` — text-align right; `.fin-loss-label` font-size 0.75rem color #94a3b8 font-style italic.

    6. Browse All stage groups (collapsible):
       - `.stage-group` — margin-bottom 1rem, border 1px solid #e5e7eb, border-radius 8px, overflow hidden.
       - `.stage-group-header` — display flex, align-items center, gap 10px, padding 10px 14px, background #f8fafc, cursor pointer, font-weight 700, color #1e293b, user-select none.
       - `.stage-group-header:hover` — background #f1f5f9.
       - `.stage-group-chevron` — transition transform .2s; `.stage-group.collapsed .stage-group-chevron` — transform rotate(-90deg).
       - `.stage-group-count` — margin-left auto, font-size 0.8rem, font-weight 600, color #64748b, background #fff, border 1px solid #e5e7eb, border-radius 10px, padding 2px 10px.
       - `.stage-group-color` — width 4px, align-self stretch (a thin colored spine); set per-group color via inline style in JS (do not hardcode here).
       - `.stage-group-body` — padding 8px 12px; `.stage-group.collapsed .stage-group-body` — display none.
       - `.stage-group-empty` — color #94a3b8, font-style italic, font-size 0.85rem, padding 6px 0.

    7. DLP left-accent overrides for div rows (Phase 102 reuse — REQUIRED so the DLP accent is
       not lost on the new `<div class="feed-row …">` rows; the shipped `tr.dlp-*` selectors
       only match table rows). These MUST be emitted AFTER the `.feed-row.tier-*` rules above so
       they win the equal-specificity tie (a Completed in-DLP project is tier `ok` → green
       border, but must display amber):
       - `.feed-row.dlp-amber` — border-left-color #f59e0b.
       - `.feed-row.dlp-red` — border-left-color #ef4444.
       - `.feed-row.dlp-green` — border-left-color #059669.
       (The JS plans add `dlp-amber/red/green` to the row's class list from `getDlpState()`,
       exactly as the Phase 102 flat table did — same class names, new selector targets.)

    Reuse `.feed-row` (and its `.fin-*` children) for the rows inside `.stage-group-body` so
    browse rows and feed rows look identical — do NOT define a second row style. The
    `.feed-row.dlp-*` overrides therefore cover browse rows too (same `.feed-row` class).

    Mobile (inside an existing or new `@media (max-width: 768px)` rule appended in this block):
       - `.feed-row` — flex-direction column, align-items stretch.
       - `.feed-row-fin` — text-align left, min-width 0.
       - `.vm-toggle` — width 100%; `.vm-btn` — flex 1, text-align center.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/dev/projects/pr-po" && node -e "const c=require('fs').readFileSync('styles/views.css','utf8'); const need=['.vm-toggle','.vm-btn','.vm-on','.pdb-mode','.feed-section','.feed-section.tier-urgent','.feed-row','.feed-row.tier-urgent','.feed-row.dlp-amber','.feed-row.dlp-red','.feed-row.dlp-green','.fin-pre','.fin-ready','.fin-active','.mini-fill.fill-blue','.fin-done','.fin-loss','.stage-group-header','.stage-group-chevron','.stage-group-count','.stage-group-body','Phase 103']; const miss=need.filter(s=>!c.includes(s)); if(miss.length){console.error('MISSING:',miss);process.exit(1);} const blk=c.split('Phase 103 — Portfolio Table Redesign')[1]||''; if(/var\(--(blue|amber|purple|green|slate-300|red)\)/.test(blk)){console.error('FORBIDDEN css var in Phase 103 block');process.exit(1);} const iTier=blk.indexOf('.feed-row.tier-urgent'); const iDlp=blk.indexOf('.feed-row.dlp-amber'); if(iDlp<0||iTier<0||iDlp<iTier){console.error('.feed-row.dlp-* must appear AFTER .feed-row.tier-* to win specificity tie');process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - styles/views.css contains the literal string `Phase 103 — Portfolio Table Redesign`
    - styles/views.css contains ALL of: `.vm-toggle`, `.vm-btn`, `.vm-on`, `.pdb-mode`, `.feed-section`, `.feed-section.tier-urgent`, `.feed-row`, `.feed-row.tier-urgent`, `.feed-row.dlp-amber`, `.feed-row.dlp-red`, `.feed-row.dlp-green`, `.fin-pre`, `.fin-ready`, `.fin-active`, `.mini-fill.fill-blue`, `.fin-done`, `.fin-loss`, `.stage-group-header`, `.stage-group-chevron`, `.stage-group-count`, `.stage-group-body`
    - `.feed-row.dlp-amber` appears AFTER `.feed-row.tier-urgent` in the block (so DLP accent wins the equal-specificity tie for in-DLP rows that fall in the On Track tier)
    - The Phase 103 block contains ZERO occurrences of `var(--blue)`, `var(--amber)`, `var(--purple)`, `var(--green)`, `var(--slate-300)`, `var(--red)`
    - The verify command above exits 0
    - The pre-existing `.project-scorecard*`, `tr.dlp-amber/red/green`, `.portfolio-dlp-tag`, `.finance-bar*` rules are unchanged (block is appended, nothing edited above line 5040)
  </acceptance_criteria>
  <done>Single Phase 103 CSS block appended; all required classes present; no forbidden CSS variables; verify exits 0.</done>
</task>

</tasks>

<verification>
- `node -e` palette/marker check (Task 1 verify) exits 0
- Visual styling is browser-confirmed downstream in Plan 02/03/04 UATs (no UAT in this CSS-only plan)
</verification>

<success_criteria>
All Phase 103 portfolio classes exist in styles/views.css using the literal hex palette, with
no clobbering of Phase 92/102 classes. Enables Plans 02/03/04 to render against known names.
</success_criteria>

<output>
After completion, create `.planning/phases/103-portfolio-table-redesign/103-01-SUMMARY.md`.
</output>
