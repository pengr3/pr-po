---
phase: 102-dlp-retention-management
plan: 04
type: execute
wave: 4
depends_on: ["102-03"]
files_modified: [app/views/project-detail.js, firestore.rules]
autonomous: false
requirements: [SC-8, SC-9]
user_setup:
  - service: firebase
    why: "Deploy updated Firestore rules to the dev project so the new DLP/retention fields + retention_released_at write gate take effect"
    dashboard_config:
      - task: "Run firebase deploy --only firestore:rules (dev project)"
        location: "Local CLI — firebase deploy --only firestore:rules"
must_haves:
  truths:
    - "A Finance user sees a Record Release button on in-dlp and expired finance-bar states; clicking it writes retention_released_at (now) to the project doc"
    - "Non-Finance users do not get a working Record Release path (gated in code and in firestore.rules)"
    - "After release the finance bar flips to green/Released via the existing project onSnapshot (no new listener)"
    - "firestore.rules permits the new projects DLP fields to be written by project-editing roles and gates retention_released_at to Finance"
  artifacts:
    - path: "app/views/project-detail.js"
      provides: "recordRetentionRelease window function + Record Release button injected into the expired/in-dlp DLP strip"
      contains: "recordRetentionRelease"
    - path: "firestore.rules"
      provides: "projects update rule extended for DLP fields + retention_released_at Finance gate"
      contains: "retention_released_at"
  key_links:
    - from: "recordRetentionRelease"
      to: "projects/{docId} retention_released_at"
      via: "updateDoc direct write (Finance-gated)"
      pattern: "retention_released_at"
---

<objective>
Add the Finance-only "Record Release" action (Spike 036) — a direct write of `retention_released_at` on the project doc — and update `firestore.rules` so the new DLP/retention fields are writable by project-editing roles while `retention_released_at` is gated to Finance. Deploy rules to dev.

Purpose: Closes the DLP loop — Finance records the retention release, flipping the project to the green/Released state. No coupling into the finance.js collectibles/COLL flow (D-03).
Output: `recordRetentionRelease` + the Record Release button in the DLP strip + extended projects rule + dev rules deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-dlp-retention-management/102-CONTEXT.md
@.planning/spikes/036-dlp-states-finance-bar/spike.html

<interfaces>
<!-- Existing contracts. -->

Plan 03 left a commented placeholder slot for the Record Release button inside the expired DLP strip
  ( "<!-- Plan 04: Record Release button injects here -->" ) in renderDlpFinanceBar.
getDlpState(project) -> 'active'|'in-dlp'|'expired'|'released' (from Plan 03).

Finance role detection: window.getCurrentUser()?.role === 'finance'.
Helpers: updateDoc, doc, db, serverTimestamp, showToast, escapeHTML, addProjectAuditEntry, _addActivityEntry.

The project listener (onSnapshot on projects/{id}) is already active and re-renders on any field change
  — including retention_released_at — so no new listener is needed (D-21, Spike 036 key finding).

firestore.rules projects update rule (firestore.rules ~200-236): two-branch allow update:
  branch 1 = hasRole(['super_admin','operations_admin','finance']) with project_code/client lock;
  branch 2 = operations_user assigned + affectedKeys().hasOnly([...lifecycle fields incl. project_completed_at]).
  The Phase 100 operations_user affectedKeys list is the one to extend with DLP fields.

init() window registration block ~3266-3283; destroy() teardown ~506-521.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: recordRetentionRelease + Record Release button + window registration</name>
  <read_first>
    - app/views/project-detail.js renderDlpFinanceBar (added in Plan 03) — locate the "Plan 04: Record Release button injects here" placeholder in the expired strip
    - app/views/project-detail.js ~3408-3419 (lcMarkProjectComplete — the updateDoc + audit + activity pattern to mirror)
    - app/views/project-detail.js ~3266-3283 (init registration) + ~506-521 (destroy teardown)
    - .planning/spikes/036-dlp-states-finance-bar/spike.html lines 90-99 (.release-btn) + 461-464 (releaseRetention)
    - .planning/phases/102-dlp-retention-management/102-CONTEXT.md D-03, D-15, D-21 (direct write; Finance-only; in-dlp + expired states; flips to green; no listener change)
  </read_first>
  <action>
    Add `window.recordRetentionRelease = async function(projectId)`: guard `currentProject?.id === projectId`; read `const cu = window.getCurrentUser?.()`; require `cu?.role === 'finance'` (showToast "Only Finance can record a retention release." + return otherwise — code-side gate complementing the rules gate); confirm via a simple `confirm()`/showToast flow that release is intended; `await updateDoc(doc(db,'projects',projectId), { retention_released_at: new Date().toISOString().slice(0,19).replace('T',' '), updated_at: serverTimestamp() })`; then `addProjectAuditEntry(projectId,'RETENTION_RELEASED', cu?.uid, cu?.full_name, 'retention_released_at set')` and `_addActivityEntry(projectId, { type:'system', is_system:true, text: \`Retention released by ${cu?.full_name || 'Unknown'}\` })`. Wrap in try/catch with console.error + showToast on failure. The existing project onSnapshot re-renders to green — no manual re-render needed (D-21).

    In `renderDlpFinanceBar` (Plan 03), replace the Plan-04 placeholder so the Record Release button (`.release-btn`, "Record Release", onclick `window.recordRetentionRelease('${escapeHTML(project.id)}')`) renders inside the DLP strip for BOTH `in-dlp` and `expired` states (D-21: in-dlp + expired), but ONLY when `window.getCurrentUser?.()?.role === 'finance'`. For non-Finance users render nothing in the slot.

    Register `window.recordRetentionRelease` in init() (comment `// Phase 102 — Record Release`) and `delete window.recordRetentionRelease` in destroy().
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js && node -e "const c=require('fs').readFileSync('app/views/project-detail.js','utf8'); if(!c.includes('recordRetentionRelease')){console.error('no fn');process.exit(1);} if(!(c.includes('window.recordRetentionRelease =')||c.includes('window.recordRetentionRelease='))){console.error('not registered');process.exit(1);} if(!c.includes('delete window.recordRetentionRelease')){console.error('not deleted');process.exit(1);} if(!c.includes(\"role === 'finance'\")){console.error('no finance gate');process.exit(1);} if(c.includes('Plan 04: Record Release button injects here')){console.error('placeholder still present');process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` exits 0.
    - `recordRetentionRelease` registered in init() AND deleted in destroy() (SC-9 symmetry).
    - Function gated on `role === 'finance'` in code; writes `retention_released_at` via direct updateDoc; no finance.js/COLL coupling (no reference to collectibles creation).
    - Record Release button renders for in-dlp + expired states, Finance-only; Plan-03 placeholder comment is gone.
  </acceptance_criteria>
  <done>Finance-only Record Release direct-writes retention_released_at; project flips green via existing onSnapshot.</done>
</task>

<task type="auto">
  <name>Task 2: Extend firestore.rules for DLP fields + retention_released_at Finance gate</name>
  <read_first>
    - firestore.rules ~200-236 (projects update rule, both branches + the Phase 100 operations_user affectedKeys list)
    - .planning/phases/102-dlp-retention-management/102-CONTEXT.md D-25 (allow new projects fields for project-editing roles; gate retention_released_at to Finance; dev deploy only)
  </read_first>
  <action>
    In the projects `allow update` rule:
    - Branch 1 (admins + finance with project_code lock) already permits arbitrary field writes for those roles, so the DLP capture write from lcMarkProjectComplete (done by admins/assigned ops via Gate 4) is covered by branch 2 for operations_user — therefore EXTEND the operations_user branch-2 `affectedKeys().hasOnly([...])` allow-list to additionally permit: `'dlp_months'`, `'dlp_start_date'`, `'dlp_expires_at'`, `'retention_percentage'`, `'retention_amount'`, `'retention_released_at'`. (This lets an assigned ops user who marks completion also write DLP fields in the same update.)
    - Add an explicit Finance gate for `retention_released_at`: add a THIRD branch to the `allow update` disjunction — `hasRole(['finance']) && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['retention_released_at','updated_at'])` — so Finance can record release even on a code-locked project without touching other fields. (Finance is already in branch 1, but branch 1 requires the project_code/client lock to be satisfied; this dedicated branch makes the Finance release write unambiguous and matches the Phase 87.2 affectedKeys field-mask pattern.)
    Keep all existing branches intact; only ADD keys to the ops allow-list and ADD the Finance release branch. Preserve rule-block brace balance.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('firestore.rules','utf8'); for(const s of ['dlp_months','dlp_start_date','dlp_expires_at','retention_percentage','retention_amount','retention_released_at']){ if(!c.includes(s)){console.error('MISSING',s);process.exit(1);} } if(!c.includes(\"hasRole(['finance'])\")){console.error('no finance branch');process.exit(1);} const o=(c.match(/\{/g)||[]).length,cl=(c.match(/\}/g)||[]).length; if(o!==cl){console.error('BRACE IMBALANCE',o,cl);process.exit(1);} console.log('OK braces',o);"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 DLP field names present in firestore.rules.
    - A dedicated Finance branch gates `retention_released_at` via affectedKeys().hasOnly([...]).
    - firestore.rules brace count balanced.
    - operations_user affectedKeys allow-list extended (existing lifecycle keys preserved).
  </acceptance_criteria>
  <done>Rules permit DLP field writes by project-editing roles; retention_released_at gated to Finance; braces balanced.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Updated firestore.rules (DLP fields + Finance retention_released_at gate) ready to deploy.</what-built>
  <how-to-verify>
    Run `firebase deploy --only firestore:rules` (dev project). Confirm it compiles and deploys with no errors. (Prod deploy is deferred per the standing v3.3→main merge debt, D-25 / Deferred Ideas — do NOT deploy to prod.)
  </how-to-verify>
  <resume-signal>Type "deployed" once dev rules deploy succeeds, or paste any compile error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Finance-only Record Release action (Spike 036) — direct write of retention_released_at flipping the project to green/Released, plus the firestore.rules gate.</what-built>
  <how-to-verify>
    1. After dev rules deploy: as a FINANCE user, open a project in the in-dlp (amber) or expired (red) state. Confirm a "Record Release" button shows inside the DLP strip.
    2. Click it → confirm → toast success; the finance bar flips to GREEN / "Fully Collected" and the retention tranche row shows "Released" — without a manual reload (existing onSnapshot).
    3. As a NON-Finance user (e.g. operations_user), confirm NO Record Release button appears; if you force the call, the write is rejected by rules (permission error in console).
    4. Firestore: project doc now has retention_released_at set; getDlpState returns 'released'.
    5. Zero console errors for the Finance path.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → Firestore | Any authenticated user can attempt writes to projects/{id}; rules are the enforcement point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Elevation of Privilege | retention_released_at write | mitigate | Dedicated Finance-only rules branch (affectedKeys hasOnly retention_released_at/updated_at) + code-side `role==='finance'` guard in recordRetentionRelease |
| T-102-02 | Tampering | DLP field writes by unassigned users | mitigate | DLP keys only added to the operations_user affectedKeys allow-list for ASSIGNED users; admins/finance covered by branch 1 |
| T-102-03 | Repudiation | retention release with no audit | mitigate | addProjectAuditEntry('RETENTION_RELEASED') + _addActivityEntry system entry on release |
</threat_model>

<verification>
- `node --check app/views/project-detail.js` exits 0; firestore.rules braces balanced.
- recordRetentionRelease Finance-gated + registered/deleted symmetric.
- Dev rules deploy succeeds (checkpoint).
- Browser UAT confirms Finance release flips to green; non-Finance blocked.
</verification>

<success_criteria>
SC-8 (Finance-only Record Release direct write, role-gated in code + rules, flips to green) and the firestore.rules portion of SC-9 met.
</success_criteria>

<output>
Create `.planning/phases/102-dlp-retention-management/102-04-SUMMARY.md` when done.
</output>
