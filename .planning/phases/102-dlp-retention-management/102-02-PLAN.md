---
phase: 102-dlp-retention-management
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified: [app/views/project-detail.js]
autonomous: true
requirements: [SC-1, SC-2]
must_haves:
  truths:
    - "On an On-going project, the Financial card shows an Edit Tranches control; clicking it expands an inline editor"
    - "Empty-tranche projects show a Set Up Tranches CTA instead of the read-only list"
    - "User can add/remove rows, type label + percentage, and the live total bar turns green only at exactly 100%"
    - "A Ret? toggle marks exactly one tranche; toggling another clears the prior one"
    - "Save writes collection_tranches (array of {label, percentage, is_retention}) to projects/{docId}; no DLP fields written at editor time"
  artifacts:
    - path: "app/views/project-detail.js"
      provides: "Inline tranche editor: renderTrancheEditor, toggleTrancheEditor, addEditorTrancheRow, removeEditorTrancheRow, toggleTrancheRetention, recalcTrancheTotal, saveTrancheEditor, cancelTrancheEditor + module state editorTranches/trancheEditorOpen"
      contains: "saveTrancheEditor"
  key_links:
    - from: "saveTrancheEditor"
      to: "projects/{docId} collection_tranches"
      via: "updateDoc"
      pattern: "updateDoc.*collection_tranches"
    - from: "Financial card render (~line 720-746)"
      to: "renderTrancheEditor + Edit Tranches button"
      via: "injected HTML in the Collectibles/tranche section"
      pattern: "toggleTrancheEditor"
---

<objective>
Add the inline collection-tranche editor to `project-detail.js` Financial card (Spike 035). On-going projects can add/edit/label/percentage tranches and flag exactly one retention tranche, writing `collection_tranches` back to the project doc. DLP fields are NOT captured here (deferred to Plan 03's completion gate per the Spike 034 hybrid).

Purpose: Closes the "tranches locked at creation" gap — today tranches are only settable in the projects.js edit modal.
Output: New editor functions + module state + the Edit Tranches / Set Up Tranches control wired into the Financial card; symmetric init/destroy registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-dlp-retention-management/102-CONTEXT.md
@.planning/spikes/035-tranche-editor-in-detail/spike.html

<interfaces>
<!-- Existing project-detail.js contracts the editor uses — do not re-explore the codebase. -->

Module-level project state already present:
  currentProject            // { id, collection_tranches, contract_cost, project_status, ... }
  currentProject.id         // Firestore doc id for projects/{id}

Firestore helpers already imported at top of project-detail.js:
  updateDoc, doc, db, serverTimestamp

Shared UI helpers already in scope:
  escapeHTML(str), formatCurrency(num), showToast(msg, type)

Existing tranche read (the spot the editor replaces/augments), project-detail.js ~838:
  function renderTrancheLifecycleRows() { const tranches = Array.isArray(currentProject?.collection_tranches) ? ... }

Existing tranche shape on the project doc (Phase 85): { label: string, percentage: number, is_retention?: boolean }
  NOTE: the field is `percentage` (NOT `pct` — the spike used `pct` as a local name only).

Permission gate already defined ~2424:
  _canAdvanceProjectStatus(project, currentUser, targetStatus) -> boolean
  window.getCurrentUser() -> { uid, role, full_name, ... }

init() window-function registration block: ~line 3266-3283.
destroy() window-function teardown block: ~line 506-521.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Editor state + render/compute/save functions</name>
  <read_first>
    - app/views/project-detail.js lines 1-60 (imports — confirm updateDoc/doc/db/serverTimestamp/escapeHTML/formatCurrency/showToast in scope)
    - app/views/project-detail.js ~833-858 (renderTrancheLifecycleRows — the existing read-only tranche surface)
    - .planning/spikes/035-tranche-editor-in-detail/spike.html lines 336-558 (renderEditor, toggleRetention, recalcTotal, addEditorRow, removeEditorRow, saveTranches — the JS logic to port)
    - .planning/phases/102-dlp-retention-management/102-CONTEXT.md D-04..D-10 (editor in Financial card; inline expand; label+%; total=100 + labels filled; one retention only; Save writes array; NO DLP fields at editor time)
  </read_first>
  <action>
    Add module-level state near the other project-detail module vars: `let editorTranches = [];` and `let trancheEditorOpen = false;`.

    Add these functions (port logic from spike.html, adapting field name `pct`→`percentage` and reading the real `currentProject`):

    - `renderTrancheEditor()` → returns the editor HTML string: the `.tranche-editor` wrapper (with `.visible` class only when `trancheEditorOpen`), `.editor-header`, a `.editor-list` of `.editor-row` rows built from `editorTranches`, the `.total-row` + `.total-progress` total bar, `+ Add Tranche` (`.add-btn`) and the `.editor-actions` Cancel/Save buttons. Each row has a text input bound to label, a number input (width via `.editor-row input[type=number]`) bound to percentage with `oninput="window.recalcTrancheTotal()"`, a `.ret-toggle` (`.on` when `is_retention`) calling `window.toggleTrancheRetention(i)`, and a `.remove-btn` calling `window.removeEditorTrancheRow(i)` (disabled when only one row). Do NOT render any DLP fields/`.dlp-expand` block — DLP is deferred to the completion gate (D-10). Escape all label values with escapeHTML for display where injected as attribute/text.
    - `renderTrancheDisplay()` → returns the read-only display: when `currentProject.collection_tranches` is empty, the `.setup-cta` block with a "⚙ Set Up Tranches" button calling `window.toggleTrancheEditor()`; otherwise the `.tranche-display` list of `.tranche-display-row` with `.td-name`, optional `.ret-tag` (◆ Retention) when `is_retention`, `.td-amount` (formatCurrency of contract_cost × percentage/100), `.td-pct`. (This read-only surface complements — does not delete — the existing `renderTrancheLifecycleRows()` lifecycle rows.)
    - `toggleTrancheEditor()` → flips `trancheEditorOpen`; on open, copies `currentProject.collection_tranches` (each read with `|| []` / fields `|| ''` / `|| 0`) into `editorTranches`; re-renders the editor host in place (see Task 2 host). Guard with `_canAdvanceProjectStatus(currentProject, window.getCurrentUser?.(), 'On-going')`-style permission: only allow opening if the user can edit (admins or assigned operations_user); otherwise showToast permission error and return.
    - `addEditorTrancheRow()` → pushes `{ label:'', percentage:0, is_retention:false }`, re-renders editor host.
    - `removeEditorTrancheRow(i)` → splices index i, re-renders editor host.
    - `toggleTrancheRetention(i)` → sets `is_retention` true on row i only if it was off, and false on ALL other rows (one-retention invariant D-09); re-renders editor host.
    - `recalcTrancheTotal()` → sums percentages, updates `#trancheTotalVal` text + class (`total-ok` at exactly 100, else `total-err`) and `#trancheTotalBar` width/background (green #059669 at 100, red #ef4444 over 100, blue #1a73e8 under).
    - `saveTrancheEditor()` → validates total === 100 (showToast error otherwise), all labels non-empty (showToast error otherwise); builds `collection_tranches` as `editorTranches.map(t => ({ label: t.label.trim(), percentage: +t.percentage, is_retention: !!t.is_retention }))`; `await updateDoc(doc(db,'projects',currentProject.id), { collection_tranches: finalTranches, updated_at: serverTimestamp() })`; on success set `trancheEditorOpen=false`, showToast success. Do NOT write any DLP field here (D-10). Wrap in try/catch with console.error + showToast on failure.
    - `cancelTrancheEditor()` → sets `trancheEditorOpen=false`, discards `editorTranches`, re-renders editor host.

    Read every project field defensively (`currentProject.collection_tranches || []`, `parseFloat(currentProject.contract_cost) || 0`) per D-22.
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/project-detail.js` exits 0.
    - Grep confirms all 8 function names present: renderTrancheEditor, renderTrancheDisplay, toggleTrancheEditor, addEditorTrancheRow, removeEditorTrancheRow, toggleTrancheRetention, recalcTrancheTotal, saveTrancheEditor, cancelTrancheEditor.
    - `saveTrancheEditor` body contains `updateDoc` writing `collection_tranches` and does NOT reference `dlp_months`, `dlp_start_date`, or `retention_percentage` (grep: those three strings must NOT appear inside this plan's diff).
    - `toggleTrancheRetention` sets is_retention false on all rows except index i (one-retention invariant).
  </acceptance_criteria>
  <done>Editor logic compiles, writes collection_tranches only, enforces one-retention + 100% + labels-filled.</done>
</task>

<task type="auto">
  <name>Task 2: Wire editor host into Financial card + register window functions</name>
  <read_first>
    - app/views/project-detail.js ~718-748 (the Collectibles group + Initiate Billing footer + `${renderTrancheLifecycleRows()}` — the Financial card insertion point)
    - app/views/project-detail.js ~3266-3283 (init window-function registration block — pattern: `window.fnName = fnName;`)
    - app/views/project-detail.js ~506-521 (destroy window-function teardown — pattern: `delete window.fnName;`)
    - app/views/project-detail.js ~430-453 (destroy state-reset block — where to reset editorTranches/trancheEditorOpen)
  </read_first>
  <action>
    In the Financial card render (~720-746), inside the Collectibles group, add a tranche section ABOVE `${renderTrancheLifecycleRows()}`: a `.tranche-header` with a `Collection Tranches` label and an `.edit-tranches-btn` ("⚙ Edit Tranches", `.active` when `trancheEditorOpen`) calling `window.toggleTrancheEditor()` — shown only when `showEditControls` is true (reuse the existing `showEditControls` local in render). Below it add a host container `<div id="trancheEditorHost">` whose contents are `renderTrancheDisplay() + renderTrancheEditor()`. The editor expands inline (CSS `.tranche-editor.visible`) — no modal.

    Add a small in-place re-render helper `renderTrancheEditorHost()` that sets `document.getElementById('trancheEditorHost').innerHTML = renderTrancheDisplay() + renderTrancheEditor()` then calls `recalcTrancheTotal()` when the editor is open. Call it from toggleTrancheEditor / add / remove / toggleRetention / cancel / save (Task 1 referenced "re-render editor host" — this is that function).

    Register all 7 window functions in init() (after the Phase 101 block, with a `// Phase 102 — inline tranche editor` comment): `window.toggleTrancheEditor`, `window.addEditorTrancheRow`, `window.removeEditorTrancheRow`, `window.toggleTrancheRetention`, `window.recalcTrancheTotal`, `window.saveTrancheEditor`, `window.cancelTrancheEditor`.

    Delete the same 7 in destroy() (with the same comment), and in the destroy state-reset block set `editorTranches = []; trancheEditorOpen = false;`.
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js && node -e "const c=require('fs').readFileSync('app/views/project-detail.js','utf8'); const fns=['toggleTrancheEditor','addEditorTrancheRow','removeEditorTrancheRow','toggleTrancheRetention','recalcTrancheTotal','saveTrancheEditor','cancelTrancheEditor']; const reg=fns.filter(f=>c.includes('window.'+f+' =')||c.includes('window.'+f+'=')); const del=fns.filter(f=>c.includes('delete window.'+f)); if(reg.length!==7||del.length!==7){console.error('register',reg.length,'delete',del.length);process.exit(1);} if(!c.includes('trancheEditorHost')){console.error('no host');process.exit(1);} console.log('OK 7/7 symmetric + host');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/project-detail.js` exits 0.
    - All 7 window functions registered in init() AND deleted in destroy() (verify command asserts 7/7 each — SC-9 symmetry).
    - `#trancheEditorHost` container present in the Financial card render.
    - destroy() resets `editorTranches` and `trancheEditorOpen`.
  </acceptance_criteria>
  <done>Edit Tranches / Set Up Tranches control renders in the Financial card; editor expands/saves; window functions symmetric.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Inline tranche editor in the project-detail Financial card (Spike 035): Edit Tranches / Set Up Tranches control, add/remove rows, label + %, live 100% total bar (green at 100), Ret? toggle (one only), Save writes collection_tranches.</what-built>
  <how-to-verify>
    1. `python -m http.server 8000`, open an On-going project at `#/projects/detail/{code}`.
    2. Financial card: a project with NO tranches shows the "⚙ Set Up Tranches" CTA; a project WITH tranches shows the read-only list + "⚙ Edit Tranches" button.
    3. Click the control — the inline editor expands (not a modal). Add a row, type labels + percentages. Confirm the total bar is RED until the sum is exactly 100%, then turns GREEN.
    4. Click "Ret?" on one row → it shows "◆ Ret." and the row tints amber; click "Ret?" on a different row → the first clears (only one retention at a time).
    5. With total = 100% and all labels filled, click Save → toast confirms; reload the page → tranches persist (written to projects/{docId}). Confirm NO DLP fields were asked for at editor time.
    6. Try saving with total ≠ 100% or a blank label → blocked with an error toast.
    7. DevTools console: zero errors on open/save/reload.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `node --check app/views/project-detail.js` exits 0.
- 7/7 window functions register↔teardown symmetric.
- collection_tranches write present; no DLP field write in this plan.
- Browser UAT (checkpoint) confirms editor behavior.
</verification>

<success_criteria>
SC-1 (inline editor + 100% validation + Set Up CTA + Save→projects/{docId}) and SC-2 (Ret? one-only toggle, is_retention persisted, no DLP at editor time) met.
</success_criteria>

<output>
Create `.planning/phases/102-dlp-retention-management/102-02-SUMMARY.md` when done.
</output>
