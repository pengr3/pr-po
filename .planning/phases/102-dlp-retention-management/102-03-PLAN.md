---
phase: 102-dlp-retention-management
plan: 03
type: execute
wave: 3
depends_on: ["102-02"]
files_modified: [app/views/project-detail.js]
autonomous: true
requirements: [SC-3, SC-4, SC-5, SC-6]
must_haves:
  truths:
    - "Marking a project Completed with a retention tranche prompts for retention %, DLP months, DLP start date and writes dlp_expires_at + retention_amount to the project doc"
    - "Marking a project Completed WITHOUT a retention tranche proceeds exactly as today (no DLP write)"
    - "getDlpState(project) returns active | in-dlp | expired | released from dlp_months, project_status, dlp_expires_at, retention_released_at"
    - "The project-detail finance bar reflects the DLP state: blue (active), amber + DLP strip (in-dlp), red + Expired strip (expired), green 100% (released)"
    - "The retention tranche row shows an In DLP / OVERDUE / Released tag matching state; the info card shows DLP Period + DLP Expires"
  artifacts:
    - path: "app/views/project-detail.js"
      provides: "getDlpState, computeDlpFields helper, Gate-4 DLP fieldset, extended lcMarkProjectComplete write, renderDlpFinanceBar, DLP info-card fields, retention-row state tag"
      contains: "function getDlpState"
  key_links:
    - from: "lcMarkProjectComplete"
      to: "projects/{docId} dlp_months/dlp_start_date/dlp_expires_at/retention_percentage/retention_amount"
      via: "updateDoc, only when a retention tranche exists"
      pattern: "dlp_expires_at"
    - from: "getDlpState(project)"
      to: "finance bar wrapper + DLP strip + tranche tag + info card"
      via: "single render-time derivation"
      pattern: "getDlpState"
---

<objective>
Capture DLP fields at the Gate-4 "Mark Completed" flow and render the 4-state DLP display on project-detail (Spikes 034 hybrid + 036). Add the single `getDlpState()` derivation that drives the finance bar, retention-tranche row tag, and info-card DLP fields.

Purpose: DLP terms are only known at completion (hybrid: editor sets `is_retention`, gate captures the values). The 4-state display makes withheld retention visible end-to-end so it is never forgotten.
Output: `getDlpState` + a DLP fieldset in the Gate-4 panel + extended `lcMarkProjectComplete` write + DLP-aware finance bar + retention row tag + info-card DLP Period/Expires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-dlp-retention-management/102-CONTEXT.md
@.planning/spikes/036-dlp-states-finance-bar/spike.html
@.planning/spikes/034-dlp-entry-placement/README.md

<interfaces>
<!-- Existing project-detail.js contracts. Do not re-explore. -->

getDlpState contract (D-16, port verbatim, returns 'active'|'in-dlp'|'expired'|'released'):
  if (!project.dlp_months || project.project_status !== 'Completed') return 'active';
  if (project.retention_released_at) return 'released';
  if (Date.now() > new Date(project.dlp_expires_at).getTime()) return 'expired';
  return 'in-dlp';

Gate-4 panel render (the `if (status === 'On-going')` branch, project-detail.js ~2260-2277):
  builds the "Mark as Completed" button via buildLifecycleBody; uses buildAttachZone, _canAdvanceProjectStatus.
  The completed-state branch (~2278-2290) renders the comp-grid + "COC → Finance" note.

lcMarkProjectComplete (project-detail.js ~3408-3419): currently writes
  { project_status:'Completed', project_completed_at: now, updated_at: serverTimestamp() }
  then addProjectAuditEntry + _addActivityEntry. Read with currentProject.

Tranche read: currentProject.collection_tranches (array of {label, percentage, is_retention}).
Retention detection: const ret = (currentProject.collection_tranches||[]).find(t => t.is_retention);

Helpers in scope: escapeHTML, formatCurrency, showToast, updateDoc, doc, db, serverTimestamp,
  window.getCurrentUser(), _canAdvanceProjectStatus, addProjectAuditEntry, _addActivityEntry.

Info card render: project-detail.js ~629-663 (the Project Information grid of label/value cells).
Financial card finance area: project-detail.js ~665-748 (Budget/Payables/Collectibles groups + tranche section from Plan 02).

D-13 computed fields: dlp_expires_at = dlp_start_date + dlp_months (months);
  retention_amount = contract_cost × retention_percentage / 100.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getDlpState + computeDlpFields + Gate-4 DLP fieldset + extended lcMarkProjectComplete write</name>
  <read_first>
    - app/views/project-detail.js ~2260-2290 (Gate-4 On-going branch + Completed branch)
    - app/views/project-detail.js ~3408-3419 (lcMarkProjectComplete — the write to extend)
    - .planning/spikes/036-dlp-states-finance-bar/spike.html lines 44-52 (getDlpState verbatim) + 26-41 (data model)
    - .planning/spikes/034-dlp-entry-placement/README.md (hybrid: Ret? in editor, DLP at completion gate)
    - .planning/phases/102-dlp-retention-management/102-CONTEXT.md D-11..D-16, D-22 (project-level fields; gate captures retention_percentage/dlp_months/dlp_start_date; computed expires+amount; only when retention tranche exists; defensive || null)
  </read_first>
  <action>
    Add `function getDlpState(project)` exactly per the D-16 contract (verbatim from the interfaces block). Read all fields defensively; treat missing `dlp_months`/`dlp_expires_at` as 'active'.

    Add `function computeDlpFields(startDateStr, months, contractCost, retentionPct)` returning `{ dlp_expires_at, retention_amount }`: parse `startDateStr` (a `YYYY-MM-DD` string), add `months` via `new Date(...).setMonth(getMonth()+months)`, format `dlp_expires_at` back to `YYYY-MM-DD`; `retention_amount = Math.round(contractCost * retentionPct / 100)`.

    In the Gate-4 On-going branch (~2260-2277), insert a DLP fieldset ABOVE the "Mark as Completed" button, shown ONLY when a retention tranche exists (`(currentProject.collection_tranches||project.collection_tranches||[]).some(t=>t.is_retention)` — use the `project` param available in buildLifecycleBody). The fieldset has: a Retention % number input (`id="gateDlpRetPct"`, default the retention tranche's `percentage` or 10), a DLP Months select (`id="gateDlpMonths"`, options 3/6/12/18/24, default 12 per D-12), and a DLP Start Date input (`id="gateDlpStart"`, type=date, defaulting to today). Add a one-line note that DLP is required because a retention tranche exists. When no retention tranche exists, render nothing extra (completion proceeds as today, D-14).

    Extend `lcMarkProjectComplete(projectId)`: after computing `now`, detect the retention tranche. If one exists, read `gateDlpRetPct`/`gateDlpMonths`/`gateDlpStart` from the DOM (fall back to defaults 10/12/today if absent), compute `{dlp_expires_at, retention_amount}` via computeDlpFields using `parseFloat(currentProject.contract_cost)||0`, and include in the updateDoc payload: `dlp_months` (number), `dlp_start_date`, `dlp_expires_at`, `retention_percentage` (number), `retention_amount`, and `retention_released_at: null` (D-15). If NO retention tranche, keep the existing payload unchanged (no DLP keys). Keep `project_completed_at`, audit entry, and activity entry exactly as today. Wrap unchanged in the existing try/catch.
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js && node -e "const c=require('fs').readFileSync('app/views/project-detail.js','utf8'); for(const s of ['function getDlpState','function computeDlpFields','gateDlpMonths','gateDlpStart','gateDlpRetPct','dlp_expires_at','retention_amount','retention_released_at: null']){ if(!c.includes(s)){console.error('MISSING',s);process.exit(1);} } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` exits 0.
    - `getDlpState` body matches the D-16 four-branch contract (active/released/expired/in-dlp).
    - DLP fieldset (`gateDlpRetPct`, `gateDlpMonths`, `gateDlpStart`) only rendered when a retention tranche exists.
    - `lcMarkProjectComplete` writes the 6 DLP fields (incl. `retention_released_at: null`) ONLY in the retention branch; the no-retention path keeps the original 3-key payload.
  </acceptance_criteria>
  <done>DLP capture at Gate 4 + getDlpState + computed fields landed; no-retention completion unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: DLP-aware finance bar (4 states) + retention-row tag</name>
  <read_first>
    - app/views/project-detail.js ~718-748 (Collectibles group / finance area where the DLP bar renders)
    - app/views/project-detail.js ~838-858 (renderTrancheLifecycleRows — where the retention-row state tag attaches)
    - .planning/spikes/036-dlp-states-finance-bar/spike.html lines 217-251 (finance-bar markup) + 286-459 (STATES map + setState/renderCollRows — the per-state values + tags)
    - .planning/phases/102-dlp-retention-management/102-CONTEXT.md D-17, D-18, D-19
  </read_first>
  <action>
    Add `function renderDlpFinanceBar(project)` returning the finance-bar HTML for the current state via `getDlpState(project)`:
    - `active` → base `.finance-bar` (no state class), blue `.bar-seg.collected`, no `.dlp-strip`.
    - `in-dlp` → `.finance-bar.state-amber`, stacked `.bar-seg.collected` + amber `.bar-seg.retention`, `.dlp-strip.amber` with text "◑ In Defect Liability Period — retention held until DLP expires" and a right value "N days remaining" (compute N from `dlp_expires_at` − now in days; copy format per D-69 discretion).
    - `expired` → `.finance-bar.state-red`, red `.bar-seg.retention`, `.dlp-strip.red` "⚠ DLP period expired — retention release overdue" + right "Expired N days ago". (The Record Release button is added in Plan 04 — leave a clearly-commented placeholder slot inside the strip, e.g. `<!-- Plan 04: Record Release button injects here -->`, do NOT add the button now.)
    - `released` → `.finance-bar.state-green`, 100% collected fill, `.dlp-strip.green` "✓ Retention released — project fully collected" + right "Released {date}".
    Compute amounts from `parseFloat(project.contract_cost)||0`, `project.retention_amount||0`, `project.retention_percentage||0`; read every field defensively (D-22). Escape any interpolated strings.

    Render `renderDlpFinanceBar(currentProject)` in the Financial card (insert at the top of the finance area, above the Budget group, so the bar is the headline — matching Spike 036 where the finance bar leads the card).

    In `renderTrancheLifecycleRows()`, for the tranche where `is_retention` is true, append a DLP state tag (`.coll-tag` with `.tag-holding` "In DLP" for in-dlp, `.tag-overdue` "OVERDUE" for expired, `.tag-released` "Released" for released; nothing extra for active). Derive the state once via `getDlpState(currentProject)`. Keep the existing per-tranche lifecycle badge untouched — this DLP tag is additive.
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js && node -e "const c=require('fs').readFileSync('app/views/project-detail.js','utf8'); for(const s of ['function renderDlpFinanceBar','state-amber','state-red','state-green','dlp-strip','tag-holding','tag-overdue','tag-released','days remaining','Plan 04']){ if(!c.includes(s)){console.error('MISSING',s);process.exit(1);} } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` exits 0.
    - `renderDlpFinanceBar` emits all 4 state branches (state-amber/state-red/state-green + base) keyed on getDlpState.
    - Retention tranche row gets `tag-holding`/`tag-overdue`/`tag-released` per state; non-retention rows unchanged.
    - A commented Plan-04 placeholder slot for the Record Release button exists inside the expired strip (button NOT added here).
    - Every new field read defensively (`|| 0` / `|| null`).
  </acceptance_criteria>
  <done>Finance bar renders 4 DLP states; retention row carries the matching state tag.</done>
</task>

<task type="auto">
  <name>Task 3: Info-card DLP Period + DLP Expires fields</name>
  <read_first>
    - app/views/project-detail.js ~629-663 (Project Information card grid — where DLP fields surface)
    - .planning/spikes/036-dlp-states-finance-bar/spike.html lines 200-209 (info-card DLP Period / DLP Expires cells)
    - .planning/phases/102-dlp-retention-management/102-CONTEXT.md D-19 (info card surfaces DLP Period + DLP Expires; Completed date already shown by Phase 100)
  </read_first>
  <action>
    In the Project Information card grid, add two cells — "DLP Period" (value `${currentProject.dlp_months || null}` rendered as "N months" or "—") and "DLP Expires" (value `formatDate`/raw `currentProject.dlp_expires_at || '—'`) — shown only when `getDlpState(currentProject) !== 'active'` (i.e., DLP exists). Match the existing cell markup pattern in that grid (label span + value div). Read both fields with `|| null` (D-22). Do not duplicate the Completed date (Phase 100 already shows it in the lifecycle Completed branch).
  </action>
  <verify>
    <automated>node --check app/views/project-detail.js && node -e "const c=require('fs').readFileSync('app/views/project-detail.js','utf8'); for(const s of ['DLP Period','DLP Expires','dlp_months','dlp_expires_at']){ if(!c.includes(s)){console.error('MISSING',s);process.exit(1);} } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` exits 0.
    - "DLP Period" and "DLP Expires" cells present in the info card, gated on a non-active DLP state.
    - Both fields read defensively with `|| null`.
  </acceptance_criteria>
  <done>Info card surfaces DLP Period + DLP Expires when DLP is set.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Gate-4 DLP capture + 4-state DLP display (Spikes 034 hybrid + 036): DLP fieldset at "Mark Completed" (only when a retention tranche exists), getDlpState-driven finance bar (blue/amber/red/green + DLP strip), retention-row In DLP/OVERDUE/Released tag, info-card DLP Period + DLP Expires.</what-built>
  <how-to-verify>
    1. `python -m http.server 8000`. On an On-going project that HAS a retention tranche (set via Plan 02 editor) with both completion docs attached: open Gate 4 — confirm a DLP fieldset (Retention %, DLP Months 3/6/12/18/24, DLP Start Date defaulting to today) appears above "Mark as Completed". On a project with NO retention tranche, confirm NO DLP fieldset appears.
    2. Set Retention 10%, DLP 12 months, start = today; click Mark as Completed. Reload → DevTools/Firestore: project doc now has dlp_months, dlp_start_date, dlp_expires_at (start + 12 months), retention_percentage, retention_amount (contract × 10%), retention_released_at = null.
    3. Finance bar now amber (in-dlp) with a DLP strip "N days remaining"; retention tranche row shows the "In DLP" tag; info card shows DLP Period "12 months" + DLP Expires date.
    4. (Expired check) Temporarily complete a project with DLP start 13+ months ago (or edit dlp_expires_at to a past date in Firestore) → bar turns red with "Expired N days ago" strip; retention row shows "OVERDUE". (Record Release button arrives in Plan 04 — its absence here is expected.)
    5. Complete a project WITHOUT a retention tranche → no DLP fields written, finance bar stays blue, no DLP strip.
    6. Zero console errors throughout.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `node --check app/views/project-detail.js` exits 0.
- getDlpState matches D-16; DLP write only in retention branch; 4-state bar + tags + info-card fields render per getDlpState.
- Browser UAT (checkpoint) confirms capture + all 4 states.
</verification>

<success_criteria>
SC-3 (Gate-4 DLP capture + computed fields, only when retention tranche exists), SC-4 (getDlpState single source), SC-5 (4-state finance bar), SC-6 (retention row tag + info-card DLP fields) met.
</success_criteria>

<output>
Create `.planning/phases/102-dlp-retention-management/102-03-SUMMARY.md` when done.
</output>
