---
phase: 102-dlp-retention-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [styles/views.css]
autonomous: true
requirements: [SC-1, SC-2, SC-5, SC-6, SC-7]
must_haves:
  truths:
    - "The tranche editor, finance-bar DLP states, and portfolio DLP states all have CSS classes available before any JS renders them"
    - "Finance bar can render amber / red / green wrapper states"
    - "Portfolio rows can render amber / red / green left-accent borders"
  artifacts:
    - path: "styles/views.css"
      provides: "Phase 102 DLP & tranche-editor CSS block (.tranche-editor, .finance-bar.state-*, .dlp-strip, .bar-seg, portfolio .dlp-*)"
      contains: ".tranche-editor"
  key_links:
    - from: "styles/views.css .finance-bar.state-amber"
      to: "project-detail.js financeBar wrapper (Plan 03)"
      via: "class name applied at render time"
      pattern: "finance-bar\\.state-amber"
---

<objective>
Add the complete CSS block for Phase 102 to `styles/views.css` so every JS surface in Plans 02–05 has its classes available before it renders. This is a CSS-only foundation plan: no JS, no behavior.

Purpose: Plans 02–05 render markup that references these classes. Landing CSS first (the Phase 100/101 precedent — CSS block ships as Plan 01) keeps each later plan a pure JS change.
Output: One appended, self-contained, commented CSS block in `styles/views.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-dlp-retention-management/102-CONTEXT.md
@.planning/spikes/035-tranche-editor-in-detail/spike.html
@.planning/spikes/036-dlp-states-finance-bar/spike.html

<interfaces>
<!-- The two spike.html <style> blocks are the AUTHORITATIVE class contract. Port their
     class names and visual values verbatim (colors, radii, paddings). Do NOT invent new names. -->

Spike 035 editor classes (port verbatim):
  .tranche-editor / .tranche-editor.visible, .editor-header (+ .hint),
  .editor-list, .editor-row / .editor-row.retention-row,
  .editor-row input[type=text] / input[type=number], .pct-suffix,
  .ret-toggle / .ret-toggle.on, .remove-btn (+ :disabled),
  .total-row, .total-label, .total-val / .total-ok / .total-err,
  .total-progress / .total-progress-fill,
  .add-btn, .editor-actions,
  .tranche-display, .tranche-display-row (.td-left/.td-name/.td-right/.td-pct/.td-amount),
  .ret-tag, .coll-badge (+ .pending/.none), .no-tranches,
  .setup-cta (+ p, + button), .edit-tranches-btn (+ :hover, + .active)

Spike 036 DLP-state classes (port verbatim):
  .finance-bar.state-amber / .state-red / .state-green,
  .bar-seg / .bar-seg.collected / .bar-seg.retention,
  .dlp-strip / .dlp-strip.amber / .red / .green / .hidden, .dlp-strip-right,
  .release-btn (+ :hover), .release-btn-outline,
  .coll-row.active / .overdue / .done,
  .coll-tag, .tag-collected/.tag-pending/.tag-overdue/.tag-holding/.tag-released,
  .portfolio-row.dlp-amber / .dlp-red / .dlp-green (left-accent border),
  .portfolio-dlp-tag
</interfaces>

Pattern to match: the existing `/* ── LIFECYCLE ACCORDION CARD (Phase 100) ── */` block (views.css ~line 3757) and the Phase 101 journal block (ends views.css ~line 4492) — single commented section, appended at end of file.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Phase 102 DLP + tranche-editor CSS block</name>
  <read_first>
    - styles/views.css (end of file ~line 4492, to append after the Phase 101 journal block; and ~3757 for the Phase 100 lifecycle block style precedent)
    - .planning/spikes/035-tranche-editor-in-detail/spike.html (the &lt;style&gt; block lines 6-221 — verbatim class source for the editor)
    - .planning/spikes/036-dlp-states-finance-bar/spike.html (the &lt;style&gt; block lines 6-170 — verbatim class source for the 4-state finance bar + portfolio row)
    - .planning/phases/102-dlp-retention-management/102-CONTEXT.md (D-67 Claude's-Discretion: follow Spike 035/036 markup class names)
  </read_first>
  <action>
    Append one new commented section to the END of `styles/views.css`, opened with a banner comment `/* ── DLP & RETENTION MANAGEMENT (Phase 102) ── */` and closed with `/* ===== End Phase 102 — DLP & Retention ===== */` (mirror the Phase 101 block delimiters).

    Port the tranche-editor classes from Spike 035 &lt;style&gt; verbatim: `.tranche-editor` (display:none) + `.tranche-editor.visible` (display:block), `.editor-header`/`.hint`, `.editor-list`, `.editor-row` (grid 1fr auto auto auto) + `.editor-row.retention-row`, the `.editor-row input[type=text]`/`input[type=number]` field styles + `.pct-suffix`, `.ret-toggle` + `.ret-toggle.on`, `.remove-btn` + `:disabled`, `.total-row`/`.total-label`/`.total-val`/`.total-ok`/`.total-err`, `.total-progress`/`.total-progress-fill`, `.add-btn`, `.editor-actions`, the read-only `.tranche-display`/`.tranche-display-row` (+ `.td-left`/`.td-name`/`.td-right`/`.td-pct`/`.td-amount`), `.ret-tag`, `.coll-badge` (+ `.pending`/`.none`), `.no-tranches`, `.setup-cta` (+ `p`, + `button`), `.edit-tranches-btn` (+ `:hover`, + `.active`).

    Port the 4-state DLP classes from Spike 036 &lt;style&gt; verbatim: `.finance-bar.state-amber`/`.state-red`/`.state-green` (wrapper bg + border), `.bar-seg`/`.bar-seg.collected`/`.bar-seg.retention`, `.dlp-strip` + `.dlp-strip.amber`/`.red`/`.green`/`.hidden` + `.dlp-strip-right`, `.release-btn` (+ `:hover`) and `.release-btn-outline`, the `.coll-row.active`/`.overdue`/`.done` row tints, `.coll-tag` + `.tag-collected`/`.tag-pending`/`.tag-overdue`/`.tag-holding`/`.tag-released`, the portfolio `.portfolio-row.dlp-amber`/`.dlp-red`/`.dlp-green` left-accent borders (3px solid #f59e0b / #ef4444 / #059669) and `.portfolio-dlp-tag`.

    Prefix every selector that could collide with existing global classes by nesting it under a Phase-102 scope where the spike used a bare generic name. Specifically: scope the editor/finance classes that ALSO exist generically (`.card-title`, `.finance-bar`, `.bar-track`, `.bar-fill`, `.card`) by NOT re-declaring them here — only declare the NEW classes listed above. If a base `.finance-bar` rule does not already exist in views.css, add a minimal base `.finance-bar` (bg #f8fafc, border, radius, padding) so the `.state-*` modifiers have something to modify; if it does exist, only add the `.state-*` modifiers. Keep all color values exactly as in the spikes (CLAUDE.md design-system palette already matches: #1a73e8, #059669, #f59e0b, #ef4444).

    No JS. No HTML. No edits anywhere except the appended block in views.css.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('styles/views.css','utf8'); const need=['.tranche-editor','.tranche-editor.visible','.editor-row.retention-row','.ret-toggle.on','.total-ok','.total-err','.setup-cta','.edit-tranches-btn','.finance-bar.state-amber','.finance-bar.state-red','.finance-bar.state-green','.bar-seg','.dlp-strip','.dlp-strip.amber','.dlp-strip.red','.dlp-strip.green','.release-btn','.portfolio-row.dlp-amber','.portfolio-row.dlp-red','.portfolio-row.dlp-green','.portfolio-dlp-tag','.tag-holding','.tag-released']; const miss=need.filter(s=>!c.includes(s)); if(miss.length){console.error('MISSING:',miss);process.exit(1);} const o=(c.match(/\{/g)||[]).length,cl=(c.match(/\}/g)||[]).length; if(o!==cl){console.error('BRACE IMBALANCE',o,cl);process.exit(1);} console.log('OK braces',o);"</automated>
  </verify>
  <acceptance_criteria>
    - All 22 class strings in the verify command are present in `styles/views.css`.
    - CSS brace count balanced (open === close) — the verify command exits 0.
    - The block is delimited by the Phase 102 banner + end comment.
    - No selector outside the new block was modified (git diff shows only an appended range).
  </acceptance_criteria>
  <done>
    `styles/views.css` ends with a balanced, self-contained Phase 102 DLP/tranche-editor CSS block containing every class Plans 02–05 reference; verify command exits 0.
  </done>
</task>

</tasks>

<verification>
- `node -e` brace-balance + class-presence check (above) exits 0.
- Open any project-detail page in the browser AFTER Plan 02 lands — editor classes resolve (manual, deferred to Plan 02 UAT).
</verification>

<success_criteria>
CSS-only foundation present for SC-1/2 (editor), SC-5/6 (finance bar + tags), SC-7 (portfolio). No behavior yet — purely enables later plans.
</success_criteria>

<output>
Create `.planning/phases/102-dlp-retention-management/102-01-SUMMARY.md` when done.
</output>
