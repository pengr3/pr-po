---
phase: 102-dlp-retention-management
plan: 05
type: execute
wave: 4
depends_on: ["102-03"]
files_modified: [app/views/projects.js]
autonomous: true
requirements: [SC-7, SC-9]
must_haves:
  truths:
    - "Projects portfolio table rows reflect the same 4 DLP states as project-detail: amber (in-dlp), red (expired), green (released) via a left-accent border + status tag + bar color"
    - "On-going / non-DLP projects render with no DLP accent (unchanged)"
    - "The portfolio DLP logic uses the same getDlpState derivation as project-detail (mirrored in projects.js)"
  artifacts:
    - path: "app/views/projects.js"
      provides: "getDlpState (mirrored), portfolio row DLP class + tag in renderProjectsTable"
      contains: "function getDlpState"
  key_links:
    - from: "renderProjectsTable row"
      to: "getDlpState(project) → dlp-amber/dlp-red/dlp-green class + portfolio-dlp-tag"
      via: "computed at render time"
      pattern: "dlp-amber"
---

<objective>
Add the 4-state DLP visuals to the projects portfolio table (`projects.js`, Spike 036 portfolio row): a left-accent border (amber/red/green) + a DLP status tag, mirroring the project-detail derivation. No new listener — uses the existing projects onSnapshot.

Purpose: Withheld retention must be visible at the portfolio level so DLP/expired projects surface in the table, not only on the detail page.
Output: A mirrored `getDlpState` in projects.js + DLP class/tag applied to the relevant portfolio rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-dlp-retention-management/102-CONTEXT.md
@.planning/spikes/036-dlp-states-finance-bar/spike.html

<interfaces>
<!-- Existing projects.js contracts. Do not re-explore. -->

renderProjectsTable() (projects.js ~901-970): maps pageItems → <tr> rows. Each row currently has
  a status <td> using project.project_status and UNIFIED_STATUS_OPTIONS, a clickable-row class,
  and an Active/Inactive badge. This is the row to augment.

getDlpState contract (must MATCH project-detail Plan 03 verbatim, D-16):
  if (!project.dlp_months || project.project_status !== 'Completed') return 'active';
  if (project.retention_released_at) return 'released';
  if (Date.now() > new Date(project.dlp_expires_at).getTime()) return 'expired';
  return 'in-dlp';

Portfolio DLP classes (defined in views.css by Plan 01): `tr.dlp-amber` / `tr.dlp-red` /
  `tr.dlp-green` (left-accent border, 3px solid #f59e0b/#ef4444/#059669) + `.portfolio-dlp-tag`.
  Plan 01 owns these selectors and targets the `<tr>` element directly (the projects table renders
  `<tr class="clickable-row">`, NOT the spike's grid `.portfolio-row`). So: just add the bare
  `dlp-amber`/`dlp-red`/`dlp-green` class to the existing `<tr>` class list — the border renders
  from Plan 01's `tr.dlp-*` rules; no views.css edit needed here.
  Map states: in-dlp→`dlp-amber` "◑ In DLP", expired→`dlp-red` "⚠ Retention Overdue",
  released→`dlp-green` "✓ Fully Collected", active→none.

projects.js has its own destroy() teardown (~332-394) — no NEW window functions needed for this plan
  (render-only change), so no init/destroy registration delta unless a helper requires it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror getDlpState + apply DLP accent/tag to portfolio rows</name>
  <read_first>
    - app/views/projects.js ~901-970 (renderProjectsTable — the row map to augment)
    - app/views/projects.js ~332-394 (destroy — confirm no new window fn needed)
    - .planning/spikes/036-dlp-states-finance-bar/spike.html lines 131-151 (.portfolio-row.dlp-* + .portfolio-dlp-tag) + 308-373 (per-state portTag text/colors)
    - .planning/phases/102-dlp-retention-management/102-CONTEXT.md D-16, D-20, D-22 (single getDlpState; portfolio left-accent + status tag + bar color; defensive || null)
  </read_first>
  <action>
    Add `function getDlpState(project)` in projects.js, VERBATIM identical to the project-detail Plan 03 version (the D-16 contract). Read fields defensively (D-22).

    In `renderProjectsTable()`, for each project compute `const dlpState = getDlpState(project);` and:
    - Add a DLP accent class to the existing `<tr class="clickable-row">` class list: `dlp-amber` for in-dlp, `dlp-red` for expired, `dlp-green` for released, nothing for active. The left-border renders from Plan 01's `tr.dlp-amber`/`tr.dlp-red`/`tr.dlp-green` rules — no views.css edit here (file-ownership: views.css belongs to Plan 01, which already defines the `tr.dlp-*` selectors targeting these exact rows). Just append the bare class name.
    - In the Status `<td>`, when `dlpState !== 'active'`, append a `.portfolio-dlp-tag` with the state text/colors: in-dlp → "◑ In DLP" (amber), expired → "⚠ Retention Overdue" (red), released → "✓ Fully Collected" (green). Escape any interpolated text. Keep the existing status text/legacy handling intact — the DLP tag is additive.

    No new window functions, no new listener (the projects onSnapshot already re-renders on retention_released_at writes). All `tr.dlp-*` border CSS is already owned by Plan 01 — this plan edits projects.js ONLY.
  </action>
  <verify>
    <automated>node --check app/views/projects.js && node -e "const fs=require('fs'); const path='app/views/projects.js'; const c=fs.readFileSync(path,'utf8'); for(const s of ['function getDlpState','getDlpState(project)','dlp-amber','dlp-red','dlp-green','portfolio-dlp-tag','In DLP','Retention Overdue','Fully Collected']){ if(!c.includes(s)){console.error('MISSING',s);process.exit(1);} } const winNow=(c.match(/window\.[A-Za-z0-9_]+\s*=/g)||[]).length; const base=parseInt(process.env.WIN_BASE||'-1',10); if(base>=0 && winNow!==base){console.error('WINDOW FN COUNT CHANGED — render-only plan must add 0 new window fns; was',base,'now',winNow);process.exit(1);} console.log('OK win=',winNow);"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/projects.js` exits 0.
    - `getDlpState` in projects.js is byte-identical in logic to the project-detail version (same 4-branch D-16 contract).
    - Portfolio rows get `dlp-amber`/`dlp-red`/`dlp-green` class + `.portfolio-dlp-tag` with the matching state text; active projects unchanged.
    - No new window functions introduced (render-only); no new Firestore listener.
    - All new field reads defensive (`|| null`).
  </acceptance_criteria>
  <done>Portfolio table rows show the same 4 DLP states as project-detail, derived by the mirrored getDlpState.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Projects portfolio table 4-state DLP visuals (Spike 036 portfolio row): left-accent border + status tag for in-dlp (amber) / expired (red) / released (green), mirroring project-detail's getDlpState.</what-built>
  <how-to-verify>
    1. `python -m http.server 8000`, open `#/projects`.
    2. A Completed project in its DLP window shows an amber left-accent + "◑ In DLP" tag in the row; an expired-DLP project shows red + "⚠ Retention Overdue"; a released project shows green + "✓ Fully Collected".
    3. On-going / non-DLP projects render with no DLP accent (unchanged from today).
    4. Record a retention release on the detail page (Plan 04) → return to the portfolio table → that row flips to green/Fully Collected without a manual reload (existing onSnapshot).
    5. Zero console errors.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `node --check app/views/projects.js` exits 0.
- getDlpState mirrors project-detail; portfolio rows carry the 4-state class + tag.
- Browser UAT confirms portfolio states + live update.
</verification>

<success_criteria>
SC-7 (portfolio table 4 DLP states: left-accent + status tag + bar color) and the projects.js portion of SC-9 (node --check; defensive reads; no listener regression) met.
</success_criteria>

<output>
Create `.planning/phases/102-dlp-retention-management/102-05-SUMMARY.md` when done.
</output>
