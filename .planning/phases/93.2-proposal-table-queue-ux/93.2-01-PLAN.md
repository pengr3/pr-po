---
phase: "93.2"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/home.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "The approval queue updates automatically when a proposal changes status — no manual page refresh needed"
    - "Queue rows display a Client column (target_client_name) between Submitted By and Amount — 6 columns total"
    - "Queue rows with an attachment_url show a clickable paperclip link inside the Proposal cell that does not trigger the row-click modal"
  artifacts:
    - path: "app/views/home.js"
      provides: "onSnapshot listener stored in _proposalListener, Client column in queue, attachment link in Proposal cell"
      contains: "_proposalListener"
  key_links:
    - from: "_loadHomeProposalsTab"
      to: "onSnapshot(collection(db, 'proposals'), ...)"
      via: "replaces getDocs"
      pattern: "onSnapshot.*proposals"
    - from: "_renderHomeApprovalQueueHtml"
      to: "target_client_name"
      via: "new Client <td> cell"
      pattern: "target_client_name"
    - from: "attachment link"
      to: "event.stopPropagation"
      via: "onclick on anchor prevents row-click modal"
      pattern: "stopPropagation"
---

<objective>
Replace the one-time getDocs in _loadHomeProposalsTab with a real-time onSnapshot listener so the approval queue refreshes automatically. Add a Client column to the queue table and an attachment paperclip link inside the Proposal cell.

Purpose: Approvers no longer see stale queue state after another user acts on a proposal. Client name gives context at a glance without opening the modal. Attachment link allows direct document access without a detour through the detail modal.
Output: app/views/home.js with three targeted changes — listener upgrade, queue column addition, queue cell link addition.
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\..\..\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\Roaming\pr-po\.planning\..\..\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\.planning\STATE.md
@C:\Users\Admin\Roaming\pr-po\.planning\phases\93.2-proposal-table-queue-ux\93.2-CONTEXT.md

<interfaces>
<!-- Key identifiers and shapes extracted from app/views/home.js -->

Module-level state variables (lines 23–37):
  let statsListeners = [];
  let _homeProposalsCache = [];
  let _homeCanApproveQueue = false;
  let _homeProposalStatusFilter = null;

Function: _loadHomeProposalsTab(canApproveQueue) — lines 553–602
  - Currently calls: getDocs(collection(db, 'proposals'))
  - Sets _homeCanApproveQueue = !!canApproveQueue
  - Sets _homeProposalStatusFilter = null
  - Calls filterProposalsForUser(all), populates _homeProposalsCache
  - Renders queueHtml + tableSection into mount element id "homeProposalsContent"

Function: _renderHomeApprovalQueueHtml(pending) — lines 227–297
  - Currently 5 columns: Proposal, Submitted By, Amount, Age in Stage, Actions
  - Each row is a <tr> with onclick for row-click modal; Approve/Reject use event.stopPropagation()
  - Proposal cell (first td) contains: title div + projectLabel div

destroy() — lines 733–762:
  - Deletes window.handleHomeProposalScorecardClick and other window functions
  - Unsubscribes statsListeners array
  - Resets _homeProposalsCache = [], _homeCanApproveQueue = false

Lines to REMOVE (manual re-fetch calls made obsolete by onSnapshot):
  - Line 313: await _loadHomeProposalsTab(_homeCanApproveQueue) inside _openHomeQueueModal stale-status path
  - Line 430: await _loadHomeProposalsTab(_homeCanApproveQueue) inside _homeQueueConfirmAction post-action refresh

Import line 7: already imports { onSnapshot, getDocs } from '../firebase.js'
  - After this task getDocs is still used by _homeQueueConfirmAction (getDoc) — keep it
  - onSnapshot already imported — no import change needed

Decision D-05 (from CONTEXT.md): onSnapshot body must
  - Update _homeProposalsCache from filtered snapshot
  - Re-render queue and table using current _homeProposalStatusFilter and _homeProposalPage (page var added in Plan 02 — for now use 1)
  - NOT reset _homeProposalStatusFilter on snapshot fire
  - DO reset page state to 1 on snapshot fire (Plan 02 adds _homeProposalPage; in this plan the table render call does not yet pass a page argument — that is fine, Plan 02 adds it)

Decision D-06: Client column — escapeHTML(p.target_client_name || '—')
  Position: after Submitted By <th>/<td>, before Amount

Decision D-07: Attachment link in Proposal cell (not Actions cell)
  Render below the projectLabel div when p.attachment_url is non-null/non-empty:
    <div style="margin-top:3px;"><a href="${escapeHTML(p.attachment_url)}" target="_blank"
    rel="noopener noreferrer" onclick="event.stopPropagation();"
    style="font-size:0.75rem;color:#3b82f6;">📎 ${escapeHTML(p.attachment_filename || 'View Attachment')}</a></div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade queue listener from getDocs to onSnapshot + add _proposalListener lifecycle</name>
  <files>app/views/home.js</files>
  <read_first>
    - app/views/home.js lines 1–40 (imports + module-level state)
    - app/views/home.js lines 553–602 (_loadHomeProposalsTab full body)
    - app/views/home.js lines 306–315 (_openHomeQueueModal stale-status path, line 313 to remove)
    - app/views/home.js lines 425–442 (_homeQueueConfirmAction post-action path, line 430 to remove)
    - app/views/home.js lines 733–762 (destroy() full body)
  </read_first>
  <action>
    1. Add module-level variable `let _proposalListener = null;` directly after the existing `let _homeProposalStatusFilter = null;` declaration (line 37 area). This is the unsubscribe handle for the proposals onSnapshot listener.

    2. Rewrite _loadHomeProposalsTab to use onSnapshot instead of getDocs. The function becomes synchronous-setup (not async — remove async keyword). Exact changes:

       a. Remove the `const snap = await getDocs(collection(db, 'proposals'));` call and the forEach that builds `all`.

       b. Cancel any existing listener first: call `_proposalListener?.();` and set `_proposalListener = null;` before registering a new one. This prevents duplicate listeners on repeated calls.

       c. Store the return value of onSnapshot: `_proposalListener = onSnapshot(collection(db, 'proposals'), (snap) => { ... }, (err) => { console.error('[Home] proposals onSnapshot error:', err); });`

       d. Inside the onSnapshot success callback, replicate the existing logic:
          - Build `all` array via `snap.forEach(d => all.push({ id: d.id, ...d.data() }));`
          - Call `filterProposalsForUser(all)` → `scoped`
          - Set `_homeProposalsCache = scoped;`
          - Build queueHtml and tableSection exactly as before (using the existing render helpers)
          - Reset page to 1 on each snapshot fire: the page variable `_homeProposalPage` is added in Plan 02. For now, the render call to `_renderHomeProposalTable` stays unchanged (no page arg yet — Plan 02 adds it)
          - Build `mount.innerHTML` as before
          - If mount is null at callback time (user navigated away), return early

       e. The outer try/catch wrapping the getDocs can be removed (onSnapshot error goes to error callback). Keep the null-mount guard.

    3. In _openHomeQueueModal (around line 313), remove the line:
       `await _loadHomeProposalsTab(_homeCanApproveQueue);`
       from the stale-status early-return path. Replace with nothing — the onSnapshot will auto-refresh. Per D-05, a `showToast` is already shown before this line; the flow just returns after the toast.

    4. In _homeQueueConfirmAction (around line 430), remove the line:
       `await _loadHomeProposalsTab(_homeCanApproveQueue);`
       from the post-action success path. The onSnapshot fires automatically after Firestore writes. Do not add any replacement call.

    5. In destroy(), add before the statsListeners unsubscription block:
       `_proposalListener?.();`
       `_proposalListener = null;`
       Also add `_homeProposalStatusFilter = null;` (matches the state reset pattern).

    Do NOT change the getDocs imports — getDoc (singular) is still used inside _openHomeQueueModal and _homeQueueConfirmAction. The getDocs (plural) import becomes unused but can remain to avoid import churn (Plan 02 may use it if needed, and it causes no runtime cost).
  </action>
  <verify>
    Load app in browser at http://localhost:8000, navigate to Home → Proposals tab. Open DevTools Console — confirm no JS errors. Approve a proposal from the queue in another tab or via Firestore console; confirm the queue updates within a few seconds without a page refresh.
  </verify>
  <acceptance_criteria>
    - `let _proposalListener = null;` is declared at module scope in home.js
    - `_loadHomeProposalsTab` no longer contains `getDocs(collection(db, 'proposals'))` — confirmed by absence of that pattern in the function body
    - `_loadHomeProposalsTab` registers `onSnapshot(collection(db, 'proposals'), ...)` and stores the unsubscribe handle in `_proposalListener`
    - destroy() calls `_proposalListener?.()` and sets `_proposalListener = null`
    - Line-313 `_loadHomeProposalsTab` call in `_openHomeQueueModal` is absent from the file
    - Line-430 `_loadHomeProposalsTab` call in `_homeQueueConfirmAction` is absent from the file
    - No JS syntax errors on page load (DevTools Console shows no red errors for home.js)
  </acceptance_criteria>
  <done>onSnapshot replaces getDocs in _loadHomeProposalsTab; _proposalListener is stored and cleaned up in destroy(); two manual re-fetch calls are removed.</done>
</task>

<task type="auto">
  <name>Task 2: Add Client column to queue table + attachment link in Proposal cell</name>
  <files>app/views/home.js</files>
  <read_first>
    - app/views/home.js lines 227–297 (_renderHomeApprovalQueueHtml full body — thead and each row td)
  </read_first>
  <action>
    In _renderHomeApprovalQueueHtml(pending):

    1. Add Client <th> to the thead row. Current column order is: Proposal | Submitted By | Amount | Age in Stage | Actions. Insert "Client" between "Submitted By" and "Amount":
       New order: Proposal | Submitted By | Client | Amount | Age in Stage | Actions
       The new <th> uses identical inline style as siblings: `padding: 0.6rem 1rem; text-align: left; font-size: 0.8125rem; color: #64748b; font-weight: 600; border-bottom: 1px solid #e5e7eb;`
       Text content: `Client`

    2. In each row's template literal (inside the `.map(p => ...)` callback), add a Client <td> after the Submitted By <td>. Insert at the correct position to match the new column order:
       `<td style="padding: 0.75rem 1rem; vertical-align: middle; color: #475569; font-size: 0.9rem;">${escapeHTML(p.target_client_name || '—')}</td>`

    3. In the Proposal cell (first <td>), append an attachment link block after the `projectLabel` div. The existing Proposal cell currently has two inner divs: title div and projectLabel div. After the projectLabel div, add:
       `${p.attachment_url ? \`<div style="margin-top:3px;"><a href="${escapeHTML(p.attachment_url)}" target="_blank" rel="noopener noreferrer" onclick="event.stopPropagation();" style="font-size:0.75rem;color:#3b82f6;">📎 ${escapeHTML(p.attachment_filename || 'View Attachment')}</a></div>\` : ''}`

       Per D-07: use escapeHTML on both attachment_url and attachment_filename to prevent XSS. The onclick="event.stopPropagation()" is a literal attribute string on the <a> tag — it prevents the parent <tr> onclick from firing.

    Do NOT change the empty-state path (when `pending.length === 0`) — it has no table.
    Do NOT change _renderHomeProposalTable (the unified proposals table below the queue) — that table is out of scope for this task.
  </action>
  <verify>
    Load app in browser. Navigate to Home → Proposals tab as a user with canApproveQueue=true (super_admin or operations_admin). Confirm the queue table shows 6 columns with "Client" between "Submitted By" and "Amount". For a queue row that has an attachment_url set on its Firestore document, confirm a paperclip link is visible in the Proposal cell. Clicking the link opens the attachment in a new tab without opening the proposal modal.
  </verify>
  <acceptance_criteria>
    - `_renderHomeApprovalQueueHtml` thead contains exactly 6 <th> elements: Proposal, Submitted By, Client, Amount, Age in Stage, Actions (in that order)
    - Each row <tr> contains 6 <td> elements in matching order
    - The Client <td> renders `escapeHTML(p.target_client_name || '—')` — em-dash literal when field is absent
    - The Proposal cell contains a conditional attachment block using `p.attachment_url ?` ternary
    - The attachment <a> tag has `onclick="event.stopPropagation();"`, `target="_blank"`, and `rel="noopener noreferrer"`
    - `escapeHTML` is applied to `p.attachment_url` and `p.attachment_filename` in the attachment block
    - No JS syntax errors on page load
  </acceptance_criteria>
  <done>Queue table has 6 columns including Client; queue rows with attachments show a paperclip link that opens in a new tab without triggering the row-click modal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore → client | proposal documents (title, target_client_name, attachment_url, attachment_filename) rendered into HTML |
| attachment_url → browser | user navigates to external URL from queue row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93.2-01 | Tampering | attachment_url rendered in href | mitigate | escapeHTML applied per D-07; href value is from Firestore doc written by authenticated user — same trust level as existing proof_url links in procurement.js |
| T-93.2-02 | Information Disclosure | attachment_filename rendered in link text | mitigate | escapeHTML applied; display-only, no eval path |
| T-93.2-03 | Spoofing | onSnapshot fires after destroy() (navigated away) | mitigate | null-mount guard: if (!mount) return; at start of snapshot callback — existing pattern in _loadHomeProposalsTab error path |
| T-93.2-04 | Denial of Service | duplicate onSnapshot if _loadHomeProposalsTab called twice | mitigate | cancel existing listener via _proposalListener?.() before registering new one (Task 1 step 2b) |
</threat_model>

<verification>
1. Browser loads http://localhost:8000 — no red JS errors in Console
2. Home → Proposals tab renders without errors for a canApproveQueue user
3. Queue table has 6 columns (Proposal, Submitted By, Client, Amount, Age in Stage, Actions)
4. Updating a proposal document in Firestore console causes queue to refresh without page reload
5. Queue rows with attachment_url show a paperclip link; clicking it opens a new tab
6. Clicking elsewhere on the row (not the attachment link) opens the proposal modal
7. Navigating away from Home and back does not produce JS errors or duplicate listeners
</verification>

<success_criteria>
- _loadHomeProposalsTab uses onSnapshot and stores unsubscribe handle in _proposalListener
- destroy() cancels _proposalListener before clearing statsListeners
- Two manual _loadHomeProposalsTab re-calls (lines 313 and 430) are absent
- Queue table has Client column between Submitted By and Amount
- Attachment link in Proposal cell uses escapeHTML + stopPropagation + target="_blank" + rel="noopener noreferrer"
</success_criteria>

<output>
Create `.planning/phases/93.2-proposal-table-queue-ux/93.2-01-SUMMARY.md` when done
</output>
