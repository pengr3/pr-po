---
phase: "93.2"
plan: "02"
type: execute
wave: 2
depends_on: ["93.2-01"]
files_modified:
  - app/views/home.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Home → Proposals tab loads showing only active-stage proposals (draft, pending_internal, pending_client, for_revision) by default — closed stages are hidden until their scorecard tile is clicked"
    - "Proposals table is sorted newest-first by updated_at"
    - "Proposals table is paginated at 10 rows per page using .pagination-container / .pagination-btn / .pagination-info CSS classes"
    - "Clicking an active-stage scorecard tile resets the view to active_only (not a single-status filter)"
    - "Clicking client_approved or loss scorecard tile toggles between that status and active_only"
    - "Clicking a scorecard tile resets the page to 1"
  artifacts:
    - path: "app/views/home.js"
      provides: "ACTIVE_PROPOSAL_STAGES constant, _homeProposalPage variable, updated filter/sort/paginate logic"
      contains: "ACTIVE_PROPOSAL_STAGES"
  key_links:
    - from: "_renderHomeProposalTable"
      to: "_homeProposalPage"
      via: "slice((page-1)*10, page*10) on sorted array"
      pattern: "_homeProposalPage"
    - from: "handleHomeProposalScorecardClick"
      to: "ACTIVE_PROPOSAL_STAGES"
      via: "includes check to determine active vs closed tile"
      pattern: "ACTIVE_PROPOSAL_STAGES"
    - from: "window.handleHomeProposalPageChange"
      to: "_rerenderProposalTable"
      via: "sets _homeProposalPage then calls rerender"
      pattern: "handleHomeProposalPageChange"
---

<objective>
Implement the active-only default filter, updated_at descending sort, 10-per-page pagination, and scorecard click behavior changes for the Home → Proposals tab. All logic is in app/views/home.js only — no new files, no new Firestore queries.

Purpose: Users see immediately actionable proposals on first load; the table is sorted so most recently updated work appears first; pagination prevents an unmanageable scroll for users with many proposals.
Output: app/views/home.js with five targeted changes — new module constants/variables, updated filter initialization, updated scorecard click handler, sort+paginate logic in _renderHomeProposalTable, pagination HTML emitted inline, handleHomeProposalPageChange window function registered in init/destroy.
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\..\..\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\Roaming\pr-po\.planning\..\..\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\.planning\STATE.md
@C:\Users\Admin\Roaming\pr-po\.planning\phases\93.2-proposal-table-queue-ux\93.2-CONTEXT.md
@C:\Users\Admin\Roaming\pr-po\.planning\phases\93.2-proposal-table-queue-ux\93.2-01-SUMMARY.md

<interfaces>
<!-- Key identifiers extracted from app/views/home.js after Plan 01 is applied -->

Module-level state (after Plan 01):
  let _homeProposalsCache = [];
  let _homeCanApproveQueue = false;
  let _homeProposalStatusFilter = null;   ← Plan 02 changes initialization to 'active_only'
  let _proposalListener = null;           ← added by Plan 01

New additions this plan:
  const ACTIVE_PROPOSAL_STAGES = ['draft','pending_internal','pending_client','for_revision'];
  let _homeProposalPage = 1;

Function _loadHomeProposalsTab(canApproveQueue):
  - After Plan 01: uses onSnapshot
  - Currently sets _homeProposalStatusFilter = null  ← change to 'active_only'
  - onSnapshot body must reset _homeProposalPage = 1 on each fire (per D-05)
  - onSnapshot body calls _renderHomeProposalTable — after Plan 02, pass _homeProposalPage as second arg

Function _renderHomeProposalTable(proposals):
  - Currently: renders all proposals in proposals array, no pagination
  - After Plan 02: accepts (proposals, page = 1) signature
    - Sort: by updated_at desc using .toMillis() / .seconds*1000 fallback (same pattern as queue sort in _loadHomeProposalsTab line ~573)
    - Slice: sorted.slice((page - 1) * 10, page * 10)
    - Return: table HTML + pagination HTML concatenated

Pagination HTML pattern from procurement.js lines 5030–5060:
  Structure: <div class="pagination-container"> wrapping
    <div class="pagination-info">Showing X-Y of Z Proposals</div>
    <div class="pagination-controls">
      <button class="pagination-btn" [disabled if page=1]>← Previous</button>
      [page number buttons with "active" class on current page, ellipsis for gaps]
      <button class="pagination-btn" [disabled if page=totalPages]>Next →</button>
    </div>
  Each page button: onclick="window.handleHomeProposalPageChange(N)"
  Prev/Next buttons: onclick="window.handleHomeProposalPageChange(page - 1 or + 1)"

Function _rerenderProposalTable():
  - Currently passes _homeProposalStatusFilter to _renderHomeProposalScorecards
  - Currently passes filtered array to _renderHomeProposalTable (no page arg)
  - After Plan 02: pass _homeProposalPage as second arg to _renderHomeProposalTable

window.handleHomeProposalScorecardClick (defined in init()):
  - Current behavior (line 652–655): toggles between statusKey and null
  - New behavior per D-01/D-02:
    if ACTIVE_PROPOSAL_STAGES.includes(statusKey): set _homeProposalStatusFilter = 'active_only'
    else (client_approved or loss):
      if _homeProposalStatusFilter === statusKey: set to 'active_only'
      else: set to statusKey
    reset _homeProposalPage = 1
    call _rerenderProposalTable()

Filter logic in _rerenderProposalTable and in onSnapshot body:
  - _homeProposalStatusFilter === 'active_only' → filter to ACTIVE_PROPOSAL_STAGES.includes(p.status)
  - _homeProposalStatusFilter === null → show all (this state is retired as default but may still occur during transition)
  - _homeProposalStatusFilter === 'client_approved' or 'loss' → filter by exact status match

Scorecard active class sync (existing safety-net in _rerenderProposalTable lines 538–540):
  - After Plan 02, 'active_only' matches no individual tile key → no tile gets --active class when filter is 'active_only'
  - This is acceptable per CONTEXT.md specifics: "activeFilter === 'active_only' will match no tile key, so no tile is highlighted in default view (acceptable)"

destroy():
  - Add: delete window.handleHomeProposalPageChange
  - Add: _homeProposalPage = 1
  - (These sit alongside the existing window function deletions)
</interfaces>

<!-- Pagination helper reference from procurement.js (read lines 5030–5060 if you need the exact structure) -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add module constants/variables + update filter initialization + onSnapshot page reset</name>
  <files>app/views/home.js</files>
  <read_first>
    - app/views/home.js lines 33–40 (module-level state block — where to add new vars)
    - app/views/home.js lines 553–602 (_loadHomeProposalsTab full body after Plan 01 changes — verify onSnapshot shape)
    - app/views/home.js lines 733–762 (destroy() — where to add new cleanup)
  </read_first>
  <action>
    1. Add module-level constant directly after the existing `let _homeProposalStatusFilter` line:
       `const ACTIVE_PROPOSAL_STAGES = ['draft', 'pending_internal', 'pending_client', 'for_revision'];`

    2. Add module-level variable directly after the ACTIVE_PROPOSAL_STAGES constant:
       `let _homeProposalPage = 1;`

    3. In _loadHomeProposalsTab, change the line:
       `_homeProposalStatusFilter = null;`
       to:
       `_homeProposalStatusFilter = 'active_only';`
       This is the initialization that sets the default filter on first load and on live snapshot reloads that call the function again.

    4. Inside the onSnapshot success callback body (added by Plan 01), after updating _homeProposalsCache, add:
       `_homeProposalPage = 1;`
       before building queueHtml and tableSection. This resets the page on every live update so users are not stranded on a non-existent page after the data changes.

    5. In the onSnapshot callback, update the filter logic used when building the table. The callback currently passes `scoped` to `_renderHomeProposalTable`. Change it to pass the filtered-by-active-only set instead:
       `const filtered = _homeProposalStatusFilter === 'active_only'`
       `    ? scoped.filter(p => ACTIVE_PROPOSAL_STAGES.includes(p.status))`
       `    : (_homeProposalStatusFilter ? scoped.filter(p => p.status === _homeProposalStatusFilter) : scoped);`
       Pass `filtered` to `_renderHomeProposalTable` (and `_homeProposalPage` as the second arg — Task 2 adds that signature; temporarily pass 1 if Task 2 is applied after).

       In practice, Tasks 1 and 2 are applied in a single editing session, so pass `_homeProposalPage` directly.

    6. Also update the tableSection construction in the onSnapshot callback to pass `_homeProposalPage`:
       `${_renderHomeProposalTable(filtered, _homeProposalPage)}`
       instead of the current `${_renderHomeProposalTable(filtered)}` or `${_renderHomeProposalTable(scoped)}`.

    7. In destroy(), add:
       `delete window.handleHomeProposalPageChange;`
       (alongside the existing window function deletions)
       `_homeProposalPage = 1;`
       (after the existing `_homeProposalsCache = []` line)

    Do NOT touch: _renderHomeProposalTable signature (that is Task 2), _rerenderProposalTable (that is Task 2), handleHomeProposalScorecardClick (that is Task 2).
  </action>
  <verify>
    Load app in browser. Navigate to Home → Proposals tab. Confirm in DevTools that _homeProposalStatusFilter initializes to 'active_only' (add a console.log temporarily if needed, or inspect via breakpoint). Confirm the table shows only draft/pending_internal/pending_client/for_revision proposals on first load.
  </verify>
  <acceptance_criteria>
    - `const ACTIVE_PROPOSAL_STAGES = ['draft', 'pending_internal', 'pending_client', 'for_revision'];` is present at module scope in home.js
    - `let _homeProposalPage = 1;` is present at module scope
    - `_loadHomeProposalsTab` sets `_homeProposalStatusFilter = 'active_only'` (not null) at the top of the function body
    - The onSnapshot callback body sets `_homeProposalPage = 1;` before rendering
    - The onSnapshot callback passes `filtered` (not `scoped`) to `_renderHomeProposalTable`
    - destroy() contains `delete window.handleHomeProposalPageChange;` and `_homeProposalPage = 1;`
    - No JS syntax errors on page load
  </acceptance_criteria>
  <done>ACTIVE_PROPOSAL_STAGES constant and _homeProposalPage variable declared; _loadHomeProposalsTab initializes to 'active_only'; onSnapshot callback resets page and applies active-only filter before rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Add sort + pagination to _renderHomeProposalTable + update scorecard click + wire window function</name>
  <files>app/views/home.js</files>
  <read_first>
    - app/views/home.js lines 480–541 (_renderHomeProposalTable full body + _rerenderProposalTable full body)
    - app/views/home.js lines 644–658 (init() window function registrations — where handleHomeProposalScorecardClick is defined)
    - app/views/procurement.js lines 5016–5060 (pagination HTML structure for reference — use exact CSS class names)
  </read_first>
  <action>
    1. Update _renderHomeProposalTable signature to accept a page parameter:
       Change `function _renderHomeProposalTable(proposals)` to `function _renderHomeProposalTable(proposals, page = 1)`

    2. At the top of _renderHomeProposalTable, before the empty-state check, sort the proposals by updated_at descending:
       `const sorted = [...proposals].sort((a, b) => {`
       `    const tsA = a.updated_at?.toMillis?.() ?? (a.updated_at?.seconds != null ? a.updated_at.seconds * 1000 : 0);`
       `    const tsB = b.updated_at?.toMillis?.() ?? (b.updated_at?.seconds != null ? b.updated_at.seconds * 1000 : 0);`
       `    return tsB - tsA;`  // descending: newest first
       `});`
       Use `sorted` everywhere below (not the original `proposals` parameter).

    3. After sorting, apply the pagination slice before building rows:
       `const totalItems = sorted.length;`
       `const totalPages = Math.max(1, Math.ceil(totalItems / 10));`
       `const safePage = Math.min(Math.max(1, page), totalPages);`
       `const startIdx = (safePage - 1) * 10;`
       `const pageItems = sorted.slice(startIdx, startIdx + 10);`
       Build rows from `pageItems` instead of `sorted`.

    4. Build pagination HTML using the same CSS classes as procurement.js — `.pagination-container`, `.pagination-info`, `.pagination-controls`, `.pagination-btn`, `.pagination-ellipsis`:
       `const endIdx = Math.min(startIdx + 10, totalItems);`
       Build paginationHtml as a string. Structure:
       - If `totalPages <= 1`: `paginationHtml = ''` (no pagination controls if everything fits on one page)
       - Else: emit the full pagination block with Previous button, page number buttons (with ellipsis pattern identical to procurement.js: show page 1, last page, and pages within 1 of safePage; ellipsis for gaps), Next button
       - Previous button: `onclick="window.handleHomeProposalPageChange(${safePage - 1})"`, disabled when safePage === 1
       - Next button: `onclick="window.handleHomeProposalPageChange(${safePage + 1})"`, disabled when safePage === totalPages
       - Page N button: `onclick="window.handleHomeProposalPageChange(N)"`, add `active` class when N === safePage
       - Pagination info text: `Showing ${startIdx + 1}–${endIdx} of ${totalItems} Proposals`

    5. In the return statement of _renderHomeProposalTable, concatenate pagination HTML after the table card:
       Return the table card HTML string + paginationHtml (when there are rows) or just the empty-state card (when sorted.length === 0, no pagination needed).
       Note: Move the empty-state check to use `sorted.length` after the sort is computed.

    6. Update _rerenderProposalTable to:
       a. Apply the 'active_only' filter logic (same pattern as Task 1 step 5):
          `const filtered = _homeProposalStatusFilter === 'active_only'`
          `    ? _homeProposalsCache.filter(p => ACTIVE_PROPOSAL_STAGES.includes(p.status))`
          `    : (_homeProposalStatusFilter ? _homeProposalsCache.filter(p => p.status === _homeProposalStatusFilter) : _homeProposalsCache);`
          Replace the existing filter logic that only handles null vs statusKey.
       b. Pass `_homeProposalPage` as second arg to _renderHomeProposalTable:
          `_renderHomeProposalTable(filtered, _homeProposalPage)`

    7. In init(), replace the existing window.handleHomeProposalScorecardClick assignment (lines 652–655) with the new D-02 behavior:
       ```
       window.handleHomeProposalScorecardClick = (statusKey) => {
           if (ACTIVE_PROPOSAL_STAGES.includes(statusKey)) {
               _homeProposalStatusFilter = 'active_only';
           } else {
               _homeProposalStatusFilter = (_homeProposalStatusFilter === statusKey) ? 'active_only' : statusKey;
           }
           _homeProposalPage = 1;
           _rerenderProposalTable();
       };
       ```
       The logic: active-stage tiles (draft/pending_internal/pending_client/for_revision) always set filter to 'active_only'. Closed tiles (client_approved/loss) toggle between their own key and 'active_only' (never null).

    8. Register handleHomeProposalPageChange window function in init(), alongside the other window function registrations:
       `window.handleHomeProposalPageChange = (page) => { _homeProposalPage = page; _rerenderProposalTable(); };`

    Do NOT touch: queue render function, scorecard render function, queue sort logic, _loadHomeProposalsTab onSnapshot setup (done by Task 1).
  </action>
  <verify>
    Load app in browser. Navigate to Home → Proposals tab:
    - Confirm table shows active-stage proposals only on first load
    - Confirm proposals are sorted newest updated_at first (check a couple of rows against Firestore)
    - If more than 10 active proposals exist, confirm pagination controls appear and Page 2 shows the next 10
    - Click the "client_approved" scorecard tile — confirm table switches to showing only client_approved proposals
    - Click it again — confirm it returns to active_only view
    - Click the "pending_internal" scorecard tile — confirm it sets filter to active_only (all four active stages visible), not just pending_internal
    - Navigate away from Home and back — confirm page resets to 1 and no JS errors
  </verify>
  <acceptance_criteria>
    - `_renderHomeProposalTable` signature is `function _renderHomeProposalTable(proposals, page = 1)`
    - Function body sorts proposals by `updated_at` descending using `.toMillis?.() ?? (.seconds * 1000)` fallback pattern
    - Pagination slice: `sorted.slice((safePage - 1) * 10, safePage * 10)` (or equivalent with startIdx)
    - Pagination HTML uses `.pagination-container`, `.pagination-info`, `.pagination-controls`, `.pagination-btn` CSS classes
    - Page buttons call `window.handleHomeProposalPageChange(N)` in onclick attributes
    - No pagination controls rendered when totalPages <= 1
    - `_rerenderProposalTable` applies the three-branch filter: 'active_only' → ACTIVE_PROPOSAL_STAGES.includes, null → all, other string → exact match
    - `_rerenderProposalTable` passes `_homeProposalPage` as second arg to `_renderHomeProposalTable`
    - `window.handleHomeProposalScorecardClick` uses `ACTIVE_PROPOSAL_STAGES.includes(statusKey)` to branch active vs closed tile behavior
    - Active-stage tile click sets `_homeProposalStatusFilter = 'active_only'`
    - Closed tile click toggles between statusKey and 'active_only' (never null)
    - `window.handleHomeProposalPageChange` is registered in init() and deleted in destroy()
    - No JS syntax errors on page load
  </acceptance_criteria>
  <done>_renderHomeProposalTable sorts by updated_at desc, paginates at 10/page with .pagination-* CSS, and returns pagination controls. Scorecard click handler uses ACTIVE_PROPOSAL_STAGES for branching. handleHomeProposalPageChange is registered and cleaned up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore → client | updated_at timestamp field used in sort comparison |
| User interaction → filter state | scorecard tile clicks write to _homeProposalStatusFilter |
| User interaction → page state | pagination button clicks write to _homeProposalPage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93.2-05 | Tampering | page number from onclick attribute passed to handleHomeProposalPageChange | mitigate | safePage = Math.min(Math.max(1, page), totalPages) clamps input before slice — out-of-range values produce empty or last-page results, never an error |
| T-93.2-06 | Information Disclosure | ACTIVE_PROPOSAL_STAGES filter hides client_approved/loss from default view | accept | by design — users who need to see closed proposals click the scorecard tile; Firestore rules already gate read access |
| T-93.2-07 | Denial of Service | Large proposals array sorted on every _rerenderProposalTable call | accept | sort is O(N log N) on in-memory array; proposals count is bounded by project count which is small in this org context; no Firestore round-trip involved |
| T-93.2-SC | Tampering | npm/pip/cargo installs | accept | no new packages installed in this plan |
</threat_model>

<verification>
1. Home → Proposals tab default view shows draft/pending_internal/pending_client/for_revision proposals only
2. Proposals in table are sorted newest updated_at first
3. If more than 10 proposals match the filter, pagination controls appear and navigation works
4. Clicking a pending_internal scorecard tile does NOT filter to only pending_internal — it shows all four active stages
5. Clicking client_approved tile shows only client_approved; clicking again returns to active_only
6. Clicking a pagination page button updates the visible rows and does not reload the page
7. Closing the Proposals sub-tab and reopening resets to active_only filter and page 1
8. No JS errors in console throughout all interactions
</verification>

<success_criteria>
- ACTIVE_PROPOSAL_STAGES constant and _homeProposalPage variable declared at module scope
- _loadHomeProposalsTab default filter is 'active_only' (not null)
- _renderHomeProposalTable sorts by updated_at desc and slices to 10 per page
- Pagination HTML uses procurement.js .pagination-* CSS class conventions
- handleHomeProposalScorecardClick correctly branches active vs closed tiles using ACTIVE_PROPOSAL_STAGES.includes
- window.handleHomeProposalPageChange registered in init() and deleted in destroy()
- All six phase goal UX improvements delivered across Plans 01 and 02
</success_criteria>

<output>
Create `.planning/phases/93.2-proposal-table-queue-ux/93.2-02-SUMMARY.md` when done
</output>
