---
phase: "93.2"
plan: "01"
subsystem: "home"
tags: [real-time, onSnapshot, approval-queue, ui-columns]
dependency_graph:
  requires: []
  provides: ["_proposalListener in home.js", "real-time approval queue", "Client column in queue", "attachment link in queue Proposal cell"]
  affects: ["app/views/home.js"]
tech_stack:
  added: []
  patterns: ["onSnapshot listener lifecycle (store + cancel in destroy)", "null-mount guard on snapshot callback"]
key_files:
  created: []
  modified:
    - app/views/home.js
decisions:
  - "D-05 (CONTEXT): onSnapshot body updates cache, re-renders queue and table, does not reset _homeProposalStatusFilter on each fire"
  - "D-06 (CONTEXT): Client column placed after Submitted By — escapeHTML(p.target_client_name || em-dash)"
  - "D-07 (CONTEXT): Attachment link in Proposal cell (not Actions cell) — stopPropagation prevents row modal"
metrics:
  duration: "~10 minutes"
  completed: "2026-05-25"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 1
---

# Phase 93.2 Plan 01: Real-Time Approval Queue + Client Column + Attachment Link Summary

One-line: Replaced one-time getDocs with onSnapshot in the home approval queue, added a Client column, and added an inline attachment paperclip link per D-05/D-06/D-07.

## Tasks Completed

| # | Task | Commit | Key Changes |
|---|------|--------|-------------|
| 1 | Upgrade queue listener from getDocs to onSnapshot + add _proposalListener lifecycle | 3350d1b | `_loadHomeProposalsTab` rewritten as sync onSnapshot setup; `_proposalListener` added at module scope and cleaned up in `destroy()`; two manual re-fetch calls removed |
| 2 | Add Client column to queue table + attachment link in Proposal cell | 923bf17 | 6th thead column inserted; Client `<td>` with `escapeHTML`; conditional attachment `<a>` with `stopPropagation`, `target="_blank"`, `rel="noopener noreferrer"` |

## Deviations from Plan

None — plan executed exactly as written.

## Threat Mitigations Applied

| Threat ID | Mitigation |
|-----------|-----------|
| T-93.2-01 | `escapeHTML(p.attachment_url)` applied in href attribute |
| T-93.2-02 | `escapeHTML(p.attachment_filename)` applied in link text |
| T-93.2-03 | `if (!mount) return;` guard at top of onSnapshot callback |
| T-93.2-04 | `_proposalListener?.(); _proposalListener = null;` before registering new listener |

## Known Stubs

None.

## Threat Flags

None — no new network endpoints or auth paths introduced. All new rendering paths use existing escapeHTML utility.

## Self-Check: PASSED

- [x] `app/views/home.js` exists and contains `_proposalListener`
- [x] Commit 3350d1b exists: `feat(93.2-01): replace getDocs with onSnapshot in _loadHomeProposalsTab`
- [x] Commit 923bf17 exists: `feat(93.2-01): add Client column and attachment link to approval queue`
- [x] `_loadHomeProposalsTab` no longer contains `getDocs(collection(db, 'proposals'))`
- [x] `destroy()` calls `_proposalListener?.()` and sets `_proposalListener = null`
- [x] Two manual `_loadHomeProposalsTab` re-calls removed from `_openHomeQueueModal` and `_homeQueueConfirmAction`
- [x] Queue thead has 6 columns in order: Proposal, Submitted By, Client, Amount, Age in Stage, Actions
- [x] Attachment block uses `escapeHTML` on both url and filename, has `onclick="event.stopPropagation();"`, `target="_blank"`, `rel="noopener noreferrer"`
