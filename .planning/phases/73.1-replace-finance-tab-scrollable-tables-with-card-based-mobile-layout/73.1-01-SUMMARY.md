---
phase: 73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout
plan: 01
subsystem: ui
tags: [css, mobile, responsive, finance, cards, media-query]

# Dependency graph
requires:
  - phase: 73-optimize-finance-tab-for-mobile-use
    provides: Finance tab mobile RESPONSIVE block in views.css (lines 1632-1673); all Finance tables wrapped in .table-scroll-container
provides:
  - "fc-* CSS card layout classes in styles/views.css (Phase 73.1 block)"
  - "Finance-scoped table hide rule at <=768px (does NOT affect Procurement/Suppliers/MRF Records)"
  - "buildMaterialPRCard(pr) helper in finance.js"
  - "buildTRCard(tr) helper in finance.js"
  - "materialPRsCardList and transportRequestsCardList DOM hooks in Pending Approvals section"
  - "renderMaterialPRs() and renderTransportRequests() populate card lists alongside tbody"
affects: [73.1-02, 73.1-03, 73.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "CSS dual-mode pattern: emit both .table-scroll-container (desktop) and .fc-card-list (mobile) in DOM simultaneously; @media hides the one not in use"
    - "Finance-scoped hide rule: prefix .table-scroll-container display:none with Finance section IDs (#approvals-section, #pos-section, #projects-section, #payables-section) to prevent leaking to Procurement/Suppliers/MRF Records"
    - "fc-* CSS prefix (Finance Card) — all card classes namespaced to avoid global collisions"
    - "Pattern A render extension: add card-list sibling in render() static HTML; populate cardList.innerHTML alongside tbody.innerHTML in render function"

key-files:
  created: []
  modified:
    - styles/views.css
    - app/views/finance.js

key-decisions:
  - "UI-SPEC values used over RESEARCH.md draft values: gap 0.25rem (not 0.375rem), fc-label weight 700 (not 600), fc-sub-card font-size 0.75rem (not 0.8125rem) — REVIEWS Concern 2 resolved"
  - "No .fc-overdue applied to Material PR or TR cards — desktop renderMaterialPRs() and renderTransportRequests() have no row-level overdue highlighting, so cards maintain parity with desktop (REVIEWS Concern 1 resolved)"
  - "CTA labels: Review PR and Review TR (verb + noun per UI-SPEC interaction contract)"

patterns-established:
  - "CSS dual-mode card/table toggle: both structures in DOM simultaneously, CSS @media selects which is visible — no JS viewport detection needed"
  - "Finance-scoped CSS: always prefix Finance @media rules with Finance section IDs to prevent Procurement regression"

requirements-completed: [MOBCARD-CSS, MOBCARD-01, MOBCARD-02, MOBCARD-06, MOBCARD-10]

# Metrics
duration: 15min
completed: 2026-04-15
---

# Phase 73.1 Plan 01: Finance Mobile Card Layout Foundation Summary

**CSS dual-mode card/table foundation for Finance Pending Approvals: Material PRs and Transport Requests now render as readable card stacks on mobile (<=768px) while desktop tables remain unaffected.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-15T05:37:10Z
- **Completed:** 2026-04-15T05:41:25Z
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments

- Added 140-line Finance Card Layout block to `styles/views.css` with all fc-* classes (fc-card, fc-card-header, fc-card-id, fc-card-body, fc-card-row, fc-label, fc-value, fc-amount, fc-card-actions, fc-overdue, fc-clickable, fc-sub-list, fc-sub-card, fc-empty) plus the Finance-scoped `.table-scroll-container` hide rule at <=768px — scoped to #approvals-section, #pos-section, #projects-section, #payables-section only, leaving Procurement/Suppliers/MRF Records unaffected
- Added `buildMaterialPRCard(pr)` and `buildTRCard(tr)` helpers in `app/views/finance.js` producing fc-* card HTML with urgency badge, dept badge, all key fields, and full-width Review PR / Review TR button (min-height 44px) — UI-SPEC CTA labels and touch target compliant
- Extended `renderMaterialPRs()` and `renderTransportRequests()` to populate their respective card lists alongside the existing tbody — dual-mode: tables on desktop, cards on mobile, no JS viewport detection

## Task Commits

1. **Task 1: Add fc-* CSS classes and Finance-scoped hide rule** - `bee314f` (feat)
2. **Task 2: Add card-list sibling DOM and populate Material PR / TR card lists** - `d306efe` (feat)

## Files Created/Modified

- `styles/views.css` — Appended Phase 73.1 Finance Card Layout block (lines 1674-1813); all fc-* classes; Finance-scoped hide rule; desktop default .fc-card-list display:none
- `app/views/finance.js` — Added buildMaterialPRCard() and buildTRCard() helpers; added fc-card-list sibling divs in render() static HTML; extended renderMaterialPRs() and renderTransportRequests() to populate card lists

## Decisions Made

- UI-SPEC values used over RESEARCH.md draft values per REVIEWS Concern 2: fc-card-body gap 0.25rem, fc-label font-weight 700, fc-sub-card font-size 0.75rem
- No .fc-overdue on Material PR / TR cards — confirmed desktop renderMaterialPRs() and renderTransportRequests() apply zero row-level overdue highlighting (REVIEWS Concern 1 — parity with desktop maintained)
- CTA labels "Review PR" / "Review TR" per UI-SPEC interaction contract (verb + noun convention)

## Deviations from Plan

None — plan executed exactly as written. REVIEWS Concerns 1 and 2 were resolved in the plan text and confirmed during implementation (desktop functions verified to have no overdue row highlighting; UI-SPEC values used verbatim).

## Known Stubs

None — card lists are populated from live in-memory data (materialPRs, transportRequests arrays). Both render functions already call the card population code alongside tbody population on every data update.

## Self-Check: PASSED

- styles/views.css: FOUND
- app/views/finance.js: FOUND
- 73.1-01-SUMMARY.md: FOUND
- Commit bee314f (CSS task): FOUND
- Commit d306efe (JS task): FOUND
