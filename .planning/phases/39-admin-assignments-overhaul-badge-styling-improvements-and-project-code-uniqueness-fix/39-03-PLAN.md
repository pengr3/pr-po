---
phase: 39-admin-assignments-overhaul-badge-styling-improvements-and-project-code-uniqueness-fix
plan: 03
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - app/views/procurement.js
  - app/views/finance.js
  - app/views/home.js
autonomous: true
requirements:
  - BADGE-01
  - BADGE-02

must_haves:
  truths:
    - "All status badges across Procurement, Finance, and Home views use CSS classes instead of inline styles"
    - "PR codes in MRF History are styled as badges colored by finance_status (no separate status badge below)"
    - "Orange = Pending states, Green = Approved/Complete/Delivered, Red = Rejected, Blue = Procuring across all views"
    - "Department badges are unchanged (Projects/Services)"
  artifacts:
    - path: "app/views/procurement.js"
      provides: "Badge-styled PR codes in MRF History + CSS class badges in PO tracking"
      contains: "status-badge"
    - path: "app/views/finance.js"
      provides: "CSS class badges replacing inline style badges"
      contains: "status-badge"
  key_links:
    - from: "app/views/procurement.js"
      to: "app/utils.js:getStatusClass"
      via: "import and call"
      pattern: "getStatusClass"
    - from: "app/views/finance.js"
      to: "app/utils.js:getStatusClass"
      via: "import and call"
      pattern: "getStatusClass"
---

<objective>
Sweep all status badge rendering across Procurement, Finance, and Home views to use the unified CSS class system instead of ad-hoc inline styles, and redesign MRF History PR codes as badge-styled links.

Purpose: Establish consistent visual language where orange = pending, green = approved/complete, red = rejected, blue = in-progress across every table in the application. Replace the stacked "PR code + badge" pattern in MRF History with PR code as badge.
Output: Updated procurement.js, finance.js, and home.js with unified badge classes.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-admin-assignments-overhaul-badge-styling-improvements-and-project-code-uniqueness-fix/39-CONTEXT.md
@.planning/phases/39-admin-assignments-overhaul-badge-styling-improvements-and-project-code-uniqueness-fix/39-RESEARCH.md
@.planning/phases/39-admin-assignments-overhaul-badge-styling-improvements-and-project-code-uniqueness-fix/39-01-SUMMARY.md
@app/views/procurement.js
@app/views/finance.js
@app/views/home.js
@app/utils.js
@app/components.js
@styles/components.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge sweep in procurement.js — MRF History PR-code-as-badge + inline style cleanup</name>
  <files>app/views/procurement.js</files>
  <action>
Ensure `getStatusClass` is imported from `utils.js` (check existing imports). Then make the following changes:

**Change 1: MRF History PR column — PR code as badge (around line 2481-2497)**

Replace the current stacked layout (PR code link + separate status badge span) with the PR code itself styled as a badge. The link becomes the badge:

Before pattern:
```javascript
return `<div style="display: flex; flex-direction: column; gap: 2px; ...">
    <a href="..." onclick="..." style="color: #1a73e8; ...">${pr.pr_id}</a>
    ${badgeText ? `<span style="background: ${badgeColor}; color: white; ...">${badgeText}</span>` : ''}
</div>`;
```

After pattern:
```javascript
const statusClass = getStatusClass(pr.finance_status || 'Pending');
return `<a href="javascript:void(0)"
    onclick="window.viewPRDetails('${pr.docId}')"
    class="status-badge ${statusClass}"
    style="font-size: 0.75rem; text-decoration: none; cursor: pointer; display: inline-block;">
    ${pr.pr_id}
</a>`;
```

The color communicates status — no tooltip or extra text needed (per user decision). The link remains clickable to open PR details.

**Change 2: PO History inline styles (around line 497)**

Look for inline `background:` styles on procurement_status badges in the PO history/records area. Replace with `class="status-badge ${getStatusClass(po.procurement_status)}"` pattern. Use `createStatusBadge()` from `components.js` where applicable.

**Change 3: PO Tracking table statusColors object (around line 3783)**

Per research recommendation: LEAVE the `statusColors` inline map for the editable `<select>` element AS-IS. That is a form control color hint, not a status badge. The badge sweep applies only to static display spans.

Scan the rest of procurement.js for any other inline `background:` + `color:` patterns on status badges (not department badges). Replace each with the CSS class pattern. Do NOT touch department badges (getDeptBadgeHTML output).

**Import check:** Ensure `getStatusClass` is in the import list from `'../utils.js'`. If `createStatusBadge` from `components.js` is useful for any replacements, import it too.
  </action>
  <verify>
    <automated>grep -n "badgeColor\|badgeText" app/views/procurement.js | grep -c "badge" | xargs -I{} test {} -eq 0 && echo "PASS: Stacked badge pattern removed" || echo "Check remaining badgeColor/badgeText references"</automated>
    <manual>Open Procurement view, check MRF History tab — PR codes should appear as colored badges (orange for Pending, green for Approved, red for Rejected). No separate status text below the code.</manual>
  </verify>
  <done>PR codes in MRF History render as badge-styled links colored by finance_status. Inline status badge styles in procurement.js replaced with CSS class system. Department badges unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Badge sweep in finance.js + home.js — replace inline badge styles with CSS classes</name>
  <files>app/views/finance.js, app/views/home.js</files>
  <action>
**finance.js changes:**

1. **Line ~1168, ~1221 area**: Find inline `background: #fef3c7; color: #f59e0b;` (or similar hardcoded "Pending" badge styles). Replace with `class="status-badge pending"`. Use `createStatusBadge('Pending')` if the element is a simple `<span>`.

2. **Line ~949 area**: Find `badge-secondary` usage. This class now exists in CSS (added in Plan 01). Verify the usage is correct — if it's used for a neutral/informational badge, it should work as-is. No code change needed if the class reference is already correct.

3. Scan all of finance.js for other inline `background:` + `color:` patterns on status-related elements (not department badges). Replace with `class="status-badge ${getStatusClass(status)}"` pattern.

4. Ensure `getStatusClass` is imported from `utils.js` (check existing imports at top of file).

**home.js changes:**

1. Check if home.js renders any status badges. The dashboard typically shows stat cards, not badge-styled statuses. If it uses inline styles for any status indicators, replace with CSS class pattern.

2. If home.js has no inline badge styles, no changes needed — document this in the summary.

**General sweep rules:**
- Only replace STATIC DISPLAY badges (spans, divs showing status text)
- Do NOT touch interactive elements (select dropdowns, inputs)
- Do NOT touch department badges (getDeptBadgeHTML output, Projects/Services badges)
- Ensure the color mapping per user decision: orange=pending, green=approved/complete/delivered, red=rejected, blue=procuring
  </action>
  <verify>
    <automated>grep -n "background: #fef3c7" app/views/finance.js | wc -l | xargs -I{} test {} -eq 0 && echo "PASS: No hardcoded pending badge colors in finance.js" || echo "FAIL: Inline pending badge styles remain"</automated>
    <manual>Open Finance view — check Pending Approvals, Purchase Orders, Historical Data tabs. All status badges should use consistent colors (orange pending, green approved, red rejected). No hardcoded inline color styles.</manual>
  </verify>
  <done>All inline status badge styles in finance.js replaced with CSS class system. home.js checked and updated if needed. Uniform color scheme: orange=pending, green=approved/complete, red=rejected, blue=procuring.</done>
</task>

</tasks>

<verification>
1. MRF History in Procurement view shows PR codes as badge-styled links (no separate status text)
2. PR badge color matches finance_status: orange (Pending), green (Approved), red (Rejected)
3. Finance Pending Approvals uses CSS class badges (no inline background/color styles)
4. PO Tracking status display uses CSS classes (select dropdown colors left as-is)
5. No inline `background: #fef3c7` or similar hardcoded badge colors remain in finance.js
6. Department badges (Projects/Services) are visually unchanged
7. Home dashboard renders correctly (no regressions)
</verification>

<success_criteria>
- Zero inline style status badges remain in finance.js (replaced with CSS classes)
- PR codes in MRF History are badge-styled links colored by status
- All views follow unified color mapping: orange/green/red/blue
- Department badges untouched
- No visual regressions in any view
</success_criteria>

<output>
After completion, create `.planning/phases/39-admin-assignments-overhaul-badge-styling-improvements-and-project-code-uniqueness-fix/39-03-SUMMARY.md`
</output>
