---
phase: 103.1-priority-feed-signal-accuracy
plan: 01
subsystem: api
tags: [firestore, status-transition, denormalization, rules, projects, services]

requires:
  - phase: 103-portfolio-table-redesign
    provides: "the Priority Feed signal contract being amended"
provides:
  - "status_changed_at stamped at every status-transition write site (proposal funnel + 4 gates + manual status, both worlds)"
  - "firestore.rules operations_user allow-list extended with status_changed_at + last_activity_at"
affects: [103.1-02, 103.1-03, 103.1-04]

tech-stack:
  added: []
  patterns:
    - "Denormalized stage-duration clock written at the single proposal chokepoint + the lifecycle gates"
    - "Field-mask write convention: ISO string for projects/services collection writes, serverTimestamp for the gate updateDocs"

key-files:
  created: []
  modified: [app/views/proposals.js, app/views/project-detail.js, app/views/service-detail.js, firestore.rules]

key-decisions:
  - "Stamped at proposals.js::_applyProposalStateTransition (one site covers all proposal transitions for both projects + services)"
  - "ISO string at proposal funnel / saveField / saveServiceField; serverTimestamp at the 4 gates (matches each site's existing convention)"
  - "status_changed_at gated on fieldName==='project_status' in saveField/saveServiceField — incidental edits never reset the stage clock (SC-2)"

patterns-established:
  - "status_changed_at ?? updated_at ?? created_at is the stage-duration clock the signal plans read"

requirements-completed: [D-01, D-02, SC-1, SC-2, SC-7]

duration: 12min
completed: 2026-06-13
---

# Phase 103.1 Plan 01: status_changed_at Backbone + Rules Summary

**Every status transition in both the projects and services worlds now stamps a `status_changed_at` stage-entry timestamp (proposal funnel, 4 lifecycle gates, manual status dropdowns), incidental field edits do not, and firestore.rules permits both new clock fields on the field-masked operations_user path.**

## Performance
- **Duration:** ~12 min
- **Completed:** 2026-06-13
- **Tasks:** 2 auto committed (Task 3 = blocking dev-rules-deploy human-action, pending)
- **Files modified:** 4

## Task Commits
1. **Task 1: proposal funnel + 4 gates + manual status** - `d8aa4d1` (feat)
2. **Task 2: service status + firestore.rules allow-list** - `ab6540c` (feat)

## Write convention per site (authoritative for Plans 03/04 reads)
- `proposals.js::_applyProposalStateTransition` — `status_changed_at: new Date().toISOString()` in the `parentCollection` batch.update (covers ALL proposal transitions, both projects + services)
- `project-detail.js` 4 gates — `status_changed_at: serverTimestamp()` (Firestore Timestamp)
- `project-detail.js::saveField` — `status_changed_at: new Date().toISOString()`, gated on `fieldName === 'project_status'`
- `service-detail.js::saveServiceField` — `status_changed_at: new Date().toISOString()`, gated on `fieldName === 'project_status'`
- → mixed shapes (ISO string + Timestamp); `normalizeUpdatedAt` already handles both, so the signal reads are convention-agnostic.

## firestore.rules
- projects operations_user `affectedKeys().hasOnly([...])` (line ~225) now includes `'status_changed_at'` and `'last_activity_at'` (the latter for Plan 02's journal writes). Braces balanced 69/69.
- **Services rule UNCHANGED** — the services update rule is role-only (no field-mask), so status_changed_at is already permitted; no new trust surface on services.

## Decisions Made
None beyond plan — executed as written.

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None.

## ⚠ BLOCKING — dev rules deploy required (Task 3, human-action)
The status_changed_at writes from an assigned operations_user (and Plan 02's last_activity_at) will be
**rejected** until the updated rules are live on dev. Run before the Plan 03/04 browser UAT:
`firebase deploy --only firestore:rules --project dev`  (CLI active project is PROD — `--project dev` is mandatory).
This dev deploy unblocks UAT only; the prod deploy still happens at the v3.3 → main merge (carries the
standing 87.4/99/100/101/102 rules-deploy debt).

## Next Phase Readiness
- Plans 03/04 can read `status_changed_at ?? updated_at ?? created_at`; Plan 02 can write `last_activity_at` (rules allow it).
- **Blocking dev-rules-deploy gate pending.**

---
*Phase: 103.1-priority-feed-signal-accuracy*
*Completed (code): 2026-06-13 — dev rules deploy pending*
