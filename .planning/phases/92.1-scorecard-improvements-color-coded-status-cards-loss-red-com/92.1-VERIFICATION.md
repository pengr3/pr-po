---
phase: 92.1-scorecard-improvements-color-coded-status-cards-loss-red-com
verified: 2026-05-18T00:00:00Z
status: human_needed
score: 10/11 must-haves verified (1 requires human browser test)
overrides_applied: 0
human_verification:
  - test: "Click each of the 5 colored scorecard cards (Loss, Completed, On-going, For Mobilization, Client Approved) and hover over each"
    expected: "Hover state uses the status color (e.g. Loss card hover is #fee2e2 background, not generic #f0f7ff blue). Active/clicked state uses status color (e.g. Loss active is #fecaca, not generic #e8f0fe blue). White/neutral cards (For Inspection, For Proposal, Proposal for Internal Approval, Proposal Under Client Review, For Revision) still show the generic blue hover/active."
    why_human: "CSS specificity correctness (0-2-1 beats 0-1-0) is verified from code, but actual rendered override behavior in-browser can only be confirmed visually or via DevTools computed styles."
  - test: "Log in as operations_user role, navigate to Projects tab, check scorecard Total count vs table row count"
    expected: "Scorecard Total count equals the number of rows visible in the table (role-scoped projects only), not the total project count across all roles. Applying Client or Search filters changes the table rows but does NOT change the scorecard counts."
    why_human: "Role-based visibility behavior (getAssignedProjectCodes returning scoped list vs null) cannot be verified from static code analysis alone — requires an operations_user session with assignments in Firestore."
---

# Phase 92.1: Scorecard Color-Coding + Visibility Scope Fix — Verification Report

**Phase Goal:** Scorecard improvements — color-coded status cards (Loss=red, Completed=blue, On-going=yellow, For Mobilization=orange, Client Approved=green) and fix visibility-scoped counts for operations_user role.
**Verified:** 2026-05-18
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Loss card has red left border (#ef4444) and red-tinted background (#fff5f5) | VERIFIED | `views.css:3036-3038` — `border-left: 3px solid #ef4444; background-color: #fff5f5` |
| 2 | Completed card has brand-blue left border (#1a73e8) and blue-tinted background (#e8f0fe) | VERIFIED | `views.css:3049-3051` — `border-left: 3px solid #1a73e8; background-color: #e8f0fe` |
| 3 | On-going card has gold left border (#eab308) and yellow-tinted background (#fefce8) | VERIFIED | `views.css:3062-3064` — `border-left: 3px solid #eab308; background-color: #fefce8` |
| 4 | For Mobilization card has orange left border (#f97316) and orange-tinted background (#fff8f3) | VERIFIED | `views.css:3075-3077` — `border-left: 3px solid #f97316; background-color: #fff8f3` |
| 5 | Client Approved card has green left border (#16a34a) and green-tinted background (#f0fdf4) | VERIFIED | `views.css:3088-3090` — `border-left: 3px solid #16a34a; background-color: #f0fdf4` |
| 6 | Other 5 status cards remain white/neutral — no data-status rules for For Inspection, For Proposal, Proposal for Internal Approval, Proposal Under Client Review, For Revision | VERIFIED | Grep for those status strings in views.css returned zero matches |
| 7 | Hover and active states on colored cards use the status color, not generic blue (code-level: correct selectors and color values present) | VERIFIED (code) / UNCERTAIN (visual) | `views.css:3040-3046, 3053-3059, 3066-3072, 3079-3085, 3092-3098` — each has `:hover` and `--active` overrides with correct status border-color. Specificity 0-2-1 beats generic 0-1-0 — no !important needed. Visual rendering requires human check. |
| 8 | No !important in the new scorecard color rules | VERIFIED | Grep for `!important` from line 3033 onward returned no matches |
| 9 | renderScorecards() receives baseProjects parameter, not allProjects | VERIFIED | `projects.js:734` — `function renderScorecards(baseProjects) {`; body uses `baseProjects.filter` (line 736) and `baseProjects.length` (line 741) only |
| 10 | No allProjects.filter or allProjects.length inside renderScorecards() | VERIFIED | Full function body (lines 734-749) contains zero references to `allProjects` |
| 11 | The single renderScorecards() call in applyFilters() passes scopedProjects | VERIFIED | `projects.js:805` — `renderScorecards(scopedProjects);` — only call site in the entire file |

**Score:** 10/11 code-verifiable truths confirmed. 1 truth (visual hover/active behavior) and 1 behavioral item (operations_user role scoping) require human browser testing.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `styles/views.css` | Per-status color rules appended after line 3033 | VERIFIED | 67-line block added starting at line 3034 with `/* Per-status color coding for Phase 92.1 */` comment header |
| `app/views/projects.js` | renderScorecards signature + call site updated | VERIFIED | Signature at line 734 accepts `baseProjects`; call at line 805 passes `scopedProjects` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `applyFilters()` | `renderScorecards()` | `renderScorecards(scopedProjects)` at line 805 | WIRED | `scopedProjects` built from `allProjects` (admin) or `allProjects.filter(assignedCodes)` (operations_user) before being passed |
| `renderScorecards(baseProjects)` | DOM scorecard count elements | `baseProjects.filter(p => p.project_status === s).length` | WIRED | Counts derived from `baseProjects` parameter, not module-level `allProjects` |
| CSS `[data-status="..."]` selectors | Scorecard card DOM elements | `data-status` attribute on `.project-scorecard-card` elements | WIRED | Attribute selectors match existing `data-status` attribute used by Phase 92 card rendering |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `renderScorecards` | `baseProjects` | `scopedProjects` in `applyFilters()`, sourced from `allProjects` Firestore snapshot | Yes — `allProjects` is populated from `onSnapshot` at line 885-887 | FLOWING |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED — no runnable entry points (zero-build static SPA, Firebase-dependent; cannot run meaningful spot-checks without a live server and Firestore session).

---

### Probe Execution

Step 7c: No probes declared or conventionally present for this phase.

---

### Requirements Coverage

No requirement IDs declared for this phase. N/A.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

Scanned `styles/views.css` (lines 3034-3099) and `app/views/projects.js` (lines 734-805) for TBD, FIXME, XXX, TODO, HACK, placeholder, return null/\[\]/\{\}, hardcoded empty state. No anti-patterns found.

---

### Human Verification Required

#### 1. Colored card hover and active visual rendering

**Test:** Open the Projects tab in a browser. Hover over the Loss card. Click the Loss card to activate it. Repeat for Completed, On-going, For Mobilization, and Client Approved cards.

**Expected:**
- Loss hover: background shifts to a deeper red (#fee2e2), border stays red (#ef4444) — NOT the generic light blue (#f0f7ff)
- Loss active: background #fecaca, border #ef4444
- Completed hover: #d2e3fc background, border #1a73e8
- Completed active: #b3cef9 background, border #1a73e8
- On-going hover: #fef9c3 background, border #eab308
- On-going active: #fef08a background, border #eab308
- For Mobilization hover: #ffedd5 background, border #f97316
- For Mobilization active: #fed7aa background, border #f97316
- Client Approved hover: #dcfce7 background, border #16a34a
- Client Approved active: #bbf7d0 background, border #16a34a
- Neutral cards (For Inspection, For Proposal, etc.) hover: generic #f0f7ff blue background (unchanged)

**Why human:** CSS specificity override (0-2-1 vs 0-1-0) is confirmed correct by code inspection, but browser computed-styles can reveal cascade surprises (e.g. if another rule intervenes). Only DevTools "Computed" tab or visual observation can confirm the rendered result.

#### 2. operations_user scorecard scope behavior

**Test:** Log in with an account that has the `operations_user` role and limited project assignments. Navigate to the Projects tab. Note the Total count on the scorecard strip. Count the visible rows in the project table.

**Expected:**
- Scorecard Total equals the number of rows in the table (scoped count), not the full database project count
- Applying a Client filter or typing in the Search box changes the table rows but the scorecard counts remain the same (they reflect scope, not filter results)
- As admin/super_admin, scorecard Total still equals the full project count (no regression)

**Why human:** `getAssignedProjectCodes()` behavior depends on the authenticated user's Firestore document and role claims at runtime. The code path is correct (scopedProjects is passed at line 805), but the actual filtering outcome for a real operations_user session can only be confirmed with a live user account that has assignment data in Firestore.

---

### Gaps Summary

No code-level gaps found. All 11 must-haves are satisfied at the implementation level:

- All 5 colored status rules present in `styles/views.css` with exact specified color values
- All 5 active override rules present with correct `border-color` values matching the status color
- No `!important` in the new section
- Existing base scorecard rules (`.project-scorecard-card`, `:hover`, `--active`) unchanged at lines 2983-3032
- `renderScorecards(baseProjects)` signature confirmed, body references only `baseProjects`
- Single call site at `applyFilters():805` passes `scopedProjects`

Two items require human browser testing before the phase can be marked fully passed: visual hover/active color override rendering and operations_user role-scoped count behavior.

---

_Verified: 2026-05-18_
_Verifier: Claude (gsd-verifier)_
