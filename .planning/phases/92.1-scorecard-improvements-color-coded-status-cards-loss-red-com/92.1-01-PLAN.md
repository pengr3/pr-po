---
plan_id: 92.1-01
phase: "92.1"
objective: Color-code five status scorecard cards and fix visibility-scoped counts
wave: 1
depends_on: []
files_modified:
  - styles/views.css
  - app/views/projects.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Loss card has red left border (#ef4444) and red-tinted background"
    - "Completed card has blue left border (#3b82f6) and blue-tinted background"
    - "On-going card has yellow left border (#f59e0b) and yellow-tinted background"
    - "For Mobilization card has orange left border (#f97316) and orange-tinted background"
    - "Client Approved card has green left border (#059669) and green-tinted background"
    - "Other 5 status cards remain white/neutral (no status color)"
    - "Hover and active states on colored cards use the status color, not generic blue"
    - "renderScorecards() receives scopedProjects (role-visibility-scoped) not allProjects"
    - "operations_user sees scorecard counts matching their table scope"
    - "Client/Search filters do NOT affect scorecard counts"
---

# Plan 92.1-01: Scorecard Color-Coding + Visibility Scope Fix

## Background

Phase 92 built the scorecard strip (monochrome, blue active state).
Phase 92.1 adds two improvements:

1. Per-status color coding for 5 statuses (requested post-92 polish)
2. Scorecard counts respect role-based visibility (bug: `renderScorecards()` uses
   `allProjects`, so `operations_user` sees inflated counts from projects outside
   their assignment scope)

---

## Task 1 — Add per-status color CSS rules

<read_first>
- styles/views.css (lines 2976–3033 — all existing scorecard rules; understand current hover/active definitions before adding overrides)
</read_first>

<action>
In `styles/views.css`, append a new section after line 3033 (after the existing
`@media (max-width: 768px)` scorecard block):

Section header comment:
`/* Per-status color coding for Phase 92.1 */`

Add rules for each of the 5 colored statuses. Each status needs 3 rules:
1. Default state: `border-left: 3px solid {color}; background-color: {default-bg};`
2. Hover override: `background-color: {hover-bg}; border-color: {color};`
3. Active override: `background-color: {active-bg}; border-color: {color};`

Selectors use the existing `data-status` attribute already on every card.

Colors per status:
- Loss: border `#ef4444`, bg default `#fff5f5`, hover `#fee2e2`, active `#fecaca`
- Completed: border `#3b82f6`, bg default `#eff6ff`, hover `#dbeafe`, active `#bfdbfe`
- On-going: border `#f59e0b`, bg default `#fffbf0`, hover `#fef3c7`, active `#fde68a`
- For Mobilization: border `#f97316`, bg default `#fff8f3`, hover `#ffedd5`, active `#fed7aa`
- Client Approved: border `#059669`, bg default `#f0fdf8`, hover `#d1fae5`, active `#a7f3d0`

Active selector example:
`.project-scorecard-card[data-status="Loss"].project-scorecard-card--active`
(specificity 0-2-1 beats `.project-scorecard-card--active` at 0-1-0 — no !important needed)

IMPORTANT: The `data-status` attribute value must be the EXACT status string as
defined in `UNIFIED_STATUS_OPTIONS`. For "For Mobilization" use `[data-status="For Mobilization"]`,
for "On-going" use `[data-status="On-going"]`, for "Client Approved" use `[data-status="Client Approved"]`.

Do NOT add rules for the 5 uncolored statuses (For Inspection, For Proposal,
Proposal for Internal Approval, Proposal Under Client Review, For Revision) or
the Total card.
</action>

<acceptance_criteria>
- styles/views.css contains `.project-scorecard-card[data-status="Loss"]` with `border-left: 3px solid #ef4444`
- styles/views.css contains `.project-scorecard-card[data-status="Loss"]` with `background-color: #fff5f5`
- styles/views.css contains `.project-scorecard-card[data-status="Loss"].project-scorecard-card--active` with `border-color: #ef4444`
- styles/views.css contains rules for Completed, On-going, For Mobilization, Client Approved (5 statuses total with their 3 rules each = 15 new rule blocks)
- No `!important` used (specificity handles override naturally)
- Existing rules for `.project-scorecard-card`, `.project-scorecard-card:hover`, `.project-scorecard-card--active` are unchanged
</acceptance_criteria>

---

## Task 2 — Fix renderScorecards() to use visibility-scoped project counts

<read_first>
- app/views/projects.js (lines 733–760 — renderScorecards function)
- app/views/projects.js (lines 762–806 — applyFilters function; note how scopedProjects is built from getAssignedProjectCodes before filteredProjects is computed)
</read_first>

<action>
Two targeted edits to `app/views/projects.js`:

**Edit 1 — renderScorecards() signature: add `baseProjects` parameter**

Change function signature at line 734 from:
`function renderScorecards() {`
to:
`function renderScorecards(baseProjects) {`

Inside the function, replace both references to `allProjects`:
- Line 736: `const count = allProjects.filter(p => p.project_status === s).length;`
  → `const count = baseProjects.filter(p => p.project_status === s).length;`
- Line 740: `if (totalEl) totalEl.textContent = allProjects.length;`
  → `if (totalEl) totalEl.textContent = baseProjects.length;`

Do NOT change anything else in renderScorecards (the active highlight logic stays as-is).

**Edit 2 — applyFilters() call site: pass scopedProjects**

Find the `renderScorecards();` call inside `applyFilters()` (line 805).
Change it to: `renderScorecards(scopedProjects);`

`scopedProjects` is already a local variable in `applyFilters()` (built at the top of
the function from `getAssignedProjectCodes()`). Passing it here ensures scorecard counts
reflect the user's visibility scope (ignoring Client/Search filters, which are applied
to `filteredProjects` downstream).
</action>

<acceptance_criteria>
- app/views/projects.js `renderScorecards` function signature is `function renderScorecards(baseProjects) {`
- No occurrence of `allProjects.filter` or `allProjects.length` inside `renderScorecards()`
- The single `renderScorecards()` call in `applyFilters()` passes `scopedProjects` as argument
- No other call sites for `renderScorecards` exist in the file (grep confirms only one call)
- For admin/finance/procurement roles: `getAssignedProjectCodes()` returns null → `scopedProjects === allProjects` → counts unchanged from before
- For operations_user: `scopedProjects` is filtered → scorecard counts match the table scope
</acceptance_criteria>

---

## Verification

After both tasks complete:

1. Open the Projects tab in the browser
2. Verify 5 cards have visible color accents:
   - Loss card: red tint
   - Completed card: blue tint
   - On-going card: yellow tint
   - For Mobilization card: orange tint
   - Client Approved card: green tint
3. Verify the other 5 cards (For Inspection, For Proposal, etc.) are white/neutral
4. Click each colored card → verify the active state uses the status color (not generic blue)
5. Click outside the scorecard strip → verify filter resets (existing behavior)
6. As admin/super_admin: scorecard Total count matches total project count
7. (Role test if available) As operations_user: scorecard Total matches table row count, not allProjects count
