---
plan_id: 92.1-01
phase: "92.1"
status: complete
completed: "2026-05-18"
commits:
  - 5bcb42a
  - dfd4b84
key-files:
  modified:
    - styles/views.css
    - app/views/projects.js
---

# Summary: Plan 92.1-01 — Scorecard Color-Coding + Visibility Scope Fix

## What Was Built

**Task 1 — Per-status color CSS rules** (`styles/views.css`)

Appended 67 lines after the existing scorecard `@media` block. Five statuses now have distinct visual treatment via `[data-status="..."]` attribute selectors:

| Status | Border | Default bg | Hover bg | Active bg |
|--------|--------|------------|----------|-----------|
| Loss | `#ef4444` | `#fff5f5` | `#fee2e2` | `#fecaca` |
| Completed | `#1a73e8` | `#e8f0fe` | `#d2e3fc` | `#b3cef9` |
| On-going | `#eab308` | `#fefce8` | `#fef9c3` | `#fef08a` |
| For Mobilization | `#f97316` | `#fff8f3` | `#ffedd5` | `#fed7aa` |
| Client Approved | `#16a34a` | `#f0fdf4` | `#dcfce7` | `#bbf7d0` |

Specificity (0-2-1) beats the generic hover/active rules (0-1-0) — no `!important` needed.

**Task 2 — Visibility scope fix** (`app/views/projects.js`)

- `renderScorecards()` → `renderScorecards(baseProjects)`: counts from `baseProjects` instead of module-level `allProjects`
- Call site in `applyFilters()` changed to `renderScorecards(scopedProjects)`
- `scopedProjects` is already built in `applyFilters()` from `getAssignedProjectCodes()` — minimal change, no new variables

## Deviations

None — implemented exactly as specified in PLAN and CONTEXT.

## Self-Check: PASSED

- [x] `renderScorecards` signature: `function renderScorecards(baseProjects)` ✓
- [x] No `allProjects.filter` or `allProjects.length` inside `renderScorecards` ✓
- [x] Single call site passes `scopedProjects` ✓
- [x] CSS contains all 5 data-status selectors with correct colors ✓
- [x] No `!important` in scorecard color section ✓
- [x] Existing `.project-scorecard-card`, `:hover`, `--active` rules unchanged ✓
