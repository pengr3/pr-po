---
phase: 86.5
plan: 04
type: execute
wave: 3
depends_on:
  - "86.5-03"
files_modified:
  - app/views/project-plan.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Dragging the blue dot leftward on empty space reduces the task's end_date by the dragged distance in days"
    - "End date never falls below start_date — minimum 1-day duration preserved"
    - "Rightward drag still extends end_date (Plan 03 behavior preserved)"
    - "Drop on another bar still creates FS predecessor link (Phase 86.1 behavior preserved)"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "leftward-drag reduces duration; rightward extends; both clamped"
      contains: "bar resize failed"
  key_links:
    - from: "svg mouseup empty-space branch"
      to: "updateDoc project_tasks end_date"
      via: "signed daysDelta → clamped newEndDate → updateDoc"
      pattern: "bar resize failed"
---

<objective>
Extend Plan 03's bar-extend gesture to also reduce duration on leftward drag. Currently leftward drops on empty space cancel silently. After this change, leftward reduces end_date by the dragged distance, clamped so end_date never falls below start_date (1-day minimum).

Purpose: UAT feedback (item 3) — user expects symmetric drag behavior. Same gesture, signed delta, same Firestore path.
Output: One block in `app/views/project-plan.js` mouseup handler is updated to compute a signed daysDelta and write a clamped end_date.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace extend-only block with bidirectional resize (extend or reduce)</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    Read app/views/project-plan.js lines 1605-1635 — the `if (!toWrapper) { ... }` block inside the svg mouseup handler in initGanttDragLink(). This block was added by Plan 86.5-03.
  </read_first>
  <action>
    In app/views/project-plan.js, locate the `if (!toWrapper) { ... }` block (approximately lines 1607-1635). Replace its entire body (everything between `if (!toWrapper) {` and the matching `}`) with the following block:

        // Dropped on empty space — extend (rightward) or reduce (leftward) duration
        if (!gantt || typeof gantt.config !== 'object') return;
        const fromTask = tasks.find(x => x.task_id === fromTaskId);
        if (!fromTask || !fromTask.start_date || !fromTask.end_date) return;
        const fromWrapper2 = svg.querySelector(`.bar-wrapper[data-id="${CSS.escape(fromTaskId)}"]`);
        const fromBar2 = fromWrapper2?.querySelector('.bar');
        if (!fromBar2) return;
        const barRect2 = fromBar2.getBBox();
        const barRightX = barRect2.x + barRect2.width;
        const pt2 = svg.createSVGPoint();
        pt2.x = e.clientX;
        pt2.y = e.clientY;
        const svgPt2 = pt2.matrixTransform(svg.getScreenCTM().inverse());
        const xPerDay2 = (gantt.config.column_width || 45) / (gantt.config.step || 1);
        const daysDelta = Math.round((svgPt2.x - barRightX) / xPerDay2);
        if (daysDelta === 0) return; // no meaningful change
        let newEndDate = addDays(fromTask.end_date, daysDelta);
        // Clamp: end_date must not be earlier than start_date (1-day minimum duration, end == start)
        if (newEndDate < fromTask.start_date) newEndDate = fromTask.start_date;
        if (newEndDate === fromTask.end_date) return; // clamp produced no change
        try {
            await updateDoc(doc(db, 'project_tasks', fromTaskId), { end_date: newEndDate, updated_at: serverTimestamp() });
            const idx2 = tasks.findIndex(x => x.task_id === fromTaskId);
            if (idx2 >= 0) tasks[idx2] = { ...tasks[idx2], end_date: newEndDate };
            if (fromTask.parent_task_id) await recomputeParentDates(fromTask.parent_task_id);
        } catch (err) {
            console.error('[ProjectPlan] bar resize failed:', err);
            showToast('Could not adjust task duration.', 'error');
        }
        return;

    Notes:
    - `daysDelta` is signed: positive = rightward (extend), negative = leftward (reduce).
    - `addDays(date, negative)` works correctly — `addDays` accepts signed days (verified in existing usage elsewhere in the file).
    - String comparison on ISO dates (`YYYY-MM-DD`) is correct for chronological clamp because the format is lexicographically ordered.
    - The error toast message is generalized from "extend" to "adjust" since the gesture now does both.
    - Console error key changes from "bar extend failed" to "bar resize failed" — this is a deliberate rename to match the broader behavior.

    Do NOT modify any other part of the mouseup handler or initGanttDragLink().
  </action>
  <verify>
    grep -n "bar resize failed" app/views/project-plan.js
    grep -n "bar extend failed" app/views/project-plan.js
    Expected: one match for "bar resize failed"; zero matches for "bar extend failed".
  </verify>
  <acceptance_criteria>
    - `grep -c "bar resize failed" app/views/project-plan.js` returns 1
    - `grep -c "bar extend failed" app/views/project-plan.js` returns 0
    - `grep -c "Could not adjust task duration" app/views/project-plan.js` returns 1
    - `grep -c "Could not extend task duration" app/views/project-plan.js` returns 0
    - `grep -c "daysDelta" app/views/project-plan.js` returns at least 2 (declaration + use)
    - `grep -c "dragged leftward — cancel silently" app/views/project-plan.js` returns 0 (old leftward guard removed)
    - `grep -c "no meaningful change" app/views/project-plan.js` returns 1
    - `git diff --stat` shows only `app/views/project-plan.js` changed
  </acceptance_criteria>
  <done>
    The if (!toWrapper) block now: extends end_date for rightward drops, reduces end_date for leftward drops (clamped at start_date), no-ops for zero-day delta or clamp-no-change, surfaces a toast on Firestore errors. Bar-to-bar predecessor link behavior is untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG coords → Firestore write | Same path as Plan 03; clamp prevents end_date < start_date |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.5-04-01 | Tampering | project_tasks end_date | mitigate | Same path as Plan 03; client-side clamp `newEndDate < start_date → start_date` prevents zero/negative durations |
</threat_model>

<verification>
1. Open a project plan view with a task whose bar spans multiple days
2. Hover the bar — blue dot appears at right edge
3. Drag from the dot LEFTWARD and release on empty timeline space — bar should shrink; toast should NOT appear
4. Drag the leftmost task all the way left so it would go below 1-day duration — bar should clamp at start_date (1-day duration), no error
5. Drag from the dot RIGHTWARD — bar should still extend (Plan 03 behavior preserved)
6. Drag from the dot onto another bar — FS predecessor link is created (Phase 86.1 behavior preserved)
7. Check DevTools console for any unexpected errors
</verification>

<success_criteria>
- Leftward drag reduces end_date proportional to drag distance, clamped at start_date
- Rightward drag still extends (Plan 03 unchanged in effect)
- Bar-to-bar drag still creates FS predecessor link
- No new errors or toasts on success paths
</success_criteria>

<output>
After completion, create `.planning/phases/86.5-gantt-ui-polish-3-header-drag-back-excel/86.5-04-SUMMARY.md`
</output>
