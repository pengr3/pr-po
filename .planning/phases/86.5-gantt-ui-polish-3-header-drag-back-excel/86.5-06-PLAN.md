---
phase: 86.5
plan: 06
type: execute
wave: 4
depends_on:
  - "86.5-04"
files_modified:
  - app/views/project-plan.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Dragging the blue dot leftward over the source bar's own area enters the resize path (not silent self-link reject)"
    - "Dragging the blue dot onto a different bar still creates an FS predecessor link"
    - "Dragging onto empty space still routes to resize (Plan 03/04 behavior preserved)"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "self-bar drop now triggers resize instead of silent self-link reject"
      contains: "self-bar drop routes to resize"
---

<objective>
Plan 04's leftward-drag resize never fires because dropping the blue dot leftward almost always lands ON the source bar's own wrapper — not on empty space. The mouseup handler's first branch (`if (!toWrapper)`) is missed; control falls through to `if (toTaskId === fromTaskId) return;` (silent self-link reject). The resize block is unreachable for leftward drags.

Fix: route self-bar drops into the same resize branch as empty-space drops. The existing resize block uses `fromTaskId` (not `toWrapper`) to compute geometry, so it works correctly for both cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route self-bar drops into the resize branch</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    Read app/views/project-plan.js around lines 1612-1647 — the start of the svg `mouseup` handler. Confirm the current shape:
    - line ~1613: `const toWrapper = e.target.closest('.bar-wrapper');`
    - line ~1614: `if (!toWrapper) {` … resize block … `return; }`
    - line ~1646: `const toTaskId = toWrapper.dataset.id;`
    - line ~1647: `if (!toTaskId || toTaskId === fromTaskId) return; // self-link — reject silently`
  </read_first>
  <action>
    Make TWO surgical changes to `app/views/project-plan.js`.

    CHANGE 1 — at the line that starts the resize branch (~line 1614), expand the condition so self-bar drops also enter:

        if (!toWrapper) {

    becomes

        if (!toWrapper || toWrapper.dataset.id === fromTaskId) {
            // self-bar drop routes to resize (Plan 86.5-06): leftward drag almost always lands
            // on the source bar's own wrapper, not empty space — both should resize, not self-link.

    Keep the rest of the resize block exactly as it is (it uses `fromTaskId` to query its own geometry, not `toWrapper`).

    CHANGE 2 — after the resize branch returns, the line `if (!toTaskId || toTaskId === fromTaskId) return; // self-link — reject silently` (~line 1647) is now redundant for the `===` case (the new condition catches it earlier). Tighten it to only handle the `!toTaskId` case:

        if (!toTaskId || toTaskId === fromTaskId) return; // self-link — reject silently

    becomes

        if (!toTaskId) return; // wrapper without id — reject silently

    The `=== fromTaskId` check is no longer reachable because CHANGE 1 routes that case into the resize branch and returns from there.

    Do NOT modify any other line in the mouseup handler or the surrounding function.
  </action>
  <verify>
    grep -n "self-bar drop routes to resize" app/views/project-plan.js
    grep -n "wrapper without id — reject silently" app/views/project-plan.js
    grep -n "self-link — reject silently" app/views/project-plan.js
    Expected: one match for "self-bar drop routes to resize"; one match for "wrapper without id"; zero matches for the old "self-link — reject silently" comment.
  </verify>
  <acceptance_criteria>
    - `grep -c "self-bar drop routes to resize" app/views/project-plan.js` returns 1
    - `grep -c "wrapper without id — reject silently" app/views/project-plan.js` returns 1
    - `grep -c "self-link — reject silently" app/views/project-plan.js` returns 0
    - `grep -c "toWrapper.dataset.id === fromTaskId" app/views/project-plan.js` returns 1
    - `git diff --stat` shows only `app/views/project-plan.js` changed
  </acceptance_criteria>
  <done>
    Self-bar drops route to the resize branch, so leftward drags reduce duration. Different-bar drops still hit the predecessor-link branch. Wrappers with no `data-id` still reject silently.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.5-06-01 | Tampering | project_tasks end_date | accept | Same write path as Plan 04; no new attack surface |
</threat_model>

<verification>
1. Open a project plan, hover a multi-day bar, drag the blue dot leftward over the bar itself, release on the source bar's left half — bar should shrink (clamped at start_date)
2. Drag the blue dot rightward beyond the bar's right edge into empty space — bar should still extend (Plan 03/04 regression)
3. Drag the blue dot onto a different task's bar — FS predecessor link should still be created
4. Console should be clean
</verification>

<success_criteria>
- Leftward drag on the source bar shrinks the bar (Plan 04's intent finally fires)
- Rightward extend still works (Plan 03)
- Bar-to-different-bar predecessor link still works (Phase 86.1)
</success_criteria>

<output>
After completion, create `.planning/phases/86.5-gantt-ui-polish-3-header-drag-back-excel/86.5-06-SUMMARY.md`
</output>
