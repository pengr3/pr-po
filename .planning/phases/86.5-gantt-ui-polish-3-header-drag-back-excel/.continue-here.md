---
context: phase
phase: 86.5-gantt-ui-polish-3-header-drag-back-excel
task: 0
total_tasks: 0
status: paused_awaiting_browser_uat
last_updated: 2026-05-08T03:15:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered through failure this session — section not applicable._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| GSD worktree spawned at stale base | Two `gsd-executor` agents launched with `isolation="worktree"` were placed inside worktrees rooted at commit `721bd52` (predates Phase 83), so the plan files and target source were missing. Plan 02's executor halted correctly; Plan 01's executor worked around by writing in the parent checkout. | advisory | When spawning executor agents in this repo, omit `isolation="worktree"` and run directly on `v3.3`. The worktree tooling here can pin agents to an outdated commit base; until the underlying base-resolution is fixed, no-isolation execution is the safer default. Two stale locked worktrees remain in `.claude/worktrees/` (`agent-a4222795d6b5c5f44`, `agent-ab5541979394bd9bb`) — benign but removable via `git worktree unlock` + `git worktree remove --force`. |
| Plan acceptance-criteria grep counts as arithmetic claim | Several Phase 86.5 plans included `grep -c "X" file == N` counts that were off-by-one because declaration lines (e.g., `let X = false`) match the same regex as use-site assignments. Executors correctly applied the code verbatim and documented the count discrepancy under Deviations. | advisory | When writing plan acceptance criteria, prefer presence/absence checks (`grep -c "...marker comment..." == 1`) over counting identifiers that appear in declarations. Or scope the count via the comment marker. |

<current_state>
Phase 86.5 is FUNCTIONALLY COMPLETE on v3.3. Eight plans executed, all CODE-side acceptance criteria pass, VERIFICATION.md committed for the original 3 plans. The remaining work is exclusively browser-side UAT — no source changes pending. Working tree is clean (the only untracked entry is `.claude/worktrees/`).
</current_state>

<completed_work>

All 8 plans executed and committed on `v3.3`:

- 86.5-01 — CSS header alignment — feat `693936a`, summary `60ff560`
- 86.5-02 — Back link to project detail + Excel _focusNewRowAfterRender flag — feat `01ec8d2`, summary `e484e9d`
- 86.5-03 — Bar extend on rightward empty-space drop — feat `c877b66`, summary `5091fce`
- 86.5-04 — Bar reduce on leftward drag (clamp at start_date) — feat `a551031`, summary `d4dda74`
- 86.5-05 — Excel-style entry: drop showLoading + toast, local-state taskId, optimistic append, rollback — feat `9879382`, summary `d9c7c55`
- 86.5-06 — Leftward drag detect: route self-bar drops to resize (fixes Plan 04 unreachable bug) — feat `a6adf85`, summary `de60e1d`
- 86.5-07 — Suppress transient FS violation toast during drag-link write window — feat `c8be71f`, summary `e209374`
- 86.5-08 — Optimistic merge across snapshot wipes + focus/text preservation in renderTaskGrid — feat `ffaff9e`, summary `fe43ed0`

Plus:
- VERIFICATION.md (after Plans 01-03) — `53c10a8`
- STATE.md updates — `2ccf195`, `7d71a8b`, `fa086ea`
</completed_work>

<remaining_work>

Browser UAT only — see <next_action> for the punch list. No outstanding source code changes.
</remaining_work>

<decisions_made>

- Append plans 04+05 (then 06+07+08) inside Phase 86.5 rather than spawning a new phase — user direction; keeps related UAT contained.
- Plan 06 expands the existing branch condition (`!toWrapper || toWrapper.dataset.id === fromTaskId`) instead of restructuring the handler — surgical fix; existing resize block was already self-bar-safe.
- Plan 07 uses a flag-with-finally pattern (`_suppressFsViolationToast`) and keeps the fingerprint update unconditional so post-window legitimate violations still toast.
- Plan 08 uses a Map (not single flag) keyed by taskId because rapid Enters yield concurrent in-flight writes; finally guarantees cleanup; `_savedEmptyRow` capture prioritizes user's actual typed text over the post-Enter focus flag.
- Worktree isolation skipped for Plans 02-08 due to local worktree tooling spawning at stale base commit; agents executed in main checkout directly.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `.planning/phases/86.5-gantt-ui-polish-3-header-drag-back-excel/VERIFICATION.md` — goal-backward verification of plans 01-03 (does NOT cover 04-08; those landed after this report)
2. `.planning/phases/86.5-gantt-ui-polish-3-header-drag-back-excel/86.5-{04,05,06,07,08}-SUMMARY.md` — implementation summaries for the gap-closure plans
3. `app/views/project-plan.js` — primary file modified by plans 02-08; key regions: `handleNewRowCommit` (~line 594), `renderTaskGrid` (~line 282), `initGanttDragLink` mouseup handler (~line 1605), onSnapshot listener (~line 125), `checkAndToastFsViolations` (~line 1680)

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: spawning gsd-executor with `isolation="worktree"` in this repo → worktree tooling pins to stale base commit; use no-isolation execution on `v3.3` directly until base-resolution is fixed.
- ANTI-PATTERN: writing acceptance-criteria grep counts based on identifier-appears-in-multiple-roles arithmetic → use comment markers (e.g., `grep -c "Phase 86.5-08: capture empty-row input state"`) for unambiguous presence/absence checks.

## Infrastructure State

- Dev server: not running (start with `python -m http.server 8000` for UAT)
- Firebase: production (no staging) — same writes as before, no schema/rule changes
- Branch: `v3.3` — 17 commits ahead of `origin/v3.3` (8 feat, 8 summary docs, plus phase docs/state/verification)
- Worktrees: two locked stale agent worktrees in `.claude/worktrees/` (benign; removable when convenient via `git worktree unlock` + `git worktree remove --force`)

<context>
Phase 86.5 evolved across two UAT cycles after the original 3 plans shipped. UAT-1 (after 01-03) found 4 issues — 2 were already nailed (header alignment, back button), 2 required new plans (bar reduce on leftward drag → 04; Excel smoothness → 05). UAT-2 (after 04-05) found 3 issues — leftward drag still didn't fire because of a routing bug (drop on own bar, not empty space → 06); a false FS violation toast appeared on link creation due to a snapshot-firing-mid-await race (07); rapid Enter still didn't feel continuous because the snapshot listener wiped optimistic rows and the renderTaskGrid replaced focused inputs (08). All three follow-ups are now committed but not yet UAT'd in browser.

The user has paused work after UAT-2's plan execution. Resume should start with browser UAT, not more code changes.
</context>

## Pre-Execution Critique Required

_Not applicable — code is shipped, awaiting validation, no design phase pending._

<next_action>
Start with: `python -m http.server 8000` and walk through the UAT punch list in browser.

UAT punch list (8 items):
1. Drag blue dot leftward over the source bar → bar shrinks proportional to drag distance; cannot reduce below `start_date` (1-day minimum). [tests Plans 04+06]
2. Drag rightward into empty space → bar extends. [Plan 03 regression]
3. Drag onto another task's bar → FS predecessor link is created (existing behavior). [Phase 86.1 regression]
4. Drag predecessor where successor needs auto-shift → no transient "starts before finishes" toast appears. [Plan 07]
5. Manually edit a successor's start_date to overlap → real FS violation toast still fires. [Plan 07 regression check]
6. Empty row → type "A" Enter → immediately type "B" Enter → immediately type "C" Enter — all rows visible continuously, none disappear. [Plans 05+08]
7. Type "A" Enter → start typing "Mid" without Enter → wait for snapshot to fire (~300ms) → "Mid" remains in input with cursor preserved. [Plan 08]
8. Refresh page → all created tasks persist (snapshot reconciliation worked).

If all 8 pass: phase is complete; consider running `/gsd-verify-work` or moving to the next phase.
If any fail: capture symptoms + console output and treat as gap closure for new plans 09+.
</next_action>
