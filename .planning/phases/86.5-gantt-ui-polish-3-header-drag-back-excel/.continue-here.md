---
context: phase
phase: 86.5-gantt-ui-polish-3-header-drag-back-excel
task: UAT
total_tasks: UAT (all 8 plans code-complete)
status: human_needed
last_updated: 2026-05-12T18:20:51Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Windows worktree ban — `isolation="worktree"` is FORBIDDEN on this repo. Windows long path limit breaks worktree creation. Use direct file edits only.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| worktree-on-windows | Using `isolation="worktree"` on Agent tool calls triggers Windows long-path failure. No worktree operations of any kind. | blocking | Never pass `isolation="worktree"` to any Agent call in this repo |

<current_state>
Phase 86.10 is COMPLETE as of 2026-05-12 (all 3 plans shipped, 4 UAT bug fixes, 2 code review BLOCKERs fixed in commit 6d778dd).

Phase 86.5 is CODE-COMPLETE — all 8 plans implemented and VERIFICATION.md exists at human_needed status. All 4 success criteria logically verified in code. Gated on browser UAT only.

We are paused at the Phase 86.5 UAT gate. User needs to run the 5 browser UAT tests from VERIFICATION.md.
</current_state>

<completed_work>

**Phase 86.10 (completed this session):**
- Plan 01: Smart indent inheritance on Enter (handleNewRowCommit inherits parent_task_id from row above)
- Plan 02: Shift+click multi-select + group drag (handleGroupDrop)
- Plan 03: Group context menu + copy/paste rows (_clipboardTasks, gridCopyRows, gridPasteRows, gridGroupIndent/Outdent/Delete)
- 4 UAT bug fixes: group indent chaining, (copy) suffix removal, duplicate paste IDs, paste depth with internal hierarchy
- 2 code review BLOCKERs fixed: CR-01 (cycle guard + visual-order anchor in gridGroupIndent, commit 6d778dd), CR-02 (topmost-only delete in gridGroupDelete, commit 6d778dd)
- REVIEW.md + VERIFICATION.md committed (4/4 DELIVERED)
- ROADMAP.md + STATE.md: Phase 86.10 marked complete

**Phase 86.5 (8 plans, implemented prior to this session):**
- SC-1: Gantt header flush alignment (CSS)
- SC-2: Unified bar drag — rightward empty-space drop extends end_date
- SC-3: Back button routes to #/projects/detail/{code}
- SC-4: Excel-style Enter commits row + auto-focuses new empty input row
</completed_work>

<remaining_work>

**Phase 86.5 — Browser UAT (5 tests from VERIFICATION.md):**
1. SC-1: Left-rail headers flush with right-panel timeline header at all panel widths
2. SC-2a: Rightward drag to empty space extends end_date in Firestore
3. SC-2b: Leftward drag to empty space silently cancels (no write, no toast)
4. SC-2c: Bar-to-bar drag still creates FS predecessor (regression on Phase 86.1)
5. SC-3: Back button → #/projects/detail/{code}, not #/projects
6. SC-4: Enter commits row, new empty input auto-focuses immediately

**After UAT passes:**
- Mark Phase 86.5 plans [x] complete in ROADMAP.md
- Update STATE.md to next phase
</remaining_work>

<decisions_made>

- 86.10 CR-01: Switched anchor search from row_order sort to DFS visualOrder (matches what user sees). Added isDescendant cycle guard before any write.
- 86.10 CR-02: gridGroupDelete filters to topmost IDs only — deleteTaskNow cascades full subtree, double-calling on child+parent corrupts recomputeParentDates.
- 86.10 paste fix: hasInternalHierarchy flag — clipboard roots with a parent-child clipboard set preserve original depth; flat selections use pasteParentId.
</decisions_made>

<blockers>
None. Phase 86.10 fully closed. Phase 86.5 is unblocked — needs human browser UAT only.
</blockers>

## Required Reading (in order)

1. `.planning/phases/86.5-gantt-ui-polish-3-header-drag-back-excel/VERIFICATION.md` — exact 5 browser UAT tests with expected results.
2. `.planning/ROADMAP.md` lines 474-489 — Phase 86.5 success criteria.

## Infrastructure State

- Dev server: NOT running. Start with `python -m http.server 8000` from project root.
- Firebase: production `clmc-procurement` (no staging).
- Branch: v3.3

<context>
Phase 86.10 was the last URGENT insertion before Phase 86.5. With 86.10 fully closed, Phase 86.5 is the only remaining unverified Gantt phase. It was implemented (8 plans) back on 2026-05-07 but UAT was never run. The resuming agent should guide the user through browser UAT then close the phase.
</context>

<next_action>
Start with: Ask the user to start `python -m http.server 8000`, open a project with tasks, and run the 5 tests in `.planning/phases/86.5-gantt-ui-polish-3-header-drag-back-excel/VERIFICATION.md`. Record pass/fail for each. All pass → close phase. Any fail → debug and fix.
</next_action>
