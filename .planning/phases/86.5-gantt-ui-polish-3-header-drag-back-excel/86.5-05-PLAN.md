---
phase: 86.5
plan: 05
type: execute
wave: 3
depends_on:
  - "86.5-02"
files_modified:
  - app/views/project-plan.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Pressing Enter in the new-task input commits, focuses the next empty row, and lets the user keep typing without any visible loading state"
    - "Successful task creation shows no per-row toast — silent commit, Excel-style"
    - "Failed Firestore writes roll back the optimistic row and show an error toast"
    - "Rapid consecutive Enters do not collide on task_id or row_order"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "Optimistic local append + silent commit in handleNewRowCommit"
      contains: "Optimistic local append"
  key_links:
    - from: "handleNewRowCommit (Enter path)"
      to: "tasks.push + renderTaskGrid synchronous"
      via: "client-derived taskId from local tasks max-seq → optimistic append → async setDoc"
      pattern: "optimistic"
---

<objective>
Make new-task entry feel truly Excel-like: no loading spinner, no per-row success toast, and the new row appears instantly (before Firestore confirms) so the user can keep typing without perceptible delay.

Purpose: UAT feedback (item 7) — `showLoading()` overlay and per-row "Task created" toast both fire on every Enter, breaking the rapid-entry feel. The generateTaskId Firestore round-trip also adds ~200-400ms per row and risks ID collisions when two Enters fire in parallel before the first snapshot returns.

Output: `handleNewRowCommit()` is rewritten to (1) drop showLoading and the success toast, (2) compute task_id locally from `tasks` array (no Firestore round-trip), (3) optimistically append the new row to `tasks` and re-render the grid before awaiting setDoc, (4) roll back the optimistic row + show an error toast on write failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite handleNewRowCommit for optimistic, silent, instant entry</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    Read app/views/project-plan.js lines 594-657 — the entire `async function handleNewRowCommit(input)` body. Confirm it currently calls `showLoading(true)`, runs `generateTaskId` in a retry loop with `getDoc` checks, awaits `setDoc`, calls `showToast('Task created.', 'success')`, and ends with `showLoading(false)` in finally.
  </read_first>
  <action>
    In app/views/project-plan.js, replace the entire body of `async function handleNewRowCommit(input)` (approximately lines 594-657) with the following implementation. The function signature stays the same; do not touch surrounding functions or imports.

        async function handleNewRowCommit(input) {
            if (!input.value.trim()) return;          // re-entry guard
            const name = input.value.trim();
            input.value = '';                          // clear synchronously before any async work
            if (!currentProject?.project_code) {
                showToast(`This project doesn't have a project code yet.`, 'warning');
                return;
            }

            // Derive next task_id from local tasks array (no Firestore round-trip).
            // Format mirrors generateTaskId(): TASK-{project_code}-{maxSeq+1}.
            // Local state is kept fresh by onSnapshot; rapid consecutive Enters see each other's
            // optimistic appends, so seq numbers stay monotonic without server queries.
            const maxSeq = tasks.reduce((m, t) => {
                if (typeof t.task_id !== 'string') return m;
                const lastDash = t.task_id.lastIndexOf('-');
                if (lastDash < 0) return m;
                const n = parseInt(t.task_id.slice(lastDash + 1), 10);
                return Number.isFinite(n) ? Math.max(m, n) : m;
            }, 0);
            const taskId = `TASK-${currentProject.project_code}-${maxSeq + 1}`;

            const maxOrder = tasks.reduce((m, t) => Math.max(m, t.row_order ?? 0), 0);
            const newRowOrder = maxOrder + 1;

            // DEFECT-6: inherit parent from the last sorted task above; Phase 86.3 D-04/D-05: anchor on row above
            const sortedForInherit = tasks.slice().sort((a, b) => (a.row_order ?? Infinity) - (b.row_order ?? Infinity));
            const inheritedParentId = sortedForInherit[sortedForInherit.length - 1]?.parent_task_id ?? null;
            const rowAbove = sortedForInherit[sortedForInherit.length - 1];
            const anchorStart = rowAbove?.start_date || formatDateISO(new Date());

            const docData = {
                task_id: taskId,
                project_id: currentProject.id,
                project_code: currentProject.project_code,
                parent_task_id: inheritedParentId,
                name,
                start_date: anchorStart,
                end_date: anchorStart,
                description: '',
                progress: 0,
                is_milestone: false,
                dependencies: [],
                assignees: [],
                resources: '',
                row_order: newRowOrder,
                created_at: serverTimestamp(),
                updated_at: serverTimestamp()
            };

            // Optimistic local append — render now, write async. Use Date placeholders on the local copy
            // since serverTimestamp() returns a sentinel that isn't a Date until snapshot reconciles.
            const now = new Date();
            const optimisticTask = { ...docData, created_at: now, updated_at: now };
            tasks.push(optimisticTask);
            renderTaskGrid();

            try {
                await setDoc(doc(db, 'project_tasks', taskId), docData);
                // Snapshot will fire and replace optimistic entry with the server version (idempotent).
            } catch (err) {
                console.error('[ProjectPlan] handleNewRowCommit failed:', err);
                // Roll back optimistic add
                const idx = tasks.findIndex(t => t.task_id === taskId);
                if (idx >= 0) tasks.splice(idx, 1);
                renderTaskGrid();
                showToast(err?.code === 'permission-denied'
                    ? `You don't have permission to add tasks on this project.`
                    : 'Could not create task. Please try again.', 'error');
            }
        }

    Removed (intentional):
    - `showLoading(true)` / `showLoading(false)` — no full-screen spinner per row
    - `showToast('Task created.', 'success')` — silent commit, Excel-style
    - `generateTaskId(...)` retry loop with `getDoc` collision check — replaced with local-state derivation; collision risk handled by the always-fresh `tasks` array (onSnapshot keeps it current; optimistic append makes consecutive Enters see prior rows)

    Preserved:
    - Empty-input re-entry guard
    - Missing-project_code warning toast
    - DEFECT-6 parent inheritance, Phase 86.3 D-04/D-05 anchor logic
    - All docData fields (no schema change)
    - Error toast on permission-denied vs generic failure
    - `_focusNewRowAfterRender` flag is still set in handleNewRowKeydown (Plan 02) — unchanged here. Because renderTaskGrid is now called synchronously after tasks.push, the flag's renderTaskGrid focus block at line ~402 fires immediately, focusing the new empty row instantly.

    Do not modify handleNewRowKeydown, renderTaskGrid, destroy(), or any other function. Do not change imports. The `generateTaskId` import at line 9 becomes unused — remove it from the import statement to avoid dead-import warnings.
  </action>
  <verify>
    grep -n "showToast('Task created" app/views/project-plan.js
    grep -n "showLoading" app/views/project-plan.js
    grep -n "generateTaskId" app/views/project-plan.js
    grep -n "Optimistic local append" app/views/project-plan.js
    Expected: zero matches for "Task created" toast; remaining showLoading uses are in OTHER functions (saveTaskFromModal, indent/outdent — not handleNewRowCommit); zero generateTaskId calls in handleNewRowCommit; one match for "Optimistic local append" comment.
  </verify>
  <acceptance_criteria>
    - `grep -c "showToast('Task created" app/views/project-plan.js` returns 0
    - `grep -c "Optimistic local append" app/views/project-plan.js` returns 1
    - Within `handleNewRowCommit` (lines 594-end-of-function), `grep` for `showLoading` returns 0
    - Within `handleNewRowCommit`, `grep` for `generateTaskId(` returns 0
    - `grep -c "import { generateTaskId } from '../task-id.js'" app/views/project-plan.js` returns 0 (import removed since unused in this file after the change)
    - `grep -c "tasks.push(optimisticTask)" app/views/project-plan.js` returns 1
    - `grep -c "tasks.splice(idx, 1)" app/views/project-plan.js` returns 1 (rollback path)
    - `git diff --stat` shows only `app/views/project-plan.js` changed
  </acceptance_criteria>
  <done>
    handleNewRowCommit clears the input synchronously, generates task_id from local state, appends to `tasks` and renders before awaiting setDoc, rolls back on failure with an error toast. No spinner, no success toast. Continuous Enter→type→Enter feels Excel-instant.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local-derived task_id | Local tasks array is kept fresh by onSnapshot; rapid optimistic appends update it synchronously. Multi-user concurrent additions are rare in the project plan view (single PM workflow). |
| Optimistic UI rollback | On Firestore write failure, the optimistic entry is removed before the next render. No persistent local state divergence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.5-05-01 | Spoofing | task_id collision | mitigate | maxSeq derivation reads local tasks (kept current by onSnapshot + optimistic append); collision only if a remote user writes the same seq within the small race window — same risk profile as the existing generateTaskId retry loop, which also reads then writes without a transaction |
| T-86.5-05-02 | Information Disclosure | Optimistic UI shows row before persisted | accept | UI shows what the user just typed; no leakage. If write fails, row is removed before any other user could observe it. |
| T-86.5-05-03 | Tampering | project_tasks setDoc | accept | Same write path as before; Firestore security rules unchanged |
</threat_model>

<verification>
1. Open a project plan view, click the empty row's Add task input
2. Type "Task A" + Enter — confirm: NO spinner appears, NO toast appears, the row appears immediately, the empty row's input is focused
3. Without pausing, type "Task B" + Enter, then "Task C" + Enter — confirm rapid succession works smoothly with no perceptible lag
4. Open DevTools Network tab — confirm one setDoc per row in flight; no per-row getDocs query for ID generation
5. Refresh the page — confirm all created rows persisted (snapshot reconciliation worked)
6. Permission-denied check (optional, requires non-procurement role): attempt creation — confirm row vanishes and an error toast appears
7. Type a name and press Escape — confirm input clears, no row is created (existing behavior preserved)
</verification>

<success_criteria>
- No full-screen loading spinner during new-row commit
- No per-row success toast
- New row appears instantly in the grid (before Firestore confirms)
- Focus moves to the next empty row instantly (no second-render delay)
- Rapid consecutive Enters do not collide on task_id or row_order
- On write failure: optimistic row vanishes and error toast appears
</success_criteria>

<output>
After completion, create `.planning/phases/86.5-gantt-ui-polish-3-header-drag-back-excel/86.5-05-SUMMARY.md`
</output>
