---
phase: 86.5
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/project-plan.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Clicking Back from the plan view navigates to #/projects/detail/{code}, not #/projects"
    - "Pressing Enter in the new-task input commits the row and immediately focuses the fresh empty input row — no click required"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "back link href with detail route + _focusNewRowAfterRender flag"
      contains: "_focusNewRowAfterRender"
  key_links:
    - from: "handleNewRowKeydown Enter branch"
      to: "_focusNewRowAfterRender = true"
      via: "flag set before handleNewRowCommit call"
      pattern: "_focusNewRowAfterRender = true"
    - from: "renderTaskGrid post-render block"
      to: ".tg-empty-row .tg-name-input focus"
      via: "setTimeout focus call when flag is true"
      pattern: "newRowInput.focus"
---

<objective>
Fix two independent JS issues in project-plan.js: (SC-3) the Back button navigates to the project list instead of project detail, and (SC-4) pressing Enter in the new-task input loses focus requiring a click to add the next task.

Purpose: Both are workflow friction issues. SC-3 is a single href string change. SC-4 requires a module-scope flag coordinating handleNewRowKeydown → handleNewRowCommit → renderTaskGrid.
Output: Two distinct changes to app/views/project-plan.js — back link href and Excel-style focus flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Back button href to navigate to project detail</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    Read app/views/project-plan.js lines 60-85 (render() function) to confirm the exact href string before editing.
    Look for: `<a href="#/projects" class="plan-back-link">‹ Back</a>`
  </read_first>
  <action>
    In app/views/project-plan.js, inside the render() function's returned HTML string (approximately line 64), find:

        <a href="#/projects" class="plan-back-link">‹ Back</a>

    Replace it with:

        <a href="#/projects/detail/${escapeHTML(projectCode || '')}" class="plan-back-link">‹ Back</a>

    `projectCode` is assigned at the very start of render() as `projectCode = param;` so it is always in scope.
    `escapeHTML` is already imported from utils.js at the top of the file.
    Router parses `#/projects/detail/CODE` → navigates to the `/project-detail` view (confirmed in router.js).

    Do not change anything else in render().
  </action>
  <verify>
    grep -n "projects/detail" app/views/project-plan.js
    Expected: one match inside the render() return string.
  </verify>
  <acceptance_criteria>
    - `grep -c "projects/detail/\${escapeHTML(projectCode" app/views/project-plan.js` returns 1
    - `grep -c 'href="#/projects" class="plan-back-link"' app/views/project-plan.js` returns 0 (old href gone)
  </acceptance_criteria>
  <done>Back link href points to #/projects/detail/${escapeHTML(projectCode || '')} in render().</done>
</task>

<task type="auto">
  <name>Task 2: Excel-style continuous task entry via _focusNewRowAfterRender flag</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    Read app/views/project-plan.js:
    - Lines 18-51 (module-scope declarations, to find insertion point for the new flag)
    - Lines 650-665 (handleNewRowKeydown, to find the Enter branch)
    - Lines 383-398 (renderTaskGrid end, to find where container.innerHTML is set — insert focus block after)
    - Lines 190-235 (destroy(), to find insertion point for flag reset)
  </read_first>
  <action>
    Make FOUR surgical changes to app/views/project-plan.js:

    CHANGE 1 — Add module-scope flag (near line 34, after the existing _gridKeydownHandler declaration):
        let _focusNewRowAfterRender = false; // Phase 86.5: Excel-style continuous entry

    CHANGE 2 — In handleNewRowKeydown (approximately line 655-658), in the `if (event.key === 'Enter')` branch, add the flag-set line BEFORE calling handleNewRowCommit:

        if (event.key === 'Enter') {
            event.preventDefault();
            _focusNewRowAfterRender = true;  // ADD THIS LINE (Phase 86.5)
            handleNewRowCommit(input);
        }

    CHANGE 3 — In renderTaskGrid(), AFTER the `container.innerHTML = ...` assignment (the non-empty path that sets the full table HTML, approximately line 385-397), add the following block immediately after the closing backtick of that template literal:

        // Phase 86.5: Excel-style continuous entry — auto-focus new row after commit
        if (_focusNewRowAfterRender) {
            _focusNewRowAfterRender = false;
            const newRowInput = container.querySelector('.tg-empty-row .tg-name-input');
            if (newRowInput) setTimeout(() => newRowInput.focus(), 0);
        }

    CHANGE 4 — In destroy() (approximately lines 184-235), after the line `_gridKeydownHandler = null;` (approximately line 191), add:
        _focusNewRowAfterRender = false; // Phase 86.5

    IMPORTANT NOTES:
    - Do not modify any other logic in handleNewRowCommit or renderTaskGrid
    - The flag is intentionally module-scope so the async Firestore write + onSnapshot re-render cycle can read it
    - The setTimeout(..., 0) defers focus to the next event-loop tick so the DOM is fully settled
    - Place CHANGE 3 only in the non-empty path (where container.innerHTML is set to the full table), NOT in any early-return or empty-state path
  </action>
  <verify>
    grep -n "_focusNewRowAfterRender" app/views/project-plan.js
    Expected: 4 matches (declaration, flag set in keydown, check+reset in renderTaskGrid, reset in destroy).
  </verify>
  <acceptance_criteria>
    - `grep -c "_focusNewRowAfterRender" app/views/project-plan.js` returns 4
    - `grep -c "_focusNewRowAfterRender = true" app/views/project-plan.js` returns 1 (only in keydown handler)
    - `grep -c "_focusNewRowAfterRender = false" app/views/project-plan.js` returns 2 (one in renderTaskGrid, one in destroy)
    - `grep -c "let _focusNewRowAfterRender" app/views/project-plan.js` returns 1 (module-scope declaration)
  </acceptance_criteria>
  <done>Four locations updated; flag coordinates Enter-keydown → renderTaskGrid focus handoff; destroy() resets it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| projectCode → href | projectCode comes from router param (URL hash); escapeHTML() already applied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.5-02-01 | Injection | plan-back-link href | mitigate | escapeHTML(projectCode) already applied at render time — same pattern used throughout project-plan.js |
</threat_model>

<verification>
1. Navigate to any project plan view (#/projects/{code}/plan)
2. Click Back — confirm browser navigates to #/projects/detail/{code} not #/projects
3. Click the empty row at bottom of task grid, type a task name, press Enter
4. Confirm the row commits (task appears) AND the new empty input row is focused immediately (no click needed)
5. Type a second task name and press Enter — confirm same behavior repeats
</verification>

<success_criteria>
- Back link lands on project detail page with correct project code in URL
- Enter in new-task input: row commits AND fresh input is focused in the same interaction — no click required between consecutive task additions
</success_criteria>

<output>
After completion, create `.planning/phases/86.5-gantt-ui-polish-3-header-drag-back-excel/86.5-02-SUMMARY.md`
</output>
