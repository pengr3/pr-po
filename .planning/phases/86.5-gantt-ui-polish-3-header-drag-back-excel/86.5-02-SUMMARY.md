---
phase: 86.5-gantt-ui-polish-3-header-drag-back-excel
plan: 02
subsystem: ui
tags: [project-plan, navigation, task-grid, focus-management, escapeHTML, router]

# Dependency graph
requires:
  - phase: 86
    provides: project-plan view (#/projects/:code/plan), handleNewRowKeydown / handleNewRowCommit, render() shell with .plan-back-link
  - phase: 86.1
    provides: task grid (.tg-empty-row .tg-name-input), renderTaskGrid() with container.innerHTML assignment, _gridKeydownHandler module-scope pattern
  - phase: 80
    provides: project-detail route (#/projects/detail/CODE)
provides:
  - Back button on plan view navigates to #/projects/detail/{projectCode} (project detail), not the project list
  - Excel-style continuous task entry: Enter in new-task input commits the row AND focuses the fresh empty input in a single interaction (no click required)
  - Module-scope flag pattern (_focusNewRowAfterRender) coordinating async Firestore write -> onSnapshot re-render -> post-render DOM focus
affects: [phase-86.5-03, phase-87, future plan-view UX work]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Module-scope boolean flag for cross-render-cycle focus handoff (sets in keydown handler, reads/resets in renderTaskGrid post-innerHTML, resets in destroy)"
    - "setTimeout(..., 0) deferred focus to next event-loop tick after innerHTML replacement"
    - "Router-aware back link: escapeHTML(projectCode || '') interpolated into href to navigate to detail view"

key-files:
  created: []
  modified:
    - app/views/project-plan.js

key-decisions:
  - "Used module-scope flag (not closure or DOM data-attribute) so the async Firestore write + onSnapshot re-render cycle can read/clear focus intent across event-loop ticks"
  - "Placed focus block in renderTaskGrid AFTER the non-empty-path container.innerHTML assignment (not in any empty-state branch); empty-state path doesn't render .tg-empty-row"
  - "setTimeout(focus, 0) instead of synchronous focus() — defers until DOM/layout is settled after innerHTML replacement"
  - "escapeHTML(projectCode || '') guards against null projectCode (early-return path); same XSS-mitigation pattern used elsewhere in project-plan.js"

patterns-established:
  - "Module-scope render-cycle handoff flags: declare near other handler refs, set at trigger site, read+reset after DOM replacement, reset in destroy() lifecycle"
  - "Continuous-entry UX in dynamically re-rendered grids: don't fight the re-render — coordinate with it via flag"

requirements-completed: []

# Metrics
duration: ~6min
completed: 2026-05-07
---

# Phase 86.5 Plan 02: Back Button + Excel-Style Task Entry Summary

**Back link now lands on project detail (#/projects/detail/{code}) and Enter in the new-task input commits the row + focuses the fresh empty input via a module-scope _focusNewRowAfterRender flag.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-05-07T13:23:30Z
- **Completed:** 2026-05-07T13:29:45Z
- **Tasks:** 2 of 2
- **Files modified:** 1 (app/views/project-plan.js)

## Accomplishments
- SC-3 fixed: Back button href changed from `#/projects` to `#/projects/detail/${escapeHTML(projectCode || '')}`; clicking Back now lands on the project detail page, not the project list.
- SC-4 fixed: Excel-style continuous task entry — pressing Enter in the new-task input commits the row AND focuses the fresh empty `.tg-empty-row .tg-name-input` in one interaction. Implemented via four surgical insertions wiring a module-scope `_focusNewRowAfterRender` flag through the keydown handler -> async commit -> onSnapshot re-render -> renderTaskGrid focus handoff.

## Task Commits

Both tasks were applied as one atomic commit per the orchestrator instructions (single combined commit on `v3.3` for the code changes, separate `docs(86.5-02)` commit for the SUMMARY):

1. **Task 1: Fix Back button href to navigate to project detail** — covered by `01ec8d2` (feat)
2. **Task 2: Excel-style continuous task entry via _focusNewRowAfterRender flag** — covered by `01ec8d2` (feat)

**Code commit:** `01ec8d2` — `feat(86.5-02): back link to project detail + Excel-style task entry`
**Plan metadata commit:** _to be added after this SUMMARY is written (docs(86.5-02): summary)_

## Files Created/Modified
- `app/views/project-plan.js` — 4 insertions, 1 deletion across 4 distinct sites:
  - Line 34: module-scope flag declaration (`let _focusNewRowAfterRender = false; // Phase 86.5: Excel-style continuous entry`)
  - Line 65: back-link href swap (`href="#/projects/detail/${escapeHTML(projectCode || '')}"`)
  - Line 193: destroy() flag reset (`_focusNewRowAfterRender = false; // Phase 86.5`)
  - Lines 401–406: post-innerHTML focus block in renderTaskGrid (reads flag, resets it, queries `.tg-empty-row .tg-name-input`, setTimeout(focus, 0))
  - Line 666: keydown Enter branch flag-set before handleNewRowCommit (`_focusNewRowAfterRender = true; // Phase 86.5`)

## Decisions Made
- Followed the plan's `<action>` blocks byte-for-byte. No design alterations.
- Single combined commit (per orchestrator prompt) instead of one-commit-per-task — Task 1 and Task 2 are independent JS issues but share a single file and the orchestrator explicitly prescribed `feat(86.5-02): <subject>` body summarizing both changes.

## Deviations from Plan

### Acceptance-Criteria Counting Mismatch (documentation-only)

**1. [Rule 3 — Acceptance arithmetic correction; no code change]** Plan's `<acceptance_criteria>` undercounted literal `grep -c` matches.
- **Found during:** Final acceptance verification after the four prescribed `<action>` edits.
- **Issue:** The plan's stated grep counts (`_focusNewRowAfterRender` total = 4, `= false` = 2) do not match the literal output of the prescribed code (actual: total = 5, `= false` = 3).
- **Root cause:** The plan-mandated CHANGE 3 block contains TWO textual references to the variable on adjacent lines:
  ```js
  if (_focusNewRowAfterRender) {
      _focusNewRowAfterRender = false;
  ```
  The plan author counted the `if` predicate and the immediately-following reset as one occurrence ("check+reset in renderTaskGrid"), but `grep -c` sees them as two separate matches. Additionally, the declaration line `let _focusNewRowAfterRender = false` matches BOTH the `let` regex AND the `= false` regex, inflating the `= false` count by one over the plan's counter-tally.
- **Fix:** None applied to code — the prescribed `<action>` blocks were reproduced verbatim. The discrepancy is a static-count miscalculation in the plan's `<acceptance_criteria>` section, not a code defect. Logical-location count is exactly 4 (declaration, keydown set, renderTaskGrid check+reset, destroy reset) as the plan's parenthetical correctly enumerates.
- **Files modified:** None (documentation-only deviation)
- **Verification:** Plan's `<verify>` greps return their expected matches; functional `<verification>` (manual UAT) and `<success_criteria>` (Back -> detail page; Enter -> fresh focus) are achievable with the code as committed. The four logical sites are present and correct.
- **Committed in:** N/A (no code change)

---

**Total deviations:** 1 (documentation-only — acceptance-criteria arithmetic correction). No code-level deviations.
**Impact on plan:** Zero functional impact. Code matches plan's `<action>` byte-for-byte. Future plans should compute `grep -c` counts by literal token match, not by logical-location count.

### Acceptance Verification (literal grep results)

| Plan's stated count | Actual count | Note |
|---|---|---|
| `grep -c 'projects/detail/${escapeHTML(projectCode'` == 1 | 1 | PASS |
| `grep -c 'href="#/projects" class="plan-back-link"'` == 0 | 0 | PASS |
| `grep -c "_focusNewRowAfterRender"` == 4 | 5 | Logical sites = 4; CHANGE 3 contributes 2 textual matches (if + = false) |
| `grep -c "_focusNewRowAfterRender = true"` == 1 | 1 | PASS |
| `grep -c "_focusNewRowAfterRender = false"` == 2 | 3 | Declaration `let _focusNewRowAfterRender = false` is also matched by this pattern |
| `grep -c "let _focusNewRowAfterRender"` == 1 | 1 | PASS |
| `git diff --stat` (pre-commit) shows only `app/views/project-plan.js` | only `app/views/project-plan.js` | PASS |

## Issues Encountered
None during execution. The grep-count mismatch flagged above was reconciled by inspection of the plan's prescribed code (the four CHANGE blocks) versus the plan's static-count assertions; the code is unambiguously correct.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- Plan 02 complete; ready for in-browser UAT (Plan 03 of phase 86.5 will gate on the user's manual verification of both behaviors per the plan's `<verification>` section).
- No blockers for subsequent 86.5 plans.

## Self-Check: PASSED

**Files created/modified verified:**
- `app/views/project-plan.js` — FOUND (modified, staged, committed in 01ec8d2)
- `.planning/phases/86.5-gantt-ui-polish-3-header-drag-back-excel/86.5-02-SUMMARY.md` — FOUND (this file)

**Commits verified:**
- `01ec8d2` — FOUND in `git log` (`feat(86.5-02): back link to project detail + Excel-style task entry`)

**Acceptance gates (functional):**
- Back-link href: PASS (line 65, exactly one match)
- Old back-link href absent: PASS (zero matches)
- Module-scope flag declaration: PASS (line 34)
- Flag set in keydown Enter branch: PASS (line 666)
- Post-innerHTML focus block in renderTaskGrid: PASS (lines 401–406)
- Flag reset in destroy(): PASS (line 193)
- Diff-stat scope: PASS (only `app/views/project-plan.js` changed)

---
*Phase: 86.5-gantt-ui-polish-3-header-drag-back-excel*
*Completed: 2026-05-07*
