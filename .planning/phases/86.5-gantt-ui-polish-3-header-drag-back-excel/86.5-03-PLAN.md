---
phase: 86.5
plan: 03
type: execute
wave: 2
depends_on:
  - "86.5-02"
files_modified:
  - app/views/project-plan.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Dragging from the blue dot onto another bar still creates a FS predecessor link (existing behavior preserved)"
    - "Dragging the blue dot rightward and releasing on empty space extends the task's end_date by the dragged distance in days"
    - "Dragging leftward and releasing on empty space cancels silently (no change)"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "unified bar drag — empty-space drop extends duration"
      contains: "bar extend failed"
  key_links:
    - from: "svg mouseup handler"
      to: "updateDoc project_tasks end_date"
      via: "xPerDay2 calculation → addDays → updateDoc"
      pattern: "bar extend failed"
---

<objective>
Unify bar drag-link and bar-extend into a single gesture: dragging from the blue dot connector handle to another bar creates a FS predecessor link (unchanged); dragging to empty space to the RIGHT of the bar's right edge extends the task's end_date by the calculated number of days.

Purpose: Currently a drop-on-empty-space silently cancels. The user expects it to work like bar-right-edge extension — extend duration. This change replaces the silent return with a duration-extension write.
Output: The `if (!toWrapper) return;` branch in the svg mouseup handler of initGanttDragLink() is replaced with a rightward-drag extension block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace silent empty-space cancel with rightward duration-extension logic</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    Read app/views/project-plan.js lines 1588-1630 (the svg mouseup handler inside initGanttDragLink()).
    Confirm the exact text of the line: `if (!toWrapper) return; // dropped on empty space — cancel silently`
    This is approximately line 1597.
  </read_first>
  <action>
    In app/views/project-plan.js, inside the `svg.addEventListener('mouseup', async (e) => { ... })` handler
    within initGanttDragLink() (approximately lines 1588-1628), find the line:

        if (!toWrapper) return; // dropped on empty space — cancel silently

    Replace that single line with the following block (preserve the surrounding code exactly):

        if (!toWrapper) {
            // Dropped on empty space — if dragged rightward, extend task duration
            if (!gantt || typeof gantt.config !== 'object') return;
            const fromTask = tasks.find(x => x.task_id === fromTaskId);
            if (!fromTask || !fromTask.end_date) return;
            const fromWrapper2 = svg.querySelector(`.bar-wrapper[data-id="${CSS.escape(fromTaskId)}"]`);
            const fromBar2 = fromWrapper2?.querySelector('.bar');
            if (!fromBar2) return;
            const barRect2 = fromBar2.getBBox();
            const barRightX = barRect2.x + barRect2.width;
            const pt2 = svg.createSVGPoint();
            pt2.x = e.clientX;
            pt2.y = e.clientY;
            const svgPt2 = pt2.matrixTransform(svg.getScreenCTM().inverse());
            if (svgPt2.x <= barRightX) return; // dragged leftward — cancel silently
            const xPerDay2 = (gantt.config.column_width || 45) / (gantt.config.step || 1);
            const daysToAdd = Math.max(1, Math.round((svgPt2.x - barRightX) / xPerDay2));
            const newEndDate = addDays(fromTask.end_date, daysToAdd);
            try {
                await updateDoc(doc(db, 'project_tasks', fromTaskId), { end_date: newEndDate, updated_at: serverTimestamp() });
                const idx2 = tasks.findIndex(x => x.task_id === fromTaskId);
                if (idx2 >= 0) tasks[idx2] = { ...tasks[idx2], end_date: newEndDate };
                if (fromTask.parent_task_id) await recomputeParentDates(fromTask.parent_task_id);
            } catch (err) {
                console.error('[ProjectPlan] bar extend failed:', err);
                showToast('Could not extend task duration.', 'error');
            }
            return;
        }

    All identifiers used are already in scope within this function:
    - `gantt`, `tasks`, `fromTaskId` — module-scope or closure vars
    - `CSS`, `svg`, `e` — closure / browser globals
    - `addDays`, `recomputeParentDates`, `updateDoc`, `doc`, `db`, `serverTimestamp`, `showToast` — module-scope imports/functions

    Do NOT change any other part of the mouseup handler or initGanttDragLink().
  </action>
  <verify>
    grep -n "bar extend failed" app/views/project-plan.js
    Expected: exactly one match inside the mouseup handler.
  </verify>
  <acceptance_criteria>
    - `grep -c "bar extend failed" app/views/project-plan.js` returns 1
    - `grep -c "dropped on empty space — cancel silently" app/views/project-plan.js` returns 0 (old line replaced)
    - `grep -c "dragged leftward — cancel silently" app/views/project-plan.js` returns 1 (new leftward guard present)
    - `grep -c "daysToAdd" app/views/project-plan.js` returns 1
    - `grep -c "xPerDay2" app/views/project-plan.js` returns 1
  </acceptance_criteria>
  <done>
    The if (!toWrapper) branch now: cancels silently for leftward drops, extends end_date by computed days for rightward drops, catches and toasts Firestore write errors. Existing bar-to-bar predecessor link behavior is untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG coords → Firestore write | Mouse position transformed via SVG CTM to day count; Math.max(1,...) prevents zero/negative extension |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.5-03-01 | Tampering | project_tasks end_date | mitigate | Firestore security rules already enforce role-based write access; client-side Math.max(1,...) prevents degenerate values |
| T-86.5-03-02 | Elevation of Privilege | updateDoc project_tasks | accept | Same write path as existing drag-to-link; no new collection or field access granted |
</threat_model>

<verification>
1. Open a project plan view with at least one non-parent task that has a visible bar
2. Hover the bar — blue dot appears at right edge
3. Drag from the dot RIGHTWARD and release on empty timeline space — task end_date should extend (bar grows); toast should NOT appear
4. Drag from the dot LEFTWARD and release on empty space — nothing should change (silent cancel)
5. Drag from the dot and release ON another bar — FS predecessor link is created (original behavior preserved)
6. Check DevTools console for any unexpected errors
</verification>

<success_criteria>
- Rightward drag-to-empty extends end_date; bar visibly grows after onSnapshot re-renders Gantt
- Leftward drag-to-empty silently cancels; no Firestore write, no toast
- Bar-to-bar drag still creates FS predecessor link (existing Phase 86.1 behavior unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/86.5-gantt-ui-polish-3-header-drag-back-excel/86.5-03-SUMMARY.md`
</output>
