---
status: testing
phase: 84.1-procurement-notifications-trigger-enhancements
source: 84.1-01-SUMMARY.md, 84.1-02-SUMMARY.md, 84.1-03-SUMMARY.md
started: 2026-05-02T05:46:00Z
updated: 2026-05-02T06:30:00Z
environment: dev Firebase project (NOT production clmc-procurement) — see Phase 53.1 dev-environment setup
---

## Current Test

number: 1, 2, 3 (re-test after fix)
name: NOTIF-14 codeless project, NOTIF-15 PR processed, NOTIF-16 RFP paid — re-test after retroactive Task 3 fix
expected: |
  Initial UAT run by user surfaced 3 failing flows. Root-caused on 2026-05-02:
    - NOTIF-15 + NOTIF-16: Plan 01 Task 3 was silently skipped by the executor — finance.js had ZERO wiring for these. Retroactively fixed in commit f5d9940.
    - NOTIF-14 (codeless project): most likely firestore.rules not yet deployed (users.list still admin-only on the live project), so fan-out from non-admin actor returned 0 recipients. Verify via console block 1a after deploying rules.
  Re-test all 3 after: (a) hard refresh to bust cache, (b) `firebase deploy --only firestore:rules` to dev, (c) using fresh PR/RFP docs (created after Phase 84.1 deploy) so creator-UID fields are populated.
awaiting: user re-runs Tests 1, 2, 3 and reports results

## Tests

### 1. NOTIF-14 — MRF submitted broadcast (3 paths)
expected: |
  Path A: operations_user submits project-side MRF → procurement role sees "New MRF MRF-YYYY-### for {project_name} needs processing" within 5s; click navigates to #/procurement/mrfs.
  Path B: procurement user submits MRF via Create MRF tab → other procurement users notified; submitting user does NOT receive self-notification (excludeActor: true).
  Path C: service-side MRF (service_name set, project_name empty) → message uses service name instead.
result: issue
reported: "Notification did not appear for codeless projects (initial test 2026-05-02)"
severity: medium
diagnosis: |
  Most likely root cause — firestore.rules `users.list` relaxation (line 127, `allow list: if isActiveUser();`) shipped in source via commit ec35c21 but has NOT been deployed to the live Firebase project. Until rules are deployed, the LIST query inside `createNotificationForRoles({roles:['procurement']})` is rejected for non-admin actors → helper try/catches the permission-denied error → returns 0 recipients silently. Codeless vs coded project is incidental — the actor's role is what determines whether the LIST succeeds.
  Secondary possibility: only one active procurement user exists and they were the actor, so excludeActor:true skipped them.
re-test: |
  After `firebase deploy --only firestore:rules` to dev, re-run as a non-admin actor (operations_user / services_user). Use console Block 1a to verify `T.usersByRole('procurement').length > 0` first. Expected: count matches the active procurement headcount.

### 2. NOTIF-15 — PR Finance decision (approve + reject paths)
expected: |
  Path A: Finance approves a PR submitted by procurement → that PR creator sees "PR PR-YYYY-### has been Approved by Finance".
  Path B: Finance rejects with reason → PR creator sees "PR PR-YYYY-### has been Rejected: {reason}".
  Notification fires only after BOTH the PR updateDoc and the MRF updateDoc resolve.
result: issue
reported: "Did not notify procurement for processed PR (initial test 2026-05-02)"
severity: high
diagnosis: |
  CONFIRMED ROOT CAUSE — Plan 01 Task 3 was silently skipped by the executor. NOTIF-15 had ZERO wiring in finance.js (`grep` returned 0 matches for NOTIF-15 / PR_DECIDED / createNotification). Retroactively fixed in commit f5d9940 (2026-05-02). Approve and reject paths now both call createNotification with PR_DECIDED type.
re-test: |
  Hard refresh, then approve a fresh PR (created after Phase 84 — verify via console §3c that pr_creator_user_id is set). Expected: PR creator receives PR_DECIDED notification within 5s. For legacy PRs missing pr_creator_user_id, the trigger skips silently by design — backfill via console §3c if testing against a legacy doc.

### 3. NOTIF-16 — RFP marked Paid (full payment only)
expected: |
  Path A: Finance records FULL payment on an RFP (amount = amount_requested) → RFP transitions to Fully Paid; RFP creator sees "RFP RFP-### for PO PO-### has been marked Paid".
  Path B: Partial payment recorded (less than amount_requested) → NO NOTIF-16 fires; status shows Partially Paid.
result: issue
reported: "Did not notify procurement for paid RFPs (initial test 2026-05-02)"
severity: high
diagnosis: |
  CONFIRMED ROOT CAUSE — same as Test 2. Plan 01 Task 3 was skipped, NOTIF-16 had ZERO wiring in finance.js. Retroactively fixed in commit f5d9940. submitPaymentRecord now computes priorPaid + new payment; if newTotal >= amount_requested AND rfp.rfp_creator_user_id present → fires RFP_PAID notification.
re-test: |
  Hard refresh, then on a fresh RFP (rfp_creator_user_id stamped) record a full-amount payment so newTotal exactly equals amount_requested. Expected: creator receives RFP_PAID. Confirm partial payment does NOT fire.

### 4. NOTIF-17 — TR Finance decision (approve + reject paths)
expected: |
  Path A: Finance approves a Transport Request → TR creator sees "TR TR-YYYY-### has been Approved by Finance".
  Path B: Finance rejects with reason → TR creator sees "TR TR-YYYY-### has been Rejected: {reason}".
result: [pending]

### 5. NOTIF-18 — PO Delivered (material PO only)
expected: |
  Path A: Procurement transitions a non-subcon PO from Procured → Delivered (entering delivery fee when prompted) → PR creator (= PO creator UID) sees "PO PO-### for MRF MRF-### has been Delivered".
  Path B: Original MRF requestor (different account) also receives the same notification (fan-out audience = MRF requestor + PO creator).
  Path C: SUBCON PO (terminates at Processed, never Delivered) does NOT trigger NOTIF-18 — out of scope by design.
result: [pending]

### 6. NOTIF-19 — Project/service Budget + Contract Cost change
expected: |
  Path A: operations_admin edits Contract Cost on a project with assigned personnel → assigned personnel see "Project '{name}' Contract Cost changed: PHP X → PHP Y".
  Path B: Edit Budget on the same project → "Project '{name}' Budget changed: ..." with personnel as recipients.
  Path C: Repeat A+B for a service via service-detail page.
  Path D: Save the same value twice → no duplicate notification (skip-on-no-change via normalizedOld guard).
  Path E: Projected Cost click-to-show-modal + Refresh button does NOT fire NOTIF-19 — out of scope per locked decision B4.
result: [pending]

### 7. NOTIF-20 — MRF rejection reason in notification body (THIS PLAN)
expected: |
  Procurement rejects a Pending MRF — when prompted, enters a specific reason like "Out of budget for Q2".
  Original requestor (other window) sees notification body: "Your MRF MRF-YYYY-### has been rejected by Procurement: Out of budget for Q2" (reason text appended after a colon).
result: [pending]

### Regression 1 (Phase 84 UAT Test 1) — MRF Rejected base notification
expected: |
  After Phase 84.1 Plan 03 augmentation, the base "has been rejected by Procurement" phrase is preserved in the notification body. The reason text is the only added segment. Existing NOTIF-07 audience (requestor only) and link target (#/procurement/mrfs) remain unchanged.
result: [pending]

### Regression 2 (Phase 84 UAT Test 2) — MRF Approved via Generate PR
expected: |
  Approve an MRF via Generate PR → requestor receives approved notification with PR ID mentioned (e.g. "Your MRF-2026-### has been approved. PR-2026-### generated.").
result: [pending]

### Regression 3 (Phase 84 UAT Test 3) — MRF Approved via Generate PR + TR
expected: |
  Approve an MRF via Generate PR and TR together → requestor receives approved notification referencing both PR and TR IDs.
result: [pending]

### Regression 4 (Phase 84 UAT Test 4) — PR Generated → Finance review
expected: |
  After generating a PR (any path), Finance role users receive "PR-2026-### needs Finance review".
result: [pending]

### Regression 5 (Phase 84 UAT Test 5) — TR Submitted → Finance review
expected: |
  After submitting a standalone Transport Request, Finance role users receive "TR-2026-### needs Finance review".
result: [pending]

### Regression 6 (Phase 84 UAT Test 6) — RFP Submitted → Finance review
expected: |
  After submitting any RFP (standard, TR-based, or Delivery Fee), Finance role users receive an RFP review notification.
result: [pending]

### Regression 7 (Phase 84 UAT Test 7) — Project/Service Status Change → personnel
expected: |
  Change project_status to a meaningful value (On-going, Completed, Client Approved, For Mobilization, Loss) → assigned personnel receive PROJECT_STATUS_CHANGED notification. Status changes to non-whitelisted values do NOT fire.
result: [pending]

### Regression 8 (Phase 84 UAT Test 8 + Phase 84.1 Plan 01 fan-out fix) — New User Registration → Super Admins
expected: |
  Register a new user with a valid invitation code → all active super_admin users receive the registration notification.
  ADDITIONAL: re-test NOTIF-08 fan-out triggered by procurement / operations_user / services_user actor — under prior firestore.rules (admin-only users.list), this silently returned 0 recipients; after Plan 01 rule relaxation (B2 Option B → users.list = isActiveUser()), Finance users SHOULD receive the fan-out notification. Confirm silent-failure mode is fixed.
result: [pending]

## Summary

total: 15
passed: 0
issues: 3
pending: 12
skipped: 0
blocked: 0

## Gaps

<!-- YAML format for plan-phase --gaps consumption. Filled in as user reports test results. -->
<!-- Test failure handling: do NOT auto-create a gap-closure plan — surface to user for approval per project memory rule (feedback_no_unauthorized_changes.md). -->

- gap_id: GAP-84.1-01
  source_test: 2 (NOTIF-15) + 3 (NOTIF-16)
  severity: high
  status: fixed-pending-retest
  issue: |
    NOTIF-15 (PR decision) and NOTIF-16 (RFP fully paid) had zero wiring in finance.js — Plan 01 Task 3 was silently skipped by the original executor. User initial-test feedback confirmed the bug: procurement users were not notified when their PRs were processed or when RFPs were marked Paid.
  fix: |
    Retroactively executed Plan 01 Task 3 in a follow-up gsd-executor run. Commit f5d9940 added 5 notification blocks to finance.js (NOTIF-15 approve + reject, NOTIF-17 approve + reject, NOTIF-16 fully paid) plus the missing import line. node --check passes, all grep counts pass.
  re-test: needed — see Tests 2 + 3
  no_replan_needed: true

- gap_id: GAP-84.1-02
  source_test: 1 (NOTIF-14 codeless project)
  severity: medium
  status: pending-deploy
  issue: |
    NOTIF-14 fan-out from non-admin actor returned 0 recipients on initial test. Most likely the firestore.rules `users.list` relaxation has not been deployed to the live Firebase project — until then, LIST against `users` from procurement / operations_user / services_user actors is rejected and the helper try/catches it to 0 silently.
  fix: |
    `firebase deploy --only firestore:rules` to dev (and later prod). No code change needed — file change shipped in commit ec35c21.
  re-test: needed — see Test 1
  no_replan_needed: true
