---
status: diagnosed
phase: 86.1-inline-grid-editor-gantt-predecessor
source: [86.1-VERIFICATION.md]
started: 2026-05-06T07:10:00Z
updated: 2026-05-06T09:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Grid renders with 7 columns — no modal/filter UI
expected: Open /projects/{project-with-tasks}/plan — left rail shows spreadsheet grid with columns: # | Name | Duration | Start | End | Predecessors | Resources. No Add Task button, no Filters button, no modal overlays.
result: pass

### 2. Save-on-blur: Name cell edit commits to Firestore
expected: Edit a Name cell, tab/click out — Firestore project_tasks doc updates (name + updated_at) within ~500ms, no error toast.
result: issue
reported: "yes, however it posts it twice"
severity: major

### 3. Duration arithmetic: '3d' on start_date=2026-05-10 → end=2026-05-12
expected: Inclusive arithmetic: 1d=same day, 3d=+2 days. end_date field in Firestore updated.
result: issue
reported: "does not auto-display — requires manual date picker input for Start; should default Start to today. Also must type '5d' suffix; plain positive integer (e.g. '5') should be accepted as days."
severity: major

### 4. Add task via empty trailing row + Enter
expected: Type a name in the bottom empty row + press Enter → new Firestore doc with row_order = max+1; task appears in grid and Gantt.
result: issue
reported: "Yes but it posts twice so it duplicates — two tasks created instead of one"
severity: major

### 5. Right-click context menu — Indent writes parent_task_id
expected: Context menu at cursor; Indent on row 2+ fires; Firestore doc shows parent_task_id = row above's task_id.
result: pass

### 6. Drag-to-reorder + D-Q4 validation
expected: Drag row by left handle (outside input) → row moves, row_order shifts via writeBatch. Dragging parent onto own child shows toast "Can't drop a task into its own subtree."
result: issue
reported: "drag-to-reorder works, but can drop a parent onto its own subtree — no toast, no rejection"
severity: major

### 7. Resource Names picker — pill toggle writes assignees
expected: Click Resources cell → popup anchored below with personnel pills. Toggle a pill → assignees array updates atomically in Firestore. Outside click closes popup.
result: issue
reported: "Resources should allow free typing — these are not app users, they are external/non-app people. Pill picker from app personnel is wrong for this use case."
severity: major

### 8. Gantt drag-to-link creates FS dependency
expected: Hover non-parent bar → blue circle handle at right edge. Drag to another bar → dependency created in Firestore, Frappe arrow renders, Predecessors column on target row shows source row#.
result: pass

### 9. Cycle rejection via Gantt drag-to-link
expected: With A→B→C, drag C's handle to A → toast "This dependency would create a cycle: ... Link rejected." No Firestore write.
result: pass

### 10. Drag to empty whitespace — silent cancel
expected: Rubber-band line vanishes on mouseup outside any bar. No Firestore change, no toast.
result: pass

### 11. Parent summary bar — NO link handle
expected: Hover a parent bar (one with children) → NO blue circle handle appears. D-07 guard confirmed.
result: pass

## Summary

total: 11
passed: 6
issues: 5
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Resources cell accepts free-text input (resources are not app users)"
  status: failed
  reason: "User reported: Resources should allow free typing — these are not app users, they are external/non-app people. Pill picker from app personnel is wrong for this use case."
  severity: major
  test: 7
  root_cause: "Resources cell renders a <span class=tg-resource-names> that opens openAssigneePicker() (app-user pill popup). Needs to be replaced with a plain <input data-col=resources> that saves a free-text string to a new Firestore field `resources` on project_tasks."
  artifacts:
    - path: "app/views/project-plan.js"
      issue: "renderTaskGrid lines 243-246 (assigneeNames derivation), lines 280-283 (span cell), bindGridEvents lines 350-355 (click→picker), openAssigneePicker lines 862-925 (entire function)"
  missing:
    - "Replace span with <input class=tg-res-input data-col=resources>"
    - "Remove bindGridEvents click block for .tg-resource-names"
    - "Add resources branch in handleGridCellBlur writing {resources, updated_at}"
    - "Add resources:'' to handleNewRowCommit and gridInsertRowAbove payloads"
    - "Delete openAssigneePicker function"
  debug_session: ""

- truth: "D-Q4: dragging a parent onto its own subtree is rejected with toast 'Can't drop a task into its own subtree'"
  status: failed
  reason: "User reported: drag-to-reorder works, but can drop a parent onto its own subtree — no toast, no rejection"
  severity: major
  test: 6
  root_cause: "_gridDragOverHandler (line 378) calls e.preventDefault() for all non-self rows with no isDescendant check. If an onSnapshot fires mid-drag and renderTaskGrid replaces innerHTML + re-registers handlers, the drop event lands on a new DOM row where closest('.tg-row') may return null, causing _gridDropHandler to exit at line 393 before handleRowDrop is called — silent success from browser's perspective."
  artifacts:
    - path: "app/views/project-plan.js"
      issue: "_gridDragOverHandler lines 378-388 missing isDescendant guard; isDescendant defined inline in handleRowDrop lines 774-781"
  missing:
    - "Extract isDescendant to module scope"
    - "Add isDescendant check in _gridDragOverHandler before e.preventDefault() — skip prevent+highlight if target is a descendant"
  debug_session: ""

- truth: "Add-via-empty-row creates exactly one new task in Firestore"
  status: failed
  reason: "User reported: Yes but it posts twice so it duplicates — two tasks created instead of one"
  severity: major
  test: 4
  root_cause: "renderTaskGrid() sets container.innerHTML (line 295-307) BEFORE calling bindGridEvents() (line 309). Replacing innerHTML destroys the focused __new__ input, firing blur on it while the OLD _gridInputHandler is still registered — causing handleNewRowCommit to run a second time before the tasks array is updated by the resulting snapshot. Same mechanism as Test 2."
  artifacts:
    - path: "app/views/project-plan.js"
      issue: "renderTaskGrid lines 295-309: innerHTML set before bindGridEvents called; handleNewRowCommit lines 475-476: no synchronous re-entry guard before first await"
  missing:
    - "Move bindGridEvents(container) call to BEFORE container.innerHTML assignment (Fix A)"
    - "OR clear input.value synchronously at top of handleNewRowCommit before first await (Fix B)"
  debug_session: ""

- truth: "Duration arithmetic works without manual date entry — Start defaults to today; plain integer input (e.g. '5') accepted as days"
  status: failed
  reason: "User reported: does not auto-display — requires manual date picker input for Start; should default Start to today. Also must type '5d' suffix; plain positive integer should be accepted."
  severity: major
  test: 3
  root_cause: "parseDuration regex /^(\\d+)\\s*([dw])$/i requires a 'd'/'w' suffix — bare integers rejected. handleNewRowCommit sets start_date:'' — no default to today's date when Duration is typed on a row with no Start."
  artifacts:
    - path: "app/views/project-plan.js"
      issue: "parseDuration function — regex requires suffix; handleGridCellBlur duration branch — no default-start-to-today fallback"
  missing:
    - "Update parseDuration to accept bare positive integers as days (treat as Nd)"
    - "In duration blur handler: if t.start_date is empty, default start_date to today before computing end_date"
  debug_session: ""

- truth: "Name cell blur commits to Firestore exactly once (name + updated_at)"
  status: failed
  reason: "User reported: yes, however it posts it twice"
  severity: major
  test: 2
  root_cause: "renderTaskGrid() sets container.innerHTML (lines 295-307) BEFORE calling bindGridEvents() (line 309). Replacing innerHTML destroys the focused input, firing a blur event on it while the OLD _gridInputHandler is still registered on the container — so handleGridCellBlur runs twice before the tasks array equality guard can stop the second write."
  artifacts:
    - path: "app/views/project-plan.js"
      issue: "renderTaskGrid lines 295-309: innerHTML set before bindGridEvents removes old handler"
  missing:
    - "Move bindGridEvents(container) call to BEFORE container.innerHTML assignment so old handler is detached before DOM replacement triggers blur"
  debug_session: ""
