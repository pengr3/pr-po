---
phase: 86.1-inline-grid-editor-gantt-predecessor
plan: "01"
subsystem: project-management
tags: [gantt, project-plan, inline-grid, firestore-write, contenteditable, css-rewrite]

dependency_graph:
  requires:
    - Phase 86 Plans 01-05 (Frappe Gantt, firestore.rules, project-plan.js scaffold, task-id.js)
  provides:
    - renderTaskGrid() inline spreadsheet grid replacing renderTaskTree()
    - handleGridCellBlur() save-on-blur Firestore write path
    - handleNewRowCommit() add-via-empty-row with row_order field
    - parseDuration() / computeDurationDisplay() / addDays() / computeDepth() helpers
    - bindGridEvents() scoped event delegation (capture-phase blur, date change, context menu stub)
    - rowOrderCache stable row# index for Predecessors column
    - Phase 86.1 task grid CSS (.task-grid-rail, .tg-* classes)
  affects:
    - app/views/project-plan.js (major rewrite — modal layer deleted, grid scaffold added)
    - styles/views.css (task-tree block removed, task-grid block inserted)

tech_stack:
  added: []
  patterns:
    - Scoped event delegation (capture-phase blur on container, not document.body)
    - rowOrderCache Map rebuilt on every snapshot for stable Predecessors display (D-Q3)
    - row_order integer field on project_tasks for stable task ordering (D-Q1)
    - Parent-locked cells via data-parent-locked + tg-locked span (D-07)
    - 5-attempt ID collision retry for handleNewRowCommit (mirrors saveTaskFromModal pattern)
    - escapeHTML on all user-controlled string interpolations in renderTaskGrid (T-86.1.1-01)
    - parseDuration strict regex /^(\d+)\s*([dw])$/i (T-86.1.1-02)
    - detectDependencyCycle reused from Phase 86 for Predecessors cycle prevention (T-86.1.1-03)
    - recomputeParentDates called after start/end writes (D-11)

key_files:
  created: []
  modified:
    - app/views/project-plan.js
    - styles/views.css

decisions:
  - "D-Q2 enforced: filter panel fully removed — getFilteredTasks, getVisibleTaskSet, togglePlanFilters, renderPlanFilterPanel, clearPlanFilters and all their state vars deleted from project-plan.js; .plan-filter-panel deleted from views.css"
  - "D-Q1 stable row#: rowOrderCache rebuilt on every snapshot from sorted row_order; Predecessors column shows stable row#s that never change on reorder"
  - "D-03 duration format: parseDuration strict regex, addDays inclusive (1d=same day), computeDurationDisplay rounds to Nd"
  - "D-07 parent locking: Duration/Start/End cells rendered as tg-locked spans (not inputs) when isParentSet.has(task_id); tooltip explains why"
  - "D-04 add-via-empty-row: trailing tg-empty-row always present, Enter commits via handleNewRowKeydown"
  - "T-86.1.1-01 XSS: all user-controlled strings in renderTaskGrid wrapped in escapeHTML (18 total usages post-Task-2)"
  - "handleGanttBarClick simplified to no-op body — selectTaskRow was deleted; Plan 02 will re-wire"
  - "editTaskProgress revert path changed from renderTaskTree() to renderGantt() — tree no longer exists"
  - "bindGridEvents context menu calls showTaskContextMenu guarded as typeof check — Plan 02 defines the function"
  - "bindGridEvents Resource Names click guards openAssigneePicker as typeof check — Plan 03 defines the function"

metrics:
  duration_minutes: 9
  completed_date: "2026-05-06"
  tasks_completed: 3
  tasks_total: 3
  files_modified: 2
---

# Phase 86.1 Plan 01: Scaffold Inline Grid Summary

**One-liner:** Replaced Phase 86 modal-based task editor and filter panel with a ProjectLibre-style inline spreadsheet grid — save-on-blur Firestore writes, duration parsing ("3d"/"2w"), add-via-empty-row with stable `row_order`, and full XSS protection on all rendered user data.

## What Was Built

### Task 1: DELETE-ONLY Pass (commit 3e3d36c)

Lines deleted from `app/views/project-plan.js` (636 lines net removed):
- Module-scope state: `expandedTaskIds`, `selectedTaskId`, `filterDateFrom`, `filterDateTo`, `filterAssignees`, `modalEditingTaskId`, `modalSelectedAssignees`
- From `render()`: Filters button, Add Task button, plan-filter-panel div, modal mount divs; task-tree div → task-grid-rail div
- From `init()`: addTaskBtn disabled block; window.* registrations for all modal/filter/tree handlers (13 removed, 3 kept)
- From `destroy()`: matching delete window.* teardowns, expandedTaskIds/selectedTaskId resets
- Functions deleted: `renderTaskTree`, `toggleTaskExpand`, `selectTaskRow`, `togglePlanFilters`, `renderPlanFilterPanel`, `applyPlanFilters`, `toggleFilterAssignee`, `clearPlanFilters`, `getFilteredTasks`, `getVisibleTaskSet`, `computeWeightedProgress`, `openAddTaskModal`, `openEditTaskModal`, `renderTaskFormModal`, `closeTaskFormModal`, `toggleTaskAssignee`, `confirmDeleteTask`, `closeDeleteTaskConfirm`, `saveTaskFromModal`
- `renderGantt()` updated: `getVisibleTaskSet()` replaced with `tasks.slice()` (D-Q2: all tasks visible)
- `handleGanttBarClick` simplified to no-op (selectTaskRow deleted)

### Task 2: PURE-ADD Pass (commit 78236ef)

Lines added to `app/views/project-plan.js` (+362 lines net):
- Module-scope grid state: `rowOrderCache`, `_gridInputHandler`, `_gridChangeHandler`, `_gridContextMenuHandler`
- `destroy()` grid cleanup block: remove event listeners, null handler refs, reset rowOrderCache
- `window.handleNewRowKeydown` registration + teardown
- `renderTaskGrid()`: 7-column table (# / Name / Duration / Start / End / Predecessors / Resources), sort by row_order, rebuild rowOrderCache, parent-locked cells, escapeHTML on all interpolations, empty trailing row
- `bindGridEvents()`: capture-phase blur delegation, change delegation for date inputs, context menu stub (Plan 02), Resource Names click guard (Plan 03)
- `handleGridCellBlur()`: 5 column branches, parseDuration validation, row#→task_id resolution, cycle detection, recomputeParentDates after date writes, permission-denied error toast
- `handleNewRowCommit()`: row_order=max+1, 5-attempt ID retry, full setDoc payload with row_order field
- `handleNewRowKeydown()`: Enter commits, Escape clears
- Duration helpers: `parseDuration()`, `computeDurationDisplay()`, `addDays()`, `computeDepth()`

### Task 3: CSS Rewrite (commit d5627a4)

Changes to `styles/views.css` (+113 / -45 net):
- Deleted: `.plan-filter-panel` rule (5 lines), entire `.task-tree*` block (57 lines)
- Inserted: Phase 86.1 task grid CSS block (113 lines) between `.plan-split-pane` and `.gantt-pane`:
  - `.task-grid-rail`, `.task-grid`, `.task-grid thead th`
  - Column widths: `.tg-rn`, `.tg-name`, `.tg-dur`, `.tg-date`, `.tg-pred`, `.tg-res`
  - `.tg-row` / hover / `.tg-empty-row`, `.tg-input` / `:focus` (blue halo), `.tg-date-input`
  - `.tg-locked` (gray italic, cursor:help), `.tg-resource-names` (blue, ellipsis, hover underline)
  - `.tg-delete-confirm` + button variants (Plan 02 prep), `.tg-drag-handle` (Plan 02 prep)
  - `.gantt-link-handle` + `.gantt-rubber-band` (Plan 04 prep)
  - `@media (max-width: 768px) { .task-grid-rail { display: none; } }`

## Final File Metrics

- `app/views/project-plan.js`: 964 lines (was 1,228 after Phase 86 Plan 05 — net -264 lines; ~500 deleted + ~250 added per plan estimate)
- `styles/views.css`: 2,682 lines (was 2,607 after Phase 86 Plan 05 — net +75 lines; delete 62 + insert 113)

## PM Requirement Status

- **PM-01 (create tasks):** Satisfied via `handleNewRowCommit()` — typing in empty row + Enter creates task in Firestore with `row_order = max+1`
- **PM-05 (progress edits):** `handleGanttProgressChange()` (Gantt drag-progress path from Phase 86 Plan 03) and `editTaskProgress()` (slider write) both survive untouched — confirmed by `grep -c "function handleGanttProgressChange" app/views/project-plan.js` == 1

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] handleGanttBarClick referenced deleted selectTaskRow**
- **Found during:** Task 1 post-deletion check
- **Issue:** `handleGanttBarClick(task)` body called `selectTaskRow(task.id)` which was deleted in Task 1
- **Fix:** Replaced body with no-op comment `// Plan 02 will wire grid row selection here`
- **Files modified:** `app/views/project-plan.js`
- **Commit:** 3e3d36c

**2. [Rule 1 - Bug] editTaskProgress catch block referenced deleted renderTaskTree**
- **Found during:** Task 1 post-deletion check
- **Issue:** `editTaskProgress()` catch block called `renderTaskTree()` to revert UI — function deleted
- **Fix:** Changed to `renderGantt()` revert (keeps Gantt in sync on error; grid will also re-sync on next snapshot)
- **Files modified:** `app/views/project-plan.js`
- **Commit:** 3e3d36c

**3. [Rule 2 - Missing Critical] bindGridEvents context menu and Resource Names click guarded**
- **Found during:** Task 2 implementation
- **Issue:** PATTERNS.md excerpt calls `showTaskContextMenu()` and `openAssigneePicker()` directly inside bindGridEvents — neither function exists until Plan 02 and Plan 03 respectively. Direct calls would throw ReferenceError at runtime on first right-click / resource-names click
- **Fix:** Wrapped both calls in `typeof` guards: `if (typeof showTaskContextMenu === 'function') showTaskContextMenu(...)` and `if (typeof openAssigneePicker === 'function') openAssigneePicker(...)`
- **Files modified:** `app/views/project-plan.js`
- **Commit:** 78236ef

## Threat Mitigation Status

All T-86.1.1-01..04 mitigations landed as required:
- **T-86.1.1-01 XSS:** `escapeHTML()` on every user-controlled string in `renderTaskGrid()` — 18 total usages in the file post-Task-2
- **T-86.1.1-02 Duration tampering:** `parseDuration()` uses strict regex `/^(\d+)\s*([dw])$/i` — null on no-match, no Firestore write
- **T-86.1.1-03 Predecessor tampering:** row#→task_id resolution, dedupe, `detectDependencyCycle()` before write, revert on cycle
- **T-86.1.1-04 Date tampering:** `<input type="date">` browser validation + JS guard `rawValue < t.start_date` → return without write

## Known Stubs

- `handleGanttBarClick()`: no-op body — Plan 02 will wire grid row highlight on Gantt bar click
- `bindGridEvents()` context menu: no-op guard — Plan 02 defines `showTaskContextMenu()`
- `bindGridEvents()` Resource Names click: guarded — Plan 03 defines `openAssigneePicker()`
- `_gridContextMenuHandler` declared but Plan 02 body pending
- `start_date` not written on new task from `handleNewRowCommit()` — user must edit Start cell after creating; this matches the "name + Enter" spec (D-04 only requires name for initial commit)

## Self-Check: PASSED

| Item | Status |
|------|--------|
| `app/views/project-plan.js` exists | FOUND |
| `styles/views.css` exists | FOUND |
| `86.1-01-SUMMARY.md` exists | FOUND |
| Task 1 commit `3e3d36c` | FOUND |
| Task 2 commit `78236ef` | FOUND |
| Task 3 commit `d5627a4` | FOUND |
| `node --check app/views/project-plan.js` | PASS |
