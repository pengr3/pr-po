---
phase: 86.1-inline-grid-editor-gantt-predecessor
verified: 2026-05-06T07:10:00Z
status: human_needed
score: 12/13 must-haves verified
overrides_applied: 0
re_verification: false
human_verification:
  - test: "Open /projects/{project-with-tasks}/plan in browser — confirm spreadsheet grid renders with 7 columns (# | Name | Duration | Start | End | Predecessors | Resources) and no modal/filter UI visible"
    expected: "Grid visible with all tasks, no Add Task button, no Filters button, no modal overlays"
    why_human: "DOM rendering cannot be verified without a browser; no automated test runner in this project"
  - test: "Edit a Name cell, tab/click out — verify Firestore project_tasks doc updates (name + updated_at)"
    expected: "Save-on-blur commits to Firestore within ~500ms; no toast on success"
    why_human: "Firestore write timing and real-time subscription behavior requires live browser + Firestore console"
  - test: "Type '3d' in Duration cell on a row with start_date=2026-05-10 — verify End becomes 2026-05-12"
    expected: "Inclusive arithmetic: 1d=same day, 3d=+2 days; end_date field in Firestore updated"
    why_human: "Arithmetic correctness on live data requires browser execution"
  - test: "Type a name in the bottom empty trailing row + press Enter — verify new task appears in grid with row_order = max+1"
    expected: "New Firestore doc created with row_order integer, task appears in grid and Gantt"
    why_human: "Requires live Firebase write and onSnapshot re-render to confirm"
  - test: "Right-click any non-empty grid row — context menu appears; click Indent (on row 2+) — verify parent_task_id updated in Firestore"
    expected: "Context menu at cursor position; Indent fires; Firestore doc shows parent_task_id = row above's task_id"
    why_human: "Right-click event and Firestore write require browser + Firestore console"
  - test: "Drag a grid row by its left handle (outside input cells) — drop on another row — verify row_order values shift in Firestore"
    expected: "Dragged row moves; row_order updated for all affected tasks via writeBatch; D-Q4: dragging parent onto own child shows toast"
    why_human: "HTML5 drag-and-drop gesture and resulting writeBatch require browser interaction"
  - test: "Click Resource Names cell on any non-empty row — pill popup appears; toggle a pill — verify assignees array updates in Firestore"
    expected: "Popup anchored below cell; pills show project personnel; each toggle writes assignees atomically; outside click closes popup"
    why_human: "Popup positioning, pill toggling, and Firestore write require live browser"
  - test: "Hover a non-parent Gantt bar — blue circle handle appears at right edge; mousedown + drag to another bar — verify dependencies array updated in Firestore and Frappe arrow renders"
    expected: "Handle appears on hover; rubber-band SVG line tracks cursor; drop on different bar creates FS dependency; Predecessors column on target row shows source row#"
    why_human: "SVG overlay, mouse event handling, and Frappe Gantt re-render require browser"
  - test: "Attempt a cycle via Gantt drag-to-link (e.g. C→A when A→B→C exists) — verify toast 'This dependency would create a cycle: ... Link rejected.' and no Firestore write"
    expected: "Toast shown, dependency array unchanged in Firestore"
    why_human: "Cycle detection path requires live task data and browser event sequence"
  - test: "Drag Gantt handle to empty whitespace (not on any bar) — verify silent cancel, no write"
    expected: "Rubber-band line vanishes, no Firestore change, no toast"
    why_human: "SVG mouseup target resolution requires browser"
  - test: "Hover a parent summary bar (one with children) — verify NO blue circle handle appears"
    expected: "parent-summary-bar guard prevents handle creation on parent bars (D-07)"
    why_human: "Frappe SVG rendering and CSS class assignment require browser"
---

# Phase 86.1: Inline Grid Editor + Gantt Predecessor Verification Report

**Phase Goal:** Replace Phase 86's modal-based task editor with a directly-editable inline spreadsheet grid (ProjectLibre-style) and add Gantt drag-to-link predecessor creation. Deliver PM-01 (create tasks), PM-02 (hierarchy), PM-03 (FS dependencies via 2 surfaces), PM-05 (progress via Gantt).
**Verified:** 2026-05-06T07:10:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | User sees a spreadsheet grid in the left rail with 7 columns (# / Name / Duration / Start / End / Predecessors / Resources) | ✓ VERIFIED | `renderTaskGrid()` present (1 def); renders `<table class="task-grid">` with 7 `<th>` columns; `task-grid-rail` id in render() shell |
| 2  | User can edit Name, Duration, Start, End, Predecessors cells directly without opening any modal | ✓ VERIFIED | `handleGridCellBlur()` present; 5 col branches (name/duration/start/end/predecessors); no modal functions remain (`openAddTaskModal` count=0, `renderTaskFormModal` count=0, `modal-overlay` count=0) |
| 3  | Duration arithmetic: '3d'/'2w' computes End=Start+N; Start change recomputes End; End change recomputes Duration | ✓ VERIFIED | `parseDuration()`, `computeDurationDisplay()`, `addDays()` all present (1 each); duration, start, end branches in handleGridCellBlur confirmed |
| 4  | Empty trailing row + Enter creates new task in Firestore with row_order=max+1 | ✓ VERIFIED | `handleNewRowCommit()` present; `row_order: newRowOrder` at line 509; `setDoc(doc(db,'project_tasks',taskId), docData)` at line 513; `data-task-id="__new__"` present (4 occurrences); `handleNewRowKeydown()` present and window-registered |
| 5  | All Phase 86 modal CRUD entry points are gone — no modal in this view | ✓ VERIFIED | `renderTaskTree`=0, `openAddTaskModal`=0, `renderTaskFormModal`=0, `saveTaskFromModal`=0, `togglePlanFilters`=0, `getVisibleTaskSet`=0; `modal-overlay`=0; `id="taskTree"`=0 |
| 6  | Filter panel removed — Filters button, .plan-filter-panel CSS, filter state vars all gone | ✓ VERIFIED | `filtersToggleBtn`=0, `taskFormModalMount`=0; `.plan-filter-panel` CSS=0; `expandedTaskIds`=0, `filterDateFrom`=0, `modalEditingTaskId`=0 |
| 7  | Predecessors cycle guard: saving a cycle-creating dep rejected with toast; detectDependencyCycle reused | ✓ VERIFIED | `detectDependencyCycle` count=5 (def + ≥2 callers); `detectDependencyCycle(newDeps` at line 1318 (drag-to-link) + handleGridCellBlur branch confirmed |
| 8  | Right-click context menu with Indent/Outdent/Insert Row Above/Delete Row; hierarchy operations write parent_task_id to Firestore | ✓ VERIFIED | `showTaskContextMenu`=1 def, 2 total; `gridIndentTask/Outdent/InsertRowAbove/DeleteRow` each=1; `window.gridIndentTask` reg=3 (init + 2 onclick); `delete window.gridIndentTask`=1; `deleteTaskNow` count=7; `tg-delete-confirm`=3; `recomputeParentDates` count=10 |
| 9  | Drag-to-reorder rows via HTML5 drag; D-Q4 hierarchy constraints enforced; commitRowOrderReorder writeBatch | ✓ VERIFIED | `commitRowOrderReorder`=1; `handleRowDrop`=1; `_gridDragStartHandler` count=6; `row_order:.+globalIdx` confirmed at line 815; D-Q4 toast strings both present (1 each); `addEventListener('dragstart/dragover/drop/dragend')` confirmed |
| 10 | Resource Names cell click opens inline pill popup; each pill toggle writes assignees atomically; outside-click closes | ✓ VERIFIED | `openAssigneePicker`=1 def; NOT on window (count=0); `tg-resource-names` click wired in `bindGridEvents()` at line 351; `currentAssignees` count=7; `taskAssigneePicker` count=3; `normalizePersonnel` count=4; `.personnel-pill.selected` exists in components.css (line 1575) |
| 11 | Gantt drag-to-link: hover handle on non-parent bars; rubber-band SVG; drop creates FS dependency; cycle rejection | ✓ VERIFIED | `initGanttDragLink()`=1 def + called from renderGantt() at line 1052; `ganttDragState`=10 occurrences; `gantt-link-handle` count=4; `gantt-rubber-band`=1; `parent-summary-bar` count=3; `detectDependencyCycle(newDeps` at line 1318; `dependencies: newDeps` at line 1327; `Link rejected`=1; `createElementNS`=3; `getScreenCTM`=1; `linkBound` sentinel=5 |
| 12 | PM-05 (progress from Gantt): handleGanttProgressChange and editTaskProgress survive from Phase 86 | ✓ VERIFIED | `handleGanttProgressChange`=1; `editTaskProgress`=1; both still wired |
| 13 | All writes land on existing Phase 86 firestore.rules predicates without modification | ? UNCERTAIN | No changes to firestore.rules detected from summaries; row_order/parent_task_id writes use tier-1 update path which plan explicitly confirmed covers these fields — but cannot programmatically verify deployed rules state |

**Score:** 12/13 truths verified (13th is UNCERTAIN/needs human)

### Deferred Items

None. All 4 requirement targets (PM-01, PM-02, PM-03, PM-05) are addressed in this phase.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/views/project-plan.js` | renderTaskGrid + all new grid helpers; modal layer deleted | ✓ VERIFIED | Syntax OK; all 17 required functions present; 6 deleted functions absent; 5 survivors confirmed |
| `styles/views.css` | `.task-grid-rail`, `.tg-*` CSS; `.task-tree*` and `.plan-filter-panel` deleted | ✓ VERIFIED | `.task-tree`=0, `.plan-filter-panel`=0; `.task-grid-rail`=1, `.task-grid`=2, `.tg-input`=2, `.tg-locked`=1, `.tg-resource-names`=2, `.tg-delete-confirm`=5, `.tg-drag-handle`=1, `.gantt-link-handle`=2; tg-dragging/tg-drag-over present |
| `styles/components.css` | `.personnel-pill.selected` modifier (added by Plan 03) | ✓ VERIFIED | `.personnel-pill.selected` at line 1575 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `renderTaskGrid()` | `rowOrderCache` Map | rebuilt every snapshot | ✓ WIRED | `rowOrderCache = new Map()` rebuilt at line 194; `rowOrderCache.set(t.task_id, i+1)` at line 195; count=7 |
| `handleGridCellBlur()` | Firestore project_tasks | `updateDoc(doc(db,'project_tasks',taskId), {...})` | ✓ WIRED | `updateDoc(doc(db, 'project_tasks', taskId), updateData)` at line 459; confirmed 5 column branches |
| `handleNewRowCommit()` | Firestore project_tasks | `setDoc` with `row_order` field | ✓ WIRED | `row_order: newRowOrder` at line 509; `setDoc(doc(db,'project_tasks',taskId), docData)` at line 513 |
| `onSnapshot listener in init()` | `renderTaskGrid()` | snapshot callback | ✓ WIRED | `renderTaskGrid()` called at line 110 inside tasksUnsub callback |
| `_gridContextMenuHandler in bindGridEvents` | `showTaskContextMenu()` | `container.addEventListener('contextmenu', ...)` | ✓ WIRED | `showTaskContextMenu(` count=2; handler wired in bindGridEvents per Plan 02 |
| `showTaskContextMenu items` | `window.gridIndentTask` etc | inline onclick handlers | ✓ WIRED | `window.gridIndentTask` count=3 (init + 2 onclick refs); `delete window.gridIndentTask`=1 |
| `gridDeleteRow with hasChildren` | `deleteTaskNow()` cascade | inline confirm Yes button | ✓ WIRED | `deleteTaskNow(` count=7 (def + callers); `tg-delete-confirm`=3 |
| `Row drag handlers` | Firestore project_tasks via `commitRowOrderReorder` writeBatch | `batch.update(doc(db,'project_tasks',...), {row_order, updated_at})` | ✓ WIRED | `commitRowOrderReorder`=1 def, called from handleRowDrop; `row_order: globalIdx + 1` at line 815 |
| `bindGridEvents .tg-resource-names` | `openAssigneePicker()` | `el.addEventListener('click', ...)` with typeof guard | ✓ WIRED | Click handler at line 351-353; `openAssigneePicker` defined at line 865; guard resolves true |
| `openAssigneePicker pill click` | Firestore project_tasks.assignees | `updateDoc({assignees: [...currentAssignees], updated_at})` | ✓ WIRED | `updateDoc(doc(db, 'project_tasks', taskId), {` at line 905; `assignees:` count=3 in file |
| `renderGantt() bottom` | `initGanttDragLink()` | call after applyFsViolationStyles/renderTodayLine | ✓ WIRED | `initGanttDragLink()` at line 1052, after applyFsViolationStyles (line 1042) and renderTodayLine (line 1045); call order correct |
| `initGanttDragLink mouseup` | Firestore project_tasks.dependencies | `updateDoc({dependencies: newDeps, updated_at})` | ✓ WIRED | `dependencies: newDeps` at line 1327; `updateDoc(doc(db, 'project_tasks', toTaskId)` at line 1326 |
| `initGanttDragLink cycle check` | `detectDependencyCycle()` | `detectDependencyCycle(newDeps, toTaskId)` before commit | ✓ WIRED | `detectDependencyCycle(newDeps` at line 1318; runs before updateDoc |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `renderTaskGrid()` | `tasks[]` | `onSnapshot(query(project_tasks, project_id==))` | Yes — live Firestore subscription | ✓ FLOWING |
| `handleNewRowCommit()` | `row_order: newRowOrder` | `tasks.reduce(max row_order)+1` | Yes — computed from live tasks array | ✓ FLOWING |
| `openAssigneePicker()` | `normalizePersonnel(currentProject)` | `currentProject` loaded from Firestore getDocs | Yes — project doc from Firestore | ✓ FLOWING |
| `initGanttDragLink()` | `tasks[]` / `existingDeps` | same onSnapshot as renderTaskGrid | Yes — same live subscription | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| JS syntax valid | `node --check app/views/project-plan.js` | exits 0 | ✓ PASS |
| renderTaskGrid function exists | `grep -c "function renderTaskGrid"` | 1 | ✓ PASS |
| handleGridCellBlur function exists | `grep -c "function handleGridCellBlur"` | 1 | ✓ PASS |
| parseDuration function exists | `grep -c "function parseDuration"` | 1 | ✓ PASS |
| initGanttDragLink defined and called | `grep -c "initGanttDragLink()"` | 2 | ✓ PASS |
| All deleted functions absent | grep counts for 6 deleted functions | all 0 | ✓ PASS |
| modal-overlay absent | `grep -c "modal-overlay"` | 0 | ✓ PASS |
| CSS grid classes present | `.task-grid-rail`, `.tg-input`, etc. | all ≥1 | ✓ PASS |
| escapeHTML coverage | `grep -c "escapeHTML"` | 25 (≥10 new) | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| PM-01 | Plans 01, 02, 03 | Create tasks with name, dates, personnel | ✓ SATISFIED | `handleNewRowCommit()` (name+row_order), `handleGridCellBlur()` (dates), `openAssigneePicker()` (assignees), `gridInsertRowAbove()` (hierarchy insert) |
| PM-02 | Plan 02 | Task hierarchy: parent/child, rollup | ✓ SATISFIED | `gridIndentTask/Outdent()` write `parent_task_id`; `recomputeParentDates()` called on hierarchy changes (count=10); `computeDepth()` present |
| PM-03 | Plans 01, 04 | Finish-to-Start dependencies via 2 surfaces | ✓ SATISFIED | Surface 1: Predecessors column in `handleGridCellBlur()` (row#→task_id→dependencies write); Surface 2: `initGanttDragLink()` (drag-to-link→updateDoc dependencies) |
| PM-05 | Plan 01 (survivor) | Progress from task list and Gantt | ✓ SATISFIED | `handleGanttProgressChange()` (Gantt-drag progress, Phase 86 survivor) + `editTaskProgress()` (slider write, Phase 86 survivor) both present and confirmed |

### Anti-Patterns Found

| File | Location | Pattern | Severity | Impact |
|------|----------|---------|----------|--------|
| `app/views/project-plan.js` | bindGridEvents() line 353 | `typeof openAssigneePicker === 'function'` guard | ℹ️ Info | Intentional forward-reference guard from Plan 01; Plan 03 defines the function so it always resolves true now — harmless dead guard, not a stub |
| `app/views/project-plan.js` | handleGanttBarClick() | No-op body `// Plan 02 will wire grid row selection here` | ⚠️ Warning | Gantt bar click does not highlight the corresponding grid row; acknowledged in all 4 SUMMARYs as out-of-scope for Phase 86.1; does not block PM-01/02/03/05 |

No blockers found. The `handleGanttBarClick` no-op is cosmetic (click-to-select not required by any of PM-01/02/03/05).

### Human Verification Required

See frontmatter `human_verification` section. All 11 items require browser execution against live Firebase.

The key behaviors that need UAT confirmation:

**1. Grid Rendering**
**Test:** Open `/projects/{project-with-tasks}/plan`
**Expected:** 7-column spreadsheet grid (# / Name / Duration / Start / End / Predecessors / Resources); no Add Task button, no Filters button, no modal overlays
**Why human:** DOM rendering requires browser

**2. Save-on-blur (all columns)**
**Test:** Edit Name, Duration ("3d"), Start, End, Predecessors cells; tab/click out after each
**Expected:** Firestore updates within ~500ms; duration arithmetic correct (3d on 2026-05-10 → end 2026-05-12)
**Why human:** Firestore write timing and live onSnapshot require browser

**3. Add-via-empty-row**
**Test:** Click empty trailing row; type name + Enter
**Expected:** New task in Firestore with `row_order = max+1`; appears in grid and Gantt
**Why human:** Firebase write + re-render require browser

**4. Context menu + hierarchy**
**Test:** Right-click row 2+; click Indent
**Expected:** Context menu at cursor; `parent_task_id` updated in Firestore
**Why human:** Right-click event + Firestore write require browser

**5. Drag-to-reorder**
**Test:** Drag a row by handle; drop on another row
**Expected:** `row_order` values updated via writeBatch; D-Q4 blocked with toast if dragging parent onto own child
**Why human:** HTML5 drag requires browser

**6. Assignee picker**
**Test:** Click Resource Names cell; toggle a pill
**Expected:** Popup anchored below cell; `assignees` array updated atomically; outside click closes
**Why human:** Popup positioning + Firestore write require browser

**7. Gantt drag-to-link (PM-03 second surface)**
**Test:** Hover non-parent bar → handle appears → drag to another bar → drop
**Expected:** Frappe arrow renders; Predecessors column shows source row#; cycle attempt toasts "This dependency would create a cycle: ... Link rejected."
**Why human:** SVG overlay + mouse events + Frappe re-render require browser

**8. Parent bar lock (D-07)**
**Test:** Hover a parent summary bar (task with children)
**Expected:** NO blue circle handle appears
**Why human:** Frappe SVG class assignment requires browser inspection

### Gaps Summary

No blockers or hard failures detected. The codebase fully implements the phase goal at the code level:

- All 17 new functions are present (Plans 01-04)
- All 6 modal/filter/tree functions from Phase 86 are deleted
- All key links are wired (onSnapshot → renderTaskGrid; blur → Firestore; drag-to-link → detectDependencyCycle → Firestore)
- CSS grid classes replace CSS tree classes correctly
- PM-01, PM-02, PM-03, PM-05 all have traceable code paths
- XSS protection (escapeHTML × 25), cycle detection, D-Q4 hierarchy constraints, D-07 parent lock all verifiable in code

Status is `human_needed` because this is a zero-build static SPA with no automated test runner. Live browser UAT is the only way to confirm the UI renders correctly and Firestore writes complete.

---

_Verified: 2026-05-06T07:10:00Z_
_Verifier: Claude (gsd-verifier)_
