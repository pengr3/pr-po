---
phase: 86.1-inline-grid-editor-gantt-predecessor
verified: 2026-05-06T10:30:00Z
status: human_needed
score: 13/13 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 12/13
  gaps_closed:
    - "Name cell blur commits to Firestore exactly once (bindGridEvents before innerHTML + re-entry guard)"
    - "Add-via-empty-row creates exactly one new task (re-entry guard + synchronous clear)"
    - "Duration arithmetic — bare integer accepted, Start defaults to today"
    - "D-Q4: dragging parent onto own subtree rejected at drag-over time (no highlight, no drop)"
    - "Resources cell is plain free-text input (not pill picker); writes resources field to Firestore"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Open /projects/{project-with-tasks}/plan in browser — confirm grid renders, Name/Duration/Start/End/Predecessors/Resources cells are editable inline, edit a Name and tab away — confirm exactly one Firestore write"
    expected: "One write to project_tasks (name + updated_at); no double-write visible in Firestore console or Network tab"
    why_human: "Firestore write count and real-time subscription behavior require live browser + Firestore console"
  - test: "Type '5' (no suffix) in a Duration cell on a row with no Start — tab away"
    expected: "Start defaults to today's date; End = today + 4 days; both written to Firestore"
    why_human: "Date arithmetic result on live data and Firestore write require browser execution"
  - test: "Type '3d' in Duration on a row with start_date=2026-05-10 — tab away"
    expected: "End = 2026-05-12 (inclusive: 3d = +2 days); only end_date written (start unchanged)"
    why_human: "Arithmetic correctness on live data requires browser execution"
  - test: "Type a name in the empty trailing row, press Enter — confirm exactly one task created"
    expected: "One new Firestore doc with row_order = max+1; single task appears in grid and Gantt; no duplicate"
    why_human: "Requires live Firebase write and onSnapshot re-render to confirm single creation"
  - test: "Right-click any non-empty grid row — context menu appears; click Indent (on row 2+)"
    expected: "Context menu at cursor position; Indent fires; Firestore doc shows parent_task_id = row above's task_id"
    why_human: "Right-click event and Firestore write require browser + Firestore console"
  - test: "Drag a parent task row by its left handle — drag over one of its own child rows — confirm no blue highlight on child; complete the drop — confirm toast"
    expected: "No tg-drag-over highlight on descendant rows during drag; completing drop shows toast 'Can't drop a task into its own subtree.'"
    why_human: "HTML5 drag-and-drop gesture and visual highlight behavior require browser interaction"
  - test: "Click the Resources cell on any non-empty row — confirm it is a plain text input (no popup)"
    expected: "Input is editable inline; type a name, tab away — Firestore project_tasks doc shows resources field with typed string"
    why_human: "Popup absence and Firestore field write require live browser + Firestore console"
  - test: "Create a new task via empty trailing row — check its Firestore doc"
    expected: "Doc includes resources: '' field (empty string)"
    why_human: "Requires live Firebase write to confirm field presence"
  - test: "Hover a non-parent Gantt bar — blue circle handle appears at right edge; drag to another bar"
    expected: "Rubber-band SVG line tracks cursor; drop creates FS dependency; Predecessors column on target row shows source row#"
    why_human: "SVG overlay, mouse event handling, and Frappe Gantt re-render require browser"
  - test: "Attempt a cycle via Gantt drag-to-link (e.g. C→A when A→B→C exists)"
    expected: "Toast 'This dependency would create a cycle: ... Link rejected.' and no Firestore write"
    why_human: "Cycle detection path requires live task data and browser event sequence"
  - test: "Drag Gantt handle to empty whitespace (not on any bar)"
    expected: "Rubber-band line vanishes, no Firestore change, no toast"
    why_human: "SVG mouseup target resolution requires browser"
  - test: "Hover a parent summary bar (one with children)"
    expected: "NO blue circle handle appears (D-07 parent lock)"
    why_human: "Frappe SVG rendering and CSS class assignment require browser inspection"
  - test: "Confirm Firestore Security Rules allow row_order, resources, parent_task_id writes without modification to firestore.rules"
    expected: "Writes succeed for project admin role; no permission-denied errors"
    why_human: "Cannot programmatically verify deployed Firestore rules state"
---

# Phase 86.1: Inline Grid Editor + Gantt Predecessor Verification Report

**Phase Goal:** Fix 5 UAT-diagnosed defects in the inline grid editor (double-write, duration UX, subtree drag guard, resources free-text) and restore all PM-01/PM-02/PM-03/PM-05 capabilities.
**Verified:** 2026-05-06T10:30:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure plans 05 and 06

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | bindGridEvents(container) called BEFORE container.innerHTML in non-empty renderTaskGrid path | ✓ VERIFIED | Line 299: `bindGridEvents(container)` precedes `container.innerHTML = ...` at line 301 |
| 2  | handleNewRowCommit has re-entry guard (!input.value.trim() return) at top | ✓ VERIFIED | Line 483: `if (!input.value.trim()) return;` is the first statement; line 485: `input.value = ''` before first await |
| 3  | parseDuration accepts bare positive integers (mBare regex pattern present) | ✓ VERIFIED | Lines 814-819: mSuffix path for '3d'/'2w', mBare path `/^(\d+)$/` with `n > 0` guard |
| 4  | handleGridCellBlur duration branch uses effectiveStart (t.start_date or today) | ✓ VERIFIED | Line 424: `const effectiveStart = t.start_date \|\| formatDateISO(new Date());` — writes {start_date, end_date} or {end_date} conditionally |
| 5  | isDescendant is a module-scope function (not inside another function) | ✓ VERIFIED | `^function isDescendant` matches at line 187, which is before renderTaskGrid (line 196) — module scope confirmed |
| 6  | _gridDragOverHandler calls isDescendant before e.preventDefault() | ✓ VERIFIED | Lines 381-382: `if (isDescendant(row.dataset.taskId, _draggedTaskId)) return;` appears before `e.preventDefault()` at line 383 |
| 7  | Resources cell uses input.tg-res-input (not span.tg-resource-names) | ✓ VERIFIED | Lines 284-287: `<input class="tg-input tg-res-input" value="${escapeHTML(t.resources \|\| '')}" data-col="resources" data-task-id="...">` |
| 8  | handleGridCellBlur has col === 'resources' branch | ✓ VERIFIED | Lines 457-461: `} else if (col === 'resources') { const res = rawValue; if (res === (t.resources ?? '')) return; updateData = { resources: res, updated_at: serverTimestamp() }; }` |
| 9  | openAssigneePicker function is deleted (count should be 0) | ✓ VERIFIED | grep -c "openAssigneePicker\|tg-resource-names\|assigneeNames\|taskAssigneePicker" = 0 |
| 10 | handleNewRowCommit and gridInsertRowAbove both have resources: '' in their payloads | ✓ VERIFIED | Line 517 (handleNewRowCommit docData) and line 714 (gridInsertRowAbove setDoc) both contain `resources: ''` |
| 11 | node --check app/views/project-plan.js passes | ✓ VERIFIED | Exit code 0 — no syntax errors |
| 12 | PM-05 (progress from Gantt): handleGanttProgressChange and editTaskProgress survive | ✓ VERIFIED | Both functions present; not modified by Plans 05/06 |
| 13 | All writes land on existing Phase 86 firestore.rules predicates without modification | ? UNCERTAIN | No changes to firestore.rules detected from summaries; row_order/parent_task_id/resources writes use tier-1 update path — but deployed rules state cannot be verified programmatically |

**Score:** 13/13 truths verified (13th is UNCERTAIN/needs human)

### Deferred Items

None. All 4 requirement targets (PM-01, PM-02, PM-03, PM-05) are addressed in this phase.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/views/project-plan.js` | Fixed renderTaskGrid, parseDuration, handleGridCellBlur, handleNewRowCommit; isDescendant module-scope; resources free-text; openAssigneePicker removed | ✓ VERIFIED | Syntax OK; all gap-closure changes confirmed in source |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `renderTaskGrid` | `bindGridEvents` | call order — bindGridEvents first, then innerHTML | ✓ WIRED | line 299 bindGridEvents, line 301 container.innerHTML |
| `_gridDragOverHandler` | `isDescendant` | module-scope call before e.preventDefault() | ✓ WIRED | line 381: `isDescendant(row.dataset.taskId, _draggedTaskId)` |
| `handleGridCellBlur` | `project_tasks` Firestore | resources branch — updateDoc({resources, updated_at}) | ✓ WIRED | line 460: `updateData = { resources: res, updated_at: serverTimestamp() }` → line 466: updateDoc |
| `parseDuration` | bare integer path | mBare regex `/^(\d+)$/` with n>0 guard | ✓ WIRED | lines 816-819 |
| `handleNewRowCommit` | resources field | `resources: ''` in docData | ✓ WIRED | line 517 |
| `gridInsertRowAbove` | resources field | `resources: ''` in setDoc payload | ✓ WIRED | line 714 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `renderTaskGrid()` | `tasks[]` | `onSnapshot(query(project_tasks, project_id==))` | Yes — live Firestore subscription | ✓ FLOWING |
| `handleNewRowCommit()` | `row_order: newRowOrder` | `tasks.reduce(max row_order)+1` | Yes — computed from live tasks array | ✓ FLOWING |
| `handleGridCellBlur (resources)` | `rawValue` | `input.value.trim()` from tg-res-input | Yes — user-typed free text | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| JS syntax valid | `node --check app/views/project-plan.js` | exit 0 | ✓ PASS |
| bindGridEvents before innerHTML (non-empty path) | grep line numbers for bindGridEvents(container) and container.innerHTML= | line 299 < line 301 | ✓ PASS |
| re-entry guard present | grep line 483 | `if (!input.value.trim()) return;` | ✓ PASS |
| mBare regex present | grep mBare | line 816 | ✓ PASS |
| effectiveStart present | grep effectiveStart | line 424 | ✓ PASS |
| isDescendant module-scope | `^function isDescendant` at line 187 | exactly 1 match | ✓ PASS |
| const isDescendant count | grep -c "const isDescendant" | 0 | ✓ PASS |
| isDescendant before e.preventDefault in dragover | line 381 vs line 383 | 381 < 383 | ✓ PASS |
| tg-res-input present | grep tg-res-input line 285 | input element confirmed | ✓ PASS |
| col === 'resources' branch | grep col === 'resources' | line 457 | ✓ PASS |
| openAssigneePicker/tg-resource-names count | grep -c combined | 0 | ✓ PASS |
| resources: '' in both payloads | grep "resources: ''" | lines 517, 714 | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| PM-01 | Plans 01, 02, 03, 05, 06 | Create tasks with name, dates, personnel/resources | ✓ SATISFIED | `handleNewRowCommit()` (name+row_order+resources:''); `handleGridCellBlur()` (dates, resources); `gridInsertRowAbove()` (resources:'') |
| PM-02 | Plan 02 | Task hierarchy: parent/child, rollup | ✓ SATISFIED | `gridIndentTask/Outdent()` write `parent_task_id`; `recomputeParentDates()` on hierarchy changes; `isDescendant()` module-scope subtree guard |
| PM-03 | Plans 01, 04 | Finish-to-Start dependencies via 2 surfaces | ✓ SATISFIED | Surface 1: Predecessors column in handleGridCellBlur(); Surface 2: initGanttDragLink() with detectDependencyCycle |
| PM-05 | Plan 01 (survivor) | Progress from task list and Gantt | ✓ SATISFIED | `handleGanttProgressChange()` and `editTaskProgress()` both present and unmodified |

### Anti-Patterns Found

| File | Location | Pattern | Severity | Impact |
|------|----------|---------|----------|--------|
| `app/views/project-plan.js` | handleGanttBarClick() | No-op body | ⚠️ Warning | Gantt bar click does not highlight corresponding grid row; acknowledged out-of-scope for Phase 86.1; does not block PM-01/02/03/05 |

No blockers found.

### Human Verification Required

All items below require browser execution against live Firebase — no automated test runner exists in this zero-build static SPA.

**1. Double-write regression check (gap 1 + gap 2)**
**Test:** Edit a Name cell, tab away. Then type a name in the empty trailing row, press Enter.
**Expected:** Exactly one Firestore write per action (no duplicate writes, no duplicate tasks).
**Why human:** Firestore write count requires Network tab or Firestore console observation.

**2. Duration bare-integer UX (gap 3)**
**Test:** Type '5' in a Duration cell on a row with no Start date, tab away.
**Expected:** Start defaults to today; End = today + 4 days; both written to Firestore.
**Why human:** Date arithmetic on live data requires browser execution.

**3. Duration with existing Start**
**Test:** Type '3d' in Duration on a row with start_date=2026-05-10, tab away.
**Expected:** End = 2026-05-12; only end_date written (start unchanged).
**Why human:** Arithmetic correctness requires browser execution.

**4. Subtree drag guard — visual (gap 4 D-Q4)**
**Test:** Drag a parent task over one of its child rows.
**Expected:** No tg-drag-over blue highlight on the child row during drag.
**Why human:** CSS class application during drag-over events requires browser.

**5. Subtree drag guard — toast on drop**
**Test:** Complete the drag of parent onto child.
**Expected:** Toast "Can't drop a task into its own subtree."
**Why human:** HTML5 drag-and-drop gesture requires browser.

**6. Resources free-text input (gap 5)**
**Test:** Click a Resources cell — confirm it is a plain text input (no pill popup). Type a name, tab away.
**Expected:** Firestore project_tasks doc shows `resources: "typed text"`. No assignee popup appears.
**Why human:** Popup absence and Firestore field write require live browser + Firestore console.

**7. Resources: '' in new task doc**
**Test:** Create a new task via empty trailing row. Inspect Firestore doc.
**Expected:** `resources: ""` field present in doc.
**Why human:** Requires live Firebase write confirmation.

**8. Right-click context menu + hierarchy**
**Test:** Right-click row 2+; click Indent.
**Expected:** Context menu at cursor; parent_task_id updated in Firestore.
**Why human:** Right-click event + Firestore write require browser.

**9. Gantt drag-to-link creates FS dependency (PM-03)**
**Test:** Hover non-parent bar, drag handle to another bar, drop.
**Expected:** Frappe arrow renders; Predecessors column shows source row#; dependency in Firestore.
**Why human:** SVG overlay, mouse events, Frappe re-render require browser.

**10. Cycle rejection via Gantt drag-to-link**
**Test:** With A→B→C, drag C's handle to A.
**Expected:** Toast "This dependency would create a cycle: ... Link rejected." No Firestore write.
**Why human:** Cycle detection with live task data requires browser.

**11. Parent summary bar — NO link handle (D-07)**
**Test:** Hover a parent bar (task with children).
**Expected:** No blue circle handle appears.
**Why human:** Frappe SVG class assignment requires browser inspection.

**12. Firestore Security Rules compatibility**
**Test:** Perform all write operations as a project admin role user.
**Expected:** All writes succeed (name, duration/start/end, predecessors, resources, row_order, parent_task_id, dependencies) without permission-denied errors.
**Why human:** Deployed rules state cannot be verified programmatically.

### Gaps Summary

No code-level blockers. All 5 UAT-diagnosed defects are closed in Plans 05 and 06:

- **Gap 1** (Name cell double-write): bindGridEvents moved before container.innerHTML at line 299 (was after at line 309). Re-entry guard at line 483 of handleNewRowCommit as defense-in-depth.
- **Gap 2** (add-via-empty-row duplicate): Same bindGridEvents fix + re-entry guard resolves the second fire path.
- **Gap 3** (Duration UX): parseDuration mBare path at lines 816-819 accepts bare integers; effectiveStart at line 424 defaults start to today.
- **Gap 4** (D-Q4 subtree drag): isDescendant extracted to module scope at line 187; guard added to _gridDragOverHandler at line 381 before e.preventDefault() at line 383.
- **Gap 5** (Resources free-text): Resources cell is now `<input class="tg-res-input" data-col="resources">` at line 285; col === 'resources' branch in handleGridCellBlur at line 457; resources: '' in both new-task payloads at lines 517 and 714; openAssigneePicker deleted (0 occurrences).

Status remains `human_needed` because this is a zero-build static SPA with no automated test runner. Live browser UAT is required to confirm the 5 closed gaps actually behave correctly at runtime and to complete the remaining 8 carry-forward items from the initial verification.

---

_Verified: 2026-05-06T10:30:00Z_
_Verifier: Claude (gsd-verifier)_
