---
plan: 91.4-01
phase: 91.4-rfp-fees-financial-breakdown-inclusion
title: "Tier 1 — RFP fee inclusion in expense-modal, project-detail, service-detail"
status: complete
completed: 2026-06-04
commit: 84b3cc5
files_modified:
  - app/expense-modal.js
  - app/views/project-detail.js
  - app/views/service-detail.js
deviations: none
---

# Summary: Plan 91.4-01

## What was done

All three Tier 1 files were updated to include RFP fees (transfer_fee, cash_out_fee, misc_fees) in expense totals. No new Firestore reads were required — all three files already iterate individual RFP docs in memory.

**app/expense-modal.js:**
- Added `getRFPTotal` to utils import
- Line ~85: `totalRequested += parseFloat(rfp.amount_requested || 0)` → `totalRequested += getRFPTotal(rfp)`
- Result: "Total Requested via RFPs" figure in the expense modal is now fee-inclusive

**app/views/project-detail.js:**
- Added `getRFPFees` to utils import
- Replaced dead `rfpTotalRequested` (accumulated but never stored) with active `rfpFeesTotal`
- `currentExpense.total`: `poTotal + trTotal` → `poTotal + trTotal + rfpFeesTotal`
- `currentExpense.remainingPayable`: updated to include `rfpFeesTotal`

**app/views/service-detail.js:**
- Added `getRFPFees` to utils import
- Added `rfpFeesTotal: 0` to module-level declaration and reset declaration
- Replaced dead `rfpTotalRequested` with `rfpFeesTotal` in rfpSnap loop
- `currentServiceExpense.rfpFeesTotal` stored; `remainingPayable` and displayed totals updated
- Displayed total (line ~428) and remaining budget calc (line ~440) now include `rfpFeesTotal`

## Verification

`node --check` passed on all three files. No `rfpTotalRequested` references remain in either detail view. Legacy RFPs (no fee fields) return 0 from `getRFPFees` — backwards-safe.
