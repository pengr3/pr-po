# Plan 92.2-02 Execution Summary

## Goal
Remove the legacy services status chart infrastructure from the home page now that scorecard strips on `services.js` (delivered in plan 92.2-01) supersede it. Eliminates a dead Firestore listener, orphaned Chart.js instances, and unused CSS.

## Scope
Pure removal/cleanup. No procurement-related code touched.

## Files Modified

### `app/views/home.js`
Removed the following services-chart infrastructure:

- `UNIFIED_STATUS_OPTIONS` const + leading comment block
- `chartInstances` Map registry + leading comment
- `HIGHLIGHTED_STATUS_COLORS` const
- `MONOCHROMATIC_STATUS_COLORS` const
- `MONOCHROMATIC_FALLBACK` const
- `getBarColor()` helper
- `getChartSizeClass()` helper
- `buildStatusBreakdownContainer()` function + JSDoc
- `servicesCardHtml()` function + JSDoc
- `renderStatusBreakdown()` function + JSDoc (full Chart.js rendering logic)
- `cachedStats.servicesByStatusOneTime` key
- `cachedStats.servicesByStatusRecurring` key
- `if (mode === 'services' || mode === 'both') { statsContent += servicesCardHtml(); }` block in `render()`
- Services `onSnapshot` block in `loadStats()` (collection `services` listener)
- Chart.js teardown in `destroy()` (`chartInstances.forEach(...)` + `chartInstances.clear()`)
- Phase 77.1 comment in `destroy()` JSDoc referencing Chart.js cleanup

### `styles/views.css`
Removed the dead CSS block:

- Section header comment `HOME DASHBOARD — UNIFIED STATUS CHART`
- Phase 77.2 sizing explainer comment
- `.hs-chart-canvas.hs-chart-status { height: 180px; min-height: 180px; }`
- `@media (max-width: 768px) { .hs-chart-canvas.hs-chart-status { height: 220px; min-height: 220px; } }`

## What Was Preserved

- `getDashboardMode()` — still consumed by `render()` and `init()`
- `procurementCardHtml()` — procurement card unaffected
- `updateStatDisplay()` — used by all three procurement listeners
- All three procurement `onSnapshot` listeners (MRFs, PRs, POs)
- `statsListeners` array + cleanup loop in `destroy()`
- Stale-while-revalidate `cachedStats` pattern for procurement keys
- All other `.hs-chart-canvas` rules and surrounding CSS sections

## Verification (post-edit)

| Check | Expected | Result |
|---|---|---|
| Removed identifiers in `home.js` | 0 | 0 |
| Retained identifiers in `home.js` (getDashboardMode, procurementCardHtml, updateStatDisplay, statsListeners) | hits | 16 hits |
| `servicesByStatus` in `home.js` | 0 | 0 |
| `hs-chart-status` in `views.css` | 0 | 0 |

## Net Effect

- One Firestore `onSnapshot` listener on the `services` collection eliminated on every home-page mount across all role modes that include services (`services_admin`, `services_user`, `super_admin`, `finance`, `procurement_staff`, unknown).
- Chart.js instance lifecycle no longer managed from this view; only the procurement card remains under `quick-stats`.
- `home.js` reduced from 441 lines to 207 lines.

## Follow-ups

None — services scorecards already render via `services.js` (plan 92.2-01).
