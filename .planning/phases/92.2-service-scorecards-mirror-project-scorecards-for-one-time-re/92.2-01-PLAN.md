---
phase: 92.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/services.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Services view shows a scorecard strip of 10 status cards above the filter bar"
    - "Clicking a scorecard card filters the table to that status (toggle off on re-click)"
    - "Clicking outside the scorecard strip clears the status filter"
    - "Total card shows count of all tab-scoped role-scoped services and is active when no status filter is set"
    - "The Status dropdown select (#serviceProjectStatusFilter) is gone from the filter bar"
    - "Scorecard counts reflect the current sub-tab (services vs recurring) and role scope, not client/search filters"
  artifacts:
    - path: "app/views/services.js"
      provides: "Service scorecard strip — renderServiceScorecards, handleServiceScorecardClick, activeServiceStatusFilter, SCORECARD_STATUS_OPTIONS"
      contains: "renderServiceScorecards"
  key_links:
    - from: "app/views/services.js handleServiceScorecardClick"
      to: "applyServiceFilters"
      via: "direct call"
      pattern: "handleServiceScorecardClick.*applyServiceFilters"
    - from: "app/views/services.js applyServiceFilters"
      to: "renderServiceScorecards(scopedServicesForTab)"
      via: "call at end of function"
      pattern: "renderServiceScorecards\\(scopedServicesForTab\\)"
---

<objective>
Add an interactive status scorecard strip to services.js, mirroring the Phase 92 implementation in projects.js. Replace the #serviceProjectStatusFilter dropdown with clickable scorecard cards. Wire click-outside dismissal.

Purpose: Services view gains the same scorecard UX as projects (Phase 92 + 92.1) — color-coded, tab-scoped, role-scoped status counts with toggle filtering.
Output: Modified app/views/services.js with scorecard strip, handleServiceScorecardClick, renderServiceScorecards, updated applyServiceFilters, and click-outside listener in init/destroy.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From app/views/projects.js (the implementation to mirror — read the actual source for exact code):

Module-scope state added in Phase 92:
  let activeStatusFilter = null;
  const UNIFIED_STATUS_OPTIONS = [...10 statuses, no Draft...];

Scorecard HTML pattern (in render(), after add-form, before filter-bar):
  div#projectScorecards.project-scorecards
    UNIFIED_STATUS_OPTIONS.map(s => div.project-scorecard-card[data-status="${s}"] onclick="window.handleScorecardClick('${s}'); event.stopPropagation()"
      span.scorecard-label ${s}
      span.scorecard-count#scorecard-count-${s.replace(/\s+/g, '_')} —
    div.project-scorecard-card.project-scorecard-card--total[data-status="__total__"] onclick="window.handleScorecardClick(null); event.stopPropagation()"
      span.scorecard-label Total
      span.scorecard-count#scorecard-count-total —

renderScorecards(baseProjects) signature:
  - Iterates UNIFIED_STATUS_OPTIONS, reads count by filtering baseProjects on project_status
  - Updates span#scorecard-count-{s.replace(/\s+/g,'_')} textContent
  - Updates span#scorecard-count-total textContent
  - Removes --active from all .project-scorecard-card
  - Adds --active to [data-status="${activeStatusFilter || '__total__'}"]

handleScorecardClick(status):
  - If status === null OR status === activeStatusFilter → set activeStatusFilter = null
  - Else → set activeStatusFilter = status
  - Calls applyFilters()

applyFilters() scorecard integration:
  - Builds scopedProjects (allProjects filtered by assignedCodes scope, not by status/client/search)
  - Uses activeStatusFilter variable (NOT a DOM read) for matchesProjectStatus check
  - Calls renderScorecards(scopedProjects) at the END

init() additions:
  window._scorecardClickOutside = (e) => {
    const strip = document.getElementById('projectScorecards');
    if (!strip) return;
    if (!strip.contains(e.target)) { activeStatusFilter = null; applyFilters(); }
  };
  document.addEventListener('click', window._scorecardClickOutside);

destroy() additions:
  if (window._scorecardClickOutside) {
    document.removeEventListener('click', window._scorecardClickOutside);
    delete window._scorecardClickOutside;
  }
  activeStatusFilter = null;
  delete window.handleScorecardClick;

From app/views/services.js (current state — key facts):

Module-scope state (lines 16-34):
  let allServices = [];
  let filteredServices = [];
  let currentActiveTab = 'services';

UNIFIED_STATUS_OPTIONS in services.js (line 36): has 11 values including 'Draft' at index 0.
  KEEP this array as-is (used for Add/Edit form status dropdowns).

attachWindowFunctions() (line 64): add window.handleServiceScorecardClick = handleServiceScorecardClick here.

render() HTML structure (line 109+):
  - Add form at line 137: div#addServiceForm
  - Filter bar at line 218: div.filter-bar containing:
      - div.form-group with select#serviceProjectStatusFilter (REMOVE this entire div.form-group)
      - div.form-group with select#serviceClientFilter (KEEP)
      - div.form-group with input#serviceSearchInput (KEEP)

applyServiceFilters() (line 746):
  - Line 748: reads projectStatusFilter from #serviceProjectStatusFilter (REMOVE this line)
  - Line 757: const assignedCodes = getAssignedServiceCodes();  (KEEP)
  - Line 759-783: filteredServices = allServices.filter(service => { matchesType, matchesSearch, matchesProjectStatus, matchesClient, assignedCodes check })
  - Line 791: renderServicesTable(); (KEEP, add renderServiceScorecards call AFTER this)

init() (line 280): add activeServiceStatusFilter = null reset and scorecard click-outside setup after existing handlers.

destroy() (line 347): add scorecard cleanup before existing window function deletes (around line 380).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add module-scope state and SCORECARD_STATUS_OPTIONS</name>
  <files>app/views/services.js</files>
  <read_first>
    Read app/views/services.js lines 15-50 to see existing let declarations and UNIFIED_STATUS_OPTIONS.
    Read app/views/services.js lines 63-85 (attachWindowFunctions) to see where to add handleServiceScorecardClick.
  </read_first>
  <action>
    In app/views/services.js:

    1. After the existing "let currentActiveTab = 'services';" line (line 33), add two new module-scope declarations:
       - let activeServiceStatusFilter = null;
       - const SCORECARD_STATUS_OPTIONS = UNIFIED_STATUS_OPTIONS.filter(s => s !== 'Draft');

    2. Inside attachWindowFunctions(), after the existing "window.applyServiceFilters = applyServiceFilters;" line, add:
       - window.handleServiceScorecardClick = handleServiceScorecardClick;

    Do NOT modify UNIFIED_STATUS_OPTIONS itself — it remains 11 values including Draft (used by Add/Edit form dropdowns).
  </action>
  <verify>
    grep -n "activeServiceStatusFilter" app/views/services.js
    grep -n "SCORECARD_STATUS_OPTIONS" app/views/services.js
    grep -n "handleServiceScorecardClick" app/views/services.js
  </verify>
  <done>
    services.js contains "let activeServiceStatusFilter = null;" at module scope.
    services.js contains "const SCORECARD_STATUS_OPTIONS = UNIFIED_STATUS_OPTIONS.filter(s => s !== 'Draft');" at module scope.
    services.js contains "window.handleServiceScorecardClick = handleServiceScorecardClick;" inside attachWindowFunctions().
  </done>
</task>

<task type="auto">
  <name>Task 2: Add scorecard strip to render() HTML and remove status dropdown from filter bar</name>
  <files>app/views/services.js</files>
  <read_first>
    Read app/views/services.js lines 109-280 (render() function body) to see the exact HTML structure — specifically the end of the add-form div, the filter-bar div contents, and surrounding structure.
    The scorecard strip goes BETWEEN the add-form closing div and the filter-bar div.
    The #serviceProjectStatusFilter select (and its parent div.form-group, label, select) must be removed.
  </read_first>
  <action>
    In app/views/services.js, inside the render() return template literal:

    1. After the closing tag of the div#addServiceForm block (the div with id="addServiceForm" ends with its closing "div" after form-actions), insert the scorecard strip:

       div#serviceScorecards.project-scorecards containing:
         For each status s in SCORECARD_STATUS_OPTIONS (via template literal map):
           div.project-scorecard-card[data-status="${s}"] with onclick="window.handleServiceScorecardClick('${s}'); event.stopPropagation()"
             span.scorecard-label ${s}
             span.scorecard-count[id="service-scorecard-count-${s.replace(/\s+/g, '_')}"] —
         Then append a Total card:
           div.project-scorecard-card.project-scorecard-card--total[data-status="__total__"] with onclick="window.handleServiceScorecardClick(null); event.stopPropagation()"
             span.scorecard-label Total
             span.scorecard-count[id="service-scorecard-count-total"] —

       The span ID prefix is "service-scorecard-count-" (not "scorecard-count-" from projects.js) to avoid DOM collision.

    2. Inside the filter-bar div (lines ~218-244), remove the entire first div.form-group block — the one containing the label "Status" and the select#serviceProjectStatusFilter element (including the option list generated from UNIFIED_STATUS_OPTIONS). Keep the Client filter div.form-group and the Search div.form-group unchanged.

    Do NOT add any CSS class or inline style that is not already in views.css. The .project-scorecards, .project-scorecard-card, .project-scorecard-card--total, .project-scorecard-card--active, .scorecard-label, .scorecard-count classes and [data-status="..."] color rules are already defined in styles/views.css from Phase 92 and 92.1.
  </action>
  <verify>
    grep -n "serviceScorecards" app/views/services.js
    grep -n "service-scorecard-count-" app/views/services.js
    grep -n "handleServiceScorecardClick" app/views/services.js
    grep -n "serviceProjectStatusFilter" app/views/services.js
  </verify>
  <done>
    services.js render() contains div#serviceScorecards with class="project-scorecards".
    services.js contains span IDs with prefix "service-scorecard-count-".
    services.js render() does NOT contain "serviceProjectStatusFilter" anywhere in the template literal.
    The Total card uses id="service-scorecard-count-total".
  </done>
</task>

<task type="auto">
  <name>Task 3: Add renderServiceScorecards and handleServiceScorecardClick functions, update applyServiceFilters, wire init and destroy</name>
  <files>app/views/services.js</files>
  <read_first>
    Read app/views/services.js lines 744-792 (applyServiceFilters full body) to understand current structure before modifying.
    Read app/views/services.js lines 280-320 (init body) to see where to add the scorecard click-outside setup.
    Read app/views/services.js lines 347-401 (destroy body) to see where to add scorecard cleanup.
    Read app/views/projects.js lines 734-760 (renderScorecards and handleScorecardClick) for the exact pattern to mirror.
  </read_first>
  <action>
    In app/views/services.js, make the following four changes:

    CHANGE A — Add renderServiceScorecards function (place it near applyServiceFilters, before or after sortFilteredServices):

      function renderServiceScorecards(baseServices) {
          for (const s of SCORECARD_STATUS_OPTIONS) {
              const count = baseServices.filter(sv => sv.project_status === s).length;
              const key = s.replace(/\s+/g, '_');
              const el = document.getElementById(`service-scorecard-count-${key}`);
              if (el) el.textContent = count;
          }
          const totalEl = document.getElementById('service-scorecard-count-total');
          if (totalEl) totalEl.textContent = baseServices.length;

          document.querySelectorAll('#serviceScorecards .project-scorecard-card').forEach(card => {
              card.classList.remove('project-scorecard-card--active');
          });
          const activeKey = activeServiceStatusFilter || '__total__';
          document.querySelector(`#serviceScorecards .project-scorecard-card[data-status="${activeKey}"]`)
              ?.classList.add('project-scorecard-card--active');
      }

    CHANGE B — Add handleServiceScorecardClick function (place directly after renderServiceScorecards):

      function handleServiceScorecardClick(status) {
          if (status === null || status === activeServiceStatusFilter) {
              activeServiceStatusFilter = null;
          } else {
              activeServiceStatusFilter = status;
          }
          applyServiceFilters();
      }

    CHANGE C — Modify applyServiceFilters():

      - Remove the line: const projectStatusFilter = document.getElementById('serviceProjectStatusFilter')?.value || '';
      - Build scopedServicesForTab BEFORE the filteredServices assignment. scopedServicesForTab is allServices filtered by TWO conditions only: service_type === serviceTypeFilter AND (assignedCodes scope check). Do NOT apply client, search, or status filters to scopedServicesForTab.
      - Change the inner filter: replace "filteredServices = allServices.filter(service => {" with "filteredServices = scopedServicesForTab.filter(service => {". Inside the filter callback, remove the matchesType check (already guaranteed by scopedServicesForTab) and remove the assignedCodes check (already guaranteed by scopedServicesForTab). Keep matchesSearch, keep the status filter using activeServiceStatusFilter variable instead of the removed projectStatusFilter variable, keep matchesClient.
      - After "renderServicesTable();" at the end of applyServiceFilters(), add the call: renderServiceScorecards(scopedServicesForTab);

      The resulting applyServiceFilters() logic flow:
        1. Read searchTerm from #serviceSearchInput
        2. Read clientFilter from #serviceClientFilter
        3. Compute serviceTypeFilter from currentActiveTab
        4. Compute assignedCodes from getAssignedServiceCodes()
        5. Build scopedServicesForTab = allServices.filter(sv => sv.service_type === serviceTypeFilter && (assignedCodes === null || assignedCodes.includes(sv.service_code)))
        6. filteredServices = scopedServicesForTab.filter(sv => matchesSearch && (activeServiceStatusFilter === null || sv.project_status === activeServiceStatusFilter) && matchesClient)
        7. currentPage = 1
        8. sortFilteredServices()
        9. renderServicesTable()
        10. renderServiceScorecards(scopedServicesForTab)

    CHANGE D — Update init() and destroy():

      In init() (after the existing click-outside handler for the personnel dropdown is set up, before "await loadServiceClients()"):
        - Add: activeServiceStatusFilter = null;
        - Add scorecard click-outside handler:
            window._serviceScorecardClickOutside = (e) => {
                const strip = document.getElementById('serviceScorecards');
                if (!strip) return;
                if (!strip.contains(e.target)) {
                    activeServiceStatusFilter = null;
                    applyServiceFilters();
                }
            };
            document.addEventListener('click', window._serviceScorecardClickOutside);

      In destroy() (before the existing "delete window.toggleAddServiceForm;" block):
        - Add scorecard cleanup:
            if (window._serviceScorecardClickOutside) {
                document.removeEventListener('click', window._serviceScorecardClickOutside);
                delete window._serviceScorecardClickOutside;
            }
            activeServiceStatusFilter = null;
        - Add at the end of the delete window.* block: delete window.handleServiceScorecardClick;
  </action>
  <verify>
    grep -n "renderServiceScorecards" app/views/services.js
    grep -n "handleServiceScorecardClick" app/views/services.js
    grep -n "activeServiceStatusFilter" app/views/services.js
    grep -n "scopedServicesForTab" app/views/services.js
    grep -n "_serviceScorecardClickOutside" app/views/services.js
    grep -n "serviceProjectStatusFilter" app/views/services.js
  </verify>
  <acceptance_criteria>
    - services.js contains function renderServiceScorecards(baseServices) with "service-scorecard-count-" span ID prefix
    - services.js contains function handleServiceScorecardClick(status) that toggles activeServiceStatusFilter and calls applyServiceFilters()
    - services.js applyServiceFilters() does NOT read from #serviceProjectStatusFilter
    - services.js applyServiceFilters() builds scopedServicesForTab and calls renderServiceScorecards(scopedServicesForTab) at the end
    - services.js uses activeServiceStatusFilter (module var, not DOM) for status match in filteredServices filter
    - services.js init() sets activeServiceStatusFilter = null and attaches window._serviceScorecardClickOutside via document.addEventListener('click', ...)
    - services.js destroy() removes the click-outside listener, deletes window._serviceScorecardClickOutside, and deletes window.handleServiceScorecardClick
    - grep -c "serviceProjectStatusFilter" app/views/services.js outputs 0 (no remaining references)
  </acceptance_criteria>
  <done>
    The services view scorecard strip is fully functional: counts update on tab switch and data load, clicking a card filters the table, clicking the Total card or outside clears the filter, and the status dropdown is removed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore → allServices | Already validated in existing onSnapshot handlers; no new boundary introduced |
| User click → handleServiceScorecardClick | UI event handler; status value sourced from SCORECARD_STATUS_OPTIONS const (not user input), no injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92.2-01 | Tampering | handleServiceScorecardClick status param | accept | status argument is hardcoded in onclick HTML from SCORECARD_STATUS_OPTIONS — not user-typed input; no sanitization needed |
| T-92.2-02 | Information Disclosure | scorecard counts visible to all roles | accept | counts derive from role-scoped data (assignedCodes applied before renderServiceScorecards); no cross-role leakage |
</threat_model>

<verification>
Manual verification steps after execution:

1. Load the Services tab (one-time sub-tab). Verify a scorecard strip of 10 status cards + Total appears above the filter bar.
2. Verify the Status dropdown is no longer present in the filter bar.
3. Click a status card (e.g., "On-going"). Verify the table filters to only On-going services and the card shows --active styling.
4. Click the same card again. Verify the filter clears and Total card becomes active.
5. Click a status card, then click outside the strip. Verify the filter clears.
6. Switch to the Recurring sub-tab. Verify the scorecard counts update to reflect recurring services only.
7. Total card count should equal the number of rows in the unfiltered table.
</verification>

<success_criteria>
- services.js render() HTML contains div#serviceScorecards.project-scorecards with 10 status cards and a Total card
- services.js render() HTML does NOT contain #serviceProjectStatusFilter
- renderServiceScorecards(baseServices) updates service-scorecard-count-* spans and manages --active class
- handleServiceScorecardClick toggles activeServiceStatusFilter and calls applyServiceFilters()
- applyServiceFilters() builds scopedServicesForTab (type+scope only) and passes it to renderServiceScorecards
- init() resets activeServiceStatusFilter and attaches click-outside listener
- destroy() tears down click-outside listener and deletes window.handleServiceScorecardClick
</success_criteria>

<output>
After completion, create .planning/phases/92.2-service-scorecards-mirror-project-scorecards-for-one-time-re/92.2-01-SUMMARY.md
</output>
