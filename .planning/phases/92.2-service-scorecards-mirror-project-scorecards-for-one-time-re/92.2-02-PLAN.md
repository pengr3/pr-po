---
phase: 92.2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/home.js
  - styles/views.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "Home page no longer renders a Services chart card for services_admin/services_user/super_admin"
    - "Procurement card still appears for all roles unchanged"
    - "home.js has no reference to servicesCardHtml, renderStatusBreakdown, buildStatusBreakdownContainer, chartInstances, HIGHLIGHTED_STATUS_COLORS, MONOCHROMATIC_STATUS_COLORS, or UNIFIED_STATUS_OPTIONS"
    - "home.js destroy() does not call chartInstances.forEach or chartInstances.clear"
    - "home.js loadStats() does not subscribe to the services Firestore collection"
    - "styles/views.css does not contain .hs-chart-canvas.hs-chart-status"
    - "getDashboardMode() function is preserved (still used for MRF department filtering in loadStats)"
  artifacts:
    - path: "app/views/home.js"
      provides: "Cleaned home view — services chart code removed, procurement card unchanged"
      contains: "getDashboardMode"
    - path: "styles/views.css"
      provides: "Removed dead CSS rule for services chart canvas"
  key_links:
    - from: "app/views/home.js render()"
      to: "procurementCardHtml()"
      via: "direct call — still present"
      pattern: "procurementCardHtml\\(\\)"
---

<objective>
Remove the services status chart infrastructure from home.js (servicesCardHtml, renderStatusBreakdown, buildStatusBreakdownContainer, chartInstances, chart color constants, UNIFIED_STATUS_OPTIONS) and remove the dead .hs-chart-canvas.hs-chart-status CSS rule from styles/views.css. The procurement card is unaffected.

Purpose: The services chart on the home page is superseded by the scorecard strip added to services.js in Plan 01. Removing it eliminates a dead Firestore listener, orphaned Chart.js instances, and dead CSS.
Output: Slimmed home.js with only procurement stats; dead CSS rule removed from views.css.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From app/views/home.js (current state — items to REMOVE vs KEEP):

REMOVE — only used by services chart:
  - const UNIFIED_STATUS_OPTIONS = [...] (lines 11-23)
  - const chartInstances = new Map(); (line 41)
  - const HIGHLIGHTED_STATUS_COLORS = {...} (lines 45-50)
  - const MONOCHROMATIC_STATUS_COLORS = {...} (lines 51-58)
  - const MONOCHROMATIC_FALLBACK = '...' (line 60)
  - function getBarColor(statusLabel) {...} (lines 62-66)
  - function getChartSizeClass(containerId) {...} (lines 68-71)
  - function buildStatusBreakdownContainer(containerId, countsMap, rowCount) {...} (lines 95-98)
  - function renderStatusBreakdown(containerId, countsMap) {...} (lines 164-248 approx)
  - function servicesCardHtml() {...} (lines 132-153)
  - cachedStats keys: servicesByStatusOneTime and servicesByStatusRecurring (lines 34-35)
  - In render(): the if block "if (mode === 'services' || mode === 'both') { statsContent += servicesCardHtml(); }" (lines 259-261)
  - In loadStats(): the entire services block "if (mode === 'services' || mode === 'both') { const servicesListener = onSnapshot(...) }" (lines 370-399)
  - In destroy(): "chartInstances.forEach(chart => { if (chart && typeof chart.destroy === 'function') { chart.destroy(); } }); chartInstances.clear();" (lines 431-436)

KEEP — still needed:
  - function getDashboardMode() (lines 77-82) — used in render() for mode variable, in init() for loadStats(mode) call
  - function procurementCardHtml() (lines 105-125) — procurement card always shown
  - cachedStats keys: activeMRFs, pendingPRs, activePOs (lines 30-33)
  - All three onSnapshot blocks in loadStats() for MRFs, PRs, POs (lines 322-368)
  - function updateStatDisplay() (lines 407-414)
  - let statsListeners = []; (line 26)
  - statsListeners cleanup in destroy() (lines 423-428)
  - render(), init(), destroy() function shells

From styles/views.css (items to REMOVE):

  Line 2282-2293 block:
    Comment block beginning "+ ~24 (top/bottom breathing room)..." (lines 2282-2284)
    .hs-chart-canvas.hs-chart-status { height: 180px; min-height: 180px; } (lines 2285-2288)
    @media (max-width: 768px) { .hs-chart-canvas.hs-chart-status { height: 220px; min-height: 220px; } } (lines 2289-2293)

  These rules are only referenced by the canvas IDs stat-services-ot-status and stat-services-rec-status,
  which are generated inside servicesCardHtml() — being removed in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove services chart code from home.js</name>
  <files>app/views/home.js</files>
  <read_first>
    Read app/views/home.js lines 1-100 to see UNIFIED_STATUS_OPTIONS, cachedStats, chartInstances, color constants, getBarColor, getChartSizeClass, buildStatusBreakdownContainer.
    Read app/views/home.js lines 100-250 to see procurementCardHtml, servicesCardHtml, renderStatusBreakdown.
    Read app/views/home.js lines 245-300 to see render() function and the servicesCardHtml call.
    Read app/views/home.js lines 300-441 to see init, loadStats services block, destroy with chartInstances.clear().
  </read_first>
  <action>
    Remove the following from app/views/home.js in a single edit pass:

    1. Remove the entire UNIFIED_STATUS_OPTIONS const block (the comment line above it starting "// Unified status list for services charts..." plus the const declaration spanning lines 11-23).

    2. Remove the chartInstances Map declaration line: "const chartInstances = new Map();" and the comment block above it (Phase 77.1 comment).

    3. Remove HIGHLIGHTED_STATUS_COLORS const (including its opening comment), MONOCHROMATIC_STATUS_COLORS const, and MONOCHROMATIC_FALLBACK const.

    4. Remove function getBarColor(statusLabel) and its leading comment.

    5. Remove function getChartSizeClass(containerId) and its leading comment.

    6. Remove function buildStatusBreakdownContainer(containerId, countsMap, rowCount) and its JSDoc comment block.

    7. Remove function servicesCardHtml() and its JSDoc comment block.

    8. Remove function renderStatusBreakdown(containerId, countsMap) and its entire JSDoc comment block (this is a large function spanning ~85 lines including the Chart.js creation logic and update path).

    9. In cachedStats object literal: remove the "servicesByStatusOneTime: null," and "servicesByStatusRecurring: null" keys. Also remove the Phase 81 D-05 comment line above them if it refers only to those keys. The resulting cachedStats should contain only activeMRFs, pendingPRs, activePOs.

    10. In render(): remove the entire "if (mode === 'services' || mode === 'both') { statsContent += servicesCardHtml(); }" block (3 lines).

    11. In loadStats(): remove the entire services onSnapshot block: the "// ---- Services card (D-03) ----" comment and the "if (mode === 'services' || mode === 'both') { const servicesListener = onSnapshot(collection(db, 'services'), ... statsListeners.push(servicesListener); }" block.

    12. In destroy(): remove the two chartInstances lines — "chartInstances.forEach(chart => { if (chart && typeof chart.destroy === 'function') { chart.destroy(); } });" and "chartInstances.clear();" — along with the Phase 77.1 comment above them.

    Do NOT remove: getDashboardMode(), procurementCardHtml(), updateStatDisplay(), the three procurement onSnapshot blocks in loadStats(), statsListeners handling, render() shell, init() shell, destroy() shell, or the "cachedStats intentionally NOT reset" comment in destroy().
  </action>
  <verify>
    grep -c "servicesCardHtml\|renderStatusBreakdown\|buildStatusBreakdownContainer\|chartInstances\|HIGHLIGHTED_STATUS_COLORS\|MONOCHROMATIC_STATUS_COLORS\|MONOCHROMATIC_FALLBACK\|getBarColor\|getChartSizeClass" app/views/home.js
    grep -n "getDashboardMode\|procurementCardHtml\|updateStatDisplay\|statsListeners" app/views/home.js
    grep -n "servicesByStatus" app/views/home.js
  </verify>
  <acceptance_criteria>
    - grep output for removed symbols returns 0 (no matches)
    - home.js still contains getDashboardMode, procurementCardHtml, updateStatDisplay, statsListeners
    - home.js cachedStats contains only activeMRFs, pendingPRs, activePOs — servicesByStatusOneTime and servicesByStatusRecurring are gone
    - home.js loadStats() contains only 3 onSnapshot blocks (mrfListener, prListener, poListener) — the servicesListener block is gone
    - home.js destroy() does not contain chartInstances
    - home.js render() does not call servicesCardHtml
  </acceptance_criteria>
  <done>
    home.js is free of all services chart infrastructure. Procurement stats card remains fully functional.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead .hs-chart-canvas.hs-chart-status CSS rule from views.css</name>
  <files>styles/views.css</files>
  <read_first>
    Read styles/views.css lines 2279-2295 to see the exact comment block and CSS rules to remove.
  </read_first>
  <action>
    In styles/views.css, remove the dead CSS block for the services chart canvas size.

    The block to remove spans approximately lines 2282-2293 and contains:
    - A multiline comment (3 lines) describing chart height calculation — the comment ends with "... avoid clipping; mobile gets +40px because wider per-item legend padding pushes legend into 4-5 rows at 360px-414px. */"
    - The rule: .hs-chart-canvas.hs-chart-status { height: 180px; min-height: 180px; }
    - The media query: @media (max-width: 768px) { .hs-chart-canvas.hs-chart-status { height: 220px; min-height: 220px; } } and its closing brace

    These selectors are exclusively referenced by the canvas elements stat-services-ot-status and stat-services-rec-status, which were generated inside the now-deleted servicesCardHtml() function.

    Do NOT remove any other .hs-chart-canvas rules, .hs-stat-card rules, or any surrounding sections.
  </action>
  <verify>
    grep -n "hs-chart-canvas\|hs-chart-status" styles/views.css
  </verify>
  <acceptance_criteria>
    - grep finds zero matches for ".hs-chart-canvas.hs-chart-status" in styles/views.css
    - grep finds zero matches for "hs-chart-status" in styles/views.css
    - No other .hs-chart-canvas rules are affected (if any exist)
  </acceptance_criteria>
  <done>
    Dead CSS rule for services chart canvas height removed. No other styles affected.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| home.js → Firestore | Existing procurement listeners (MRFs, PRs, POs) unchanged; services listener removed entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92.2-03 | Denial of Service | Orphaned Chart.js instances | accept (resolved) | Removing chartInstances eliminates the risk of orphaned canvas listeners — this plan fixes the issue rather than mitigating it |
| T-92.2-04 | Information Disclosure | services Firestore listener removed | accept | Removing the listener reduces the data fetched on home page load; no new exposure introduced |
</threat_model>

<verification>
Manual verification steps after execution:

1. Navigate to the home page as super_admin. Verify only the Procurement card appears in quick-stats (no Services chart card).
2. Navigate to the home page as services_admin. Verify only the Procurement card appears (no Services chart card).
3. Verify Procurement card stats (Pending MRFs, Pending PRs, Active POs) load correctly.
4. Open browser DevTools console. Verify no "Chart is not defined" or "canvas" errors appear.
5. Navigate away from home and back. Verify no memory leak errors or "Cannot read property of undefined" errors.
6. Verify grep -c "hs-chart-status" styles/views.css returns 0.
</verification>

<success_criteria>
- home.js does NOT contain: servicesCardHtml, renderStatusBreakdown, buildStatusBreakdownContainer, chartInstances, UNIFIED_STATUS_OPTIONS, HIGHLIGHTED_STATUS_COLORS, MONOCHROMATIC_STATUS_COLORS, getBarColor, getChartSizeClass
- home.js cachedStats object has exactly 3 keys: activeMRFs, pendingPRs, activePOs
- home.js loadStats() has exactly 3 onSnapshot subscriptions (MRFs, PRs, POs)
- home.js destroy() does not reference chartInstances
- home.js still contains getDashboardMode(), procurementCardHtml(), updateStatDisplay()
- styles/views.css does NOT contain .hs-chart-canvas.hs-chart-status
</success_criteria>

<output>
After completion, create .planning/phases/92.2-service-scorecards-mirror-project-scorecards-for-one-time-re/92.2-02-SUMMARY.md
</output>
