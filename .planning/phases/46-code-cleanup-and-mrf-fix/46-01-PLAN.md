---
phase: 46-code-cleanup-and-mrf-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/project-assignments.js
  - app/views/service-assignments.js
  - nul
  - scripts/validate-permissions.js
  - app/views/procurement.js
  - app/views/finance.js
  - app/views/home.js
  - app/components.js
  - app/expense-modal.js
  - app/firebase.js
  - app/utils.js
  - app/views/assignments.js
  - app/views/procurement-base.js
autonomous: true
requirements:
  - CLN-01
  - CLN-02

must_haves:
  truths:
    - "project-assignments.js and service-assignments.js no longer exist in the repository"
    - "The nul file no longer exists in the repository"
    - "No unreferenced view or utility JS files remain in app/ (aside from seed-roles.js which is flagged for user review)"
    - "All remaining console.log calls in live SPA files use bracketed prefixes like [Module]"
    - "The validate-permissions.js stale route reference to /project-assignments is removed"
    - "A git tag pre-cleanup exists for rollback safety"
  artifacts:
    - path: "app/views/project-assignments.js"
      provides: "DELETED - must not exist"
    - path: "app/views/service-assignments.js"
      provides: "DELETED - must not exist"
    - path: "nul"
      provides: "DELETED - must not exist"
  key_links:
    - from: "app/views/admin.js"
      to: "app/views/assignments.js"
      via: "dynamic import"
      pattern: "import.*assignments"
---

<objective>
Remove dead files, clean ad-hoc console.log calls, and eliminate other codebase debris.

Purpose: Reduce codebase noise by removing files that are no longer referenced by any live import chain, removing the accidental `nul` file, cleaning up ad-hoc debug logs, and fixing a stale route reference in a dev script. Creates a safety git tag before any destructive operations.

Output: Clean codebase with only referenced files, structured-only console.log calls, and a pre-cleanup rollback tag.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-code-cleanup-and-mrf-fix/46-CONTEXT.md
@.planning/phases/46-code-cleanup-and-mrf-fix/46-RESEARCH.md

<interfaces>
<!-- Import chain trace (from 46-RESEARCH.md) confirming dead files: -->
<!--
  LIVE: All views imported by router.js + admin.js sub-imports (assignments.js, user-management.js, role-config.js)
  DEAD: project-assignments.js — zero imports anywhere in SPA
  DEAD: service-assignments.js — zero imports anywhere in SPA
  EDGE CASE: seed-roles.js — not imported by SPA, only by scripts/sync-role-permissions.js
  EDGE CASE: validate-permissions.js line 79 has stale '/project-assignments' route
-->

<!-- Console.log identification rule (from CONTEXT.md): -->
<!-- KEEP: console.log('[Module] ...') — bracketed prefix = structured diagnostic -->
<!-- REMOVE: console.log('...') without bracket prefix — ad-hoc debug output -->
<!-- REMOVE: console.log with emoji prefix (not bracketed) — ad-hoc debug output -->
<!-- KEEP: All console.error() and console.warn() calls -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create safety tag and remove dead files</name>
  <files>app/views/project-assignments.js, app/views/service-assignments.js, nul, scripts/validate-permissions.js</files>
  <action>
1. Create a git tag `pre-cleanup` on the current HEAD for one-command rollback safety:
   ```
   git tag pre-cleanup
   ```

2. Remove dead JS files using `git rm`:
   - `git rm app/views/project-assignments.js`
   - `git rm app/views/service-assignments.js`
   These files are confirmed unreferenced — neither appears in any `import` statement across the SPA import chain. They were superseded by `app/views/assignments.js` in Phase 39.

3. Commit dead JS file removal:
   ```
   git commit -m "chore(46): remove dead project-assignments.js and service-assignments.js"
   ```

4. Remove the accidental `nul` file from repo root:
   ```
   git rm nul
   git commit -m "chore(46): remove accidental nul file"
   ```

5. Fix stale route reference in `scripts/validate-permissions.js`:
   - Find line 79 (or nearby) containing `'/project-assignments': 'role_config'`
   - Remove that entire line/entry (the route no longer exists in router.js)
   - Commit: `git commit -m "chore(46): remove stale /project-assignments route from validate-permissions.js"`

6. CDN dependency audit:
   - Grep index.html for `<script` tags with external `src` URLs (http/https)
   - Grep all CSS files for external URL references
   - Based on research, Firebase is imported via ES module in firebase.js (not a script tag), and `lib/signature_pad.umd.min.js` is self-hosted. No unused CDN script tags are expected.
   - If any unused CDN references are found, remove them and commit separately. If none found (expected), skip this commit.

7. Flag `app/seed-roles.js` for user review:
   - Do NOT delete it. Per CONTEXT.md: "Edge cases (files referenced but never actually used at runtime) are flagged for user review, not auto-removed."
   - Include in the SUMMARY: "seed-roles.js is in app/ but only imported by scripts/sync-role-permissions.js (not the SPA). Recommend: keep in place or move to scripts/. User decision needed."
  </action>
  <verify>
    <automated>cd C:/Users/franc/dev/projects/pr-po && test ! -f app/views/project-assignments.js && test ! -f app/views/service-assignments.js && test ! -f nul && echo "PASS: dead files removed" || echo "FAIL: dead files still exist"</automated>
  </verify>
  <done>project-assignments.js, service-assignments.js, and nul are deleted from the repository. validate-permissions.js stale route is removed. git tag pre-cleanup exists. seed-roles.js is flagged (not deleted) for user review.</done>
</task>

<task type="auto">
  <name>Task 2: Remove ad-hoc console.log calls from all live SPA files</name>
  <files>app/views/procurement.js, app/views/finance.js, app/views/home.js, app/components.js, app/expense-modal.js, app/firebase.js, app/utils.js, app/views/assignments.js, app/views/procurement-base.js</files>
  <action>
Remove all ad-hoc (unprefixed) `console.log()` calls from live SPA files. The rule is:
- **KEEP** any `console.log` whose first string argument starts with `[` (e.g., `console.log('[Router] ...')`).
- **REMOVE** any `console.log` whose first argument does NOT start with `[`. This includes:
  - Plain string logs: `console.log('Projects loaded:', ...)`
  - Emoji-prefixed logs: `console.log('📋 Generating PR...', ...)`
  - Template literal logs without bracket: `` console.log(`Loaded ${count}...`) ``
  - Variable-only logs: `console.log(data)`
- **KEEP** all `console.error()` and `console.warn()` calls — these are error reporting, not debug output.
- **Do NOT modify files that are being deleted** (project-assignments.js, service-assignments.js) — they are already gone from Task 1.
- **Do NOT modify seed-roles.js** — it is a standalone script, not SPA code.

Scan these files (all live SPA files with known ad-hoc logs per research):
- `app/views/procurement.js` (~22 ad-hoc logs)
- `app/views/finance.js` (~9 ad-hoc logs)
- `app/views/home.js` (~3 ad-hoc logs)
- `app/components.js` (~1 ad-hoc log)
- `app/expense-modal.js` (~1 ad-hoc log)
- `app/firebase.js` (~1 ad-hoc log)
- `app/utils.js` (~7 ad-hoc logs)
- `app/views/assignments.js` (~5 ad-hoc logs)
- `app/views/procurement-base.js` (~6 ad-hoc logs)

Also scan any other app/ files not listed above — if they have unprefixed console.log calls, remove those too. The research may not have caught every file.

For each file, read it, identify unprefixed console.log lines, remove them (delete the entire line including any trailing semicolon), and verify surrounding code still makes sense (e.g., don't leave dangling commas or empty blocks).

Commit all console.log removals in a single commit:
```
git commit -m "chore(46): remove ad-hoc console.log calls from SPA files"
```
  </action>
  <verify>
    <automated>cd C:/Users/franc/dev/projects/pr-po && grep -rn "console\.log(" app/ --include="*.js" | grep -v "console\.log(\s*'" | grep -v "console\.log(\s*\`\[" | grep -v seed-roles | grep -v "console\.log('\[" | wc -l</automated>
  </verify>
  <done>All ad-hoc console.log calls are removed from live SPA files. Only bracketed-prefix structured logs remain. All console.error and console.warn calls are preserved.</done>
</task>

</tasks>

<verification>
1. `git log --oneline -5` shows separate commits for: dead JS removal, nul removal, stale route fix, console.log cleanup
2. `git tag -l pre-cleanup` confirms safety tag exists
3. `test ! -f app/views/project-assignments.js && test ! -f app/views/service-assignments.js && test ! -f nul` all pass
4. `grep -rn "console\.log(" app/ --include="*.js" | grep -v seed-roles | grep -v "console\.log('\["` returns zero results (no ad-hoc logs remain in SPA files, excluding template literals with brackets which use backtick+bracket)
5. No `import` statement anywhere in `app/` references `project-assignments` or `service-assignments`
</verification>

<success_criteria>
- project-assignments.js and service-assignments.js are deleted from the repository
- nul file is deleted from the repository
- validate-permissions.js no longer references /project-assignments
- pre-cleanup git tag exists
- Zero ad-hoc console.log calls remain in live SPA files
- All structured console.log calls (bracketed prefix) are preserved
- All console.error and console.warn calls are preserved
- seed-roles.js edge case is documented (flagged, not deleted)
</success_criteria>

<output>
After completion, create `.planning/phases/46-code-cleanup-and-mrf-fix/46-01-SUMMARY.md`
</output>
