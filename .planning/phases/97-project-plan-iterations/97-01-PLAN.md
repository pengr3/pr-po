---
phase: "97"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - firestore.rules
autonomous: true
requirements:
  - Spike-015c
  - Spike-016
  - Spike-017B
  - Spike-018

must_haves:
  truths:
    - "Active users can read project_iterations documents"
    - "Only super_admin and operations_admin can create or delete project_iterations documents"
    - "No role can update a project_iterations document (iterations are write-once)"
  artifacts:
    - path: "firestore.rules"
      provides: "Security rules for project_iterations top-level collection"
      contains: "match /project_iterations/{iterationId}"
  key_links:
    - from: "project-plan.js (addDoc to project_iterations)"
      to: "firestore.rules project_iterations block"
      via: "Firestore Security Rules enforcement"
      pattern: "match /project_iterations/\\{iterationId\\}"
---

<objective>
Add the Firestore Security Rules block for the new `project_iterations` top-level collection.

Purpose: Without these rules, the first JS write from project-plan.js (Plan 02+) will fail with "Missing or insufficient permissions" even for super_admin. Per CLAUDE.md, security rules must ship before or with the first JS write to a new collection.

Output: A new `match /project_iterations/{iterationId}` block in firestore.rules, inserted after the `baselines` subcollection block (after line 248).
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\97-project-plan-iterations\97-RESEARCH.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\CLAUDE.md
@C:\Users\Admin\Roaming\pr-po\firestore.rules
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert project_iterations security rules block</name>
  <files>firestore.rules</files>
  <read_first>
    - firestore.rules lines 230–260: baselines subcollection block ends at line 248, the closing brace for the projects match is at line 249; insert the new top-level block AFTER line 249 (after the closing brace of the projects match block) as a top-level collection match — NOT as a subcollection of projects
    - firestore.rules lines 496–543: project_tasks block pattern for reference
    - 97-RESEARCH.md "Firestore Security Rules" section: exact block to insert
  </read_first>
  <action>
    Insert the following block in firestore.rules as a new top-level collection rule, placed after the closing brace of the `match /projects/{projectId}` block (near line 249) and before the `project_tasks` collection block.

    The block reads:

    Section header comment: "project_iterations collection (Phase 97 — rewindable plan save-states)"
    Comment notes: Top-level collection; project_id field used for project-scoping at JS query layer.
    Read: all active users via isActiveUser().
    Create: hasRole(['super_admin', 'operations_admin']).
    Update: if false — iterations are write-once (no editing labels/tasks).
    Delete: hasRole(['super_admin', 'operations_admin']) — needed for Undo mechanic (auto-snapshot cleanup).

    Exact match path: match /project_iterations/{iterationId}

    Do NOT place this inside `match /projects/{projectId}` — it is a top-level collection, not a subcollection.
  </action>
  <verify>
    Open firestore.rules and confirm:
    - The text `match /project_iterations/{iterationId}` exists as a top-level rule (indented at the same level as `match /projects/{projectId}`, not nested inside it)
    - `allow read: if isActiveUser();` is present inside the block
    - `allow create: if hasRole(['super_admin', 'operations_admin']);` is present
    - `allow update: if false;` is present
    - `allow delete: if hasRole(['super_admin', 'operations_admin']);` is present
  </verify>
  <acceptance_criteria>
    - firestore.rules contains `match /project_iterations/{iterationId}` at top-level collection scope (not nested inside projects match)
    - firestore.rules contains `allow update: if false;` inside the project_iterations block
    - firestore.rules contains `allow delete: if hasRole(['super_admin', 'operations_admin']);` inside the project_iterations block
    - The block is placed after the projects match closing brace and before the project_tasks collection block
  </acceptance_criteria>
  <done>firestore.rules contains a correctly-scoped project_iterations block with read=isActiveUser, create=hasRole admin, update=false, delete=hasRole admin</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client JS → Firestore | project_iterations writes must be gated by role; unauthenticated and low-privilege users must not be able to create or delete iteration snapshots |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | project_iterations create | mitigate | hasRole(['super_admin', 'operations_admin']) gate on create; operations_user cannot overwrite project state |
| T-97-02 | Tampering | project_iterations delete | mitigate | hasRole gate on delete; only admin roles can remove auto-snapshots (Undo mechanic) |
| T-97-03 | Elevation of Privilege | project_iterations update | mitigate | update: if false — iterations are write-once; no label or task edits allowed server-side |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; all Firebase APIs already in use |
</threat_model>

<verification>
- firestore.rules parses without syntax errors (Firebase CLI `firebase deploy --only firestore:rules --dry-run` if available, else visual inspection)
- project_iterations block is a top-level sibling of projects, project_tasks, etc. — not nested inside any other match
- All four allow directives (read, create, update, delete) are present with correct conditions
</verification>

<success_criteria>
project_iterations Firestore security rules are in place so that Plans 02–05 (which write to project_iterations from JS) will not be blocked by permissions errors.
</success_criteria>

<output>
Create `.planning/phases/97-project-plan-iterations/97-01-SUMMARY.md` when done
</output>
