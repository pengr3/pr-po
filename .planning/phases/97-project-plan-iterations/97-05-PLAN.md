---
phase: "97"
plan: "05"
type: execute
wave: 4
depends_on:
  - "97-04"
files_modified:
  - app/views/project-plan.js
autonomous: true
requirements:
  - Spike-018

must_haves:
  truths:
    - "computeDiff() compares live tasks[] to a snapshot's tasks[] by task id, returning {status, live, snap, changes[]} for every task id in either set"
    - "toggleIterDiff() opens the diff panel for a given iterationId, sets window._activeDiffIterationId, and re-renders the rail to mark the active Diff button"
    - "closeIterDiff() hides the diff panel, clears window._activeDiffIterationId to null, re-renders the rail"
    - "The diff panel header shows the iteration label, a summary count (N changed, M added, K removed), and a Load this → button"
    - "Diff rows are color-coded: amber=changed, green=added, red=removed, grey=same"
    - "Changed tasks show inline old→new values per field (name, start_date, end_date)"
    - "window.toggleIterDiff and window.closeIterDiff are registered in init() and deleted in destroy()"
    - "window._activeDiffIterationId is set and cleared on window (not just module scope) so diff panel onclick can read it"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "Diff computation + diff panel rendering + toggle functions"
      contains: "function computeDiff("
  key_links:
    - from: "toggleIterDiff()"
      to: "window._activeDiffIterationId"
      via: "window._activeDiffIterationId = iterationId (set); = null (cleared in closeIterDiff)"
      pattern: "window\\._activeDiffIterationId"
    - from: "computeDiff()"
      to: "#iterDiffBody tbody"
      via: "renderDiffPanel() sets innerHTML"
      pattern: "iterDiffBody"
    - from: "iter-diff-load-btn onclick"
      to: "window.openIterConfirm(window._activeDiffIterationId)"
      via: "inline onclick in Plan 02 render() HTML — reads window._activeDiffIterationId at click time"
      pattern: "_activeDiffIterationId"
---

<objective>
Implement the inline diff view — the final feature of Phase 97. Users click "⟷ Diff" on a rail card to see a color-coded comparison between the saved iteration and the live plan without loading it.

Purpose: The diff view is the observability layer of the iteration feature. It lets users inspect what changed before committing to a restore, completing the "save game" mechanic with full transparency.

Output: Three new functions in project-plan.js — computeDiff(), toggleIterDiff(), closeIterDiff() — plus a renderDiffPanel() helper; all wired into init() and destroy().
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\97-project-plan-iterations\97-RESEARCH.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\CLAUDE.md
@C:\Users\Admin\Roaming\pr-po\app\views\project-plan.js

<interfaces>
<!-- State variables from Plan 02 (module scope): -->
  let _activeDiffIterationId = null;  — also exposed as window._activeDiffIterationId (Pitfall 6)

<!-- DOM elements from Plan 02 render(): -->
  #iterDiffPanel     — the diff panel div (hidden by default)
  #iterDiffTitle     — span showing "Comparing with [label]"
  #iterDiffSummary   — span showing summary counts
  #iterDiffBody      — tbody to fill with diff rows
  iter-diff-load-btn — "Load this →" button — reads window._activeDiffIterationId at click time

<!-- functions from Plan 03/04 used here: -->
  renderIterRail()   — call after toggling _activeDiffIterationId to update Diff button active state
  openIterConfirm()  — called from diff panel "Load this →" button via window.openIterConfirm(window._activeDiffIterationId)

<!-- DIFF_FIELDS from 97-RESEARCH.md Pattern 5: -->
  const DIFF_FIELDS = ['name', 'start_date', 'end_date', 'dependencies', 'assignees', 'progress', 'is_milestone', 'notes', 'row_order'];
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement computeDiff() and renderDiffPanel()</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    - app/views/project-plan.js: locate the Phase 97 section (after the restore functions from Plan 04) — add new functions here
    - 97-RESEARCH.md Pattern 5 "computeDiff() Algorithm": exact implementation — allIds union, status logic (added/removed/changed/same), DIFF_FIELDS list, array-to-string normalization for comparison
    - 97-RESEARCH.md "Code Examples CSS" section for .iter-diff-row-{same|changed|added|removed} and .iter-diff-badge class names — ensure the renderDiffPanel() table rows use these exact class names
  </read_first>
  <action>
    Add in the Phase 97 section of project-plan.js:

    CONSTANT: const DIFF_FIELDS = ['name', 'start_date', 'end_date', 'dependencies', 'assignees', 'progress', 'is_milestone', 'notes', 'row_order'];

    FUNCTION: function computeDiff(liveTasks, snapTasks)
    - const allIds = [...new Set([...liveTasks.map(t => t.id), ...snapTasks.map(t => t.id)])]
    - return allIds.map(id => {
        const l = liveTasks.find(t => t.id === id);
        const s = snapTasks.find(t => t.id === id);
        if (!l && s) return { status: 'added',   snap: s, live: null, changes: [] };
        if (l && !s) return { status: 'removed',  snap: null, live: l, changes: [] };
        const changes = DIFF_FIELDS.map(f => {
          const lv = Array.isArray(l[f]) ? (l[f] || []).join(',') : String(l[f] ?? '');
          const sv = Array.isArray(s[f]) ? (s[f] || []).join(',') : String(s[f] ?? '');
          return lv !== sv ? { field: f, live: l[f], snap: s[f] } : null;
        }).filter(Boolean);
        return { status: changes.length ? 'changed' : 'same', live: l, snap: s, changes };
      })

    FUNCTION: function renderDiffPanel(iter, diffRows)
    - Get elements: panel = document.getElementById('iterDiffPanel'), title = document.getElementById('iterDiffTitle'), summary = document.getElementById('iterDiffSummary'), tbody = document.getElementById('iterDiffBody')
    - Guard: if (!panel || !tbody) return
    - Update title: title.textContent = `Comparing with "${iter.label}"`
    - Compute counts: const changed = diffRows.filter(r => r.status==='changed').length; const added = diffRows.filter(r => r.status==='added').length; const removed = diffRows.filter(r => r.status==='removed').length
    - Update summary: summary.textContent = `${changed} changed · ${added} added · ${removed} removed`
    - Build tbody HTML: diffRows.map(row => {
        const task = row.live || row.snap;
        const statusClass = { same:'iter-diff-row-same', changed:'iter-diff-row-changed', added:'iter-diff-row-added', removed:'iter-diff-row-removed' }[row.status];
        const badgeClass  = { same:'same', changed:'changed', added:'added', removed:'removed' }[row.status];
        const badgeLabel  = { same:'Same', changed:'Changed', added:'Added', removed:'Removed' }[row.status];

        // For changed rows: find old→new for name, start_date, end_date
        const getName  = () => { const c = row.changes.find(f => f.field==='name');       return c ? `<span class="iter-diff-old">${escapeHTML(String(c.snap??''))}</span> <span class="iter-diff-new">${escapeHTML(String(c.live??''))}</span>` : escapeHTML(String(task?.name??'')); };
        const getStart = () => { const c = row.changes.find(f => f.field==='start_date'); return c ? `<span class="iter-diff-old">${escapeHTML(String(c.snap??''))}</span> <span class="iter-diff-new">${escapeHTML(String(c.live??''))}</span>` : escapeHTML(String(task?.start_date??'')); };
        const getEnd   = () => { const c = row.changes.find(f => f.field==='end_date');   return c ? `<span class="iter-diff-old">${escapeHTML(String(c.snap??''))}</span> <span class="iter-diff-new">${escapeHTML(String(c.live??''))}</span>` : escapeHTML(String(task?.end_date??'')); };
        const getDeps  = () => escapeHTML(String((task?.dependencies||[]).join(', ')));
        const getAsgn  = () => escapeHTML(String((task?.assignees||[]).join(', ')));
        const getProg  = () => escapeHTML(String(task?.progress??''));

        return `<tr class="${statusClass}">
          <td><span class="iter-diff-badge ${badgeClass}">${badgeLabel}</span></td>
          <td>${getName()}</td>
          <td>${getStart()}</td>
          <td>${getEnd()}</td>
          <td>${getDeps()}</td>
          <td>${getAsgn()}</td>
          <td>${getProg()}%</td>
        </tr>`;
      }).join('')
    - tbody.innerHTML = built HTML
    - panel.removeAttribute('hidden')
  </action>
  <verify>
    Grep project-plan.js for `function computeDiff(` — must be present.
    Grep project-plan.js for `const DIFF_FIELDS` — must be present.
    Grep project-plan.js for `function renderDiffPanel(` — must be present.
    Grep project-plan.js for `iter-diff-row-changed` — must appear in renderDiffPanel() tbody HTML.
    Grep project-plan.js for `iter-diff-old` — must appear in renderDiffPanel() (old→new inline display).
  </verify>
  <acceptance_criteria>
    - project-plan.js contains `const DIFF_FIELDS = [`
    - project-plan.js DIFF_FIELDS includes 'name', 'start_date', 'end_date', 'dependencies', 'assignees', 'progress'
    - project-plan.js contains `function computeDiff(liveTasks, snapTasks)`
    - project-plan.js computeDiff() returns objects with status 'added' for tasks in snap but not live
    - project-plan.js computeDiff() returns objects with status 'removed' for tasks in live but not snap
    - project-plan.js computeDiff() normalizes arrays to join(',') before string comparison
    - project-plan.js contains `function renderDiffPanel(iter, diffRows)`
    - project-plan.js renderDiffPanel() uses `escapeHTML()` on all user-supplied string values
    - project-plan.js renderDiffPanel() uses class names `iter-diff-row-changed`, `iter-diff-row-added`, `iter-diff-row-removed`, `iter-diff-row-same`
    - project-plan.js renderDiffPanel() uses `.iter-diff-old` and `.iter-diff-new` spans for changed fields
    - project-plan.js renderDiffPanel() calls `panel.removeAttribute('hidden')` to show the panel
  </acceptance_criteria>
  <done>computeDiff() correctly identifies added/removed/changed/same tasks by id comparison; renderDiffPanel() fills the diff table with color-coded rows and inline old→new values for changed fields; all user-supplied strings are escaped</done>
</task>

<task type="auto">
  <name>Task 2: Implement toggleIterDiff(), closeIterDiff(), wire into init()/destroy()</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    - app/views/project-plan.js: locate the init() window assignments block (Phase 97 section after Plan 04 additions) — add two more window assignments
    - app/views/project-plan.js: locate the destroy() Phase 97 cleanup block — add two more delete statements + clear window._activeDiffIterationId
    - 97-RESEARCH.md Pitfall 6 "_activeDiffIterationId Must Be on window": set window._activeDiffIterationId = iterationId when opening diff; clear to null when closing — this allows the inline onclick `window.openIterConfirm(window._activeDiffIterationId)` in the diff panel header button to work correctly
    - 97-RESEARCH.md Pitfall 5 "Diff Panel Positioning": the diff panel is already placed outside plan-split-pane in Plan 02 render() — do not move it; just toggle hidden attribute
  </read_first>
  <action>
    Add in the Phase 97 section of project-plan.js:

    FUNCTION: function toggleIterDiff(iterationId)
    - If _activeDiffIterationId === iterationId (same card clicked again): call closeIterDiff() and return (toggle off)
    - Else:
        const iter = _iterations.find(i => i.id === iterationId);
        if (!iter) return;
        _activeDiffIterationId = iterationId;
        window._activeDiffIterationId = iterationId;  // CRITICAL: must be on window for inline onclick
        const diffRows = computeDiff(tasks, iter.tasks);
        renderDiffPanel(iter, diffRows);
        renderIterRail();  // re-render rail to show "▶ Diffing" active state on the button

    FUNCTION: function closeIterDiff()
    - _activeDiffIterationId = null;
    - window._activeDiffIterationId = null;
    - document.getElementById('iterDiffPanel')?.setAttribute('hidden', '');
    - renderIterRail();  // re-render rail to clear "▶ Diffing" active state

    WIRE INTO init() — add after window.undoIterRestore = undoIterRestore (from Plan 04):
    - window.toggleIterDiff = toggleIterDiff;
    - window.closeIterDiff = closeIterDiff;

    EXTEND destroy() Phase 97 cleanup block — add after delete window.undoIterRestore:
    - delete window.toggleIterDiff;
    - delete window.closeIterDiff;
    - window._activeDiffIterationId = null;   // clear the window global (not just module scope)
    Also ensure the diff panel is hidden on destroy:
    - document.getElementById('iterDiffPanel')?.setAttribute('hidden', '');
  </action>
  <verify>
    Grep project-plan.js for `function toggleIterDiff(` — must be present.
    Grep project-plan.js for `function closeIterDiff(` — must be present.
    Grep project-plan.js for `window._activeDiffIterationId = iterationId` — must be in toggleIterDiff.
    Grep project-plan.js for `window._activeDiffIterationId = null` — must appear at least twice (closeIterDiff + destroy).
    Grep project-plan.js for `window.toggleIterDiff = toggleIterDiff` — must be in init().
    Grep project-plan.js for `delete window.toggleIterDiff` — must be in destroy().
    Grep project-plan.js for `delete window.closeIterDiff` — must be in destroy().
  </verify>
  <acceptance_criteria>
    - project-plan.js contains `function toggleIterDiff(iterationId)`
    - project-plan.js toggleIterDiff() calls `closeIterDiff()` when iterationId === _activeDiffIterationId (toggle off)
    - project-plan.js toggleIterDiff() sets both `_activeDiffIterationId = iterationId` AND `window._activeDiffIterationId = iterationId`
    - project-plan.js toggleIterDiff() calls `computeDiff(tasks, iter.tasks)` and then `renderDiffPanel(iter, diffRows)`
    - project-plan.js toggleIterDiff() calls `renderIterRail()` after opening the diff panel
    - project-plan.js contains `function closeIterDiff()`
    - project-plan.js closeIterDiff() sets `window._activeDiffIterationId = null`
    - project-plan.js closeIterDiff() calls `setAttribute('hidden', '')` on #iterDiffPanel
    - project-plan.js closeIterDiff() calls `renderIterRail()`
    - project-plan.js init() contains `window.toggleIterDiff = toggleIterDiff;`
    - project-plan.js init() contains `window.closeIterDiff = closeIterDiff;`
    - project-plan.js destroy() contains `delete window.toggleIterDiff;`
    - project-plan.js destroy() contains `delete window.closeIterDiff;`
    - project-plan.js destroy() contains `window._activeDiffIterationId = null;`
  </acceptance_criteria>
  <done>toggleIterDiff() opens/closes the diff panel for a given iteration, sets window._activeDiffIterationId so the "Load this →" button onclick works; closeIterDiff() hides the panel and clears the global; both are wired into init() and destroy()</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderDiffPanel() → #iterDiffBody innerHTML | Diff data includes user-supplied task names and dates from Firestore — all must be escaped before HTML injection |
| window._activeDiffIterationId | Exposed as a window global; value is a Firestore auto-id string (no SQL/code risk); consumed only by openIterConfirm() which looks it up in _iterations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-05-01 | Tampering (XSS) | renderDiffPanel() tbody innerHTML | mitigate | escapeHTML() applied to all task.name, task.start_date, task.end_date, task.dependencies.join, task.assignees.join values before injection |
| T-97-05-02 | Tampering | window._activeDiffIterationId global | accept | value is a Firestore auto-id (alphanumeric); consumed by openIterConfirm() which validates against _iterations; no arbitrary code execution path |
| T-97-05-03 | Information Disclosure | computeDiff() accesses all task fields | accept | operates on in-memory tasks[] already fetched via authorized onSnapshot; no new Firestore read |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- Open a project plan with at least two iterations saved
- Click "⟷ Diff" on a rail card — diff panel appears at the bottom of the plan view (NOT clipped by the gantt overflow)
- Diff panel header shows the iteration label and a summary count (N changed · M added · K removed)
- Diff table rows are color-coded: amber background for changed tasks, green for added, red for removed, grey for same
- Changed tasks show inline strikethrough old value and green new value for name/start/end fields
- Click "⟷ Diff" again on the same card — panel closes; button text returns to "⟷ Diff"
- Click "⟷ Diff" on a different card — previous diff closes, new diff opens for the second iteration
- Click "Load this →" in the diff panel — openIterConfirm() modal appears for the currently-diffed iteration (window._activeDiffIterationId is set correctly)
- Navigate away — no console errors; diff panel cleaned up (window._activeDiffIterationId = null)
</verification>

<success_criteria>
The diff view works end-to-end: computeDiff() correctly classifies all tasks by status; renderDiffPanel() displays color-coded rows with inline old→new values; toggleIterDiff()/closeIterDiff() correctly manage panel visibility and window._activeDiffIterationId; the "Load this →" button in the diff header triggers the correct iteration's confirm modal. All four spike requirements (015c, 016, 017B, 018) are now implemented.
</success_criteria>

<output>
Create `.planning/phases/97-project-plan-iterations/97-05-SUMMARY.md` when done
</output>
