---
phase: "97"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/project-plan.js
  - styles/views.css
autonomous: true
requirements:
  - Spike-017B
  - Spike-018

must_haves:
  truths:
    - "Module-scope state variables for iteration history are declared at the top of project-plan.js"
    - "render() output wraps plan-split-pane and iter-rail in a .plan-body-row flex row"
    - "render() output includes the iter-diff-panel div as a direct child of plan-view-surface, after plan-body-row"
    - "Two new toolbar buttons (Save Iteration, History) appear between baselineToggleBtn and the search input"
    - "All iter-rail-* and iter-diff-* CSS classes are defined in views.css"
    - "iter-undo-toast CSS is defined in views.css"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "State variables for iteration feature + updated render() HTML shell"
      contains: "let _iterations = [];"
    - path: "styles/views.css"
      provides: "All visual styling for right rail, diff panel, undo toast, and toolbar buttons"
      contains: ".iter-rail {"
  key_links:
    - from: "render() HTML"
      to: "styles/views.css"
      via: ".iter-rail, .iter-diff-panel, .plan-body-row CSS classes"
      pattern: "\\.iter-rail \\{"
    - from: "render() toolbar"
      to: "window.saveIteration / window.toggleIterRail"
      via: "onclick attributes on new toolbar buttons"
      pattern: "window\\.saveIteration|window\\.toggleIterRail"
---

<objective>
Add the HTML/CSS/state-variable scaffolding for the iteration history feature — the foundation that Plans 03–05 build functions on top of.

Purpose: Plans 03–05 cannot wire functions into DOM elements that don't exist. This plan establishes all state variables, the HTML shell in render(), and every CSS class needed by the feature.

Output:
- 19 new module-scope state variables in project-plan.js (lines 11–110 area)
- Updated render() that wraps planSplitPane in plan-body-row, adds iterRail, adds iterDiffPanel, adds two toolbar buttons
- Full CSS block in views.css covering .plan-body-row, .iter-rail-*, .iter-tl-*, .iter-diff-*, .iter-undo-toast
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\97-project-plan-iterations\97-RESEARCH.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\CLAUDE.md
@C:\Users\Admin\Roaming\pr-po\app\views\project-plan.js
@C:\Users\Admin\Roaming\pr-po\styles\views.css

<interfaces>
<!-- Key existing patterns in project-plan.js that new state vars must match in style -->

Module-scope state block (lines 11–110) — all let declarations with inline comments:
  let currentProject = null;
  let tasks = [];
  let listeners = [];
  let _baselines = [];        // Phase 86.12 (mbl): all baselines
  let _activeBaselineId = null;
  // handler references:
  let _gridInputHandler = null;
  let _searchInputHandler = null;
  let _searchClearHandler = null;

Existing render() toolbar (lines 121–153):
  Ends with: <button id="baselineToggleBtn" class="plan-export-btn plan-baseline-btn" onclick="window.toggleBaseline()">Set Baseline</button>
  Then: <div class="plan-toolbar-search"> ... </div>
  (New buttons insert between baselineToggleBtn and plan-toolbar-search div)

Existing render() body (lines 154–165):
  <div class="baseline-slip-summary" id="baselineSlipSummary" style="display:none"></div>
  <div class="plan-split-pane" id="planSplitPane">
    <div class="task-grid-rail" id="taskGridRail" ...>...</div>
    <div class="plan-divider" id="planDivider" ...></div>
    <div class="gantt-pane"><div id="ganttPane"></div></div>
  </div>
  (planSplitPane must be wrapped in .plan-body-row; iterRail added as sibling; iterDiffPanel added after plan-body-row)

views.css insertion point — after line 2352 (.plan-divider:hover block ends):
  New CSS blocks go here, before the Phase 86.1 TASK GRID comment at line 2354.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 97 state variables to project-plan.js</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    - app/views/project-plan.js lines 1–112: read the full existing module-scope state block to find the correct insertion point (end of state declarations, before the render() function at line 114)
    - 97-RESEARCH.md "Complete Module-Scope State Variables Required" section: exact variable names and types
  </read_first>
  <action>
    Insert the following Phase 97 state variable block immediately before the `// ---- Lifecycle ----` comment (which precedes the export function render() declaration at line 114). Place the block after the last existing state variable declaration (_searchClearHandler).

    Declare these variables with a `// Phase 97 — Iteration history` section comment:
    - let _iterations = [];  — array of iteration docs loaded from project_iterations
    - let _autoSnapId = null;  — Firestore doc ID of the auto-snapshot created before last restore; null if no undo available
    - let _undoToastTimer = null;  — setTimeout handle for the 5s undo toast auto-dismiss
    - let _activeDiffIterationId = null;  — iteration id currently shown in diff panel; null if diff closed
    - let _iterRailOpen = false;  — whether the right rail is currently open
    - let _iterSeq = 0;  — counter for default label "Iteration N"; updated from non-auto iterations count on load

    Then a sub-comment `// Phase 97: window function handler refs for destroy() cleanup`:
    - let _iterSaveHandler = null;
    - let _iterToggleRailHandler = null;
    - let _iterCloseRailHandler = null;
    - let _iterOpenConfirmHandler = null;
    - let _iterToggleDiffHandler = null;
    - let _iterCloseIterDiffHandler = null;
    - let _iterUndoRestoreHandler = null;
    - let _iterConfirmLoadHandler = null;

    Do not modify any existing state variables. Insert only at the end of the state block.
  </action>
  <verify>
    Grep project-plan.js for `let _iterations = [];` — must be present.
    Grep project-plan.js for `let _iterRailOpen = false;` — must be present.
    Grep project-plan.js for `let _iterConfirmLoadHandler = null;` — must be present.
  </verify>
  <acceptance_criteria>
    - project-plan.js contains `let _iterations = [];`
    - project-plan.js contains `let _autoSnapId = null;`
    - project-plan.js contains `let _undoToastTimer = null;`
    - project-plan.js contains `let _activeDiffIterationId = null;`
    - project-plan.js contains `let _iterRailOpen = false;`
    - project-plan.js contains `let _iterSeq = 0;`
    - project-plan.js contains `let _iterConfirmLoadHandler = null;`
    - All 14 new declarations are placed before the `// ---- Lifecycle ----` comment
  </acceptance_criteria>
  <done>All 14 Phase 97 state variables are declared in module scope, following the existing naming and comment conventions</done>
</task>

<task type="auto">
  <name>Task 2: Update render() with iteration HTML shell + toolbar buttons</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    - app/views/project-plan.js lines 114–166: the full render() function, focusing on the toolbar HTML (lines 121–153) and body HTML (lines 154–165)
    - 97-RESEARCH.md Pattern 7 "Right Rail HTML Injection Point": exact before/after template showing plan-body-row wrapper, iterRail div, and iterDiffPanel placement
    - 97-RESEARCH.md "Toolbar Button Placement": exact button HTML for Save Iteration and History buttons
  </read_first>
  <action>
    Make two modifications to the render() function string (lines 121–165):

    MODIFICATION 1 — Toolbar buttons:
    After the closing `</button>` of the baselineToggleBtn (the "Set Baseline" button with id="baselineToggleBtn"), and BEFORE the `<div class="plan-toolbar-search">` div, insert two new buttons:
    - Button 1: type="button", class="plan-export-btn plan-iter-save-btn", onclick="window.saveIteration()", title="Save a named snapshot of the current plan", label text "Save Iteration"
    - Button 2: type="button", id="iterHistoryBtn", class="plan-export-btn plan-iter-history-btn", onclick="window.toggleIterRail()", title="Browse saved iterations", label text "History"
    Add a comment line `<!-- Phase 97: Iteration controls — after baseline button, before search -->` before them.

    MODIFICATION 2 — Body restructure:
    Replace the existing:
      `<div class="plan-split-pane" id="planSplitPane">...</div>`
    with a new structure:
      `<div class="plan-body-row" id="planBodyRow">`
        containing the existing `<div class="plan-split-pane" id="planSplitPane">...</div>` unchanged as the first child,
        followed by the iterRail div:
          `<div class="iter-rail" id="iterRail" aria-label="Iteration history" hidden>`
          containing:
            - iter-rail-head div with text "History", a span.iter-count-badge#iterCountBadge with textContent 0, and a close button with onclick="window.closeIterRail()" and aria-label="Close history" and text "×"
            - iter-rail-save-row div containing one button.iter-rail-save-btn with onclick="window.saveIteration()" and text "+ Save current state"
            - iter-rail-timeline div with id="iterRailTimeline" (empty — renderIterRail fills it)
          closing `</div>`
      closing `</div>` for plan-body-row.

    After the closing `</div>` of plan-body-row, add the iterDiffPanel as a direct child of plan-view-surface:
      `<div class="iter-diff-panel" id="iterDiffPanel" hidden>`
      containing:
        - iter-diff-head div with: span.iter-diff-title#iterDiffTitle ("Comparing with …"), span.iter-diff-summary#iterDiffSummary (empty), button.iter-diff-load-btn with onclick="window.openIterConfirm(window._activeDiffIterationId)" text "Load this →", button.iter-diff-close with onclick="window.closeIterDiff()" text "×"
        - iter-diff-legend div with four legend items (Changed, Added in iteration, Removed in iteration, Unchanged) each prefixed with a span.iter-diff-dot.iter-diff-dot-{chg|add|del|same}
        - iter-diff-table-wrap div containing a table.iter-diff-table with thead (columns: Status, Task, Start, End, Deps, Assignees, Progress) and tbody#iterDiffBody (empty)
      closing `</div>` for iter-diff-panel.

    Add a comment `<!-- Phase 97: diff panel (inside plan surface, below body row — NOT inside plan-split-pane which has overflow:hidden) -->` before the diff panel.

    The baseline-slip-summary div (id="baselineSlipSummary") stays in its current position (directly inside plan-view-surface, before plan-body-row).
  </action>
  <verify>
    Grep project-plan.js for `plan-body-row` — must appear in render() return string.
    Grep project-plan.js for `id="iterRail"` — must be present.
    Grep project-plan.js for `id="iterDiffPanel"` — must be present.
    Grep project-plan.js for `plan-iter-save-btn` — must be present in the toolbar section.
    Confirm `id="iterDiffPanel"` appears AFTER the closing div of `id="planBodyRow"` in the template string (diff panel is a sibling of plan-body-row, not a child).
  </verify>
  <acceptance_criteria>
    - project-plan.js render() contains `class="plan-body-row"` wrapping planSplitPane and iterRail
    - project-plan.js render() contains `id="iterRail"` with `hidden` attribute as a child of plan-body-row
    - project-plan.js render() contains `id="iterDiffPanel"` with `hidden` attribute as a direct child of plan-view-surface (sibling of plan-body-row, not nested inside it)
    - project-plan.js render() contains `class="plan-iter-save-btn"` button with `onclick="window.saveIteration()"`
    - project-plan.js render() contains `id="iterHistoryBtn"` button with `onclick="window.toggleIterRail()"`
    - The existing planSplitPane interior (taskGridRail, planDivider, ganttPane) is unchanged
    - The baseline-slip-summary div remains as a direct child of plan-view-surface (before plan-body-row)
  </acceptance_criteria>
  <done>render() produces the complete HTML shell for right rail, diff panel, and toolbar buttons; the plan-split-pane content is unchanged; diff panel is a sibling of plan-body-row (not nested inside it)</done>
</task>

<task type="auto">
  <name>Task 3: Add iteration CSS blocks to views.css</name>
  <files>styles/views.css</files>
  <read_first>
    - styles/views.css lines 2326–2358: .plan-split-pane block (confirm overflow:hidden at line 2329) and where the Phase 86.1 comment begins — new CSS goes between them (after .plan-divider:hover block, before Phase 86.1 comment)
    - 97-RESEARCH.md "CSS: Right Rail and Diff Panel" code block: the full CSS to insert verbatim
  </read_first>
  <action>
    Insert the complete Phase 97 CSS block in views.css after the `.plan-divider:hover, .plan-divider.dragging` rule block (around line 2352) and before the `/* === PHASE 86.1 — TASK GRID` comment. Add a section header comment `/* === PHASE 97 — ITERATION HISTORY RAIL + DIFF PANEL ===` before the CSS.

    The CSS to insert covers these selectors in order:
    1. .plan-body-row — display:flex; flex-direction:row; flex:1 1 auto; min-height:0; overflow:hidden
    2. .plan-body-row > .plan-split-pane — flex:1 1 auto; min-width:0
    3. .iter-rail — width:0; overflow:hidden; border-left:1px solid transparent; background:#ffffff; display:flex; flex-direction:column; transition:width 0.25s, border-color 0.25s; flex-shrink:0
    4. .iter-rail:not([hidden]) — width:260px; border-left-color:var(--gray-200, #e2e8f0)
    5. .iter-rail-head — padding:10px 14px; border-bottom:1px solid var(--gray-200); font-weight:600; font-size:13px; display:flex; align-items:center; gap:6px; flex-shrink:0; white-space:nowrap
    6. .iter-count-badge — background:#e8f0fe; color:#1a73e8; font-size:10px; padding:1px 6px; border-radius:10px; font-weight:700
    7. .iter-rail-close — margin-left:auto; background:none; border:none; font-size:16px; cursor:pointer; color:#64748b; line-height:1
    8. .iter-rail-save-row — padding:8px 12px; border-bottom:1px solid var(--gray-200); flex-shrink:0
    9. .iter-rail-save-btn — width:100%; padding:6px 0; background:#1a73e8; color:white; border:none; border-radius:5px; font-size:11px; font-weight:600; cursor:pointer
    10. .iter-rail-timeline — flex:1; overflow-y:auto; padding:8px 12px; display:flex; flex-direction:column; gap:2px
    11. .iter-timeline-item — display:flex; gap:10px; padding-bottom:14px; position:relative
    12. .iter-timeline-item:not(:last-child)::before — connector line (left:11px; top:24px; bottom:0; width:2px; background:var(--gray-200))
    13. .iter-tl-dot — 24x24px circle, #e8f0fe bg, 2px solid #1a73e8 border, flex-shrink:0
    14. .iter-tl-dot.auto — background:#d1fae5; border-color:#059669; color:#059669
    15. .iter-tl-content — flex:1; min-width:0
    16. .iter-tl-name — font-weight:600; font-size:12px; white-space:nowrap; overflow:hidden; text-overflow:ellipsis
    17. .iter-tl-meta — font-size:10px; color:#64748b; margin-top:2px
    18. .iter-auto-tag — background:#d1fae5; color:#065f46; font-size:9px; font-weight:700; padding:1px 5px; border-radius:6px; text-transform:uppercase
    19. .iter-tl-actions — display:flex; gap:4px; margin-top:5px
    20. .iter-tl-btn — padding:3px 8px; border:none; border-radius:4px; font-size:10px; font-weight:600; cursor:pointer
    21. .iter-tl-btn.diff-btn — background:#f1f5f9; color:#1e293b
    22. .iter-tl-btn.diff-btn:hover — background:#e2e8f0
    23. .iter-tl-btn.diff-btn.active — background:#fef3c7; color:#92400e
    24. .iter-tl-btn.load-btn — background:#1a73e8; color:white
    25. .iter-tl-btn.load-btn:hover — background:#1557b0
    26. .iter-diff-panel — border-top:2px solid #f59e0b; background:white; display:flex; flex-direction:column; flex-shrink:0; max-height:50%; overflow:hidden
    27. .iter-diff-panel[hidden] — display:none
    28. .iter-diff-head — padding:8px 16px; background:#fffbeb; border-bottom:1px solid #fcd34d; display:flex; align-items:center; gap:8px; flex-shrink:0
    29. .iter-diff-title — font-size:12px; font-weight:700; flex:1
    30. .iter-diff-summary — font-size:11px; color:#64748b; margin-right:auto
    31. .iter-diff-load-btn — padding:4px 10px; background:#1a73e8; color:white; border:none; border-radius:4px; font-size:11px; font-weight:600; cursor:pointer
    32. .iter-diff-close — background:none; border:none; font-size:16px; cursor:pointer; color:#64748b
    33. .iter-diff-legend — display:flex; gap:12px; padding:5px 16px; background:#fffbeb; border-bottom:1px solid #fcd34d; font-size:11px; align-items:center; flex-shrink:0
    34. .iter-diff-dot — width:10px; height:10px; border-radius:2px; flex-shrink:0
    35. .iter-diff-dot-chg — background:#fcd34d
    36. .iter-diff-dot-add — background:#86efac
    37. .iter-diff-dot-del — background:#fca5a5
    38. .iter-diff-dot-same — background:#e2e8f0
    39. .iter-diff-table-wrap — flex:1; overflow:auto
    40. .iter-diff-table — width:100%; border-collapse:collapse
    41. .iter-diff-table th — background:#f1f5f9; padding:6px 12px; text-align:left; font-size:10px; font-weight:600; color:#64748b; text-transform:uppercase; letter-spacing:0.04em; position:sticky; top:0; border-bottom:1px solid #e2e8f0; white-space:nowrap
    42. .iter-diff-table td — padding:6px 12px; border-bottom:1px solid #f1f5f9; font-size:12px
    43. .iter-diff-row-same td — background:#f8fafc
    44. .iter-diff-row-changed td — background:#fef3c7
    45. .iter-diff-row-added td — background:#dcfce7
    46. .iter-diff-row-removed td — background:#fee2e2
    47. .iter-diff-badge — font-size:9px; font-weight:700; padding:2px 6px; border-radius:8px; white-space:nowrap
    48. .iter-diff-badge.changed — background:#fef3c7; color:#92400e; border:1px solid #fcd34d
    49. .iter-diff-badge.added — background:#dcfce7; color:#166534; border:1px solid #86efac
    50. .iter-diff-badge.removed — background:#fee2e2; color:#991b1b; border:1px solid #fca5a5
    51. .iter-diff-badge.same — background:#f1f5f9; color:#64748b
    52. .iter-diff-old — text-decoration:line-through; color:#ef4444
    53. .iter-diff-new — color:#059669; font-weight:600
    54. .iter-undo-toast — position:fixed; bottom:90px; left:50%; transform:translateX(-50%); background:#1e293b; color:white; padding:9px 18px; border-radius:8px; font-size:12px; display:flex; align-items:center; gap:10px; z-index:9999; box-shadow:0 4px 16px rgba(0,0,0,0.25); white-space:nowrap
    55. .iter-undo-btn — color:#60a5fa; background:none; border:none; cursor:pointer; font-weight:700; text-decoration:underline; font-size:12px; padding:0
    56. .plan-iter-save-btn, .plan-iter-history-btn — inherits plan-export-btn (comment only; no additional rules needed unless adding active state)
    57. .plan-iter-history-btn.active — background:#e8f0fe; border-color:#1a73e8; color:#1a73e8

    Use the exact values from 97-RESEARCH.md "CSS: Right Rail and Diff Panel" — do not approximate colors or sizes.
  </action>
  <verify>
    Grep views.css for `.iter-rail {` — must be present.
    Grep views.css for `.iter-diff-panel {` — must be present.
    Grep views.css for `.iter-undo-toast {` — must be present.
    Grep views.css for `.plan-body-row {` — must be present.
    Grep views.css for `.iter-diff-row-removed` — must be present.
  </verify>
  <acceptance_criteria>
    - views.css contains `.iter-rail {`
    - views.css contains `.iter-rail:not([hidden])` with `width: 260px`
    - views.css contains `.iter-diff-panel {`
    - views.css contains `.iter-diff-panel[hidden] { display: none; }`
    - views.css contains `.iter-undo-toast {`
    - views.css contains `.plan-body-row {`
    - views.css contains `.plan-body-row > .plan-split-pane {`
    - views.css contains `.iter-diff-row-removed td`
    - views.css contains `.plan-iter-history-btn.active {`
    - All new CSS is inserted between the .plan-divider:hover block and the Phase 86.1 TASK GRID section comment
  </acceptance_criteria>
  <done>All iteration feature CSS classes are defined in views.css; the right rail, diff panel, timeline cards, undo toast, and active history button state all have complete styling</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render() HTML → window.* onclick | onclick attributes reference window functions; functions are not registered until init() in Plan 03 — clicking before init completes is a no-op (acceptable) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02-01 | Tampering | iter-rail-save-btn onclick in rail | mitigate | escapeHTML() used in renderIterRail() in Plan 03 to prevent stored XSS from malicious iteration labels rendered into the timeline |
| T-97-02-02 | Tampering | iter-diff-load-btn onclick uses window._activeDiffIterationId | mitigate | window._activeDiffIterationId is set and cleared only by toggleIterDiff/closeIterDiff in Plan 05; inline id must be a Firestore auto-id string (no SQL/code injection risk) |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- Serve project locally (python -m http.server 8000) and open a project plan view
- Confirm "Save Iteration" and "History" toolbar buttons appear between "Set Baseline" and the search input
- Confirm clicking "History" does not throw JS errors (function not wired yet until Plan 03, but button must render without breaking the page)
- Confirm right rail (#iterRail) and diff panel (#iterDiffPanel) are present in the DOM (hidden by default)
- Confirm .plan-body-row flex row is present in DOM wrapping planSplitPane
- Confirm existing plan view (task grid + Gantt) still loads and functions correctly
</verification>

<success_criteria>
project-plan.js render() outputs the complete HTML shell for the iteration feature; all 14 Phase 97 state variables are declared; all CSS classes for the iteration UI are in views.css; the existing plan view is not broken.
</success_criteria>

<output>
Create `.planning/phases/97-project-plan-iterations/97-02-SUMMARY.md` when done
</output>
