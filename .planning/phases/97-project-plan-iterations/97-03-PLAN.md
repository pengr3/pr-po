---
phase: "97"
plan: "03"
type: execute
wave: 2
depends_on:
  - "97-01"
  - "97-02"
files_modified:
  - app/views/project-plan.js
autonomous: true
requirements:
  - Spike-016
  - Spike-017B

must_haves:
  truths:
    - "loadIterations() fetches project_iterations for the current project, sorts newest-first, stores in _iterations"
    - "saveIteration() prompts for a label, writes a full task snapshot to project_iterations, refreshes the rail"
    - "renderIterRail() renders timeline cards from _iterations into #iterRailTimeline with Diff and Load buttons"
    - "toggleIterRail() shows/hides #iterRail and updates #iterHistoryBtn.active state"
    - "closeIterRail() hides the rail and removes the active state from the toolbar button"
    - "promptSaveIteration() is an alias/entry point for saveIteration() called from the rail + Save button"
    - "All 8 new window functions are registered in init() and deleted in destroy()"
    - "loadIterations() is called from init() after loadBaselines()"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "Data layer + render + toggle functions for iteration rail"
      contains: "async function loadIterations()"
  key_links:
    - from: "loadIterations()"
      to: "project_iterations Firestore collection"
      via: "getDocs(query(collection(db, 'project_iterations'), where('project_id', '==', currentProject.id)))"
      pattern: "project_iterations"
    - from: "renderIterRail()"
      to: "#iterRailTimeline DOM element"
      via: "innerHTML assignment with escapeHTML(iter.label)"
      pattern: "iterRailTimeline"
    - from: "saveIteration()"
      to: "addDoc(collection(db, 'project_iterations'), ...)"
      via: "tasks.map(t => ({ id: t.id, ...all fields }))"
      pattern: "addDoc.*project_iterations"
---

<objective>
Implement the data layer and right-rail rendering functions for the iteration history feature.

Purpose: This plan wires the Firestore data flow (load, save) and the rail UI (render, toggle). After this plan, a user can save a named iteration and browse it in the right rail.

Output: Six functions added to project-plan.js — loadIterations(), saveIteration(), renderIterRail(), toggleIterRail(), closeIterRail(), promptSaveIteration() — wired into init() and destroy().
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\97-project-plan-iterations\97-RESEARCH.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\pr-po\CLAUDE.md
@C:\Users\Admin\Roaming\pr-po\app\views\project-plan.js

<interfaces>
<!-- Key imports already in project-plan.js line 7 (verified): -->
  import { db, collection, doc, getDoc, addDoc, setDoc, updateDoc, deleteDoc,
           onSnapshot, query, where, getDocs, writeBatch, serverTimestamp, orderBy } from '../firebase.js';
  import { formatDate, showLoading, showToast, normalizePersonnel, escapeHTML } from '../utils.js';

<!-- Existing pattern: loadBaselines() from Phase 86.12 — follow the same structure -->
  async function loadBaselines() {
      if (!currentProject) { _baselines = []; return; }
      try {
          const snap = await getDocs(query(collection(db, 'baselines'), where(...)));
          _baselines = snap.docs.map(d => ({ id: d.id, ...d.data() })).sort(...);
      } catch (e) { ... }
  }

<!-- State variables from Plan 02 (already declared in module scope): -->
  let _iterations = [];
  let _autoSnapId = null;
  let _undoToastTimer = null;
  let _activeDiffIterationId = null;
  let _iterRailOpen = false;
  let _iterSeq = 0;

<!-- init() window assignments block (lines 250–277): -->
  window.saveBaseline = saveBaseline;
  window.toggleBaseline = toggleBaseline;
  window.selectBaseline = selectBaseline;
  window.clearBaseline = clearBaseline;
  updateBaselineToolbarUI();   ← insert loadIterations() + renderIterRail() calls here; window assignments before this line

<!-- destroy() cleanup block (lines 519–536): -->
  delete window.saveBaseline;
  delete window.toggleBaseline;
  delete window.selectBaseline;
  delete window.clearBaseline;
  _searchQuery = '';            ← insert Phase 97 cleanup block before the Phase 86.8 search cleanup section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement loadIterations() and saveIteration()</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    - app/views/project-plan.js: search for `async function loadBaselines()` — read that function in full to understand the pattern; insert the new functions nearby (in the Phase 86.12 baseline section or in a new Phase 97 section comment block)
    - 97-RESEARCH.md Pattern 1 "Iteration Data Shape": full task field list to include in tasks array
    - 97-RESEARCH.md Pattern 2 "Firestore Query for Iterations": exact getDocs query with no orderBy + client-side sort
    - 97-RESEARCH.md Pitfall 1 "serverTimestamp() Inside Nested Array": do NOT call serverTimestamp() inside the tasks[] array; copy resolved Timestamp objects from the in-memory tasks[] directly
    - 97-RESEARCH.md "Open Questions" item 1: use custom confirm modal matching spike-018 design for restore (this plan only covers save; no confirm modal needed here)
  </read_first>
  <action>
    Add a `// ---- Phase 97: Iteration history ----` section comment and implement these functions:

    FUNCTION: async function loadIterations()
    - Guard: if (!currentProject) { _iterations = []; return; }
    - getDocs with query(collection(db, 'project_iterations'), where('project_id', '==', currentProject.id)) — NO orderBy
    - Map docs: snap.docs.map(d => ({ id: d.id, ...d.data() }))
    - Sort newest-first: (a, b) => (b.saved_at?.toMillis?.() ?? 0) - (a.saved_at?.toMillis?.() ?? 0)
    - Store in _iterations
    - Update _iterSeq = _iterations.filter(i => !i.auto).length
    - Wrap in try/catch; on error: console.error('[Plan] loadIterations error:', e); _iterations = []

    FUNCTION: async function saveIteration(label = null)
    - If label is null: prompt user with window.prompt(`Save iteration as:`, `Iteration ${_iterSeq + 1}`) — if user cancels (null returned from prompt), return early
    - Trim label; if empty after trim, return early
    - Truncate label to 60 characters (per RESEARCH.md security domain V5)
    - Build the tasks snapshot: tasks.map(t => ({ id: t.id, task_id: t.task_id, project_id: t.project_id, project_code: t.project_code, name: t.name, start_date: t.start_date, end_date: t.end_date, progress: t.progress, is_milestone: t.is_milestone, parent_task_id: t.parent_task_id, dependencies: t.dependencies || [], assignees: t.assignees || [], row_order: t.row_order, notes: t.notes || '', status: t.status, created_at: t.created_at, updated_at: t.updated_at, created_by: t.created_by }))
    - CRITICAL: do NOT use serverTimestamp() inside the tasks array; t.created_at and t.updated_at are already resolved Timestamp objects from the onSnapshot — copy them directly
    - Write: await addDoc(collection(db, 'project_iterations'), { project_id: currentProject.id, label: label, saved_at: serverTimestamp(), auto: false, tasks: snapshot })
    - After write: await loadIterations(); renderIterRail()
    - Show success: showToast(`Iteration "${label}" saved.`, 'success')
    - Wrap entire function in try/catch; on error: showToast('Failed to save iteration.', 'error')
  </action>
  <verify>
    Grep project-plan.js for `async function loadIterations()` — must be present.
    Grep project-plan.js for `async function saveIteration` — must be present.
    Grep project-plan.js for `project_iterations` — must appear in at least 2 places (loadIterations query + saveIteration addDoc).
    Grep project-plan.js for `serverTimestamp()` inside the saveIteration function body — must appear exactly once (for the saved_at field on the iteration doc, NOT inside tasks.map()).
  </verify>
  <acceptance_criteria>
    - project-plan.js contains `async function loadIterations()`
    - project-plan.js contains `collection(db, 'project_iterations')` used inside loadIterations
    - project-plan.js saveIteration() contains `saved_at: serverTimestamp()` for the iteration doc field
    - project-plan.js saveIteration() does NOT contain `serverTimestamp()` inside a tasks.map() callback
    - project-plan.js saveIteration() truncates label to 60 chars before writing
    - project-plan.js saveIteration() uses window.prompt() for label input
    - project-plan.js saveIteration() calls loadIterations() then renderIterRail() after successful write
  </acceptance_criteria>
  <done>loadIterations() loads project_iterations with client-side newest-first sort; saveIteration() prompts for label, writes full task snapshot with no serverTimestamp() inside tasks array, then refreshes the rail</done>
</task>

<task type="auto">
  <name>Task 2: Implement renderIterRail(), toggleIterRail(), closeIterRail() and wire into init()/destroy()</name>
  <files>app/views/project-plan.js</files>
  <read_first>
    - app/views/project-plan.js lines 249–278: the window assignments block inside init() — new window assignments go here
    - app/views/project-plan.js lines 519–537: the destroy() cleanup tail — new delete/cleanup goes here before the search handler cleanup block
    - 97-RESEARCH.md "renderIterRail() Function Pattern": exact innerHTML template with escapeHTML, iter.auto tag, date formatting, button onclick patterns
    - 97-RESEARCH.md "Window Functions to Register and Clean Up": exact list of window assignments and deletes
  </read_first>
  <action>
    Add three functions in the Phase 97 section:

    FUNCTION: function renderIterRail()
    - Get elements: timeline = document.getElementById('iterRailTimeline'), badge = document.getElementById('iterCountBadge')
    - Guard: if (!timeline) return
    - Update badge: if (badge) badge.textContent = _iterations.length
    - If _iterations.length === 0: timeline.innerHTML = empty-state div with text "No saved iterations yet." (styled inline: padding:12px; font-size:12px; color:#64748b; text-align:center)
    - Else: timeline.innerHTML = _iterations.map((iter, idx) => ...).join('')
    - Each timeline item renders: .iter-timeline-item div containing:
        - .iter-tl-dot (with class "auto" if iter.auto) showing number (_iterations.length - idx)
        - .iter-tl-content div with:
            - .iter-tl-name: escapeHTML(iter.label) + auto tag span if iter.auto
            - .iter-tl-meta: iter.saved_at?.toDate?.().toLocaleDateString() ?? 'Just now' + ' · ' + (iter.tasks?.length ?? 0) + ' tasks'
            - .iter-tl-actions div with two buttons:
                - button.iter-tl-btn.diff-btn (class adds "active" if _activeDiffIterationId === iter.id) onclick="window.toggleIterDiff('${iter.id}')" text: if active → '▶ Diffing' else '⟷ Diff'
                - button.iter-tl-btn.load-btn onclick="window.openIterConfirm('${iter.id}')" text "Load →"
    - Use escapeHTML() for all user-supplied strings (iter.label, iter.id in onclick is a Firestore auto-id — safe but consistent)

    FUNCTION: function toggleIterRail()
    - const rail = document.getElementById('iterRail'); const btn = document.getElementById('iterHistoryBtn')
    - If rail has `hidden` attribute: remove it (rail.removeAttribute('hidden')); _iterRailOpen = true; btn?.classList.add('active')
    - Else: rail.setAttribute('hidden', ''); _iterRailOpen = false; btn?.classList.remove('active')

    FUNCTION: function closeIterRail()
    - const rail = document.getElementById('iterRail'); const btn = document.getElementById('iterHistoryBtn')
    - rail?.setAttribute('hidden', ''); _iterRailOpen = false; btn?.classList.remove('active')

    WIRE INTO init() — add after the window.clearBaseline = clearBaseline line (and before updateBaselineToolbarUI()):
    - window.saveIteration = saveIteration;
    - window.toggleIterRail = toggleIterRail;
    - window.closeIterRail = closeIterRail;
    (openIterConfirm, confirmIterLoad, toggleIterDiff, closeIterDiff, undoIterRestore added in Plans 04 and 05)

    Also add a call sequence after the window assignments (before or after updateBaselineToolbarUI()):
    - await loadIterations();
    - renderIterRail();
    Note: loadIterations() must be awaited because it's async; place the await calls at the top-level of init()'s try block, after loadBaselines() is already called at line 202.

    WIRE INTO destroy() — add Phase 97 cleanup block before `_searchQuery = '';`:
    - delete window.saveIteration;
    - delete window.toggleIterRail;
    - delete window.closeIterRail;
    (openIterConfirm, toggleIterDiff etc. will be added in Plans 04 and 05)
    - dismissUndoToast(); (function implemented in Plan 04 — add the call here as a forward reference; it will be defined by the time destroy() is called at runtime)
    - _autoSnapId = null;
    - _iterations = [];
    - _activeDiffIterationId = null;
    - _iterRailOpen = false;
    - _iterSeq = 0;
  </action>
  <verify>
    Grep project-plan.js for `function renderIterRail()` — must be present.
    Grep project-plan.js for `function toggleIterRail()` — must be present.
    Grep project-plan.js for `window.saveIteration = saveIteration` — must be present inside init().
    Grep project-plan.js for `await loadIterations()` — must be present inside init().
    Grep project-plan.js for `delete window.saveIteration` — must be present inside destroy().
  </verify>
  <acceptance_criteria>
    - project-plan.js contains `function renderIterRail()`
    - project-plan.js contains `function toggleIterRail()`
    - project-plan.js contains `function closeIterRail()`
    - project-plan.js init() contains `window.saveIteration = saveIteration;`
    - project-plan.js init() contains `window.toggleIterRail = toggleIterRail;`
    - project-plan.js init() contains `window.closeIterRail = closeIterRail;`
    - project-plan.js init() contains `await loadIterations();`
    - project-plan.js init() contains `renderIterRail();` after loadIterations()
    - project-plan.js destroy() contains `delete window.saveIteration;`
    - project-plan.js destroy() contains `_iterations = [];` in the Phase 97 cleanup block
    - project-plan.js renderIterRail() uses escapeHTML() on iter.label
    - project-plan.js renderIterRail() includes iter.auto dot class and iter-auto-tag span
  </acceptance_criteria>
  <done>renderIterRail() builds the timeline HTML; toggleIterRail()/closeIterRail() handle show/hide; all three functions are wired into init() and destroy(); the rail renders correctly after loadIterations() populates _iterations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore project_iterations → renderIterRail() innerHTML | iter.label is user-supplied and must be escaped before injection |
| window.prompt() → saveIteration() | label input from user must be trimmed and truncated before Firestore write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03-01 | Tampering (XSS) | renderIterRail() innerHTML | mitigate | escapeHTML(iter.label) on every render; iter.id is a Firestore auto-id (safe for onclick attribute) |
| T-97-03-02 | Information Disclosure | loadIterations() queries all project iterations | accept | query is gated by where('project_id', '==', currentProject.id); Firestore rules require isActiveUser(); project-scoping at JS layer mirrors the existing project_tasks pattern |
| T-97-03-03 | Tampering | saveIteration() label field | mitigate | label truncated to 60 chars before write; Firestore rules require hasRole(['super_admin', 'operations_admin']) for create |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- Serve locally and open a project plan view
- Click "Save Iteration" — prompt appears with default "Iteration 1"; submit a name; check Firestore project_iterations collection has a new doc with project_id, label, auto:false, tasks array
- Click "History" — right rail slides open (width 260px); the saved iteration appears as a timeline card with label, date, task count, Diff and Load buttons
- Click "History" again — rail closes; button loses active styling
- Confirm _iterations is reset to [] and deletewindow.saveIteration is called on view navigation (destroy test: navigate away and confirm no console errors)
</verification>

<success_criteria>
Users can save named iterations (full task snapshots) and browse them in the right-rail history panel. The rail shows timeline cards with correct labels, dates, and task counts. Saving triggers a success toast. The feature integrates cleanly into init()/destroy() without leaking listeners or window functions.
</success_criteria>

<output>
Create `.planning/phases/97-project-plan-iterations/97-03-SUMMARY.md` when done
</output>
