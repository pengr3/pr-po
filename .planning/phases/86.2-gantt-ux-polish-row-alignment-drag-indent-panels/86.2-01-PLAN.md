---
phase: 86.2-gantt-ux-polish-row-alignment-drag-indent-panels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - styles/views.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Left-rail task rows are exactly 42px tall, aligning vertically with Frappe Gantt SVG bars"
    - "Grid header row is 85px tall, aligning with Frappe's upper+lower header region"
    - "Gantt pane fills its full grid column (no truncation on the right)"
    - "Cell padding on <td> elements does not push row height beyond 42px"
  artifacts:
    - path: "styles/views.css"
      provides: "Row height sync rules (.tg-row height: 42px, thead tr height: 85px, td padding-top/bottom: 0)"
      contains: "height: 42px"
    - path: "styles/views.css"
      provides: "Gantt full-width fix (.gantt-pane min-width: 0)"
      contains: "min-width: 0"
  key_links:
    - from: "styles/views.css (.tg-row)"
      to: "Frappe Gantt SVG row height (bar_height:24 + padding:18 = 42px)"
      via: "CSS height property"
      pattern: "height: 42px"
    - from: "styles/views.css (.gantt-pane)"
      to: "CSS grid column boundary"
      via: "min-width: 0"
      pattern: "min-width: 0"
---

<objective>
Fix the visual row misalignment between the left-rail task grid and the Frappe Gantt SVG bars, and ensure the Gantt pane fills its full right column.

Purpose: Defects 1 (row misalignment) and 9 (Gantt right panel not full-width) are pure CSS changes — no JS needed. Landing these in Wave 1 unblocks Wave 3's structural panel work because it establishes correct dimensions before the divider is added.

Output: Updated `styles/views.css` with row height sync (42px/85px) and Gantt full-width fix.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Admin/Roaming/pr-po/.planning/ROADMAP.md
@C:/Users/Admin/Roaming/pr-po/.planning/phases/86.2-gantt-ux-polish-row-alignment-drag-indent-panels/86.2-RESEARCH.md

<interfaces>
<!-- Current CSS layout state — extracted from styles/views.css lines 2421-2574 -->

/* Current .plan-split-pane */
.plan-split-pane {
    display: grid;
    grid-template-columns: 35% 1fr;
    flex: 1 1 auto;
    min-height: 0;
    border-top: 1px solid var(--gray-200, #dadce0);
}

/* Current .tg-row — MISSING explicit height */
.tg-row {
    border-bottom: 1px solid var(--gray-200, #dadce0);
}

/* Current .gantt-pane — MISSING min-width: 0 */
.gantt-pane {
    overflow: auto;
    background: #ffffff;
}

/* Frappe Gantt row formula (from CDN source) */
/* y = config.header_height + options.padding/2 + task._index * (bar_height + padding) */
/* bar_height: 24, padding: 18 → each row = 42px in the SVG */
/* header: upper_header_height(45) + lower_header_height(30) + 10 = 85px */

/* Current .task-grid thead th */
.task-grid thead th {
    position: sticky;
    top: 0;
    background: #f8f9fa;
    border-bottom: 2px solid var(--gray-200, #dadce0);
    padding: 6px 8px;
    /* NO explicit height */
}

/* Current task-grid td — uses .tg-input padding */
.tg-input {
    padding: 4px 6px;
}
.tg-rn {
    padding: 4px 4px;
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix left-rail row height to match Frappe Gantt (42px) and align header (85px)</name>
  <read_first>
    styles/views.css — read lines 2370–2680 to see all current plan-view CSS before touching anything
  </read_first>
  <files>styles/views.css</files>
  <action>
In `styles/views.css`, make these targeted edits to the Phase 86.1 task grid section (around line 2471):

**Change 1 — `.tg-row` height (DEFECT 1 fix):**
Replace the existing `.tg-row` rule:
```css
.tg-row {
    border-bottom: 1px solid var(--gray-200, #dadce0);
}
```
With:
```css
.tg-row {
    height: 42px;                                     /* MUST match Frappe bar_height(24) + padding(18) = 42px */
    border-bottom: 1px solid var(--gray-200, #dadce0);
    box-sizing: border-box;
}
```

**Change 2 — `<td>` vertical padding (DEFECT 1 fix — prevents td padding inflating row past 42px):**
After the `.tg-row` rule, add a new rule for task-grid td cells. Insert after `.tg-row.tg-drag-over { ... }`:
```css
.task-grid tbody td {
    padding-top: 0;
    padding-bottom: 0;
    box-sizing: border-box;
    vertical-align: middle;
}
```

**Change 3 — `thead tr` height (DEFECT 1 fix — aligns grid header with Frappe's 85px header region):**
After `.task-grid thead th { ... }` rule, add:
```css
.task-grid thead tr {
    height: 85px;
}
.task-grid thead th {
    vertical-align: bottom;
    padding-bottom: 8px;
}
```
NOTE: The existing `.task-grid thead th` rule already has `padding: 6px 8px`. Change it to add `padding-bottom: 8px` and `vertical-align: bottom` by merging into the existing rule (do not create a duplicate rule — merge into the existing `thead th` rule or add a more specific override after it).

**Change 4 — `.gantt-pane` full-width fix (DEFECT 9 fix):**
Find the existing `.gantt-pane` rule (around line 2560):
```css
.gantt-pane {
    overflow: auto;
    background: #ffffff;
}
```
Replace with:
```css
.gantt-pane {
    overflow: auto;
    background: #ffffff;
    min-width: 0;                  /* CSS grid child: prevents overflow past column boundary */
}
.gantt-pane .gantt-container {
    min-width: 100%;               /* Frappe SVG container fills available column width */
}
```
  </action>
  <verify>
    <automated>node --check C:/Users/Admin/Roaming/pr-po/app/views/project-plan.js</automated>
    <!-- CSS has no syntax checker in zero-build; use grep to verify the key rules are present -->
    <automated>grep -c "height: 42px" C:/Users/Admin/Roaming/pr-po/styles/views.css</automated>
    <!-- expected: 1 (exactly one .tg-row height rule) -->
    <automated>grep -c "min-width: 0" C:/Users/Admin/Roaming/pr-po/styles/views.css</automated>
    <!-- expected: >= 1 -->
    <automated>grep -c "padding-top: 0" C:/Users/Admin/Roaming/pr-po/styles/views.css</automated>
    <!-- expected: >= 1 (tbody td padding suppression) -->
  </verify>
  <acceptance_criteria>
    - styles/views.css contains `.tg-row {` rule with `height: 42px`
    - styles/views.css contains `box-sizing: border-box` inside `.tg-row` rule
    - styles/views.css contains `padding-top: 0` inside `.task-grid tbody td` rule
    - styles/views.css contains `min-width: 0` inside `.gantt-pane` rule
    - styles/views.css contains `.task-grid thead tr {` with `height: 85px`
    - styles/views.css contains `.gantt-pane .gantt-container {` with `min-width: 100%`
    - `grep -c "height: 42px" styles/views.css` returns 1 (no duplicate rules)
    - `node --check app/views/project-plan.js` exits 0 (JS unchanged, still valid)
  </acceptance_criteria>
  <done>CSS changes committed; row height sync and Gantt full-width rules are in place for Wave 3 to build the panel divider on top of.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS delivery | Static file served by Netlify — no user input, no XSS surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.2-01-01 | Tampering | styles/views.css | accept | CSS file is version-controlled; Netlify serves only committed files. No runtime user input involved. |
| T-86.2-01-02 | Information Disclosure | `.tg-row height: 42px` | accept | Height value is a layout constant derived from Frappe source; no sensitive data. |
</threat_model>

<verification>
After the plan executes:

1. Open browser at `http://localhost:8000` (or Netlify preview URL)
2. Navigate to a project plan with several tasks
3. Visually confirm: each task row in the left-rail grid aligns vertically with its Gantt bar in the right pane
4. Visually confirm: the Gantt header (Day/Week labels) aligns with the grid header row
5. Visually confirm: Gantt bars are not cut off on the right side of the viewport

Grep checks (run from repo root):
```
grep -v "^/\*" styles/views.css | grep -c "height: 42px"   # must be 1
grep -v "^/\*" styles/views.css | grep -c "min-width: 0"   # must be >= 1
grep -c "padding-top: 0" styles/views.css                  # must be >= 1
```
</verification>

<success_criteria>
- `.tg-row` has `height: 42px` in views.css — exactly once
- `td` padding-top/bottom suppressed in `.task-grid tbody td` rule
- `.task-grid thead tr` has `height: 85px`
- `.gantt-pane` has `min-width: 0`
- `.gantt-pane .gantt-container` has `min-width: 100%`
- `node --check app/views/project-plan.js` exits 0 (JS file untouched)
</success_criteria>

<output>
After completion, create `.planning/phases/86.2-gantt-ux-polish-row-alignment-drag-indent-panels/86.2-01-SUMMARY.md`
</output>
