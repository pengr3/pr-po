---
phase: 86.2-gantt-ux-polish-row-alignment-drag-indent-panels
plan: 03
type: execute
wave: 3
depends_on:
  - 86.2-01-PLAN.md
files_modified:
  - app/views/project-plan.js
  - styles/views.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "A thin drag handle divider sits between the left task-grid rail and the right Gantt pane"
    - "Dragging the divider updates grid-template-columns in real time — left rail grows/shrinks, Gantt adjusts"
    - "Panel width is clamped between 15% and 75% of the split-pane width"
    - "mousemove and mouseup listeners attached to document are removed in destroy()"
    - "Deleting a task with children shows a centered confirm modal overlay (not inline tooltip)"
    - "Deleting a leaf task still deletes immediately with no confirm"
    - "Cancel or backdrop click on delete modal removes modal without deleting"
  artifacts:
    - path: "app/views/project-plan.js"
      provides: "initPanelResize() with document-level mousemove/mouseup"
      contains: "initPanelResize"
    - path: "app/views/project-plan.js"
      provides: "showDeleteConfirmModal() function"
      contains: "showDeleteConfirmModal"
    - path: "app/views/project-plan.js"
      provides: "Module-scope resize handler vars"
      contains: "_resizeMouseMoveHandler"
    - path: "app/views/project-plan.js"
      provides: "render() shell includes plan-divider element"
      contains: "plan-divider"
    - path: "styles/views.css"
      provides: "Divider column in .plan-split-pane grid-template-columns (3 columns)"
      contains: "4px 1fr"
    - path: "styles/views.css"
      provides: ".plan-divider CSS (col-resize cursor, hover blue)"
      contains: "plan-divider"
  key_links:
    - from: "initPanelResize()"
      to: "document mousemove"
      via: "_resizeMouseMoveHandler"
      pattern: "_resizeMouseMoveHandler"
    - from: "destroy()"
      to: "document.removeEventListener('mousemove', _resizeMouseMoveHandler)"
      via: "cleanup guard"
      pattern: "removeEventListener.*_resizeMouseMoveHandler"
    - from: "gridDeleteRow"
      to: "showDeleteConfirmModal"
      via: "hasChildren branch"
      pattern: "showDeleteConfirmModal"
    - from: "showDeleteConfirmModal planDeleteYes button"
      to: "deleteTaskNow(taskId)"
      via: "addEventListener click"
      pattern: "deleteTaskNow"
---

<objective>
Add resizable panel divider between task grid and Gantt pane, and replace the inline delete tooltip with a centered confirm modal for tasks with children.

Purpose: These two features require coordinated changes to both files. Plan 03 depends on Plan 01 having established the CSS foundation (row height, min-width: 0 on gantt-pane). The grid-template-columns change here adds a third column for the divider.

Output: Updated `app/views/project-plan.js` and `styles/views.css` delivering DEFECT 7 (delete modal) and DEFECT 8 (resizable panels).
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Admin/Roaming/pr-po/.planning/ROADMAP.md
@C:/Users/Admin/Roaming/pr-po/.planning/phases/86.2-gantt-ux-polish-row-alignment-drag-indent-panels/86.2-RESEARCH.md

<interfaces>
<!-- Current render() split-pane HTML (app/views/project-plan.js lines 56-63) -->
<!-- render() returns string; no async -->
<!--
<div class="plan-split-pane">
    <div class="task-grid-rail" id="taskGridRail" aria-label="Task grid">
        <div class="empty-state"><h3>Loading...</h3></div>
    </div>
    <div class="gantt-pane">
        <div id="ganttPane"></div>
    </div>
</div>
-->

<!-- Current .plan-split-pane CSS — Plan 01 will have added row-height rules but NOT changed grid-template-columns -->
<!--
.plan-split-pane {
    display: grid;
    grid-template-columns: 35% 1fr;
    flex: 1 1 auto;
    min-height: 0;
    border-top: 1px solid var(--gray-200, #dadce0);
}
-->

<!-- Current gridDeleteRow (lines 731-753) -->
<!--
function gridDeleteRow(taskId) {
    const t = tasks.find(x => x.task_id === taskId);
    if (!t) return;
    const hasChildren = tasks.some(x => x.parent_task_id === taskId);
    if (hasChildren) {
        // inline tooltip — will be replaced by modal
        ...
    } else {
        deleteTaskNow(taskId);
    }
}
-->

<!-- Module-scope handler vars (lines 20-34) — Plan 02 added _gridKeydownHandler -->
<!--
let _gridKeydownHandler = null;
// ADD after this:
let _resizeMouseMoveHandler = null;
let _resizeMouseUpHandler = null;
let _resizeDragging = false;
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add resizable panel divider (HTML, CSS, JS init + destroy)</name>
  <read_first>
    app/views/project-plan.js lines 36-66 (render shell), lines 68-135 (init), lines 137-181 (destroy), lines 20-34 (module-scope vars).
    styles/views.css lines 2421-2435 (plan-split-pane rule) — confirm Plan 01 changes are in place.
  </read_first>
  <files>app/views/project-plan.js, styles/views.css</files>
  <action>
Four coordinated edits for DEFECT 8.

EDIT A — Module-scope resize state vars (app/views/project-plan.js):
After the line "let _gridKeydownHandler = null;" (added by Plan 02), add:
  let _resizeMouseMoveHandler = null;
  let _resizeMouseUpHandler = null;
  let _resizeDragging = false;

EDIT B — Update render() HTML (app/views/project-plan.js):
In render(), change the plan-split-pane div opening tag from:
  class="plan-split-pane"
to:
  class="plan-split-pane" id="planSplitPane"

Between the closing div of .task-grid-rail and the opening div of .gantt-pane, insert:
  div class="plan-divider" id="planDivider" aria-hidden="true" closing-div

Target structure:
  div.plan-split-pane#planSplitPane
    div.task-grid-rail#taskGridRail
      div.empty-state Loading...
    /div
    div.plan-divider#planDivider aria-hidden="true" /div
    div.gantt-pane
      div#ganttPane /div
    /div
  /div

EDIT C — Add initPanelResize() and call it from init() (app/views/project-plan.js):
Add the function after destroy() and before the grid rendering comment block (around line 183):

function initPanelResize() {
    const divider = document.getElementById('planDivider');
    const splitPane = document.getElementById('planSplitPane');
    if (!divider || !splitPane) return;

    divider.addEventListener('mousedown', (e) => {
        _resizeDragging = true;
        divider.classList.add('dragging');
        e.preventDefault();
    });

    _resizeMouseMoveHandler = function(e) {
        if (!_resizeDragging) return;
        const rect = splitPane.getBoundingClientRect();
        const pct = Math.max(15, Math.min(75, ((e.clientX - rect.left) / rect.width) * 100));
        splitPane.style.gridTemplateColumns = pct.toFixed(1) + '% 4px 1fr';
    };

    _resizeMouseUpHandler = function() {
        if (!_resizeDragging) return;
        _resizeDragging = false;
        divider.classList.remove('dragging');
    };

    document.addEventListener('mousemove', _resizeMouseMoveHandler);
    document.addEventListener('mouseup', _resizeMouseUpHandler);
}

In init(), add the call to initPanelResize() at the end of the try block, after window.gridDeleteRow = gridDeleteRow; and before the closing brace of try:
    initPanelResize();

EDIT D — Add cleanup in destroy() (app/views/project-plan.js):
After the existing "document.getElementById('taskGridContextMenu')?.remove();" line, add:
    document.getElementById('planDeleteConfirm')?.remove();

After the existing delete window.* cleanup block, add the resize cleanup block:
    if (_resizeMouseMoveHandler) document.removeEventListener('mousemove', _resizeMouseMoveHandler);
    if (_resizeMouseUpHandler)   document.removeEventListener('mouseup',   _resizeMouseUpHandler);
    _resizeMouseMoveHandler = null;
    _resizeMouseUpHandler   = null;
    _resizeDragging = false;

EDIT E — CSS changes (styles/views.css):
1. In .plan-split-pane, change grid-template-columns from "35% 1fr" to "35% 4px 1fr"
2. Add .plan-divider rule after .plan-split-pane rule:
   .plan-divider {
       background: var(--gray-200, #dadce0);
       cursor: col-resize;
       user-select: none;
       transition: background 0.15s;
   }
   .plan-divider:hover,
   .plan-divider.dragging {
       background: var(--primary, #1a73e8);
   }
  </action>
  <verify>
    <automated>node --check C:/Users/Admin/Roaming/pr-po/app/views/project-plan.js</automated>
    <automated>grep -c "initPanelResize" C:/Users/Admin/Roaming/pr-po/app/views/project-plan.js</automated>
    <automated>grep -c "_resizeMouseMoveHandler" C:/Users/Admin/Roaming/pr-po/app/views/project-plan.js</automated>
    <automated>grep -c "4px 1fr" C:/Users/Admin/Roaming/pr-po/styles/views.css</automated>
    <automated>grep -c "plan-divider" C:/Users/Admin/Roaming/pr-po/styles/views.css</automated>
  </verify>
  <acceptance_criteria>
    - app/views/project-plan.js contains "let _resizeMouseMoveHandler = null" at module scope
    - app/views/project-plan.js contains "let _resizeMouseUpHandler = null" at module scope
    - app/views/project-plan.js contains "id=\"planSplitPane\"" in render() HTML string
    - app/views/project-plan.js contains "class=\"plan-divider\"" in render() HTML string
    - app/views/project-plan.js contains "function initPanelResize()" definition
    - app/views/project-plan.js contains "initPanelResize()" call in init()
    - app/views/project-plan.js contains "document.removeEventListener('mousemove', _resizeMouseMoveHandler)" in destroy()
    - app/views/project-plan.js contains "document.removeEventListener('mouseup', _resizeMouseUpHandler)" in destroy()
    - styles/views.css contains "4px 1fr" inside .plan-split-pane grid-template-columns
    - styles/views.css contains ".plan-divider {" CSS rule with "cursor: col-resize"
    - grep -c "initPanelResize" app/views/project-plan.js returns >= 2
    - grep -c "_resizeMouseMoveHandler" app/views/project-plan.js returns >= 4
    - grep -c "4px 1fr" styles/views.css returns 1
    - node --check app/views/project-plan.js exits 0
  </acceptance_criteria>
  <done>Resizable panel divider wired — dragging the 4px divider resizes the task grid vs Gantt pane, clamped 15-75%; handlers cleaned up on destroy().</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace inline delete tooltip with centered confirm modal for parent-task deletes (DEFECT 7)</name>
  <read_first>
    app/views/project-plan.js lines 730-755 (gridDeleteRow full function) and lines 1296-1350 (deleteTaskNow) to see current code before editing.
  </read_first>
  <files>app/views/project-plan.js</files>
  <action>
DEFECT 7 fix. The ROADMAP Phase 86.2 explicitly requires a "delete-confirmation modal for indented tasks" — this overrides Phase 86.1 CONTEXT.md D-05 ("inline confirm") because the ROADMAP represents the later user intent.

Leaf tasks (no children) continue to delete immediately — no confirm.

EDIT A — Add showDeleteConfirmModal() function (app/views/project-plan.js):
Add this function after gridDeleteRow and before handleRowDrop:

function showDeleteConfirmModal(taskId, childCount) {
    document.getElementById('planDeleteConfirm')?.remove();
    const modal = document.createElement('div');
    modal.id = 'planDeleteConfirm';
    modal.style.cssText = 'position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:rgba(0,0,0,0.3);z-index:10001;';
    modal.innerHTML =
        '<div style="background:#fff;border-radius:8px;padding:24px 28px;min-width:320px;max-width:480px;box-shadow:0 8px 32px rgba(0,0,0,0.2);">' +
            '<h3 style="margin:0 0 10px;font-size:1rem;color:#1e293b;">Delete task and subtasks?</h3>' +
            '<p style="color:#475569;margin:0 0 20px;font-size:0.875rem;line-height:1.5;">' +
                'This will permanently delete this task and its <strong>' + childCount + '</strong> subtask' + (childCount !== 1 ? 's' : '') + '. This cannot be undone.' +
            '</p>' +
            '<div style="display:flex;gap:8px;justify-content:flex-end;">' +
                '<button id="planDeleteNo" style="padding:8px 16px;border:1px solid #e5e7eb;border-radius:6px;background:#fff;color:#475569;cursor:pointer;font-size:0.875rem;">Cancel</button>' +
                '<button id="planDeleteYes" style="padding:8px 16px;border:none;border-radius:6px;background:#ef4444;color:#fff;cursor:pointer;font-size:0.875rem;font-weight:600;">Delete</button>' +
            '</div>' +
        '</div>';
    document.body.appendChild(modal);
    modal.querySelector('#planDeleteNo').addEventListener('click', () => modal.remove());
    modal.querySelector('#planDeleteYes').addEventListener('click', () => { modal.remove(); deleteTaskNow(taskId); });
    modal.addEventListener('click', e => { if (e.target === modal) modal.remove(); });
}

NOTE: innerHTML uses string concatenation (not template literals) to avoid any backtick conflicts with the surrounding template literal in render(). Both are fine in a module-scope function — but string concat is safest here since this is not inside a template literal.

EDIT B — Update gridDeleteRow() to use showDeleteConfirmModal (app/views/project-plan.js):
In gridDeleteRow(), replace the entire "if (hasChildren) { ... }" block with:

    if (hasChildren) {
        const childCount = tasks.filter(x => x.parent_task_id === taskId).length;
        showDeleteConfirmModal(taskId, childCount);
    } else {
        deleteTaskNow(taskId);
    }

The old inline tooltip code (row.querySelector('.tg-delete-confirm')?.remove(), the span creation, etc.) is entirely removed.

EDIT C — Ensure planDeleteConfirm cleanup in destroy() (app/views/project-plan.js):
This was added in Task 1 EDIT D. If Task 1 was committed first, confirm the line
    document.getElementById('planDeleteConfirm')?.remove();
is present in destroy(). If not yet present, add it now after the taskGridContextMenu cleanup line.
  </action>
  <verify>
    <automated>node --check C:/Users/Admin/Roaming/pr-po/app/views/project-plan.js</automated>
    <automated>grep -c "showDeleteConfirmModal" C:/Users/Admin/Roaming/pr-po/app/views/project-plan.js</automated>
    <automated>grep -c "planDeleteConfirm" C:/Users/Admin/Roaming/pr-po/app/views/project-plan.js</automated>
    <automated>grep -c "tg-delete-confirm" C:/Users/Admin/Roaming/pr-po/app/views/project-plan.js</automated>
  </verify>
  <acceptance_criteria>
    - app/views/project-plan.js contains "function showDeleteConfirmModal(taskId, childCount)" definition
    - app/views/project-plan.js contains "showDeleteConfirmModal(taskId, childCount)" call inside gridDeleteRow
    - app/views/project-plan.js contains "planDeleteConfirm" in at least 2 places (modal id + destroy cleanup)
    - app/views/project-plan.js contains "planDeleteNo" and "planDeleteYes" button ids inside showDeleteConfirmModal
    - app/views/project-plan.js does NOT contain the old inline tooltip code: "row.querySelector('.tg-delete-confirm')?.remove()" inside gridDeleteRow (grep -c should return 0 for that string in gridDeleteRow context)
    - grep -c "showDeleteConfirmModal" app/views/project-plan.js returns >= 2
    - grep -c "tg-delete-confirm" app/views/project-plan.js returns 0 in JS file (tooltip span class no longer created in JS; CSS rule in views.css can remain)
    - node --check app/views/project-plan.js exits 0
  </acceptance_criteria>
  <done>Delete confirmation modal replaces inline tooltip for parent-task deletes. Leaf tasks still delete immediately. Modal has Cancel, Delete (red), and backdrop-click dismiss.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Delete modal → deleteTaskNow | taskId comes from in-memory tasks[] (Firestore snapshot), not user text — not injectable |
| Panel resize → gridTemplateColumns | pct is Math.max/Math.min clamped arithmetic on clientX — no user string interpolated into CSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86.2-03-01 | Tampering | showDeleteConfirmModal — childCount in innerHTML | accept | childCount is tasks.filter(...).length (integer arithmetic) — not user-supplied string; no XSS surface |
| T-86.2-03-02 | Tampering | initPanelResize — gridTemplateColumns | accept | pct value is clamped 15-75 numeric; no user string reaches style property |
| T-86.2-03-03 | Denial of Service | document-level mousemove listener | mitigate | Handler is no-op when _resizeDragging=false (early return); destroy() removes handler on view exit |
| T-86.2-03-04 | Tampering | planDeleteConfirm modal not cleaned up | mitigate | destroy() calls document.getElementById('planDeleteConfirm')?.remove() — modal cannot persist across view navigations |
</threat_model>

<verification>
After the plan executes:

1. Resizable panel: Drag the thin divider between task grid and Gantt — confirm left rail grows/shrinks, Gantt adjusts. Release mouse — confirm divider returns to gray. Test clamping: drag far left/right — confirm panel cannot exceed 15%/75%.

2. Navigate away and back (go to another view, return to project plan) — confirm the panel width resets to default 35% (handlers cleaned up on destroy).

3. Delete modal: Right-click a parent task (one with children), choose Delete Row — confirm a centered modal appears with task count and Cancel/Delete buttons. Click Cancel — confirm modal removes and task is still present. Click Delete — confirm task and subtasks are removed.

4. Delete leaf task: Right-click a leaf task, choose Delete Row — confirm immediate deletion with no modal.

Grep checks:
  node --check app/views/project-plan.js                               # exit 0
  grep -c "initPanelResize" app/views/project-plan.js                  # >= 2
  grep -c "_resizeMouseMoveHandler" app/views/project-plan.js          # >= 4
  grep -c "showDeleteConfirmModal" app/views/project-plan.js           # >= 2
  grep -c "4px 1fr" styles/views.css                                   # 1
  grep -c "plan-divider" styles/views.css                              # >= 1
</verification>

<success_criteria>
- node --check app/views/project-plan.js exits 0
- initPanelResize() defined and called from init()
- _resizeMouseMoveHandler and _resizeMouseUpHandler declared at module scope, wired in initPanelResize, cleaned in destroy()
- render() HTML includes id="planSplitPane" and class="plan-divider" elements
- styles/views.css .plan-split-pane uses grid-template-columns: 35% 4px 1fr
- styles/views.css has .plan-divider rule with cursor: col-resize and hover blue
- showDeleteConfirmModal() defined; called from gridDeleteRow when hasChildren
- Old inline tooltip code (tg-delete-confirm span creation) removed from gridDeleteRow
- planDeleteConfirm cleanup in destroy()
</success_criteria>

<output>
After completion, create `.planning/phases/86.2-gantt-ux-polish-row-alignment-drag-indent-panels/86.2-03-SUMMARY.md`
</output>
