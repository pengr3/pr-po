---
phase: 36-fix-the-expense-breakdown-modal-in-services-export-the-one-we-ve-been-using-in-projects
task: 0
total_tasks: 0
status: context_captured
last_updated: 2026-02-20T10:37:16.540Z
---

<current_state>
Phase 36 context gathering complete. No plans created yet. CONTEXT.md committed. Ready for /gsd:plan-phase 36.
</current_state>

<completed_work>

- Phase 36 added to ROADMAP.md via /gsd:add-phase
- /gsd:discuss-phase 36 completed — CONTEXT.md written and committed
- STATE.md updated with session info

</completed_work>

<remaining_work>

- Plan the phase: /gsd:plan-phase 36
- Execute the plan: /gsd:execute-phase 36

</remaining_work>

<decisions_made>

- Services modal has WRONG scorecards (MRF count/PR total/PO total) and WRONG tabs (Purchase Requests/Purchase Orders) — must be replaced with projects-style breakdown (Material/Transport/Subcon scorecards, By Category/Transport Fees tabs)
- Fix: services modal should query POs by `service_code` and apply the exact same item-level category breakdown logic as the projects modal
- Unify into ONE function: `showExpenseBreakdownModal` with a mode parameter (`'project'` | `'service'`)
- DELETE `showServiceExpenseBreakdownModal` — no backward-compat wrapper
- Update ALL call sites: both `service-detail.js` and `project-detail.js` call the unified function directly
- Budget field: use `budget` (not `contract_cost`), labeled generically as `Budget` (not "Service Budget")

</decisions_made>

<blockers>

None.

</blockers>

<context>
The expense-modal.js currently exports two separate functions. `showExpenseBreakdownModal` is the full-featured projects modal (category breakdown, subcon, transport). `showServiceExpenseBreakdownModal` is a completely different implementation that shows MRF/PR/PO counts instead of the real item breakdown — this is what needs to be fixed. The planner needs to: (1) refactor `showExpenseBreakdownModal` to accept a mode so it can query by either `project_name` or `service_code`, (2) remove `showServiceExpenseBreakdownModal` and its window helpers, (3) update service-detail.js call sites to use the unified function.
</context>

<next_action>
Start with: /gsd:plan-phase 36
(In fresh context window — CONTEXT.md has all decisions locked for the planner)
</next_action>
