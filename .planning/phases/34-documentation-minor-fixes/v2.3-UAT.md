---
status: complete
phase: v2.3-services-department-support
source: 28-02-SUMMARY.md, 28-03-SUMMARY.md, 29-01-SUMMARY.md, 30-01-SUMMARY.md, 31-01-SUMMARY.md, 33-01-SUMMARY.md, 34-02-SUMMARY.md
started: 2026-02-19T00:00:00Z
updated: 2026-02-19T12:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Services tab navigation
expected: "Services" nav link visible between Projects and Material Request. Clicking it loads the Services list view with "Services" and "Recurring" sub-tabs inside the card.
result: pass

### 2. Sub-tab filtering
expected: Clicking "Services" sub-tab shows only one-time services; clicking "Recurring" shows only recurring services. Switching sub-tabs does not reload the page or lose state.
result: pass

### 3. Create a service
expected: (As services_admin or super_admin) Clicking "Add Service" opens a modal/form. Filling in name, selecting a client and service type, and saving creates a new service with an auto-generated code in CLMC_CLIENT_YYYY### format. The new service appears in the list.
result: pass

### 4. Service detail page
expected: Clicking a service row (or its name link) navigates to a full-page service detail at #/services/detail/CODE. The page shows 3 cards: Service Info (code, name, type, client, personnel), Financial Summary (budget, contract cost, expense scorecards), and Status & Assignment (internal status, project status selects).
result: issue
reported: "Error when clicking a row in services, detail does not appear — Expense aggregation failed: FirebaseError: The query requires an index on prs (service_code + total_amount)"
severity: blocker

### 5. Inline editing on service detail
expected: Clicking on the service_name field (or any editable field) makes it editable. Typing a new value and clicking away (blur) or pressing Enter saves it to Firestore silently. No page reload — the field shows the updated value.
result: pass

### 6. Service expense breakdown
expected: The Financial Summary card on service detail shows three scorecards: "MRFs Linked" (count), "PR Total" (PHP amount or em-dash if none), and "PO Total" (PHP amount or em-dash if none). A "Refresh" button is present. If no MRFs/PRs/POs are linked to this service yet, totals show "—".
result: pass

### 7. MRF form — role-based dropdown (single department role)
expected: (As operations_admin or operations_user) Open the MRF form. Only a "Project" dropdown is visible — no Services dropdown. (As services_admin or services_user) Only a "Service" dropdown is visible — no Projects dropdown.
result: pass

### 8. MRF form — shared roles see both dropdowns
expected: (As super_admin, finance, or procurement) Open the MRF form. Both a "Project" dropdown AND a "Service" dropdown are visible, clearly labeled. The form can be submitted linking to either.
result: issue
reported: "I hate this separate drop-down. Combine it in a single dropdown. Just provide a divider for projects and services within the scrollable dropdown. So projects and services be easily divided."
severity: major

### 9. Services-linked MRF in MRF list
expected: After submitting an MRF linked to a service, it appears in the MRF list. The service name/code is shown in the list row (in the Project/Service column). The MRF detail view shows the service code and service name.
result: pass

### 10. Finance Pending Approvals — department badges and filter
expected: In Finance > Pending Approvals (Material PRs tab), each PR row shows a small colored badge: purple "Services" for services-linked PRs, blue "Projects" for project-linked PRs. A department filter dropdown in the card header lets you filter to "Projects only" or "Services only".
result: issue
reported: "pass, however fix the line underneath see the photo attached. It looks crooked — the department badge in the table row causes vertical misalignment, making the row border look uneven."
severity: cosmetic

### 11. Finance Purchase Orders — department filter
expected: In Finance > Purchase Orders tab, a department filter dropdown appears in the card header (next to the Refresh button). Selecting "Projects" or "Services" filters the PO list accordingly.
result: issue
reported: "pass, but same UI seems broken — department badges (Services/Projects) in the PO table rows cause vertical misalignment; rows with badges sit unevenly, and 'Pending Procurement' status wraps to two lines making affected rows taller."
severity: cosmetic

### 12. Dashboard stats by role
expected: (As operations_admin/operations_user) Home page shows 3 stats: Active MRFs, Pending PRs, Active POs — no Services stats. (As services_admin/services_user) Home page shows 2 stats: Active Services, Active MRFs. (As super_admin/finance) Home page shows 5 stats in two labeled groups: "Projects" (3 stats) and "Services" (2 stats) separated by a divider.
result: pass

## Summary

total: 12
passed: 8
issues: 4
pending: 0
skipped: 0

## Gaps

- truth: "Department badges in all Finance and Procurement tables align vertically with surrounding cell content — no row height distortion"
  status: failed
  reason: "User reported (tests 10 & 11): Department badges (Services/Projects) cause vertical misalignment in table rows. Finance PR table row border looks crooked. Finance PO table rows with badges sit unevenly; 'Pending Procurement' status wraps to two lines making affected rows taller."
  severity: cosmetic
  test: 10
  artifacts:
    - path: "app/views/finance.js"
      issue: "Department badge spans in renderMaterialPRs() and renderPOs() cause row height distortion — need vertical-align: middle on td elements or badge display fix"
    - path: "app/views/procurement.js"
      issue: "Same badge alignment issue likely present in PO Tracking table renderPOTrackingTable()"
  missing:
    - "Add vertical-align: middle to table td/th in components.css or views.css"
    - "Or set badge to display: inline-flex; vertical-align: middle so it doesn't push row height"

- truth: "MRF form for shared roles (super_admin/finance/procurement) uses a single combined dropdown with a divider separating Projects and Services options"
  status: failed
  reason: "User reported: I hate this separate drop-down. Combine it in a single dropdown. Just provide a divider for projects and services within the scrollable dropdown. So projects and services be easily divided."
  severity: major
  test: 8
  artifacts: []
  missing: []

- truth: "Service detail page renders with 3 cards when clicking a service row"
  status: failed
  reason: "User reported: Error when clicking a row in services, detail does not appear — [ServiceDetail] Expense aggregation failed: FirebaseError: The query requires an index"
  severity: blocker
  test: 4
  root_cause: "getAggregateFromServer on prs (and pos) collection with where('service_code','==',X) + sum('total_amount') requires a composite Firestore index on (service_code ASC, total_amount ASC). Index not deployed. Since refreshServiceExpense is awaited before renderServiceDetail(), the render never happens."
  artifacts:
    - path: "app/views/service-detail.js"
      issue: "refreshServiceExpense awaited in onSnapshot callback before renderServiceDetail — aggregation failure prevents render"
    - path: "firestore.indexes.json"
      issue: "Missing composite indexes for prs (service_code + total_amount) and pos (service_code + total_amount)"
  missing:
    - "Add composite index: prs collection, fields: service_code ASC + total_amount ASC"
    - "Add composite index: pos collection, fields: service_code ASC + total_amount ASC"
    - "Deploy indexes via firebase deploy --only firestore:indexes"
