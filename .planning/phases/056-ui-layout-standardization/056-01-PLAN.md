---
phase: 056-ui-layout-standardization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/procurement.js
  - app/views/admin.js
  - app/views/mrf-form.js
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06

must_haves:
  truths:
    - "MRF Processing work area stretches to full viewport width with no narrow box constraint"
    - "Pending MRFs panel left edge aligns to the same horizontal position as the Finance tab left margin"
    - "MRF Details panel right edge aligns to the Finance tab right margin"
    - "Material Request sub-tab nav bar left-aligns to the logo position, matching Finance"
    - "Procurement sub-tab nav bar left-aligns to the logo position, matching Finance"
    - "Admin sub-tab nav bar left-aligns to the logo position, matching Finance"
  artifacts:
    - path: "app/views/procurement.js"
      provides: "MRF Processing full-width layout and correct sub-nav centering"
      contains: "max-width: 1600px"
    - path: "app/views/admin.js"
      provides: "Admin sub-tab nav bar at correct 1600px centering"
      contains: "max-width: 1600px"
    - path: "app/views/mrf-form.js"
      provides: "Material Request sub-tab nav bar at correct 1600px centering"
      contains: "max-width: 1600px"
  key_links:
    - from: "app/views/finance.js render()"
      to: "app/views/procurement.js render()"
      via: "matching max-width: 1600px on sub-nav wrapper and content area"
      pattern: "max-width: 1600px"
    - from: "app/views/finance.js render()"
      to: "app/views/admin.js render()"
      via: "matching max-width: 1600px on sub-nav wrapper"
      pattern: "max-width: 1600px"
    - from: "app/views/finance.js render()"
      to: "app/views/mrf-form.js renderSubTabNav()"
      via: "matching max-width: 1600px centering wrapper"
      pattern: "max-width: 1600px"
---

<objective>
Standardize all sub-tab nav bar widths and the MRF Processing content area to match the Finance tab's 1600px-centered layout.

Purpose: Finance tab is the reference alignment — logo-left, logout-right. All other tabs with sub-tab navs and content areas currently use max-width: 1400px (procurement, admin) or no centering wrapper (mrf-form), causing visible misalignment.

Output: Three modified view files where all sub-tab nav bars and the MRF Processing content area use max-width: 1600px centering, matching Finance exactly.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Reference: Finance tab layout (CORRECT — do not modify)

From `app/views/finance.js` render():

```javascript
// Sub-tab nav wrapper — REFERENCE ALIGNMENT
<div style="background: white; border-bottom: 2px solid var(--gray-200);">
    <div style="max-width: 1600px; margin: 0 auto; padding: 0 2rem;">
        <div class="tabs-nav">
            ...tabs...
        </div>
    </div>
</div>

// Content wrapper — REFERENCE ALIGNMENT
<div class="container" style="max-width: 1600px; margin: 0 auto; padding: 1.5rem;">
```

The top-nav `.top-nav-content` also uses `max-width: 1600px` (from components.css). This is the shared alignment axis — every horizontal band (nav, sub-tab bar, content) must use the same max-width and auto-margin to align vertically.

## Current broken states

### procurement.js (UI-01, UI-02, UI-03, UI-05)
Sub-nav wrapper uses `max-width: 1400px` — 200px narrower than Finance:
```javascript
<div style="background: white; border-bottom: 1px solid var(--gray-200);">
    <div style="max-width: 1400px; margin: 0 auto; padding: 0 2rem;">  <!-- WRONG: 1400px -->
```

Content wrapper uses `.container` class which is `max-width: 1400px` from main.css:
```javascript
<div class="container" style="margin-top: 2rem;">  <!-- WRONG: resolves to 1400px via .container -->
```

### admin.js (UI-06)
Sub-nav wrapper uses `max-width: 1400px`:
```javascript
<div style="background: white; border-bottom: 1px solid var(--gray-200);">
    <div style="max-width: 1400px; margin: 0 auto; padding: 0 2rem;">  <!-- WRONG: 1400px -->
```

### mrf-form.js (UI-04)
`renderSubTabNav()` returns a bare `.tabs-nav` with no centering wrapper:
```javascript
function renderSubTabNav(activeTab) {
    return `
        <div class="tabs-nav" style="background: white; border-bottom: 1px solid var(--gray-200); padding: 0 2rem;">
        <!-- WRONG: no max-width container, tabs-nav goes edge-to-edge -->
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix procurement.js sub-nav and content wrapper to 1600px</name>
  <files>app/views/procurement.js</files>
  <action>
In the `render()` function (around line 163), make two changes:

**Change 1 — Sub-tab nav wrapper:** Change `max-width: 1400px` to `max-width: 1600px` in the inner centering div of the tab navigation section. The outer div has `border-bottom: 1px solid var(--gray-200)`. The inner div currently reads `style="max-width: 1400px; margin: 0 auto; padding: 0 2rem;"` — update it to `style="max-width: 1600px; margin: 0 auto; padding: 0 2rem;"`.

**Change 2 — Content wrapper:** Change `<div class="container" style="margin-top: 2rem;">` to `<div style="max-width: 1600px; margin: 2rem auto 0; padding: 0 2rem;">`. Do NOT use the `.container` class — that class resolves to `max-width: 1400px` which is the root cause of UI-01/UI-02/UI-03. Use an inline style with `max-width: 1600px` directly, matching the Finance tab pattern.

Do not modify any other part of the render() function or any other function.
  </action>
  <verify>
    <automated>grep -n "max-width: 1600px" C:/Users/Admin/Roaming/pr-po/app/views/procurement.js | head -5</automated>
  </verify>
  <done>File contains at least two occurrences of `max-width: 1600px` in the render() function — one in the sub-nav wrapper div and one in the content wrapper div. No remaining `max-width: 1400px` strings appear in the render() function.</done>
</task>

<task type="auto">
  <name>Task 2: Fix admin.js sub-nav wrapper to 1600px and fix mrf-form.js renderSubTabNav to use centering wrapper</name>
  <files>app/views/admin.js, app/views/mrf-form.js</files>
  <action>
**admin.js — render() function (around line 52):**
In the tab navigation section, find the inner centering div: `style="max-width: 1400px; margin: 0 auto; padding: 0 2rem;"` and change `1400px` to `1600px`. The outer div has `style="background: white; border-bottom: 1px solid var(--gray-200);"`. Only this one inline style value changes — do not touch sectionButtons logic or anything else.

**mrf-form.js — renderSubTabNav() function (around line 26):**
The current implementation returns a `.tabs-nav` div directly with `padding: 0 2rem` as a top-level element. Replace it with the Finance tab's two-level wrapper pattern:

```javascript
function renderSubTabNav(activeTab) {
    return `
        <div style="background: white; border-bottom: 1px solid var(--gray-200);">
            <div style="max-width: 1600px; margin: 0 auto; padding: 0 2rem;">
                <div class="tabs-nav">
                    <button class="tab-btn ${activeTab === 'form' ? 'active' : ''}"
                        onclick="window.navigateToTab('form')">
                        Material Request Form
                    </button>
                    <button class="tab-btn ${activeTab === 'my-requests' ? 'active' : ''}"
                        onclick="window.navigateToTab('my-requests')">
                        My Requests
                    </button>
                </div>
            </div>
        </div>
    `;
}
```

The key change: the outer div carries `background: white; border-bottom: 1px solid var(--gray-200)` (moved from the old `.tabs-nav` style), the inner div provides `max-width: 1600px; margin: 0 auto; padding: 0 2rem;`, and `.tabs-nav` becomes the innermost flex container with no extra padding/background. This exactly mirrors the Finance and fixed Procurement pattern. Do not modify any other function in mrf-form.js.
  </action>
  <verify>
    <automated>grep -n "max-width: 1600px" C:/Users/Admin/Roaming/pr-po/app/views/admin.js C:/Users/Admin/Roaming/pr-po/app/views/mrf-form.js</automated>
  </verify>
  <done>admin.js contains `max-width: 1600px` in its render() sub-nav wrapper (no remaining `max-width: 1400px` in render()). mrf-form.js renderSubTabNav() contains `max-width: 1600px` inside a two-level wrapper structure matching the Finance pattern.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify all three files are consistent:

```bash
grep -n "max-width: 1600px" \
  C:/Users/Admin/Roaming/pr-po/app/views/procurement.js \
  C:/Users/Admin/Roaming/pr-po/app/views/admin.js \
  C:/Users/Admin/Roaming/pr-po/app/views/mrf-form.js
```

Expected: at least 1 match per file (2 matches in procurement.js).

Also verify no remaining 1400px in the affected view render sections:
```bash
grep -n "max-width: 1400px" \
  C:/Users/Admin/Roaming/pr-po/app/views/procurement.js \
  C:/Users/Admin/Roaming/pr-po/app/views/admin.js \
  C:/Users/Admin/Roaming/pr-po/app/views/mrf-form.js
```

Expected: zero matches.

Manual check: Open the app in browser (`python -m http.server 8000`), navigate to:
1. Finance tab — note exact left edge of sub-tab nav and content width (reference)
2. Procurement > MRF Processing — sub-tab nav left edge must match Finance; content must span full 1600px
3. Material Request tab — sub-tab nav left edge must match Finance
4. Admin tab — sub-tab nav left edge must match Finance
</verification>

<success_criteria>
1. All six requirement IDs addressed: UI-01 (MRF Processing full-width), UI-02 (left edge aligned), UI-03 (right edge aligned), UI-04 (Material Request sub-nav), UI-05 (Procurement sub-nav), UI-06 (Admin sub-nav)
2. All three modified files use max-width: 1600px (matching Finance's reference layout)
3. No remaining max-width: 1400px in the render() / renderSubTabNav() sections of these files
4. The .container CSS class is no longer used for the MRF Processing content wrapper (that class is 1400px — using inline max-width: 1600px instead)
5. No functional behavior changed — only layout container widths
</success_criteria>

<output>
After completion, create `.planning/phases/056-ui-layout-standardization/056-01-SUMMARY.md`
</output>
