---
phase: 98-ui-fixes-client-contact-notifications-payables-home
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - styles/hero.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "On a wide monitor (≥1080px tall viewport) all 5 department tiles (Clients/Projects/Services on top, Procurement/Finance on bottom) AND the hero title fit above the fold on load without scrolling (D-13, SC4)"
    - "The 3 + 2 tile grouping is preserved (.dept-cards-row--top repeat(3,1fr) / .dept-cards-row--bottom repeat(2,1fr)) — NOT changed to a single row of 5 (D-14)"
    - "Fit is achieved by VERTICAL COMPRESSION (reduced card padding, icon size, title/text sizes, hero title/subtitle, inter-row spacing) — a height fix, not a width change (D-15)"
    - "The container width cap (max-width:1200px) is preserved on .dept-cards and .navigation-cards (D-16)"
    - "The ≤1024px / ≤768px / ≤480px responsive media blocks are preserved (D-16)"
  artifacts:
    - path: "styles/hero.css"
      provides: "Vertically compressed hero + dept-card sizing so 5 tiles fit above the fold"
      contains: "dept-cards-row--top"
  key_links:
    - from: ".hero-section / .nav-card / .dept-cards spacing"
      to: "above-the-fold fit on ≥1080px-tall viewports"
      via: "reduced padding/font/icon/gap values (no width change)"
      pattern: "max-width:\\s*1200px"
---

<objective>
Slice 4 — Home Fit. Make all 5 department tiles (3 top: Clients/Projects/Services; 2 bottom: Procurement/Finance) plus the hero title fit above the fold on load on a wide monitor. The problem is vertical (the bottom 2 tiles clip below the fold), not horizontal (D-13). Fix by vertical compression only: reduce `.nav-card` padding, icon size, title/text sizes, inter-row/grid gaps, and the hero title/subtitle spacing. Keep the 3 + 2 grouping (D-14), keep the `max-width:1200px` width cap (D-16), and preserve the ≤1024/≤768/≤480 responsive blocks (D-16).

Purpose: Operator reported "the Hero with the 5 widgets does not fit the screen on load" on a wide monitor — bottom tiles are clipped (D-13).
Output: `styles/hero.css` edited in place — compressed desktop sizing for the hero header and `.nav-card`/`.dept-cards` grid. No JS/markup change in `home.js`. No width widening. No grouping change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-ui-fixes-client-contact-notifications-payables-home/98-CONTEXT.md
@.planning/phases/98-ui-fixes-client-contact-notifications-payables-home/98-PATTERNS.md

<interfaces>
<!-- Exact current hero.css values (verified working tree 2026-06-03). These are the compression targets (D-15). -->
<!-- home.js render() hero block: .hero-section (:130) → .hero-title (:131) / .hero-subtitle (:132) → .dept-cards (:134) with --top (:135, 3 tiles) / --bottom (:155, 2 tiles). Each .nav-card = icon emoji + <h3> + <p> + <button>. .quick-stats (:187) is the overview card — NOT a hero tile; do NOT touch it. -->

styles/hero.css current values:
```css
/* :5  */  .hero-section { min-height: calc(100vh - 64px); display:flex; flex-direction:column;
                           justify-content:center; align-items:center; padding: 4rem 2rem; background:linear-gradient(...); }
/* :15 */  .hero-title { font-size: 3rem; font-weight:700; margin-bottom: 0.5rem; line-height:1.1; }
/* :24 */  .hero-subtitle { font-size: 1.25rem; margin-bottom: 4rem; font-weight:400; }   /* large 4rem gap */

/* :33 */  .navigation-cards { display:grid; grid-template-columns:repeat(3,1fr); gap:2rem; max-width:1200px; width:100%; margin-bottom:4rem; }  /* legacy block — keep max-width:1200px (D-16) */

/* :42 */  .nav-card { background:white; border-radius:12px; padding: 2.5rem 2rem; text-align:center; box-shadow:...; transition:...; cursor:pointer; border:2px solid transparent; }
/* :59 */  .nav-card-icon { font-size: 4rem; margin-bottom: 1.5rem; display:block; }
/* :65 */  .nav-card h3 { font-size: 1.5rem; font-weight:600; margin-bottom: 1rem; }
/* :72 */  .nav-card p  { font-size: 1rem; margin-bottom: 2rem; line-height:1.6; }
/* :79 */  .nav-card .btn { width:100%; padding:0.75rem 1.5rem; font-size:1rem; font-weight:600; }

/* :87 */  .dept-cards { display:flex; flex-direction:column; gap: 2rem; max-width:1200px; width:100%; margin-bottom: 2rem; }  /* keep max-width:1200px (D-16) */
/* :96 */  .dept-cards-row { display:grid; gap: 2rem; }
/* :101*/  .dept-cards-row--top { grid-template-columns:repeat(3,1fr); }    /* KEEP 3 (D-14) */
/* :105*/  .dept-cards-row--bottom { grid-template-columns:repeat(2,1fr); max-width:calc((100% - 4rem) * 2 / 3 + 2rem); margin:0 auto; }  /* KEEP 2 (D-14) */

/* responsive — PRESERVE all three (D-16): */
/* :329*/  @media (max-width:1024px) { .dept-cards-row--top{repeat(2,1fr)} .dept-cards-row--bottom{repeat(2,1fr); max-width:100%} ... }
/* :353*/  @media (max-width:768px)  { .hero-section{padding:3rem 1rem;min-height:auto} .hero-title{2rem} .hero-subtitle{1rem;mb:2rem} .nav-card{padding:2rem 1.5rem} .nav-card-icon{3rem} .nav-card h3{1.25rem} .nav-card p{0.9375rem} ... }
/* :422*/  @media (max-width:480px)  { .hero-title{1.75rem} .hero-subtitle{0.9375rem} .nav-card{padding:1.5rem 1rem} .nav-card-icon{2.5rem} ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Vertically compress hero header + nav-card + dept-card grid (desktop base values)</name>
  <read_first>
    - styles/hero.css (full file — lines 1-130 for the hero/nav-card/dept-card base rules; lines 329-484 for the responsive blocks that must stay intact)
    - .planning/phases/98-ui-fixes-client-contact-notifications-payables-home/98-PATTERNS.md (Slice 4.B + 4.C — exact current values + what to compress vs preserve)
  </read_first>
  <behavior>
    - The desktop base values for the vertical-space consumers are reduced so the 5 tiles + hero title fit above the fold on a ≥1080px-tall viewport.
    - The 3 + 2 grid template columns are unchanged; the max-width:1200px caps are unchanged; the bottom-row centering calc is unchanged.
    - The ≤1024 / ≤768 / ≤480 media blocks are unchanged (they already override from the larger base; with the smaller base they still produce sensible smaller sizes).
  </behavior>
  <action>
    Edit `styles/hero.css` in place. Apply these specific value reductions (D-15 vertical compression). These are starting values tuned to fit a ~1080px-tall viewport; the executor may fine-tune within ±0.25rem during UAT (Claude's Discretion per CONTEXT lines 47-49) but must NOT change any width/grid-template/max-width property (D-14/D-16).

    1. **`.hero-section` (:5):** reduce vertical padding `padding: 4rem 2rem;` → `padding: 2rem 2rem;`. Keep `min-height: calc(100vh - 64px)` and the flex centering (the centering still works; less padding = more room for tiles).

    2. **`.hero-title` (:15):** `font-size: 3rem;` → `font-size: 2.25rem;`. Keep `margin-bottom: 0.5rem; line-height: 1.1;`.

    3. **`.hero-subtitle` (:24):** `font-size: 1.25rem;` → `font-size: 1.05rem;`; `margin-bottom: 4rem;` → `margin-bottom: 1.5rem;` (this 4rem→1.5rem gap reduction is the single biggest vertical saving).

    4. **`.nav-card` (:42):** `padding: 2.5rem 2rem;` → `padding: 1.5rem 1.5rem;`.

    5. **`.nav-card-icon` (:59):** `font-size: 4rem;` → `font-size: 2.75rem;`; `margin-bottom: 1.5rem;` → `margin-bottom: 0.75rem;`.

    6. **`.nav-card h3` (:65):** `font-size: 1.5rem;` → `font-size: 1.25rem;`; `margin-bottom: 1rem;` → `margin-bottom: 0.5rem;`.

    7. **`.nav-card p` (:72):** `font-size: 1rem;` → `font-size: 0.9rem;`; `margin-bottom: 2rem;` → `margin-bottom: 1rem;`; keep `line-height: 1.6;` (or reduce to 1.45 if still tight during UAT).

    8. **`.nav-card .btn` (:79):** `padding: 0.75rem 1.5rem;` → `padding: 0.5rem 1.5rem;` (keep width:100%, font-size:1rem).

    9. **`.dept-cards` (:87):** `gap: 2rem;` → `gap: 1rem;`; `margin-bottom: 2rem;` → `margin-bottom: 1rem;`. **Do NOT change `max-width: 1200px`** (D-16).

    10. **`.dept-cards-row` (:96):** `gap: 2rem;` → `gap: 1rem;`.

    **DO NOT TOUCH:** `.dept-cards-row--top` grid-template (repeat(3,1fr)), `.dept-cards-row--bottom` grid-template/max-width/calc/margin (D-14), `.navigation-cards max-width:1200px` (D-16), `.quick-stats` (it is the overview card, not a tile), and all three `@media` blocks at :329 / :353 / :422 (D-16).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('styles/hero.css','utf8'); const ok = c.includes('grid-template-columns: repeat(3, 1fr)') && c.includes('grid-template-columns: repeat(2, 1fr)') && (c.match(/max-width:\s*1200px/g)||[]).length >= 2 && c.includes('@media (max-width: 1024px)') && c.includes('@media (max-width: 768px)') && c.includes('@media (max-width: 480px)') && c.includes('calc((100% - 4rem) * 2 / 3 + 2rem)') && !/\.hero-subtitle\s*\{[^}]*margin-bottom:\s*4rem/.test(c) && !/\.nav-card-icon\s*\{[^}]*font-size:\s*4rem/.test(c); process.exit(ok?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - hero.css STILL contains `grid-template-columns: repeat(3, 1fr)` and `grid-template-columns: repeat(2, 1fr)` (3+2 grouping preserved, D-14)
    - hero.css STILL contains the bottom-row centering `calc((100% - 4rem) * 2 / 3 + 2rem)` (D-14 unchanged)
    - hero.css STILL contains at least two `max-width: 1200px` declarations (.dept-cards + .navigation-cards, D-16)
    - hero.css STILL contains all three media blocks: `@media (max-width: 1024px)`, `@media (max-width: 768px)`, `@media (max-width: 480px)` (D-16)
    - hero.css `.hero-subtitle` no longer has `margin-bottom: 4rem` (compressed — D-15)
    - hero.css `.nav-card-icon` no longer has `font-size: 4rem` (compressed — D-15)
    - hero.css `.hero-title` font-size reduced from 3rem (no `font-size: 3rem` in the base `.hero-title` rule)
    - `.quick-stats` rules are unchanged (not edited)
  </acceptance_criteria>
  <done>
    Desktop base values for hero header, nav-card, and dept-card grid are vertically compressed; 3+2 grouping, 1200px width caps, bottom-row centering, and all responsive blocks preserved; `.quick-stats` untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only stylesheet change; no data, no script, no input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-08 | n/a | styles/hero.css | accept | CSS/markup-only change with no new threat surface — reuses existing already-escaped home.js render output; no JS, data, or input introduced. L1 review found no threats beyond standard output-encoding, which is unaffected by this stylesheet change. |

No new attack surface.
</threat_model>

<verification>
**Automated (grep/file-read — no test harness):**
- Run the Task 1 `<verify>` node command; must exit 0 (confirms preservation invariants AND that compression was applied).

**Manual browser UAT checklist (no automated harness; viewport-height dependent):**
1. Serve `python -m http.server 8000`, open `#/` (home) on a wide monitor or a maximized browser on a ≥1080px-tall display.
2. On load (no scrolling): confirm the hero title "🏗️ CLMC", the subtitle, AND all 5 tiles (Clients/Projects/Services on top row, Procurement/Finance on bottom row) are fully visible above the fold. The bottom 2 tiles must NOT be clipped.
3. Confirm tiles remain legible (icon + title + description + Enter button readable, not cramped). If still clipped on a 1080px display, reduce `.nav-card` padding and `.dept-cards` gap further (e.g. padding 1.25rem, gap 0.75rem) within Claude's Discretion.
4. Confirm layout is still the 3-on-top / 2-on-bottom grouping (not one row of 5).
5. Resize the window to <1024px → confirm tiles reflow per the existing media block (2-up). Resize to <768px → confirm single-column reflow. (Media blocks preserved.)
6. Confirm the Procurement quick-stats overview section below the tiles is unchanged.
</verification>

<success_criteria>
- All 5 tiles + hero title fit above the fold on a ≥1080px-tall wide-monitor load (D-13, SC4).
- 3 + 2 grouping preserved (D-14).
- Fit achieved via vertical compression, not width change (D-15).
- max-width:1200px caps + ≤1024/≤768/≤480 media blocks preserved (D-16).
</success_criteria>

<output>
After completion, create `.planning/phases/98-ui-fixes-client-contact-notifications-payables-home/98-04-SUMMARY.md`
</output>
