---
phase: 98-ui-fixes-client-contact-notifications-payables-home
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/notifications.js
  - styles/components.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "On the #/notifications history page each row is a single-line INLINE layout (type-label + icon, then message, then time + ✓ on one line) — NOT the stacked label-above-message body (D-07, SC2)"
    - "The type-label + icon occupies a fixed-width left column sized to fit the longest label 'REGISTRATION PENDING' (20 chars uppercased) so every row's message text begins at the same x-position (D-08, SC2)"
    - "The relative time + ✓ mark-read button are right-aligned consistently across all rows and all notification types (D-09, SC2)"
    - "TYPE_META, the dropdown (.na-* rules), and truncation/wrap behavior are NOT changed (D-10)"
    - "Inline style= attributes that previously forced the stacked layout are removed/adjusted so the alignment is actually effective (inline-style precedence pitfall, 98-PATTERNS.md Slice 2.B)"
  artifacts:
    - path: "app/views/notifications.js"
      provides: "renderRows() restructured to inline single-line rows with fixed-width label column"
      contains: "notif-row-label"
    - path: "styles/components.css"
      provides: "fixed-width .notif-row-label column + right-aligned time/✓ rules"
      contains: "notif-row-label"
  key_links:
    - from: "renderRows() row markup"
      to: ".notif-row-label / .notif-row-message / .notif-row-time / .notif-row-mark-read CSS"
      via: "class-driven flex layout (inline conflicting styles removed)"
      pattern: "notif-row-label"
---

<objective>
Slice 2 — Notifications Alignment. Restructure the `#/notifications` history-page rows from the current STACKED body (type-label above message above time, on separate lines — see 98-PATTERNS.md Slice 2.A verbatim) back to a single-line INLINE layout matching the approved `notifications-alignment-screenshot.png`. Give the type-label + icon a fixed-width left column so every row's message text begins at the same x-position, and right-align the relative time + ✓ mark-read button consistently across all rows and types.

Purpose: Operator reported the history rows look ragged ("they are not aligned"); the inline screenshot the user approved requires a fixed message-start-x and right-aligned time/✓ (D-07, D-08, D-09).
Output: `app/views/notifications.js` `renderRows()` restructured to inline; `styles/components.css` `.notif-row*` rules gain a fixed-width label column + right-aligned trailing column. No TYPE_META / dropdown / truncation changes (D-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-ui-fixes-client-contact-notifications-payables-home/98-CONTEXT.md
@.planning/phases/98-ui-fixes-client-contact-notifications-payables-home/98-PATTERNS.md
@.planning/phases/98-ui-fixes-client-contact-notifications-payables-home/notifications-alignment-screenshot.png

<interfaces>
<!-- Exact current code (98-PATTERNS.md Slice 2, verified working tree 2026-06-03). -->
<!-- LONGEST TYPE_META label = "Registration Pending" → "REGISTRATION PENDING" (20 chars). Size the fixed column to this. -->

Current renderRows() row markup — notifications.js:268-302 (STACKED — to be restructured to INLINE):
```javascript
return `
    <div class="notif-row ${isUnread ? 'notif-row--unread' : ''}"
         style="display:flex;align-items:flex-start;gap:0.75rem;cursor:pointer;"
         onclick="window.handleNotificationClick('${docId}')">
        <span class="notif-type-badge" style="background:${meta.color}15;color:${meta.color};" title="${escapeHTML(meta.label)}">
            ${meta.icon}
        </span>
        <div class="notif-row-body" style="flex:1;min-width:0;">
            <div class="notif-row-label" style="font-size:0.7rem;font-weight:600;color:${meta.color};text-transform:uppercase;letter-spacing:0.04em;margin-bottom:0.2rem;">
                ${escapeHTML(meta.label)}
            </div>
            <div class="notif-row-message" style="font-size:0.875rem;color:#1e293b;line-height:1.4;${isUnread ? 'font-weight:600;' : ''}">
                ${message}
            </div>
            <div class="notif-row-time" style="font-size:0.75rem;color:#64748b;margin-top:0.25rem;" title="${absoluteDate}">
                ${relativeTime}
            </div>
        </div>
        <button class="notif-row-mark-read" style="flex-shrink:0;background:none;border:1px solid #e2e8f0;border-radius:4px;padding:0.25rem 0.5rem;font-size:0.75rem;color:#64748b;cursor:pointer;white-space:nowrap;"
                onclick="event.stopPropagation(); window.markNotificationRead('${docId}')" aria-label="Mark as read" title="Mark as read">
            ✓
        </button>
    </div>
`;
```
Container wrapper — notifications.js:305: `list.innerHTML = \`<div class="notif-rows-container">${rowsHtml}</div>\`;`

Existing .notif-row* CSS — styles/components.css (anchors):
```css
/* :1952 */ .notif-row { display:flex; align-items:flex-start; gap:0.75rem; padding:0.75rem 1rem; border-bottom:1px solid #f1f5f9; border-left:3px solid transparent; cursor:pointer; transition:background 0.12s ease; }
/* :1976 */ .notif-type-badge { display:inline-flex; align-items:center; justify-content:center; width:28px; height:28px; border-radius:6px; flex-shrink:0; }
/* :1997 */ .notif-row-body { display:flex; align-items:flex-start; gap:0.5rem; flex:1; min-width:0; cursor:pointer; }
/* :2030 */ .notif-row-message { flex:1 1 auto; font-size:0.875rem; color:#1e293b; line-height:1.4; min-width:0; word-wrap:break-word; overflow-wrap:anywhere; }
/* :2043 */ .notif-row-time { font-size:0.75rem; color:#64748b; flex-shrink:0; white-space:nowrap; }
/* :2135 */ .notif-row-mark-read { background:transparent; border:1px solid transparent; color:#64748b; cursor:pointer; padding:0.25rem; border-radius:4px; flex-shrink:0; }
```
NOTE: there is NO `.notif-row-label` CSS rule today — the label is styled purely inline. The `.na-*` block (components.css:2056-2133) belongs to the DROPDOWN (Phase 95) — do NOT touch it (D-10).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add fixed-width inline .notif-row* CSS rules (label column + right-aligned trailing)</name>
  <read_first>
    - styles/components.css (lines 1948-2157 — the .notif-row* and .notif-type-badge block; the .na-* block at 2056-2133 must stay untouched)
    - .planning/phases/98-ui-fixes-client-contact-notifications-payables-home/98-PATTERNS.md (Slice 2.B + 2.C — CSS anchors + label-set sizing)
    - .planning/phases/98-ui-fixes-client-contact-notifications-payables-home/notifications-alignment-screenshot.png (target inline look)
  </read_first>
  <behavior>
    - .notif-row remains a flex row with align-items:center (inline single-line, not flex-start/stacked).
    - A new fixed-width left label column rule (.notif-row-label) sizes to fit "REGISTRATION PENDING" at font-size:0.7rem; letter-spacing:0.04em — flex:0 0 ~160px, no shrink, no grow, nowrap with ellipsis safety.
    - .notif-row-message takes the remaining width (flex:1) and starts at the same x on every row.
    - A right-aligned trailing cluster (.notif-row-time + .notif-row-mark-read) is pushed to the right (margin-left:auto on the time, or a wrapping right cluster), consistent across rows.
    - No change to .na-* / dropdown / TYPE_META.
  </behavior>
  <action>
    Edit `styles/components.css`. Locate the `.notif-row*` block (anchors ~1952-2155). Make these specific changes; do NOT touch the `.na-*` block (2056-2133) per D-10.

    1. **`.notif-row` (:1952):** change `align-items:flex-start;` to `align-items:center;` (inline single-line baseline).

    2. **`.notif-type-badge` (:1976):** keep as-is (28x28 icon square, flex-shrink:0).

    3. **`.notif-row-body` (:1997):** change to inline single-line: `display:flex; align-items:center; gap:0.75rem; flex:1; min-width:0;`. (Previously held a stacked column; now it holds label-column + message inline.)

    4. **Add a NEW rule `.notif-row-label`** (none exists today). Insert immediately after the `.notif-row-body` rule:
       ```css
       .notif-row-label {
           flex: 0 0 160px;            /* fixed-width column — fits "REGISTRATION PENDING" (20ch uppercase @0.7rem) */
           font-size: 0.7rem;
           font-weight: 600;
           text-transform: uppercase;
           letter-spacing: 0.04em;
           white-space: nowrap;
           overflow: hidden;
           text-overflow: ellipsis;
       }
       ```
       (160px is the starting estimate per Claude's Discretion / 98-PATTERNS.md Slice 2.C. If browser UAT shows "REGISTRATION PENDING" clipping, bump to 165-170px; if there is excessive dead space, trim toward 150px. Color stays per-type and is set inline in markup — that is allowed since color is not an alignment property.)

    5. **`.notif-row-message` (:2030):** keep `flex:1 1 auto; min-width:0; word-wrap/overflow-wrap` (truncation/wrap unchanged per D-10) — confirm it has `font-size:0.875rem; color:#1e293b; line-height:1.4;`.

    6. **Right-aligned trailing cluster:** ensure `.notif-row-time` (:2043) gets `margin-left:auto;` so it and the following `.notif-row-mark-read` button are pushed right. Add to the `.notif-row-time` rule: `margin-left:auto;` (keep existing `flex-shrink:0; white-space:nowrap;`). `.notif-row-mark-read` (:2135) keeps `flex-shrink:0;`. Add a small `gap` between time and ✓ by relying on the `.notif-row` parent gap (0.75rem) — time and button are now siblings of label-column/message at the row level (see Task 2 markup restructure).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('styles/components.css','utf8'); const ok = /\.notif-row-label\s*\{[^}]*flex:\s*0 0 1[0-9][0-9]px/.test(c) && c.includes('REGISTRATION PENDING') && /\.notif-row-time\s*\{[^}]*margin-left:\s*auto/.test(c) && c.includes('.na-'); process.exit(ok?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - components.css defines a `.notif-row-label` rule with `flex: 0 0 1NNpx` (fixed-width column, 150-170px)
    - components.css `.notif-row-label` includes a comment naming `REGISTRATION PENDING` as the sizing target
    - components.css `.notif-row-time` rule contains `margin-left: auto` (right-aligns the trailing cluster)
    - components.css `.notif-row` uses `align-items: center` (not flex-start)
    - The `.na-` dropdown block is still present and unmodified (grep finds `.na-` still in file)
  </acceptance_criteria>
  <done>
    `.notif-row*` CSS provides an inline single-line layout: fixed-width label column (~160px sized to "REGISTRATION PENDING"), message taking remaining width, and a right-aligned time + ✓ cluster. `.na-*` dropdown block untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restructure renderRows() markup from stacked to inline (remove conflicting inline styles)</name>
  <read_first>
    - app/views/notifications.js (renderRows() at lines 237-306 — the verbatim stacked markup is in 98-PATTERNS.md Slice 2.A)
    - .planning/phases/98-ui-fixes-client-contact-notifications-payables-home/98-PATTERNS.md (Slice 2.A + the ⚠ Inline-style precedence note in 2.B)
  </read_first>
  <behavior>
    - renderRows() emits an INLINE single-line row: [badge] [label-column] [message] [time] [✓] as direct flex children of .notif-row (the stacked .notif-row-body wrapper holding three vertical divs is removed; message + time + ✓ become row-level siblings so the right cluster aligns).
    - The inline style= attributes that forced the stacked layout (display:flex;align-items:flex-start on the row; flex:1 on body; margin-bottom/margin-top on label/time) are REMOVED so the class rules from Task 1 take effect (inline-precedence pitfall).
    - Per-type color stays inline on the label (color:${meta.color}) and badge (background/color) — color is not an alignment property.
    - TYPE_META, truncation, and the onclick handlers are unchanged (D-10).
  </behavior>
  <action>
    Edit `app/views/notifications.js` `renderRows()`. Replace the row template (notifications.js:268-302, the verbatim block in the interfaces section) with this inline structure. Remove all alignment-related inline `style=` attributes (display/align-items/flex/margin) so the Task-1 CSS classes own layout; keep only per-type color inline:

    ```javascript
    return `
        <div class="notif-row ${isUnread ? 'notif-row--unread' : ''}"
             onclick="window.handleNotificationClick('${docId}')">
            <span class="notif-type-badge"
                  style="background:${meta.color}15;color:${meta.color};"
                  title="${escapeHTML(meta.label)}">
                ${meta.icon}
            </span>
            <div class="notif-row-label" style="color:${meta.color};">
                ${escapeHTML(meta.label)}
            </div>
            <div class="notif-row-message${isUnread ? ' notif-row-message--unread' : ''}">
                ${message}
            </div>
            <div class="notif-row-time" title="${absoluteDate}">
                ${relativeTime}
            </div>
            <button class="notif-row-mark-read"
                    onclick="event.stopPropagation(); window.markNotificationRead('${docId}')"
                    aria-label="Mark as read"
                    title="Mark as read">
                ✓
            </button>
        </div>
    `;
    ```

    Notes:
    - The `.notif-row-body` wrapper div is GONE — label/message/time/✓ are now direct children of `.notif-row` so `margin-left:auto` on `.notif-row-time` (Task 1) pushes time + ✓ to the right edge.
    - The unread bold weight previously applied via inline `font-weight:600` is now carried by a `notif-row-message--unread` modifier class. Add this one small rule near `.notif-row-message` in components.css if not present: `.notif-row-message--unread { font-weight:600; }`. (This keeps the inline-precedence-free approach — no inline font-weight.)
    - Do NOT change `message`, `meta`, `relativeTime`, `absoluteDate`, `docId`, or the `TYPE_META` lookup logic above the return (D-10). Container wrapper at line 305 stays `<div class="notif-rows-container">`.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('app/views/notifications.js','utf8'); const r=c.slice(c.indexOf('function renderRows')); const ok = !/notif-row[^\"]*\"[^>]*style=\"display:flex;align-items:flex-start/.test(c) && c.includes('class=\"notif-row-label\" style=\"color:') && !/notif-row-body/.test(r) && c.includes('handleNotificationClick') && c.includes('markNotificationRead'); process.exit(ok?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - notifications.js renderRows() no longer contains `notif-row-body` (the stacked wrapper is removed)
    - notifications.js no longer has `style="display:flex;align-items:flex-start` on the `.notif-row` div
    - notifications.js label div is `class="notif-row-label" style="color:${meta.color};"` (color-only inline, no margin/font-size inline)
    - notifications.js still calls `window.handleNotificationClick` and `window.markNotificationRead` (handlers unchanged, D-10)
    - notifications.js does NOT modify TYPE_META (no `TYPE_META` assignment edits in renderRows region)
    - components.css contains `.notif-row-message--unread { font-weight:600; }` (or equivalent) for the unread bold
  </acceptance_criteria>
  <done>
    renderRows() emits an inline single-line row with badge + fixed-width label column + message + right-aligned time + ✓; conflicting inline alignment styles removed so Task-1 CSS governs layout; TYPE_META/truncation/handlers unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore notification doc → rendered row HTML | meta.label and message are rendered into the row; label is escapeHTML'd; message is pre-built upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-04 | Information disclosure / XSS (I) | renderRows() row markup | accept | No NEW data surface — this is a CSS/markup alignment change that reuses the same already-escaped values (`escapeHTML(meta.label)`, the upstream-built `message`). No new untrusted field is introduced. L1 review found no new threat surface beyond standard output-encoding, which is preserved (escapeHTML on label retained). |

CSS/markup-only alignment fix; no new attack surface.
</threat_model>

<verification>
**Automated (grep/file-read — no test harness):**
- Run both task `<verify>` node commands; both must exit 0.
- `node -e "const c=require('fs').readFileSync('app/views/notifications.js','utf8'); process.exit(c.includes('escapeHTML(meta.label)')?0:1);"` — label still escaped.
- `node -e "const c=require('fs').readFileSync('styles/components.css','utf8'); process.exit(/\.na-body|\.na-chip|\.na-time/.test(c)?0:1);"` — `.na-*` dropdown rules untouched/present (D-10).

**Manual browser UAT checklist (no automated harness; compare to notifications-alignment-screenshot.png):**
1. Serve `python -m http.server 8000`, open `#/notifications`.
2. Confirm each row is a single line: icon, then uppercase type label, then message, then time + ✓ on the right.
3. Confirm message text starts at the SAME x-position on every row regardless of label length (e.g. a "NEW MRF" row's message aligns with a "REGISTRATION PENDING" row's message).
4. Confirm the longest label "REGISTRATION PENDING" is NOT clipped in its column; if clipped, bump `.notif-row-label flex-basis` from 160px toward 170px.
5. Confirm relative time + ✓ are flush-right and vertically aligned across all rows and across different notification types.
6. Confirm the notification DROPDOWN (bell menu) is visually unchanged (D-10 — .na-* untouched).
7. Confirm unread rows still show the left blue border + bold message.
</verification>

<success_criteria>
- History rows are inline single-line (D-07, SC2).
- Fixed-width label column makes message-start-x constant; sized to "REGISTRATION PENDING" (D-08, SC2).
- Time + ✓ right-aligned consistently across rows/types (D-09, SC2).
- TYPE_META, dropdown (.na-*), and truncation unchanged (D-10).
- Conflicting inline styles removed so CSS alignment is effective.
</success_criteria>

<output>
After completion, create `.planning/phases/98-ui-fixes-client-contact-notifications-payables-home/98-02-SUMMARY.md`
</output>
