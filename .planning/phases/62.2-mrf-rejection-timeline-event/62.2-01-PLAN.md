# Phase 62.2 Plan 01: MRF Rejection Timeline Event

## Goal
Add an "MRF Rejected" event to the Procurement Timeline so rejected MRFs show their rejection reason, actor, and timestamp in the timeline view.

## Tasks

1. **procurement.js — showProcurementTimeline()**: Insert MRF Rejected event block after "MRF Created" entry, guarded by `mrf.status === 'Rejected'`, using `rejected` CSS class with `rejected_at`, `rejection_reason`, `rejected_by` fields
2. **mrf-records.js — showTimelineLocal()**: Same insertion — self-contained copy of timeline logic needs identical MRF Rejected event

## Files Modified
- `app/views/procurement.js` — 8 lines added
- `app/views/mrf-records.js` — 8 lines added
