---
phase: 87.1-proposal-lifecycle-integration-proposal-project-bidirectiona
plan: "04"
subsystem: ui
tags:
  - proposals
  - project-detail
  - inline-card
  - modal
  - firestore

dependency_graph:
  requires:
    - phase: 87.1-01
      provides: "PROPOSAL_RANGE_STATUSES, _applyProposalStateTransition, getProposalStatusBadge, renderAgeBadge exported from proposals.js"
  provides:
    - "project-detail.js inline proposal card (loadProposalCard, renderInlineProposalCard)"
    - "Submit for Approval mini-modal in project-detail.js"
    - "PROPOSAL_RANGE_STATUSES imported (not declared locally)"
  affects:
    - "app/views/service-detail.js (Wave 3, plan 05 — parallel card implementation)"

tech-stack:
  added: []
  patterns:
    - "One-time getDocs inside a render callback — no listener push, no destroy() cleanup needed"
    - "DOM-patch post-render: loadProposalCard patches #proposalInlineCard after container.innerHTML is set"
    - "insertAdjacentHTML mini-modal pattern with double-submit guard via button.disabled"

key-files:
  created: []
  modified:
    - path: "app/views/project-detail.js"
      changes:
        - "Added import for _applyProposalStateTransition, getProposalStatusBadge, renderAgeBadge, PROPOSAL_RANGE_STATUSES from ./proposals.js (line 11)"
        - "Added isInProposalRange declaration in renderProjectDetail() (line 357)"
        - "Injected proposalInlineCard placeholder div between Card 3 and planCardHtml (lines 568-570)"
        - "Added loadProposalCard() call after focus-restore block in renderProjectDetail() (lines 595-597)"
        - "Added async loadProposalCard() function (lines 601-618)"
        - "Added renderInlineProposalCard() function with overdue border logic (lines 619-657)"
        - "Added openProposalInlineSubmitModal() function (lines 658-685)"
        - "Added closeProposalInlineSubmitModal() function (line 686-690)"
        - "Added async confirmProposalInlineSubmit() function (lines 691-710)"
        - "Registered 3 new window functions in attachWindowFunctions() (lines 1430-1432)"
        - "Added 3 delete window. lines in destroy() (lines 292-294)"

key-decisions:
  - "PROPOSAL_RANGE_STATUSES imported from proposals.js — no local duplicate (addresses cross-AI review concern)"
  - "getAgeInStageDays inlined in renderInlineProposalCard since it is not exported from proposals.js — same logic as proposals.js implementation"
  - "loadProposalCard uses getDocs (one-time read) not onSnapshot — consistent with plan D-01 decision to avoid extra listeners on project detail"
  - "confirmProposalInlineSubmit fetches fresh proposal doc via getDoc before calling _applyProposalStateTransition to ensure latest state"

requirements-completed:
  - PROP-INT-01

duration: 12min
completed: 2026-05-19
---

# Phase 87.1 Plan 04: Inline Proposal Card in Project Detail Summary

**Inline proposal card added to project-detail.js — getDocs fetch, status badge, age badge, and Submit for Approval mini-modal calling _applyProposalStateTransition from proposals.js**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-19T00:00:00Z
- **Completed:** 2026-05-19
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Import block updated with PROPOSAL_RANGE_STATUSES and 3 proposal functions from proposals.js (single source of truth, no local duplicate)
- Inline proposal card placeholder injected between Card 3 and Project Plan card in renderProjectDetail()
- loadProposalCard() fetches linked proposal via one-time getDocs query on `proposals` collection filtered by `project_id`
- renderInlineProposalCard() renders status badge, age badge, amount, and context-appropriate action (Submit button or View link)
- Submit for Approval mini-modal with optional comment textarea; confirmProposalInlineSubmit() calls imported _applyProposalStateTransition with newStatus 'pending_internal'
- Three window functions registered in attachWindowFunctions() and deleted in destroy()

## Task Commits

1. **Task 1: Add imports to project-detail.js** - `25fceed` (feat)
2. **Task 2: Add inline card rendering and modal functions** - `b158acd` (feat)

## Files Created/Modified

- `app/views/project-detail.js` - Added import, isInProposalRange, card placeholder, loadProposalCard, renderInlineProposalCard, three modal functions, window registrations, destroy cleanup

## Decisions Made

- PROPOSAL_RANGE_STATUSES imported from proposals.js (not declared locally) — eliminates duplication identified in cross-AI review
- getAgeInStageDays not exported from proposals.js, so inlined in renderInlineProposalCard with identical logic
- getDocs (one-time read) used instead of onSnapshot — no listener added to project detail, consistent with plan architecture

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. Card fetches real data from Firestore `proposals` collection.

## Threat Surface Scan

No new network endpoints introduced. The submit flow crosses the DOM → Firestore boundary via `_applyProposalStateTransition` (same trust boundary as proposals.js direct submission). T-87.1-04-01 mitigated: Submit button only renders for `draft`/`for_revision` statuses. T-87.1-04-03 mitigated: confirmBtn.disabled set immediately on click.

## Self-Check: PASSED

- `grep -c "loadProposalCard" app/views/project-detail.js` → 3 (function def + call site in renderProjectDetail + call in loadProposalCard itself via el.innerHTML)
- `grep "delete window.openProposalInlineSubmitModal" app/views/project-detail.js` → line 292
- `grep "import.*_applyProposalStateTransition.*PROPOSAL_RANGE_STATUSES" app/views/project-detail.js` → line 11
- `grep "const PROPOSAL_RANGE_STATUSES" app/views/project-detail.js` → 0 results
- `grep "loadProposalCard failed" app/views/project-detail.js` → line 614

---
*Phase: 87.1*
*Completed: 2026-05-19*
