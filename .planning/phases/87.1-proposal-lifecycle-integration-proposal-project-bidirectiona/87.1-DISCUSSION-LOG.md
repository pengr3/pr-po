# Phase 87.1: Proposal Lifecycle Integration — Discussion Log

**Session:** 2026-05-19
**Mode:** /gsd-discuss-phase 87.1 --analyze

---

## Areas Discussed

All four gray areas were selected by the user.

---

### Area 1: Project/Service Detail Panel

**Options presented:**
1. Inline proposal card — title, amount, stage, age + action buttons
2. Status strip + deep link only
3. Full embedded proposal section

**Selected:** Inline proposal card

**Follow-up — Which actions on the card:**
1. Submit for Approval only (Recommended)
2. All lifecycle actions
3. Read-only card + link only
4. Create proposal here too

**Selected:** Submit for Approval only

**Decision locked:** Inline proposal card in project/service detail shows title, amount, stage badge, days-in-stage, "Submit for Approval" button (draft/for_revision only), "View Proposal →" link for other statuses.

---

### Area 2: Services Proposal Support

**Context presented:** Critical gap — `_applyProposalStateTransition` hardcodes `doc(db, 'projects', ...)` at line 1464. Service engagements going through proposal lifecycle fail to update service status.

**Options presented:**
1. Yes — add `parent_collection` field (Recommended)
2. Projects only — keep as-is

**Selected:** Yes — add `parent_collection: 'projects' | 'services'` to proposal schema

**Decision locked:** Optional `parent_collection` field added to proposal schema; defaults to `'projects'` for existing docs. `_applyProposalStateTransition` checks this field to update either `projects` or `services` collection.

---

### Area 3: Proposals Tab Retirement

**Options presented:**
1. Keep tab as global proposals list; move Create Engagement to Projects tab
2. Retire the nav link entirely; Create Engagement moves to Projects tab
3. Keep the tab as-is; just add the project/service integration on top

**User response:** "No just keep a separate tab for New Engagements"

**Follow-up — What should the Proposals tab contain:**
1. Keep all three sections as-is
2. Keep Create Engagement + Queue; remove Proposal Dashboard
3. Create Engagement only; queue and dashboard move elsewhere

**User response:** "Approval Queue and Proposal dashboard remains. Create a new tab for new engagements."

**Follow-up — New tab details:**
1. Engagements tab — Super Admin only
2. Engagements tab — Operations Admin too
3. Merge into Projects tab as a section

**User response:** Option 2 + "include services admin too"

**Decision locked:**
- Proposals tab: removes Create Engagement form; retains Approval Queue + Proposal Dashboard
- New "Engagements" tab created at `#/engagements`; visible to Super Admin + Operations Admin + Services Admin

---

### Area 4: Home Dashboard Proposals Widget

**Options presented:**
1. Proposals summary card (Recommended)
2. Sub-tab navigation
3. Just a count badge

**Selected:** Sub-tab navigation

**Follow-up — Sub-tab content and visibility:**
1. Full grouped-by-stage dashboard, Super Admin only
2. Summary list, Super Admin + Operations Admin
3. Full dashboard, all roles with active proposals

**Selected:** Full dashboard, all roles with active proposals (filtered to their projects/services)

**Decision locked:** Home dashboard gains a "Proposals" sub-tab showing the full grouped-by-stage dashboard. All roles see it filtered to their relevant proposals. Super Admin sees all. Sub-tab only appears when the user has at least one active proposal.

---

## Deferred Ideas

- Full proposal lifecycle actions (approve/reject, client decision) from project/service detail
- Real-time proposals listener on home
- "Start Proposal" button from project/service detail (proposal creation inline)
- Loss reason analytics / structured taxonomy (v4.1+)
