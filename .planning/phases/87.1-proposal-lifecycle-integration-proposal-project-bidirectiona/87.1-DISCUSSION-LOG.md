# Phase 87.1: Proposal Lifecycle Integration — Discussion Log (Redesign)

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-20
**Phase:** 87.1 — Proposal Lifecycle Integration (Redesign)
**Areas discussed:** What went wrong, Proposal actions in project/service detail, Create Engagement location, Proposals tab + home page role
**Session note:** Full redesign session. Prior 87.1 implementation was fully reverted ("scrap phase for redesign"). This discussion establishes the replacement approach.

---

## What went wrong

| Option | Description | Selected |
|--------|-------------|----------|
| Card was too passive | Read-only info card with one button isn't useful — need to drive proposal workflow from inside project/service record | ✓ |
| Wrong scope in card | Card showed wrong things / missing key info | |
| Nav restructuring was confusing | Splitting proposals.js into two tabs created two places for engagement work | |
| All of the above / fragmented | Three half-measures that didn't add up to a real integration | |

**User's choice:** Card was too passive

**Follow-up — What "not passive" means:**

| Option | Description | Selected |
|--------|-------------|----------|
| Full lifecycle from card | Do EVERYTHING from project/service detail | |
| Submit + view proposal detail inline | Open full proposal modal without leaving the record | ✓ |
| Proposal as tab/section, not card | Full tab or expandable section | |

**Follow-up — Modal reuse:**

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse exact same modal from proposals.js | Don't build a second proposal detail UI | ✓ |
| Stripped-down read-only panel | Key facts only | |
| Same modal but context-aware | Reuse but hide/disable some actions | |

**Additional issues raised (free text):** "Home tab is fucked, and you created a new engagements tab as a main nav tab but we initially intended to create 'Engagements' and 'Proposals' as a new sub tab nav in home"

**Notes:** The fundamental mismatch was the previous approach adding a top-level Engagements nav tab, when the original intent was home sub-tabs. The home sub-tab also appeared conditionally (only when proposals existed) which felt broken.

---

## Proposal actions in project/service detail

**Home sub-tab contents confirmed:**

| Option | Description | Selected |
|--------|-------------|----------|
| Overview: stats / Engagements: Create form / Proposals: dashboard | Home has three sub-tabs with those contents | ✓ |
| Overview / Engagements: list / Proposals: dashboard | Engagements = portfolio list | |
| Overview / Engagements: list / Proposals: full tab content | Proposals sub-tab mirrors entire top-level Proposals tab | |

**Inline card additional content (free text):** "attachment + latest comms entry"

**Final card fields:** title, ID, amount, stage badge, age badge + attachment link + latest comms log entry + Submit for Approval button (draft/for_revision only) + View Proposal button (always)

---

## Create Engagement location

**Engagements sub-tab visibility:**

| Option | Description | Selected |
|--------|-------------|----------|
| Super Admin only | Same gate as current Proposals tab | |
| Super Admin + Operations Admin + Services Admin | Broader | ✓ |
| All non-procurement/finance roles | Widest | |

**Form code location:**

| Option | Description | Selected |
|--------|-------------|----------|
| Inside home.js directly | Simple | |
| app/engagement-create.js (already exists) | Extend existing shared helper | ✓ |
| proposals.js non-routed, home imports | Keep in proposals.js | |

**What happens to proposals.js:** "You decide" — recorded as Claude's discretion: pure module, no route.

---

## Proposals tab + home page role

**Top-level Proposals nav tab:**

| Option | Description | Selected |
|--------|-------------|----------|
| Approval queue only | Shrinks to just queue | |
| Retired entirely | Remove from nav | ✓ |
| Stay full + home mirrors it | Two places | |

**Approval queue in home Proposals sub-tab:**

| Option | Description | Selected |
|--------|-------------|----------|
| At top, approvers only | Queue at top, dashboard below | ✓ |
| Below the dashboard | Scroll to find queue | |
| Inline in dashboard cards | Action buttons on stage cards | |

**Finance/procurement_staff see Proposals sub-tab?**

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — all users, empty state | Tab visible to all | |
| No — hidden for finance/procurement_staff | Hide completely | ✓ |

**Operations User + Services User see Proposals sub-tab?**

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — read-only, filtered | Sub-tab visible | ✓ |
| No — Admin-level only | Restricted | |

**Proposal modal location:**

| Option | Description | Selected |
|--------|-------------|----------|
| proposals.js becomes a module (no route) | Module retained | |
| app/proposal-modal.js shared utility | Extracted | ✓ |
| Tied to home.js | Locked to home | |

**Other nav changes:** None — only remove the Proposals nav link.

---

## Claude's Discretion

- Whether proposals.js keeps minimal render/init/destroy stubs or strips them entirely
- Exact sub-nav visual style on home (pill tabs vs underline — match finance view pattern)
- Whether "No proposals in your scope" empty state or hide Proposals sub-tab when user has no active proposals
- Toast copy for proposal card actions
- Card layout for inline proposal card (compact vs stacked)
- Whether `openProposalModal()` accepts a pre-fetched object or a Firestore doc ID

## Deferred Ideas

- Full proposal lifecycle actions from project/service detail (approve/reject, send to client, client decision) — only Submit for Approval + View modal in scope
- Proposal creation from project/service detail — "Start Proposal" button; deferred to v4.1+
- Real-time proposals listener on home — getDocs snapshot is sufficient
- Loss reason analytics / structured loss taxonomy — v4.1+
- Multi-attachment per proposal — v4.1+
