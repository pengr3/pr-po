---
phase: 87.1
plan: "05"
subsystem: ui
tags:
  - proposals
  - service-detail
  - inline-card
  - modal
  - firestore

dependency_graph:
  requires:
    - phase: 87.1-01
      provides: "PROPOSAL_RANGE_STATUSES, _applyProposalStateTransition, getProposalStatusBadge, renderAgeBadge exported from proposals.js"
    - phase: 87.1-04
      provides: "project-detail.js inline proposal card (reference implementation)"
  provides:
    - "service-detail.js inline proposal card (loadProposalCard, renderInlineProposalCard)"
    - "Submit for Approval mini-modal in service-detail.js"
    - "PROPOSAL_RANGE_STATUSES imported (not declared locally)"
  affects: []

tech-stack:
  added: []
  patterns:
    - "One-time getDocs inside a render callback — no listener push, no destroy() cleanup needed"
    - "DOM-patch post-render: loadProposalCard patches #proposalInlineCard after container.innerHTML is set"
    - "insertAdjacentHTML mini-modal pattern with double-submit guard via button.disabled"

key-files:
  created: []
  modified:
    - path: "app/views/service-detail.js"
      changes:
        - "Added import for _applyProposalStateTransition, getProposalStatusBadge, renderAgeBadge, PROPOSAL_RANGE_STATUSES from ./proposals.js (line 12)"
        - "Added isInProposalRange declaration in renderServiceDetail() (line 287)"
        - "Injected proposalInlineCard placeholder div after Card 3 (status select closing div) in renderServiceDetail()"
        - "Added loadProposalCard() call after focus-restore block in renderServiceDetail() (line 511)"
        - "Added async loadProposalCard() function (line 516) — passes 'services' as parentCollection"
        - "Added renderInlineProposalCard() function with overdue border logic (line 534)"
        - "Added openProposalInlineSubmitModal() function (line 573)"
        - "Added closeProposalInlineSubmitModal() function"
        - "Added async confirmProposalInlineSubmit() function (line 606)"
        - "Registered 3 new window functions in attachWindowFunctions() (lines 1203-1205)"
        - "Added 3 delete window. lines in destroy() (lines 226-228)"

key-decisions:
  - "PROPOSAL_RANGE_STATUSES imported from proposals.js — no local duplicate (same approach as Plan 04)"
  - "getAgeInStageDays inlined in renderInlineProposalCard since it is not exported from proposals.js — identical logic to project-detail.js"
  - "getDocs (one-time read) used instead of onSnapshot — no extra listener added to service detail"
  - "confirmProposalInlineSubmit fetches fresh proposal doc via getDoc before calling _applyProposalStateTransition"
  - "Error log prefix [ServiceDetail] distinguishes from [ProjectDetail] in production console"
  - "Window function names identical to project-detail.js — correct because both views are never active simultaneously"

requirements-completed:
  - PROP-INT-01

duration: 8min
completed: 2026-05-19
---

# Phase 87.1 Plan 05: Inline Proposal Card in Service Detail Summary

**Inline proposal card added to service-detail.js — services-side mirror of Plan 04, with loadProposalCard passing 'services' as parentCollection**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-05-19T00:00:00Z
- **Completed:** 2026-05-19
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Import block updated with PROPOSAL_RANGE_STATUSES and 3 proposal functions from proposals.js (single source of truth, no local duplicate)
- Inline proposal card placeholder injected after Card 3 (Status & Assignment) in renderServiceDetail()
- loadProposalCard() fetches linked proposal via one-time getDocs query on `proposals` collection filtered by `project_id`, passes `'services'` as parentCollection
- renderInlineProposalCard() renders status badge, age badge, amount, and context-appropriate action (Submit button or View link)
- Submit for Approval mini-modal with optional comment textarea; confirmProposalInlineSubmit() calls imported _applyProposalStateTransition with newStatus 'pending_internal'
- Three window functions registered in attachWindowFunctions() and deleted in destroy()

## Task Commits

1. **Task 1: Add imports to service-detail.js** - `daea93f` (feat)
2. **Task 2: Add inline card rendering and modal functions** - `8f43f4b` (feat)

## Files Created/Modified

- `app/views/service-detail.js` — Added import, isInProposalRange, card placeholder, loadProposalCard, renderInlineProposalCard, three modal functions, window registrations, destroy cleanup

## Decisions Made

- PROPOSAL_RANGE_STATUSES imported from proposals.js (not declared locally) — eliminates duplication identified in cross-AI review
- getAgeInStageDays not exported from proposals.js, so inlined in renderInlineProposalCard with identical logic
- getDocs (one-time read) used instead of onSnapshot — no listener added to service detail
- Error log prefix [ServiceDetail] to distinguish from [ProjectDetail] errors in production

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. Card fetches real data from Firestore `proposals` collection.

## Threat Surface Scan

No new network endpoints introduced. T-87.1-05-01 mitigated: Submit button only renders for `draft`/`for_revision` statuses. T-87.1-05-02 accepted: parent_collection used for query scope only; actual write target comes from proposal.parent_collection field set at creation time (Plan 01 fix). Double-submit guard via confirmBtn.disabled applied.

## Self-Check: PASSED

- `grep -c "loadProposalCard" app/views/service-detail.js` → 3 (function def + call site in renderServiceDetail + call within loadProposalCard body)
- `grep "loadProposalCard.*'services'" app/views/service-detail.js` → line 511
- `grep "delete window.openProposalInlineSubmitModal" app/views/service-detail.js` → line 226
- `grep "import.*_applyProposalStateTransition.*PROPOSAL_RANGE_STATUSES" app/views/service-detail.js` → line 12
- `grep -c "const PROPOSAL_RANGE_STATUSES" app/views/service-detail.js` → 0 results
- `grep "ServiceDetail.*loadProposalCard failed" app/views/service-detail.js` → line 529

---
*Phase: 87.1*
*Completed: 2026-05-19*
