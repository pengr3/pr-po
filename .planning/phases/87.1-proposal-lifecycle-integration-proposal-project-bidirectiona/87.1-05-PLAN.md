---
phase: 87.1
plan: 05
type: execute
wave: 3
depends_on:
  - 87.1-01
files_modified:
  - app/views/service-detail.js
autonomous: true
requirements:
  - PROP-INT-01
must_haves:
  truths:
    - "When a service's project_status is in the proposal range, an Active Proposal card appears inline in the service detail view below Card 3 (Status & Assignment)"
    - "The card shows: proposal ID link, proposal title, formatted amount, age badge, and either a Submit for Approval button (draft/for_revision) or View Proposal link (all other statuses)"
    - "Clicking Submit for Approval opens a mini-modal; clicking Confirm Submission calls _applyProposalStateTransition with newStatus 'pending_internal' and newProjectStatus 'Proposal for Internal Approval'"
    - "loadProposalCard passes 'services' as the parentCollection parameter (not 'projects')"
    - "Window functions openProposalInlineSubmitModal, closeProposalInlineSubmitModal, confirmProposalInlineSubmit are deleted in destroy()"
  artifacts:
    - path: "app/views/service-detail.js"
      provides: "Inline proposal card in service detail"
      contains: "loadProposalCard"
  key_links:
    - from: "renderServiceDetail()"
      to: "loadProposalCard()"
      via: "called after container.innerHTML set, when isInProposalRange is true"
      pattern: "loadProposalCard.*services"
    - from: "confirmProposalInlineSubmit()"
      to: "_applyProposalStateTransition() imported from proposals.js"
      via: "named import at top of file"
      pattern: "_applyProposalStateTransition"
---

<objective>
Add the inline proposal card to service-detail.js. This is the services-side mirror of Plan 04. The implementation is structurally identical to project-detail.js but uses `currentService`, `currentServiceDocId`, and passes `'services'` to loadProposalCard() so the state transition updates the services collection.

Purpose: D-01 covers both project and service detail views. Plan 04 covered projects; this plan covers services. The parent_collection fix from Plan 01 ensures the writeBatch in _applyProposalStateTransition writes to `services` when called from here.
Output: service-detail.js with the same inline card, modal, and destroy cleanup as project-detail.js.
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-PATTERNS.md
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-UI-SPEC.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-01-SUMMARY.md
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add imports and PROPOSAL_RANGE_STATUSES to service-detail.js</name>
  <files>app/views/service-detail.js</files>
  <read_first>
    - app/views/service-detail.js (lines 1-40 for current imports and UNIFIED_STATUS_OPTIONS definition)
  </read_first>
  <action>
    Two additions to the top section of service-detail.js, identical in purpose to Plan 04 Task 1:

    ADDITION 1 — Import proposals.js exports (add as a new import line after the existing import block):
    `import { _applyProposalStateTransition, getProposalStatusBadge, renderAgeBadge } from './proposals.js';`

    ADDITION 2 — Add PROPOSAL_RANGE_STATUSES constant immediately after the UNIFIED_STATUS_OPTIONS const block:
    ```
    const PROPOSAL_RANGE_STATUSES = [
        'For Proposal',
        'Proposal for Internal Approval',
        'Proposal Under Client Review',
        'For Revision',
        'Client Approved'
    ];
    ```

    Do not modify the existing firebase.js import line or any other existing import.
  </action>
  <verify>
    <automated>grep -n "PROPOSAL_RANGE_STATUSES\|_applyProposalStateTransition.*proposals" /c/Users/Admin/Roaming/pr-po/app/views/service-detail.js</automated>
  </verify>
  <done>
    service-detail.js contains an import of `_applyProposalStateTransition` from './proposals.js' and a `const PROPOSAL_RANGE_STATUSES` array with the five proposal-range status strings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add inline card rendering and modal functions; wire into renderServiceDetail() and destroy()</name>
  <files>app/views/service-detail.js</files>
  <read_first>
    - app/views/service-detail.js (lines 268-496 for renderServiceDetail() — specifically lines 459-483 where Card 3 (Status & Assignment) closes; lines that contain the destroy() function for the window function delete block)
    - app/views/project-detail.js (the loadProposalCard, renderInlineProposalCard, openProposalInlineSubmitModal, confirmProposalInlineSubmit implementations just added in Plan 04 — use these as the direct pattern)
  </read_first>
  <action>
    Apply the same six additions as Plan 04 Task 2, adapted for service-detail.js. The differences from the project-detail.js version are:

    DIFFERENCE 1 — Data variable: use `currentService` instead of `currentProject`, and `currentServiceDocId` instead of `currentProject.id`. The `currentService.project_status` field is the same name as in projects.

    DIFFERENCE 2 — loadProposalCard call: pass `'services'` as the parentCollection argument instead of `'projects'`:
    `loadProposalCard(currentServiceDocId, 'services');`

    DIFFERENCE 3 — Injection point in renderServiceDetail(): after Card 3's closing `</div>` (at approximately line 482, just before the template literal closes the main `.container` div). In project-detail.js the proposal card went before `${planCardHtml}` — service-detail.js has no planCardHtml equivalent, so the card goes at the end of the container before the final `</div>`.

    DIFFERENCE 4 — The `isInProposalRange` check uses `currentService.project_status` (same field name as projects, confirmed in service-detail.js UNIFIED_STATUS_OPTIONS and schema CLAUDE.md).

    All other implementation details (loadProposalCard logic, renderInlineProposalCard HTML structure, modal copy per UI-SPEC, button IDs, window function names, window function registration, destroy cleanup) are identical to project-detail.js. Use the Plan 04 SUMMARY as the implementation reference.

    The window function names are the same as in project-detail.js (`openProposalInlineSubmitModal`, `closeProposalInlineSubmitModal`, `confirmProposalInlineSubmit`). This is correct — project-detail.js and service-detail.js are never active simultaneously; the router destroys one view before loading another.
  </action>
  <verify>
    <automated>grep -n "loadProposalCard\|renderInlineProposalCard\|openProposalInlineSubmitModal\|confirmProposalInlineSubmit\|PROPOSAL_RANGE_STATUSES" /c/Users/Admin/Roaming/pr-po/app/views/service-detail.js</automated>
  </verify>
  <done>
    service-detail.js contains: loadProposalCard (function definition), renderInlineProposalCard (function definition), openProposalInlineSubmitModal (function definition), confirmProposalInlineSubmit (function definition), PROPOSAL_RANGE_STATUSES.includes() call in renderServiceDetail(), and three destroy() deletes for the window functions. The loadProposalCard call in renderServiceDetail() passes `'services'` as the second argument.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service-detail.js UI → confirmProposalInlineSubmit → _applyProposalStateTransition → Firestore services collection | User-triggered proposal submission writes to the services collection via the parent_collection-aware batch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.1-05-01 | Elevation of Privilege | Submit for Approval from service detail | mitigate | Same button-visibility guard as Plan 04: only renders when proposal.status is 'draft' or 'for_revision'. Firestore security rules on proposals collection enforce write = super_admin + operations_admin. |
| T-87.1-05-02 | Tampering | parent_collection write target | accept | Plan 01 fixed _applyProposalStateTransition to use proposal.parent_collection — value is 'services' for service-linked proposals (set at creation time). The loadProposalCard call from service-detail passes 'services' as parentCollection but this is only used for the getDocs query scope; the actual collection written to comes from the proposal doc itself. |
</threat_model>

<verification>
1. `grep -c "loadProposalCard" /c/Users/Admin/Roaming/pr-po/app/views/service-detail.js` returns >= 2 (definition + call site).
2. `grep "services" /c/Users/Admin/Roaming/pr-po/app/views/service-detail.js | grep loadProposalCard` returns the call with 'services' argument.
3. `grep "delete window.openProposalInlineSubmitModal" /c/Users/Admin/Roaming/pr-po/app/views/service-detail.js` returns a result.
4. `grep "import.*_applyProposalStateTransition" /c/Users/Admin/Roaming/pr-po/app/views/service-detail.js` returns a result.
</verification>

<success_criteria>
- Service detail view renders an inline proposal card when the service's project_status is in the proposal range
- loadProposalCard is called with 'services' as the parentCollection parameter
- Submit for Approval modal and confirmation work identically to the project-detail version
- destroy() cleans up all three window functions
- No additional onSnapshot listeners added to service-detail.js
</success_criteria>

<output>
After completion, create `.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-05-SUMMARY.md` with the functions added and their line numbers.
</output>
