---
phase: 87.1
plan: "01"
subsystem: proposals
tags:
  - export
  - refactor
  - bug-fix
  - css
dependency_graph:
  requires: []
  provides:
    - "proposals.js exported constants (PROPOSAL_RANGE_STATUSES, STAGE_ORDER)"
    - "proposals.js exported functions (getProposalStatusBadge, renderAgeBadge, renderStageGroupCard, _applyProposalStateTransition)"
    - "proposals.js exported personnel pill wrappers (5 functions)"
    - "styles/components.css .proposal-inline-card block"
  affects:
    - "app/views/project-detail.js (Wave 3 — imports _applyProposalStateTransition, getProposalStatusBadge, renderAgeBadge)"
    - "app/views/service-detail.js (Wave 3)"
    - "app/views/engagements.js (Wave 2 — imports pill wrapper functions)"
    - "app/views/home.js (Wave 4 — imports STAGE_ORDER, renderAgeBadge)"
tech_stack:
  added: []
  patterns:
    - "module-level exported constants (PROPOSAL_RANGE_STATUSES, STAGE_ORDER) — prevents downstream duplication"
    - "exported wrapper functions with state-as-parameter — engagements.js imports instead of copying"
    - "data-collection attribute on dropdown options — signals Firestore collection to saveProposal()"
key_files:
  created: []
  modified:
    - path: "app/views/proposals.js"
      changes:
        - "Added export const PROPOSAL_RANGE_STATUSES (line 44)"
        - "Added export const STAGE_ORDER extracted from renderProposalsDashboard (line 46)"
        - "Exported getProposalStatusBadge (line 745)"
        - "Exported renderAgeBadge (line 779)"
        - "Exported renderStageGroupCard (line 854)"
        - "Exported _applyProposalStateTransition (line 1536)"
        - "Added 5 new exported pill wrapper functions (lines 300-393)"
        - "Fixed D-02 bug: parentCollection from proposal.parent_collection || 'projects' (line 1572)"
        - "Added data-collection='projects' to #proposalCreateProject options (line 1295)"
        - "Added parent_collection field to saveProposal() docPayload (line 1458)"
    - path: "styles/components.css"
      changes:
        - "Appended .proposal-inline-card block with 3 selectors (lines 1931-1952)"
decisions:
  - "Wrapper functions use distinct names (renderProposalPillsFor, etc.) so engagements.js can import without name collision with the private module helpers"
  - "STAGE_ORDER moved to module level (not duplicated) — renderProposalsDashboard continues to use it via module scope"
  - "parent_collection defaults to 'projects' in both saveProposal() and _applyProposalStateTransition for backwards compatibility with proposals lacking the field"
metrics:
  duration: "< 10 minutes"
  completed: "2026-05-19"
  tasks_completed: 3
  files_modified: 2
---

# Phase 87.1 Plan 01: Export Foundation + parent_collection Fix Summary

One-liner: Exported 11 symbols from proposals.js (2 constants, 9 functions) and fixed the D-02 parent_collection bug so service-linked proposals transition the correct Firestore collection.

## Tasks Completed

| Task | Description | Commit | Status |
|------|-------------|--------|--------|
| 1 | Export constants/functions + fix parent_collection transition | 6d9da0d | DONE |
| 2 | Persist parent_collection at proposal creation in saveProposal() | 793e6b7 | DONE |
| 3 | Add .proposal-inline-card CSS block to components.css | b365c72 | DONE |

## Artifacts Produced

### app/views/proposals.js

**New module-level exports (all at top of file, after module state block):**
- Line 44: `export const PROPOSAL_RANGE_STATUSES` — array of 5 proposal-range status strings for use by project-detail.js and service-detail.js
- Lines 46-54: `export const STAGE_ORDER` — extracted from inside `renderProposalsDashboard()` body to module level; array of 5 `{ key, label }` objects for home.js

**Exported existing functions (export keyword added only, no body change):**
- Line 745: `export function getProposalStatusBadge(status)` — for project-detail, service-detail, home
- Line 779: `export function renderAgeBadge(proposal)` — for project-detail, service-detail, home
- Line 854: `export function renderStageGroupCard(label, proposals)` — for home
- Line 1536: `export async function _applyProposalStateTransition({...})` — for project-detail, service-detail

**New exported wrapper functions (state-as-parameter, lines 300-393):**
- `renderProposalPillsFor(selectedPersonnel, containerId, searchInputId)` — line 300
- `proposalFilterPersonnelDropdownFor(searchText, selectedPersonnel, usersData, dropdownId)` — line 334
- `proposalShowPersonnelDropdownFor(selectedPersonnel, usersData, searchInputId, dropdownId)` — line 363
- `proposalSelectPersonnelIn(userId, userName, selectedPersonnel, renderFn, searchInputId, dropdownId)` — line 370
- `proposalRemovePersonnelFrom(userId, selectedPersonnel, renderFn)` — line 386

**D-02 parent_collection fix (line 1572):**
```javascript
const parentCollection = proposal.parent_collection || 'projects';
if (newProjectStatus && proposal.project_id) {
    batch.update(doc(db, parentCollection, proposal.project_id), { ... });
```

**saveProposal() changes (lines 1443-1458):**
- Reads `dataset?.collection` from selected `#proposalCreateProject` option
- Defaults to `'projects'` if absent
- Adds `parent_collection: parentCollection` to docPayload adjacent to `project_id`

**Dropdown option change (line 1295):**
- Added `data-collection="projects"` to each `<option>` in the `#proposalCreateProject` options template

### styles/components.css

**New block appended at end of file (lines 1931-1952):**
```css
/* Phase 87.1 D-01 — Inline proposal card in project/service detail */
.proposal-inline-card { ... }
.proposal-inline-card__header { ... }
.proposal-inline-card__label { ... }
```

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. This plan adds exports and CSS; no data rendering or UI stubs introduced.

## Threat Surface Scan

No new network endpoints, auth paths, or trust boundaries introduced. The `parent_collection` field is derived from a `data-collection` attribute set by application code (not a free-text user input), consistent with T-87.1-01-01 accept disposition.

## Self-Check: PASSED

- `app/views/proposals.js` — modified and committed (6d9da0d, 793e6b7)
- `styles/components.css` — modified and committed (b365c72)
- `grep -n "^export" app/views/proposals.js` returns 14 lines including all required exports
- `grep -c "const STAGE_ORDER" app/views/proposals.js` returns 1 (module-level only)
- `grep -c "proposal-inline-card" styles/components.css` returns 3
- `grep "parent_collection" app/views/proposals.js` shows lines 1443 (comment), 1458 (docPayload), 1572 (transition fix)
