---
phase: 87.1
plan: 07
type: execute
wave: 6
depends_on:
  - 87.1-06
files_modified: []
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08

must_haves:
  truths:
    - "Home page shows Overview | Engagements | Proposals sub-tabs for super_admin (manual confirm)"
    - "Finance user sees home page with no sub-nav (manual confirm)"
    - "Engagements sub-tab: Create Engagement form submits successfully; personnel picker add/remove works (CR-01 regression)"
    - "Proposals sub-tab: stage group cards render; approval queue visible for super_admin; confirm action advances proposal status"
    - "Proposals nav link absent from desktop + mobile nav (D-02)"
    - "Inline proposal card visible in For Proposal project detail with attachment, comms, Submit+View buttons (D-05)"
    - "openProposalModal opens full detail modal from home sub-tab and from project/service detail (D-04)"
    - "parent_collection fix: services engagement proposal transition updates the 'services' collection (D-06)"
  artifacts:
    - path: ".planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-UAT.md"
      provides: "UAT results confirming all D-01 through D-08 decisions"
      contains: "APPROVED"
  key_links:
    - from: "UAT checklist"
      to: "D-01 through D-08 decisions"
      via: "manual verification steps in checkpoint"
      pattern: "APPROVED"
---

<objective>
End-of-phase UAT checkpoint. The executor runs manual verification steps and confirms all design decisions (D-01 through D-08) are working correctly before the phase is closed.

Purpose: Catch any integration issues missed by per-plan grep checks. This is especially important for CR-01 regression (personnel pill window function names) and for the D-06 parent_collection write path.
Output: UAT confirmation from the user; phase marked complete.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-CONTEXT.md
@.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-UAT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 87.1 changes are now deployed:
    - proposals.js is a pure module with all required exports (Plan 01)
    - app/proposal-modal.js created — full proposal detail modal callable from any context (Plan 02)
    - engagement-create.js extended with form exports; proposals.js engagement code removed (Plan 03)
    - home.js has Overview | Engagements | Proposals sub-tabs (Plan 04)
    - project-detail.js and service-detail.js have enriched inline proposal cards (Plan 05)
    - /proposals route retired; Proposals nav link removed (Plan 06)
  </what-built>
  <how-to-verify>
    Run the app: python -m http.server 8000 (or npx http-server)

    **D-01: Home sub-tab navigation**
    1. Log in as super_admin → home page shows 3 tabs: Overview | Engagements | Proposals
    2. Overview tab active by default; stats visible
    3. Click Engagements → Create Engagement form appears
    4. Click Proposals → stage group cards appear (or "No active proposals" state if no proposals exist)
    5. Log in as operations_user → home shows Overview | Proposals (no Engagements tab)
    6. Log in as finance → home shows no sub-nav at all; stats render normally

    **CR-01 regression test (personnel picker)**
    7. As super_admin, click Engagements tab on home
    8. Click in the "Add Personnel" input field → dropdown appears
    9. Type a name fragment → dropdown filters correctly
    10. Click a user → pill appears showing the user name
    11. Click X on pill → pill disappears, user removed
    12. Open browser DevTools console → confirm NO errors mentioning "proposalSelectPersonnelIn", "proposalRemovePersonnelFrom", or any "is not a function" error

    **D-04: Proposal modal from home**
    13. On home Proposals sub-tab, find a proposal row; click View (or proposal ID link)
    14. Full proposal detail modal opens showing audit trail, comms log, attachment section, action buttons
    15. Close modal — modal removed from DOM

    **D-02: Proposals nav retired**
    16. Confirm "Proposals" link is absent from desktop navigation bar
    17. Confirm "Proposals" link is absent from mobile hamburger menu
    18. Type #/proposals in the address bar → does NOT show the old proposals page (may show home or 404)

    **D-05: Inline proposal card**
    19. Navigate to a project whose status is "For Proposal" (or "Proposal for Internal Approval" etc.)
    20. Confirm inline proposal card appears below the Status & Assignment card
    21. Confirm card shows: proposal ID, title, amount, status badge, age badge
    22. Confirm attachment row shows link/filename or "No attachment"
    23. Confirm comms row shows latest comms entry or "No comms yet"
    24. If proposal.status is "draft": "Submit for Approval" button visible → click it → mini-modal opens
    25. Confirm mini-modal has optional textarea and "Confirm Submission" button
    26. Confirm "View Proposal" button opens full modal

    **D-06: parent_collection fix**
    27. Create a service engagement (or find an existing one with a linked proposal in "draft" status)
    28. Submit for Approval from the inline card on the service detail view
    29. Check Firestore: confirm the parent services document (not projects) was updated with the new project_status

    **D-08: Role-filtered proposals on home**
    30. Log in as operations_user → Proposals sub-tab shows only proposals where parent_collection = 'projects' (or null/missing defaulting to projects)
    31. Log in as services_user → Proposals sub-tab shows only proposals where parent_collection = 'services'
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any failures for the executor to investigate</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT session | Human tester has super_admin access to production Firebase — all UAT actions affect live data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.1-07-01 | Tampering | UAT writes to production Firestore | accept | This project has no staging environment; UAT always uses production; test proposals and engagements created during UAT should be cleaned up manually |
</threat_model>

<verification>
Human approval required. All items in how-to-verify must pass before typing "approved".
</verification>

<success_criteria>
User types "approved" after completing all UAT steps. Phase 87.1 is complete.
</success_criteria>

<output>
After completion, create .planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-07-SUMMARY.md
</output>
