---
phase: 87.1
plan: 03
type: execute
wave: 3
depends_on:
  - 87.1-02
files_modified:
  - app/engagement-create.js
  - app/views/proposals.js
autonomous: true
requirements:
  - D-07

must_haves:
  truths:
    - "app/engagement-create.js exports renderEngagementForm(), initEngagementForm(), destroyEngagementForm()"
    - "initEngagementForm registers all 6 engagement window functions under their unsuffixed names"
    - "destroyEngagementForm deletes all 6 window functions and cleans up clients/users listeners"
    - "proposals.js render() no longer contains the new-engagement-section HTML block"
    - "proposals.js init() no longer registers the 6 engagement window functions"
  artifacts:
    - path: "app/engagement-create.js"
      provides: "Engagement form render/init/destroy exports"
      exports: ["createEngagement", "renderEngagementForm", "initEngagementForm", "destroyEngagementForm"]
    - path: "app/views/proposals.js"
      provides: "Pure module without engagement form code"
      contains: "export function render() { return ''"
  key_links:
    - from: "initEngagementForm()"
      to: "window.proposalSelectPersonnel"
      via: "direct assignment (unsuffixed name matches form HTML oninput/onfocus)"
      pattern: "window.proposalSelectPersonnel = proposalSelectPersonnel"
    - from: "destroyEngagementForm()"
      to: "listeners cleanup"
      via: "_formListeners.forEach(unsub => unsub?.())"
      pattern: "_formListeners"
---

<objective>
Move the Create Engagement form — including all private helper functions — from proposals.js into engagement-create.js as a self-contained unit. This is the CR-01 prevention plan: by moving the form HTML AND its private helper functions together, the window function names stay consistent (unsuffixed, matching what the form HTML generates).

Purpose: home.js Wave 4 plan imports renderEngagementForm, initEngagementForm, destroyEngagementForm from engagement-create.js. Those exports must exist before home.js can be written.
Output: engagement-create.js extended to ~350 lines with form exports; proposals.js slimmed of engagement code.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-CONTEXT.md
@.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend engagement-create.js with form render/init/destroy exports</name>
  <files>app/engagement-create.js</files>
  <read_first>
    - app/engagement-create.js — full file (131 lines) — understand current imports, createEngagement export, module structure
    - app/views/proposals.js lines 44-167 — render() function containing the new-engagement-section HTML (lines 52-167 are the section to extract)
    - app/views/proposals.js lines 186-330 — private engagement helper functions: renderProposalPills (line 186), proposalFilterPersonnelDropdown (line 220), proposalShowPersonnelDropdown (line 249), proposalSelectPersonnel (line 256), proposalRemovePersonnel (line 270), handleEngagementTypeChange (line 283), populateClientDropdown (line 324)
    - app/views/proposals.js lines 350-440 — submitNewEngagement function (line 350)
    - app/views/proposals.js lines 491-522 — clients onSnapshot listener setup in init()
    - app/views/proposals.js lines 523-565 — users onSnapshot listener setup in init()
    - app/views/proposals.js lines 27-31 — module state: clientsData, usersData, selectedPersonnel, currentEngagementType
    - app/views/proposals.js lines 448-453 — window function registrations for the 6 engagement functions
  </read_first>
  <action>
    Extend app/engagement-create.js by appending the following (do not remove the existing createEngagement export):

    ADDITIONAL IMPORTS: Add onSnapshot, query, where to the firebase.js import. Add showLoading, showToast, syncPersonnelToAssignments, syncServicePersonnelToAssignments, escapeHTML to the utils.js import (these are needed by the form helper functions).

    MODULE STATE (add after existing module code):
    - let clientsData = [];
    - let usersData = [];
    - let selectedPersonnel = [];
    - let currentEngagementType = 'project';
    - let _formListeners = [];

    PRIVATE FUNCTIONS (copy verbatim from proposals.js — do NOT export these):
    - renderProposalPills() — from proposals.js line ~186
    - proposalFilterPersonnelDropdown(searchText) — from proposals.js line ~220
    - proposalShowPersonnelDropdown() — from proposals.js line ~249
    - proposalSelectPersonnel(userId, userName) — from proposals.js line ~256
    - proposalRemovePersonnel(userId, userName) — from proposals.js line ~270
    - handleEngagementTypeChange(type) — from proposals.js line ~283
    - populateClientDropdown() — from proposals.js line ~324
    - submitNewEngagement() — from proposals.js line ~350

    NEW EXPORTED FUNCTIONS:

    export function renderEngagementForm() — returns the HTML string for the engagement form section. This is the body of the <section id="new-engagement-section"> block extracted from proposals.js render() (lines 52-167). Return just the inner HTML content wrapped in a <div> or return the section element — the caller (home.js) will inject it into #homeEngagementsContent.

    export async function initEngagementForm() — call destroyEngagementForm() first (reset guard). Then:
    - Register the 6 window functions under their UNSUFFIXED names (CR-01 avoidance — these names must match exactly what the form HTML calls):
      window.submitNewEngagement = submitNewEngagement;
      window.handleEngagementTypeChange = handleEngagementTypeChange;
      window.proposalSelectPersonnel = proposalSelectPersonnel;
      window.proposalRemovePersonnel = proposalRemovePersonnel;
      window.proposalShowPersonnelDropdown = proposalShowPersonnelDropdown;
      window.proposalFilterPersonnelDropdown = proposalFilterPersonnelDropdown;
    - Start the clients onSnapshot listener (copy from proposals.js init() lines ~491-522). Push to _formListeners.
    - Start the users onSnapshot listener (copy from proposals.js init() lines ~523-565). Push to _formListeners.

    export function destroyEngagementForm() — must be idempotent (safe to call multiple times):
    - _formListeners.forEach(unsub => unsub?.()); _formListeners = [];
    - delete window.submitNewEngagement;
    - delete window.handleEngagementTypeChange;
    - delete window.proposalSelectPersonnel;
    - delete window.proposalRemovePersonnel;
    - delete window.proposalShowPersonnelDropdown;
    - delete window.proposalFilterPersonnelDropdown;
    - Reset module state: clientsData = []; usersData = []; selectedPersonnel = []; currentEngagementType = 'project';
  </action>
  <verify>
    <automated>grep -n "export function renderEngagementForm\|export async function initEngagementForm\|export function destroyEngagementForm\|window.proposalSelectPersonnel\|window.proposalRemovePersonnel\|window.submitNewEngagement" app/engagement-create.js</automated>
  </verify>
  <acceptance_criteria>
    - "grep 'export function renderEngagementForm' app/engagement-create.js" returns 1 match
    - "grep 'export async function initEngagementForm' app/engagement-create.js" returns 1 match
    - "grep 'export function destroyEngagementForm' app/engagement-create.js" returns 1 match
    - "grep 'window.proposalSelectPersonnel = proposalSelectPersonnel' app/engagement-create.js" returns 1 match (confirms unsuffixed registration)
    - "grep 'proposalSelectPersonnelIn\|proposalRemovePersonnelFrom' app/engagement-create.js" returns 0 matches (confirms no suffixed names — CR-01 check)
    - "grep '_formListeners' app/engagement-create.js" returns at least 3 matches (declaration + push + cleanup)
    - The existing createEngagement export is still present: "grep 'export.*createEngagement' app/engagement-create.js" returns 1 match
  </acceptance_criteria>
  <done>engagement-create.js exports renderEngagementForm, initEngagementForm, destroyEngagementForm with unsuffixed window function names and listener cleanup</done>
</task>

<task type="auto">
  <name>Task 2: Remove engagement form code from proposals.js</name>
  <files>app/views/proposals.js</files>
  <read_first>
    - app/views/proposals.js lines 44-180 — render() function; the new-engagement-section block to remove is lines ~52-167
    - app/views/proposals.js lines 186-450 — private engagement functions to remove (renderProposalPills through submitNewEngagement) and init() engagement window function registrations (lines 448-453)
    - app/views/proposals.js lines 491-565 — clients and users onSnapshot listener setup in init() to remove
    - app/views/proposals.js lines 570-590 — destroy() engagement cleanup block to remove (window function deletes + state resets for clientsData, usersData, selectedPersonnel, currentEngagementType)
    - app/views/proposals.js lines 27-31 — module state declarations to remove (clientsData, usersData, selectedPersonnel, currentEngagementType)
  </read_first>
  <action>
    Remove the following from proposals.js — in order to avoid accidentally breaking nearby code, make each deletion a targeted edit:

    1. render() function: Delete the entire <section id="new-engagement-section">...</section> block (lines ~52-167). The remaining render() should return only the proposal queue mount and proposal dashboard mount divs plus any heading. Replace render() body with: export function render() { return ''; } — a no-op stub (proposals.js is becoming a pure module; its render is no longer used as a routed view).

    2. Module state: Remove the let declarations for clientsData, usersData, selectedPersonnel, currentEngagementType (lines ~27-31). Keep proposalsData and projectsData.

    3. Private engagement functions: Delete all 8 of these function bodies entirely: renderProposalPills, proposalFilterPersonnelDropdown, proposalShowPersonnelDropdown, proposalSelectPersonnel, proposalRemovePersonnel, handleEngagementTypeChange, populateClientDropdown, submitNewEngagement (lines ~186-440).

    4. init() engagement registrations: Delete the 6 window function assignment lines (lines ~448-453): window.submitNewEngagement, window.handleEngagementTypeChange, window.proposalSelectPersonnel, window.proposalRemovePersonnel, window.proposalShowPersonnelDropdown, window.proposalFilterPersonnelDropdown.

    5. init() listener setup: Delete the clients onSnapshot block and users onSnapshot block (lines ~491-565) that populated clientsData and usersData.

    6. destroy() cleanup: Delete the 6 window function delete lines for the engagement functions (lines ~575-580) and the module state resets for clientsData, usersData, selectedPersonnel, currentEngagementType (lines ~583-586).

    Replace render() with a minimal stub since proposals.js is becoming a pure module (its routed view is retired in Plan 06):
    export function render() { return ''; }

    Do NOT stub init() or destroy() to empty. After deletions 1–5 above, init() and destroy() remain functional but are now responsible only for the proposal-side lifecycle (no longer for engagement). Specifically:
    - init() RETAINS: the proposalsData onSnapshot listener, the projectsData onSnapshot listener, and the Phase 87 / Phase 89 modal + approval-queue window function registrations (everything in init() that is NOT in the engagement set deleted in steps 1–5).
    - destroy() RETAINS: the unsubscribe calls for those two listeners, the window function deletions for the Phase 87 / Phase 89 modal + queue functions, and the module-state resets for proposalsData / projectsData (everything in destroy() that is NOT in the engagement-cleanup block deleted in step 6 above).

    Rationale: Plan 06 retires the /proposals route, but it does NOT delete proposals.js — Plans 04, 05, and 06 still import functions from this module. Stubbing init/destroy here would destroy the proposalsData onSnapshot that downstream callers may rely on transitively. Keep them lean but functional.
  </action>
  <verify>
    <automated>grep -c "new-engagement-section\|submitNewEngagement\|proposalSelectPersonnel\|proposalRemovePersonnel\|proposalShowPersonnelDropdown\|proposalFilterPersonnelDropdown\|handleEngagementTypeChange\|populateClientDropdown\|renderProposalPills" app/views/proposals.js</automated>
  </verify>
  <acceptance_criteria>
    - "grep -c 'new-engagement-section' app/views/proposals.js" returns 0 (section HTML removed)
    - "grep -c 'function submitNewEngagement\|function proposalSelectPersonnel\|function proposalRemovePersonnel\|function renderProposalPills\|function handleEngagementTypeChange\|function populateClientDropdown' app/views/proposals.js" returns 0 (all 8 private functions removed)
    - "grep 'export function render\|export async function init\|export async function destroy' app/views/proposals.js" returns 3 matches (stubs present)
    - "grep 'export function renderApprovalQueue\|export async function _applyProposalStateTransition' app/views/proposals.js" returns 2 matches (proposal exports from Plan 01 still intact)
    - Manual: navigate to #/proposals — tab still renders approval queue and dashboard without errors (engagement section gone is expected)
  </acceptance_criteria>
  <done>proposals.js engagement form code fully removed; render/init/destroy replaced with stubs; proposal functionality intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client DOM | renderEngagementForm() injects HTML with form inputs; submitNewEngagement() writes to Firestore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.1-03-01 | Tampering | submitNewEngagement Firestore write | accept | Firestore Security Rules (Phase 87 D-10) gate who can create engagement/proposal documents; role check is server-side |
| T-87.1-03-02 | Tampering | XSS in engagement form inputs | mitigate | escapeHTML() applied to user-supplied strings when rendering pills and dropdown items — carry forward verbatim from proposals.js |
</threat_model>

<verification>
Manual: Navigate to home page (after Wave 4 wires the Engagements sub-tab). Confirm the Create Engagement form renders correctly. Confirm the personnel picker: click the personnel input, type a name, select from dropdown — pill appears. Click remove pill — pill disappears. No console errors mentioning proposalSelectPersonnelIn or any suffixed name variant.
</verification>

<success_criteria>
engagement-create.js exports 3 new functions. proposals.js no longer contains engagement form code. All CR-01 checks pass (no suffixed names in engagement-create.js).
</success_criteria>

<output>
After completion, create .planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-03-SUMMARY.md
</output>
