---
phase: 87.1
plan: 03
type: execute
wave: 2
depends_on:
  - 87.1-01
files_modified:
  - app/views/proposals.js
autonomous: true
requirements:
  - PROP-INT-04
must_haves:
  truths:
    - "proposals.js render() no longer contains the <section id='new-engagement-section'> block"
    - "proposals.js render() retains the Proposals h2 heading, #proposal-queue-mount section, and #proposal-dashboard-mount section"
    - "proposals.js destroy() no longer deletes the six engagement window functions (submitNewEngagement, handleEngagementTypeChange, proposalSelectPersonnel, proposalRemovePersonnel, proposalShowPersonnelDropdown, proposalFilterPersonnelDropdown)"
    - "proposals.js init() no longer registers the six engagement window functions (window.submitNewEngagement, window.handleEngagementTypeChange, window.proposalSelectPersonnel, window.proposalRemovePersonnel, window.proposalShowPersonnelDropdown, window.proposalFilterPersonnelDropdown)"
    - "proposals.js module-level state variables for engagement (clientsData, usersData, selectedPersonnel, currentEngagementType) remain in the file until manually cleaned, OR are removed — either is acceptable as long as they are not referenced from any function that is still called"
  artifacts:
    - path: "app/views/proposals.js"
      provides: "Proposals tab refocused to queue + dashboard only"
      contains: "proposal-queue-mount"
  key_links:
    - from: "proposals.js render()"
      to: "#proposal-queue-mount and #proposal-dashboard-mount"
      via: "section elements in the returned HTML string"
      pattern: "proposal-queue-mount"
---

<objective>
Remove the Create Engagement section from proposals.js render(), remove its window function registrations from init(), and remove its window function cleanup from destroy(). The Proposals tab becomes a focused Approval Queue + Dashboard view. This is the D-04 refocus.

Purpose: The engagement form now lives in engagements.js (Plan 02). Keeping it in proposals.js would mean the form renders twice — once in each tab — and init() would register window functions that destroy() (after this plan) no longer cleans up, causing window function leaks on tab navigation.
Output: proposals.js with the engagement section stripped from render(), the six engagement window function registrations stripped from init(), and the engagement window function deletes stripped from destroy().
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-PATTERNS.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove engagement section from render(), window registrations from init(), window deletes from destroy()</name>
  <files>app/views/proposals.js</files>
  <read_first>
    - app/views/proposals.js (lines 44-180 for full render() body — locate the <section id="new-engagement-section"> open and close tags; lines 440-560 for init() — locate the six engagement window function registration lines window.submitNewEngagement = ..., window.handleEngagementTypeChange = ..., window.proposalSelectPersonnel = ..., window.proposalRemovePersonnel = ..., window.proposalShowPersonnelDropdown = ..., window.proposalFilterPersonnelDropdown = ...; lines 570-623 for destroy() — locate the six engagement window function delete calls and the engagement module-state resets)
  </read_first>
  <action>
    Three distinct edits to proposals.js:

    EDIT 1 — render() cleanup (per D-04):
    Delete the entire `<section id="new-engagement-section">` block from render(). This block starts at the comment `<!-- New Engagement Form — Phase 88 D-03 -->` and ends at `</section>` closing the engagement section (approximately lines 51-167 inside render()). The retained render() output should contain:
    - The outer `.container` div with the `<h2>Proposals</h2>` heading
    - `<section id="proposal-queue-mount" ...>` (Phase 89 mount point)
    - `<section id="proposal-dashboard-mount" ...>` (Phase 87 mount point)

    Do NOT remove the h2 heading, the queue-mount section, or the dashboard-mount section.

    EDIT 2 — destroy() cleanup:
    Remove the six `delete window.` calls for engagement functions from destroy(). The lines to remove are:
    - `delete window.submitNewEngagement;`
    - `delete window.handleEngagementTypeChange;`
    - `delete window.proposalSelectPersonnel;`
    - `delete window.proposalRemovePersonnel;`
    - `delete window.proposalShowPersonnelDropdown;`
    - `delete window.proposalFilterPersonnelDropdown;`

    Also remove the four module-state reset lines that follow immediately after those deletes:
    - `clientsData = [];`
    - `usersData = [];`
    - `selectedPersonnel = [];`
    - `currentEngagementType = 'project';`

    These state variables can also be removed from the module-level declarations (lines 27-31) if desired, but this is optional — removing their usage from destroy() is the mandatory part.

    Do NOT touch any Phase 87, Phase 89, or proposal-specific window function deletes. Retain all `delete window.openProposalDetail;` and subsequent lines. The `// Remove window functions attached in init().` comment can stay or be removed.

    EDIT 3 — init() cleanup:
    After Plan 01 runs, proposals.js init() will contain wrapper closure registrations for the six engagement window functions. Remove all six of these `window.X = ...` assignment lines from init(). The lines to remove are (exact names, regardless of whether they are simple assignments or wrapper closures after Plan 01):
    - `window.submitNewEngagement = ...` (the assignment, including any trailing semicolon)
    - `window.handleEngagementTypeChange = ...`
    - `window.proposalSelectPersonnel = ...` (this may be a wrapper closure after Plan 01 — remove the entire closure assignment)
    - `window.proposalRemovePersonnel = ...`
    - `window.proposalShowPersonnelDropdown = ...`
    - `window.proposalFilterPersonnelDropdown = ...`

    These six are at approximately lines 448-453 in the original file (after Plan 01 modifies them to wrapper closures, the line numbers may shift slightly but they remain consecutive). Remove the entire assignment statement for each, including any multi-line closure body if Plan 01 expanded them to arrow function closures.

    Do NOT remove any other window function registrations in init() — specifically retain all `window.openProposalDetail = ...`, `window.submitProposalApproval = ...`, and any other Phase 87/89 window function registrations.
  </action>
  <verify>
    <automated>grep -n "new-engagement-section\|window\.submitNewEngagement\s*=\|window\.handleEngagementTypeChange\s*=\|window\.proposalSelectPersonnel\s*=\|window\.proposalRemovePersonnel\s*=\|window\.proposalShowPersonnelDropdown\s*=\|window\.proposalFilterPersonnelDropdown\s*=\|delete window\.submitNewEngagement\|delete window\.handleEngagementTypeChange\|delete window\.proposalSelectPersonnel\|delete window\.proposalRemovePersonnel\|delete window\.proposalShowPersonnelDropdown\|delete window\.proposalFilterPersonnelDropdown" /c/Users/Admin/Roaming/pr-po/app/views/proposals.js</automated>
  </verify>
  <done>
    The grep above returns zero lines. proposals.js contains:
    - No `window.proposalSelectPersonnel =` assignment (not registered in init).
    - No `delete window.proposalSelectPersonnel` call (not deleted in destroy).
    - No `window.submitNewEngagement =` assignment.
    - No `delete window.submitNewEngagement` call.
    - No `window.handleEngagementTypeChange =`, `window.proposalRemovePersonnel =`, `window.proposalShowPersonnelDropdown =`, or `window.proposalFilterPersonnelDropdown =` assignments.
    - No corresponding `delete window.*` calls for the same six names.
    - No `new-engagement-section` string.

    Note: the function names `proposalSelectPersonnel`, `proposalRemovePersonnel`, etc. may still appear in export lines (from Plan 01 Task 1 Change 4, the new wrapper functions have different names — proposalSelectPersonnelIn, etc. — so export lines will not match these patterns). The criterion checks only for window assignments (with `=`) and window deletes — not bare function name occurrences.

    The #proposal-queue-mount and #proposal-dashboard-mount sections remain present in render(). All Phase 87 and Phase 89 window function registrations and cleanup (openProposalDetail, submitProposalApproval, etc.) remain intact in init() and destroy().
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proposals.js (reduced surface) | Removing the engagement section reduces the attack surface — fewer form inputs, fewer window functions exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.1-03-01 | Denial of Service | proposals.js destroy() removing wrong functions | mitigate | Read the full destroy() block before editing; only remove the six engagement-specific lines listed; verify Phase 87/89 cleanup lines remain intact after the edit. |
| T-87.1-03-02 | Denial of Service | proposals.js init() removing wrong window registrations | mitigate | Read the full init() block before editing; only remove the six engagement window.X = assignments; verify all Phase 87/89 window registrations (openProposalDetail, etc.) remain after the edit. |
</threat_model>

<verification>
1. `grep -c "new-engagement-section" /c/Users/Admin/Roaming/pr-po/app/views/proposals.js` returns 0.
2. `grep -c "submitNewEngagement" /c/Users/Admin/Roaming/pr-po/app/views/proposals.js` returns 0.
3. `grep -cn "window\.proposalSelectPersonnel\s*=" /c/Users/Admin/Roaming/pr-po/app/views/proposals.js` returns 0 (no assignment in init()).
4. `grep -cn "delete window\.proposalSelectPersonnel" /c/Users/Admin/Roaming/pr-po/app/views/proposals.js` returns 0 (no delete in destroy()).
5. `grep "proposal-queue-mount" /c/Users/Admin/Roaming/pr-po/app/views/proposals.js` returns a result (section still present).
6. `grep "proposal-dashboard-mount" /c/Users/Admin/Roaming/pr-po/app/views/proposals.js` returns a result (section still present).
7. `grep "openProposalDetail" /c/Users/Admin/Roaming/pr-po/app/views/proposals.js` returns results (Phase 87 cleanup intact).
</verification>

<success_criteria>
- proposals.js render() outputs only the Proposals heading + #proposal-queue-mount + #proposal-dashboard-mount — the Create Engagement form is gone
- proposals.js init() does not register any of the six engagement window functions (no window.X = assignments for submitNewEngagement, handleEngagementTypeChange, proposalSelectPersonnel, proposalRemovePersonnel, proposalShowPersonnelDropdown, proposalFilterPersonnelDropdown)
- proposals.js destroy() no longer deletes the six engagement window functions
- No window function leak: functions that are not registered in init() are not attempted to be deleted in destroy() and vice versa
- All Phase 87 and Phase 89 window function registration and cleanup in init() and destroy() is intact and unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-03-SUMMARY.md` confirming the three edits were made and listing the line count change.
</output>
