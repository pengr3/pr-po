---
phase: 87.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/proposals.js
  - app/utils.js
autonomous: true
requirements:
  - D-03
  - D-06

must_haves:
  truths:
    - "proposals.js exports getProposalStatusBadge, renderAgeBadge, getAgeInStageDays, isOverdueInStage, renderStageGroupCard, renderApprovalQueue, _applyProposalStateTransition"
    - "STAGE_ORDER is a module-level export in proposals.js (not inside renderProposalDashboard)"
    - "PROPOSAL_RANGE_STATUSES is a new module-level export in proposals.js"
    - "_applyProposalStateTransition reads proposal.parent_collection to choose the target Firestore collection"
    - "cryptoRandomUuid is exported from app/utils.js"
  artifacts:
    - path: "app/views/proposals.js"
      provides: "Pure module foundation with all required exports"
      contains: "export const STAGE_ORDER"
    - path: "app/utils.js"
      provides: "Shared UUID utility"
      exports: ["cryptoRandomUuid"]
  key_links:
    - from: "app/views/proposals.js STAGE_ORDER"
      to: "renderProposalDashboard()"
      via: "module-level const referenced by function body"
      pattern: "export const STAGE_ORDER"
    - from: "_applyProposalStateTransition"
      to: "Firestore batch.update"
      via: "parentCollection = proposal.parent_collection || 'projects'"
      pattern: "parentCollection"
---

<objective>
Lay the module foundation for all downstream plans. proposals.js gains 9 new exports (7 functions + 2 constants) and the D-06 parent_collection fix. cryptoRandomUuid moves to utils.js so proposal-modal.js can import it without a circular dependency.

Purpose: Every other plan in this phase imports from proposals.js or utils.js. This plan makes those imports possible without breaking the existing Proposals tab (which still renders until Wave 6 retires it).
Output: proposals.js with 9 new exports and D-06 fix; utils.js with exported cryptoRandomUuid.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-CONTEXT.md
@.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export cryptoRandomUuid from utils.js</name>
  <files>app/utils.js, app/views/proposals.js</files>
  <read_first>
    - app/utils.js — full file read to find existing exports and confirm cryptoRandomUuid is absent
    - app/views/proposals.js lines 1380-1395 — exact cryptoRandomUuid function body (research cites line 1385)
    - app/views/proposals.js lines 1-25 — top import block to see the current utils.js import line
  </read_first>
  <action>
    Step 1 — app/utils.js: Append a new exported function at the bottom of the file. Name: cryptoRandomUuid. Body: return ([1e7]+-1e3+-4e3+-8e3+-1e11).replace(/[018]/g, c => (c ^ crypto.getRandomValues(new Uint8Array(1))[0] & 15 >> c / 4).toString(16)); This is identical to the private copy in proposals.js.

    Step 2 — app/views/proposals.js: Find the existing utils.js import line near the top of the file. Add cryptoRandomUuid to the named import list. Then delete the private function declaration named cryptoRandomUuid from the body of proposals.js (the function body around line 1385). Do not delete any other function. After deletion, references to cryptoRandomUuid inside proposals.js (e.g., in _applyProposalStateTransition) will resolve to the imported version.
  </action>
  <verify>
    <automated>grep -n "export function cryptoRandomUuid\|export.*cryptoRandomUuid" app/utils.js</automated>
  </verify>
  <acceptance_criteria>
    - app/utils.js contains "export function cryptoRandomUuid(" (or export const cryptoRandomUuid =)
    - app/views/proposals.js import line for utils.js includes cryptoRandomUuid in the named imports
    - app/views/proposals.js does NOT contain a standalone private "function cryptoRandomUuid(" declaration (grep for "^function cryptoRandomUuid\|^async function cryptoRandomUuid" returns 0 results)
  </acceptance_criteria>
  <done>cryptoRandomUuid exported from utils.js; proposals.js imports it rather than declaring it privately</done>
</task>

<task type="auto">
  <name>Task 2: Lift STAGE_ORDER, add PROPOSAL_RANGE_STATUSES, and export 7 functions from proposals.js</name>
  <files>app/views/proposals.js</files>
  <read_first>
    - app/views/proposals.js lines 1-45 — top of file (module-level state, imports)
    - app/views/proposals.js lines 680-700 — STAGE_ORDER local const inside renderProposalDashboard()
    - app/views/proposals.js lines 628-640 — getProposalStatusBadge declaration
    - app/views/proposals.js lines 645-670 — getAgeInStageDays, isOverdueInStage, renderAgeBadge declarations
    - app/views/proposals.js lines 748-756 — renderStageGroupCard declaration
    - app/views/proposals.js lines 1425-1435 — _applyProposalStateTransition declaration
    - app/views/proposals.js lines 1460-1472 — the hardcoded 'projects' collection reference (D-06 target)
    - app/views/proposals.js lines 1730-1740 — renderApprovalQueue declaration
  </read_first>
  <action>
    Make the following targeted edits to app/views/proposals.js:

    1. STAGE_ORDER lift: Locate the "const STAGE_ORDER = [...]" declaration inside renderProposalDashboard() (around line 684). Delete it from that function body. Add the following at module top level (after the import block, before the first "let" module-state declaration): export const STAGE_ORDER = [ { key: 'pending_internal', label: 'Pending Internal Approval' }, { key: 'pending_client', label: 'Pending Client Review' }, { key: 'for_revision', label: 'For Revision' }, { key: 'client_approved', label: 'Client Approved' }, { key: 'loss', label: 'Loss' } ]; The body of renderProposalDashboard() continues to reference STAGE_ORDER unchanged — it now resolves to the module-level const.

    2. PROPOSAL_RANGE_STATUSES: Immediately after the STAGE_ORDER export, add: export const PROPOSAL_RANGE_STATUSES = [ 'For Proposal', 'Proposal for Internal Approval', 'Proposal Under Client Review', 'For Revision', 'Client Approved' ];

    3. Add "export" keyword to exactly these 7 function declarations (one edit each):
       - "function getProposalStatusBadge(status)" → "export function getProposalStatusBadge(status)"
       - "function getAgeInStageDays(proposal)" → "export function getAgeInStageDays(proposal)"
       - "function isOverdueInStage(proposal)" → "export function isOverdueInStage(proposal)"
       - "function renderAgeBadge(proposal)" → "export function renderAgeBadge(proposal)"
       - "function renderStageGroupCard(label, proposals)" → "export function renderStageGroupCard(label, proposals)"
       - "async function _applyProposalStateTransition(" → "export async function _applyProposalStateTransition("
       - "function renderApprovalQueue()" → "export function renderApprovalQueue()"

    4. D-06 fix: Inside _applyProposalStateTransition, find the line containing: batch.update(doc(db, 'projects', proposal.project_id) Immediately above it, insert: const parentCollection = proposal.parent_collection || 'projects'; Then change the string literal 'projects' in that batch.update call to the variable parentCollection. Do not change any other part of the function.
  </action>
  <verify>
    <automated>grep -c "export const STAGE_ORDER\|export const PROPOSAL_RANGE_STATUSES\|export function getProposalStatusBadge\|export function renderAgeBadge\|export function renderStageGroupCard\|export async function _applyProposalStateTransition\|export function renderApprovalQueue\|export function getAgeInStageDays\|export function isOverdueInStage\|parentCollection" app/views/proposals.js</automated>
  </verify>
  <acceptance_criteria>
    - "grep -n 'export const STAGE_ORDER' app/views/proposals.js" returns exactly 1 match, and that match is NOT inside a function body (line number is before any function definition in the file)
    - "grep -c 'export const PROPOSAL_RANGE_STATUSES' app/views/proposals.js" returns 1
    - All 7 function exports confirmed by grep (each returns exactly 1 match): getProposalStatusBadge, getAgeInStageDays, isOverdueInStage, renderAgeBadge, renderStageGroupCard, _applyProposalStateTransition, renderApprovalQueue
    - "grep -n 'parentCollection' app/views/proposals.js" returns at least 2 lines (the "const parentCollection = " assignment and its use in batch.update)
    - The existing Proposals tab loads without JavaScript errors in browser DevTools (manual: navigate to #/proposals as super_admin)
  </acceptance_criteria>
  <done>proposals.js has all 9 new exports at module scope; D-06 parent_collection fix applied; existing Proposals tab renders correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Firestore | _applyProposalStateTransition uses parent_collection from a Firestore doc field — a tampered value could target the wrong collection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.1-01-01 | Tampering | _applyProposalStateTransition parent_collection | accept | Field is constrained by || 'projects' default; Firestore Security Rules (Phase 87 D-10) already gate who can write proposal documents — only super_admin and operations_admin can update proposals |
</threat_model>

<verification>
Manual: Navigate to #/proposals as super_admin. Confirm the approval queue and proposal dashboard render without errors. Confirm browser DevTools console has no SyntaxError or "is not defined" errors related to STAGE_ORDER or proposals exports.
</verification>

<success_criteria>
grep confirms 9 new exports in proposals.js. utils.js exports cryptoRandomUuid. Existing Proposals tab behaviour unchanged. D-06 fix visible via grep showing parentCollection in _applyProposalStateTransition.
</success_criteria>

<output>
After completion, create .planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-01-SUMMARY.md
</output>
