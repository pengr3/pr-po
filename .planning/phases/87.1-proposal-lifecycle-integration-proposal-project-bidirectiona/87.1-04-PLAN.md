---
phase: 87.1
plan: 04
type: execute
wave: 3
depends_on:
  - 87.1-01
files_modified:
  - app/views/project-detail.js
autonomous: true
requirements:
  - PROP-INT-01
must_haves:
  truths:
    - "When a project's project_status is in the proposal range, an Active Proposal card appears inline in the project detail view below Card 3 (Status & Assignment)"
    - "The card shows: proposal ID link, proposal title, formatted amount, age badge, and either a Submit for Approval button (draft/for_revision) or View Proposal link (all other statuses)"
    - "Clicking Submit for Approval opens a mini-modal; clicking Confirm Submission calls _applyProposalStateTransition with newStatus 'pending_internal' and newProjectStatus 'Proposal for Internal Approval'"
    - "The card does not add an onSnapshot listener — it uses one getDocs query when the project detail opens"
    - "PROPOSAL_RANGE_STATUSES is imported from proposals.js — not declared locally in this file"
    - "Window functions openProposalInlineSubmitModal, closeProposalInlineSubmitModal, confirmProposalInlineSubmit are deleted in destroy()"
  artifacts:
    - path: "app/views/project-detail.js"
      provides: "Inline proposal card in project detail"
      contains: "loadProposalCard"
  key_links:
    - from: "renderProjectDetail()"
      to: "loadProposalCard()"
      via: "called after container.innerHTML set, when isInProposalRange is true"
      pattern: "loadProposalCard"
    - from: "confirmProposalInlineSubmit()"
      to: "_applyProposalStateTransition() imported from proposals.js"
      via: "named import at top of file"
      pattern: "_applyProposalStateTransition"
---

<objective>
Add the inline proposal card to project-detail.js. When a project's status is in the proposal lifecycle range, the card appears below Card 3 showing the linked proposal's stage, amount, age, and a context-appropriate action. The Submit for Approval action calls the imported _applyProposalStateTransition function from proposals.js.

PROPOSAL_RANGE_STATUSES is imported from proposals.js rather than declared locally — this is the single source of truth for which statuses put a project in the proposal lifecycle range, eliminating the duplication identified in the cross-AI review.

Purpose: D-01 requires project/service detail views to surface the active proposal inline. This plan implements D-01 for projects.
Output: project-detail.js with imported PROPOSAL_RANGE_STATUSES, loadProposalCard() async function, renderInlineProposalCard() helper, openProposalInlineSubmitModal() function, confirmProposalInlineSubmit() async function, and destroy() cleanup.
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-PATTERNS.md
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-UI-SPEC.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add imports to project-detail.js — import PROPOSAL_RANGE_STATUSES from proposals.js</name>
  <files>app/views/project-detail.js</files>
  <read_first>
    - app/views/project-detail.js (lines 1-30 for current imports and UNIFIED_STATUS_OPTIONS definition)
    - .planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-01-SUMMARY.md (confirm the exact export names from Plan 01 before writing the import line)
  </read_first>
  <action>
    One addition to the top section of project-detail.js:

    Import proposals.js exports (add as a new import line after the existing import block, before the module state declarations):
    `import { _applyProposalStateTransition, getProposalStatusBadge, renderAgeBadge, PROPOSAL_RANGE_STATUSES } from './proposals.js';`

    This import includes PROPOSAL_RANGE_STATUSES (addresses review concern: duplication of status list). Do NOT add a local `const PROPOSAL_RANGE_STATUSES` declaration anywhere in project-detail.js — the imported constant is the only definition.

    Do not modify the existing firebase.js import line or any other existing import.
  </action>
  <verify>
    <automated>grep -n "PROPOSAL_RANGE_STATUSES\|_applyProposalStateTransition.*proposals" /c/Users/Admin/Roaming/pr-po/app/views/project-detail.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import.*PROPOSAL_RANGE_STATUSES.*proposals.js" app/views/project-detail.js` returns one result — the import line.
    - `grep "const PROPOSAL_RANGE_STATUSES" app/views/project-detail.js` returns 0 results — no local declaration.
    - `grep "_applyProposalStateTransition" app/views/project-detail.js` shows the import line referencing './proposals.js'.
  </acceptance_criteria>
  <done>
    project-detail.js contains an import line with `_applyProposalStateTransition`, `getProposalStatusBadge`, `renderAgeBadge`, and `PROPOSAL_RANGE_STATUSES` from './proposals.js'. No local `const PROPOSAL_RANGE_STATUSES` declaration exists in this file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add inline card rendering and modal functions; wire into renderProjectDetail() and destroy()</name>
  <files>app/views/project-detail.js</files>
  <read_first>
    - app/views/project-detail.js (lines 339-580 for renderProjectDetail() — specifically lines 537-570 where Card 3 closes and planCardHtml begins; lines 232-290 for destroy() window function delete block)
    - app/views/proposals.js (lines 625-673 for getProposalStatusBadge and renderAgeBadge signatures — needed to understand what they return for the inline card template)
  </read_first>
  <action>
    Four additions to project-detail.js:

    ADDITION 1 — In renderProjectDetail(), after the Card 3 closing `</div>` (which closes the `<div class="card-body">` of Card 3 at line ~558) and before the `${planCardHtml}` line, inject the proposal card placeholder. The injection uses a ternary: if `PROPOSAL_RANGE_STATUSES.includes(currentProject.project_status)`, include `<div id="proposalInlineCard" style="margin-top:1rem;"><div class="proposal-inline-card"><p style="color:#64748b;font-size:0.9rem;">Loading proposal...</p></div></div>`; else empty string.

    After the existing `container.innerHTML = ...` assignment (and after the focus-restore block that follows it), add a call to `loadProposalCard()` guarded by the same isInProposalRange check. Pattern:
    ```
    const isInProposalRange = PROPOSAL_RANGE_STATUSES.includes(currentProject.project_status);
    if (isInProposalRange) {
        loadProposalCard(currentProject.id, 'projects');
    }
    ```
    The `isInProposalRange` constant should be declared once near the top of renderProjectDetail() (after the existing canEdit/user declarations) and reused in both locations.

    ADDITION 2 — New `async function loadProposalCard(parentDocId, parentCollection)` placed after renderProjectDetail(). This function:
    - Runs `getDocs(query(collection(db, 'proposals'), where('project_id', '==', parentDocId)))` — a one-time read, no listener.
    - Gets the element `document.getElementById('proposalInlineCard')`. If the element is null (user navigated away), return early.
    - If the snapshot is empty, sets innerHTML to `<div class="proposal-inline-card"><p style="color:#64748b;">No proposal linked yet.</p></div>`.
    - Otherwise takes `snap.docs[0]`, builds `{ id: snap.docs[0].id, ...snap.docs[0].data() }` as the proposal object, and sets innerHTML to `renderInlineProposalCard(proposal)`.
    - Wraps all Firestore calls in try/catch, logging errors with `[ProjectDetail] loadProposalCard failed for ${parentDocId}:` prefix. Include the project/service ID in the error log to aid production debugging.

    ADDITION 3 — New `function renderInlineProposalCard(proposal)` placed after loadProposalCard(). This function:
    - Checks `const isActionable = ['draft', 'for_revision'].includes(proposal.status)`.
    - Returns an HTML string using `.proposal-inline-card` structure per UI-SPEC:
      - Header div with `.proposal-inline-card__header` containing `.proposal-inline-card__label` span (text "Active Proposal") and `${getProposalStatusBadge(proposal.status)}`.
      - Body div with `.proposal-inline-card__body` containing: proposal ID as a styled span (font-size:13px, font-weight:600, color:#1a73e8), title (font-size:15px, font-weight:600, color:#1e293b), amount formatted as `₱${formatCurrency(proposal.amount)}` or `—` if null (font-size:13px, color:#475569), and `${renderAgeBadge(proposal)}`.
      - Footer div with `.proposal-inline-card__footer` (margin-top:12px): if isActionable, a `<button class="btn btn-primary" onclick="window.openProposalInlineSubmitModal('${escapeHTML(proposal.id)}')">Submit for Approval</button>`; otherwise an `<a href="#/proposals" style="color:#1a73e8;font-size:13px;font-weight:600;text-decoration:none;">View Proposal →</a>`.
    - Applies the overdue left-border style when `renderAgeBadge` would indicate overdue: check if the outer div should carry `style="border-left:3px solid #f59e0b"`. Use the same overdue logic: `['pending_internal','pending_client'].includes(proposal.status) && getAgeInStageDays(proposal) > 7`. Since getAgeInStageDays is not exported from proposals.js, inline the same logic: compute days from `proposal.current_status_since || proposal.created_at` and check `> 7`.

    ADDITION 4 — Three new functions for the mini-modal:
    - `function openProposalInlineSubmitModal(proposalDocId)` — removes any existing `#proposal-inline-submit-modal`, builds the modal HTML and calls `document.body.insertAdjacentHTML('beforeend', html)`. Modal structure: `.modal` (display:flex, z-index:1001) > `.modal-content` (max-width:480px, margin:auto) > `.modal-header` (h2 "Submit for Approval" at font-size:1.125rem + close button calling `window.closeProposalInlineSubmitModal()`) + `.modal-body` (body text per UI-SPEC: "Submitting will advance this project to 'Proposal for Internal Approval'. This action is recorded in the audit trail." + textarea with id `proposalInlineSubmitComment`, label "Submission Notes", placeholder "Describe the proposal and any context for approvers...") + `.modal-footer` (Cancel button → `window.closeProposalInlineSubmitModal()`, Confirm button id `proposalInlineConfirmBtn` → `window.confirmProposalInlineSubmit('${escapeHTML(proposalDocId)}')`). Comment textarea is optional — no minimum character count.
    - `function closeProposalInlineSubmitModal()` — removes `document.getElementById('proposal-inline-submit-modal')`.
    - `async function confirmProposalInlineSubmit(proposalDocId)` — disables `#proposalInlineConfirmBtn` to prevent double-submit; fetches the proposal doc via `getDoc(doc(db, 'proposals', proposalDocId))`; calls `_applyProposalStateTransition({ proposal: { id: proposalDocId, ...docSnap.data() }, newStatus: 'pending_internal', newProjectStatus: 'Proposal for Internal Approval', auditAction: 'SUBMITTED', auditComment: document.getElementById('proposalInlineSubmitComment')?.value?.trim() || null })`; on success: calls `closeProposalInlineSubmitModal()` + `showToast('Proposal submitted for approval.', 'success')`; on error: re-enables the button + `showToast('Failed to submit proposal. Please try again.', 'error')`.

    ADDITION 5 — In `attachWindowFunctions()` (the function called from init()), add registrations for the three new window functions:
    `window.openProposalInlineSubmitModal = openProposalInlineSubmitModal;`
    `window.closeProposalInlineSubmitModal = closeProposalInlineSubmitModal;`
    `window.confirmProposalInlineSubmit = confirmProposalInlineSubmit;`

    ADDITION 6 — In destroy(), add three `delete window.` lines for the three new functions, in the same block as the existing deletes.
  </action>
  <verify>
    <automated>grep -n "loadProposalCard\|renderInlineProposalCard\|openProposalInlineSubmitModal\|confirmProposalInlineSubmit\|PROPOSAL_RANGE_STATUSES" /c/Users/Admin/Roaming/pr-po/app/views/project-detail.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "loadProposalCard" app/views/project-detail.js` returns >= 2 (function definition + call site).
    - `grep "delete window.openProposalInlineSubmitModal" app/views/project-detail.js` returns a result in destroy().
    - `grep "import.*_applyProposalStateTransition" app/views/project-detail.js` returns a result.
    - `grep "PROPOSAL_RANGE_STATUSES" app/views/project-detail.js` returns results showing `.includes(` call but NO `const PROPOSAL_RANGE_STATUSES` declaration.
    - `grep "ProjectDetail.*loadProposalCard failed" app/views/project-detail.js` returns a result in the catch block — confirms the ID-bearing error log is present.
  </acceptance_criteria>
  <done>
    project-detail.js contains: loadProposalCard (function definition), renderInlineProposalCard (function definition), openProposalInlineSubmitModal (function definition + window assignment), confirmProposalInlineSubmit (function definition), PROPOSAL_RANGE_STATUSES.includes() call in renderProjectDetail() but no local PROPOSAL_RANGE_STATUSES declaration, and three `delete window.` lines in destroy() for the three new functions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project-detail.js UI → confirmProposalInlineSubmit → _applyProposalStateTransition → Firestore | User-triggered proposal submission crosses from DOM event into Firestore batch write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.1-04-01 | Elevation of Privilege | Submit for Approval button visibility | mitigate | Button only renders when `['draft','for_revision'].includes(proposal.status)` — server-sourced status. A user cannot click the button for proposals they have no write access to; Firestore security rules (proposals block) enforce write = super_admin + operations_admin. |
| T-87.1-04-02 | Spoofing | proposalDocId in window.openProposalInlineSubmitModal | accept | proposalDocId is the Firestore doc ID embedded in the rendered button; it is Firestore-sourced and escapeHTML'd. If tampered with in DevTools, the getDoc call in confirmProposalInlineSubmit returns notFound and the transition fails with an error toast. |
| T-87.1-04-03 | Denial of Service | double-submit on Confirm button | mitigate | confirmProposalInlineSubmit disables #proposalInlineConfirmBtn immediately on click before the async call; re-enables only on error path. |
</threat_model>

<verification>
1. `grep -c "loadProposalCard" /c/Users/Admin/Roaming/pr-po/app/views/project-detail.js` returns >= 2 (definition + call site).
2. `grep "delete window.openProposalInlineSubmitModal" /c/Users/Admin/Roaming/pr-po/app/views/project-detail.js` returns a result.
3. `grep "import.*_applyProposalStateTransition.*PROPOSAL_RANGE_STATUSES" /c/Users/Admin/Roaming/pr-po/app/views/project-detail.js` returns a result — both are in the same import from proposals.js.
4. `grep "const PROPOSAL_RANGE_STATUSES" /c/Users/Admin/Roaming/pr-po/app/views/project-detail.js` returns 0 results — no local declaration.
5. Firestore index note: The `getDocs(query(collection(db, 'proposals'), where('project_id', '==', parentDocId)))` query uses a single-field filter on `project_id`. Firestore auto-indexes single-field queries. If the collection grows and a `FAILED_PRECONDITION` error appears in the browser console, a composite index is required — follow the Firebase Console link included in the error message to create it.
</verification>

<success_criteria>
- Project detail view renders an inline proposal card when the project's status is in the proposal range
- Card shows correct data from the linked proposal doc (fetched once via getDocs)
- Submit for Approval modal opens, confirms via _applyProposalStateTransition, and shows correct toasts
- destroy() cleans up all three new window functions
- No additional onSnapshot listeners added to project-detail.js
- PROPOSAL_RANGE_STATUSES comes from proposals.js import — no local duplicate
</success_criteria>

<output>
After completion, create `.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-04-SUMMARY.md` with the functions added and their line numbers.
</output>
