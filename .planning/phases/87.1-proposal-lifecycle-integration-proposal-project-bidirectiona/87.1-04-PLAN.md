---
phase: 87.1
plan: 04
type: execute
wave: 4
depends_on:
  - 87.1-02
  - 87.1-03
files_modified:
  - app/views/home.js
  - styles/views.css
autonomous: true
requirements:
  - D-01
  - D-07
  - D-08

must_haves:
  truths:
    - "home.js render() includes the home-sub-nav div and homeEngagementsContent + homeProposalsContent divs"
    - "Finance and procurement_staff users see no sub-nav; all other eligible roles see a sub-nav with their tabs"
    - "Engagements sub-tab content is populated by renderEngagementForm() from engagement-create.js"
    - "Proposals sub-tab fetches proposals via one-time getDocs, filters by role, renders stage group cards + approval queue for approvers"
    - "switchHomeTab() is exposed as window.switchHomeTab and handles overview/engagements/proposals"
    - "home.js destroy() calls destroyEngagementForm() and deletes window.switchHomeTab"
    - "styles/views.css contains .home-sub-nav, .home-sub-nav-tabs, .home-sub-nav-tab, .home-sub-nav-tab--active"
  artifacts:
    - path: "app/views/home.js"
      provides: "Home page with sub-tab navigation"
      contains: "homeSubNav"
    - path: "styles/views.css"
      provides: "Home sub-nav pill tab CSS"
      contains: ".home-sub-nav-tab--active"
  key_links:
    - from: "home.js init()"
      to: "renderEngagementForm() from engagement-create.js"
      via: "document.getElementById('homeEngagementsContent').innerHTML = renderEngagementForm()"
      pattern: "renderEngagementForm"
    - from: "home.js init()"
      to: "getDocs(collection(db, 'proposals'))"
      via: "_loadHomeProposalsTab() one-time fetch"
      pattern: "getDocs.*proposals"
    - from: "home.js destroy()"
      to: "destroyEngagementForm()"
      via: "direct call"
      pattern: "destroyEngagementForm"
---

<objective>
Add three sub-tabs to the home page: Overview (existing stats), Engagements (form from engagement-create.js), and Proposals (approval queue + stage group dashboard). The sub-nav is only rendered for eligible roles; finance/procurement_staff users see the home page unchanged.

Purpose: This is the primary user-facing surface for proposal management after the standalone Proposals tab is retired in Wave 6.
Output: home.js updated with sub-tab logic (~120 new lines); styles/views.css with .home-sub-nav CSS block.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-CONTEXT.md
@.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-RESEARCH.md
@.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add home-sub-nav CSS to styles/views.css</name>
  <files>styles/views.css</files>
  <read_first>
    - styles/views.css lines 1716-1765 — .finance-sub-nav-tab pattern (the analog to replicate)
    - styles/views.css — last 10 lines to find the append point
  </read_first>
  <action>
    Append the following CSS block to the end of styles/views.css:

    /* Phase 87.1 D-01 — Home dashboard sub-nav */
    .home-sub-nav {
        background: #ffffff;
        border-bottom: 1px solid var(--gray-200);
        margin-bottom: 1.5rem;
    }
    .home-sub-nav-tabs {
        display: flex;
        gap: 0.25rem;
        padding: 0.5rem 0;
        overflow-x: auto;
        scrollbar-width: none;
    }
    .home-sub-nav-tabs::-webkit-scrollbar { display: none; }
    .home-sub-nav-tab {
        flex: 0 0 auto;
        padding: 0.5rem 1rem;
        border: none;
        border-radius: 999px;
        background: transparent;
        color: var(--gray-700);
        font-size: 0.9375rem;
        font-weight: 600;
        cursor: pointer;
        white-space: nowrap;
        transition: background 0.15s, color 0.15s;
    }
    .home-sub-nav-tab:hover { background: var(--gray-50); color: var(--gray-900); }
    .home-sub-nav-tab--active { background: var(--primary); color: #ffffff; }
    .home-sub-nav-tab--active:hover { background: var(--primary-dark); color: #ffffff; }
  </action>
  <verify>
    <automated>grep -c "home-sub-nav-tab--active\|home-sub-nav-tab\|home-sub-nav-tabs\|home-sub-nav" styles/views.css</automated>
  </verify>
  <acceptance_criteria>
    - "grep 'home-sub-nav-tab--active' styles/views.css" returns at least 1 match
    - "grep 'home-sub-nav-tabs' styles/views.css" returns at least 1 match
    - "grep '.home-sub-nav {' styles/views.css" returns 1 match
  </acceptance_criteria>
  <done>Home sub-nav CSS appended to views.css; pill tab pattern mirrors finance-sub-nav</done>
</task>

<task type="auto">
  <name>Task 2: Add sub-tab navigation to home.js</name>
  <files>app/views/home.js</files>
  <read_first>
    - app/views/home.js — full file (206 lines) — understand render(), init(), destroy(), getDashboardMode(), loadStats(), statsListeners
    - app/views/home.js lines 60-103 — render() return string structure
    - app/views/home.js lines 108-121 — init() body (loadStats call, skeleton handling)
    - app/views/home.js lines 195-206 — destroy() body
    - app/views/home.js lines 22-27 — getDashboardMode() role branching pattern
    - app/views/proposals.js lines 1-10 — verify STAGE_ORDER and renderStageGroupCard are exported (result of Plan 01)
    - app/engagement-create.js lines 1-15 — verify renderEngagementForm, initEngagementForm, destroyEngagementForm are exported (result of Plan 03)
    - app/proposal-modal.js lines 1-10 — verify openProposalModal is exported (result of Plan 02)
  </read_first>
  <action>
    Edit app/views/home.js as follows:

    IMPORTS:
    - Add BOTH getDocs AND getDoc to the firebase.js import line (both are used: getDocs for the one-time proposals query, getDoc for the fresh-fetch in _homeQueueConfirmAction)
    - Add new import: import { renderEngagementForm, initEngagementForm, destroyEngagementForm } from '../engagement-create.js';
    - Add new import: import { STAGE_ORDER, PROPOSAL_RANGE_STATUSES, renderStageGroupCard, getProposalStatusBadge, renderAgeBadge, _applyProposalStateTransition } from './proposals.js';
      (renderApprovalQueue is intentionally NOT imported — Plan 04 builds a local home-only minimal queue per Pitfall 7 in RESEARCH.md, since the proposals.js renderApprovalQueue depends on module state that home.js does not maintain. _applyProposalStateTransition IS imported because _homeQueueConfirmAction calls it directly with a fresh proposal doc.)
    - Add new import: import { openProposalModal } from '../proposal-modal.js';

    NEW PRIVATE FUNCTIONS (add before render()):

    function getHomeSubTabConfig(): Returns { showSubNav, canEngagements, canProposals, canApproveQueue } based on window.getCurrentUser?.()?.role. Logic: finance and procurement_staff roles return showSubNav: false, all false. All others return showSubNav: true. canEngagements: ['super_admin', 'operations_admin', 'services_admin'].includes(role). canProposals: true (all non-finance/non-procurement roles). canApproveQueue: ['super_admin', 'operations_admin'].includes(role).

    function filterProposalsForUser(allProposals): Reads role from window.getCurrentUser?.()?.role. super_admin: return all. operations_admin or operations_user: filter where (p.parent_collection || 'projects') === 'projects'. services_admin or services_user: filter where (p.parent_collection || 'projects') === 'services'. Default: return [].

    async function _loadHomeProposalsTab(canApproveQueue): One-time getDocs(collection(db, 'proposals')). Map to array. Call filterProposalsForUser. Filter active proposals: status !== 'client_approved' && status !== 'loss'. Build HTML string: if canApproveQueue render approval queue section (use the same renderApprovalQueue pattern but with local data — see Pitfall 7 in RESEARCH.md: build a local minimal queue using inline proposal data, do NOT import queueConfirmAction from proposals.js since it depends on module state). For each stage in STAGE_ORDER render renderStageGroupCard(stage.label, proposalsInStage) only when proposalsInStage.length > 0. Insert result into document.getElementById('homeProposalsContent').innerHTML. Also register window.openProposalModal = openProposalModal so the stage card "View" buttons work. For the approval queue confirm action: home.js writes its own _homeQueueConfirmAction(proposalDocId, mode) that calls getDoc fresh then calls _applyProposalStateTransition directly (imported from proposals.js). Register window.homeQueueConfirmAction = _homeQueueConfirmAction; window.homeQueueCancelModal = () => { document.getElementById('home-queue-action-modal')?.remove(); }; window.homeQueueOpenApproveModal = (id) => _openHomeQueueModal(id, 'approve'); window.homeQueueOpenRejectModal = (id) => _openHomeQueueModal(id, 'reject');

    function switchHomeTab(tab): Hides/shows overview (.quick-stats), engagements (#homeEngagementsContent), proposals (#homeProposalsContent) sections. Updates active class on homeTabOverview, homeTabEngagements, homeTabProposals buttons. Only shows elements that are not null (guard against finance user where these elements don't exist).

    RENDER() CHANGES:
    In the render() return string, add inside the hero-section div, after the hero navigation cards block and before .quick-stats: the home-sub-nav div with initially display:none, and homeEngagementsContent + homeProposalsContent divs also with display:none. Sub-nav structure:
    <div class="home-sub-nav" id="homeSubNav" style="display:none;">
      <div class="home-sub-nav-tabs">
        <button class="home-sub-nav-tab home-sub-nav-tab--active" id="homeTabOverview" onclick="window.switchHomeTab('overview')">Overview</button>
        <button class="home-sub-nav-tab" id="homeTabEngagements" style="display:none;" onclick="window.switchHomeTab('engagements')">Engagements</button>
        <button class="home-sub-nav-tab" id="homeTabProposals" style="display:none;" onclick="window.switchHomeTab('proposals')">Proposals</button>
      </div>
    </div>
    <div id="homeEngagementsContent" style="display:none;"></div>
    <div id="homeProposalsContent" style="display:none;"></div>

    INIT() CHANGES:
    After the existing loadStats(mode) call, add:
    - const { showSubNav, canEngagements, canProposals, canApproveQueue } = getHomeSubTabConfig();
    - if (showSubNav): show #homeSubNav; if canEngagements: show #homeTabEngagements, set #homeEngagementsContent.innerHTML = renderEngagementForm(), call await initEngagementForm(); if canProposals: show #homeTabProposals, call await _loadHomeProposalsTab(canApproveQueue);
    - window.switchHomeTab = switchHomeTab;

    DESTROY() CHANGES:
    Before the existing statsListeners cleanup, add:
    - delete window.switchHomeTab;
    - delete window.openProposalModal;
    - delete window.homeQueueConfirmAction;
    - delete window.homeQueueCancelModal;
    - delete window.homeQueueOpenApproveModal;
    - delete window.homeQueueOpenRejectModal;
    - destroyEngagementForm();
  </action>
  <verify>
    <automated>grep -n "getHomeSubTabConfig\|filterProposalsForUser\|_loadHomeProposalsTab\|switchHomeTab\|homeSubNav\|homeEngagementsContent\|homeProposalsContent\|destroyEngagementForm\|renderEngagementForm\|initEngagementForm" app/views/home.js</automated>
  </verify>
  <acceptance_criteria>
    - "grep 'function getHomeSubTabConfig' app/views/home.js" returns 1 match
    - "grep 'function filterProposalsForUser' app/views/home.js" returns 1 match
    - "grep '_loadHomeProposalsTab' app/views/home.js" returns at least 2 matches (definition + call in init)
    - "grep 'window.switchHomeTab = switchHomeTab' app/views/home.js" returns 1 match
    - "grep 'destroyEngagementForm' app/views/home.js" returns 1 match (in destroy())
    - "grep 'homeSubNav' app/views/home.js" returns at least 2 matches (in render HTML + init show logic)
    - "grep 'getDocs' app/views/home.js" returns at least 1 match (import + usage)
    - "grep 'getDoc' app/views/home.js" returns at least 1 match within the _homeQueueConfirmAction function body (confirms fresh fetch, not module-state lookup)
    - "grep '_applyProposalStateTransition' app/views/home.js" returns at least 2 matches (import line + call inside _homeQueueConfirmAction)
    - "grep 'renderApprovalQueue' app/views/home.js" returns 0 matches (intentionally NOT imported — Plan 04 uses a local home-only queue per Pitfall 7)
    - Manual (as super_admin on home): Three tabs visible — Overview, Engagements, Proposals. Overview tab active by default. Click Engagements — Create Engagement form renders. Personnel picker works (select user → pill appears, remove → pill disappears). Click Proposals — stage group cards render with existing proposals.
    - Manual (as finance user): No sub-nav visible; home page stats render as before.
  </acceptance_criteria>
  <done>home.js has sub-tab navigation; Engagements sub-tab shows form; Proposals sub-tab shows stage cards + approval queue for approvers; finance users unaffected</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Firestore | _loadHomeProposalsTab fetches all proposals then client-side filters — server returns all docs readable by the user per Firestore rules |
| client DOM | Proposal fields rendered in stage group cards must use escapeHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.1-04-01 | Information Disclosure | _loadHomeProposalsTab fetches all proposals | accept | Firestore rules (Phase 87 D-10) already restrict proposals to active users only; client-side filter is a display filter, not a security control |
| T-87.1-04-02 | Elevation of Privilege | home approval queue actions | mitigate | _homeQueueConfirmAction calls _applyProposalStateTransition which is gated by Firestore Security Rules; caller role is validated server-side; getHomeSubTabConfig() canApproveQueue gate prevents the queue UI from rendering for non-approvers |
| T-87.1-04-03 | Tampering | XSS in renderStageGroupCard | mitigate | renderStageGroupCard imported verbatim from proposals.js which already applies escapeHTML on title, client name, and other user fields |
</threat_model>

<verification>
Manual: Log in as super_admin. Home page shows 3 sub-tabs. Engagements tab shows form. Proposals tab shows stage groups and approval queue. Log in as operations_user. Home shows Overview and Proposals tabs only (no Engagements). Log in as finance. Home shows no sub-nav. Navigate away and back — no console errors, no stale listeners.
</verification>

<success_criteria>
All grep checks pass. Manual role checks confirm correct tab visibility per D-01/D-08. Personnel picker on Engagements tab works without console errors (CR-01 regression). Approval queue confirm action succeeds (status changes in Firestore).
</success_criteria>

<output>
After completion, create .planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-04-SUMMARY.md
</output>
