---
phase: 87.1
plan: 02
type: execute
wave: 2
depends_on:
  - 87.1-01
files_modified:
  - app/proposal-modal.js
autonomous: true
requirements:
  - D-04

must_haves:
  truths:
    - "app/proposal-modal.js exists and exports openProposalModal(proposalId, context) and closeProposalModal()"
    - "openProposalModal fetches a fresh doc via getDoc(doc(db, 'proposals', proposalId)) — does NOT use proposalsData.find()"
    - "All proposal detail modal window functions are registered in openProposalModal() and deleted in closeProposalModal()"
    - "All lifecycle action functions (submit, approve/reject, loss, client approved, attachment, comms) are present in proposal-modal.js"
  artifacts:
    - path: "app/proposal-modal.js"
      provides: "Shared proposal detail modal utility"
      exports: ["openProposalModal", "closeProposalModal"]
  key_links:
    - from: "openProposalModal()"
      to: "getDoc(doc(db, 'proposals', proposalId))"
      via: "async fresh fetch — no cached state"
      pattern: "getDoc.*proposals.*proposalId"
    - from: "closeProposalModal()"
      to: "window function cleanup"
      via: "delete window.closeProposalDetailModal (and all 20+ others)"
      pattern: "delete window\\.close"
---

<objective>
Create app/proposal-modal.js as a new shared utility. This file contains all proposal detail modal code extracted verbatim from proposals.js, with one critical adaptation: openProposalModal() fetches a fresh Firestore doc instead of looking up module-level state.

Purpose: home.js, project-detail.js, and service-detail.js all need to open the full proposal modal. Without this shared module they would each need to duplicate ~600 lines of modal logic.
Output: app/proposal-modal.js (~600 lines) exporting openProposalModal and closeProposalModal.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-CONTEXT.md
@.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/proposal-modal.js with extracted modal code</name>
  <files>app/proposal-modal.js</files>
  <read_first>
    - app/views/proposals.js lines 800-1135 — all modal HTML builder functions: renderAuditTrail (line 834), renderProposalActionButtons (line 874), buildProposalDetailModalHtml (line 909), buildProposalDetailsBlock (line 943), buildAttachmentSection (line 977), buildCommsLogSection (line 1077), _renderCommsTypeBadge (line 1044), _renderCommsEntry (line 1049)
    - app/views/proposals.js lines 1130-1260 — openProposalDetail (line 1132), closeProposalDetailModal (line 1144), openCreateProposalModal (line 1154), openEditProposalModal (line 1160), showCreateModal (line 1171), closeCreateProposalModal (line 1251)
    - app/views/proposals.js lines 1258-1430 — saveProposal (line 1258), _refreshDetailModalAfterTransition (line 1480)
    - app/views/proposals.js lines 1480-1600 — submitProposalForApproval (line 1509), submitMarkSentToClient (line 1561), openApproveModal / openRejectModal / _openApproveOrRejectModal (lines 1592-1660), submitProposalApproval (line 1656)
    - app/views/proposals.js lines 1970-2385 — openLossModal, submitLoss, openClientApprovedModal, submitClientApproved, saveProposalAttachment, removeProposalAttachment, _openProposalAttachmentReplace, _openProposalAttachmentRemoveConfirm, toggleAddCommsForm, saveCommsEntry
    - app/views/proposals.js lines 805-835 — AUDIT_ACTION_DOT_COLORS, AUDIT_ACTION_LABELS constants
    - app/views/proposals.js lines 1035-1045 — COMMS_TYPE_META constant
    - app/views/proposals.js lines 33-42 — module state: currentProposal, createModalMode, createModalEditingId
    - app/views/proposals.js lines 455-495 — init() window function registrations (the full list of modal window functions)
    - app/firebase.js — confirm exact Firebase named exports available
    - app/utils.js — confirm showLoading, showToast, formatCurrency, formatTimestamp, escapeHTML, generateProposalId, cryptoRandomUuid exports
    - app/notifications.js — confirm createNotification, createNotificationForRoles, NOTIFICATION_TYPES exports
  </read_first>
  <action>
    Create the file app/proposal-modal.js (does not exist yet). Structure:

    IMPORTS block:
    - From app/firebase.js: db, collection, doc, getDoc, updateDoc, addDoc, writeBatch, serverTimestamp (import path: './firebase.js')
    - From app/utils.js: showLoading, showToast, formatCurrency, formatTimestamp, escapeHTML, generateProposalId, cryptoRandomUuid (import path: './utils.js')
    - From app/notifications.js: createNotification, createNotificationForRoles, NOTIFICATION_TYPES (import path: './notifications.js')
    - From app/views/proposals.js: getProposalStatusBadge, renderAgeBadge, getAgeInStageDays, isOverdueInStage, _applyProposalStateTransition (import path: './views/proposals.js')

    MODULE STATE (private):
    - let currentProposal = null;
    - let createModalMode = 'create';
    - let createModalEditingId = null;

    PRIVATE CONSTANTS (copy verbatim from proposals.js):
    - AUDIT_ACTION_DOT_COLORS (from line ~809)
    - AUDIT_ACTION_LABELS (from line ~821)
    - COMMS_TYPE_META (from line ~1038)

    PRIVATE FUNCTIONS (copy verbatim from proposals.js, do NOT add export):
    - renderAuditTrail(proposal)
    - renderProposalActionButtons(proposal)
    - buildProposalDetailModalHtml(proposal)
    - buildProposalDetailsBlock(proposal)
    - buildAttachmentSection(proposal)
    - buildCommsLogSection(proposal)
    - _renderCommsTypeBadge(type)
    - _renderCommsEntry(entry)
    - _refreshDetailModalAfterTransition(...)
    - showCreateModal(existing)
    - saveProposal()
    - submitProposalForApproval(id)
    - submitMarkSentToClient(id)
    - _openApproveOrRejectModal(id, mode)
    - submitProposalApproval(id, mode)
    - openLossModal(id)
    - submitLoss(id)
    - openClientApprovedModal(id)
    - submitClientApproved(id)
    - saveProposalAttachment(id)
    - removeProposalAttachment(id)
    - _openProposalAttachmentReplace(id)
    - _openProposalAttachmentRemoveConfirm(id)
    - toggleAddCommsForm(id)
    - saveCommsEntry(id)

    The saveProposal() function needs projectsData and clientsData for the Create/Edit dropdown. Add module-level: let _modalProjectsData = []; let _modalClientsData = []; and populate them with one-time getDocs calls at the start of showCreateModal() (before building the modal HTML). Import getDocs and query, where from firebase.js.

    EXPORTED FUNCTIONS:

    export async function openProposalModal(proposalId, context) — ADAPTED from openProposalDetail():
    - Fetch fresh: const snap = await getDoc(doc(db, 'proposals', proposalId));
    - If !snap.exists(): showToast('Proposal not found.', 'error'); return;
    - const proposal = { id: snap.id, ...snap.data() };
    - currentProposal = proposal;
    - Remove any existing #proposalDetailModal from DOM
    - Register ALL modal window functions on window: window.closeProposalDetailModal = closeProposalModal; window.openCreateProposalModal = openCreateProposalModal; window.openEditProposalModal = openEditProposalModal; window.closeCreateProposalModal = closeCreateProposalModal; window.saveProposal = saveProposal; window.submitProposalForApproval = submitProposalForApproval; window.openApproveModal = (id) => _openApproveOrRejectModal(id, 'approve'); window.openRejectModal = (id) => _openApproveOrRejectModal(id, 'reject'); window.submitProposalApproval = submitProposalApproval; window.submitMarkSentToClient = submitMarkSentToClient; window.openLossModal = openLossModal; window.submitLoss = submitLoss; window.openClientApprovedModal = openClientApprovedModal; window.submitClientApproved = submitClientApproved; window.saveProposalAttachment = saveProposalAttachment; window.removeProposalAttachment = removeProposalAttachment; window._openProposalAttachmentReplace = _openProposalAttachmentReplace; window._openProposalAttachmentRemoveConfirm = _openProposalAttachmentRemoveConfirm; window.toggleAddCommsForm = toggleAddCommsForm; window.saveCommsEntry = saveCommsEntry;
    - Inject modal HTML: document.body.insertAdjacentHTML('beforeend', buildProposalDetailModalHtml(proposal));

    export function closeProposalModal() — ADAPTED from closeProposalDetailModal():
    - const el = document.getElementById('proposalDetailModal'); if (el) el.remove();
    - currentProposal = null;
    - Delete ALL window functions registered in openProposalModal() (delete window.closeProposalDetailModal; delete window.openCreateProposalModal; ... all 20 names listed above)

    openCreateProposalModal() and openEditProposalModal(id) remain private functions called from window.openCreateProposalModal and window.openEditProposalModal wrappers registered in openProposalModal.
  </action>
  <verify>
    <automated>grep -n "export async function openProposalModal\|export function closeProposalModal" app/proposal-modal.js</automated>
  </verify>
  <acceptance_criteria>
    - File app/proposal-modal.js exists
    - "grep 'export async function openProposalModal' app/proposal-modal.js" returns 1 match
    - "grep 'export function closeProposalModal' app/proposal-modal.js" returns 1 match
    - "grep 'proposalsData.find' app/proposal-modal.js" returns 0 matches (fresh fetch only, never module-state lookup)
    - "grep 'getDoc.*proposals.*proposalId\|getDoc(doc(db' app/proposal-modal.js" returns at least 1 match (confirms fresh fetch)
    - "grep 'delete window.closeProposalDetailModal' app/proposal-modal.js" returns 1 match (confirms window cleanup in closeProposalModal)
    - "grep 'import.*proposals.js' app/proposal-modal.js" returns 1 match (confirms import of getProposalStatusBadge etc. from proposals.js)
    - "grep 'window.openApproveModal\|window.openRejectModal' app/proposal-modal.js" returns at least 2 matches (confirms both window function registrations present)
    - Manual: open #/proposals as super_admin, click a proposal ID — full detail modal opens with audit trail, comms log, action buttons
  </acceptance_criteria>
  <done>app/proposal-modal.js created; openProposalModal and closeProposalModal exported; fresh fetch confirmed; window function registration and cleanup present</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Firestore | openProposalModal fetches by proposalId — any user who can call the function can read any proposal doc |
| client DOM | buildProposalDetailModalHtml injects proposal fields into HTML — must use escapeHTML on all user-supplied strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.1-02-01 | Information Disclosure | openProposalModal proposalId fetch | accept | proposals collection is readable by all active users per Phase 87 D-10 Firestore rules; no new exposure |
| T-87.1-02-02 | Tampering | buildProposalDetailModalHtml XSS | mitigate | All proposal title, description, client name, and comms entry fields passed through escapeHTML() — carry forward verbatim from proposals.js which already applies escapeHTML throughout |
</threat_model>

<verification>
Manual: Navigate to #/proposals as super_admin. Click a proposal row to open the detail modal. Confirm full modal renders: audit trail, comms log, attachment section, action buttons. Close modal. Confirm all modal elements removed from DOM. Confirm no console errors.
</verification>

<success_criteria>
app/proposal-modal.js exists and exports openProposalModal + closeProposalModal. grep confirms no proposalsData.find usage. Manual test confirms modal opens and closes correctly.
</success_criteria>

<output>
After completion, create .planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-02-SUMMARY.md
</output>
