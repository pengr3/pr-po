---
phase: 87.1
plan: 02
type: execute
wave: 2
depends_on: ["87.1-01"]
files_modified:
  - app/views/engagements.js
autonomous: true
requirements:
  - PROP-INT-03
must_haves:
  truths:
    - "Navigating to #/engagements renders the Create Engagement form with engagement type radio buttons, client picker, name, location, budget, contract cost, personnel picker, and Create Engagement button"
    - "engagements.js follows the view module structure: exports render(), init(), destroy()"
    - "engagements.js imports the five personnel pill helpers from proposals.js instead of copying them verbatim — a single source of truth for pill rendering logic"
    - "destroy() unsubscribes all listeners and deletes all window functions registered in init()"
  artifacts:
    - path: "app/views/engagements.js"
      provides: "Standalone Create Engagement view extracted from proposals.js"
      exports: ["render", "init", "destroy"]
  key_links:
    - from: "app/router.js (Plan 07)"
      to: "app/views/engagements.js"
      via: "import('./views/engagements.js')"
      pattern: "export function render"
    - from: "engagements.js init()"
      to: "createEngagement() in app/engagement-create.js"
      via: "import { createEngagement } from '../engagement-create.js'"
      pattern: "createEngagement"
    - from: "engagements.js"
      to: "proposals.js pill wrapper functions"
      via: "import { renderProposalPillsFor, proposalFilterPersonnelDropdownFor, proposalSelectPersonnelIn, proposalRemovePersonnelFrom, proposalShowPersonnelDropdownFor } from './proposals.js'"
      pattern: "from.*proposals.js"
---

<objective>
Create app/views/engagements.js as a new standalone view containing the Create Engagement form. This extracts the form that currently lives inside proposals.js and gives it its own module, route, and navigation entry. The form itself is unchanged — same fields, same validation, same createEngagement() call.

The personnel pill helpers are imported from proposals.js as the five exported wrapper functions added by Plan 01 (renderProposalPillsFor, proposalFilterPersonnelDropdownFor, proposalSelectPersonnelIn, proposalRemovePersonnelFrom, proposalShowPersonnelDropdownFor) rather than copied verbatim, per the review recommendation. These wrappers accept state as parameters — engagements.js passes its own local selectedPersonnel/usersData.

Purpose: D-03 requires a dedicated Engagements tab at #/engagements visible to super_admin, operations_admin, and services_admin. Extracting the form clears the way for Plan 03 to remove it from proposals.js.
Output: app/views/engagements.js with render(), init(), destroy() — ready for the router wiring in Plan 07.
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-PATTERNS.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\PROJECT.md
@C:\Users\Admin\Roaming\pr-po\app\views\proposals.js
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/views/engagements.js — extracted engagement form view with imported pill wrapper functions</name>
  <files>app/views/engagements.js</files>
  <read_first>
    - app/views/proposals.js (lines 1-180 for the engagement form section in render(); lines 182-278 for the five private pill helpers — read to understand the logic before calling the exported wrappers; lines 280-440 for submitNewEngagement/handleEngagementTypeChange functions; lines 448-563 for init() window function registration and listeners; lines 570-586 for destroy() cleanup)
    - app/engagement-create.js (full file — understand what createEngagement() expects so the form passes correct values)
    - .planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-01-SUMMARY.md (verify the exact exported wrapper signatures before writing the import line and window closures)
  </read_first>
  <action>
    Create app/views/engagements.js as a new file. It is an extraction of the engagement form logic from proposals.js, repackaged as a standalone view module. The key difference from a verbatim copy is that the personnel pill helpers are imported as the five exported wrapper functions from Plan 01 (not duplicated here).

    File structure:

    1. Module-level comment block: "ENGAGEMENTS VIEW — Phase 87.1 D-03 — extracted from proposals.js. Personnel pill helpers imported from proposals.js (pure wrapper functions, state passed as args per Phase 87.1 review)."

    2. Imports:
       - Firebase: `db, collection, onSnapshot, query, where` from ../firebase.js
       - Utils: `showToast, syncPersonnelToAssignments, syncServicePersonnelToAssignments, escapeHTML` from ../utils.js
       - Engagement create: `createEngagement` from ../engagement-create.js
       - Pill wrapper functions (the five NEW exported wrappers added by Plan 01 — NOT the original private helper names):
         `import { renderProposalPillsFor, proposalFilterPersonnelDropdownFor, proposalSelectPersonnelIn, proposalRemovePersonnelFrom, proposalShowPersonnelDropdownFor } from './proposals.js';`

       Do NOT import generateProposalId, renderProposalPills, proposalFilterPersonnelDropdown, proposalSelectPersonnel, proposalRemovePersonnel, proposalShowPersonnelDropdown, or any other proposal-specific symbols. The original private helper names are NOT exported from proposals.js and would be undefined at runtime.

    3. Module-level state: `clientsData`, `usersData`, `selectedPersonnel`, `listeners`, `currentEngagementType` — these are LOCAL to engagements.js and independent from the copies in proposals.js. They are the state arguments passed to the imported wrapper functions.

    4. `export function render(activeTab = null, param = null)` — returns a container div with an h2 "New Engagement", subtitle "Create a new project or service engagement.", and the form card. The form card body contains the complete `<section id="new-engagement-section">` contents (verbatim from proposals.js render() lines ~52-167), but wrapped in `.card` + `.card-body`. Remove the outer `<section>` wrapper — the card IS the content root.

    5. Personnel pill helper wiring: Do NOT define renderProposalPillsFor, proposalFilterPersonnelDropdownFor, proposalSelectPersonnelIn, proposalRemovePersonnelFrom, or proposalShowPersonnelDropdownFor locally — they are imported from proposals.js. In init(), register window functions as closures that bind the local module state and pass it to the imported wrappers. Use the exact wrapper names and parameter signatures from Plan 01:

       window.proposalSelectPersonnel = (userId, userName) => {
           selectedPersonnel = proposalSelectPersonnelIn(
               userId, userName, selectedPersonnel,
               () => renderProposalPillsFor(selectedPersonnel)
           );
       };
       window.proposalRemovePersonnel = (userId) => {
           selectedPersonnel = proposalRemovePersonnelFrom(
               userId, selectedPersonnel,
               () => renderProposalPillsFor(selectedPersonnel)
           );
       };
       window.proposalShowPersonnelDropdown = () =>
           proposalShowPersonnelDropdownFor(selectedPersonnel, usersData);
       window.proposalFilterPersonnelDropdown = (text) =>
           proposalFilterPersonnelDropdownFor(text, selectedPersonnel, usersData);

       The window function names (proposalSelectPersonnel, proposalRemovePersonnel, etc.) are the names used by onclick handlers in the rendered HTML — they must match the HTML attributes exactly. These window closures simply delegate to the imported wrappers with the correct state arguments.

       Note on proposalSelectPersonnelIn signature: `(userId, userName, selectedPersonnel, renderFn, searchInputId, dropdownId)` — searchInputId and dropdownId default to the standard element IDs in the wrapper body, so they can be omitted here.

       Note on proposalRemovePersonnelFrom signature: `(userId, selectedPersonnel, renderFn)` — three parameters only. The userName param from the old private helper is NOT present in the new wrapper.

    6. After window function registration in init(), call `renderProposalPillsFor(selectedPersonnel)` to initialize the pill container if it exists in the DOM at that point.

    7. `submitNewEngagement()` and `handleEngagementTypeChange()` — copy from proposals.js verbatim. These functions are NOT imported because they are view-specific logic (they reference local module state and the local form DOM). Only the pill wrappers, which are pure utilities, are imported.

    8. `export async function init(activeTab = null, param = null)` — copy the window function registration and listener setup from proposals.js init() lines ~448-563, scoped to engagement-only functions. Register: `window.submitNewEngagement`, `window.handleEngagementTypeChange`, and the four pill window functions via the closures above.

    9. `export async function destroy()` — unsubscribe all listeners in `listeners` array; delete all six window functions registered in init(): `window.submitNewEngagement`, `window.handleEngagementTypeChange`, `window.proposalSelectPersonnel`, `window.proposalRemovePersonnel`, `window.proposalShowPersonnelDropdown`, `window.proposalFilterPersonnelDropdown`. Reset `clientsData`, `usersData`, `selectedPersonnel`, `currentEngagementType` to their initial values.

    The page heading in render() must use copy from 87.1-UI-SPEC.md Surface 2: h2 "New Engagement", subtitle paragraph "Create a new project or service engagement." Submit button label "Create Engagement".
  </action>
  <verify>
    <automated>grep -n "export function render\|export async function init\|export async function destroy\|createEngagement\|window.submitNewEngagement\|from.*proposals" /c/Users/Admin/Roaming/pr-po/app/views/engagements.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from.*proposals" app/views/engagements.js` returns the import line containing renderProposalPillsFor, proposalFilterPersonnelDropdownFor, proposalSelectPersonnelIn, proposalRemovePersonnelFrom, proposalShowPersonnelDropdownFor (the five wrapper names from Plan 01).
    - `grep -c "renderProposalPills,\|proposalFilterPersonnelDropdown,\|proposalSelectPersonnel,\|proposalRemovePersonnel,\|proposalShowPersonnelDropdown," app/views/engagements.js` returns 0 — none of the original private helper names appear in the import line.
    - `grep -c "function renderProposalPillsFor\|function proposalFilterPersonnelDropdownFor\|function proposalSelectPersonnelIn\|function proposalRemovePersonnelFrom\|function proposalShowPersonnelDropdownFor" app/views/engagements.js` returns 0 — no local function definitions for pill wrappers (they are imported, not defined here).
    - `grep "export function render\|export async function init\|export async function destroy" app/views/engagements.js` returns 3 results.
    - `grep "import { createEngagement }" app/views/engagements.js` returns a result targeting engagement-create.js.
    - `grep "let listeners\|let currentEngagementType\|let selectedPersonnel" app/views/engagements.js` returns 3 results (module-level state declarations).
    - `grep "delete window.submitNewEngagement" app/views/engagements.js` returns a result in destroy().
    - `grep "proposalSelectPersonnelIn\|proposalRemovePersonnelFrom\|proposalShowPersonnelDropdownFor\|proposalFilterPersonnelDropdownFor\|renderProposalPillsFor" app/views/engagements.js` returns results showing each wrapper name is called inside a window closure body.
  </acceptance_criteria>
  <done>
    app/views/engagements.js exists and contains: `export function render`, `export async function init`, `export async function destroy`, an import of `createEngagement` from engagement-create.js, an import of the five Plan 01 wrapper functions (renderProposalPillsFor, proposalFilterPersonnelDropdownFor, proposalSelectPersonnelIn, proposalRemovePersonnelFrom, proposalShowPersonnelDropdownFor) from proposals.js, `window.submitNewEngagement` assignment in init(), `delete window.submitNewEngagement` in destroy(), and window closures for the four pill window functions that call the imported wrappers with local state as arguments. No local definitions of any pill helper or wrapper functions exist in this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engagements.js form → createEngagement() → Firestore | User-submitted engagement form fields (name, type, client) cross the module boundary to createEngagement() which writes to Firestore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.1-02-01 | Elevation of Privilege | /engagements route access | mitigate | Router hard-gate in Plan 07 blocks non-allowed roles (super_admin, operations_admin, services_admin) before this view loads; auth.js hides the nav link; route guard is defense-in-depth. |
| T-87.1-02-02 | Tampering | createEngagement() call from engagements.js | accept | createEngagement() in engagement-create.js performs its own Firestore writes subject to the existing security rules — no new attack surface opened by this extraction. |
</threat_model>

<verification>
1. `grep -c "export" /c/Users/Admin/Roaming/pr-po/app/views/engagements.js` returns >= 3 (render, init, destroy).
2. File contains `import { createEngagement }` targeting engagement-create.js.
3. File contains `import { renderProposalPillsFor` targeting proposals.js — the Plan 01 wrapper name, not the old private name.
4. File contains module-level `let listeners = []` and `let currentEngagementType = 'project'`.
5. File does NOT contain `function renderProposalPillsFor` or `function proposalFilterPersonnelDropdownFor` as local definitions.
6. destroy() contains `delete window.submitNewEngagement` — confirms cleanup is present.
7. `grep "proposalSelectPersonnelIn\|proposalRemovePersonnelFrom" app/views/engagements.js` returns results confirming the wrapper call sites use the correct names.
</verification>

<success_criteria>
- app/views/engagements.js is a self-contained view module with render/init/destroy exports
- The Create Engagement form renders identically to the form that was in proposals.js
- Personnel pill wrappers are imported from proposals.js using the five Plan 01 exported names (renderProposalPillsFor, proposalFilterPersonnelDropdownFor, proposalSelectPersonnelIn, proposalRemovePersonnelFrom, proposalShowPersonnelDropdownFor) with module-local state passed as arguments — no verbatim copy of pill helper logic
- Window closures in init() call each imported wrapper with the correct parameter signature
- All window functions registered in init() are deleted in destroy()
- All onSnapshot listeners pushed to `listeners` are unsubscribed in destroy()
</success_criteria>

<output>
After completion, create `.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-02-SUMMARY.md` with the new file path, its line count, confirmation that the three exports are present, and confirmation that pill wrappers are imported from proposals.js using the Plan 01 exported names (not defined locally).
</output>
