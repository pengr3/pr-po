---
phase: 87.1
plan: 02
type: execute
wave: 2
depends_on: ["87.1-01"]
files_modified:
  - app/views/engagements.js
autonomous: true
requirements:
  - PROP-INT-03
must_haves:
  truths:
    - "Navigating to #/engagements renders the Create Engagement form with engagement type radio buttons, client picker, name, location, budget, contract cost, personnel picker, and Create Engagement button"
    - "engagements.js follows the view module structure: exports render(), init(), destroy()"
    - "engagements.js imports the five personnel pill helpers from proposals.js instead of copying them verbatim — a single source of truth for pill rendering logic"
    - "destroy() unsubscribes all listeners and deletes all window functions registered in init()"
  artifacts:
    - path: "app/views/engagements.js"
      provides: "Standalone Create Engagement view extracted from proposals.js"
      exports: ["render", "init", "destroy"]
  key_links:
    - from: "app/router.js (Plan 07)"
      to: "app/views/engagements.js"
      via: "import('./views/engagements.js')"
      pattern: "export function render"
    - from: "engagements.js init()"
      to: "createEngagement() in app/engagement-create.js"
      via: "import { createEngagement } from '../engagement-create.js'"
      pattern: "createEngagement"
    - from: "engagements.js"
      to: "proposals.js pill helpers"
      via: "import { renderProposalPills, proposalFilterPersonnelDropdown, proposalSelectPersonnel, proposalRemovePersonnel, proposalShowPersonnelDropdown } from './proposals.js'"
      pattern: "from.*proposals.js"
---

<objective>
Create app/views/engagements.js as a new standalone view containing the Create Engagement form. This extracts the form that currently lives inside proposals.js and gives it its own module, route, and navigation entry. The form itself is unchanged — same fields, same validation, same createEngagement() call.

The personnel pill helpers (renderProposalPills, proposalFilterPersonnelDropdown, etc.) are imported from proposals.js rather than copied verbatim, per the review recommendation. This is safe because Plan 01 refactored those helpers to accept state as parameters — engagements.js passes its own local selectedPersonnel/usersData.

Purpose: D-03 requires a dedicated Engagements tab at #/engagements visible to super_admin, operations_admin, and services_admin. Extracting the form clears the way for Plan 03 to remove it from proposals.js.
Output: app/views/engagements.js with render(), init(), destroy() — ready for the router wiring in Plan 07.
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-PATTERNS.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\PROJECT.md
@C:\Users\Admin\Roaming\pr-po\app\views\proposals.js
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/views/engagements.js — extracted engagement form view with imported pill helpers</name>
  <files>app/views/engagements.js</files>
  <read_first>
    - app/views/proposals.js (lines 1-180 for the engagement form section in render(); lines 182-278 for the personnel pill helpers — read to understand the pure-function signatures added by Plan 01; lines 280-440 for submitNewEngagement/handleEngagementTypeChange functions; lines 448-563 for init() window function registration and listeners; lines 570-586 for destroy() cleanup)
    - app/engagement-create.js (full file — understand what createEngagement() expects so the form passes correct values)
    - .planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-01-SUMMARY.md (verify the exact exported signatures from Plan 01 before writing the import line)
  </read_first>
  <action>
    Create app/views/engagements.js as a new file. It is an extraction of the engagement form logic from proposals.js, repackaged as a standalone view module. The key difference from a verbatim copy is that the personnel pill helpers are imported from proposals.js (not duplicated here).

    File structure:

    1. Module-level comment block: "ENGAGEMENTS VIEW — Phase 87.1 D-03 — extracted from proposals.js. Personnel pill helpers imported from proposals.js (pure functions, state passed as args per Phase 87.1 review)."

    2. Imports:
       - Firebase: `db, collection, onSnapshot, query, where` from ../firebase.js
       - Utils: `showToast, syncPersonnelToAssignments, syncServicePersonnelToAssignments, escapeHTML` from ../utils.js
       - Engagement create: `createEngagement` from ../engagement-create.js
       - Pill helpers (addresses review concern: no verbatim copy): `import { renderProposalPills, proposalFilterPersonnelDropdown, proposalSelectPersonnel, proposalRemovePersonnel, proposalShowPersonnelDropdown } from './proposals.js';`

       Do NOT import generateProposalId or any proposal-specific symbols.

    3. Module-level state: `clientsData`, `usersData`, `selectedPersonnel`, `listeners`, `currentEngagementType` — these are LOCAL to engagements.js and independent from the copies in proposals.js. They are the arguments passed to the imported pill helper functions.

    4. `export function render(activeTab = null, param = null)` — returns a container div with an h2 "New Engagement", subtitle "Create a new project or service engagement.", and the form card. The form card body contains the complete `<section id="new-engagement-section">` contents (verbatim from proposals.js render() lines ~52-167), but wrapped in `.card` + `.card-body`. Remove the outer `<section>` wrapper — the card IS the content root.

    5. Personnel pill helper wiring: Do NOT define renderProposalPills, proposalFilterPersonnelDropdown, proposalSelectPersonnel, proposalRemovePersonnel, proposalShowPersonnelDropdown locally. They are imported from proposals.js. In init(), register window functions as closures that bind the local module state and pass it to the imported helpers:

       ```javascript
       window.proposalSelectPersonnel = (userId, userName) => {
           selectedPersonnel = proposalSelectPersonnel(userId, userName, selectedPersonnel,
               () => renderProposalPills(selectedPersonnel));
       };
       window.proposalRemovePersonnel = (userId, userName) => {
           selectedPersonnel = proposalRemovePersonnel(userId, userName, selectedPersonnel,
               () => renderProposalPills(selectedPersonnel));
       };
       window.proposalShowPersonnelDropdown = () =>
           proposalShowPersonnelDropdown(selectedPersonnel, usersData);
       window.proposalFilterPersonnelDropdown = (text) =>
           proposalFilterPersonnelDropdown(text, selectedPersonnel, usersData);
       ```

       The window function names are identical to proposals.js. This is correct — project-detail and engagements are never active simultaneously; router destroys one view before loading another.

    6. `submitNewEngagement()` and `handleEngagementTypeChange()` — copy from proposals.js verbatim. These functions are NOT imported because they are view-specific logic (they reference local module state and the local form DOM). Only the pill helpers, which are pure utilities, are imported.

    7. `export async function init(activeTab = null, param = null)` — copy the window function registration and listener setup from proposals.js init() lines ~448-563, scoped to engagement-only functions. After the listener setup, also call `renderProposalPills(selectedPersonnel)` to initialize the pill container if it exists. Register: `window.submitNewEngagement`, `window.handleEngagementTypeChange`, and the four pill window functions via the closures above.

    8. `export async function destroy()` — unsubscribe all listeners in `listeners` array; delete all six window functions registered in init(): `window.submitNewEngagement`, `window.handleEngagementTypeChange`, `window.proposalSelectPersonnel`, `window.proposalRemovePersonnel`, `window.proposalShowPersonnelDropdown`, `window.proposalFilterPersonnelDropdown`. Reset `clientsData`, `usersData`, `selectedPersonnel`, `currentEngagementType` to their initial values.

    The page heading in render() must use copy from 87.1-UI-SPEC.md Surface 2: h2 "New Engagement", subtitle paragraph "Create a new project or service engagement." Submit button label "Create Engagement".
  </action>
  <verify>
    <automated>grep -n "export function render\|export async function init\|export async function destroy\|createEngagement\|window.submitNewEngagement\|from.*proposals" /c/Users/Admin/Roaming/pr-po/app/views/engagements.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from.*proposals" app/views/engagements.js` returns the import line containing renderProposalPills, proposalFilterPersonnelDropdown, proposalSelectPersonnel, proposalRemovePersonnel, proposalShowPersonnelDropdown.
    - `grep -c "function renderProposalPills\|function proposalFilterPersonnelDropdown\|function proposalSelectPersonnel\|function proposalRemovePersonnel\|function proposalShowPersonnelDropdown" app/views/engagements.js` returns 0 — no local function definitions for pill helpers (they are imported, not defined here).
    - `grep "export function render\|export async function init\|export async function destroy" app/views/engagements.js` returns 3 results.
    - `grep "import { createEngagement }" app/views/engagements.js` returns a result targeting engagement-create.js.
    - `grep "let listeners\|let currentEngagementType\|let selectedPersonnel" app/views/engagements.js` returns 3 results (module-level state declarations).
    - `grep "delete window.submitNewEngagement" app/views/engagements.js` returns a result in destroy().
  </acceptance_criteria>
  <done>
    app/views/engagements.js exists and contains: `export function render`, `export async function init`, `export async function destroy`, an import of `createEngagement` from engagement-create.js, an import of the five pill helpers from proposals.js, `window.submitNewEngagement` assignment in init(), and `delete window.submitNewEngagement` in destroy(). No local definitions of the five pill helper functions exist in this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engagements.js form → createEngagement() → Firestore | User-submitted engagement form fields (name, type, client) cross the module boundary to createEngagement() which writes to Firestore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.1-02-01 | Elevation of Privilege | /engagements route access | mitigate | Router hard-gate in Plan 07 blocks non-allowed roles (super_admin, operations_admin, services_admin) before this view loads; auth.js hides the nav link; route guard is defense-in-depth. |
| T-87.1-02-02 | Tampering | createEngagement() call from engagements.js | accept | createEngagement() in engagement-create.js performs its own Firestore writes subject to the existing security rules — no new attack surface opened by this extraction. |
</threat_model>

<verification>
1. `grep -c "export" /c/Users/Admin/Roaming/pr-po/app/views/engagements.js` returns >= 3 (render, init, destroy).
2. File contains `import { createEngagement }` targeting engagement-create.js.
3. File contains `import { renderProposalPills` targeting proposals.js.
4. File contains module-level `let listeners = []` and `let currentEngagementType = 'project'`.
5. File does NOT contain `function renderProposalPills` or `function proposalFilterPersonnelDropdown` as local definitions.
6. destroy() contains `delete window.submitNewEngagement` — confirms cleanup is present.
</verification>

<success_criteria>
- app/views/engagements.js is a self-contained view module with render/init/destroy exports
- The Create Engagement form renders identically to the form that was in proposals.js
- Personnel pill helpers are imported from proposals.js with module-local state passed as arguments — no verbatim copy of pill helper logic
- All window functions registered in init() are deleted in destroy()
- All onSnapshot listeners pushed to `listeners` are unsubscribed in destroy()
</success_criteria>

<output>
After completion, create `.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-02-SUMMARY.md` with the new file path, its line count, confirmation that the three exports are present, and confirmation that pill helpers are imported from proposals.js (not defined locally).
</output>
