---
phase: 87.1
plan: "07"
subsystem: routing
tags:
  - router
  - auth
  - nav
  - role-gate
dependency_graph:
  requires:
    - "87.1-02 (app/views/engagements.js created — the view being wired)"
  provides:
    - "app/router.js — /engagements route + hard role gate"
    - "app/auth.js — Engagements nav visibility for allowed roles"
    - "index.html — desktop + mobile Engagements nav links"
  affects:
    - "All users — Engagements nav link now visible for super_admin, operations_admin, services_admin"
tech_stack:
  added: []
  patterns:
    - "hard router role gate — same two-tier pattern as /proposals gate"
    - "nav visibility via data-route selector in auth.js updateNavState()"
decisions:
  - "/engagements excluded from routePermissionMap — hard gate in navigate() is the sole role check; adding to routePermissionMap would call hasTabAccess() which requires project/service template and would block the route"
  - "Allowed roles: super_admin, operations_admin, services_admin (broader than /proposals which is super_admin only)"
  - "Nav link positioned between Finance and Proposals in both desktop and mobile nav"
key_files:
  created: []
  modified:
    - path: "app/router.js"
      changes:
        - "Route entry '/engagements' added at line 117 (after '/proposals', before closing })"
        - "Hard role gate block at lines 305-319 in navigate() — checks allowed array, calls showAccessDenied() on mismatch"
        - "6-line comment block documenting two-tier permission pattern rationale (routePermissionMap vs hard gate)"
    - path: "app/auth.js"
      changes:
        - "Authenticated branch: engagements visibility block at line 427 (after proposals block)"
        - "Unauthenticated branch: engagements hide at line 474 (after proposals hide)"
    - path: "index.html"
      changes:
        - "Desktop nav: Engagements link at line 84 (between Finance line 83 and Proposals line 85)"
        - "Mobile nav: Engagements item at line 111 (between Finance line 110 and Proposals line 112)"
metrics:
  duration: "< 10 minutes"
  completed: "2026-05-19"
  tasks_completed: 2
  files_modified: 3
---

# Phase 87.1 Plan 07: Engagements Routing Wiring Summary

One-liner: Wired engagements.js into the app with a /engagements route in router.js (lazy-load + hard role gate for super_admin/operations_admin/services_admin), nav visibility control in auth.js, and desktop + mobile nav links in index.html positioned between Finance and Proposals.

## Tasks Completed

| Task | Description | Commit | Status |
|------|-------------|--------|--------|
| 1 | Add /engagements route and hard role gate to router.js | 23a6546 | DONE |
| 2 | Add Engagements link visibility to auth.js and nav links to index.html | f616dc7 | DONE |

## Artifacts Produced

### app/router.js (modified)

**Route entry** (line 117, inside routes object):
```javascript
'/engagements': {
    name: 'Engagements',
    load: () => import('./views/engagements.js'),
    title: 'New Engagement | CLMC Operations'
}
```

**Hard role gate** (lines 305-319, in navigate(), after /proposals gate):
```javascript
// Phase 87.1 D-03 — hard role gate for /engagements (mirrors /proposals pattern above)
// NOTE: /engagements is NOT in routePermissionMap. routePermissionMap calls hasTabAccess()
// ...two-tier pattern explanation...
if (path === '/engagements') {
    const user = window.getCurrentUser?.();
    const allowed = ['super_admin', 'operations_admin', 'services_admin'];
    if (!user || !allowed.includes(user.role)) {
        showAccessDenied();
        return;
    }
}
```

### app/auth.js (modified)

**Authenticated branch** (lines 426-431): Queries `[data-route="engagements"]` and sets display based on `canSeeEngagements` (includes check for super_admin, operations_admin, services_admin).

**Unauthenticated branch** (line 474): Hides all `[data-route="engagements"]` elements.

### index.html (modified)

**Desktop nav** (line 84): `<a href="#/engagements" class="nav-link" data-route="engagements">Engagements</a>` — between Finance (83) and Proposals (85).

**Mobile nav** (line 111): `<a href="#/engagements" class="mobile-nav-item" data-route="engagements" onclick="mobileNavClick(event, this)">Engagements</a>` — between Finance (110) and Proposals (112).

## Acceptance Criteria Verification

- `grep "'/engagements'" app/router.js` — returns route entry at line 117. PASS
- `grep "import.*views/engagements" app/router.js` — returns lazy-load line 119. PASS
- `grep "path === '/engagements'" app/router.js` — returns gate block at line 311. PASS
- `grep "routePermissionMap.*engagements\|engagements.*routePermissionMap" app/router.js` — 0 results (only comment). PASS
- `grep "two-tier pattern\|assignment-scoped" app/router.js` — returns comment lines 309-310. PASS
- `grep -c "engagements" app/auth.js` returns 3 (>= 2). PASS
- `grep -c "data-route=\"engagements\"" index.html` returns 2. PASS
- Desktop engagements line 84 appears between Finance (83) and Proposals (85). PASS
- Mobile engagements line 111 appears between Finance (110) and Proposals (112). PASS

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. This plan adds routing/navigation wiring only; no data rendering stubs introduced.

## Threat Surface Scan

Both mitigations from the plan's threat model are implemented:

| Threat | Mitigation |
|--------|-----------|
| T-87.1-07-01: Direct URL navigation by non-allowed role | Hard gate in router.js navigate() checks getCurrentUser().role against allowed array; calls showAccessDenied() on mismatch |
| T-87.1-07-02: Unauthenticated /engagements access | auth.js hides nav links for unauthenticated users; router gate returns null for getCurrentUser() and blocks with showAccessDenied() |

No new threat surface introduced beyond what the plan's threat model covers.

## Self-Check: PASSED

- `app/router.js` modified (commit 23a6546)
- `app/auth.js` modified (commit f616dc7)
- `index.html` modified (commit f616dc7)
- `/engagements` route entry at line 117 confirmed via grep
- Hard role gate at line 311 confirmed via grep
- Two-tier pattern comment confirmed via grep
- auth.js: 3 engagements occurrences (authenticated block line 427 + 429 + unauthenticated line 474)
- index.html: 2 data-route="engagements" occurrences (lines 84 and 111)
- Nav ordering: Finance → Engagements → Proposals confirmed in both desktop (83/84/85) and mobile (110/111/112)
