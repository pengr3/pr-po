---
phase: 87.1
plan: "06"
subsystem: home
tags:
  - proposals
  - sub-tab
  - role-filter
  - getDocs
dependency_graph:
  requires:
    - "87.1-01 (STAGE_ORDER, getProposalStatusBadge, renderAgeBadge exported from proposals.js)"
  provides:
    - "app/views/home.js Proposals sub-tab (filterProposalsForUser, renderHomeProposalsDashboard, switchHomeTab)"
    - "styles/views.css .home-sub-nav CSS block"
  affects:
    - "home page — shows pill-tab sub-nav when user has active proposals"
tech_stack:
  added: []
  patterns:
    - "getDocs one-shot query in init() — no listener pushed to statsListeners (D-05)"
    - "separate try/catch for proposals fetch — stats load unaffected by proposals failure (T-87.1-06-03)"
    - "role-based client-side filter with assignedCodes null check for all_projects"
    - "STAGE_ORDER imported from proposals.js — single source of truth for stage/label config"
    - "DOM-patch post-render pattern — render() outputs hidden elements, init() reveals them"
key_files:
  created: []
  modified:
    - path: "app/views/home.js"
      changes:
        - "Line 6: Added getDocs to firebase.js import"
        - "Line 7: Added import of escapeHTML, formatCurrency from utils.js"
        - "Line 8: Added import of getProposalStatusBadge, renderAgeBadge, STAGE_ORDER from proposals.js"
        - "Lines 31-63: Added filterProposalsForUser() — role/assignedCodes filter"
        - "Lines 65-98: Added renderHomeProposalsDashboard() — uses imported STAGE_ORDER"
        - "Lines 100-143: Added renderHomeStageCard() — table with overdue border, View → link"
        - "Lines 145-178: Added switchHomeTab() — toggles .quick-stats / #homeProposalsContent"
        - "Lines 272-295: Added getDocs proposals fetch block in init() with window.switchHomeTab assignment"
        - "Lines 375-376: Added delete window.switchHomeTab in destroy()"
        - "Lines 244-255: render() — added homeSubNav div and homeProposalsContent div (both display:none)"
    - path: "styles/views.css"
      changes:
        - "Lines 3081-3112: Appended .home-sub-nav block (7 selectors) matching finance-sub-nav pill pattern"
decisions:
  - "filterProposalsForUser returns [] for finance and procurement_staff — sub-nav never shown (T-87.1-06-01 mitigate)"
  - "getDocs in separate try/catch so proposals failure cannot block home stats (T-87.1-06-03 mitigate)"
  - "STAGE_ORDER imported from proposals.js — no local redeclaration per review recommendation"
  - "renderHomeStageCard Actions column links to #/proposals only — Submit for Approval not available from home (D-05)"
  - "homeSubNav uses getElementById not querySelector('.home-sub-nav') to avoid ambiguity if multiple nav elements present"
metrics:
  duration: "< 15 minutes"
  completed: "2026-05-19"
  tasks_completed: 2
  files_modified: 2
---

# Phase 87.1 Plan 06: Home Dashboard Proposals Sub-Tab Summary

One-liner: Added role-filtered Proposals sub-tab to home dashboard using getDocs + STAGE_ORDER imported from proposals.js, with pill-tab nav, grouped-by-stage table, and overdue row highlighting.

## Tasks Completed

| Task | Description | Commit | Status |
|------|-------------|--------|--------|
| 1 | Add sub-nav HTML to render() and .home-sub-nav CSS to views.css | dbf3227 | DONE |
| 2 | Add getDocs fetch, filterProposalsForUser, renderHomeProposalsDashboard, switchHomeTab | 1f3115d | DONE |

## Artifacts Produced

### app/views/home.js

**New imports (lines 6-8):**
- `getDocs` added to firebase.js import
- `import { escapeHTML, formatCurrency } from '../utils.js'`
- `import { getProposalStatusBadge, renderAgeBadge, STAGE_ORDER } from './proposals.js'`

**render() changes (lines 244-255):**
- Added `<div class="home-sub-nav" style="display:none;" id="homeSubNav">` with two pill buttons
- Added `<div id="homeProposalsContent" style="display:none;padding:0 1rem;">` after the sub-nav
- Both hidden on render; revealed by init() only if active proposals exist for the role

**New functions:**
- `filterProposalsForUser(allProposals)` — branches on super_admin (all), operations roles (projects + assignedCodes), services roles (services + assignedCodes), others ([])
- `renderHomeProposalsDashboard(proposals)` — groups by status, renders Draft first, then STAGE_ORDER groups
- `renderHomeStageCard(label, stageProposals)` — table with PROP ID / Title / Project-Service / Client / Amount / Age in Stage / Actions (View → link to #/proposals); overdue left border for pending_internal/pending_client > 7 days
- `switchHomeTab(tab)` — toggles .quick-stats and #homeProposalsContent; manages active class on pill buttons

**init() additions:**
- Proposals getDocs block in separate try/catch after loadStats()
- `window.switchHomeTab = switchHomeTab` after the proposals try/catch

**destroy() additions:**
- `delete window.switchHomeTab` before statsListeners cleanup

### styles/views.css

**New block appended (7 selectors):**
- `.home-sub-nav` — white background, gray bottom border, 1.5rem bottom margin
- `.home-sub-nav-tabs` — flex, 0.25rem gap, overflow-x auto, scrollbar hidden
- `.home-sub-nav-tabs::-webkit-scrollbar` — display none
- `.home-sub-nav-tab` — pill style, 999px border-radius, 0.9375rem font, 600 weight
- `.home-sub-nav-tab:hover` — gray-50 background
- `.home-sub-nav-tab--active` — primary blue background, white text
- `.home-sub-nav-tab--active:hover` — primary-dark background

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. The sub-tab renders live Firestore data via getDocs.

## Threat Surface Scan

No new network endpoints or auth paths introduced. The getDocs call reads the `proposals` collection — same collection already accessed by proposals.js. Client-side role filter (filterProposalsForUser) mitigates T-87.1-06-01 and T-87.1-06-02. Separate try/catch mitigates T-87.1-06-03.

## Self-Check: PASSED

- `app/views/home.js` — modified and committed (dbf3227, 1f3115d)
- `styles/views.css` — modified and committed (dbf3227)
- `grep -c "home-sub-nav" styles/views.css` returns 7 (>= 6 required)
- `grep "id=\"homeSubNav\"" app/views/home.js` returns div with `style="display:none"`
- `grep "id=\"homeProposalsContent\"" app/views/home.js` returns div with `style="display:none"`
- `grep "import.*STAGE_ORDER.*proposals" app/views/home.js` returns import line
- `grep "const STAGE_ORDER\|let STAGE_ORDER" app/views/home.js` returns 0 results
- `grep "filterProposalsForUser" app/views/home.js` returns function definition + call
- `grep "getDocs" app/views/home.js` returns import + call in init()
- `grep "delete window.switchHomeTab" app/views/home.js` returns result in destroy()
</content>
</invoke>