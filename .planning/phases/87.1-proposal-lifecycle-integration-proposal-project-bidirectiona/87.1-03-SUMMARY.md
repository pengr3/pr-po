---
phase: 87.1
plan: "03"
subsystem: proposals
tags:
  - refactor
  - cleanup
  - window-functions
dependency_graph:
  requires:
    - "87.1-01 (exported wrapper functions so engagements.js can import them)"
    - "87.1-02 (engagements.js now owns the Create Engagement form)"
  provides:
    - "proposals.js render() outputs heading + queue-mount + dashboard-mount only (no engagement form)"
    - "proposals.js init() does not register engagement window functions"
    - "proposals.js destroy() does not delete engagement window functions"
  affects:
    - "app/views/proposals.js (reduced surface — no engagement section)"
tech_stack:
  added: []
  patterns:
    - "window function lifecycle symmetry — only register in init() what destroy() will clean up"
key_files:
  created: []
  modified:
    - path: "app/views/proposals.js"
      changes:
        - "Removed <section id='new-engagement-section'> block from render() (~116 lines removed)"
        - "Removed 6 engagement window function registrations from init()"
        - "Removed 6 engagement window function deletes from destroy()"
        - "Removed 4 engagement module-state resets from destroy()"
decisions:
  - "clients/users listeners retained in init() — clientsData still used by proposal create/edit modal (line 1299); usersData referenced by private pill helpers that remain in the file"
  - "Module-level engagement state variables (clientsData, usersData, selectedPersonnel, currentEngagementType) left in place — their references in private helpers are harmless and removal was optional per plan"
metrics:
  duration: "< 5 minutes"
  completed: "2026-05-19"
  tasks_completed: 1
  files_modified: 1
---

# Phase 87.1 Plan 03: D-04 Proposals Tab Refocus Summary

One-liner: Stripped the Create Engagement form from proposals.js render(), init(), and destroy() so the Proposals tab shows only the Approval Queue and Dashboard mount points.

## Tasks Completed

| Task | Description | Commit | Status |
|------|-------------|--------|--------|
| 1 | Remove engagement section from render(), window registrations from init(), window deletes from destroy() | 6828610 | DONE |

## Three Edits Made

**EDIT 1 — render() cleanup:**
Removed the entire `<section id="new-engagement-section">` block (previously lines 65–181, approximately 116 lines). The retained render() output is:
- `<h2>Proposals</h2>` heading
- `<section id="proposal-queue-mount" ...>` — Phase 89 mount point
- `<section id="proposal-dashboard-mount" ...>` — Phase 87 mount point

**EDIT 2 — destroy() cleanup:**
Removed these 10 lines from destroy():
- `delete window.submitNewEngagement;`
- `delete window.handleEngagementTypeChange;`
- `delete window.proposalSelectPersonnel;`
- `delete window.proposalRemovePersonnel;`
- `delete window.proposalShowPersonnelDropdown;`
- `delete window.proposalFilterPersonnelDropdown;`
- `clientsData = [];`
- `usersData = [];`
- `selectedPersonnel = [];`
- `currentEngagementType = 'project';`

All Phase 87 and Phase 89 cleanup lines were left untouched.

**EDIT 3 — init() cleanup:**
Removed these 6 lines from init():
- `window.submitNewEngagement = submitNewEngagement;`
- `window.handleEngagementTypeChange = handleEngagementTypeChange;`
- `window.proposalSelectPersonnel = proposalSelectPersonnel;`
- `window.proposalRemovePersonnel = proposalRemovePersonnel;`
- `window.proposalShowPersonnelDropdown = proposalShowPersonnelDropdown;`
- `window.proposalFilterPersonnelDropdown = proposalFilterPersonnelDropdown;`

All Phase 87 and Phase 89 window function registrations left untouched.

## Line Count Change

`1 file changed, 140 deletions(-)` — confirmed by git commit output.

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. This plan is pure deletion; no data rendering or UI stubs introduced.

## Threat Surface Scan

No new network endpoints, auth paths, or trust boundaries introduced. Removal of the engagement section reduces attack surface (T-87.1-03-01 and T-87.1-03-02 mitigations applied: only the specified six functions removed; Phase 87/89 cleanup verified intact).

## Self-Check: PASSED

- `app/views/proposals.js` — modified and committed (6828610)
- `grep -c "new-engagement-section" proposals.js` returns 0
- `grep -c "submitNewEngagement" proposals.js` returns 0
- `grep -c "window.proposalSelectPersonnel\s*=" proposals.js` returns 0
- `grep -c "delete window.proposalSelectPersonnel" proposals.js` returns 0
- `grep "proposal-queue-mount" proposals.js` returns 2 results (render + init mount activation)
- `grep "proposal-dashboard-mount" proposals.js` returns multiple results (render + init + renderProposalDashboard)
- `grep "openProposalDetail" proposals.js` returns multiple results (Phase 87 cleanup intact)
