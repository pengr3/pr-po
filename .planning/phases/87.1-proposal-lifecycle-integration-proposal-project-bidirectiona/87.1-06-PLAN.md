---
phase: 87.1
plan: 06
type: execute
wave: 5
depends_on:
  - 87.1-04
  - 87.1-05
files_modified:
  - app/router.js
  - app/auth.js
  - index.html
  - app/views/proposals.js
autonomous: true
requirements:
  - D-02

must_haves:
  truths:
    - "The #/proposals route no longer exists in router.js"
    - "The /proposals hard role gate block is removed from router.js navigate()"
    - "The Proposals nav link is absent from index.html desktop and mobile nav"
    - "auth.js no longer shows/hides a Proposals nav link"
    - "Navigating directly to #/proposals in the browser redirects or shows access denied (not the proposals page)"
  artifacts:
    - path: "app/router.js"
      provides: "Router without /proposals route"
      contains: "no '/proposals' key in routes object"
    - path: "index.html"
      provides: "Nav without Proposals link"
      contains: "no data-route=proposals anchor"
    - path: "app/auth.js"
      provides: "Auth without proposals visibility block"
      contains: "no querySelectorAll proposals"
  key_links:
    - from: "index.html nav"
      to: "no data-route=proposals"
      via: "deleted anchor tags"
      pattern: "data-route.*proposals"
    - from: "router.js routes"
      to: "no /proposals entry"
      via: "deleted route object"
      pattern: "'/proposals'"
---

<objective>
Retire the standalone Proposals top-nav tab. Remove the /proposals route from router.js, remove the Proposals link from index.html (desktop + mobile), and remove the proposals visibility block from auth.js. This is the last wave — executed after all consuming surfaces (home sub-tabs, inline cards) are working.

Purpose: Users no longer need the standalone Proposals tab; all proposal access is through home sub-tabs and detail view inline cards. Removing the nav link and route prevents confusion and dead navigation.
Output: router.js, auth.js, index.html, and proposals.js stubs cleaned up.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-CONTEXT.md
@.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove /proposals route and gate from router.js</name>
  <files>app/router.js</files>
  <read_first>
    - app/router.js lines 108-120 — the /proposals route entry in the routes object (research cites lines 112-116)
    - app/router.js lines 285-305 — the /proposals hard role gate block in navigate() (research cites lines 291-298)
    - app/router.js lines 1-30 — routePermissionMap and surrounding context to confirm /proposals is NOT in it
  </read_first>
  <action>
    Make two targeted deletions in app/router.js:

    1. In the routes object: Delete the entire '/proposals': { name: 'Proposals', load: () => import('./views/proposals.js'), title: '...' } entry (lines ~112-116). Remove trailing comma from adjacent entry if needed to keep valid JavaScript object syntax.

    2. In the navigate() function: Delete the entire if (path === '/proposals') { ... } block (lines ~291-298) including the role check, console.warn, showAccessDenied() call, and return statement.

    Do NOT change the routePermissionMap — /proposals was intentionally absent from it per the comment at lines 22-26.
  </action>
  <verify>
    <automated>grep -c "'/proposals'\|\"/proposals\"" app/router.js</automated>
  </verify>
  <acceptance_criteria>
    - "grep -c \"'/proposals'\" app/router.js" returns 0 (route entry gone)
    - "grep -c \"path === '/proposals'\" app/router.js" returns 0 (hard gate gone)
    - app/router.js is valid JavaScript — no syntax errors (open DevTools console after loading the app; check for SyntaxError in router.js)
  </acceptance_criteria>
  <done>router.js has no /proposals route and no /proposals hard gate</done>
</task>

<task type="auto">
  <name>Task 2: Remove Proposals nav links from index.html and auth.js visibility block</name>
  <files>index.html, app/auth.js</files>
  <read_first>
    - index.html lines 80-90 — desktop nav links area (research cites Proposals at line 84)
    - index.html lines 106-115 — mobile nav items (research cites Proposals at line 110)
    - app/auth.js lines 415-430 — authenticated branch proposals visibility block (research cites lines 418-424)
    - app/auth.js lines 458-468 — unauthenticated branch proposals hide block (research cites lines 462-464)
  </read_first>
  <action>
    index.html: Delete the desktop nav anchor: <a href="#/proposals" class="nav-link" data-route="proposals">Proposals</a> (line ~84). Delete the mobile nav anchor: <a href="#/proposals" class="mobile-nav-item" data-route="proposals" onclick="mobileNavClick(event, this)">Proposals</a> (line ~110).

    app/auth.js authenticated branch (lines ~418-424): Delete the entire block that queries [data-route="proposals"] links and shows/hides them based on user role. This is the block that starts with something like: const proposalsLinks = document.querySelectorAll('[data-route="proposals"]');

    app/auth.js unauthenticated branch (lines ~462-464): Delete the block that hides proposals links for unauthenticated users.

    No new auth.js code is needed — the home sub-nav is role-gated in home.js via getHomeSubTabConfig(), not via auth.js nav visibility.
  </action>
  <verify>
    <automated>grep -c "data-route=\"proposals\"\|data-route='proposals'" index.html</automated>
  </verify>
  <acceptance_criteria>
    - "grep -c 'data-route=\"proposals\"' index.html" returns 0 (both nav links removed)
    - "grep -c 'proposals' index.html" returns 0 OR only contains comment references (no active anchor elements)
    - "grep -c \"querySelectorAll.*proposals\|data-route.*proposals\" app/auth.js" returns 0 (visibility block removed)
    - Manual: Load the app as super_admin. Confirm "Proposals" link is absent from both desktop nav and mobile nav. Navigate to #/proposals directly — should show 404 or fallback route, not the proposals page.
  </acceptance_criteria>
  <done>Proposals nav links removed from index.html; auth.js proposals visibility block removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→router | Removing the route means any bookmark or direct navigation to #/proposals will no longer load the proposals view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.1-06-01 | Information Disclosure | Stale #/proposals bookmarks | accept | Router will fall through to default route (home or 404) when /proposals is not found — no data leakage, just navigation failure that is expected |
| T-87.1-06-02 | Elevation of Privilege | proposals.js still accessible as a module | accept | proposals.js is now a pure module with no route; it cannot be navigated to; it is imported by home.js and detail views which have their own role gates |
</threat_model>

<verification>
Manual: Log in as any role. Confirm no Proposals link in desktop nav. Confirm no Proposals link in mobile nav. Type #/proposals in address bar — confirm it does not load the old proposals page. Confirm home page Overview/Engagements/Proposals sub-tabs still work. Confirm inline cards in project/service detail still work. No console errors.
</verification>

<success_criteria>
grep confirms /proposals route absent from router.js. grep confirms no data-route="proposals" in index.html. Manual navigation to #/proposals does not show the proposals page. All other Phase 87.1 functionality remains working.
</success_criteria>

<output>
After completion, create .planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-06-SUMMARY.md
</output>
