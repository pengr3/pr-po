---
phase: 87.1
plan: 06
type: execute
wave: 4
depends_on:
  - 87.1-01
files_modified:
  - app/views/home.js
  - styles/views.css
autonomous: true
requirements:
  - PROP-INT-05
must_haves:
  truths:
    - "Home page shows a pill-tab sub-nav with Overview and Proposals buttons when the current user has at least one active proposal relevant to their role"
    - "Home page renders as before (no sub-nav) when the user has no active proposals or is Finance/Procurement Staff"
    - "Proposals sub-tab content shows proposals grouped by stage using a local renderHomeProposalsDashboard() function; the table Actions column links to #/proposals (no Submit button)"
    - "Switching between Overview and Proposals tabs shows/hides the .quick-stats section and #homeProposalsContent respectively"
    - "init() uses getDocs (not onSnapshot) for proposals — no listener is pushed to statsListeners"
    - "destroy() deletes window.switchHomeTab"
  artifacts:
    - path: "app/views/home.js"
      provides: "Home Proposals sub-tab with role-filtered grouped proposals view"
      contains: "filterProposalsForUser"
    - path: "styles/views.css"
      provides: ".home-sub-nav CSS classes"
      contains: ".home-sub-nav-tab--active"
  key_links:
    - from: "home.js init()"
      to: "filterProposalsForUser()"
      via: "called after getDocs resolves to filter proposals by current user role"
      pattern: "filterProposalsForUser"
    - from: "window.switchHomeTab"
      to: ".quick-stats and #homeProposalsContent"
      via: "toggles display property on both elements"
      pattern: "switchHomeTab"
---

<objective>
Add a Proposals sub-tab to the home dashboard. When the current user has active proposals relevant to their role, a pill-tab nav bar appears below the navigation cards. Switching to the Proposals tab hides the .quick-stats section and shows a grouped-by-stage proposals overview. The sub-tab uses getDocs (one-time read) rather than a live listener.

Purpose: D-05 gives all roles with active proposals a quick overview of the proposal pipeline from the home screen.
Output: home.js with sub-nav HTML in render(), getDocs proposals fetch in init(), filterProposalsForUser(), renderHomeProposalsDashboard(), switchHomeTab(); styles/views.css with .home-sub-nav block.
</objective>

<execution_context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-PATTERNS.md
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-UI-SPEC.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\pr-po\.planning\phases\87.1-proposal-lifecycle-integration-proposal-project-bidirectiona\87.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sub-nav HTML to render() and .home-sub-nav CSS to views.css</name>
  <files>app/views/home.js, styles/views.css</files>
  <read_first>
    - app/views/home.js (lines 60-103 for render() — identify where `.quick-stats` div is rendered to find the insertion point)
    - styles/views.css (last 30 lines to find the append point; also grep for `.finance-sub-nav` to see the pill-tab pattern being mirrored)
  </read_first>
  <action>
    EDIT 1 — home.js render(): Add the sub-nav HTML and proposals content div immediately before the closing `</div>` of the `.hero-section` div (after the `.quick-stats` div). Both new elements start hidden — they are shown by init() only if active proposals exist. The HTML to add (per UI-SPEC Surface 4 and PATTERNS.md render() sub-nav section):

    ```
    <div class="home-sub-nav" style="display:none;" id="homeSubNav">
        <div class="home-sub-nav-tabs">
            <button class="home-sub-nav-tab home-sub-nav-tab--active"
                    onclick="window.switchHomeTab('overview')" id="homeTabOverview">Overview</button>
            <button class="home-sub-nav-tab"
                    onclick="window.switchHomeTab('proposals')" id="homeTabProposals">Proposals</button>
        </div>
    </div>
    <div id="homeProposalsContent" style="display:none;padding:0 1rem;"></div>
    ```

    Do not change anything in the existing navigation-cards div or the quick-stats div.

    EDIT 2 — styles/views.css: Append the .home-sub-nav CSS block from UI-SPEC.md (the exact CSS from the "New CSS" code block in Surface 4). Precede with comment `/* Phase 87.1 D-05 — Home dashboard Proposals sub-tab */`. The six selectors to add: `.home-sub-nav`, `.home-sub-nav-tabs`, `.home-sub-nav-tabs::-webkit-scrollbar`, `.home-sub-nav-tab`, `.home-sub-nav-tab:hover`, `.home-sub-nav-tab--active`, `.home-sub-nav-tab--active:hover`.
  </action>
  <verify>
    <automated>grep -n "home-sub-nav\|homeSubNav\|homeProposalsContent" /c/Users/Admin/Roaming/pr-po/app/views/home.js && grep -c "home-sub-nav" /c/Users/Admin/Roaming/pr-po/styles/views.css</automated>
  </verify>
  <done>
    home.js render() contains `id="homeSubNav"` and `id="homeProposalsContent"` divs both with `style="display:none"`. styles/views.css contains at least 6 occurrences of `home-sub-nav` (one per selector).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add getDocs proposals fetch, filterProposalsForUser(), renderHomeProposalsDashboard(), and switchHomeTab() to home.js</name>
  <files>app/views/home.js</files>
  <read_first>
    - app/views/home.js (lines 1-10 for current imports; lines 108-121 for init() body; lines 192-206 for destroy())
    - app/views/proposals.js (lines 625-800 for getProposalStatusBadge, renderAgeBadge, renderStageGroupCard — to understand the stage group card output format so the home version can link to #/proposals rather than calling window.openProposalDetail())
  </read_first>
  <action>
    Four edits to home.js:

    EDIT 1 — imports: Update the firebase.js import to add `getDocs`: change `import { db, collection, query, where, onSnapshot } from '../firebase.js'` to `import { db, collection, query, where, onSnapshot, getDocs } from '../firebase.js'`.

    EDIT 2 — init(): After the existing `loadStats(mode);` call, add the proposals sub-tab logic. Use a try/catch block. Inside:
    - `const proposalsSnap = await getDocs(collection(db, 'proposals'));`
    - Build allProposals array from the snapshot (map to `{ id: d.id, ...d.data() }`).
    - Call `const filtered = filterProposalsForUser(allProposals);`
    - Filter active: `const active = filtered.filter(p => p.status !== 'client_approved' && p.status !== 'loss');`
    - If `active.length > 0`: get `document.getElementById('homeSubNav')` and `document.getElementById('homeProposalsContent')`. If subNav exists, set `subNav.style.display = ''`. If proposalsContent exists, set `proposalsContent.innerHTML = renderHomeProposalsDashboard(active)`.
    - Catch any error and log with `[Home] Proposals sub-tab load error:` prefix.
    - After the try/catch: `window.switchHomeTab = switchHomeTab;`

    EDIT 3 — New `function filterProposalsForUser(allProposals)` placed after the getDashboardMode() function. Implementation per D-05 role filter logic in CONTEXT.md and PATTERNS.md filterProposalsForUser() section:
    - `const user = window.getCurrentUser?.(); if (!user) return [];`
    - `const role = user.role;`
    - `if (role === 'super_admin') return allProposals;`
    - `if (role === 'operations_admin' || role === 'operations_user')`: filter to proposals where `(p.parent_collection || 'projects') === 'projects'`. Additionally filter by assigned project codes: `const assignedCodes = window.getAssignedProjectCodes?.();` — if `assignedCodes === null` (all_projects), return all project proposals; else filter by `assignedCodes.includes(p.project_code)`.
    - `if (role === 'services_admin' || role === 'services_user')`: filter to proposals where `(p.parent_collection || 'projects') === 'services'`, then filter by `window.getAssignedServiceCodes?.()` in the same pattern.
    - All other roles (finance, procurement_staff): `return []`.

    EDIT 4 — New `function renderHomeProposalsDashboard(proposals)` placed after filterProposalsForUser(). This function renders a grouped-by-stage view for the home sub-tab. It must NOT use the imported renderStageGroupCard from proposals.js (which links to window.openProposalDetail). Instead, implement a LOCAL version:
    - Group proposals by status using the same STAGE_ORDER from proposals.js: `['pending_internal', 'pending_client', 'for_revision', 'client_approved', 'draft']` with labels matching proposals.js STAGE_ORDER labels: "Pending Internal Approval", "Pending Client Review", "For Revision", "Client Approved", "Draft".
    - For each group, if proposals.length === 0, skip.
    - Build a table card with the same header structure as renderStageGroupCard. Table columns: PROP ID, Title, Project/Service, Client, Amount, Age in Stage, Actions.
    - In the Actions column, render `<a href="#/proposals" style="color:#1a73e8;font-size:13px;font-weight:600;text-decoration:none;">View →</a>` (NOT a button that calls window.openProposalDetail). Home is read-only overview per D-05 deferred section.
    - Apply overdue left border (3px solid #f59e0b) on the row when the same overdue logic applies (compute age inline from current_status_since).
    - Use `escapeHTML`, `formatCurrency` imported from utils.js.
    - Use `getProposalStatusBadge` and `renderAgeBadge` imported from proposals.js — these are now exported per Plan 01.
    - If proposals.length === 0 (no active proposals to display after filtering), return an empty-state paragraph: `<p style="color:#64748b;text-align:center;padding:2rem;">No active proposals</p>`.

    EDIT 5 — New `function switchHomeTab(tab)` placed after renderHomeProposalsDashboard(). Implementation per PATTERNS.md switchHomeTab() section:
    - Gets `overviewSection = document.querySelector('.quick-stats')`, `proposalsContent = document.getElementById('homeProposalsContent')`, `overviewBtn = document.getElementById('homeTabOverview')`, `proposalsBtn = document.getElementById('homeTabProposals')`.
    - If tab === 'proposals': hide overviewSection (display:none), show proposalsContent (display:''), remove active class from overviewBtn, add to proposalsBtn.
    - Else: show overviewSection, hide proposalsContent, add active class to overviewBtn, remove from proposalsBtn.

    EDIT 6 — destroy(): Add `delete window.switchHomeTab;` before the statsListeners cleanup.

    Note on imports from proposals.js: The home.js import of `getProposalStatusBadge` and `renderAgeBadge` requires adding an import line. Add after the firebase.js import:
    `import { getProposalStatusBadge, renderAgeBadge } from './proposals.js';`
    Also add `escapeHTML, formatCurrency` to the utils.js import if not already present — check the current utils.js import first.
  </action>
  <verify>
    <automated>grep -n "filterProposalsForUser\|renderHomeProposalsDashboard\|switchHomeTab\|getDocs\|from.*proposals" /c/Users/Admin/Roaming/pr-po/app/views/home.js</automated>
  </verify>
  <done>
    home.js contains: `import { getProposalStatusBadge, renderAgeBadge } from './proposals.js'`, `getDocs` in the firebase.js import, `filterProposalsForUser` function definition, `renderHomeProposalsDashboard` function definition, `switchHomeTab` function definition and `window.switchHomeTab = switchHomeTab` in init(), and `delete window.switchHomeTab` in destroy().
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| home.js getDocs → proposals collection → client-side filter | One-time read of all proposals; filtered client-side by role and assigned codes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87.1-06-01 | Information Disclosure | Finance/Procurement role sees proposal sub-tab | mitigate | filterProposalsForUser returns [] for finance and procurement_staff roles; active.length will be 0 so the sub-nav is never shown. |
| T-87.1-06-02 | Information Disclosure | Operations/Services user sees proposals for wrong projects | mitigate | filterProposalsForUser filters by assignedCodes from window.getAssignedProjectCodes / getAssignedServiceCodes. If those helpers return null (all_projects flag), all proposals in the collection for that type are shown — intentional for admins. |
| T-87.1-06-03 | Denial of Service | getDocs failure blocks home stats | mitigate | The proposals getDocs is in a separate try/catch block; statsListeners (the existing onSnapshot stats) run independently before and are not affected by a proposals query failure. |
</threat_model>

<verification>
1. `grep -c "home-sub-nav" /c/Users/Admin/Roaming/pr-po/styles/views.css` returns >= 6.
2. `grep "homeSubNav" /c/Users/Admin/Roaming/pr-po/app/views/home.js` returns the render() div with `display:none`.
3. `grep "filterProposalsForUser" /c/Users/Admin/Roaming/pr-po/app/views/home.js` returns the function definition and its call in init().
4. `grep "getDocs" /c/Users/Admin/Roaming/pr-po/app/views/home.js` returns the import and the call in init().
5. `grep "delete window.switchHomeTab" /c/Users/Admin/Roaming/pr-po/app/views/home.js` returns a result in destroy().
</verification>

<success_criteria>
- Home page renders the sub-nav when the user has active proposals filtered to their role
- Home page renders identically to pre-Phase-87.1 when no active proposals exist for the user
- Overview tab shows the .quick-stats section; Proposals tab shows the grouped-by-stage dashboard
- The Proposals sub-tab table Actions column links to #/proposals (no Submit for Approval button)
- getDocs is used — no new onSnapshot listener added to statsListeners
- destroy() deletes window.switchHomeTab
</success_criteria>

<output>
After completion, create `.planning/phases/87.1-proposal-lifecycle-integration-proposal-project-bidirectiona/87.1-06-SUMMARY.md` with the functions added, lines modified in render(), and confirmation of the CSS classes added to views.css.
</output>
