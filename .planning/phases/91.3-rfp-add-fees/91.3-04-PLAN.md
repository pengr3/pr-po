---
phase: 91.3-rfp-add-fees
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified: [styles/components.css]
autonomous: true
requirements: [SC-3, SC-4]
must_haves:
  truths:
    - "Fee add-chips (.btn-ghost-fee) render in the fee-accent palette: bg #f0f9ff, color #0369a1, 1px dashed #bae6fd"
    - "Fee rows (.fee-row) use the 1fr 150px 32px grid and amount inputs show a ₱ prefix via .fee-amount-wrap::before"
    - "The 'incl. fees' cue pill (.cue) and the fee tinted card use the fee accent, distinct from status colors"
    - "Invalid-fee state (.fee-amount.err / .field-err) shows the destructive #ef4444 border/ring/message"
    - "All interactive fee controls meet the 44px touch target on viewports <768px with no horizontal overflow"
  artifacts:
    - path: "styles/components.css"
      provides: ".btn-ghost-fee, .fee-row, .fee-amount-wrap, .cue, .fee-card, .field-err, mobile 44px overrides"
      contains: ".btn-ghost-fee"
  key_links:
    - from: "procurement.js fee chips/rows (Plan 02)"
      to: ".btn-ghost-fee / .fee-row / .fee-amount-wrap"
      via: "shared CSS class"
      pattern: ".btn-ghost-fee"
    - from: "finance.js cue pill (Plan 05)"
      to: ".cue"
      via: "shared CSS class"
      pattern: ".cue"
---

<objective>
Ship the fee-control CSS — chips, fee rows, ₱-prefixed amount inputs, running-total card, the downstream "incl. fees" cue pill, invalid-fee states, and the <768px touch overrides — per UI-SPEC §A/§B/§C/§Color. **This plan OWNS styles/components.css for Wave 2** — no other plan touches it. The classes here are consumed by Plan 02 (modal) and Plan 05 (downstream cue/breakdown).

Purpose: SC #3 (fee breakdown surfaced without breaking layouts) + SC #4 (mobile-friendly, 44px targets, no overflow).
Output: ~10 new CSS rules in the fee-accent namespace + mobile overrides inside the existing media block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91.3-rfp-add-fees/91.3-UI-SPEC.md
@.planning/phases/91.3-rfp-add-fees/91.3-PATTERNS.md

<interfaces>
<!-- Existing classes (do NOT redefine): .btn (:225), .btn-primary (:246), .btn-outline (:298), .form-control,
     .modal/.modal-content/.modal-footer. New fee classes LAYER on top. -->
<!-- Mobile media query opens at components.css:1451 (@media max-width:768px). Base rules end ~:1445.
     Place base fee rules just BEFORE the RESPONSIVE comment block (~:1447); place mobile overrides
     INSIDE the existing :1451 media block. -->

Fee-accent palette (UI-SPEC §Color — the ONLY net-new token):
  text #0369a1 · bg #f0f9ff · border #bae6fd · base hue #0ea5e9
Destructive (errors): border/ring #ef4444 · alert text #991b1b on #fef2f2
Layout tracks: fee-row grid 1fr 150px 32px · touch target min 44px (<768px)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add base fee-control CSS classes (chips, rows, ₱ prefix, total card, cue, error states)</name>
  <files>styles/components.css</files>
  <read_first>
    - styles/components.css (the file being modified) — read :225 (.btn), :246 (.btn-primary), :298 (.btn-outline), and the end of the base rules (~:1445) so the new rules are appended BEFORE the RESPONSIVE comment at :1447
    - .planning/phases/91.3-rfp-add-fees/91.3-UI-SPEC.md § "Component Inventory" A/B/C/D and § Color (exact hexes)
    - .planning/phases/91.3-rfp-add-fees/91.3-PATTERNS.md § F "styles/components.css — fee control styles"
  </read_first>
  <action>
Insert these rules just before the `/* RESPONSIVE */` comment (~line 1447). Use the EXACT hexes. Add a section banner comment `/* ===== Phase 91.3 — RFP Fees ===== */`.

```css
/* ===== Phase 91.3 — RFP Fees ===== */

/* Progressive-disclosure add-chips */
.btn-ghost-fee {
    padding: 6px 12px;
    background: #f0f9ff;
    color: #0369a1;
    border: 1px dashed #bae6fd;
    border-radius: 9999px;
    font-size: 0.8rem;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.15s ease;
}
.btn-ghost-fee:hover { background: #e0f2fe; }

/* Fee row: label / amount / remove-× */
.fee-row {
    display: grid;
    grid-template-columns: 1fr 150px 32px;
    gap: 8px;
    align-items: center;
    margin-top: 8px;
}

/* ₱ prefix on amount inputs (non-interactive) */
.fee-amount-wrap { position: relative; display: block; }
.fee-amount-wrap::before {
    content: "\20B1"; /* ₱ */
    position: absolute;
    left: 10px;
    top: 50%;
    transform: translateY(-50%);
    color: #64748b;
    font-size: 0.875rem;
    pointer-events: none;
}
.fee-amount {
    text-align: right;
    padding-left: 22px;
    font-variant-numeric: tabular-nums;
}

/* Invalid-fee state (positive-only, D-05) */
.fee-amount.err {
    border-color: #ef4444;
    box-shadow: 0 0 0 3px rgba(239, 68, 68, 0.12);
}
.field-err {
    margin-top: 4px;
    font-size: 0.74rem;
    color: #ef4444;
}
.fee-footer-alert {
    margin-top: 0.75rem;
    padding: 8px 12px;
    background: #fef2f2;
    color: #991b1b;
    border-radius: 6px;
    font-size: 0.875rem;
}

/* Running-total / tinted fee card */
.fee-card {
    border: 1px dashed #bae6fd;
    background: #f0f9ff;
    border-radius: 10px;
    padding: 0.75rem;
}
.fee-card .grand .amt {
    font-size: 1.3rem;
    font-weight: 800;
    font-variant-numeric: tabular-nums;
}

/* Downstream "incl. fees" cue pill (tables/cards) */
.cue {
    display: inline-block;
    margin-left: 6px;
    padding: 1px 7px;
    background: #f0f9ff;
    color: #0369a1;
    border: 1px solid #bae6fd;
    border-radius: 9999px;
    font-size: 0.62rem;
    font-weight: 700;
    letter-spacing: 0.02em;
    cursor: pointer;
    vertical-align: middle;
}
.cue:focus-visible { outline: 2px solid #0ea5e9; outline-offset: 1px; }

/* Cue tooltip (desktop hover + JS tap-toggle .cue-open) */
.cue-tip {
    position: absolute;
    z-index: 50;
    width: 210px;
    background: #0f172a;
    color: #e2e8f0;
    border-radius: 8px;
    padding: 10px 12px;
    font-size: 0.75rem;
    box-shadow: 0 6px 18px rgba(0, 0, 0, 0.25);
    display: none;
}
.cue:hover + .cue-tip,
.cue.cue-open + .cue-tip { display: block; }

/* PO-summary fee sub-note */
.fee-subnote { font-size: 0.7rem; color: #0369a1; }
```
The `.cue` must NOT reuse any status-pill color — fee accent only (UI-SPEC §Color). Do NOT redefine `.btn`/`.btn-primary`/`.btn-outline`/`.form-control`.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('styles/components.css','utf8'); ['.btn-ghost-fee','.fee-row','.fee-amount-wrap','.cue','.field-err','.fee-card'].forEach(s=>{if(!c.includes(s)){throw new Error('missing '+s)}}); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The node -e check prints `OK` (all 6 classes present)
    - components.css contains `#0369a1` and `#f0f9ff` and `#bae6fd` (fee accent)
    - components.css contains `grid-template-columns: 1fr 150px 32px`
    - components.css contains `content: "\20B1"` (₱ prefix)
    - components.css contains `box-shadow: 0 0 0 3px rgba(239, 68, 68, 0.12)` (.fee-amount.err ring)
    - `.btn`, `.btn-primary`, `.btn-outline`, `.form-control` NOT redefined (grep their selectors — count unchanged from before edit)
  </acceptance_criteria>
  <done>Base fee classes added in the fee-accent palette before the RESPONSIVE block; existing button/form classes untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add <768px mobile overrides (44px touch targets, chip wrap, no overflow)</name>
  <files>styles/components.css</files>
  <read_first>
    - styles/components.css (the file being modified) — read inside the @media (max-width: 768px) block (:1451 onward), esp. the .modal-footer column-reverse stack (:1517) and existing 44px patterns
    - .planning/phases/91.3-rfp-add-fees/91.3-UI-SPEC.md § "Responsive Behavior" table (44px contract, chip wrap, no horizontal overflow at 375–390px)
  </read_first>
  <action>
Inside the existing `@media (max-width: 768px)` block (opens at :1451), append these overrides (do NOT open a new media block):

```css
    /* Phase 91.3 — fee controls mobile */
    .btn-ghost-fee { min-height: 44px; }
    #feeChips { display: flex; flex-wrap: wrap; gap: 8px; }
    .fee-amount { min-height: 44px; }
    .fee-row button { min-height: 44px; min-width: 44px; }
    .fee-row { grid-template-columns: 1fr 130px 44px; }
    .cue {
        min-height: 44px;
        display: inline-flex;
        align-items: center;
        /* keep the visible pill small; expand the hit area via padding */
        padding-top: 0; padding-bottom: 0;
    }
    .cue-tip { width: calc(100vw - 48px); max-width: 240px; }
```
This satisfies the 44px touch contract and keeps the fee-row grid usable at 375–390px (amount column shrinks to 130px, × column grows to a 44px tap target). No horizontal overflow (SC #4). The modal shell already inherits `.modal-content width:95%` and the footer column-reverse stack from existing rules — do not duplicate those.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('styles/components.css','utf8'); const m=c.slice(c.indexOf('@media (max-width: 768px)')); ['min-height: 44px','#feeChips','flex-wrap: wrap'].forEach(s=>{if(!m.includes(s)){throw new Error('missing in media: '+s)}}); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The node -e check prints `OK` (44px + #feeChips + flex-wrap present inside the media block)
    - components.css contains exactly ONE `@media (max-width: 768px)` block (grep count == 1 — no new media block opened)
    - components.css mobile section contains `.fee-row button { min-height: 44px; min-width: 44px; }`
  </acceptance_criteria>
  <done>All interactive fee controls meet 44px on mobile; chips wrap; cue tap-target expanded; no new media block opened; no horizontal overflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | CSS-only plan. No input, no I/O, no DOM scripting, no Firestore. No trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (n/a) | — | styles only | accept | Pure presentation. The fee accent is visually distinct from the destructive red and from status pills (UI-SPEC §Color) so an invalid-fee error can never be mistaken for an additive fee — a usability safeguard, not a security control. No auth/endpoint/rules surface. |

No Firestore rules update needed (no data layer change).
</threat_model>

<verification>
- Both node -e checks print OK.
- Existing `.btn`/`.btn-primary`/`.btn-outline`/`.form-control`/`.modal*` rules unchanged.
- Exactly one `@media (max-width: 768px)` block (no duplicate).
- Fee accent (#0369a1/#f0f9ff/#bae6fd) used only on fee classes, never on status or error classes.
</verification>

<success_criteria>
- SC #3: cue pill / breakdown card / fee rows styled without altering existing layout classes.
- SC #4: 44px touch targets + chip wrap + no horizontal overflow at 375–390px.
</success_criteria>

<output>
After completion, create `.planning/phases/91.3-rfp-add-fees/91.3-04-SUMMARY.md` listing the final class names (.btn-ghost-fee, .fee-row, .fee-amount-wrap/.fee-amount, .cue, .cue-tip, .fee-card, .field-err, .fee-footer-alert, .fee-subnote) so Plans 02/05/06 reference the exact names.
</output>
