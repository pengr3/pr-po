---
phase: 91.3-rfp-add-fees
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [app/views/procurement.js]
autonomous: true
requirements: [SC-1, SC-2, SC-4, SC-5]
must_haves:
  truths:
    - "All three RFP creation modals (PO-tranche, Delivery-Fee, TR) render a 4-section shell: 1 Reference, 2 Base Amount, 3 Fees, 4 Payment Details"
    - "The Fees section starts with three add-chips (+ Transfer fee, + Cash-out fee, + Misc fee) and a 'No fees added' hint (progressive disclosure)"
    - "Tapping a fee chip reveals a removable fee row; × clears and restores the chip"
    - "A live running total (Base / Fees / Grand total) updates as fees are typed and shows once any fee exists"
    - "All three RFP write paths persist transfer_fee, cash_out_fee, misc_fees, and total_with_fees on the rfps doc; base stays in amount_requested"
    - "Procurement scoreboard fills (getPOPaymentFill, getTRPaymentFill, procurement deriveRFPStatus, inline DF checks) track against the fee-inclusive total"
  artifacts:
    - path: "app/views/procurement.js"
      provides: "4-section RFP modals with progressive-disclosure fee controls, fee write, scoreboard conversion"
      contains: "transfer_fee"
  key_links:
    - from: "RFP modal fee inputs"
      to: "recomputeRFPTotal"
      via: "oninput/onchange handler"
      pattern: "recomputeRFPTotal"
    - from: "submitRFP / submitTRRFP / submitDeliveryFeeRFP"
      to: "rfps doc fee fields"
      via: "addDoc rfpDoc"
      pattern: "total_with_fees"
    - from: "getPOPaymentFill / getTRPaymentFill"
      to: "getRFPTotal"
      via: "import from utils.js"
      pattern: "getRFPTotal"
---

<objective>
Overhaul all three RFP creation modals in procurement.js into the locked 4-section shell with progressive-disclosure fee controls + live running total, persist the fees on the rfps doc, and convert the procurement-side scoreboard fills to the fee-inclusive total. **This plan OWNS app/views/procurement.js for Wave 2** — no other Wave-2 plan touches it.

Purpose: Deliver SC #1 (fee controls on all 3 RFP paths — D-01), the write half of SC #2 (fees persisted, base distinct), and the mobile-friendly overhaul (SC #4).
Output: 3 overhauled modals, ~6 new window-bound fee handlers, fee fields in 3 rfpDoc literals, 4 scoreboard read-site conversions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91.3-rfp-add-fees/91.3-CONTEXT.md
@.planning/phases/91.3-rfp-add-fees/91.3-PATTERNS.md
@.planning/phases/91.3-rfp-add-fees/91.3-UI-SPEC.md
@.planning/phases/91.3-rfp-add-fees/91.3-01-SUMMARY.md

<interfaces>
<!-- Plan 01 shipped these in app/utils.js — import them, do NOT recompute fee math inline. -->
From app/utils.js:
```javascript
export function getRFPFees(rfp): { transfer, cashOut, misc[], feesTotal }
export function getRFPTotal(rfp): number   // fee-inclusive grand total, legacy-safe
export function getRFPFeeBreakdown(rfp): { base, lines[], feesTotal, grandTotal, hasFees }
export const RFP_FEE_FIELDS: string[]
```
procurement.js already imports from '../utils.js' at line 8 (formatCurrency, escapeHTML, etc.) — ADD getRFPTotal (and getRFPFees if needed) to that import line.

LOCKED field shape (from Plan 01 SUMMARY):
- amount_requested (number) = BASE, unchanged
- transfer_fee (number), cash_out_fee (number) — optional, omit/0 when not applied
- misc_fees: Array<{label:string, amount:number}> — zero-or-more
- total_with_fees (number) = base + all fees, persisted at creation

Existing in-modal toggle/clear analog to mirror (procurement.js):
```javascript
function showAltBank() { section.style.display='block'; btn.style.display='none'; }      // :1499
function removeAltBank() { /* hide, clear inputs */ btn.style.display='block'; }          // :1507
window.showAltBank = showAltBank;                                                          // :1505
```
Repeatable-row analog: addLineItem() (:3952) — createElement('tr'), build innerHTML, append.
Live-amount analog: updateRFPAmount(poDocId) (:1485) — fired by tranche select onchange (:1128).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build the 4-section shell + progressive-disclosure fee controls in all 3 RFP modals</name>
  <files>app/views/procurement.js</files>
  <read_first>
    - app/views/procurement.js (the file being modified) — read openRFPModal (:1057, HTML :1102–1204), openDeliveryFeeRFPModal (:1222, HTML :1243–1338), openTRRFPModal (:1351), and the alt-bank handlers (:1499–1517)
    - .planning/phases/91.3-rfp-add-fees/91.3-PATTERNS.md § A "procurement.js — RFP creation modals" (verbatim current-state excerpts + gotchas)
    - .planning/phases/91.3-rfp-add-fees/91.3-UI-SPEC.md § "Component Inventory → A. Sectioned RFP creation modal" and § Typography + § Color + § Copywriting Contract
  </read_first>
  <action>
Restructure each of the three modal HTML literals (openRFPModal, openDeliveryFeeRFPModal, openTRRFPModal) into FOUR numbered sections in this exact order: **1 REFERENCE → 2 BASE AMOUNT → 3 FEES → 4 PAYMENT DETAILS** (D-09/D-13/D-15). Reuse the existing `.modal`/`.modal-content`/`.modal-header`/`.modal-body`/`.modal-footer` shell and inline-style convention verbatim (do NOT switch to a framework). Keep `max-width:520px`, keep the modal `id="rfpModal"`, keep the existing field IDs (`rfpAmount`, `rfpInvoiceNumber`, `rfpPaymentMode`, `rfpBankFields`, `altBankSection`, etc.) so `toggleRFPBankFields()`/`showAltBank()` keep working.

**Section header markup** (use for each of the 4 sections — class `.section-head` is styled in Plan 04, but inline-style fallback is fine since Plan 04 runs in parallel; render BOTH the class AND the inline styles so it looks right even before CSS loads):
```html
<div class="section-head" style="display:flex;align-items:center;gap:8px;margin:1.25rem 0 0.75rem;">
  <span style="width:20px;height:20px;border-radius:9999px;background:#1a73e8;color:#fff;font-size:11px;font-weight:700;display:flex;align-items:center;justify-content:center;">3</span>
  <span style="font-size:0.72rem;font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:#64748b;">FEES <span style="color:#94a3b8;text-transform:none;letter-spacing:0;font-weight:400;">— optional</span></span>
  <span style="flex:1;height:1px;background:#e5e7eb;"></span>
</div>
```
Section titles (numbered): `1 REFERENCE`, `2 BASE AMOUNT`, `3 FEES — optional`, `4 PAYMENT DETAILS`.

**Section 2 (Base Amount):** PO modal keeps the tranche `<select id="rfpTrancheSelect" onchange="window.updateRFPAmount('${poDocId}')">` + read-only `#rfpAmount` (label `Amount Requested` with muted suffix `(auto from tranche %)`). Delivery-Fee/TR modals render section 2 as a SINGLE read-only base line (no tranche select) — the compact 3-section variant is rejected (D-15).

**Section 3 (Fees) — progressive disclosure (D-13, sketch 001-B), identical in all 3 modals:**
```html
<div id="feeChips" style="display:flex;flex-wrap:wrap;gap:8px;">
  <button type="button" id="chipTransfer" class="btn-ghost-fee" onclick="window.revealFee('transfer')"
    style="padding:6px 12px;min-height:36px;background:#f0f9ff;color:#0369a1;border:1px dashed #bae6fd;border-radius:9999px;font-size:0.8rem;font-weight:600;cursor:pointer;">+ Transfer fee</button>
  <button type="button" id="chipCashout" class="btn-ghost-fee" onclick="window.revealFee('cashout')" style="...same...">+ Cash-out fee</button>
  <button type="button" id="chipMisc" class="btn-ghost-fee" onclick="window.addMiscFeeRow()" style="...same...">+ Misc fee</button>
</div>
<div id="feeNoneHint" style="margin-top:8px;font-size:0.8rem;font-style:italic;color:#94a3b8;">No fees added — disbursement equals the base amount.</div>

<!-- hidden fixed-fee rows, revealed on chip tap -->
<div id="rowTransfer" class="fee-row" style="display:none;grid-template-columns:1fr 150px 32px;gap:8px;align-items:center;margin-top:8px;">
  <label class="fld" style="font-size:0.875rem;font-weight:600;color:#475569;">Transfer Fee</label>
  <span class="fee-amount-wrap" style="position:relative;"><input type="text" id="feeTransferAmount" class="form-control fee-amount" inputmode="decimal" placeholder="0.00" oninput="window.recomputeRFPTotal()" style="text-align:right;padding-left:20px;"></span>
  <button type="button" onclick="window.removeFee('transfer')" aria-label="Remove transfer fee" style="background:none;border:none;color:#64748b;font-size:1.1rem;cursor:pointer;min-height:32px;">×</button>
</div>
<div id="rowCashout" class="fee-row" style="display:none;...same shape, id=feeCashoutAmount, removeFee('cashout')...">...</div>

<!-- repeatable misc rows -->
<div id="miscFeesBody"></div>

<!-- running total — hidden until a fee exists -->
<div id="rfpTotalBlock" style="display:none;margin-top:1rem;padding-top:0.75rem;border-top:1px dashed #bae6fd;">
  <div style="display:flex;justify-content:space-between;font-size:0.875rem;"><span>Base amount</span><span id="rtBase" style="font-variant-numeric:tabular-nums;">₱0.00</span></div>
  <div style="display:flex;justify-content:space-between;font-size:0.875rem;"><span>Fees</span><span id="rtFees" style="font-variant-numeric:tabular-nums;color:#0369a1;">₱0.00</span></div>
  <div class="grand" style="display:flex;justify-content:space-between;align-items:baseline;margin-top:6px;"><span style="font-weight:700;">Grand total</span><span id="rtGrand" class="amt" style="font-size:1.3rem;font-weight:800;font-variant-numeric:tabular-nums;">₱0.00</span></div>
</div>
```

**Footer:** add a left-aligned live total + conditional `incl. fees` pill before the Discard/Submit buttons:
```html
<span id="rfpFooterTotal" style="margin-right:auto;font-size:0.875rem;font-weight:600;">Total <span id="rfpFooterAmt" style="font-variant-numeric:tabular-nums;">₱0.00</span> <span id="rfpFooterPill" style="display:none;padding:2px 8px;border-radius:9999px;font-size:0.62rem;font-weight:700;background:#f0f9ff;color:#0369a1;border:1px solid #bae6fd;">incl. fees</span></span>
```
Keep the existing `Discard RFP` label on the PO/DF modals (UI-SPEC Assumption 3 — avoid regression). Keep the Submit button text `Submit RFP`.

**Handlers (add as module functions, hang EVERY one on window per CLAUDE.md):**
- `revealFee(kind)` — show `rowTransfer`/`rowCashout`, hide the matching chip, then call `recomputeRFPTotal()` and `syncFeeSectionVisibility()`. Mirror `showAltBank()`.
- `removeFee(kind)` — hide the row, clear its `.fee-amount` input value, restore the chip, then `recomputeRFPTotal()` + `syncFeeSectionVisibility()`. Mirror `removeAltBank()`.
- `addMiscFeeRow()` — append a row to `#miscFeesBody` (createElement('div'), class `fee-row`, grid `1fr 150px 32px`) with a label input (`class="fee-misc-label form-control"`, placeholder `Label (e.g. Notary, Courier)`) + amount input (`class="fee-misc-amount fee-amount form-control"`, `inputmode="decimal"`, placeholder `0.00`, `oninput="window.recomputeRFPTotal()"`) + `×` button calling `window.removeMiscFeeRow(this)`. **Auto-focus the new label input.** Mirror `addLineItem()` (:3952).
- `removeMiscFeeRow(btn)` — `btn.closest('.fee-row').remove()`, then `recomputeRFPTotal()` + `syncFeeSectionVisibility()`.
- `recomputeRFPTotal()` — read base from the computed tranche math (PO: re-derive `tranche.percentage/100*poTotal`, NOT the formatted input string; DF/TR: the base value stored when the modal opened). Sum ONLY visible/valid fee amounts: transfer (if `rowTransfer` visible), cashout (if visible), each `#miscFeesBody .fee-misc-amount`. Write `#rtBase`/`#rtFees`/`#rtGrand` and `#rfpFooterAmt` via `formatCurrency()`. Use `parseFloat(val)||0`; ignore NaN/empty for the sum.
- `syncFeeSectionVisibility()` — if any fee row is visible/present AND has a value, show `#rfpTotalBlock` + `#rfpFooterPill`, hide `#feeNoneHint`; else hide block/pill, show hint.

Bind all: `window.revealFee = revealFee;` etc. (follow the `window.showAltBank = showAltBank;` pattern at :1505).

**Mobile (SC #4):** the modal uses generic `.modal*` classes so existing <768px overrides apply automatically. The fee chips already have `flex-wrap:wrap`. Do not add `.rfp-modal`-specific overrides here (Plan 04 owns CSS).
  </action>
  <verify>
    <automated>node --check app/views/procurement.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/procurement.js` exits 0
    - procurement.js contains `revealFee`
    - procurement.js contains `addMiscFeeRow`
    - procurement.js contains `recomputeRFPTotal`
    - procurement.js contains `window.recomputeRFPTotal`
    - procurement.js contains `fee-misc-amount` and `fee-misc-label`
    - procurement.js contains `id="feeChips"` and `id="rfpTotalBlock"`
    - procurement.js contains `+ Transfer fee` and `+ Cash-out fee` and `+ Misc fee`
    - procurement.js contains `No fees added — disbursement equals the base amount.`
    - procurement.js contains `btn-ghost-fee` (chip class for Plan 04 styling)
    - All 4 section titles present: grep `REFERENCE`, `BASE AMOUNT`, `FEES`, `PAYMENT DETAILS`
  </acceptance_criteria>
  <done>All 3 modals render the 4-section shell with progressive-disclosure chips, removable rows, repeatable misc rows, and a live running total; all handlers window-bound; node --check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Persist fee fields in all 3 rfpDoc writes + convert procurement scoreboard fills</name>
  <files>app/views/procurement.js</files>
  <read_first>
    - app/views/procurement.js (the file being modified) — read submitRFP rfpDoc (:1601–1632), submitTRRFP rfpDoc (:1715–1747), submitDeliveryFeeRFP rfpDoc (:1814–1844); getPOPaymentFill (:566), getTRPaymentFill (:605), procurement deriveRFPStatus (:550), inline DF checks (:5819, :7426)
    - .planning/phases/91.3-rfp-add-fees/91.3-PATTERNS.md § B "rfpDoc write assemblies" and § "PRIMARY REGRESSION SURFACE → Read side — procurement.js (scoreboard fills)"
    - .planning/phases/91.3-rfp-add-fees/91.3-01-SUMMARY.md (locked field names)
  </read_first>
  <action>
**Part A — read fees from the modal + write them (all 3 submit handlers):**

Add a shared helper `readRFPFeesFromModal()` (module function) that reads the modal fee inputs by CSS class/ID and returns `{ transfer_fee, cash_out_fee, misc_fees, feesTotal }`:
- transfer = `rowTransfer` visible ? `parseFloat(#feeTransferAmount.value)||0` : 0
- cashOut = `rowCashout` visible ? `parseFloat(#feeCashoutAmount.value)||0` : 0
- misc = loop `document.querySelectorAll('#miscFeesBody .fee-row')`, read `.fee-misc-label`/`.fee-misc-amount` by class (CLAUDE.md DOM rule — NEVER data attributes), keep only rows where `amount > 0` (empty/≤0 omitted, D-05), build `{ label: label.trim(), amount }`.
- Returns the three fields ready to spread, plus feesTotal.

In each of the three `rfpDoc` literals (submitRFP, submitTRRFP, submitDeliveryFeeRFP), after computing the server-side base (`submitRFP` re-derives `amountRequested = tranche.percentage/100*poTotal` at :1596 — do NOT trust the input string), call `readRFPFeesFromModal()` and add the SAME keys to all three docs (each RFP carries its own per-RFP fee set, no PO-level aggregate — D-02):
```javascript
const { transfer_fee, cash_out_fee, misc_fees, feesTotal } = readRFPFeesFromModal();
const rfpDoc = {
    ...existing fields...,
    amount_requested: amountRequested,   // BASE — unchanged
    transfer_fee,                         // 0 when not applied
    cash_out_fee,
    misc_fees,                            // [] when none
    total_with_fees: amountRequested + feesTotal,   // persisted grand total (D-11)
    ...payment_records: [], etc...
};
```
Only persist fees that were entered/valid; transfer_fee/cash_out_fee = 0 when their row is hidden; misc_fees omits empty rows. Do NOT reorder the addDoc/generateRFPId sequence (D-12). Do not interfere with the duplicate-tranche / one-RFP early-return guards (:1580, :1234, :1680).

**Part B — convert procurement scoreboard fills to the fee-inclusive total:**

Add `getRFPTotal` to the existing `'../utils.js'` import at procurement.js:8.

Replace every `rfp.amount_requested` comparison in the scoreboard math with `getRFPTotal(rfp)`:
- `getPOPaymentFill` (:583, :584): `totalRequested += getRFPTotal(rfp);` and `if (paid < getRFPTotal(rfp)) allFullyPaid = false;`
- `getTRPaymentFill` (:617): `if (paid < getRFPTotal(rfp)) allFullyPaid = false;`
- procurement `deriveRFPStatus` (:550–555): `const total = getRFPTotal(rfp); if (totalPaid >= total && total > 0) return 'Fully Paid';`
- inline DF scoreboard checks (:5819, :7426): `dfTotalPaid >= getRFPTotal(dfRfp)` (verify each is an RFP doc, not a collectible — these are DF RFPs, in scope).

Do NOT touch any `coll.amount_requested` (collectibles) — out of scope per PATTERNS.md § E gotcha.
  </action>
  <verify>
    <automated>node --check app/views/procurement.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/procurement.js` exits 0
    - procurement.js contains `total_with_fees:` written in 3 places (grep -c, expect ≥3 — the 3 rfpDoc literals)
    - procurement.js contains `readRFPFeesFromModal`
    - procurement.js import line contains `getRFPTotal`
    - procurement.js contains `getRFPTotal(rfp)` in getPOPaymentFill and getTRPaymentFill (grep `getRFPTotal` count ≥4)
    - procurement.js misc read uses class selector: grep `.fee-misc-amount` and `.fee-misc-label` present in read code (querySelectorAll)
    - NO `coll.amount_requested` lines were changed (grep `coll.amount_requested` still present unchanged)
  </acceptance_criteria>
  <done>All 3 rfpDoc writes persist transfer_fee/cash_out_fee/misc_fees/total_with_fees with base in amount_requested; all 4 procurement scoreboard read sites use getRFPTotal; node --check passes; collectibles untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → Firestore (`rfps` write) | Fee amounts and the Misc free-text label are user input crossing into the persisted rfps doc and re-rendered downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91.3-01 | Tampering | fee amount inputs | mitigate | readRFPFeesFromModal coerces with `parseFloat(val)||0` and omits ≤0; the persisted total_with_fees is recomputed server-side from the re-derived base + parsed fees (NOT trusted from the formatted input string). Full positive-only enforcement + disabled-Submit gate ships in Plan 06; this plan stores only validated/coerced numbers. |
| T-91.3-02 | Tampering/XSS | Misc fee `label` (free text) | mitigate | Label is stored as-is (a string). Any place this plan ECHOES the label back into the modal DOM (e.g. if a row re-renders) MUST use escapeHTML (already imported in procurement.js:8). Downstream rendering sanitization is enforced in Plan 05. No innerHTML interpolation of the raw label without escapeHTML. |
| T-91.3-03 | Elevation of privilege | rfps write | accept | No new collection, no new write path — fees are additive numeric/string fields on the existing `rfps` collection the user can already write. Existing Firestore Security Rules govern `rfps` writes unchanged. **No rules update required** (additive fields on an existing writable collection). Stated explicitly per the security gate. |

No new auth, no new endpoints, no role changes.
</threat_model>

<verification>
- `node --check app/views/procurement.js` passes (SC #5).
- Existing RFP create / cancel / ID-generation guards unaltered — duplicate-tranche guard (:1580), DF/TR one-RFP guards (:1234/:1680) still fire before write; generateRFPId/addDoc order preserved (D-12).
- A no-fee RFP writes transfer_fee:0, cash_out_fee:0, misc_fees:[], total_with_fees === amount_requested (legacy-equivalent).
- collectibles `coll.amount_requested` sites untouched.
</verification>

<success_criteria>
- SC #1: fee controls on all 3 RFP creation paths.
- SC #2 (write half): fees persisted, base distinct in amount_requested, grand total in total_with_fees.
- SC #4: 4-section shell uses generic .modal* classes, chips flex-wrap, no .rfp-modal overrides needed.
- SC #5: node --check passes; no regression to existing guards/ID-gen.
</success_criteria>

<output>
After completion, create `.planning/phases/91.3-rfp-add-fees/91.3-02-SUMMARY.md` listing the new modal IDs/classes and the readRFPFeesFromModal contract so Plan 06 (validation) consumes them.
</output>
