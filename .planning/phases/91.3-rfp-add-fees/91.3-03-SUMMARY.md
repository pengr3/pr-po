---
phase: 91.3-rfp-add-fees
plan: 03
subsystem: payments
tags: [rfp, fees, finance, payables, payment-records]

requires:
  - phase: 91.3-01
    provides: getRFPTotal, getRFPFees (utils.js)
provides:
  - "finance.js compute layer (deriveRFPStatus, derivePOSummary, balance calcs, record-payment math) returns fee-inclusive totals"
  - "derivePOSummary adds fees on top of the authoritative poTotalAmount branch (fees never dropped)"
affects: [91.3-05]

tech-stack:
  added: []
  patterns:
    - "All payables/status/payment math routes through getRFPTotal/getRFPFees (single source, legacy-safe)"

key-files:
  created: []
  modified:
    - "app/views/finance.js — import += getRFPTotal/getRFPFees; deriveRFPStatus, derivePOSummary (both branches), 4 balance calcs, openRecordPaymentModal (fully-paid + default amount), submitPaymentRecord (recorded amount + NOTIF-16)"

key-decisions:
  - "derivePOSummary computes feesTotal once across regularRFPs and adds it to the authoritative poTotalAmount branch (PO total_amount excludes fees) — the trickiest edit per PATTERNS § C"
  - "Recorded payment amount = getRFPTotal(rfp) so a single full payment clears base + fees (D-11); deterministic from frozen-at-creation fee fields (T-91.3-05 accepted)"

patterns-established:
  - "Functions returning fee-inclusive totals that Plan 05 renders against: deriveRFPStatus, derivePOSummary, openRecordPaymentModal"

requirements-completed: [SC-2, SC-5]

duration: ~10min
completed: 2026-06-03
---

# Phase 91.3 Plan 03: Finance Compute Layer → Fee-Inclusive Summary

**finance.js payables totals, RFP status derivation, and record-payment math now track the fee-inclusive grand total (getRFPTotal); derivePOSummary adds fees on top of the authoritative PO total so they never silently drop.**

## Functions now returning fee-inclusive totals (Plan 05 renders against these)
- `deriveRFPStatus(rfp)` — Fully-Paid threshold uses `getRFPTotal(rfp)`
- `derivePOSummary(rfpList, poTotalAmount)` — `totalAmount` = `poTotalAmount + feesTotal` (authoritative) OR `Σ getRFPTotal(r)` (fallback)
- `openRecordPaymentModal` — `isFullyPaid` test + default `paymentAmount` value
- `submitPaymentRecord` — recorded `amount` + NOTIF-16 `becomesFullyPaid`
- Balance calcs feeding buildRFPCard / renderRFPTable / buildPOTrancheSubCard / renderPOSummaryTable rows (math only)

## Performance
- **Duration:** ~10 min
- **Completed:** 2026-06-03
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Both derivePOSummary branches fee-aware (10 getRFPTotal call sites total)
- Legacy RFPs unchanged (getRFPTotal falls back to base)
- Collectibles (`deriveCollectibleStatus`, all `coll.amount_requested`) untouched; reference "RFP Amount" tile (:438) left for Plan 05

## Task Commits
1. **Task 1 + Task 2** — `9796303` (feat) — both tasks in app/views/finance.js (single atomic commit, same file)

## Files Created/Modified
- `app/views/finance.js` — compute-layer conversions

## Decisions Made
- Fees added on top of authoritative `poTotalAmount` (the silent-drop trap).
- Recorded payment amount = fee-inclusive total (D-11).

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None — `node --check` passes; all acceptance greps pass; the :438 display tile and collectibles confirmed untouched.

## User Setup Required
None.

## Next Phase Readiness
- Plan 05 (Wave 3) renders the cue pill / breakdown card / PO-summary note against these fee-inclusive numbers.

---
*Phase: 91.3-rfp-add-fees*
*Completed: 2026-06-03*
