---
context: phase
phase: 91.3-rfp-add-fees
task: 6
total_tasks: 6
status: awaiting_uat
last_updated: 2026-06-03T22:45:00.000Z
---

# Phase 91.3 (RFP Fees) — Paused: code-complete, awaiting browser UAT

> No `blocking` constraints. The phase is fully executed and structurally verified —
> the only remaining work is human browser UAT, then bookkeeping to mark complete.
> (Blocking-constraints section intentionally omitted so resumption is not gated.)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| gsd-sdk assumed present | `gsd-sdk` is NOT installed on this machine; `gsd-sdk query ...` fails. Confirmed this session (`command -v gsd-sdk` → MISSING). | advisory | Use git directly + inline execution. Do not call any `gsd-sdk query`. |
| Worktree/subagent execution | 2 stale **LOCKED** agent worktrees (`agent-a4222795...`, `agent-ab5541979...`) make `isolation="worktree"` / parallel subagent execution unsafe. | advisory | Execute inline sequentially on the main `v3.3` tree (Phase-98 precedent). |
| Duplicate DOM ids across the two finance tables | The same RFP renders in BOTH the RFP table and the PO-summary sub-table; keying an element id on `rfp_id` produces invalid duplicate ids. | advisory | Plan 05 used a module counter (`_feeCueSeq`) for cue ids — reuse that pattern for any per-RFP element id in finance.js. |

<current_state>
Phase 91.3 (RFP Fees) is **fully executed** — all 6 plans committed atomically inline on `v3.3`. Verifier verdict was `human_needed` (zero-build SPA, no UI test harness). ROADMAP row reads `91.3 | 6/6 | Executed — UAT pending`; the phase checkbox (ROADMAP line ~248) is intentionally NOT ticked yet. 8 browser-UAT items are persisted in `91.3-HUMAN-UAT.md`.
</current_state>

<completed_work>

All 6 plans done (feat + docs commit each):
- Plan 01: shared fee helpers in utils.js — `f18540f`
- Plan 02: procurement.js 4-section RFP modals + fee write + scoreboard — `7af7c62`
- Plan 03: finance.js compute layer fee-inclusive — `9796303`
- Plan 04: components.css fee CSS + mobile 44px — `9d70060`
- Plan 05: finance.js display (cue + breakdown card + roll-up) — `dfa9b83`
- Plan 06: procurement.js validation + blocked-guard dimming — `1fa9a7c`
- Verification + UAT persisted + tracking — `40d8106`

Structural verification PASS: `node --check` on all 3 JS modules, CSS braces balanced, cross-plan import contract intact, pre-write guards + generateRFPId + NOTIF-08 + collectibles all untouched, legacy-safe (no fee fields → total === base).
</completed_work>

<remaining_work>

1. **Run the 8 browser-UAT items** in `91.3-HUMAN-UAT.md` (PO/DF/TR fee add → persist → payables surfacing; legacy unchanged; positive-only validation; blocked-guard dimming; mobile 375–390px; payment clears base+fees).
2. **On pass** → tick Phase 91.3 in ROADMAP (checkbox line ~248 + progress-table row), flip `91.3-HUMAN-UAT.md`/`91.3-VERIFICATION.md` status, update STATE.md. **On fail** → `/gsd-plan-phase 91.3 --gaps`.
3. Optional non-blocking polish: tighten the RFP modal micro-text type ladder (UI-SPEC FLAG).
</remaining_work>

<decisions_made>

- Inline sequential execution on `v3.3` (gsd-sdk unavailable + locked worktrees) — Phase-98 precedent.
- Field strategy (a): `amount_requested` = BASE; additive `transfer_fee`/`cash_out_fee`/`misc_fees`/`total_with_fees`.
- `derivePOSummary` adds fees ON TOP of the authoritative `poTotalAmount` branch (else they silently drop).
- Plan 05 cue ids via module counter; Plan 06 TR blocked-Submit also `data-locked`; Plan 04 reused the existing :768px media block.
</decisions_made>

<blockers>
- None technical. Pending human action: browser UAT (8 items). ⚠ RFP creation writes to LIVE Firebase (no staging) — use a disposable PO/TR or delete test RFPs.
</blockers>

## Required Reading (in order)
1. `.planning/phases/91.3-rfp-add-fees/91.3-HUMAN-UAT.md` — the 8 UAT items to run
2. `.planning/phases/91.3-rfp-add-fees/91.3-VERIFICATION.md` — what was verified structurally + per-SC assessment
3. `.planning/phases/91.3-rfp-add-fees/91.3-0[1-6]-SUMMARY.md` — per-plan contracts + scoped deviations

## Infrastructure State
- Branch `v3.3`, tracking `origin/v3.3`. All 91.3 work committed. (Untracked `.claude/worktrees/`, `86.9-*` debug files, `debug-diag-86.9.js` are pre-existing, unrelated.)
- No running servers/watchers. Production = live Firebase (`clmc-procurement`); no staging.

<context>
The phase is code-complete and confidence is high (every acceptance grep + node --check passed, guards preserved, legacy-safe). Mental model: the whole phase hangs on one shared contract — `getRFPTotal(rfp)` (utils.js) — that every write/read/display routes through. Resuming = open the app, exercise the 8 UAT flows, then do the trivial bookkeeping to close the phase.
</context>

<next_action>
Start with: open `91.3-HUMAN-UAT.md`, launch the app (`python -m http.server 8000`), and run UAT item 1 (PO-tranche RFP: add Transfer + Cash-out + Misc, watch the live total, submit, confirm Finance → Payables shows the fee-inclusive total + "incl. fees" cue + breakdown card).
</next_action>
