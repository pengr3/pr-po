---
phase: 105-service-plan-gantt-parity
plan: 03
type: execute
wave: 3
depends_on: ["105-02"]
files_modified:
  - app/views/service-detail.js
autonomous: false
requirements: []
must_haves:
  truths:
    - "service-detail.js shows a 'Service Plan' summary card (task count, % complete, highlights) mirroring the project-detail plan card (D-01)"
    - "The card's 'Open Plan' CTA links to #/services/{code}/plan and is disabled when the service has no service_code (Phase 78 clientless block)"
    - "The card updates live via a service_tasks onSnapshot scoped by service_id (D-02)"
    - "The new tasks listener is torn down in BOTH init() (same-view re-init) and destroy() (no orphaned listener)"
    - "The deferred iterations strip is NOT included on the card (D-05)"
  artifacts:
    - path: "app/views/service-detail.js"
      provides: "buildServicePlanCardHtml + computeServiceProgress + ensureTasksListener (card stats) + card insertion"
      contains: "buildServicePlanCardHtml"
  key_links:
    - from: "service-detail.js ensureTasksListener"
      to: "service_tasks collection"
      via: "onSnapshot query filtered by service_id"
      pattern: "where\\('service_id'"
    - from: "service-detail.js Service Plan card Open Plan CTA"
      to: "#/services/{code}/plan route"
      via: "anchor href"
      pattern: "/plan"
---

<objective>
Add the "Service Plan" summary card to `app/views/service-detail.js`, mirroring the project-detail plan card: task count, % complete, health badge, overdue/upcoming highlights, and an "Open Plan" CTA (disabled when the service has no `service_code`). The card is fed by a NEW `service_tasks` onSnapshot — service-detail has no tasks listener today, so this plan adds the listener plus its teardown in BOTH init() (same-view re-init gotcha) and destroy() (D-01, D-05).

Purpose: Surface the service plan from the service detail page and give a one-click entry point (mirror project-detail's plan card slot).
Output: card builder + progress compute + tasks listener wired into service-detail.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-service-plan-gantt-parity/105-CONTEXT.md
@.planning/phases/105-service-plan-gantt-parity/105-PATTERNS.md
@.planning/phases/105-service-plan-gantt-parity/105-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Analog functions in project-detail.js to mirror (105-PATTERNS.md anchors): -->
project-detail.js:3925-4005  buildPlanCardHtml()           -> mirror as buildServicePlanCardHtml()
project-detail.js:3860       computeProjectProgress(tasks) -> mirror as computeServiceProgress(tasks)
project-detail.js:325-343    ensureTasksListener()         -> mirror (onSnapshot feeding the card)
project-detail.js:42         currentProjectProgress default state -> mirror as currentServiceProgress

<!-- computeProjectProgress return shape (consumed by the card; mirror verbatim, no field swaps inside): -->
{ taskCount, leafCount, doneCount, percentComplete, health, overdueCount, overdueMore, overdueTasks[], upcomingTasks[], recentDone }

<!-- service-detail.js insertion + lifecycle anchors (105-PATTERNS.md): -->
  line ~776-777  proposalCardHtml construction      -> build planCardHtml = buildServicePlanCardHtml() here
  line ~956      render tail: ${proposalCardHtml} then ${_buildServiceJournalPanelHtml(...)} -> insert ${planCardHtml} between them
  line 134-146   init() per-service listener teardown block (re-init gotcha) -> add tasks-listener teardown here
  line 248-297   destroy() unsubscribe block -> add tasks-listener teardown here

<!-- CSS: reuse the .project-plan-card class VERBATIM (generic, not project-namespaced). The card
     DOM class stays project-plan-card; only the element id differs (servicePlanCard) for JS targeting. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add computeServiceProgress + buildServicePlanCardHtml + tasks-listener state</name>
  <files>app/views/service-detail.js</files>
  <read_first>
    - app/views/project-detail.js:3860-4005 (computeProjectProgress + buildPlanCardHtml — the exact analogs to mirror)
    - app/views/project-detail.js:42 (currentProjectProgress default-state declaration)
    - app/views/service-detail.js:1-60 (module-state block — where to add currentServiceProgress / currentTasks / currentTasksListenerUnsub)
    - app/views/service-detail.js:770-790 (proposalCardHtml construction — where to build planCardHtml)
  </read_first>
  <action>
    Add to service-detail.js module state: `currentTasks = []`, `currentTasksListenerUnsub = null`, and `currentServiceProgress` initialized to the default shape (mirror project-detail.js:42): `{ taskCount: 0, leafCount: 0, doneCount: 0, percentComplete: 0, health: 'on-track', overdueCount: 0, overdueMore: 0, overdueTasks: [], upcomingTasks: [], recentDone: null }`.

    Mirror `computeProjectProgress` (project-detail.js:3860) verbatim as `computeServiceProgress(tasks)` — it is a pure function over a tasks array; NO internal identifier swaps needed except the function name.

    Mirror `buildPlanCardHtml` (project-detail.js:3925-4005) as `buildServicePlanCardHtml()` with these swaps: `currentProjectProgress`→`currentServiceProgress`; `currentProject`→`currentService`; `project_code`→`service_code`; `planUrl` → `` `#/services/${encodeURIComponent(currentService?.service_code || '')}/plan` ``; heading "Project Plan"→"Service Plan"; element id `projectPlanCard`→`servicePlanCard`; CTA disabled-when-null title "No project code"→"No service code". KEEP the CSS class `project-plan-card` verbatim (do NOT rename to service-plan-card). **DELETE** the `iterStrip` / `currentIterationLabel` block (analog 3930-3933) — iterations are deferred to 105.1 (D-05). Keep the empty-state CTA, progress bar, health badge, and overdue/upcoming/last-done sections verbatim.

    Build `const planCardHtml = buildServicePlanCardHtml();` near the proposalCardHtml construction (~line 776-777).
  </action>
  <verify>
    <automated>node --check app/views/service-detail.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/service-detail.js` exits 0.
    - `grep -c "function buildServicePlanCardHtml" app/views/service-detail.js` returns 1.
    - `grep -c "function computeServiceProgress" app/views/service-detail.js` returns 1.
    - `grep -c "id=\"servicePlanCard\"\|id='servicePlanCard'" app/views/service-detail.js` returns 1.
    - `grep -c "project-plan-card" app/views/service-detail.js` >= 1 (CSS class reused verbatim).
    - `grep -c "#/services/\${encodeURIComponent(currentService" app/views/service-detail.js` returns 1 (Open Plan CTA links to the service plan route).
    - `grep -c "No service code" app/views/service-detail.js` >= 1 (clientless-disabled CTA copy).
    - `grep -ci "iterStrip\|currentIterationLabel" app/views/service-detail.js` returns 0 (iterations strip NOT ported, D-05).
    - `grep -c "Service Plan" app/views/service-detail.js` >= 1 (heading swapped).
  </acceptance_criteria>
  <done>computeServiceProgress + buildServicePlanCardHtml + module state present; CTA links to the service plan and disables on null service_code; iterations strip omitted; node --check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the service_tasks listener (init re-init + destroy) and insert the card</name>
  <files>app/views/service-detail.js</files>
  <read_first>
    - app/views/project-detail.js:325-343 (ensureTasksListener — the onSnapshot that feeds + re-renders the card in place)
    - app/views/service-detail.js:128-148 (init() per-service listener teardown block — the documented same-view re-init gotcha)
    - app/views/service-detail.js:248-297 (destroy() unsubscribe block)
    - app/views/service-detail.js:950-962 (render tail — the insertion slot between ${proposalCardHtml} and the journal panel)
  </read_first>
  <action>
    Mirror `ensureTasksListener` (project-detail.js:325-343) as a service-detail function with swaps: guard `if (currentTasksListenerUnsub) return; if (!currentService || !currentService.id) return;`; onSnapshot `query(collection(db,'service_tasks'), where('service_id','==',currentService.id))`; on snapshot rebuild `currentTasks`, set `currentServiceProgress = computeServiceProgress(currentTasks)`, then re-render the card in place by id `servicePlanCard` (replaceWith the freshly built `buildServicePlanCardHtml()` first element child). Assign the unsubscriber to `currentTasksListenerUnsub`.

    Call `ensureTasksListener()` after `currentService` is set in init() (mirror where project-detail calls it post-load).

    Teardown — service-detail's router skips destroy() on same-view nav, so the listener must be torn down in BOTH places:
    - init() re-init block (~134-146): add `if (currentTasksListenerUnsub) { try { currentTasksListenerUnsub(); } catch(e){} currentTasksListenerUnsub = null; }` and reset `currentTasks = []`, following the existing per-listener teardown pattern there.
    - destroy() block (~259-262): add the same unsubscribe + null.

    Insert the card into the render tail (~line 956): place `${planCardHtml}` right after `${proposalCardHtml}` and before `${_buildServiceJournalPanelHtml(currentService)}`.

    Do NOT add a new CSS file or class. Do NOT subscribe to the users collection (assignee/personnel handled in the plan view, not the card).
  </action>
  <verify>
    <automated>node --check app/views/service-detail.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check app/views/service-detail.js` exits 0.
    - `grep -c "function ensureTasksListener" app/views/service-detail.js` returns 1.
    - `grep -c "where('service_id', '==', currentService.id)\|where('service_id','==',currentService.id)" app/views/service-detail.js` >= 1.
    - `grep -c "collection(db, 'service_tasks')\|collection(db,'service_tasks')" app/views/service-detail.js` >= 1.
    - `grep -c "ensureTasksListener()" app/views/service-detail.js` >= 2 (the call site + at least one reference; call is reached after currentService is set).
    - `currentTasksListenerUnsub` appears in BOTH the init() teardown block AND the destroy() block: `grep -c "currentTasksListenerUnsub" app/views/service-detail.js` >= 3 (declaration + assign + init-teardown + destroy-teardown — at minimum 3).
    - `grep -c "\${planCardHtml}" app/views/service-detail.js` returns 1 (card inserted into render tail).
    - The insertion is between proposalCardHtml and the journal panel: manual confirm `${proposalCardHtml}` precedes `${planCardHtml}` precedes `_buildServiceJournalPanelHtml` in the template literal.
  </acceptance_criteria>
  <done>service_tasks listener feeds + live-re-renders the card; torn down in both init re-init and destroy; card inserted in the render tail; node --check passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The "Service Plan" summary card on service-detail.js (task count, % complete, health badge, overdue/upcoming highlights, "Open Plan" CTA) fed by a live `service_tasks` onSnapshot, with teardown in both init() re-init and destroy().</what-built>
  <how-to-verify>
    1. Open a service detail page that has service_tasks (create a couple in the plan first). Confirm the "Service Plan" card shows the correct task count, % complete, health badge, and overdue/upcoming highlights — matching the plan.
    2. Open a service with NO tasks → the card shows the empty-state ("No tasks yet. Open the plan to get started.") + enabled "Open Plan" CTA.
    3. Click "Open Plan" → navigates to `#/services/{service_code}/plan`.
    4. With the detail page open, edit a task's progress in another tab/window → the card updates live (onSnapshot re-render in place).
    5. Open a service WITHOUT a service_code (if any) → "Open Plan" is disabled (greyed, "No service code" title).
    6. Navigate service → service (same view) → confirm the prior service's card data does NOT leak (re-init teardown works); navigate away to another view and back → no duplicate/orphaned listener, zero console errors.
    7. Confirm NO iterations/save-game strip appears on the card (deferred to 105.1).

    Reply "approved" or list failures.
  </how-to-verify>
  <resume-signal>Type "approved" when the card UAT passes, or describe issues for a `/gsd-plan-phase 105 --gaps` pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → service_tasks read (card stats) | the card's onSnapshot reads service_tasks; authorized by the Plan-01 read rule (isActiveUser, JS-scoped by service_id) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-09 | Information Disclosure | card service_tasks onSnapshot | mitigate | query scoped `where('service_id','==',currentService.id)` — the card only reads the current service's tasks; read rule is isActiveUser (low-sensitivity scheduling data, mirrors project_tasks D-06) |
| T-105-10 | Denial of Service | orphaned tasks listener on same-view nav | mitigate | listener torn down in BOTH init() re-init block AND destroy() (router skips destroy on service→service nav); prevents stale-overwrite + listener leak (mirrors the documented service-detail same-view gotcha) |
| T-105-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (read-only card on existing collection). N/A. |
</threat_model>

<verification>
- `node --check app/views/service-detail.js` exits 0.
- buildServicePlanCardHtml + computeServiceProgress + ensureTasksListener present; card inserted in render tail.
- service_tasks listener scoped by service_id; torn down in both init re-init and destroy.
- iterations strip omitted (D-05).
- Browser UAT (human gate) covers stats accuracy, empty-state, Open Plan nav, live update, clientless-disable, same-view teardown.
</verification>

<success_criteria>
- "Service Plan" card mirrors the project-detail plan card with live stats + Open Plan CTA (D-01).
- CTA disabled when no service_code (Phase 78 clientless pattern).
- No iterations strip (D-05); no project-side file touched (D-01).
</success_criteria>

<output>
Create `.planning/phases/105-service-plan-gantt-parity/105-03-SUMMARY.md` when done.
</output>
