---
phase: 71-revamp-expense-modal-into-financials-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/expense-modal.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Modal header reads 'Financial Breakdown: {name}' instead of 'Expense Breakdown: {name}'"
    - "All other modal behavior is unchanged (tabs, scoreboards, CSV export, close button)"
    - "Internal symbols (showExpenseBreakdownModal, #expenseBreakdownModal, .expense-tab, _switchExpenseBreakdownTab, _exportExpenseBreakdownCSV, _closeExpenseBreakdownModal, _toggleExpenseCategory) are NOT renamed"
  artifacts:
    - path: "app/expense-modal.js"
      provides: "Modal title string updated"
      contains: "Financial Breakdown:"
  key_links:
    - from: "app/expense-modal.js"
      to: "modal h3 title"
      via: "template literal in showExpenseBreakdownModal"
      pattern: "Financial Breakdown:"
---

<objective>
Rename the user-visible modal header from "Expense Breakdown" to "Financial Breakdown" per D-09 of CONTEXT.md.

Purpose: First step of revamping the modal's identity. User-facing rename only — no internal symbol changes, no behavioral changes.

Output: A one-line edit in `app/expense-modal.js` that changes the modal `<h3>` text. Everything else (function names, DOM IDs, CSS classes, CSV filename) stays exactly as it is per the explicit "out of scope" list in D-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/71-revamp-expense-modal-into-financials-modal-rename-to-financial-breakdown-add-payables-tab-with-particulars-status-total-payable-total-paid-columns-sorted-by-status/71-CONTEXT.md
@app/expense-modal.js

<scope_clarification>
Discovery (run before edit):
```
grep -n "Expense Breakdown" app/expense-modal.js app/views/procurement.js app/views/finance.js app/views/project-detail.js app/views/service-detail.js
```

Expected result: ONLY ONE user-visible match in `app/expense-modal.js:341` — the `<h3>Expense Breakdown: ${title}</h3>` line.

Other "Expense" references in views are for the unrelated "Expense" widget/section (e.g. `<label>Expense</label>` in project-detail.js, `Project Expenses` tab heading in finance.js, expense tracking sub-tabs). These describe a different concept (expense tracking) and MUST NOT be touched.

Specifically OUT OF SCOPE per D-09:
- `showExpenseBreakdownModal` function name
- `#expenseBreakdownModal` DOM ID
- `.expense-tab` CSS class
- `#expBreakdownCategoryTab`, `#expBreakdownTransportTab` DOM IDs
- `window._switchExpenseBreakdownTab`, `window._closeExpenseBreakdownModal`, `window._exportExpenseBreakdownCSV`, `window._toggleExpenseCategory` window functions
- File path `app/expense-modal.js`
- CSV filename pattern `${safeName}-expenses-${today}.csv` (uses "expenses" as a generic noun, not "Expense Breakdown")
- The "Expense" label/section on project-detail.js and service-detail.js (separate concept)
- The "Project Expenses" / `expense-subtab-btn` UI in finance.js (separate Finance feature)
</scope_clarification>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename modal h3 from "Expense Breakdown" to "Financial Breakdown"</name>

  <read_first>
    1. `app/expense-modal.js` — read lines 330-350 to confirm current `<h3>` content at line 341
    2. `.planning/phases/71-revamp-expense-modal-into-financials-modal-rename-to-financial-breakdown-add-payables-tab-with-particulars-status-total-payable-total-paid-columns-sorted-by-status/71-CONTEXT.md` — re-read D-09 to confirm internal symbols stay unchanged
  </read_first>

  <files>app/expense-modal.js</files>

  <action>
    Run discovery first to verify there is only one user-visible match:

    ```
    grep -n "Expense Breakdown" app/expense-modal.js
    ```

    Expected: exactly one line — `341:                    <h3>Expense Breakdown: ${title}</h3>`

    Then make the single edit:

    Find this exact line in `app/expense-modal.js` (around line 341):

    ```html
                        <h3>Expense Breakdown: ${title}</h3>
    ```

    Replace with:

    ```html
                        <h3>Financial Breakdown: ${title}</h3>
    ```

    DO NOT touch ANY other line in the file. Specifically do NOT rename:
    - The function `showExpenseBreakdownModal` (line 17)
    - The DOM id `expenseBreakdownModal` (lines 19, 338, 422, 427)
    - The CSS class `.expense-tab` (lines 397, 401, 437)
    - The DOM ids `expBreakdownCategoryTab` / `expBreakdownTransportTab` (lines 408, 413, 433, 434)
    - Any `window._*ExpenseBreakdown*` window functions (lines 184, 343, 344, 426, 432)
    - The CSV filename pattern at line 210 (`${safeName}-expenses-${today}.csv`)
    - The export comment "Export function — closure over computed data..." at line 182

    Per D-09 of CONTEXT.md, this rename is user-visible only. Internal symbols are explicitly LEFT UNCHANGED to keep this diff zero-risk.

    After the edit, run discovery again to confirm:
    ```
    grep -n "Expense Breakdown" app/expense-modal.js
    grep -n "Financial Breakdown" app/expense-modal.js
    ```

    Expected after edit:
    - Zero hits for "Expense Breakdown" in this file
    - Exactly one hit for "Financial Breakdown" at line 341
  </action>

  <verify>
    <automated>grep -n "Financial Breakdown:" app/expense-modal.js && ! grep -n "Expense Breakdown" app/expense-modal.js</automated>
  </verify>

  <acceptance_criteria>
    - `app/expense-modal.js` contains the literal string `<h3>Financial Breakdown: ${title}</h3>`
    - `app/expense-modal.js` does NOT contain the literal string `Expense Breakdown` anywhere
    - `app/expense-modal.js` STILL contains `showExpenseBreakdownModal` (function name preserved)
    - `app/expense-modal.js` STILL contains `id="expenseBreakdownModal"`
    - `app/expense-modal.js` STILL contains `class="expense-tab` (the class is preserved)
    - `app/expense-modal.js` STILL contains `window._switchExpenseBreakdownTab`
    - `app/expense-modal.js` STILL contains `window._closeExpenseBreakdownModal`
    - `app/expense-modal.js` STILL contains `window._exportExpenseBreakdownCSV`
    - `app/expense-modal.js` STILL contains the CSV filename pattern `${safeName}-expenses-${today}.csv`
    - No other files in `app/views/` are modified
  </acceptance_criteria>

  <done>
    Modal title reads "Financial Breakdown: {name}" when opened from any caller (Procurement, Finance, Project Detail, Service Detail). All other modal behavior and internal symbols are unchanged. Diff is exactly one line.
  </done>
</task>

</tasks>

<verification>
After Task 1:

1. `grep -c "Financial Breakdown:" app/expense-modal.js` → `1`
2. `grep -c "Expense Breakdown" app/expense-modal.js` → `0`
3. `grep -c "showExpenseBreakdownModal" app/expense-modal.js` → at least `1` (function definition preserved)
4. `grep -c "expenseBreakdownModal" app/expense-modal.js` → at least `4` (DOM id references preserved)
5. `grep -c "_switchExpenseBreakdownTab\|_closeExpenseBreakdownModal\|_exportExpenseBreakdownCSV\|_toggleExpenseCategory" app/expense-modal.js` → at least `4`
6. Manual smoke test: open the modal from any caller (Procurement project list, Finance project list, Project Detail page) — header should read "Financial Breakdown: {name}".

If grep #2 is non-zero, the rename is incomplete or accidentally re-introduced. If grep #3, #4, or #5 are zero, internal symbols were wrongly renamed and the file is broken — revert.
</verification>

<success_criteria>
- Modal `<h3>` reads "Financial Breakdown: {title}" in all four call sites
- Zero internal symbol changes
- Diff is one line in one file
- All four window functions still exist on `window` after the modal is opened
</success_criteria>

<output>
After completion, create `.planning/phases/71-revamp-expense-modal-into-financials-modal-rename-to-financial-breakdown-add-payables-tab-with-particulars-status-total-payable-total-paid-columns-sorted-by-status/71-01-SUMMARY.md`
</output>
