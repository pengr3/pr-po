---
phase: 73-optimize-finance-tab-for-mobile-use
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/finance.js
autonomous: false
requirements:
  - MOBFIN-03
  - MOBFIN-04
must_haves:
  truths:
    - "All five bare 'overflow-x: auto' table wrappers in finance.js are replaced with class='table-scroll-container' (matching the Phase 44 project pattern established across all other tabs)"
    - "The Project List sub-tab bar container div in finance.js render() carries the class 'finance-expense-subtab-bar' so Plan 01's CSS rule can target it"
    - "No JS logic changes — only template string edits inside existing render() / renderProjectExpensesTable() / renderServiceExpensesTable() / renderRecurringExpensesTable() functions"
    - "Expense tables (Projects/Services/Recurring) now display the shadow-scroll indicator from .table-scroll-container, matching the visual pattern of Pending Approvals, Purchase Orders, and Payables tables"
  artifacts:
    - path: "app/views/finance.js"
      provides: "5 upgraded table wrappers + 1 classed sub-tab bar container"
      contains: "finance-expense-subtab-bar"
  key_links:
    - from: "app/views/finance.js :: sub-tab bar container div (line ~1641)"
      to: "styles/views.css :: .finance-expense-subtab-bar rule (added by Plan 01)"
      via: "CSS class name"
      pattern: "class=\"finance-expense-subtab-bar\""
    - from: "app/views/finance.js :: renderProjectExpensesTable / renderServiceExpensesTable / renderRecurringExpensesTable"
      to: "styles/components.css :: .table-scroll-container (existing)"
      via: "CSS class name"
      pattern: "class=\"table-scroll-container\""
---

<objective>
Upgrade 5 bare `<div style="overflow-x: auto;">` table wrappers in app/views/finance.js to `<div class="table-scroll-container">` so expense tables match the project's Phase 44 shadow-scroll pattern. Add a `finance-expense-subtab-bar` class to the Project List sub-tab container so Plan 01's CSS scroll rule can target it.

Purpose: RESEARCH.md "Pattern 2" flags these bare overflow wrappers as cosmetic inconsistencies — functional but lacking the shadow indicator that every other Finance/Procurement/Projects table uses. UI-SPEC.md "Bare overflow-x Upgrade" lists the specific render functions. This plan is a pure template-string cosmetic fix — no render logic changes, no data model changes, no Firebase changes.

Output: One JS file updated with 6 targeted template edits. No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-optimize-finance-tab-for-mobile-use/73-RESEARCH.md
@.planning/phases/73-optimize-finance-tab-for-mobile-use/73-UI-SPEC.md
@.planning/phases/73-optimize-finance-tab-for-mobile-use/73-VALIDATION.md

<interfaces>
<!-- Exact locations of bare overflow-x wrappers in finance.js (verified 2026-04-15) -->

File: app/views/finance.js (3,935 lines)

Line 1641 (inside render()): Sub-tab bar container
  Current: `<div style="display: flex; gap: 0; border-bottom: 2px solid #e5e7eb; margin-bottom: 1rem; padding: 0 1rem;">`
  Target:  `<div class="finance-expense-subtab-bar" style="...">` (PRESERVE the inline style; only ADD the class attribute)

Line 1673 (inside render(), Projects skeleton table):     `<div style="overflow-x: auto;">`
Line 1705 (inside render(), Services skeleton table):     `<div style="overflow-x: auto;">`
Line 1737 (inside render(), Recurring skeleton table):    `<div style="overflow-x: auto;">`
Line 2058 (in renderProjectExpensesTable()):              `<div style="overflow-x: auto;">` (tableHTML string)
Line 2331 (in renderServiceExpensesTable()):              `<div style="overflow-x: auto;"><table class="data-table">...`
Line 2418 (in renderRecurringExpensesTable()):            `<div style="overflow-x: auto;"><table class="data-table">...`

All five overflow-x wrappers must become: `<div class="table-scroll-container">` (preserving any inline table-level attributes that come after).

Existing pattern confirmation — `.table-scroll-container` is already used at:
  - line 1576 (Material PRs table)
  - line 1605 (Transport Requests table)
  - line 1780 (RFP Processing table)
  - line 1825 (PO Payment Summary table)
  - line 3776 (Purchase Orders table in renderPOs())

`.table-scroll-container` CSS lives in styles/components.css (line 449 area) — already loaded, already styled. No CSS changes needed from this plan; Plan 01 handles the .finance-expense-subtab-bar CSS.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Upgrade 5 bare overflow-x wrappers to .table-scroll-container in finance.js</name>
  <files>app/views/finance.js</files>
  <read_first>
    - app/views/finance.js lines 1670-1745 (3 skeleton table wrappers in render())
    - app/views/finance.js lines 2055-2062 (renderProjectExpensesTable tableHTML opener)
    - app/views/finance.js lines 2328-2340 (renderServiceExpensesTable tableHTML opener)
    - app/views/finance.js lines 2415-2425 (renderRecurringExpensesTable tableHTML opener)
    - app/views/finance.js lines 1570-1580 (reference: existing .table-scroll-container usage for Material PRs)
    - .planning/phases/73-optimize-finance-tab-for-mobile-use/73-UI-SPEC.md ("Bare overflow-x Upgrade" section)
  </read_first>
  <action>
Perform 5 targeted string replacements in app/views/finance.js. Each replacement is: find `<div style="overflow-x: auto;">` at the specified line and replace with `<div class="table-scroll-container">`. Preserve everything that comes after the opening `<div ...>` (tables, skeleton rows, etc.).

**Replacement 1 — line 1673 (Projects skeleton):**
- Find (within line 1673, inside the `projectExpensesContainer` div):
  ```
  <div style="overflow-x: auto;">
  ```
- Replace with:
  ```
  <div class="table-scroll-container">
  ```

**Replacement 2 — line 1705 (Services skeleton):**
- Same find/replace as Replacement 1 — this is the wrapper inside `serviceExpensesContainer` div.

**Replacement 3 — line 1737 (Recurring skeleton):**
- Same find/replace — wrapper inside `recurringExpenseSection`.

**Replacement 4 — line 2058 (renderProjectExpensesTable multi-line tableHTML):**
- Find (multi-line):
  ```
  let tableHTML = `
      <div style="overflow-x: auto;">
          <table class="data-table">
  ```
- Replace with:
  ```
  let tableHTML = `
      <div class="table-scroll-container">
          <table class="data-table">
  ```

**Replacement 5 — line 2331 (renderServiceExpensesTable single-line):**
- Find:
  ```
  let tableHTML = `<div style="overflow-x: auto;"><table class="data-table"><thead><tr>
  ```
- Replace with:
  ```
  let tableHTML = `<div class="table-scroll-container"><table class="data-table"><thead><tr>
  ```

**Replacement 6 — line 2418 (renderRecurringExpensesTable single-line):**
- Find:
  ```
  let tableHTML = `<div style="overflow-x: auto;"><table class="data-table"><thead><tr>
  ```
- Replace with:
  ```
  let tableHTML = `<div class="table-scroll-container"><table class="data-table"><thead><tr>
  ```

Total: 6 replacements (3 skeleton wrappers + 3 render-function table wrappers).

IMPORTANT CONSTRAINTS:
- Do NOT touch the existing `.table-scroll-container` wrappers at lines 1576, 1605, 1780, 1825, 3776 — those are already correct.
- Do NOT add `.table-scroll-container` anywhere else (no nested containers — RESEARCH.md Pitfall 2).
- Do NOT change any render logic, JS function signatures, or data manipulation.
- Do NOT change table column counts, cell content, or event handlers.
- The `style="overflow-x: auto;"` on other wrappers in the file (e.g., empty-state divs using `overflow-x: auto` for scroll safety — there should be zero others after this edit) should remain untouched.

After the edits, run this grep to confirm exactly zero remaining bare overflow-x wrappers in finance.js:
```
grep -n 'style="overflow-x: auto;"' app/views/finance.js
```
Expected result: **no matches** (0 lines of output).
  </action>
  <verify>
    <automated>grep -c 'style="overflow-x: auto;"' app/views/finance.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'style="overflow-x: auto;"' app/views/finance.js` returns `0` (exact count, no bare overflow-x wrappers remain).
    - `grep -c 'class="table-scroll-container"' app/views/finance.js` returns a count at least 5 greater than the pre-edit count. Pre-edit count was 5 (lines 1576, 1605, 1780, 1825, 3776). Post-edit count MUST be 10 or more (pre-existing 5 + 5 upgrades; the 6th replacement is a sub-tab bar class in Task 2, not this task's concern). Expected exact count: 10.
    - Grep: `grep -n 'class="table-scroll-container"' app/views/finance.js` shows matches at approximately lines 1576, 1605, 1673, 1705, 1737, 1780, 1825, 2058, 2331, 2418, 3776.
    - File byte count changed by approximately -20 to -40 bytes (replacing `style="overflow-x: auto;"` with `class="table-scroll-container"` is slightly shorter per occurrence).
    - No render function signatures changed — `grep -n "^function render\|^async function render" app/views/finance.js` returns the same set of function names as before the edit.
    - File loads without syntax errors: `node --check app/views/finance.js` (if Node syntax-checks ES modules) OR simply open http://localhost:8000/#/finance in the browser and verify DevTools Console shows no SyntaxError.
  </acceptance_criteria>
  <done>All 5 expense-table wrappers in finance.js use `.table-scroll-container` instead of bare `overflow-x: auto;`. Zero bare wrappers remain in finance.js.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add .finance-expense-subtab-bar class to sub-tab bar container div</name>
  <files>app/views/finance.js</files>
  <read_first>
    - app/views/finance.js lines 1640-1658 (sub-tab bar container + 3 expense-subtab-btn buttons)
    - .planning/phases/73-optimize-finance-tab-for-mobile-use/73-UI-SPEC.md ("Sub-Tab Bar" section — class name is exactly `finance-expense-subtab-bar`)
    - styles/views.css end-of-file (Plan 01 Task 2 added the matching CSS rule there — verify class name matches)
  </read_first>
  <action>
In app/views/finance.js at line 1641, the sub-tab bar container div currently reads:

```html
<div style="display: flex; gap: 0; border-bottom: 2px solid #e5e7eb; margin-bottom: 1rem; padding: 0 1rem;">
```

Edit this line to ADD a `class="finance-expense-subtab-bar"` attribute while preserving the entire existing inline style. The result must be:

```html
<div class="finance-expense-subtab-bar" style="display: flex; gap: 0; border-bottom: 2px solid #e5e7eb; margin-bottom: 1rem; padding: 0 1rem;">
```

Rules:
- The class name must be EXACTLY `finance-expense-subtab-bar` (lowercase, hyphenated) — this is the selector Plan 01 Task 2 added to views.css. A typo here will silently break the 320px scroll rule.
- PRESERVE the entire existing inline style attribute — it controls the desktop layout (flex, border-bottom, padding). Do NOT remove or rewrite these styles.
- Do NOT add the class to either of the three `<button class="expense-subtab-btn ..."` elements (lines 1642, 1647, 1652) — those already have their own class and are targeted via the parent selector `.finance-expense-subtab-bar button` in Plan 01's CSS.
- Do NOT change the buttons' existing classes, onclick handlers, data-tab attributes, or inline styles.
- Do NOT add the class anywhere else in the file. Only this one div at line ~1641 gets the class.
  </action>
  <verify>
    <automated>grep -c 'class="finance-expense-subtab-bar"' app/views/finance.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class="finance-expense-subtab-bar"' app/views/finance.js` returns exactly `1`.
    - `grep -n 'finance-expense-subtab-bar' app/views/finance.js` shows the match at line ~1641 (in the div that precedes the three `expense-subtab-btn` buttons).
    - The matched line ALSO contains `border-bottom: 2px solid #e5e7eb` AND `display: flex` — verify inline style preserved: `grep 'finance-expense-subtab-bar' app/views/finance.js | grep -c 'border-bottom: 2px solid #e5e7eb'` returns `1`.
    - The three `expense-subtab-btn` buttons are unchanged — `grep -c 'class="expense-subtab-btn' app/views/finance.js` returns `3` (one "active", two default — pattern `class="expense-subtab-btn`).
    - No other element in the file picked up the class — `grep -c 'finance-expense-subtab-bar' app/views/finance.js` returns `1` (single occurrence).
    - `app/views/finance.js` loads without SyntaxError in the browser console when visiting `#/finance`.
  </acceptance_criteria>
  <done>Exactly one div (the Project List sub-tab bar container) carries class `finance-expense-subtab-bar`, existing inline style preserved, buttons untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual visual verification at 375px, 768px, and 1440px</name>
  <files>app/views/finance.js, styles/components.css, styles/views.css (verification only — no edits in this task)</files>
  <action>Run the server () and perform the full cross-viewport browser verification described in <how-to-verify> below. This is the VALIDATION.md-mandated 3-viewport full pass (375px, 768px, 1440px) across all 4 Finance tabs. Do NOT edit any files in this task — it is the Plan 02 final gate before phase completion.</action>
  <verify><automated>echo "Manual 3-viewport verification — see how-to-verify steps. Resume signal captures user sign-off."</automated></verify>
  <what-built>
    - app/views/finance.js: 5 bare `overflow-x: auto` wrappers upgraded to `.table-scroll-container` (Task 1)
    - app/views/finance.js: sub-tab bar container div gained `finance-expense-subtab-bar` class (Task 2)

    Combined with Plan 01's CSS, this completes the Phase 73 mobile optimization. This checkpoint is the full cross-viewport verification required by VALIDATION.md.
  </what-built>
  <how-to-verify>
    Start local server:
    ```
    python -m http.server 8000
    ```
    Open http://localhost:8000/#/finance in Chrome. Open DevTools (F12) → device toolbar (Ctrl+Shift+M).

    **A. 375px (iPhone SE) pass — all 4 Finance tabs:**
    1. Set viewport to 375 x 667.
    2. **Pending Approvals tab:**
       - Material PRs card-header: h2 above controls, select + "View Details" button each ~full width, 44px tall, wrap cleanly. No page horizontal scroll.
       - Material PRs table: horizontal scroll WITH shadow indicator on right edge (proves `.table-scroll-container` is working — no change here, was already correct).
       - Transport Requests: same pattern.
    3. **Purchase Orders tab:**
       - Card-header with 2 selects + 2 buttons stacks neatly (controls wrap to 2 rows of 2).
       - Table scrolls horizontally with shadow.
    4. **Project List tab:**
       - Sub-tab bar (Projects | Services | Recurring): if labels clip at 375px, the bar must scroll horizontally (this is the `.finance-expense-subtab-bar` CSS kicking in).
       - Projects table: horizontal scroll WITH shadow indicator (this is the NEW `.table-scroll-container` from Task 1 — verify shadow appears when scrolling right, same as other Finance tables). This was previously a bare overflow div with no shadow.
       - Switch to Services sub-tab — same check (shadow now present).
       - Switch to Recurring sub-tab — same check.
    5. **Payables tab:**
       - Filter toolbars wrap (no regression from existing flex-wrap).
       - RFP Processing + PO Payment Summary tables scroll horizontally with shadow (no change, already correct).

    **B. 768px (iPad portrait) pass:**
    - Repeat A steps 2-5 at 768px. Layout should transition — card-headers may stack (depending on content width) but tables and sub-tab bar fit more comfortably.

    **C. 320px (smallest phone) sub-tab stress test:**
    1. Set viewport to 320 x 568.
    2. Visit Project List tab — confirm sub-tab bar scrolls horizontally (swipe left/right on touch emulation, or shift+scroll in DevTools) and no button label is clipped mid-word.

    **D. 1440px desktop regression:**
    1. Set viewport to 1440 x 900.
    2. Visit all 4 Finance tabs.
    3. Verify card-headers are back to horizontal (heading left, controls right — no stacking).
    4. Verify expense tables at Project List tab no longer need scrolling (fit comfortably).
    5. Verify sub-tab bar displays all 3 buttons inline without scroll indicators.

    **E. Console + render check:**
    - DevTools Console → no errors on any Finance tab navigation.
    - Click into a project detail (if any project has expenses) → table still renders, rows clickable.
    - Open PR Details modal from Pending Approvals → modal fits 375px viewport, signature canvas adapts.
    - Open Record Payment modal from Payables → modal fits 375px viewport.
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 Finance tabs render correctly at 375px, 768px, and 1440px with no console errors, AND the Project List expense tables now show shadow-scroll indicators. If ANY regression, describe: which tab, which viewport, what's broken.</resume-signal>
  <acceptance_criteria>
    - User confirms "approved" OR reports specific regression.
    - Project List expense tables (Projects/Services/Recurring) now show the `.table-scroll-container` shadow-scroll indicator (visual confirmation of Task 1).
    - Sub-tab bar scrolls horizontally at 320px without clipping (visual confirmation of Task 2 + Plan 01 Task 2).
    - Card-headers remain horizontal at 1440px (no desktop regression from Plan 01 Task 1).
    - No JS errors in DevTools Console on any Finance tab.
  </acceptance_criteria>
  <done>All 4 Finance tabs verified visually across 3 viewports. No regressions. Shadow-scroll shown on previously-bare expense tables.</done>
</task>

</tasks>

<verification>
Overall plan verification:
1. `grep -c 'style="overflow-x: auto;"' app/views/finance.js` returns `0`.
2. `grep -c 'class="table-scroll-container"' app/views/finance.js` returns `10` (5 pre-existing + 5 upgraded).
3. `grep -c 'class="finance-expense-subtab-bar"' app/views/finance.js` returns `1`.
4. `grep -c 'class="expense-subtab-btn' app/views/finance.js` still returns `3` (buttons unchanged).
5. No new files created.
6. Human checkpoint confirms visual correctness at 375px, 768px, 1440px across all 4 Finance tabs.
</verification>

<success_criteria>
- [ ] Task 1 — all 5 bare wrappers upgraded, grep counts match
- [ ] Task 2 — exactly 1 sub-tab bar container classed, buttons untouched
- [ ] Task 3 — user-approved visual verification across 375px/768px/1440px and all 4 Finance tabs
- [ ] VALIDATION.md sampling rate satisfied (full 3-viewport pass completed)
- [ ] RESEARCH.md Pitfall 2 avoided (no nested `.table-scroll-container` wrappers created)
</success_criteria>

<output>
After completion, create `.planning/phases/73-optimize-finance-tab-for-mobile-use/73-02-SUMMARY.md` documenting:
- Exact line numbers edited in finance.js
- Pre/post grep counts for `style="overflow-x: auto;"` and `class="table-scroll-container"`
- Human checkpoint outcome (approved / regression list)
- Readiness for phase completion / /gsd:verify-work
</output>
