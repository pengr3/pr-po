---
phase: 73.1
plan: 03
type: execute
wave: 2
depends_on:
  - "73.1-01"
files_modified:
  - app/views/finance.js
autonomous: true
requirements:
  - MOBCARD-04
  - MOBCARD-05
  - MOBCARD-07
  - MOBCARD-10
user_setup: []

must_haves:
  truths:
    - "At viewport 375px on Finance > Payables, the RFP Processing table is hidden and a vertical stack of RFP cards is visible"
    - "At viewport 375px on Finance > Payables, the PO Payment Summary table is hidden and a vertical stack of PO Summary cards is visible"
    - "At viewport 1280px on Finance > Payables, both Payables tables are visible and no card stacks are visible"
    - "Overdue RFP cards render with .fc-overdue class (red tint) matching desktop row background #fef2f2 at finance.js line 633"
    - "Tapping Record Payment / Manage Payments on an RFP card opens window.openRecordPaymentModal"
    - "Tapping Show Tranches on a PO Summary card toggles a .fc-sub-list with .fc-sub-card tranche rows and flips label to Hide Tranches"
    - "PO card expand uses distinct IDs #po-card-expand-{safePoId} and #po-card-chevron-{safePoId} to avoid collision with desktop #po-expand-{safePoId}"
    - "poSummaryPagination div remains visible on mobile (sits outside .table-scroll-container so is NOT hidden by scoped rule)"
    - "RFP card Due Date is rendered raw as ${rfp.due_date || 'N/A'} — matches desktop table rendering at finance.js lines 647 and 849 (field is stored as an ISO string; see deriveRFPStatus at finance.js line 36 which passes due_date directly to new Date())"
  artifacts:
    - path: "app/views/finance.js"
      provides: "buildRFPCard, buildPOSummaryCard, buildPOTrancheSubCard helpers; togglePOCardExpand window function; rfpCardList and poSummaryCardList sibling divs; populated card lists"
      contains: "function togglePOCardExpand"
  key_links:
    - from: "renderRFPTable"
      to: "rfpCardList"
      via: "getElementById + innerHTML"
      pattern: "rfpCardList"
    - from: "renderPOSummaryTable"
      to: "poSummaryCardList"
      via: "getElementById + innerHTML"
      pattern: "poSummaryCardList"
    - from: "togglePOCardExpand"
      to: "po-card-expand- element"
      via: "style.display block toggle"
      pattern: "po-card-expand-"
---

# Objective

Convert both Finance > Payables tables (RFP Processing and PO Payment Summary) to mobile card layouts at viewports 768px and below. Both are Pattern A (tbody-only) so sibling card-list divs go in the static render() HTML and are populated by existing render functions.

The PO Payment Summary has expandable tranche sub-rows. Per RESEARCH Pitfall 3, the card layout uses DISTINCT element IDs (po-card-expand-*, po-card-chevron-*) and a DISTINCT toggle function (window.togglePOCardExpand) to avoid colliding with desktop togglePOExpand which sets display: table-row on tr elements.

**REVIEWS Concern 3 resolution — `rfp.due_date` rendering:**
Audit of finance.js confirms `rfp.due_date` is a STRING (ISO format):
- Line 36: `const isOverdue = rfp.due_date && new Date(rfp.due_date) < new Date();` — passes string directly to Date constructor
- Line 647 (renderRFPTable): `<td>${rfp.due_date || 'N/A'}</td>` — renders raw on desktop
- Line 849 (renderPOSummaryTable tranche row): `<td>${rfp.due_date || 'N/A'}</td>` — renders raw on desktop

Therefore card rendering as `${rfp.due_date || 'N/A'}` is CONSISTENT with desktop — do NOT wrap in formatDate(). If the value were a Firestore Timestamp, `new Date(timestamp)` on line 36 would receive an object and produce an Invalid Date; since desktop works today, we know it's a string.

**Parallel edit coordination (REVIEWS Suggestion):** This plan and Plan 02 both modify `app/views/finance.js` in Wave 2. Run SEQUENTIALLY. If the orchestrator attempts parallel sub-agent dispatch, the executor must apply the file edits serially (Plan 02 commits first, then Plan 03) to avoid merge conflicts on the 3943-line file.

## Output

- buildRFPCard(rfp) helper — respects Overdue via fc-overdue class; due_date rendered raw to match desktop (REVIEWS Concern 3)
- buildPOSummaryCard(po) helper — includes Show/Hide Tranches toggle button
- buildPOTrancheSubCard(rfp) helper — individual tranche rows inside sub-list; due_date rendered raw
- togglePOCardExpand(safePoId) function registered on window
- Static Payables render() HTML extended with two fc-card-list sibling divs
- renderRFPTable() and renderPOSummaryTable() populate card lists alongside tbody

# Execution Context

See workflow files:
- $HOME/.claude/get-shit-done/workflows/execute-plan.md
- $HOME/.claude/get-shit-done/templates/summary.md

# Context Files

- .planning/ROADMAP.md
- .planning/STATE.md
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-RESEARCH.md
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-UI-SPEC.md
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-REVIEWS.md
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-01-PLAN.md
- app/views/finance.js

## Interfaces

Pattern A render functions (tbody-only):
- renderRFPTable() — finance.js line 568, populates #rfpTableBody inside static .table-scroll-container at line 1788
- renderPOSummaryTable() — finance.js line 759, populates #poSummaryTableBody inside static .table-scroll-container at line 1833

Existing state and helpers in finance.js:
- rfpsData — array from onSnapshot
- rfpStatusFilter, rfpDeptFilter — filter state
- poSummaryStatusFilter, poSummaryDeptFilter, poSummaryCurrentPage, poSummaryItemsPerPage
- posAmountMap — kept live by initPayablesTab
- deriveRFPStatus(rfp), statusBadgeColors, buildPOMap(rfps), derivePOSummary(rfps, poTotalAmount)
- escapeHTML, formatCurrency from utils.js
- `rfp.due_date` is a STRING (ISO format, e.g. "2026-05-01") — confirmed by line 36 `new Date(rfp.due_date)` usage and desktop raw rendering at lines 647/849

Window onclick targets already registered:
- window.openRecordPaymentModal(rfpId)
- window.viewPODetailsFromRFP(poDocId)
- window.togglePOExpand(safePoId) — DESKTOP ONLY, targets tr with display: table-row

Distinct IDs and function for cards (Pitfall 3):
- Desktop: #po-expand-{safePoId}, #po-chevron-{safePoId}, togglePOExpand sets display: table-row
- Cards (NEW): #po-card-expand-{safePoId}, #po-card-chevron-{safePoId}, togglePOCardExpand sets display: block

CSS classes from Plan 01: fc-card, fc-card.fc-overdue, fc-sub-list, fc-sub-card, fc-empty, fc-card-actions .btn (min-height 44px).

attachWindowFunctions at finance.js line 191 — add window.togglePOCardExpand registration here.

# Tasks

<task type="auto">
<name>Task 1: Add sibling card-list divs and buildRFPCard helper; populate RFP card list</name>
<files>app/views/finance.js</files>


Type: auto
Files: app/views/finance.js

<read_first>

- app/views/finance.js (lines 1766-1856 Payables static HTML; lines 568-652 renderRFPTable; line 36 deriveRFPStatus confirms due_date is a string; lines 633, 647 for desktop RFP row rendering parity)
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-RESEARCH.md (Table 4b column mapping)
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-UI-SPEC.md (Copywriting Contract)
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-REVIEWS.md (Concern 3 — due_date is string, render raw)

</read_first>
<behavior>

- Static HTML inside #payables-section emits `<div class="fc-card-list" id="rfpCardList"></div>` as sibling immediately AFTER the RFP .table-scroll-container closing div (around line 1809)
- Static HTML emits `<div class="fc-card-list" id="poSummaryCardList"></div>` as sibling AFTER the PO Summary .table-scroll-container closing div and BEFORE the #poSummaryPagination div (around line 1852) so pagination stays visible on mobile
- buildRFPCard(rfp) returns card HTML with RFP ID plus status badge in header; Supplier, PO Ref (clickable link), Project/Service, Tranche, Amount, Paid, Balance, Due Date in body; Record Payment or Manage Payments button in actions; Overdue RFPs add fc-overdue class
- Due Date rendered RAW as `${rfp.due_date || 'N/A'}` — matches desktop at line 647 (REVIEWS Concern 3 resolution: field is a string, not Firestore Timestamp)
- renderRFPTable() populates #rfpCardList alongside tbody using the same displayed filtered-and-sorted array
- Empty state uses fc-empty div with text matching existing td colspan=11 empty message

</behavior>
<action>

**Pre-flight verification (REVIEWS Concern 3):** Before coding, confirm that `rfp.due_date` is a string by running: `grep -n "new Date(rfp.due_date)" app/views/finance.js`. Expected match at line 36. Also `grep -n "rfp.due_date || 'N/A'" app/views/finance.js` — expected matches at lines 647 and 849. These confirm desktop renders due_date as a raw string. Cards will mirror this exact expression. If grep reveals any `formatDate(rfp.due_date)` or `rfp.due_date.toDate()` in the codebase, STOP and flag — the field may be a Timestamp and this plan must be updated. Current codebase audit: no such calls exist.

Step 1: Add sibling card-list divs in static render() HTML.

Locate the Payables static HTML at lines 1788-1852. For the RFP Processing table, the current structure ends with `</div>` for .table-scroll-container (around line 1809). Immediately AFTER that closing div, insert this line:

```
<div class="fc-card-list" id="rfpCardList"></div>
```

For the PO Payment Summary table (lines 1833-1853), the current structure has `.table-scroll-container` followed by `<div id="poSummaryPagination" class="pagination-container"></div>`. Insert this line BETWEEN those two:

```
<div class="fc-card-list" id="poSummaryCardList"></div>
```

Final structure for PO Summary area should be:
1. div.table-scroll-container containing the table
2. div.fc-card-list id=poSummaryCardList (NEW — empty on load, populated by renderPOSummaryTable)
3. div#poSummaryPagination (existing — stays visible on mobile per Pitfall 7)

Step 2: Add buildRFPCard helper.

Insert IMMEDIATELY BEFORE `function renderRFPTable()` at line 568. The helper reads rfp fields, derives status via deriveRFPStatus, computes totalPaid and balance, builds PO Ref display (link for po_id, plain text for tr_id, dash for neither), selects Record Payment vs Manage Payments button based on Fully Paid status, and wraps in fc-card div with fc-overdue class when status is Overdue.

```
function buildRFPCard(rfp) {
    const status = deriveRFPStatus(rfp);
    const totalPaid = (rfp.payment_records || [])
        .filter(r => r.status !== 'voided')
        .reduce((s, r) => s + (r.amount || 0), 0);
    const balance = (rfp.amount_requested || 0) - totalPaid;
    const isOverdue = status === 'Overdue';
    const deptLabel = rfp.service_code
        ? escapeHTML(rfp.service_code)
        : escapeHTML(rfp.project_code || '');
    const badgeStyle = statusBadgeColors[status] || '';

    const canEdit = window.canEditTab?.('finance');
    const showEditControls = canEdit !== false;
    const actionBtn = showEditControls
        ? (status === 'Fully Paid'
            ? '<button class="btn btn-sm btn-outline" onclick="window.openRecordPaymentModal(\'' + rfp.id + '\')">Manage Payments</button>'
            : '<button class="btn btn-sm btn-primary" onclick="window.openRecordPaymentModal(\'' + rfp.id + '\')">Record Payment</button>')
        : '';

    const poRefDisplay = rfp.po_id
        ? '<a href="javascript:void(0)" onclick="window.viewPODetailsFromRFP(\'' + (rfp.po_doc_id || '') + '\')" style="color:#1a73e8;text-decoration:none;cursor:pointer;">' + escapeHTML(rfp.po_id) + '</a>'
        : rfp.tr_id
        ? '<span style="color:#1e293b;font-weight:600;">' + escapeHTML(rfp.tr_id) + '</span>'
        : '<span style="color:#999;">-</span>';

    return `
        <div class="fc-card${isOverdue ? ' fc-overdue' : ''}">
            <div class="fc-card-header">
                <span class="fc-card-id">${escapeHTML(rfp.rfp_id || '')}</span>
                <span style="display:inline-block;padding:2px 8px;border-radius:4px;font-size:0.75rem;font-weight:600;${badgeStyle}">${status}</span>
            </div>
            <div class="fc-card-body">
                <div class="fc-card-row"><span class="fc-label">Supplier</span><span class="fc-value">${escapeHTML(rfp.supplier_name || '')}</span></div>
                <div class="fc-card-row"><span class="fc-label">PO Ref</span><span class="fc-value">${poRefDisplay}</span></div>
                <div class="fc-card-row"><span class="fc-label">Project / Service</span><span class="fc-value">${deptLabel}</span></div>
                <div class="fc-card-row"><span class="fc-label">Tranche</span><span class="fc-value">${escapeHTML(rfp.tranche_label || '')} (${rfp.tranche_percentage || 0}%)</span></div>
                <div class="fc-card-row"><span class="fc-label">Amount</span><span class="fc-value fc-amount">${formatCurrency(rfp.amount_requested || 0)}</span></div>
                <div class="fc-card-row"><span class="fc-label">Paid</span><span class="fc-value">${formatCurrency(totalPaid)}</span></div>
                <div class="fc-card-row"><span class="fc-label">Balance</span><span class="fc-value">${formatCurrency(balance)}</span></div>
                <div class="fc-card-row"><span class="fc-label">Due Date</span><span class="fc-value">${rfp.due_date || 'N/A'}</span></div>
            </div>
            ${actionBtn ? '<div class="fc-card-actions">' + actionBtn + '</div>' : ''}
        </div>`;
}
```

**Due Date note (REVIEWS Concern 3):** The expression `${rfp.due_date || 'N/A'}` is IDENTICAL to desktop at finance.js line 647. `rfp.due_date` is stored as a string (ISO date); this was confirmed via line 36's direct `new Date(rfp.due_date)` usage. Do NOT wrap with `formatDate()` — that would produce output different from the desktop table and break parity.

Step 3: Populate RFP card list inside renderRFPTable().

The existing early-return when displayed.length === 0 is at line 604-612. Modify it to also populate the card list with the empty-state message BEFORE returning:

```
if (displayed.length === 0) {
    const isFiltered = rfpStatusFilter || rfpDeptFilter;
    const emptyMsg = isFiltered
        ? 'No RFPs match the selected filters. Clear filters to see all requests.'
        : 'No outstanding payment requests. Use the status filter to view Fully Paid RFPs, or check the PO Payment Summary below.';
    tbody.innerHTML = '<tr><td colspan="11" style="text-align:center;padding:2rem;color:#64748b;">' + emptyMsg + '</td></tr>';
    const rfpCardListEmpty = document.getElementById('rfpCardList');
    if (rfpCardListEmpty) rfpCardListEmpty.innerHTML = '<div class="fc-empty">' + emptyMsg + '</div>';
    return;
}
```

At the BOTTOM of renderRFPTable() (just BEFORE the closing brace), add:

```
// Mobile card list (Phase 73.1) — populated alongside tbody
const rfpCardList = document.getElementById('rfpCardList');
if (rfpCardList) {
    rfpCardList.innerHTML = displayed.map(rfp => buildRFPCard(rfp)).join('');
}
```

Do NOT:
- Do NOT register buildRFPCard on window (internal helper)
- Do NOT modify filter or sort logic in renderRFPTable
- Do NOT populate poSummaryCardList here (Task 2 handles it)
- Do NOT wrap rfp.due_date with formatDate() — must match desktop raw rendering (REVIEWS Concern 3)

</action>
<verify>
<automated>

Run these grep checks:

- grep -c "id=\"rfpCardList\"" app/views/finance.js  (expect 1)
- grep -c "id=\"poSummaryCardList\"" app/views/finance.js  (expect 1)
- grep -c "function buildRFPCard" app/views/finance.js  (expect 1)
- grep -c "getElementById('rfpCardList')" app/views/finance.js  (expect 2 — empty branch + non-empty branch)
- grep -c "fc-overdue" app/views/finance.js  (expect >= 2 — CSS from plan 01 + JS class application)
- grep -c "rfp.due_date || 'N/A'" app/views/finance.js  (expect >= 3 — desktop table row 647, tranche row 849, and new card at buildRFPCard)

</automated>
</verify>
<acceptance_criteria>

- `grep -c "id=\"rfpCardList\"" app/views/finance.js` returns 1 (sibling div in static HTML)
- `grep -c "id=\"poSummaryCardList\"" app/views/finance.js` returns 1 (sibling div in static HTML — populated by Task 2)
- `grep -c "function buildRFPCard" app/views/finance.js` returns 1
- `grep -c "getElementById('rfpCardList')" app/views/finance.js` returns 2 (empty-path + populated-path)
- `grep -c "Record Payment<" app/views/finance.js` returns >= 2 (existing table button + new card button)
- `grep -c "Manage Payments<" app/views/finance.js` returns >= 2
- buildRFPCard renders due_date raw (no formatDate): `grep -A 40 "function buildRFPCard" app/views/finance.js | grep -c "formatDate(rfp.due_date)"` returns 0 (REVIEWS Concern 3)
- buildRFPCard uses the same expression as desktop for due_date: `grep -A 40 "function buildRFPCard" app/views/finance.js | grep -c "rfp.due_date || 'N/A'"` returns 1
- Visual: Finance > Payables at 375px → RFP table hidden; RFP card stack visible; each card shows 8 data rows; Overdue cards have red tint; Record Payment/Manage Payments buttons full-width
- Visual: Due Date in card shows a readable string (e.g. "2026-05-01") — NOT "[object Object]" and NOT "Invalid Date". Compare side-by-side with desktop at 1280px — must be identical text.
- Visual: Finance > Payables at 1280px → RFP table visible; RFP card list hidden
- Filter the RFP Status dropdown to a value with no results → card list shows fc-empty with same text as the td colspan=11 message

</acceptance_criteria>
<done>

RFP Processing table has mobile card alternative. Static HTML has both fc-card-list sibling divs. buildRFPCard generates complete cards with Overdue highlighting. Due Date rendered raw to match desktop (REVIEWS Concern 3). Empty state parity between tbody and card list.

</done>
</task>

<task type="auto">
<name>Task 2: Add PO Summary helpers and togglePOCardExpand; populate PO Summary card list</name>
<files>app/views/finance.js</files>


Type: auto
Files: app/views/finance.js

<read_first>

- app/views/finance.js (lines 759-897 renderPOSummaryTable; line 477 togglePOExpand; line 191 attachWindowFunctions; line 849 desktop tranche row due_date raw rendering)
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-RESEARCH.md (Pitfall 3; Table 4c column mapping)
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-UI-SPEC.md (Interaction Contract section 4)
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-REVIEWS.md (Concern 3 — tranche sub-card due_date raw rendering)

</read_first>
<behavior>

- buildPOTrancheSubCard(rfp) returns a fc-sub-card div with RFP ID, Tranche, Amount, Paid, Balance, Due Date (raw per REVIEWS Concern 3), Status, and optional Record Payment / Manage Payments button
- buildPOSummaryCard(po) returns a fc-card div with PO ID (link for regular POs, plain text for TR group) + overall status badge in header; Supplier, Project/Service, Current Active Tranche, Total Amount, Total Paid, Remaining in body; Show Tranches button at bottom (full-width) that calls window.togglePOCardExpand; hidden fc-sub-list div with id po-card-expand-{safePoId} containing tranche sub-cards
- togglePOCardExpand(safePoId) flips display block/none on #po-card-expand-{safePoId}, updates chevron at #po-card-chevron-{safePoId} between &#9654; (collapsed) and &#9660; (expanded), and flips button label between "Show Tranches" and "Hide Tranches"
- Function registered on window in attachWindowFunctions
- renderPOSummaryTable() populates #poSummaryCardList with the same pageEntries used for tbody (pagination applies to both)
- Empty state (no POs or all filtered) shows fc-empty with matching text

</behavior>
<action>

Step 1: Add buildPOTrancheSubCard helper.

Insert IMMEDIATELY BEFORE `function renderPOSummaryTable()` (line 759). Template literal return value (in backticks):

```
function buildPOTrancheSubCard(rfp) {
    const rfpStatus = deriveRFPStatus(rfp);
    const rfpTotalPaid = (rfp.payment_records || [])
        .filter(r => r.status !== 'voided')
        .reduce((s, r) => s + (r.amount || 0), 0);
    const rfpBalance = (rfp.amount_requested || 0) - rfpTotalPaid;
    const rfpBadgeStyle = statusBadgeColors[rfpStatus] || '';

    const canEdit = window.canEditTab?.('finance');
    const showEditControls = canEdit !== false;
    const subBtn = showEditControls
        ? (rfpStatus === 'Fully Paid'
            ? '<button class="btn btn-sm btn-outline" onclick="window.openRecordPaymentModal(\'' + rfp.id + '\')">Manage Payments</button>'
            : '<button class="btn btn-sm btn-primary" onclick="window.openRecordPaymentModal(\'' + rfp.id + '\')">Record Payment</button>')
        : '';

    return `
        <div class="fc-sub-card">
            <div style="display:flex;justify-content:space-between;align-items:baseline;margin-bottom:0.25rem;">
                <span style="font-weight:700;color:#1e293b;">${escapeHTML(rfp.rfp_id || '')}</span>
                <span style="display:inline-block;padding:2px 8px;border-radius:4px;font-size:0.75rem;font-weight:600;${rfpBadgeStyle}">${rfpStatus}</span>
            </div>
            <div style="display:flex;flex-direction:column;gap:0.25rem;font-size:0.75rem;">
                <div style="display:flex;justify-content:space-between;"><span style="color:#64748b;font-weight:600;text-transform:uppercase;letter-spacing:0.03em;">Tranche</span><span>${escapeHTML(rfp.tranche_label || '')} (${rfp.tranche_percentage || 0}%)</span></div>
                <div style="display:flex;justify-content:space-between;"><span style="color:#64748b;font-weight:600;text-transform:uppercase;letter-spacing:0.03em;">Amount</span><span>${formatCurrency(rfp.amount_requested || 0)}</span></div>
                <div style="display:flex;justify-content:space-between;"><span style="color:#64748b;font-weight:600;text-transform:uppercase;letter-spacing:0.03em;">Paid</span><span>${formatCurrency(rfpTotalPaid)}</span></div>
                <div style="display:flex;justify-content:space-between;"><span style="color:#64748b;font-weight:600;text-transform:uppercase;letter-spacing:0.03em;">Balance</span><span>${formatCurrency(rfpBalance)}</span></div>
                <div style="display:flex;justify-content:space-between;"><span style="color:#64748b;font-weight:600;text-transform:uppercase;letter-spacing:0.03em;">Due Date</span><span>${rfp.due_date || 'N/A'}</span></div>
            </div>
            ${subBtn ? '<div style="margin-top:0.5rem;">' + subBtn + '</div>' : ''}
        </div>`;
}
```

**Due Date note (REVIEWS Concern 3):** Again, `${rfp.due_date || 'N/A'}` renders raw to match desktop line 849. Do NOT change to formatDate.

Step 2: Add buildPOSummaryCard helper.

Insert IMMEDIATELY AFTER buildPOTrancheSubCard (still before renderPOSummaryTable):

```
function buildPOSummaryCard(po) {
    const badgeStyle = statusBadgeColors[po.overallStatus] || '';
    const isOverdue = po.overallStatus === 'Overdue';
    const safePoId = po.poId.replace(/[^a-zA-Z0-9_-]/g, '_');

    const refDisplay = po.isTR
        ? '<span style="font-weight:600;color:#1e293b;">' + escapeHTML(po.poId) + '</span>'
        : '<a href="javascript:void(0)" onclick="window.viewPODetailsFromRFP(\'' + po.poId + '\')" style="color:#1a73e8;text-decoration:none;cursor:pointer;font-weight:600;">' + escapeHTML(po.poId) + '</a>';

    const subCards = po.sortedRFPs.map(rfp => buildPOTrancheSubCard(rfp)).join('');

    return `
        <div class="fc-card${isOverdue ? ' fc-overdue' : ''}">
            <div class="fc-card-header">
                <span class="fc-card-id">${refDisplay}</span>
                <span style="display:inline-block;padding:2px 8px;border-radius:4px;font-size:0.75rem;font-weight:600;${badgeStyle}">${po.overallStatus}</span>
            </div>
            <div class="fc-card-body">
                <div class="fc-card-row"><span class="fc-label">Supplier</span><span class="fc-value">${escapeHTML(po.supplier || '')}</span></div>
                <div class="fc-card-row"><span class="fc-label">Project / Service</span><span class="fc-value">${po.deptLabel}</span></div>
                <div class="fc-card-row"><span class="fc-label">Current Active Tranche</span><span class="fc-value">${po.currentTranche}</span></div>
                <div class="fc-card-row"><span class="fc-label">Total Amount</span><span class="fc-value">${formatCurrency(po.totalAmount)}</span></div>
                <div class="fc-card-row"><span class="fc-label">Total Paid</span><span class="fc-value">${formatCurrency(po.totalPaid)}</span></div>
                <div class="fc-card-row"><span class="fc-label">Remaining</span><span class="fc-value fc-amount">${formatCurrency(po.remaining)}</span></div>
            </div>
            <div class="fc-card-actions">
                <button class="btn btn-sm btn-secondary" id="po-card-btn-${safePoId}" onclick="window.togglePOCardExpand('${safePoId}')">
                    <span id="po-card-chevron-${safePoId}">&#9654;</span> <span id="po-card-btn-label-${safePoId}">Show Tranches</span>
                </button>
            </div>
            <div class="fc-sub-list" id="po-card-expand-${safePoId}" style="display:none;">
                ${subCards}
            </div>
        </div>`;
}
```

Step 3: Add togglePOCardExpand function.

Insert IMMEDIATELY AFTER the existing `function togglePOExpand(poId)` at line 477-484. Do NOT modify togglePOExpand.

```
function togglePOCardExpand(safePoId) {
    const expandEl = document.getElementById('po-card-expand-' + safePoId);
    const chevron = document.getElementById('po-card-chevron-' + safePoId);
    const btnLabel = document.getElementById('po-card-btn-label-' + safePoId);
    if (!expandEl) return;
    const isOpen = expandEl.style.display === 'block';
    expandEl.style.display = isOpen ? 'none' : 'block';
    if (chevron) chevron.innerHTML = isOpen ? '&#9654;' : '&#9660;';
    if (btnLabel) btnLabel.textContent = isOpen ? 'Show Tranches' : 'Hide Tranches';
}
```

Step 4: Register on window in attachWindowFunctions.

Find `function attachWindowFunctions()` at line 191. Find the line that registers `window.togglePOExpand = togglePOExpand;` (search for it). IMMEDIATELY AFTER that line, add:

```
window.togglePOCardExpand = togglePOCardExpand;
```

Step 5: Populate poSummaryCardList inside renderPOSummaryTable.

The existing empty-state early return is at line 802-812. Modify it to also populate the card list BEFORE returning:

```
if (poEntries.length === 0) {
    const isFiltered = poSummaryStatusFilter || poSummaryDeptFilter;
    const emptyMsg = isFiltered
        ? 'No POs match the selected filters. Clear filters to see all.'
        : 'No payment requests yet. PO summaries will appear once RFPs are submitted.';
    tbody.innerHTML = '<tr><td colspan="9" style="text-align:center;padding:2rem;color:#64748b;">' + emptyMsg + '</td></tr>';
    const poSummaryCardListEmpty = document.getElementById('poSummaryCardList');
    if (poSummaryCardListEmpty) poSummaryCardListEmpty.innerHTML = '<div class="fc-empty">' + emptyMsg + '</div>';
    const paginationDiv = document.getElementById('poSummaryPagination');
    if (paginationDiv) paginationDiv.style.display = 'none';
    return;
}
```

At the BOTTOM of renderPOSummaryTable (after the updatePOSummaryPagination call, before the closing brace of the function), add:

```
// Mobile card list (Phase 73.1) — populated from same pageEntries (pagination applies to both)
const poSummaryCardList = document.getElementById('poSummaryCardList');
if (poSummaryCardList) {
    poSummaryCardList.innerHTML = pageEntries.map(po => buildPOSummaryCard(po)).join('');
}
```

Do NOT:
- Do NOT register buildPOSummaryCard, buildPOTrancheSubCard on window (internal helpers)
- Do NOT modify togglePOExpand — desktop still uses it for tr elements
- Do NOT reuse #po-expand- or #po-chevron- IDs for cards — IDs must be distinct per Pitfall 3
- Do NOT modify updatePOSummaryPagination — pagination stays visible on mobile per Pitfall 7
- Do NOT wrap rfp.due_date with formatDate inside buildPOTrancheSubCard (REVIEWS Concern 3 — must match desktop raw rendering)

</action>
<verify>
<automated>

- grep -c "function buildPOSummaryCard" app/views/finance.js  (expect 1)
- grep -c "function buildPOTrancheSubCard" app/views/finance.js  (expect 1)
- grep -c "function togglePOCardExpand" app/views/finance.js  (expect 1)
- grep -c "window.togglePOCardExpand = togglePOCardExpand" app/views/finance.js  (expect 1)
- grep -c "po-card-expand-" app/views/finance.js  (expect >=3 — id attr, toggle function, chevron companion)
- grep -c "po-card-chevron-" app/views/finance.js  (expect >=2)
- grep -c "Show Tranches" app/views/finance.js  (expect >=2 — button default label + toggle restore)
- grep -c "Hide Tranches" app/views/finance.js  (expect 1 — toggle expand state)
- Confirm NO modification to togglePOExpand: grep -c "function togglePOExpand(" app/views/finance.js still returns 1

</automated>
</verify>
<acceptance_criteria>

- `grep -c "function buildPOSummaryCard" app/views/finance.js` returns 1
- `grep -c "function buildPOTrancheSubCard" app/views/finance.js` returns 1
- `grep -c "function togglePOCardExpand" app/views/finance.js` returns 1
- `grep -c "window.togglePOCardExpand" app/views/finance.js` returns >= 2 (registration + onclick usage in card)
- `grep -c "po-card-expand-" app/views/finance.js` returns >= 3
- `grep -c "po-card-chevron-" app/views/finance.js` returns >= 2
- `grep -c "Show Tranches" app/views/finance.js` returns >= 2
- `grep -c "Hide Tranches" app/views/finance.js` returns >= 1
- `grep -c "function togglePOExpand(" app/views/finance.js` returns 1 (desktop function NOT removed)
- `grep -c "getElementById('poSummaryCardList')" app/views/finance.js` returns 2 (empty path + populated path)
- buildPOTrancheSubCard renders due_date raw: `grep -A 30 "function buildPOTrancheSubCard" app/views/finance.js | grep -c "formatDate(rfp.due_date)"` returns 0 (REVIEWS Concern 3)
- Visual 375px: PO Summary table hidden; card stack visible; tap Show Tranches → sub-list slides in below; chevron flips; label changes to Hide Tranches; tap again to collapse; multiple cards can be expanded simultaneously
- Visual: Tranche sub-card Due Date shows identical text as desktop at 1280px (same raw string, no formatting drift)
- Visual 1280px: Desktop PO Summary table visible with chevron expand/collapse working via togglePOExpand; card stack hidden
- Visual: poSummaryPagination Previous/Next controls visible at 375px and functional
- Visual: Filter to a value with no results → fc-empty card-list message shown; table empty state also shown

</acceptance_criteria>
<done>

PO Payment Summary has mobile card alternative with expandable tranche sub-cards. Uses distinct IDs and toggle function to avoid colliding with desktop togglePOExpand. Pagination remains visible on mobile. Due Date rendered raw to match desktop (REVIEWS Concern 3). No desktop regression.

</done>
</task>

# Verification

1. Automated: run all grep acceptance criteria from both tasks. All must pass.

2. Desktop regression at 1280px:
   - Finance > Payables RFP Processing table renders identically to pre-73.1
   - RFP actions (Record Payment, Manage Payments) route through existing window.openRecordPaymentModal
   - PO Summary table chevron expand/collapse works via desktop togglePOExpand targeting tr elements

3. Mobile visual at 375px:
   - Both Payables tables hidden; card stacks visible
   - Overdue RFP cards have red tint; status badges render with correct colors
   - PO Summary cards: Show Tranches toggle reveals tranche sub-list; chevron updates; label flips
   - Multiple PO cards can be expanded simultaneously (no single-expand accordion)
   - Pagination controls visible and functional on mobile
   - Due Date text in RFP cards AND tranche sub-cards matches desktop exactly (REVIEWS Concern 3)

4. Cross-view regression:
   - Procurement > Records at 375px still shows tables with horizontal scroll (Finance scoping preserved)

5. Empty state:
   - Filter RFP status to "Fully Paid" when no fully-paid RFPs exist → fc-empty message with matching text
   - Filter PO Summary dept to one with no POs → fc-empty with matching text

# Success Criteria

- Both sibling fc-card-list divs live in static render() HTML for Payables
- buildRFPCard, buildPOSummaryCard, buildPOTrancheSubCard helpers defined and called
- togglePOCardExpand defined and registered on window
- renderRFPTable and renderPOSummaryTable populate both tbody and card list
- Empty-state parity between table and card list for both tables
- Desktop Payables behavior unchanged (togglePOExpand still works for tr elements)
- Due Date rendered raw in both RFP cards and tranche sub-cards (REVIEWS Concern 3)
- No cross-view regression to Procurement/Suppliers/MRF Records

# Output

After completion, create .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-03-SUMMARY.md
</content>
</invoke>