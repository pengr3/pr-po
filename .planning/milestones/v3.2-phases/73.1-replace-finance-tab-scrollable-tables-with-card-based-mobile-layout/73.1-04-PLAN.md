---
phase: 73.1
plan: 04
type: execute
wave: 3
depends_on:
  - "73.1-01"
  - "73.1-02"
  - "73.1-03"
files_modified:
  - app/views/finance.js
autonomous: false
requirements:
  - MOBCARD-08
  - MOBCARD-09
user_setup: []

must_haves:
  truths:
    - "At viewport 375px on Finance > Project List > Projects sub-tab, the projects expense table is hidden and a vertical stack of clickable expense cards is visible instead"
    - "At viewport 375px on Finance > Project List > Services sub-tab, the services expense table is hidden and clickable expense cards are shown"
    - "At viewport 375px on Finance > Project List > Recurring sub-tab, the recurring services table is hidden and clickable expense cards are shown"
    - "At viewport 1280px on Project List, all three tables are visible and no card stacks are visible"
    - "Tapping anywhere on an expense card opens window.showProjectExpenseModal (projects) or window.showServiceExpenseModal (services and recurring) with the correct identifier — matches desktop tr onclick behavior"
    - "Expense cards with negative remainingBudget show red styling and warning indicator, matching desktop table row"
    - "Project names containing single quotes (apostrophes) do not break the onclick handler — modal opens correctly (REVIEWS Suggestion 3)"
    - "At 320px viewport, no Finance tab produces horizontal page overflow on any sub-tab"
    - "Across all Finance tabs, desktop behavior at 1280px is unchanged (tables render, card lists hidden, sort, filter, pagination, modals all work)"
    - "Procurement, Suppliers, MRF Records views at 375px still show tables with horizontal scroll (Finance-scoped CSS rule does not leak)"
  artifacts:
    - path: "app/views/finance.js"
      provides: "buildProjectExpenseCard, buildServiceExpenseCard, buildRecurringExpenseCard helpers; three render functions emit both table-scroll-container and fc-card-list in their container.innerHTML"
      contains: "function buildProjectExpenseCard"
  key_links:
    - from: "renderProjectExpensesTable"
      to: "fc-card-list inside projectExpensesContainer"
      via: "container.innerHTML template literal"
      pattern: "buildProjectExpenseCard"
    - from: "renderServiceExpensesTable"
      to: "fc-card-list inside serviceExpensesContainer"
      via: "container.innerHTML template literal"
      pattern: "buildServiceExpenseCard"
    - from: "renderRecurringExpensesTable"
      to: "fc-card-list inside recurringExpensesContainer"
      via: "container.innerHTML template literal"
      pattern: "buildRecurringExpenseCard"
---

# Objective

Convert the three Project List expense tables (Projects, Services, Recurring) to mobile card layouts at viewports 768px and below, then complete a human-verify checkpoint covering the full Finance tab across all 4 sub-tabs and the desktop regression.

All three expense render functions use Pattern B (full-container re-render) — the render function assigns container.innerHTML with the complete HTML including the table wrapper. Cards must be emitted in the SAME innerHTML assignment so both structures exist in the DOM simultaneously and CSS dual-mode governs visibility.

Expense rows are click-everywhere (tr onclick). Per RESEARCH Pitfall 6, the cards use the same pattern: entire fc-card div carries onclick, with fc-clickable class for hover styling. No nested action button — mirrors desktop tr behavior exactly.

**Review feedback applied (73.1-REVIEWS.md):**
- Reviewer flagged no blocking issues in Plan 04 ("Plan 04 human-verify checkpoint is well-specified")
- Suggestion 3 applied: `escapeHTML(proj.projectName)` inside the onclick string converts single quotes to `&#39;`, which HTML-decodes back to `'` before JS evaluation. Safe by browser behavior, but the human-verify checkpoint now explicitly tests a project name containing an apostrophe

## Output

- buildProjectExpenseCard(proj) helper
- buildServiceExpenseCard(svc) helper
- buildRecurringExpenseCard(svc) helper
- renderProjectExpensesTable(), renderServiceExpensesTable(), renderRecurringExpensesTable() modified so each container.innerHTML emits table AND card list
- Final checkpoint: human verification of Finance tab on mobile (375px) and desktop (1280px), including apostrophe smoke-test

# Execution Context

See workflow files:
- $HOME/.claude/get-shit-done/workflows/execute-plan.md
- $HOME/.claude/get-shit-done/templates/summary.md

# Context Files

- .planning/ROADMAP.md
- .planning/STATE.md
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-RESEARCH.md
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-UI-SPEC.md
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-REVIEWS.md
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-01-PLAN.md
- app/views/finance.js

## Interfaces

Pattern B render functions (full-container):
- renderProjectExpensesTable() — finance.js line 2035, populates #projectExpensesContainer
- renderServiceExpensesTable() — finance.js line 2305, populates #serviceExpensesContainer
- renderRecurringExpensesTable() — finance.js line 2392, populates #recurringExpensesContainer

Data shapes:
- projectExpenses items have: projectName, projectCode, clientCode, budget, totalExpense, remainingBudget, active (boolean)
- serviceExpenses items have: serviceName, serviceCode, clientCode, budget, totalExpense, remainingBudget, status ('active'|'inactive')
- recurringExpenses items have: same shape as serviceExpenses

Existing state:
- projectExpenseSearchTerm, serviceExpenseSearchTerm, recurringExpenseSearchTerm
- projectExpenseSortColumn/Direction, serviceExpenseSortColumn/Direction, recurringExpenseSortColumn/Direction

Window onclick targets already registered:
- window.showProjectExpenseModal(projectName) — opens expense modal for project
- window.showServiceExpenseModal(serviceCode, budget) — opens modal for service (one-time or recurring)

CSS classes from Plan 01:
- fc-card.fc-clickable — cursor:pointer, hover border + shadow ring
- fc-empty — empty state

# Tasks

<task type="auto">
<name>Task 1: Add expense-card helpers and emit card lists in all three render functions</name>
<files>app/views/finance.js</files>

Type: auto
Files: app/views/finance.js

<read_first>

- app/views/finance.js (lines 2035-2138 renderProjectExpensesTable; lines 2305-2386 renderServiceExpensesTable; lines 2392-2473 renderRecurringExpensesTable)
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-RESEARCH.md (Table 4a expense column mapping; Pitfall 6)
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-UI-SPEC.md (Interaction Contract section 3; Copywriting Contract empty states)
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-REVIEWS.md (Suggestion 3: apostrophe safety via escapeHTML in onclick)

</read_first>
<behavior>

- buildProjectExpenseCard(proj) returns fc-card.fc-clickable div with onclick opening window.showProjectExpenseModal; header shows projectName (bold) + status badge (Active/Inactive); body shows Code, Client, Budget, Total Expense, Remaining (red with warning when over budget)
- buildServiceExpenseCard(svc) same structure but calls window.showServiceExpenseModal and uses status === 'active' condition
- buildRecurringExpenseCard(svc) same as service card (identical shape) but uses recurring data source — implemented as thin alias to buildServiceExpenseCard per RESEARCH guidance
- Each render function's container.innerHTML assignment emits BOTH table-scroll-container and fc-card-list in the same string
- Empty-state branches: for Pattern B the empty state already replaces container.innerHTML with a single "no results" div — that div is visible on both desktop and mobile since it's NOT inside .table-scroll-container and not an .fc-card-list, so no change needed to empty-state branches
- onclick strings use escapeHTML() around projectName / serviceCode to safely handle apostrophes (browser HTML-decodes &#39; back to ' before JS evaluation, per REVIEWS Suggestion 3)

</behavior>
<action>

Step 1: Add buildProjectExpenseCard helper.

Insert IMMEDIATELY BEFORE `function renderProjectExpensesTable()` (line 2035):

```
function buildProjectExpenseCard(proj) {
    const isOverBudget = proj.remainingBudget < 0;
    const remainingColor = isOverBudget ? '#ef4444' : '#1e293b';
    const remainingWeight = isOverBudget ? '700' : '400';
    const statusClass = proj.active ? 'approved' : 'rejected';
    const statusLabel = proj.active ? 'Active' : 'Inactive';
    return `
        <div class="fc-card fc-clickable" onclick="window.showProjectExpenseModal('${escapeHTML(proj.projectName)}')">
            <div class="fc-card-header">
                <span class="fc-card-id">${escapeHTML(proj.projectName)}</span>
                <span class="status-badge ${statusClass}">${statusLabel}</span>
            </div>
            <div class="fc-card-body">
                <div class="fc-card-row"><span class="fc-label">Code</span><span class="fc-value" style="font-size:0.8125rem;color:#64748b;">${escapeHTML(proj.projectCode)}</span></div>
                <div class="fc-card-row"><span class="fc-label">Client</span><span class="fc-value">${escapeHTML(proj.clientCode)}</span></div>
                <div class="fc-card-row"><span class="fc-label">Budget</span><span class="fc-value">₱${formatCurrency(proj.budget)}</span></div>
                <div class="fc-card-row"><span class="fc-label">Total Expense</span><span class="fc-value">₱${formatCurrency(proj.totalExpense)}</span></div>
                <div class="fc-card-row"><span class="fc-label">Remaining</span><span class="fc-value" style="color:${remainingColor};font-weight:${remainingWeight};">${isOverBudget ? '⚠️ ' : ''}₱${formatCurrency(Math.abs(proj.remainingBudget))}${isOverBudget ? ' over' : ''}</span></div>
            </div>
        </div>`;
}
```

Note: escapeHTML() converts `'` to `&#39;`. Browser parses the onclick attribute and HTML-decodes the entity back to `'` before JS execution. Safe for names like "O'Brien Construction" — verified by reviewer in REVIEWS.md Suggestion 3. The human-verify checkpoint in Task 2 explicitly exercises this with an apostrophe-containing project name.

Step 2: Modify renderProjectExpensesTable() to emit both table and card list.

Find the `container.innerHTML = tableHTML;` line at line 2125. BEFORE that assignment, append the card list HTML to tableHTML:

Replace the block starting at line 2119 (`tableHTML += ...` closing the table) through line 2125 with:

```
    tableHTML += `
                </tbody>
            </table>
        </div>
        <div class="fc-card-list">
            ${filtered.map(proj => buildProjectExpenseCard(proj)).join('')}
        </div>
    `;

    container.innerHTML = tableHTML;
```

Step 3: Add buildServiceExpenseCard helper.

Insert IMMEDIATELY BEFORE `function renderServiceExpensesTable()` (line 2305):

```
function buildServiceExpenseCard(svc) {
    const isOverBudget = svc.remainingBudget < 0;
    const remainingColor = isOverBudget ? '#ef4444' : '#1e293b';
    const remainingWeight = isOverBudget ? '700' : '400';
    const isActive = svc.status === 'active';
    const badgeClass = isActive ? 'badge-success' : 'badge-secondary';
    const statusLabel = isActive ? 'Active' : 'Inactive';
    return `
        <div class="fc-card fc-clickable" onclick="window.showServiceExpenseModal('${escapeHTML(svc.serviceCode)}', ${svc.budget})">
            <div class="fc-card-header">
                <span class="fc-card-id">${escapeHTML(svc.serviceName)}</span>
                <span class="badge ${badgeClass}">${statusLabel}</span>
            </div>
            <div class="fc-card-body">
                <div class="fc-card-row"><span class="fc-label">Code</span><span class="fc-value" style="font-size:0.8125rem;color:#64748b;">${escapeHTML(svc.serviceCode)}</span></div>
                <div class="fc-card-row"><span class="fc-label">Client</span><span class="fc-value">${escapeHTML(svc.clientCode)}</span></div>
                <div class="fc-card-row"><span class="fc-label">Budget</span><span class="fc-value">₱${formatCurrency(svc.budget)}</span></div>
                <div class="fc-card-row"><span class="fc-label">Total Expense</span><span class="fc-value">₱${formatCurrency(svc.totalExpense)}</span></div>
                <div class="fc-card-row"><span class="fc-label">Remaining</span><span class="fc-value" style="color:${remainingColor};font-weight:${remainingWeight};">${isOverBudget ? '⚠️ ' : ''}₱${formatCurrency(Math.abs(svc.remainingBudget))}${isOverBudget ? ' over' : ''}</span></div>
            </div>
        </div>`;
}
```

Step 4: Modify renderServiceExpensesTable() to emit both table and card list.

Find the `tableHTML += \`</tbody></table></div>\`;` line at line 2372. Replace with:

```
    tableHTML += `</tbody></table></div>
        <div class="fc-card-list">
            ${sorted.map(svc => buildServiceExpenseCard(svc)).join('')}
        </div>`;
```

(The existing innerHTML assignment and sort-indicator block stay unchanged.)

Step 5: Add buildRecurringExpenseCard helper.

Insert IMMEDIATELY BEFORE `function renderRecurringExpensesTable()` (line 2392):

```
function buildRecurringExpenseCard(svc) {
    // Recurring expenses share the same data shape as one-time services.
    // Both use window.showServiceExpenseModal per existing tr onclick pattern.
    return buildServiceExpenseCard(svc);
}
```

Note: The desktop recurring table's tr onclick at line 2439 uses `window.showServiceExpenseModal` — identical to service row, so buildServiceExpenseCard can be reused directly. Keeping buildRecurringExpenseCard as a thin alias documents the architectural intent and leaves room for future divergence.

Step 6: Modify renderRecurringExpensesTable() to emit both table and card list.

Find the `tableHTML += \`</tbody></table></div>\`;` line at line 2459. Replace with:

```
    tableHTML += `</tbody></table></div>
        <div class="fc-card-list">
            ${sorted.map(svc => buildRecurringExpenseCard(svc)).join('')}
        </div>`;
```

Do NOT:
- Do NOT register build* helpers on window (internal)
- Do NOT modify the empty-state early-return blocks (they return before tableHTML is built — empty state div already works on both viewports since it's NOT inside .table-scroll-container)
- Do NOT add a nested action button inside expense cards — entire card is clickable (Pitfall 6)
- Do NOT modify any sort function
- Do NOT omit escapeHTML() around the onclick arguments — this guards against apostrophe breakage (REVIEWS Suggestion 3)

</action>
<verify>
<automated>

- grep -c "function buildProjectExpenseCard" app/views/finance.js  (expect 1)
- grep -c "function buildServiceExpenseCard" app/views/finance.js  (expect 1)
- grep -c "function buildRecurringExpenseCard" app/views/finance.js  (expect 1)
- grep -c "fc-card fc-clickable" app/views/finance.js  (expect >= 2 — appears in two distinct build helpers; 3rd reuses builder)
- grep -c "class=\"fc-card-list\"" app/views/finance.js  (expect >= 6 — 2 from Plan 01 approvals, 1 from Plan 02 POs, 2 from Plan 03 Payables, 3 from this plan)
- grep -c "window.showProjectExpenseModal" app/views/finance.js  (expect >= 2 — table row + card)
- grep -c "window.showServiceExpenseModal" app/views/finance.js  (expect >= 3 — service table row, recurring table row, card onclick via buildServiceExpenseCard)
- grep -c "escapeHTML(proj.projectName)" app/views/finance.js  (expect >= 1 — inside buildProjectExpenseCard onclick)
- grep -c "escapeHTML(svc.serviceCode)" app/views/finance.js  (expect >= 1 — inside buildServiceExpenseCard onclick)

</automated>
</verify>
<acceptance_criteria>

- `grep -c "function buildProjectExpenseCard" app/views/finance.js` returns 1
- `grep -c "function buildServiceExpenseCard" app/views/finance.js` returns 1
- `grep -c "function buildRecurringExpenseCard" app/views/finance.js` returns 1
- `grep -c "fc-card fc-clickable" app/views/finance.js` returns >= 2
- `grep -c "buildProjectExpenseCard(proj)" app/views/finance.js` returns 1 (called inside renderProjectExpensesTable)
- `grep -c "buildServiceExpenseCard(svc)" app/views/finance.js` returns >= 2 (called in service render AND inside buildRecurringExpenseCard body)
- `grep -c "buildRecurringExpenseCard(svc)" app/views/finance.js` returns 1 (called inside renderRecurringExpensesTable)
- `grep -c "window.showProjectExpenseModal" app/views/finance.js` returns >= 2 (existing tr onclick + new card onclick)
- `grep -c "window.showServiceExpenseModal" app/views/finance.js` returns >= 3
- `grep -c "escapeHTML(proj.projectName)" app/views/finance.js` returns >= 1 (onclick-safe apostrophe handling — REVIEWS Suggestion 3)
- `grep -c "escapeHTML(svc.serviceCode)" app/views/finance.js` returns >= 1 (onclick-safe for service code)
- Visual 375px Projects sub-tab: projects table hidden; clickable expense cards visible; tap any card → showProjectExpenseModal opens with correct project name
- Visual 375px Services sub-tab: services cards visible; tap → service expense modal opens with serviceCode + budget
- Visual 375px Recurring sub-tab: recurring cards visible; tap → service expense modal opens (same handler)
- Over-budget card: Remaining row shows red text, warning emoji, " over" suffix
- Visual 1280px: all three tables visible with click-everywhere rows still working; no card lists visible

</acceptance_criteria>
<done>

Three expense render functions emit both table and card-list structures. Cards are fully clickable per Pitfall 6. Over-budget highlighting preserved. No action button nested inside card (matches desktop tr click-everywhere). Apostrophe-safe onclick via escapeHTML.

</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
<name>Task 2: Cross-tab regression checkpoint</name>
<files>(checkpoint — no files modified; manual browser verification)</files>

<read_first>

- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-UI-SPEC.md (entire spec — used to judge visual parity)
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-REVIEWS.md (Suggestion 3 apostrophe smoke-test)
- .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-01-PLAN.md through 73.1-04-PLAN.md (acceptance criteria across all plans)

</read_first>

<action>
Pause execution. Human verifier performs the manual browser regression described in what-built / how-to-verify below. Resume only on approval.
</action>

<verify>
<automated>MISSING — this task requires manual human verification across 4 Finance sub-tabs at 375px, 320px, and 1280px viewports per how-to-verify instructions. No automated command replaces human visual inspection.</automated>
</verify>

<acceptance_criteria>

- All 10 how-to-verify steps below executed in Chrome DevTools responsive mode
- Mobile (375px): card stacks visible on every Finance sub-tab, tables hidden, no horizontal page overflow
- Mobile (320px): no horizontal page overflow on any Finance sub-tab; tab dropdown fits width
- Desktop (1280px): all tables render as before; card lists hidden; sort, filter, pagination, all modals work
- Cross-view: Procurement, Suppliers, Projects views at 375px still show tables with horizontal scroll (no CSS leak)
- Empty state: fc-empty text matches tbody colspan empty-state text across all 5 Pattern A tables
- Apostrophe smoke-test: project name containing `'` (e.g. "O'Brien Construction") — tap card → modal opens correctly with full name displayed

</acceptance_criteria>

<done>
User responds "approved" confirming: all mobile card stacks render correctly across Pending Approvals, Purchase Orders, Payables (RFP + PO Summary with expand), and Project List (Projects/Services/Recurring); desktop tables render unchanged at 1280px; no horizontal page overflow at 320px; Procurement/Suppliers/Projects views unaffected; empty-state parity between table and card list on all 5 table groups; apostrophe-containing project names open modal correctly.
</done>

<what-built>

Complete Finance tab mobile card layout across all 4 sub-tabs:
- Pending Approvals: Material PRs and Transport Requests cards (Plan 01)
- Purchase Orders: PO cards with proof indicator (Plan 02)
- Payables: RFP Processing cards and PO Payment Summary cards with expand toggle (Plan 03)
- Project List: Projects, Services, Recurring expense cards (this plan, Task 1)

Plus CSS foundation (Plan 01 Task 1) and the extraction of buildProofIndicator (Plan 02 Task 1).

</what-built>
<how-to-verify>

Start local server: `python -m http.server 8000`

Open `http://localhost:8000/` in Chrome DevTools with responsive mode.

**Mobile (375px viewport) checks across every Finance sub-tab:**

1. Navigate to `#/finance/approvals`. Expect:
   - Material PRs card stack visible, table hidden
   - Each card shows PR ID (bold), urgency badge, Project, Supplier, Amount, Date Issued, Date Needed, MRF, full-width "Review PR" button
   - Transport Requests card stack below — same pattern with TR ID and "Review TR"
   - Tap "Review PR" → PR Details modal opens
   - No horizontal page overflow (no horizontal scroll bar at page level)

2. Navigate to `#/finance/pos`. Expect:
   - PO card stack visible, table hidden
   - Each card shows PO ID, status badge, proof indicator (3-state: green check / orange dash / empty circle), supplier, project, amount, PR, date, "View PO" button
   - Tap View PO → PO Document modal opens
   - For a PO with proof URL: tap green check → opens URL in new tab; long-press (>600ms) → proof edit modal

3. Navigate to `#/finance/projects`. Expect:
   - Sub-tab bar (Projects | Services | Recurring) scrolls horizontally without clipping labels at 320px
   - Projects sub-tab: clickable expense cards stack; tap → project expense breakdown modal opens
   - Switch to Services sub-tab: service expense cards; tap → service expense modal opens
   - Switch to Recurring sub-tab: recurring expense cards; tap → same service expense modal (but with recurring data)
   - Over-budget projects/services show red Remaining with warning

4. Navigate to `#/finance/payables`. Expect:
   - RFP Processing cards: each shows RFP ID, status badge (Overdue cards have red tint), Supplier, PO Ref (clickable), Project/Service, Tranche, Amount, Paid, Balance, Due Date, Record Payment button
   - Tap Record Payment → modal opens; voided payment remains visible in history
   - Overdue RFP cards display fc-overdue red tint
   - PO Payment Summary cards: PO ID (link), status badge, supplier, project/service, current tranche, total amount, total paid, remaining, Show Tranches button
   - Tap Show Tranches → sub-list reveals tranche sub-cards; chevron rotates; button label changes to Hide Tranches
   - Expand multiple PO cards simultaneously — each tranche sub-list persists
   - Pagination Previous/Next controls visible below cards; tapping changes page; card stack updates

5. Narrow to 320px. Expect:
   - No Finance sub-tab produces horizontal page overflow
   - Tab dropdown (finance-tab-select) fits width
   - Sub-tab bar in Project List scrolls horizontally without clipping

**Apostrophe smoke-test (REVIEWS Suggestion 3):**

6. While at 375px on Finance > Project List > Projects: locate any project with an apostrophe in its name (e.g. "O'Brien Construction"). If none exists in live data, temporarily edit a project document in Firestore to add an apostrophe to projectName, then hard-reload the page.
   - Expect: card header text displays the apostrophe correctly (no `&#39;` leak)
   - Tap the card: window.showProjectExpenseModal receives the full project name (check modal title)
   - Check browser console: NO "SyntaxError" or "Uncaught" messages from onclick
   - If you edited live data, revert the change after the test

**Desktop (1280px viewport) checks:**

7. Resize to 1280px. Navigate to each Finance sub-tab. Expect:
   - All tables render identically to pre-73.1 (no visual regression)
   - Card lists hidden (display:none from CSS default)
   - Sort indicators work on all sortable columns
   - PO Summary desktop chevron expand/collapse works (togglePOExpand targets <tr> elements with display:table-row — distinct from card toggle)
   - Filter dropdowns (status, department) work on RFP Processing, PO Summary, Material PRs, Transport Requests, Purchase Orders
   - RFP Record Payment and Manage Payments work on table rows

**Cross-view regression checks:**

8. Navigate to `#/procurement/records` at 375px → procurement tables still show with horizontal scroll (NOT replaced by cards — Finance-scoped rule must not leak)
9. Navigate to `#/procurement/suppliers` at 375px → supplier table still has horizontal scroll
10. Navigate to `#/projects` at 375px → projects table still has horizontal scroll

**Empty-state checks:**

- Filter RFP Status to a value with no results → fc-empty card-list message matches the tbody colspan=11 text
- Filter PO Summary to a dept with no POs → fc-empty matches tbody colspan=9 text
- Set Material PRs dept filter to Services when no service PRs exist → fc-empty with "No pending material PRs to review for Services"

</how-to-verify>
<resume-signal>

Type "approved" if all checks pass, or describe any issues (e.g. "card header text overflows at 320px", "proof long-press does not trigger modal on iPhone Safari", or "apostrophe project name breaks onclick").

</resume-signal>
</task>

# Verification

- Automated: run all grep acceptance criteria from Task 1 — all must pass.
- Manual: full Task 2 checklist (steps 1-10) executed in browser, including apostrophe smoke-test.
- Git: confirm no unrelated changes in finance.js diff (only expense-card helper additions and container.innerHTML modifications).

# Success Criteria

- Three expense-card helpers defined and called by their respective render functions
- Each expense render function emits both table and card-list in container.innerHTML
- onclick strings use escapeHTML() for safe apostrophe handling (REVIEWS Suggestion 3)
- All 5 Finance table groups (Material PRs, TR, PO, RFP, PO Summary, plus 3 expense tables) have mobile card alternatives
- Desktop (1280px) behavior unchanged across all Finance tabs
- No Procurement / Suppliers / MRF Records / Projects view regression
- Checkpoint Task 2 approved by user (including apostrophe test)

# Output

After completion, create .planning/phases/73.1-replace-finance-tab-scrollable-tables-with-card-based-mobile-layout/73.1-04-SUMMARY.md
