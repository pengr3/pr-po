---
phase: 68-fix-expense-breakdown-modal-remove-document-count-from-scoreboard-and-show-line-items-instead-of-po-id
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/expense-modal.js
autonomous: true
requirements:
  - EXPMOD-01
  - EXPMOD-02

must_haves:
  truths:
    - "Total Cost scoreboard card in expense breakdown modal shows only the total amount with no document count note below it"
    - "Category item tables in expense breakdown modal show Item Name as the first column instead of PO ID"
    - "Transport & Hauling item tables show Item Name as the first column instead of PO ID"
    - "Delivery Fees table shows Supplier as the first column instead of PO ID"
    - "CSV export continues to work with all data fields intact"
  artifacts:
    - path: "app/expense-modal.js"
      provides: "Expense breakdown modal with corrected scoreboard and table columns"
      contains: "Total Cost"
  key_links:
    - from: "app/expense-modal.js"
      to: "showExpenseBreakdownModal"
      via: "function export"
      pattern: "export async function showExpenseBreakdownModal"
---

<objective>
Fix the expense breakdown modal by removing the document count note from the Total Cost scoreboard card and replacing PO ID columns in the item breakdown tables with the item name (line items).

Purpose: The document count ("N documents") below Total Cost is misleading/unnecessary, and users want to see what they bought (item names) rather than which PO it came from when scanning the breakdown tables.

Output: Updated app/expense-modal.js with cleaner Total Cost display and item-focused table columns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@app/expense-modal.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove document count from Total Cost and replace PO ID columns with line item details</name>
  <files>app/expense-modal.js</files>
  <read_first>app/expense-modal.js</read_first>
  <action>
Make these exact changes to app/expense-modal.js:

**1. Remove document count from Total Cost scoreboard (line ~315)**

Find the Total Cost card div (around line 312-316). Remove the document count line entirely. Change from:

```html
<div style="background: #eff6ff; border: 2px solid #3b82f6; padding: 1rem; border-radius: 8px; margin-bottom: 2rem;">
    <div style="font-size: 0.875rem; color: #1e40af; font-weight: 600; margin-bottom: 0.5rem;">Total Cost</div>
    <div style="font-size: 2rem; font-weight: 700; color: #1e40af;">&#8369;${formatCurrency(totalCost)}</div>
    <div style="font-size: 0.75rem; color: #1e40af; margin-top: 0.25rem;">${materialCount + transportRequests.length + subconCount} documents</div>
</div>
```

To:

```html
<div style="background: #eff6ff; border: 2px solid #3b82f6; padding: 1rem; border-radius: 8px; margin-bottom: 2rem;">
    <div style="font-size: 0.875rem; color: #1e40af; font-weight: 600; margin-bottom: 0.5rem;">Total Cost</div>
    <div style="font-size: 2rem; font-weight: 700; color: #1e40af;">&#8369;${formatCurrency(totalCost)}</div>
</div>
```

Delete the entire `<div>` that contains `${materialCount + transportRequests.length + subconCount} documents</div>`.

**2. Replace PO ID column with Item Name in category items table (line ~177)**

In the categoryHTML builder, change the table header and row for material category items. Change from:

```html
<thead><tr><th>PO ID</th><th>Item</th><th>Qty</th><th>Unit Cost</th><th style="text-align: right;">Subtotal</th></tr></thead>
```

To:

```html
<thead><tr><th>Item</th><th>Qty</th><th>Unit</th><th>Unit Cost</th><th style="text-align: right;">Subtotal</th></tr></thead>
```

And change the row template from:

```html
<tr>
    <td>${item.po_id}</td><td>${item.item_name}</td>
    <td>${item.quantity} ${item.unit}</td><td>${formatCurrency(item.unit_cost)}</td>
    <td style="text-align: right;">${formatCurrency(item.subtotal)}</td>
</tr>
```

To:

```html
<tr>
    <td>${item.item_name}</td>
    <td>${item.quantity}</td><td>${item.unit}</td><td>${formatCurrency(item.unit_cost)}</td>
    <td style="text-align: right;">${formatCurrency(item.subtotal)}</td>
</tr>
```

Note: Split quantity and unit into separate columns for cleaner display. Remove PO ID cell entirely.

**3. Replace PO ID column with Item Name in Transportation & Hauling table (line ~228)**

Same change for the transportCategoryItems table. Change header from:

```html
<thead><tr><th>PO ID</th><th>Item</th><th>Qty</th><th>Unit Cost</th><th style="text-align: right;">Subtotal</th></tr></thead>
```

To:

```html
<thead><tr><th>Item</th><th>Qty</th><th>Unit</th><th>Unit Cost</th><th style="text-align: right;">Subtotal</th></tr></thead>
```

And change the row template to match the same pattern as change #2 (remove po_id cell, split quantity and unit).

**4. Replace PO ID column with Supplier in Delivery Fees table (line ~253)**

Change the Delivery Fees table header from:

```html
<thead><tr><th>PO ID</th><th style="text-align: right;">Amount</th></tr></thead>
```

To:

```html
<thead><tr><th>Supplier</th><th style="text-align: right;">Amount</th></tr></thead>
```

And change the row from:

```html
<tr><td>${item.po_id}</td><td style="text-align: right;">${formatCurrency(item.amount)}</td></tr>
```

To:

```html
<tr><td>${item.supplier}</td><td style="text-align: right;">${formatCurrency(item.amount)}</td></tr>
```

The supplier field is already collected and stored on deliveryFeeItems (line 73: `deliveryFeeItems.push({ po_id: po.po_id, amount: fee, date: poDate, supplier: poSupplier })`), so no data changes needed.

**5. Do NOT change the CSV export function** (`_exportExpenseBreakdownCSV`). The CSV export continues to include all fields including PO ID, date, supplier — these are useful for spreadsheet analysis.

**6. Do NOT change the Transport Requests table** (lines 206-212) — that table uses TR ID and Supplier which is correct.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('app/expense-modal.js','utf8'); const checks=[!c.includes('documents</div>'), !c.includes('<th>PO ID</th>'), c.includes('<th>Item</th><th>Qty</th><th>Unit</th><th>Unit Cost</th>'), c.includes('<th>Supplier</th><th style=\"text-align: right;\">Amount</th>'), c.includes('_exportExpenseBreakdownCSV'), c.includes('Total Cost')]; console.log(checks.every(Boolean)?'PASS':'FAIL:'+JSON.stringify(checks));"</automated>
  </verify>
  <acceptance_criteria>
    - app/expense-modal.js does NOT contain the string `documents</div>` (document count line removed from Total Cost)
    - app/expense-modal.js does NOT contain `<th>PO ID</th>` anywhere (all PO ID column headers removed)
    - app/expense-modal.js contains `<th>Item</th><th>Qty</th><th>Unit</th><th>Unit Cost</th>` for category and transport tables
    - app/expense-modal.js contains `<th>Supplier</th><th style="text-align: right;">Amount</th>` for delivery fees table
    - app/expense-modal.js still contains `_exportExpenseBreakdownCSV` function (CSV export preserved)
    - app/expense-modal.js still contains `<th>TR ID</th>` in Transport Requests section (unchanged)
    - app/expense-modal.js still contains `Total Cost` text in the scoreboard card
  </acceptance_criteria>
  <done>Total Cost card shows amount only with no document count. All item breakdown tables show item names and details instead of PO IDs. CSV export unchanged. Transport Requests table unchanged.</done>
</task>

</tasks>

<verification>
1. Open the app and navigate to a project or service detail page
2. Click the expense breakdown button to open the modal
3. Verify: Total Cost scoreboard shows only the peso amount — no "N documents" text below
4. Verify: By Category tab tables show Item | Qty | Unit | Unit Cost | Subtotal columns (no PO ID)
5. Verify: Transport Fees tab — Transport & Hauling table shows Item columns (no PO ID), Delivery Fees table shows Supplier column (no PO ID), Transport Requests table still shows TR ID and Supplier
6. Verify: CSV export button still works and includes all fields
</verification>

<success_criteria>
- Total Cost scoreboard is clean — amount only, no document count note
- All item breakdown tables show line item details instead of PO ID
- CSV export is unaffected
- Transport Requests section (TR ID / Supplier / Amount) is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/68-fix-expense-breakdown-modal-remove-document-count-from-scoreboard-and-show-line-items-instead-of-po-id/68-01-SUMMARY.md`
</output>
