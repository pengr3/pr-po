# Phase 65.9: Integrate Delivery Fees to Enable RFP for Delivery - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-26
**Phase:** 65.9-integrate-delivery-fees-rfp-support
**Areas discussed:** RFP Trigger, Data Structure, Capture Flow, Finance Display, Visual Cue

---

## RFP Trigger

| Option | Description | Selected |
|--------|-------------|----------|
| Extend context menu | Add "Request Delivery Fee Payment" to right-click menu on PO ID cell | ✓ |
| Dedicated button on PO row | Separate button visible when delivery_fee > 0 | |
| Auto-create RFP on fee entry | Automatically create RFP when delivery fee is entered in prompt | |

**User's choice:** Extend context menu

| Option | Description | Selected |
|--------|-------------|----------|
| Only after Delivered | Option shown only when po.delivery_fee > 0 | ✓ |
| Always visible | Always show; error if no delivery fee set | |

**User's choice:** Only after Delivered (when delivery_fee > 0)

| Option | Description | Selected |
|--------|-------------|----------|
| Disable if RFP exists | Gray out option with "RFP exists" if delivery fee RFP already created | ✓ |
| Allow duplicates | No dedup guard | |

**User's choice:** Disable if RFP exists

---

## Data Structure

| Option | Description | Selected |
|--------|-------------|----------|
| Synthetic tranche | tranche_label='Delivery Fee', tranche_percentage=null, amount_requested=delivery_fee | ✓ |
| New rfp_type field | Add rfp_type: 'delivery_fee' \| 'tranche' to rfps documents | |
| Separate collection | New 'delivery_rfps' collection | |

**User's choice:** Synthetic tranche — reuse existing rfps schema

| Option | Description | Selected |
|--------|-------------|----------|
| Simplified modal | Pre-fills Supplier, Amount, Due Date, Mode of Payment — no tranche dropdown | ✓ |
| Same modal with hardcoded tranche | Reuse openRFPModal() with synthetic tranche array | |

**User's choice:** Simplified modal (new openDeliveryFeeRFPModal function)

---

## Capture Flow

| Option | Description | Selected |
|--------|-------------|----------|
| Keep prompt as-is | Existing prompt() unchanged; RFP is a separate step via context menu | ✓ |
| Prompt + immediate RFP offer | Follow-up modal after fee entry: "Request payment now?" | |
| Replace prompt with modal | Replace browser prompt() with styled modal | |

**User's choice:** Keep prompt as-is

---

## Finance Display

| Option | Description | Selected |
|--------|-------------|----------|
| Same as regular RFPs | No visual distinction; Tranche Label column shows "Delivery Fee" | ✓ |
| Tag as Delivery Fee type | Add badge/pill or Type column | |

**User's choice:** Same as regular RFPs

| Option | Description | Selected |
|--------|-------------|----------|
| Automatic (no code change) | rfpsByPO already includes delivery fee RFPs; getPOPaymentFill() covers them | ✓ |
| Explicit handling | Separate code path for delivery fee RFP payments in fill calculation | |

**User's choice:** Automatic — no change needed to getPOPaymentFill()

---

## Visual Cue (user-initiated — "how should Procurement know a delivery fee needs to be RFP'd?")

| Option | Description | Selected |
|--------|-------------|----------|
| Sub-badge under PO ID | Colored pill below PO ID link: "Delivery: PHP X — Not yet paid" | |
| Dot indicator on cell corner | 8px dot, top-right corner of PO ID cell, red/green | ✓ |
| Extended tooltip only | No permanent indicator; tooltip gains delivery fee section | |

**User's choice:** Dot indicator on cell corner
**Notes:** Red = not yet paid; Green = paid. Use existing palette (#ef4444 red, #059669 green). Tooltip on hover shows fee amount and status. Dot only shown when delivery_fee > 0.

---

## Claude's Discretion

- CSS positioning of dot within PO ID cell
- Dot size
- Whether dot also appears in Finance PO Payment Summary table

## Deferred Ideas

None

