---
phase: 77.2-polish-home-graph-proportions-and-legend
task: 0
total_tasks: 3
status: planned_not_started
last_updated: 2026-04-27T10:15:30.500Z
---

<current_state>
Phase 77.2 was inserted and fully planned this session. Execution has NOT started. Plan 77.2-01-PLAN.md is written and passed plan-checker verification. No source files have been modified yet. Ready to execute.
</current_state>

<completed_work>

- Phase 77.2 inserted into ROADMAP.md after Phase 77.1 (INSERTED marker)
- STATE.md updated with Roadmap Evolution entry
- 77.2-01-PLAN.md created and verified by plan-checker (all 10 dimensions passed)
- Plan values confirmed against actual code by plan-checker (exact before/after values verified)
</completed_work>

<remaining_work>

- Task 1 (auto): Reduce `.hs-chart-canvas.hs-chart-status` height in styles/views.css
  - Desktop: 320px → 180px
  - Mobile (≤768px): 360px → 220px
  - Update comment block from "Phase 81" rationale to "Phase 77.2" rationale
- Task 2 (auto): Bump Chart.js options in app/views/home.js `renderStatusBreakdown()`
  - barThickness: 22 → 28
  - legend boxWidth: 10 → 12
  - legend boxHeight: 10 → 12
  - legend font.size: 10 → 12
  - Add `// Phase 77.2` inline comment above legend.labels block
- Task 3 (human checkpoint): Visual verify on local server
  - Start `python -m http.server 8000`, open http://localhost:8000
  - Confirm bar dominates card, legend +2px, both Projects + Services cards updated, no console errors
  - Type "approved" or describe tuning needed
</remaining_work>

<decisions_made>

- Desktop card height 180px chosen: single stacked bar (~28px) + ~3 rows of 12px legend + breathing room ≈ 130px, rounded to 180px for wrapping safety
- Mobile card height 220px: wider screens push legend to 4-5 rows, +40px absorbs that
- barThickness bumped 22→28: bar must grow proportionally with smaller card so it doesn't still look tiny
- Single CSS rule applies to all 3 chart cards (Projects, Services OT, Recurring) via shared `.hs-chart-status` class — no per-card CSS needed
- Update branch in `renderStatusBreakdown` (lines 189-200) only updates dataset.data, not options — so only the Chart constructor needs changing, no migration needed
</decisions_made>

<blockers>
None. Plan is ready to execute.
</blockers>

<context>
The problem: Phase 77.1 introduced Chart.js horizontal bar charts on the home page. The `.hs-chart-canvas.hs-chart-status` wrapper was sized at 320px (from Phase 81 which assumed 10 separate bars). But now there's only ONE stacked bar per chart — so the 320px card is mostly empty space and the bar looks tiny. The user shared a screenshot showing exactly this. The fix is 5 numeric literal changes in 2 files — pure visual polish, no logic changes.

The plan was created inline (no separate research since it's all numeric constants). Plan-checker confirmed all values match actual code at lines 2357-2366 in views.css and lines 208, 227-229 in home.js.
</context>

<next_action>
Start with: `/gsd:execute-phase 77.2` — `/clear` first for fresh context window.
</next_action>
