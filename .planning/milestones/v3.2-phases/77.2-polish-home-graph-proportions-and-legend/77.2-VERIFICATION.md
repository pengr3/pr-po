---
phase: 77.2-polish-home-graph-proportions-and-legend
verified: 2026-04-28T00:00:00Z
status: passed
score: 5/5 must-haves verified
---

# Phase 77.2: Polish Home Graph Proportions and Legend Sizing — Verification Report

**Phase Goal:** Tighten the Projects and Services chart cards on the home page so the bar graph fills the card proportionally — reduce card height, enlarge the canvas, bump legend font size by 2px, and adjust the legend color-square size to match.

**Verified:** 2026-04-28
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Projects chart card no longer shows a tiny bar lost in a tall empty card — the bar visually dominates a tighter card | VERIFIED | `styles/views.css:2360-2361` height/min-height = 180px (down from 320px); `app/views/home.js:208` barThickness = 28 (up from 22). User signaled "approved" on visual checkpoint. |
| 2 | Services One-time and Recurring chart cards match the Projects chart card in proportion | VERIFIED | All three containers `stat-projects-status`, `stat-services-ot-status`, `stat-services-rec-status` are emitted via `buildStatusBreakdownContainer()` (home.js:134, 153, 161) and routed through `getChartSizeClass()` (home.js:66-67) which always returns `'hs-chart-status'` — single CSS class drives all three. |
| 3 | Legend text is +2px larger than before and remains readable on desktop and mobile | VERIFIED | `app/views/home.js:231` `font: { size: 12 }` (up from 10). Mobile `@media (max-width: 768px)` height bumped to 220px (up from 360px shrunk to 220px) at `views.css:2363-2367`. User confirmed visual readability on both desktop and mobile (375px). |
| 4 | Legend color squares are larger to match the +2px legend text | VERIFIED | `app/views/home.js:229-230` `boxWidth: 12, boxHeight: 12` (up from 10/10). |
| 5 | Charts still render all 10 unified status segments without overlap or clipping | VERIFIED | Phase 81 architecture unchanged — `renderStatusBreakdown()` (home.js:202-256) preserves the `labels.map(...)` dataset construction over the unified status set; only barThickness and legend dimensions changed. User confirmed "no console errors and no chart-instance leaks on view switching" on visual checkpoint. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `styles/views.css` | `.hs-chart-canvas.hs-chart-status` reduced height (desktop + mobile) | VERIFIED | Lines 2359-2367: desktop `height: 180px; min-height: 180px;` and mobile (max-width: 768px) `height: 220px; min-height: 220px;`. Comment block at 2354-2358 documents Phase 77.2 sizing math. No stale 320px/360px values for this selector remain. |
| `app/views/home.js` | Chart.js legend font.size, boxWidth, boxHeight bumped +2px; barThickness increased | VERIFIED | Line 208: `barThickness: 28`. Lines 229-231: `boxWidth: 12`, `boxHeight: 12`, `font: { size: 12 }`. Inline `// Phase 77.2` comment at lines 226-227 documents the rationale. No stale `barThickness: 22` / `boxWidth: 10` / `boxHeight: 10` / `font: { size: 10 }` matches remain. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `styles/views.css` | `.hs-chart-canvas.hs-chart-status` | height + min-height rule | WIRED | Selector matched at line 2359 with `height: 180px; min-height: 180px;`. Pattern `hs-chart-canvas\.hs-chart-status\s*\{[^}]*height:\s*180px` confirmed via grep. |
| `app/views/home.js` | Chart.js `options.plugins.legend.labels` | boxWidth/boxHeight/font.size all set to 12 | WIRED | Confirmed at lines 229-231 inside the `new window.Chart(canvas, ...)` constructor of `renderStatusBreakdown()`. |
| `app/views/home.js` | Chart dataset | barThickness bumped from 22 to 28 | WIRED | Confirmed at line 208 inside `datasets = labels.map(...)`. |
| `getChartSizeClass()` | `.hs-chart-status` CSS class | always returns `'hs-chart-status'` | WIRED | home.js:66-67 returns the constant; home.js:93 applies the class via `buildStatusBreakdownContainer()` to all 3 chart wrappers, ensuring the CSS height change propagates uniformly to Projects + Services One-time + Services Recurring. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `app/views/home.js` `renderStatusBreakdown()` | `countsMap`, `labels` | Computed from `cachedStats.projectsByStatus`, `servicesByStatusOneTime`, `servicesByStatusRecurring` (home.js:400, 430, 431) | Yes — Phase 77.1/81 wired the Firestore listeners that populate cachedStats; this phase did not modify that data path | FLOWING |

Note: Phase 77.2 is a pure visual-tuning phase (5 numeric literal changes). The data-flow path was established in Phases 77.1/81 and is out of scope for this verification, but spot-checked to confirm no regression.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| `barThickness: 28` literal present in home.js | `grep -nE "barThickness:\s*28" app/views/home.js` | Match at line 208 | PASS |
| `boxWidth: 12` literal present in home.js | `grep -nE "boxWidth:\s*12" app/views/home.js` | Match at line 229 | PASS |
| `boxHeight: 12` literal present in home.js | `grep -nE "boxHeight:\s*12" app/views/home.js` | Match at line 230 | PASS |
| `font: { size: 12 }` literal present in home.js | `grep -nE "font:\s*\{\s*size:\s*12\s*\}"` | Match at line 231 | PASS |
| Stale `barThickness: 22 / boxWidth: 10 / boxHeight: 10 / font: { size: 10 }` removed | inverse grep | 0 matches | PASS |
| `.hs-chart-canvas.hs-chart-status` desktop height = 180px | grep + context view of views.css:2359-2362 | `height: 180px; min-height: 180px;` | PASS |
| `.hs-chart-canvas.hs-chart-status` mobile height = 220px | grep + context view of views.css:2363-2367 | `height: 220px; min-height: 220px;` inside `@media (max-width: 768px)` | PASS |
| Stale `height: 320px / 360px` removed for `.hs-chart-canvas.hs-chart-status` | grep | 0 matches for that selector | PASS |
| `// Phase 77.2` rationale comment present in home.js | grep | Match at line 226 | PASS |
| `Phase 77.2` rationale comment present in views.css | grep | Match at line 2354 | PASS |
| Task commits exist in git history | `git log --oneline` | `a1f74bf` (CSS height) and `c950323` (Chart.js options) both present | PASS |
| All 3 chart containers wired through shared class | grep `stat-projects-status`, `stat-services-ot-status`, `stat-services-rec-status` | All 3 IDs found at home.js:134, 153, 161 (HTML) and 400, 430, 431 (render calls) | PASS |

Skip note: live-server visual rendering checks were performed by the user during Task 3 (human-verify checkpoint) and approved — re-running them automatically is unnecessary.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| HOMECHART-PROP-01 | 77.2-01-PLAN | Card height reduced so bar chart is proportionally large | SATISFIED (tracked-as-decisions) | views.css:2360 height 320→180px; home.js:208 barThickness 22→28. User-approved on visual checkpoint. |
| HOMECHART-PROP-02 | 77.2-01-PLAN | Legend font-size +2px (10 → 12) | SATISFIED (tracked-as-decisions) | home.js:231 `font: { size: 12 }`. |
| HOMECHART-PROP-03 | 77.2-01-PLAN | Legend color-square dimensions updated to match (10 → 12) | SATISFIED (tracked-as-decisions) | home.js:229-230 `boxWidth: 12, boxHeight: 12`. |
| HOMECHART-PROP-04 | 77.2-01-PLAN | Projects and Services cards both updated consistently | SATISFIED (tracked-as-decisions) | Single `.hs-chart-status` class via `getChartSizeClass()` (home.js:66-67) drives all 3 chart wrappers (`stat-projects-status`, `stat-services-ot-status`, `stat-services-rec-status` at lines 134/153/161). One CSS rule + one Chart options block update propagated to all three. |

**Note on requirement tracking:** All four HOMECHART-PROP-* IDs are intentionally NOT in `.planning/REQUIREMENTS.md` — the user confirmed these are documented as decisions in the 77 family at the phase level (PLAN.md frontmatter `requirements:` array + SUMMARY.md `requirements-completed:` array + ROADMAP.md). They are flagged as `tracked-as-decisions` rather than missing/orphaned. Cross-referenced REQUIREMENTS.md confirms zero matches for `HOMECHART-PROP` — by design.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| (none) | — | No TODO/FIXME/PLACEHOLDER, no empty handlers, no static-array stubs introduced by this phase | — | — |

Both modified files were checked; the changes are pure numeric-literal updates inside an existing, working function. No regressions or stubs introduced.

### Human Verification Required

None. The user already executed the 7-step visual-verification checklist in Task 3 (human-verify checkpoint) and signaled approval — covering chart proportions, legend size (DevTools-confirmed font-size = 12px and box = 12×12), mobile rendering at 375px, all 10 status segments, tooltips, no console errors, and no chart-instance leaks on view switching. Documented in 77.2-01-SUMMARY.md (Task 3 outcome) and the user's prompt to this verifier.

### Gaps Summary

No gaps. All 5 observable truths verified, both required artifacts pass all 4 levels (exists, substantive, wired, data flowing), all 4 key links wired, all 4 HOMECHART-PROP requirements satisfied (tracked-as-decisions per project convention), no anti-patterns introduced, behavioral spot-checks pass, and the user has already approved visual rendering. Phase 77.2 achieved its goal: the bar graph now visually dominates a tightened card with larger, more readable legend text and color squares — applied uniformly across Projects, Services One-time, and Services Recurring cards via the shared `.hs-chart-status` class.

---

_Verified: 2026-04-28_
_Verifier: Claude (gsd-verifier)_
