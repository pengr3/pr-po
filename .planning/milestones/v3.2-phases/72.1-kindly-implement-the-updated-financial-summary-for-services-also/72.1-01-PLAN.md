---
phase: 72.1-kindly-implement-the-updated-financial-summary-for-services-also
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/project-detail.js
  - app/views/service-detail.js
autonomous: true
requirements: [PAYALWAYS-01, PAYALWAYS-02]

must_haves:
  truths:
    - "Service-detail Financial Summary card always shows Paid and Remaining Payable cells, even when the service has zero RFPs filed"
    - "Project-detail Financial Summary card always shows Paid and Remaining Payable cells, even when the project has zero RFPs filed"
    - "When no RFPs exist: Paid shows formatted 0 value; Remaining Payable shows full unpaid amount (PO+TR total for projects, PR+PO total for services) in red when > 0"
    - "When RFPs exist: behavior remains identical to Phase 72 (no regression) — totalPaid from non-voided payment_records, remainingPayable = total minus paid"
    - "No changes to RFP query logic, destroy() cleanup, refresh button, or any other Phase 72 mechanic — this phase only removes the conditional hide wrapper"
  artifacts:
    - path: "app/views/project-detail.js"
      provides: "Financial Summary card with always-visible Paid and Remaining Payable cells"
      contains: "label=\"Paid\""
    - path: "app/views/service-detail.js"
      provides: "Financial Summary card with always-visible Paid and Remaining Payable cells"
      contains: "Remaining Payable"
  key_links:
    - from: "app/views/project-detail.js render()"
      to: "currentExpense.totalPaid and currentExpense.remainingPayable"
      via: "unconditional template literal (no hasRfps gate)"
      pattern: "formatCurrency\\(currentExpense\\.totalPaid\\)"
    - from: "app/views/service-detail.js render()"
      to: "currentServiceExpense.totalPaid and currentServiceExpense.remainingPayable"
      via: "unconditional template literal (no hasRfps gate)"
      pattern: "formatCurrency\\(currentServiceExpense\\.totalPaid\\)"
---

<objective>
Remove the `hasRfps` conditional wrapper in project-detail.js and service-detail.js so Paid and Remaining Payable cells always render on the Financial Summary card — showing 0 paid / full unpaid amount when no RFPs exist, instead of hiding the cells entirely.

Purpose: Reverses one specific Phase 72 design decision (conditional hide) so the cards are consistent regardless of whether RFPs have been filed. Users previously saw inconsistent card layouts depending on RFP presence.

Output: Two files updated with a micro-change removing the `${currentExpense.hasRfps ? ` ... ` : ''}` and `${currentServiceExpense.hasRfps ? ` ... ` : ''}` wrappers. No state, query, or behavior changes beyond render.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/72-add-paid-and-remaining-payable-to-project-service-financial-summary-cards-with-clickable-refresh/72-01-SUMMARY.md
@app/views/project-detail.js
@app/views/service-detail.js

<scope_lock>
This is a targeted micro-change. DO NOT:
- Modify RFP query logic (getDocs / where filters on project_code/service_code stay as-is)
- Remove or alter `hasRfps` field in state object (keep populating it — just stop using it to gate render)
- Touch destroy() cleanup
- Touch refreshAndShowExpenseModal / refreshAndShowServiceExpenseModal / Refresh button
- Change formatCurrency, status colors, or any styling other than deleting the conditional template wrapper
- Change the inner <div class="form-group"> blocks for Paid and Remaining Payable — they stay verbatim

The literal change is: delete the `${currentExpense.hasRfps ? ` + backtick prefix and the matching `` + ` : ''}` suffix that currently wrap the two cells. Keep the cells themselves.
</scope_lock>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove hasRfps render wrapper in project-detail.js</name>
  <files>app/views/project-detail.js</files>
  <read_first>
    - app/views/project-detail.js (entire file, but focus on lines ~360–373 where `${currentExpense.hasRfps ? ` wraps the Paid and Remaining Payable cells)
    - .planning/phases/72-add-paid-and-remaining-payable-to-project-service-financial-summary-cards-with-clickable-refresh/72-01-SUMMARY.md (understand what Phase 72 built — this task only reverses the conditional-render decision)
  </read_first>
  <action>
    Remove the template-literal conditional wrapper that hides the Paid and Remaining Payable cells when `currentExpense.hasRfps === false`. The wrapper is `${currentExpense.hasRfps ? `` (opening prefix with backtick) and `` : ''}` (closing suffix).

    BEFORE (approximately lines 360–373):
    ```
                            ${currentExpense.hasRfps ? `
                            <div class="form-group" style="margin-bottom: 0;">
                                <label style="margin-bottom: 0.5rem; display: block; font-weight: 600; color: #1e293b;">Paid</label>
                                <div style="font-weight: 600; color: #059669; font-size: 1.125rem;">
                                    ${formatCurrency(currentExpense.totalPaid)}
                                </div>
                            </div>
                            <div class="form-group" style="margin-bottom: 0;">
                                <label style="margin-bottom: 0.5rem; display: block; font-weight: 600; color: #1e293b;">Remaining Payable</label>
                                <div style="font-weight: 600; color: ${currentExpense.remainingPayable > 0 ? '#ef4444' : '#059669'}; font-size: 1.125rem;">
                                    ${formatCurrency(currentExpense.remainingPayable)}
                                </div>
                            </div>
                            ` : ''}
    ```

    AFTER:
    ```
                            <div class="form-group" style="margin-bottom: 0;">
                                <label style="margin-bottom: 0.5rem; display: block; font-weight: 600; color: #1e293b;">Paid</label>
                                <div style="font-weight: 600; color: #059669; font-size: 1.125rem;">
                                    ${formatCurrency(currentExpense.totalPaid)}
                                </div>
                            </div>
                            <div class="form-group" style="margin-bottom: 0;">
                                <label style="margin-bottom: 0.5rem; display: block; font-weight: 600; color: #1e293b;">Remaining Payable</label>
                                <div style="font-weight: 600; color: ${currentExpense.remainingPayable > 0 ? '#ef4444' : '#059669'}; font-size: 1.125rem;">
                                    ${formatCurrency(currentExpense.remainingPayable)}
                                </div>
                            </div>
    ```

    That is: delete exactly two lines — the `${currentExpense.hasRfps ? ` + opening backtick line, and the closing ` + ` : ''}` line. The two `<div class="form-group">` blocks stay verbatim.

    DO NOT:
    - Remove the `hasRfps` field from the state object — it is still populated by `refreshExpense()` and may be referenced elsewhere; leaving it in state is explicitly in scope.
    - Touch the `refreshExpense()` function, the RFP getDocs query, destroy(), or the Refresh button onclick.
    - Modify the two inner `<div class="form-group">` blocks — preserve the exact markup including styling, label text, and `${formatCurrency(...)}` interpolations.

    Addresses PAYALWAYS-02 (project-detail.js wrapper removed) and Success Criteria 2 and 3 (projects side).
  </action>
  <verify>
    <automated>grep -c "currentExpense.hasRfps ?" app/views/project-detail.js</automated>
    Expected output: `0` (the conditional render wrapper is gone).

    Additional greps to confirm the cells survived:
    - `grep -n "label.*>Paid<" app/views/project-detail.js` returns the Paid label line
    - `grep -n "Remaining Payable" app/views/project-detail.js` returns at least one line (the Remaining Payable label)
    - `grep -n "currentExpense.totalPaid" app/views/project-detail.js` returns at least one line (the formatted paid amount cell survives)
    - `grep -n "currentExpense.remainingPayable" app/views/project-detail.js` returns at least two lines (both the color ternary and the formatCurrency cell survive)

    Syntax sanity:
    - `node --check app/views/project-detail.js` exits 0 (no syntax error after edit). If node rejects ES module syntax, this step is informational only — manual review of the diff is sufficient.
  </verify>
  <acceptance_criteria>
    - `grep -c "currentExpense.hasRfps ?" app/views/project-detail.js` returns `0`
    - `grep -c "label.*>Paid<" app/views/project-detail.js` returns `>= 1`
    - `grep -c "Remaining Payable" app/views/project-detail.js` returns `>= 1`
    - `grep -c "formatCurrency(currentExpense.totalPaid)" app/views/project-detail.js` returns `>= 1`
    - `grep -c "formatCurrency(currentExpense.remainingPayable)" app/views/project-detail.js` returns `>= 1`
    - The surrounding `<div class="form-group">` blocks for Paid and Remaining Payable are unchanged verbatim (inner HTML, styling, label text, ternary color logic all identical to BEFORE)
    - The `hasRfps` field in the state object is NOT removed (grep for `hasRfps` in the rest of project-detail.js still returns results from state initialization / refresh function)
    - `app/views/project-detail.js` still loads in the browser without runtime error
  </acceptance_criteria>
  <done>
    - The `${currentExpense.hasRfps ? ` / `` : ''}` wrapper is removed around the Paid and Remaining Payable form-group cells
    - The two form-group cells themselves render unconditionally
    - No other code in the file changed
    - Project detail page renders Paid (0 when no RFPs) and Remaining Payable (full unpaid amount in red when no RFPs) on the Financial Summary card
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove hasRfps render wrapper in service-detail.js</name>
  <files>app/views/service-detail.js</files>
  <read_first>
    - app/views/service-detail.js (entire file, but focus on lines ~430–443 where `${currentServiceExpense.hasRfps ? ` wraps the Paid and Remaining Payable cells)
    - .planning/phases/72-add-paid-and-remaining-payable-to-project-service-financial-summary-cards-with-clickable-refresh/72-01-SUMMARY.md (understand what Phase 72 built — this task only reverses the conditional-render decision)
  </read_first>
  <action>
    Remove the template-literal conditional wrapper that hides the Paid and Remaining Payable cells when `currentServiceExpense.hasRfps === false`. Same transform as Task 1 but with `currentServiceExpense` instead of `currentExpense`.

    BEFORE (approximately lines 430–443):
    ```
                            ${currentServiceExpense.hasRfps ? `
                            <div class="form-group" style="margin-bottom: 0;">
                                <label style="margin-bottom: 0.5rem; display: block; font-weight: 600; color: #1e293b;">Paid</label>
                                <div style="font-weight: 600; color: #059669; font-size: 1.125rem;">
                                    ${formatCurrency(currentServiceExpense.totalPaid)}
                                </div>
                            </div>
                            <div class="form-group" style="margin-bottom: 0;">
                                <label style="margin-bottom: 0.5rem; display: block; font-weight: 600; color: #1e293b;">Remaining Payable</label>
                                <div style="font-weight: 600; color: ${currentServiceExpense.remainingPayable > 0 ? '#ef4444' : '#059669'}; font-size: 1.125rem;">
                                    ${formatCurrency(currentServiceExpense.remainingPayable)}
                                </div>
                            </div>
                            ` : ''}
    ```

    AFTER:
    ```
                            <div class="form-group" style="margin-bottom: 0;">
                                <label style="margin-bottom: 0.5rem; display: block; font-weight: 600; color: #1e293b;">Paid</label>
                                <div style="font-weight: 600; color: #059669; font-size: 1.125rem;">
                                    ${formatCurrency(currentServiceExpense.totalPaid)}
                                </div>
                            </div>
                            <div class="form-group" style="margin-bottom: 0;">
                                <label style="margin-bottom: 0.5rem; display: block; font-weight: 600; color: #1e293b;">Remaining Payable</label>
                                <div style="font-weight: 600; color: ${currentServiceExpense.remainingPayable > 0 ? '#ef4444' : '#059669'}; font-size: 1.125rem;">
                                    ${formatCurrency(currentServiceExpense.remainingPayable)}
                                </div>
                            </div>
    ```

    That is: delete exactly two lines — the `${currentServiceExpense.hasRfps ? ` + opening backtick line, and the closing ` + ` : ''}` line. The two `<div class="form-group">` blocks stay verbatim.

    DO NOT:
    - Remove the `hasRfps` field from the state object — it is still populated by `refreshServiceExpense()` and may be referenced elsewhere; leaving it in state is explicitly in scope.
    - Touch the `refreshServiceExpense()` function, the RFP getDocs query, destroy(), or the Refresh button onclick.
    - Modify the two inner `<div class="form-group">` blocks — preserve the exact markup including styling, label text, and `${formatCurrency(...)}` interpolations.

    Addresses PAYALWAYS-01 (service-detail.js wrapper removed) and Success Criteria 1 and 3 (services side).
  </action>
  <verify>
    <automated>grep -c "currentServiceExpense.hasRfps ?" app/views/service-detail.js</automated>
    Expected output: `0` (the conditional render wrapper is gone).

    Additional greps to confirm the cells survived:
    - `grep -n "label.*>Paid<" app/views/service-detail.js` returns the Paid label line
    - `grep -n "Remaining Payable" app/views/service-detail.js` returns at least one line (the Remaining Payable label)
    - `grep -n "currentServiceExpense.totalPaid" app/views/service-detail.js` returns at least one line (the formatted paid amount cell survives)
    - `grep -n "currentServiceExpense.remainingPayable" app/views/service-detail.js` returns at least two lines (both the color ternary and the formatCurrency cell survive)

    Syntax sanity:
    - `node --check app/views/service-detail.js` exits 0 (no syntax error after edit). If node rejects ES module syntax, this step is informational only — manual review of the diff is sufficient.
  </verify>
  <acceptance_criteria>
    - `grep -c "currentServiceExpense.hasRfps ?" app/views/service-detail.js` returns `0`
    - `grep -c "label.*>Paid<" app/views/service-detail.js` returns `>= 1`
    - `grep -c "Remaining Payable" app/views/service-detail.js` returns `>= 1`
    - `grep -c "formatCurrency(currentServiceExpense.totalPaid)" app/views/service-detail.js` returns `>= 1`
    - `grep -c "formatCurrency(currentServiceExpense.remainingPayable)" app/views/service-detail.js` returns `>= 1`
    - The surrounding `<div class="form-group">` blocks for Paid and Remaining Payable are unchanged verbatim (inner HTML, styling, label text, ternary color logic all identical to BEFORE)
    - The `hasRfps` field in the state object is NOT removed (grep for `hasRfps` in the rest of service-detail.js still returns results from state initialization / refresh function)
    - `app/views/service-detail.js` still loads in the browser without runtime error
  </acceptance_criteria>
  <done>
    - The `${currentServiceExpense.hasRfps ? ` / `` : ''}` wrapper is removed around the Paid and Remaining Payable form-group cells
    - The two form-group cells themselves render unconditionally
    - No other code in the file changed
    - Service detail page renders Paid (0 when no RFPs) and Remaining Payable (full unpaid amount in red when no RFPs) on the Financial Summary card
  </done>
</task>

</tasks>

<verification>
Phase-level verification (after both tasks complete):

1. Grep both files to confirm `hasRfps ?` conditional-render wrappers are gone:
   - `grep -c "currentExpense.hasRfps ?" app/views/project-detail.js` → `0`
   - `grep -c "currentServiceExpense.hasRfps ?" app/views/service-detail.js` → `0`

2. Grep both files to confirm cells survived:
   - `grep -c "formatCurrency(currentExpense.totalPaid)" app/views/project-detail.js` → `>= 1`
   - `grep -c "formatCurrency(currentExpense.remainingPayable)" app/views/project-detail.js` → `>= 1`
   - `grep -c "formatCurrency(currentServiceExpense.totalPaid)" app/views/service-detail.js` → `>= 1`
   - `grep -c "formatCurrency(currentServiceExpense.remainingPayable)" app/views/service-detail.js` → `>= 1`

3. Git diff sanity: the total diff for both files should be small — primarily two deleted lines per file (the opening `${...hasRfps ? \`` line and the closing `\` : ''}` line), with possibly minor whitespace re-indentation. No new code added, no other code changed.

4. Manual browser verification (to be covered by human-verify in execute-phase, not this plan):
   - Navigate to a Project WITH RFPs — Paid/Remaining Payable values match Phase 72 behavior (no regression)
   - Navigate to a Project WITHOUT RFPs — Paid shows formatted 0, Remaining Payable shows full unpaid amount in red (or green if 0)
   - Same for Services — with and without RFPs
</verification>

<success_criteria>
- PAYALWAYS-01 satisfied: service-detail.js conditional wrapper removed; Paid/Remaining Payable cells always render
- PAYALWAYS-02 satisfied: project-detail.js conditional wrapper removed; Paid/Remaining Payable cells always render
- ROADMAP Success Criteria 1 satisfied: service card always shows Paid and Remaining Payable
- ROADMAP Success Criteria 2 satisfied: project card always shows Paid and Remaining Payable
- ROADMAP Success Criteria 3 satisfied: when no RFPs exist, Paid shows 0 and Remaining Payable shows full unpaid amount (red when > 0) — because `currentExpense.totalPaid` defaults to 0 and `currentExpense.remainingPayable` is populated with the PO+TR total (or PR+PO for services) regardless of `hasRfps` status in existing state-refresh code
- ROADMAP Success Criteria 4 satisfied: no regression when RFPs exist — the exact same cells render with the exact same values
- ROADMAP Success Criteria 5 satisfied: only the conditional-hide wrapper changed; RFP query logic, destroy(), refresh button, and all other Phase 72 mechanics untouched
</success_criteria>

<output>
After completion, create `.planning/phases/72.1-kindly-implement-the-updated-financial-summary-for-services-also/72.1-01-SUMMARY.md` summarizing the two-line deletion per file and confirming no other changes.
</output>
