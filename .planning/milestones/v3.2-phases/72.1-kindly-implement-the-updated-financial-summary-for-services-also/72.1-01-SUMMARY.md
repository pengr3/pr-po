---
phase: 72.1-kindly-implement-the-updated-financial-summary-for-services-also
plan: "01"
subsystem: views
tags: [financial-summary, project-detail, service-detail, conditional-render, paid, remaining-payable]
dependency_graph:
  requires: [72-01]
  provides: [PAYALWAYS-01, PAYALWAYS-02]
  affects: [app/views/project-detail.js, app/views/service-detail.js]
tech_stack:
  added: []
  patterns: [unconditional-template-literal-render]
key_files:
  created: []
  modified:
    - app/views/project-detail.js
    - app/views/service-detail.js
decisions:
  - "[Phase 72.1]: hasRfps field retained in state objects (refreshExpense/refreshServiceExpense still populate it) — only the render-side conditional gate removed, not the data-side population"
metrics:
  duration: "1 minute"
  completed: "2026-04-14"
  tasks: 2
  files: 2
---

# Phase 72.1 Plan 01: Remove hasRfps Conditional Render Wrapper Summary

Removed the `${currentExpense.hasRfps ? \`` / `\` : ''}` and `${currentServiceExpense.hasRfps ? \`` / `\` : ''}` template-literal conditional wrappers so Paid and Remaining Payable cells always render on both Project and Service Financial Summary cards, showing formatted 0 / full unpaid amount when no RFPs exist instead of hiding the cells.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Remove hasRfps render wrapper in project-detail.js | 5d223b7 | app/views/project-detail.js |
| 2 | Remove hasRfps render wrapper in service-detail.js | e0fd3f9 | app/views/service-detail.js |

## Changes Made

### Task 1 — project-detail.js (~line 360)

Deleted two lines:
- Opening: `${currentExpense.hasRfps ? \`` (line 360)
- Closing: `\` : ''}` (line 373)

The two inner `<div class="form-group">` blocks for Paid and Remaining Payable are unchanged verbatim.

### Task 2 — service-detail.js (~line 430)

Deleted two lines:
- Opening: `${currentServiceExpense.hasRfps ? \`` (line 430)
- Closing: `\` : ''}` (line 443)

Same inner blocks preserved verbatim.

## Verification Results

| Check | project-detail.js | service-detail.js |
|-------|-------------------|-------------------|
| `hasRfps ?` conditional count | 0 | 0 |
| `formatCurrency(*.totalPaid)` count | 1 | 1 |
| `formatCurrency(*.remainingPayable)` count | 1 | 1 |
| `Paid` label present | yes | yes |
| `Remaining Payable` label present | yes | yes |
| `hasRfps` field in state (not removed) | 5 occurrences | 5 occurrences |

## Deviations from Plan

None — plan executed exactly as written. Two lines deleted per file, zero other changes.

## Known Stubs

None.

## Self-Check: PASSED

- app/views/project-detail.js modified — confirmed exists and `hasRfps ?` count = 0
- app/views/service-detail.js modified — confirmed exists and `hasRfps ?` count = 0
- Commit 5d223b7 confirmed (task 1)
- Commit e0fd3f9 confirmed (task 2)
