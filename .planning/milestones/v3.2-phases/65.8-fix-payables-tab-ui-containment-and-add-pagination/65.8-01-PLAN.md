---
phase: 65.8-fix-payables-tab-ui-containment-and-add-pagination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/finance.js
autonomous: true
requirements:
  - PAYCON-01
  - PAYCON-02

must_haves:
  truths:
    - "Payables tables scroll horizontally when content overflows the viewport instead of breaking page layout"
    - "Table 1 (RFP Processing) displays ALL RFPs without pagination — Finance staff see every outstanding item at once"
    - "Table 2 (PO Payment Summary) pagination is unaffected"
  artifacts:
    - path: "app/views/finance.js"
      provides: "table-scroll-container class on both Payables table wrapper divs"
      contains: "table-scroll-container"
  key_links:
    - from: "render() HTML — Table 1 wrapper"
      to: ".table-scroll-container CSS rule"
      via: "class attribute on div wrapping RFP Processing table"
      pattern: "table-scroll-container"
    - from: "render() HTML — Table 2 wrapper"
      to: ".table-scroll-container CSS rule"
      via: "class attribute on div wrapping PO Payment Summary table"
      pattern: "table-scroll-container"
---

<objective>
Fix Payables tab UI containment on both tables.

Purpose: Both Payables tables use `class="table-container"` which has no CSS rule, causing horizontal overflow on narrow viewports. This plan swaps the wrapper class to the existing `.table-scroll-container` (from components.css) on both Table 1 (RFP Processing) and Table 2 (PO Payment Summary).

Output: finance.js with scroll containment on both Payables tables. No pagination changes — Table 1 intentionally shows all RFPs, Table 2 already has pagination from Phase 65.7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65.8-fix-payables-tab-ui-containment-and-add-pagination/65.8-RESEARCH.md

@app/views/finance.js
@styles/components.css
</context>

<interfaces>
<!-- Existing CSS class from styles/components.css that MUST be used (not reinvented): -->

.table-scroll-container — horizontal scroll wrapper with shadow-edge technique (components.css ~line 449)
  - overflow-x: auto
  - -webkit-overflow-scrolling: touch
  - shadow-edge gradient backgrounds
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Swap table-container to table-scroll-container on both Payables table wrappers</name>
  <files>app/views/finance.js</files>
  <read_first>
    - app/views/finance.js (lines 1710-1800 for payables HTML with both table wrapper divs)
    - styles/components.css (lines 449-463 for .table-scroll-container definition)
  </read_first>
  <action>
NOTE: The RESEARCH.md primary recommendation includes Table 1 pagination (PAYPAG-01/02/03). That pagination work is OUT OF SCOPE for this phase per user decision. Only the containment class swap is in scope. Do not add rfpCurrentPage, rfpItemsPerPage, changeRFPPage, updateRFPPagination, or any pagination HTML to Table 1.

Two class swaps in app/views/finance.js render() HTML:

1. Line ~1728 (Table 1 — RFP Processing wrapper): Replace `class="table-container"` with `class="table-scroll-container"`
2. Line ~1770 (Table 2 — PO Payment Summary wrapper): Replace `class="table-container"` with `class="table-scroll-container"`

That is the entire change. Do NOT add any pagination to Table 1 — it intentionally shows all RFPs so Finance staff can see every outstanding item. Do NOT touch Table 2 pagination (already working from Phase 65.7). Do NOT add any new state variables, functions, or window registrations.
  </action>
  <verify>
    <automated>cd C:/Users/Admin/Roaming/pr-po && echo "table-scroll-container count:" && grep -c "table-scroll-container" app/views/finance.js && echo "table-container count:" && grep -c "table-container" app/views/finance.js</automated>
  </verify>
  <done>
    - finance.js contains exactly 2 occurrences of "table-scroll-container" (one per Payables table wrapper)
    - finance.js contains 0 occurrences of `class="table-container"` (both replaced)
    - No other changes to finance.js — no new variables, functions, or HTML elements added
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'table-scroll-container' app/views/finance.js` returns `2` (both table wrappers)
2. `grep -c 'class="table-container"' app/views/finance.js` returns `0` (none remain)
3. `grep -c 'rfpCurrentPage' app/views/finance.js` returns `0` (no pagination added to Table 1)
4. `grep -c 'changeRFPPage' app/views/finance.js` returns `0` (no pagination functions added)
</verification>

<success_criteria>
- Both Payables tables wrapped in `table-scroll-container` for horizontal scroll containment
- Table 1 (RFP Processing) shows ALL RFPs without any pagination
- Table 2 (PO Payment Summary) pagination is completely untouched
- No new JavaScript state, functions, or DOM elements introduced
</success_criteria>

<output>
After completion, create `.planning/phases/65.8-fix-payables-tab-ui-containment-and-add-pagination/65.8-01-SUMMARY.md`
</output>
