---
phase: 73.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - styles/components.css
  - app/views/finance.js
autonomous: true
requirements:
  - FINMOD-PR-GRID
  - FINMOD-PR-TABLE
  - FINMOD-TR-GRID
  - FINMOD-TR-TABLE
  - FINMOD-BODY-PADDING
user_setup: []

must_haves:
  truths:
    - "At viewport 375px on Finance > Pending Approvals, opening a PR Details modal shows the detail grid (PR ID, MRF, Department, Requestor, Urgency, Date, etc.) stacked in a single column — not 2 columns"
    - "At viewport 375px on Finance > Pending Approvals, opening a TR Details modal shows the detail grid stacked in a single column — not 2 columns"
    - "At viewport 375px, the PR items breakdown table (6 columns: Item, Category, Qty, Unit Cost, Supplier, Subtotal) is wrapped in a horizontally scrollable container AND the table itself has `min-width: 500px` so that horizontal scrolling is actually triggered (review concern MEDIUM — without min-width, the table would collapse vertically instead of scrolling horizontally)"
    - "At viewport 375px, the TR service items table (5 columns) is wrapped in a horizontally scrollable container AND the table itself has `min-width: 450px` so horizontal scroll triggers"
    - "At viewport 1440px (desktop), the PR Details and TR Details modals retain their 2-column detail grids — no regression"
    - "At viewport 375px, all .modal-body elements use 1rem padding (not 2rem desktop padding) — increasing effective content width"
    - "At viewport 375px, Finance > Approval modal and Rejection modal (other modals sharing .modal-body) display their content without clipping — the global .modal-body padding reduction does not break other Finance modals (review concern LOW)"
  artifacts:
    - path: "styles/components.css"
      provides: "Mobile overrides inside existing @media (max-width: 768px) block at lines 1284-1415 — adds .modal-details-grid 1-col collapse and .modal-body padding reduction"
      contains: ".modal-details-grid {"
    - path: "app/views/finance.js"
      provides: "viewPRDetails wraps items table in overflow-x:auto div AND sets min-width: 500px on the table; viewTRDetails outer grid div uses .modal-details-grid class; viewTRDetails service items table wrapped in overflow-x:auto div with min-width: 450px on the table"
      contains: "class=\"modal-details-grid\""
  key_links:
    - from: "styles/components.css @media (max-width: 768px)"
      to: ".modal-details-grid (used by viewPRDetails and — after this plan — viewTRDetails)"
      via: "grid-template-columns: 1fr override"
      pattern: "\\.modal-details-grid \\{"
    - from: "app/views/finance.js viewPRDetails line ~3432"
      to: "mobile horizontal scroll with enforced table width"
      via: "<div style=\"overflow-x: auto; -webkit-overflow-scrolling: touch;\"> wrapping <table class=\"modal-items-table\" style=\"min-width: 500px;\">"
      pattern: "min-width: 500px"
    - from: "app/views/finance.js viewTRDetails line ~3540"
      to: "mobile grid collapse"
      via: "replace inline style=\"display: grid; grid-template-columns: repeat(2, 1fr)...\" with class=\"modal-details-grid\""
      pattern: "class=\"modal-details-grid\""
---

<objective>
Mobile-optimize the PR Details Modal and TR Details Modal (both using #prModal shell in finance.js).

Purpose: Make the PR/TR review modals usable on 375-414px phone viewports. Today the 2-column metadata grid renders at ~155px per cell and wide items tables (5-6 columns) overflow the modal body. Fix is CSS-only for the PR modal grid (it already has .modal-details-grid class) and template-string edits for the TR modal (needs class swap) plus table scroll wrappers for both modals. Per peer review (MEDIUM), tables MUST have explicit `min-width` to force horizontal scroll — otherwise mobile browsers will wrap cells vertically and the scroll wrapper is useless.

Output:
- styles/components.css — 3 rules added inside existing @media (max-width: 768px) block
- app/views/finance.js — 3 edits: PR items table wrapper + min-width, TR outer grid class swap, TR items table wrapper + min-width

No data logic changes. No new files. No new CSS classes (reuses global .modal-details-grid).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/73.2-mobile-optimize-finance-modals-financial-breakdown-modal-pr-details-modal/73.2-RESEARCH.md
@.planning/phases/73.2-mobile-optimize-finance-modals-financial-breakdown-modal-pr-details-modal/73.2-VALIDATION.md
@.planning/phases/73.2-mobile-optimize-finance-modals-financial-breakdown-modal-pr-details-modal/73.2-REVIEWS.md

<interfaces>
<!-- Existing CSS classes that will be reused (from styles/components.css) -->

.modal-details-grid (components.css L708-717) — desktop rule:
```css
.modal-details-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
    margin-bottom: 2rem;
    background: #ffffff;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
}
```

.modal-body (components.css L663-669) — desktop rule:
```css
.modal-body {
    padding: 2rem;
    flex: 1;
    overflow-y: auto;
    background: #fafbfc;
}
```

Existing @media (max-width: 768px) block in components.css:
- Opens at line 1284
- Closes at line 1415
- Already contains .modal-footer overrides, .card-header, .filter-toolbar, .table-scroll-container sticky rules
- NEW rules must be inserted INSIDE this block (before the closing brace at line 1415)

.modal-items-table (components.css L744-) — used by PR modal only (TR modal uses inline-styled table).

Note: .modal-details-grid is used in ONLY ONE place in the codebase — viewPRDetails in finance.js. After this plan, viewTRDetails will also use it. Safe to make the 1-col override global.

<!-- Review-driven note on TR Details nested cards -->
The `.modal-body` has `background: #fafbfc` (light gray). The `.modal-details-grid` adds `background: #ffffff` (white) + `padding: 2rem` + `border-radius: 12px` + `box-shadow`. When applied to the TR modal outer grid, this creates a white card on the light gray modal body — THIS IS INTENTIONAL and matches how PR Details currently looks. It is NOT a "double card" issue because the original TR modal body is the light gray modal-body surface, not another white card. Reviewer (Gemini) flagged this as MEDIUM risk; our UAT will confirm visually at 1440px and 375px.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile overrides for .modal-details-grid and .modal-body in components.css</name>
  <read_first>
    - styles/components.css (read lines 660-720 for .modal-body and .modal-details-grid desktop rules; read lines 1284-1415 for the existing @media (max-width: 768px) block)
  </read_first>
  <files>styles/components.css</files>
  <action>
    Insert THIS EXACT BLOCK inside the existing `@media (max-width: 768px) { ... }` block in styles/components.css. Place it immediately BEFORE the closing brace at line 1415 (after the .table-scroll-container tr:hover rule at line 1412-1414). Do NOT create a new @media block — add to the existing one.

    ```css
        /* Phase 73.2: Finance modals mobile overrides */
        .modal-body {
            padding: 1rem;
        }

        .modal-details-grid {
            grid-template-columns: 1fr;
            padding: 1rem;
            gap: 1rem;
        }
    ```

    Rationale:
    - .modal-body padding reduced from 2rem (desktop) to 1rem. At 375px viewport × 95% modal width = 356px; with 2 × 16px padding = 324px effective content width.
    - .modal-details-grid collapses from 2-col to 1-col, with reduced internal padding (2rem → 1rem) and gap (1.5rem → 1rem) so stacked cells don't eat vertical space.
    - Only `grid-template-columns`, `padding`, `gap` are overridden — the card background, border-radius, box-shadow from the desktop rule still apply (CSS inherit-through-cascade).
    - NO `!important` is needed — classes always win over the inline styles we're replacing in Task 2/3 (TR grid will use the class, no inline grid-template-columns).

    Do NOT modify or delete anything else in components.css. The insertion point is ONLY inside the existing @media block.
  </action>
  <verify>
    <automated>grep -n "Phase 73.2: Finance modals mobile overrides" styles/components.css &amp;&amp; grep -c "grid-template-columns: 1fr" styles/components.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Phase 73.2: Finance modals mobile overrides" styles/components.css` returns a line number BETWEEN 1284 and 1415 (inside the existing @media block)
    - `grep -n "\\.modal-body {" styles/components.css` returns at least 2 matches — one at desktop (~L664) and one inside the @media block
    - `grep -n "\\.modal-details-grid {" styles/components.css` returns at least 2 matches — one at desktop (~L708) and one inside the @media block
    - `grep -A1 "Phase 73.2" styles/components.css` shows `padding: 1rem;` on the next line
    - No `!important` appears in the added lines: `grep -A10 "Phase 73.2: Finance modals mobile overrides" styles/components.css | grep -c "!important"` returns 0
    - Line count of components.css increased by exactly 10 lines from baseline (1574 before → 1584 after) — verify with `wc -l styles/components.css`
  </acceptance_criteria>
  <done>components.css contains Phase 73.2 mobile overrides inside the existing 768px media query block. `wc -l styles/components.css` returns 1584.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap PR items table in scroll container with min-width + switch TR grid to .modal-details-grid class + wrap TR items table with min-width</name>
  <read_first>
    - app/views/finance.js (lines 3380-3650 — full viewPRDetails and viewTRDetails template strings)
    - styles/components.css (lines 707-717 and lines 1415 new mobile block from Task 1 — so you understand what .modal-details-grid gives you)
    - .planning/phases/73.2-mobile-optimize-finance-modals-financial-breakdown-modal-pr-details-modal/73.2-REVIEWS.md (peer review — note MEDIUM concerns #1 on table min-width and #2 on TR nested card appearance)
  </read_first>
  <files>app/views/finance.js</files>
  <action>
    Make THREE exact edits to app/views/finance.js. Use the Edit tool for each change to keep diffs surgical.

    IMPORTANT — review-driven change: Both table scroll wrappers MUST include `min-width` on the `<table>` element itself. Without this, per peer review (Gemini MEDIUM #1), the table will simply shrink columns and wrap cell content vertically instead of triggering horizontal scroll. Values chosen:
    - PR items breakdown table (6 cols: Item, Category, Qty, Unit Cost, Supplier, Subtotal): `min-width: 500px`
    - TR service items table (5 cols): `min-width: 450px`

    --- EDIT 1: Wrap PR items table in scroll wrapper + add min-width (viewPRDetails) ---

    Find this block (currently at approximately lines 3431-3461):

    ```
                <h4 style="margin-bottom: 1rem; font-size: 1rem; font-weight: 600; color: #1e293b;">Items Breakdown</h4>
                <table class="modal-items-table">
                    <thead>
    ```

    Replace with (note the `style="min-width: 500px;"` on the table):

    ```
                <h4 style="margin-bottom: 1rem; font-size: 1rem; font-weight: 600; color: #1e293b;">Items Breakdown</h4>
                <div style="overflow-x: auto; -webkit-overflow-scrolling: touch;">
                <table class="modal-items-table" style="min-width: 500px;">
                    <thead>
    ```

    Then find the matching closing `</table>` of this same items table (it is the one immediately before the backtick-closing of modalContent, followed by `;`). It looks like:

    ```
                    </tfoot>
                </table>
            `;
    ```

    Replace with:

    ```
                    </tfoot>
                </table>
                </div>
            `;
    ```

    IMPORTANT: viewPRDetails has only ONE table in its template. Do NOT accidentally edit a table in viewTRDetails (below) — that is Edit 3.

    --- EDIT 2: Swap TR outer grid inline style to .modal-details-grid class (viewTRDetails) ---

    Find this single line (currently at approximately line 3540):

    ```
                <div style="display: grid; grid-template-columns: repeat(2, 1fr); gap: 1rem; margin-bottom: 1.5rem;">
    ```

    Replace with EXACTLY:

    ```
                <div class="modal-details-grid">
    ```

    DO NOT modify any of the inner `<div>` cells inside this grid. The TR body uses raw inline-styled label/value divs (not `.modal-detail-item` / `.modal-detail-label` / `.modal-detail-value`). Per research Pitfall 2 — leave inner divs untouched.

    EXPECTED VISUAL BEHAVIOR (from peer review MEDIUM #2): The desktop rule for `.modal-details-grid` adds `background: #ffffff`, `padding: 2rem`, `border-radius: 12px`, `box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04)`. The TR modal body uses `.modal-body` which has `background: #fafbfc` (light gray). Applying `.modal-details-grid` to the TR outer grid therefore produces: light-gray modal body → white card containing the label/value pairs. This MATCHES how PR Details already looks today and is INTENTIONAL visual harmonization. It is NOT a "double card" problem because the modal body itself is not a white card. UAT will confirm at 1440px and 375px.

    If during visual UAT the white card looks wrong (i.e., the TR modal body turns out to already have white background from some other rule), the fix is to add a scoped override to components.css:

    ```css
    /* Only if UAT shows TR grid double-card at desktop */
    #prModal .modal-body .modal-details-grid {
        /* remove decoration if outer already has it */
    }
    ```

    DO NOT pre-emptively add this scoped override. Only add it if UAT reveals a problem. Note this expectation in the SUMMARY.md.

    --- EDIT 3: Wrap TR service items table in scroll wrapper + add min-width (viewTRDetails) ---

    Find this block (currently at approximately lines 3579-3581):

    ```
                <div style="margin: 1.5rem 0; padding: 1.5rem; background: #f8f9fa; border-radius: 8px;">
                    <h4 style="margin-bottom: 1rem;">Service Items</h4>
                    <table style="width: 100%; font-size: 0.875rem;">
    ```

    Replace with (note the new `min-width: 450px;` merged into the existing inline style on `<table>`):

    ```
                <div style="margin: 1.5rem 0; padding: 1.5rem; background: #f8f9fa; border-radius: 8px;">
                    <h4 style="margin-bottom: 1rem;">Service Items</h4>
                    <div style="overflow-x: auto; -webkit-overflow-scrolling: touch;">
                    <table style="width: 100%; min-width: 450px; font-size: 0.875rem;">
    ```

    Then find the matching closing `</table>` of this TR service items table. It looks like:

    ```
                    </tfoot>
                </table>
            </div>
    ```

    Replace with:

    ```
                    </tfoot>
                </table>
                </div>
            </div>
    ```

    (The added `</div>` closes the new overflow-x wrapper, BEFORE the existing `</div>` that closes the outer padded card.)

    --- VERIFICATION BEFORE COMMIT ---

    After all three edits, run:
    - `grep -c "overflow-x: auto; -webkit-overflow-scrolling: touch;" app/views/finance.js` — should return 2 (one for PR table, one for TR table).
    - `grep -c "min-width: 500px" app/views/finance.js` — should return 1 (PR table).
    - `grep -c "min-width: 450px" app/views/finance.js` — should return 1 (TR table).
    - `grep -c "display: grid; grid-template-columns: repeat(2, 1fr); gap: 1rem; margin-bottom: 1.5rem;" app/views/finance.js` — should return 0 (TR inline grid removed).
    - `grep -c "class=\"modal-details-grid\"" app/views/finance.js` — should return 2 (PR existing + TR new).
  </action>
  <verify>
    <automated>grep -c "overflow-x: auto; -webkit-overflow-scrolling: touch;" app/views/finance.js &amp;&amp; grep -c "class=\"modal-details-grid\"" app/views/finance.js &amp;&amp; grep -c "min-width: 500px\|min-width: 450px" app/views/finance.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "overflow-x: auto; -webkit-overflow-scrolling: touch;" app/views/finance.js` returns exactly 2
    - `grep -c "class=\"modal-details-grid\"" app/views/finance.js` returns exactly 2
    - `grep -c "display: grid; grid-template-columns: repeat(2, 1fr); gap: 1rem; margin-bottom: 1.5rem;" app/views/finance.js` returns exactly 0 (the old TR inline grid style is gone)
    - `grep -c "min-width: 500px" app/views/finance.js` returns exactly 1 (PR items breakdown table — review concern MEDIUM #1)
    - `grep -c "min-width: 450px" app/views/finance.js` returns exactly 1 (TR service items table — review concern MEDIUM #1)
    - `grep -n "Items Breakdown" app/views/finance.js` — the line IMMEDIATELY after contains `overflow-x: auto` (not `<table class="modal-items-table">`)
    - `grep -n "modal-items-table" app/views/finance.js` — the match line includes `style="min-width: 500px;"` (i.e. table has min-width set)
    - `grep -n "Service Items" app/views/finance.js` — within 3 lines after, `overflow-x: auto` appears BEFORE the `<table` tag, and the table line contains `min-width: 450px`
    - `grep -c "<table class=\"modal-items-table\"" app/views/finance.js` returns exactly 1 (PR table — unchanged count, still there)
    - No JavaScript syntax errors: `node --check app/views/finance.js` exits 0 (if node available) OR load localhost:8000/#/finance in browser and open DevTools console — no parse errors on module load
  </acceptance_criteria>
  <done>viewPRDetails and viewTRDetails produce modal HTML with scroll wrappers for both item tables; each inner table has explicit min-width (500px PR, 450px TR) so horizontal scroll triggers on mobile; TR outer grid uses .modal-details-grid class which collapses to 1-col on mobile via Task 1 CSS.</done>
</task>

</tasks>

<verification>
Automated checks:
- `grep -n "Phase 73.2: Finance modals mobile overrides" styles/components.css` matches inside @media block
- `grep -c "overflow-x: auto; -webkit-overflow-scrolling: touch;" app/views/finance.js` returns 2
- `grep -c "class=\"modal-details-grid\"" app/views/finance.js` returns 2
- `grep -c "min-width: 500px" app/views/finance.js` returns 1
- `grep -c "min-width: 450px" app/views/finance.js` returns 1
- `grep -c "display: grid; grid-template-columns: repeat(2, 1fr); gap: 1rem; margin-bottom: 1.5rem;" app/views/finance.js` returns 0
- `wc -l styles/components.css` returns 1584 (was 1574)

Manual UAT (performed after phase verification — no in-plan checkpoint, project convention):

1. **Primary UAT at 375px** (iPhone SE / DevTools narrow):
   - Open Finance > Pending Approvals → click a PR → PR Details modal
     - Detail grid is 1 column ✓
     - Items breakdown table scrolls HORIZONTALLY (drag right reveals hidden columns) ✓
     - Columns do NOT wrap vertically (min-width: 500px is doing its job) ✓
   - Open a TR → TR Details modal
     - Detail grid is 1 column ✓
     - Service Items table scrolls horizontally ✓
     - No visual "double card" — TR grid shows as ONE white card on the light gray modal body ✓

2. **Review-driven LOW concern UAT** (global .modal-body padding reduction at 375px):
   - Finance > Pending Approvals → Approve a PR → Approval modal opens at 375px
     - Modal content is not clipped ✓
     - Buttons and text have reasonable spacing with 1rem padding ✓
   - Finance > Pending Approvals → Reject a PR → Rejection modal opens at 375px
     - Rejection reason textarea and controls are not clipped ✓

3. **Desktop regression UAT at 1440px**:
   - PR Details modal still shows 2-column detail grid ✓
   - TR Details modal shows 2-column detail grid with the SAME card styling as PR (white card on light gray modal body — this is NEW for TR but matches PR) ✓
   - No other Finance modals visually regressed ✓
</verification>

<success_criteria>
- PR Details modal detail grid stacks to 1-col at 375px
- TR Details modal detail grid stacks to 1-col at 375px
- PR items table scrolls horizontally inside modal at 375px (confirmed by min-width: 500px on table)
- TR service items table scrolls horizontally inside modal at 375px (confirmed by min-width: 450px on table)
- All modals display 2-col grids at 1440px (no desktop regression)
- Modal body padding is 1rem on mobile, 2rem on desktop
- Finance Approval and Rejection modals at 375px are not clipped by the new 1rem .modal-body padding
- TR Details modal at desktop shows a single white card (modal-details-grid) on the light-gray modal body — matches PR Details visual pattern, no "double card"
- No JavaScript module load errors
</success_criteria>

<output>
After completion, create `.planning/phases/73.2-mobile-optimize-finance-modals-financial-breakdown-modal-pr-details-modal/73.2-01-SUMMARY.md`
</output>
</content>
</invoke>