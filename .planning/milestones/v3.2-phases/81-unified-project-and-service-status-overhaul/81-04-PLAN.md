---
phase: 81-unified-project-and-service-status-overhaul
plan: 04
type: execute
wave: 2
depends_on:
  - 81-01
  - 81-02
  - 81-03
files_modified: []
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06

must_haves:
  truths:
    - "User confirms Projects list, Project Detail, Services list, Service Detail, Home dashboard, and Edit History all use unified Status with no Internal Status remnants"
    - "User confirms a legacy project (with old project_status value) renders with grey italic (legacy) suffix in the list cell, can be found via Status filter under 'Other (legacy)' optgroup, and re-saves correctly to a new value (REVIEWS Concern 2 verified)"
    - "User confirms home charts render all 10 status labels readably at 1366x768 and at 375-414px mobile (REVIEWS Concern 1 verified)"
    - "User confirms a legacy edit-history record with field='internal_status' displays as 'Internal Status (Legacy) changed from X to Y' (REVIEWS Suggestion 3 verified)"
    - "User confirms Firestore writes from new project/service creation do not include internal_status"
  artifacts:
    - path: ".planning/phases/81-unified-project-and-service-status-overhaul/81-04-SUMMARY.md"
      provides: "UAT verification record with screenshots/notes for all 13 checks (10 D-* + 3 review-fix verifications)"
  key_links: []
---

<objective>
Manual UAT checkpoint to verify Plans 81-01, 81-02, and 81-03 work together end-to-end. The user walks through the 5 surfaces (Projects list/detail, Services list/detail, Home, Edit History) on the live site and confirms expected behavior across 13 checks — including the three review-fix verifications (chart label readability at 1366x768, legacy filter optgroup, edit-history Internal Status (Legacy) label). No new code; this plan only writes a UAT record.

Purpose: All implementation plans ran in parallel and were verified individually via grep. UAT confirms cross-plan integration (e.g. saving a project on Plan 81-01's form correctly updates the chart in Plan 81-03's home dashboard via onSnapshot) AND that the three review-driven fixes survive end-to-end.
Output: Filled UAT record in 81-04-SUMMARY.md with pass/fail per check and any defects flagged for follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-unified-project-and-service-status-overhaul/81-CONTEXT.md
@.planning/phases/81-unified-project-and-service-status-overhaul/81-REVIEWS.md
@.planning/phases/81-unified-project-and-service-status-overhaul/81-01-PLAN.md
@.planning/phases/81-unified-project-and-service-status-overhaul/81-02-PLAN.md
@.planning/phases/81-unified-project-and-service-status-overhaul/81-03-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT — verify unified status across 5 surfaces + 3 review-fix verifications</name>
  <files>.planning/phases/81-unified-project-and-service-status-overhaul/81-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/81-unified-project-and-service-status-overhaul/81-CONTEXT.md
    - .planning/phases/81-unified-project-and-service-status-overhaul/81-REVIEWS.md
    - .planning/phases/81-unified-project-and-service-status-overhaul/81-01-PLAN.md
    - .planning/phases/81-unified-project-and-service-status-overhaul/81-02-PLAN.md
    - .planning/phases/81-unified-project-and-service-status-overhaul/81-03-PLAN.md
  </read_first>
  <what-built>
    Plans 81-01, 81-02, 81-03 implemented:
    - Single Status dropdown on Projects + Services create/edit forms
    - Single Status filter (replacing two filters) on Projects + Services list pages, with dynamic "Other (legacy)" optgroup injection when legacy values exist (REVIEWS Concern 2 fix)
    - Single Status column in Projects + Services tables; legacy values render with grey italic (legacy) suffix
    - Single Status dropdown on Project Detail and Service Detail Status & Assignment cards (with legacy fallback for old values)
    - Single chart per entity on Home dashboard (Projects: 1; Services: 2 — one per service type), each sized via new .hs-chart-status class (320px desktop / 360px mobile) so 10-bar layouts and full-length labels render without clipping (REVIEWS Concern 1 fix)
    - Edit history modal labels project_status changes as "Status" and shows "Internal Status (Legacy)" for orphaned internal_status records (REVIEWS Suggestion 3 fix)
    - Firestore writes no longer include internal_status field
    - 10 unified status options: For Inspection, For Proposal, Proposal for Internal Approval, Proposal Under Client Review, For Revision, Client Approved, For Mobilization, On-going, Completed, Loss
    - cachedStats refactored to a fresh 6-key literal so first render after deployment cannot read stale 8-key shape (REVIEWS Concern 4 fix)
  </what-built>
  <action>
    Pause execution and present the human-verify checkpoint to the user. Do NOT proceed past this task until the user types "approved" or describes specific failures. After the user signals, write `.planning/phases/81-unified-project-and-service-status-overhaul/81-04-SUMMARY.md` capturing the verification matrix below.

    The 13 walkthrough checks are detailed in &lt;how-to-verify&gt;. Hand off to the user with a clear summary of what to test and where; collect their results; produce the SUMMARY.md.

    SUMMARY.md must contain:
    1. Header: `# Phase 81 UAT — Plan 04 Summary`
    2. A results table with all 13 rows:
       ```
       | Check | Surface | Result | Notes |
       |-------|---------|--------|-------|
       | 1 | Projects list form | PASS/FAIL | ... |
       | 2 | Projects list filter & table | PASS/FAIL | ... |
       | ... | ... | ... | ... |
       | 13 | Console hygiene | PASS/FAIL | ... |
       ```
    3. Section "Review fixes verified" cross-referencing REVIEWS.md (Concern 1, Concern 2, Concern 4, Suggestion 3) with PASS/FAIL each
    4. Section "Defects flagged for follow-up" (empty if all pass)
    5. Section "Sign-off" with date and "Approved by user: yes/no"
  </action>
  <how-to-verify>
    Run the static server and walk through these 13 checks. For each, note PASS/FAIL and any unexpected behavior in 81-04-SUMMARY.md.

    **Setup:**
    1. From repo root: `python -m http.server 8000` (or `npx http-server`)
    2. Open http://localhost:8000 in Chrome with DevTools Console + Network tabs visible
    3. Resize browser window to exactly 1366x768 for desktop checks (use DevTools Device Toolbar > Responsive > set 1366x768) — REVIEWS Concern 1 specifically calls out this resolution
    4. Log in with a Super Admin account

    **Check 1 — Projects list form (Plan 81-01 Task 1):**
    - Navigate to `#/projects` (or click Projects nav)
    - Click "Add New Project"
    - Verify the form shows ONE Status dropdown labeled "Status *" (no Internal Status field, no Project Status field)
    - Open the dropdown — verify exactly 10 options in this order: For Inspection, For Proposal, Proposal for Internal Approval, Proposal Under Client Review, For Revision, Client Approved, For Mobilization, On-going, Completed, Loss
    - Cancel the form

    **Check 2 — Projects list filter & table (Plan 81-01 Task 1):**
    - On the Projects list, verify the filter bar shows ONE Status filter (no Internal Status filter)
    - Verify the table has ONE Status column header (no Internal Status column)
    - If existing projects have legacy values (e.g. "Approved by Client"), verify the cells display the raw value with grey italic `(legacy)` suffix (no crash, no blank cell)

    **Check 3 — Create new project (Plan 81-01 Task 1 + Plan 81-03 Task 1 wiring):**
    - Click "Add New Project", fill all fields, pick Status = "On-going", save
    - In DevTools Network tab, find the Firestore POST and inspect the request body — confirm `project_status: "On-going"` is present and `internal_status` is NOT present anywhere
    - Confirm the new project row appears in the table with Status = "On-going"

    **Check 4 — CSV export (Plan 81-01 Task 1):**
    - Click "Export CSV" (or whatever button triggers downloadCSV)
    - Open the downloaded file in a text editor
    - Verify header row: `Code,Name,Client,Status,Active` (5 columns, no "Internal Status" or "Project Status")

    **Check 5 — Project Detail page (Plan 81-01 Task 2):**
    - Click into the project just created
    - In Status & Assignment card, verify ONE Status dropdown (no second dropdown)
    - Change Status to "Completed", click outside to trigger save
    - Refresh the page and verify "Completed" persists
    - In DevTools Network: verify the updateDoc payload contains `project_status: "Completed"` and NOT `internal_status`

    **Check 6 — Legacy fallback in detail page (Plan 81-01 Task 2 + Plan 81-02 Task 2):**
    - In Firebase Console (or via a project with old data), find a project whose `project_status` is a legacy value not in the new list (e.g. "Pending Client Review", "Approved by Client", "Under Client Review")
    - Open Project Detail for that project
    - Verify the Status dropdown shows a `(legacy)` option in grey italic, selected
    - Pick a new status from the list (e.g. "On-going") — verify save succeeds
    - Refresh — the dropdown should now show "On-going" selected with no legacy option visible
    - If no legacy data exists: skip this check or manually edit a Firestore doc to set `project_status: "Pending Client Review"` for testing

    **Check 7 — Legacy filter optgroup [REVIEWS CONCERN 2 VERIFICATION] (Plan 81-01 Task 1 Edit 8b):**
    - With at least one project having a legacy `project_status` value (e.g. "Pending Client Review" — set via Firebase Console or by editing a doc directly)
    - Reload the Projects list page
    - Open the Status filter dropdown — verify there is an `<optgroup label="Other (legacy)">` at the bottom containing the legacy value(s) with `(legacy)` suffix
    - Select that legacy value — verify the table filters down to ONLY projects with that legacy status
    - Verify those filtered rows render with grey italic `(legacy)` suffix in the Status column
    - Edit one of those projects via the detail page and pick a new unified status; refresh the list — verify that legacy value disappears from the optgroup once no project still holds it

    **Check 8 — Services list mirror (Plan 81-02 Task 1):**
    - Repeat Checks 1-4 on `#/services` — same expectations apply to Services
    - Confirm DOM ids `serviceProjectStatus` and `serviceProjectStatusFilter` are intact (DevTools Inspect)
    - Repeat Check 7's legacy-optgroup logic on the Services filter (using a service with a legacy `project_status` value)

    **Check 9 — Service Detail mirror (Plan 81-02 Task 2):**
    - Repeat Checks 5-6 on a Service Detail page

    **Check 10 — Home dashboard charts (Plan 81-03 Task 1):**
    - Navigate to `#/` (Home) at exactly 1366x768
    - As Super Admin (mode = 'both'), verify Projects card shows ONE Status chart and Services card shows TWO Status charts (One-time + Recurring) — total 3 charts (was 6 before)
    - Verify chart segments use the new highlighted colors for For Inspection / For Proposal / Proposal Under Client Review / On-going (lighter brand hues) and slate for the rest
    - Hover a segment — tooltip should show "Status: count (percent%)"
    - Edit a project's status from another tab (or via Firebase Console) — within ~2s, the home chart should update via onSnapshot

    **Check 11 — Chart label readability [REVIEWS CONCERN 1 VERIFICATION] (Plan 81-03 Task 1 Edit 4):**
    - At 1366x768: read each of the 10 status labels on every chart (Projects, Services One-time, Services Recurring) — verify NONE are clipped, truncated, or overlapping. Specifically check the long labels: "Proposal Under Client Review" (28 chars) and "Proposal for Internal Approval" (30 chars). Both must be fully visible.
    - Switch to mobile viewport (375x667 iPhone SE in DevTools Device Toolbar) — verify the same labels remain readable; multi-line wrap is acceptable but no clipping
    - Inspect the canvas wrapper element — confirm it has class `hs-chart-status` and computed height of 320px (desktop) or 360px (mobile)

    **Check 12 — Edit History modal [REVIEWS SUGGESTION 3 VERIFICATION] (Plan 81-03 Task 2):**
    - On a project where you changed status during Check 5, click Edit History
    - Verify the most recent timeline entry reads `Status: <old> → <new>` (NOT "Project Status: ..." and NOT "Internal Status: ...")
    - If legacy edit-history entries with field='internal_status' exist, verify they display as `Internal Status (Legacy) changed from X to Y` — NOT as the raw key `internal_status`. (If no legacy edit-history entries exist, you may seed one via Firebase Console: add a record under `edit_history` for an existing project with field='internal_status', old_value='For Inspection', new_value='Ready to Submit'.)

    **Check 13 — Console hygiene + cachedStats freshness [REVIEWS CONCERN 4 VERIFICATION]:**
    - With DevTools Console open throughout the walkthrough, verify zero JavaScript errors on Projects, Services, Detail pages, and Home
    - On the Home page, type `cachedStats` in the DevTools console (if exposed) or inspect via the running module — verify it has exactly 6 keys: activeMRFs, pendingPRs, activePOs, projectsByStatus, servicesByStatusOneTime, servicesByStatusRecurring. NO legacy keys (projectsByInternalStatus, servicesByInternalStatusOneTime, etc.) should be present.
    - Chart.js warnings about missing data are acceptable on first paint before onSnapshot fires; persistent warnings are NOT acceptable
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/81-unified-project-and-service-status-overhaul/81-04-SUMMARY.md && grep -q "PASS\|FAIL" .planning/phases/81-unified-project-and-service-status-overhaul/81-04-SUMMARY.md && grep -q "Review fixes verified" .planning/phases/81-unified-project-and-service-status-overhaul/81-04-SUMMARY.md</automated>
  </verify>
  <resume-signal>Type "approved" if all 13 checks pass, or describe any failures with check number(s) and screenshots/error messages.</resume-signal>
  <acceptance_criteria>
    - File `.planning/phases/81-unified-project-and-service-status-overhaul/81-04-SUMMARY.md` exists
    - SUMMARY contains a results table with one row per check (1 through 13) and a PASS/FAIL column
    - SUMMARY contains the literal string `# Phase 81 UAT` in a header
    - SUMMARY contains a "Review fixes verified" section with explicit PASS/FAIL for REVIEWS Concern 1 (chart sizing), Concern 2 (legacy filter), Concern 4 (cachedStats), and Suggestion 3 (edit-history label)
    - SUMMARY contains a "Defects flagged for follow-up" section (may be empty)
    - SUMMARY contains a "Sign-off" section with the user's approval status
    - All 13 checks marked PASS, OR failures explicitly enumerated with the defect handed off to a follow-up gap-closure plan
  </acceptance_criteria>
  <done>User has walked through the 13 checks on the live site (including the 3 review-fix verifications), all 13 pass (or failures captured for follow-up), and the SUMMARY.md captures the verification record with sign-off.</done>
</task>

</tasks>

<verification>
- 81-04-SUMMARY.md exists and contains a results matrix with PASS/FAIL markers per check
- All 13 checks marked PASS, or follow-up gap closure scoped if any FAIL
- Review-fix section explicitly addresses REVIEWS Concern 1, Concern 2, Concern 4, and Suggestion 3
</verification>

<success_criteria>
- User confirms unified status works end-to-end on the live site
- User confirms all three review-driven fixes (chart sizing, legacy filter optgroup, edit-history Legacy label) work as expected
- 81-04-SUMMARY.md captures the verification record
</success_criteria>

<output>
After completion, ensure `.planning/phases/81-unified-project-and-service-status-overhaul/81-04-SUMMARY.md` exists with the UAT results matrix and any defect notes.
</output>
</content>
</invoke>