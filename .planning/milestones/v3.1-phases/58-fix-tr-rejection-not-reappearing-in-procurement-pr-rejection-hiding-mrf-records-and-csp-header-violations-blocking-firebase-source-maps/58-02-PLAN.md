---
phase: 58-production-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - _headers
  - netlify.toml
autonomous: true
requirements:
  - BUG-03

must_haves:
  truths:
    - "Browser console shows no CSP connect-src violation for Firebase .js.map source map files"
    - "Firebase SDK initializes correctly in strict CSP environments (no script execution blocked by CSP)"
    - "Both _headers and netlify.toml CSP connect-src directives include https://www.gstatic.com"
  artifacts:
    - path: "_headers"
      provides: "Netlify headers with corrected CSP connect-src"
      contains: "https://www.gstatic.com"
    - path: "netlify.toml"
      provides: "Netlify TOML headers with corrected CSP connect-src"
      contains: "https://www.gstatic.com"
  key_links:
    - from: "_headers connect-src directive"
      to: "Firebase source map fetches from https://www.gstatic.com"
      via: "CSP allow-list checked by browser before each network request"
      pattern: "connect-src.*gstatic"
    - from: "netlify.toml Content-Security-Policy value"
      to: "Firebase source map fetches from https://www.gstatic.com"
      via: "Netlify serves CSP header to browser for all routes"
      pattern: "connect-src.*gstatic"
---

<objective>
Fix CSP (Content Security Policy) violations caused by Firebase SDK attempting to fetch source map files (*.js.map) from https://www.gstatic.com, which is not in the current connect-src allowlist.

Purpose: CSP violations may cause Firebase to fail initialization in strict-CSP browsers, explaining why Bugs #1 and #2 manifest only for specific Procurement users. Adding https://www.gstatic.com to connect-src stops the violations and allows Firebase to function correctly in all browsers.
Output: Updated _headers and netlify.toml with https://www.gstatic.com in connect-src.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@_headers
@netlify.toml
</context>

<interfaces>
<!-- Current CSP connect-src in both files (identical). -->

Current connect-src in _headers (line 11, line 23):
  connect-src 'self' https://*.firebaseio.com https://*.googleapis.com wss://*.firebaseio.com

Current connect-src in netlify.toml (two Content-Security-Policy entries, /* and /*.html):
  connect-src 'self' https://*.firebaseio.com https://*.googleapis.com wss://*.firebaseio.com

CSP violation reported by users:
  Connecting to 'https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js.map' violates CSP connect-src directive
  Connecting to 'https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js.map' violates CSP connect-src directive
  Connecting to 'https://www.gstatic.com/firebasejs/10.7.1/firebase-auth.js.map' violates CSP connect-src directive

Fix: append https://www.gstatic.com to connect-src in all occurrences.

Note: script-src already includes https://*.gstatic.com (correct for script loading).
connect-src is separate and controls fetch/XHR/WebSocket — source map fetches use connect-src.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add https://www.gstatic.com to connect-src in _headers</name>
  <files>_headers</files>
  <action>
    The _headers file has two Content-Security-Policy entries (one for /* at line ~11, one for /*.html at line ~23). Both have identical connect-src directives that must be updated.

    In BOTH CSP header lines, change:
      connect-src 'self' https://*.firebaseio.com https://*.googleapis.com wss://*.firebaseio.com

    To:
      connect-src 'self' https://*.firebaseio.com https://*.googleapis.com wss://*.firebaseio.com https://www.gstatic.com

    Do NOT change script-src (it already has https://*.gstatic.com which covers script loading).
    Do NOT change any other directives.
    Both CSP entries (/* and /*.html) must be updated — they are independently specified and must match.
  </action>
  <verify>
    grep "connect-src" _headers
    Expected: both lines contain "https://www.gstatic.com" at the end of connect-src
    grep -c "https://www.gstatic.com" _headers
    Expected: 2 (one per CSP entry)
  </verify>
  <done>Both Content-Security-Policy headers in _headers include https://www.gstatic.com in connect-src.</done>
</task>

<task type="auto">
  <name>Task 2: Add https://www.gstatic.com to connect-src in netlify.toml</name>
  <files>netlify.toml</files>
  <action>
    The netlify.toml file has two [[headers]] sections with Content-Security-Policy values (one for /* and one for /*.html). Both use identical connect-src that must be updated.

    In BOTH Content-Security-Policy string values, change:
      connect-src 'self' https://*.firebaseio.com https://*.googleapis.com wss://*.firebaseio.com

    To:
      connect-src 'self' https://*.firebaseio.com https://*.googleapis.com wss://*.firebaseio.com https://www.gstatic.com

    The CSP value is a single long string on one line inside the TOML value — append https://www.gstatic.com after wss://*.firebaseio.com in both occurrences.
    Do NOT change script-src or any other directives.
  </action>
  <verify>
    grep "connect-src" netlify.toml
    Expected: both Content-Security-Policy lines contain "https://www.gstatic.com"
    grep -c "https://www.gstatic.com" netlify.toml
    Expected: 2 (one per CSP entry)
  </verify>
  <done>Both Content-Security-Policy entries in netlify.toml include https://www.gstatic.com in connect-src.</done>
</task>

</tasks>

<verification>
After both tasks:
1. grep "connect-src" _headers — both lines end with https://www.gstatic.com
2. grep "connect-src" netlify.toml — both lines end with https://www.gstatic.com
3. No other header values changed
4. After deployment: browser console shows no CSP violation for gstatic.com source map requests
</verification>

<success_criteria>
- _headers: connect-src includes https://www.gstatic.com in both /* and /*.html sections
- netlify.toml: connect-src includes https://www.gstatic.com in both /* and /*.html [[headers]] blocks
- All other CSP directives remain unchanged
- No new CSP directives added beyond the one gstatic.com addition
</success_criteria>

<output>
After completion, create `.planning/phases/58-fix-tr-rejection-not-reappearing-in-procurement-pr-rejection-hiding-mrf-records-and-csp-header-violations-blocking-firebase-source-maps/58-02-SUMMARY.md`
</output>
