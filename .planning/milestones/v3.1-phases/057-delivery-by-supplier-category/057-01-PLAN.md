---
phase: 057-delivery-by-supplier-category
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/mrf-form.js
  - app/views/procurement.js
autonomous: true
requirements:
  - PRTR-01
  - PRTR-02
  - PRTR-03
  - PRTR-04

must_haves:
  truths:
    - "The standalone MRF form category dropdown includes 'DELIVERY BY SUPPLIER' as a selectable option"
    - "The Procurement Create MRF category dropdown includes 'DELIVERY BY SUPPLIER' as a selectable option"
    - "The Procurement MRF editing item row category dropdown includes 'DELIVERY BY SUPPLIER' as a selectable option"
    - "When all items are 'DELIVERY BY SUPPLIER' category, the action buttons show 'Generate PR' (not 'Submit as TR')"
    - "Attempting to generate a PR with a 'DELIVERY BY SUPPLIER' item that has no supplier selected is blocked with a supplier-required toast"
    - "A 'DELIVERY BY SUPPLIER' item passes the PR item filter (not the TR filter) and appears on the resulting PR/PO"
  artifacts:
    - path: "app/views/mrf-form.js"
      provides: "Standalone MRF form with DELIVERY BY SUPPLIER category option"
      contains: "DELIVERY BY SUPPLIER"
    - path: "app/views/procurement.js"
      provides: "Procurement MRF editing with DELIVERY BY SUPPLIER in all category dropdowns"
      contains: "DELIVERY BY SUPPLIER"
  key_links:
    - from: "procurement.js item-category select (render + addItem template)"
      to: "updateActionButtons() transportCategories array"
      via: "'DELIVERY BY SUPPLIER' absent from transportCategories => hasNonTransport=true => Generate PR button shown"
      pattern: "transportCategories = \\['TRANSPORTATION', 'HAULING & DELIVERY'\\]"
    - from: "procurement.js generatePR() / generatePRandTR()"
      to: "prItems filter"
      via: "!transportCategories.includes(category) => DELIVERY BY SUPPLIER lands in prItems"
      pattern: "allItems.filter\\(item => !transportCategories"
    - from: "procurement.js supplier validation"
      to: "DELIVERY BY SUPPLIER items"
      via: "!transportCategories.includes(category) && !supplier => toast error (same as all other PR items)"
      pattern: "if \\(!transportCategories.includes\\(category\\) && !supplier\\)"
---

<objective>
Add "DELIVERY BY SUPPLIER" as an item category in both MRF creation surfaces and ensure it routes through the PR/PO workflow (not TR) with mandatory supplier selection.

Purpose: Supplier-delivered goods currently have no dedicated category. If tagged as HAULING & DELIVERY or TRANSPORTATION they incorrectly generate a TR. This change introduces a purpose-built category that stays outside the `transportCategories` array, so it automatically flows to PR generation and requires a supplier — matching the validation already in place for all other PR item categories.

Output: Two files modified (mrf-form.js, procurement.js). No logic changes required beyond adding the option in the right places; existing routing and supplier-validation logic handles the rest.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Key interfaces the executor needs — no codebase exploration required -->
<interfaces>
<!-- From app/views/procurement.js — transportCategories (defined in 4 functions) -->
<!-- Lines ~1331, ~3290, ~3587, ~3101 — all identical: -->
const transportCategories = ['TRANSPORTATION', 'HAULING & DELIVERY'];

<!-- From procurement.js — supplier validation (in generatePR and generatePRandTR) -->
// Supplier is required for PR items (NOT transport items)
if (!transportCategories.includes(category) && !supplier) {
    showToast(`Please select supplier for item: ${itemName}`, 'error');
    return;
}

<!-- From procurement.js — PR item filter (in generatePR and generatePRandTR) -->
const prItems = allItems.filter(item => !transportCategories.includes(item.category));
const trItems = allItems.filter(item => transportCategories.includes(item.category));

<!-- From procurement.js — updateActionButtons category routing -->
// hasTransport = any item whose category is in transportCategories
// hasNonTransport = any item whose category is set but NOT in transportCategories
// DELIVERY BY SUPPLIER (not in transportCategories) => hasNonTransport = true => Generate PR button

<!-- Category dropdown locations in procurement.js that need the new option: -->
// 1. renderItemRow() template — line ~1161 (existing MRF item rows, data-driven render)
// 2. addItem() new-row template — line ~1392 (inline HTML string for new rows)
// Both use: <select class="item-category table-select" ...>

<!-- Category dropdown location in mrf-form.js: -->
// Static HTML template — line ~307: <select class="item-category" required>
// After "HAULING & DELIVERY" option, before "OTHERS" option
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DELIVERY BY SUPPLIER option to standalone MRF form</name>
  <files>app/views/mrf-form.js</files>
  <action>
In the static HTML template inside `render()`, find the `<select class="item-category" required>` element (around line 307). It currently ends with:
```html
<option value="HAULING & DELIVERY">HAULING & DELIVERY</option>
<option value="OTHERS">OTHERS</option>
```
Insert the new option between HAULING & DELIVERY and OTHERS:
```html
<option value="HAULING & DELIVERY">HAULING & DELIVERY</option>
<option value="DELIVERY BY SUPPLIER">DELIVERY BY SUPPLIER</option>
<option value="OTHERS">OTHERS</option>
```
No other changes needed in mrf-form.js. The standalone form does not have PR/TR routing — it only submits the MRF with items_json. The routing decisions happen in procurement.js when the MRF is processed.
  </action>
  <verify>grep -n "DELIVERY BY SUPPLIER" /c/Users/Admin/Roaming/pr-po/app/views/mrf-form.js</verify>
  <done>"DELIVERY BY SUPPLIER" appears in the item-category select in mrf-form.js</done>
</task>

<task type="auto">
  <name>Task 2: Add DELIVERY BY SUPPLIER option to all three procurement.js category dropdowns</name>
  <files>app/views/procurement.js</files>
  <action>
Three distinct locations in procurement.js need the new option added. In all three, insert after the `HAULING & DELIVERY` option and before the `OTHERS` option.

**Location 1 — renderItemRow() existing-row template (around line 1171):**
The data-driven row render used when loading an existing MRF's items. Currently:
```html
<option value="HAULING & DELIVERY" ${(item.category || '') === 'HAULING & DELIVERY' ? 'selected' : ''}>HAULING & DELIVERY</option>
<option value="OTHERS" ${(item.category || '') === 'OTHERS' ? 'selected' : ''}>OTHERS</option>
```
Add between them:
```html
<option value="DELIVERY BY SUPPLIER" ${(item.category || '') === 'DELIVERY BY SUPPLIER' ? 'selected' : ''}>DELIVERY BY SUPPLIER</option>
```

**Location 2 — addItem() new-row template (around line 1402):**
The inline HTML string for dynamically added rows. Currently:
```html
<option value="HAULING & DELIVERY">HAULING & DELIVERY</option>
<option value="OTHERS">OTHERS</option>
```
Add between them:
```html
<option value="DELIVERY BY SUPPLIER">DELIVERY BY SUPPLIER</option>
```

**Location 3 — Verify transportCategories is NOT modified:**
The `transportCategories` array is defined as `['TRANSPORTATION', 'HAULING & DELIVERY']` in four functions: `updateActionButtons()`, `generatePR()`, `generatePRandTR()`, and `submitTransportRequest()`. Do NOT add 'DELIVERY BY SUPPLIER' to this array. Its absence from `transportCategories` is what makes it route to PR (not TR) and triggers the existing supplier-required validation.

After adding the options, do a search to confirm there are now exactly 2 occurrences of "DELIVERY BY SUPPLIER" in procurement.js (one per dropdown location) and that transportCategories still only contains 'TRANSPORTATION' and 'HAULING & DELIVERY'.
  </action>
  <verify>grep -c "DELIVERY BY SUPPLIER" /c/Users/Admin/Roaming/pr-po/app/views/procurement.js</verify>
  <done>grep returns 2 (one in renderItemRow, one in addItem template). transportCategories arrays remain unchanged with only 'TRANSPORTATION' and 'HAULING & DELIVERY'. The routing logic (PR filter, TR filter, supplier validation) requires no edits — DELIVERY BY SUPPLIER items will automatically: (a) set hasNonTransport=true in updateActionButtons, showing Generate PR button; (b) pass to prItems via the !transportCategories filter; (c) trigger supplier-required validation via the same !transportCategories check.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify end-to-end routing correctness:

1. grep confirms "DELIVERY BY SUPPLIER" appears in mrf-form.js (Task 1)
2. grep confirms "DELIVERY BY SUPPLIER" appears in procurement.js (Task 2)
3. grep confirms transportCategories arrays in procurement.js still only reference TRANSPORTATION and HAULING & DELIVERY (no contamination)
4. Code trace confirms: `!transportCategories.includes('DELIVERY BY SUPPLIER')` is `true` => supplier validation fires + item goes to prItems filter
</verification>

<success_criteria>
1. mrf-form.js category dropdown includes "DELIVERY BY SUPPLIER" between HAULING & DELIVERY and OTHERS
2. procurement.js renderItemRow() includes "DELIVERY BY SUPPLIER" with selected state support
3. procurement.js addItem() new-row template includes "DELIVERY BY SUPPLIER"
4. transportCategories arrays unchanged (DELIVERY BY SUPPLIER absent = routes to PR)
5. No new functions, no new arrays, no new logic — purely additive option insertion
</success_criteria>

<output>
After completion, create `.planning/phases/057-delivery-by-supplier-category/057-01-SUMMARY.md`
</output>
