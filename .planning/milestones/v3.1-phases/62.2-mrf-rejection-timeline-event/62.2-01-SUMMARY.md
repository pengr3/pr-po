# Phase 62.2 Plan 01 Summary: MRF Rejection Timeline Event

## What Changed
Added "MRF Rejected" timeline event to both `showProcurementTimeline()` in procurement.js and `showTimelineLocal()` in mrf-records.js.

## Implementation
- Guarded by `mrf.status === 'Rejected'` — only shown for rejected MRFs
- Uses `rejected` CSS class (red styling, consistent with PR/TR rejection events)
- Displays: rejection reason, rejected_by actor, rejected_at timestamp
- Positioned after "MRF Created" entry, before PR/TR lifecycle events

## Files Modified
- `app/views/procurement.js` — 8 lines inserted after MRF Created block
- `app/views/mrf-records.js` — 8 lines inserted after MRF Created block

## Verification
- Code follows identical pattern to existing PR Rejected and TR Rejected timeline events
- Fields (`rejected_at`, `rejection_reason`, `rejected_by`) are written by `rejectMRF()` in Phase 62
