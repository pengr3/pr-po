---
phase: 46-code-cleanup-and-mrf-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/procurement-base.js
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [CLN-01, CLN-02, MRF-01]
must_haves:
  truths:
    - "app/views/procurement-base.js no longer exists in the repository"
    - "No unreferenced view or utility JS files remain in app/ (aside from seed-roles.js which is flagged for user review)"
    - "CLN-02 is marked Complete in REQUIREMENTS.md"
  artifacts:
    - path: "app/views/procurement-base.js"
      provides: "DELETED — must not exist"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CLN-02 marked complete"
      contains: "[x] **CLN-02**"
  key_links: []
---

<objective>
Remove the dead file `app/views/procurement-base.js` and mark CLN-02 as complete.

Purpose: The Phase 46 verification found that `procurement-base.js` (321 lines, full view lifecycle exports render/init/destroy) has zero imports anywhere in the SPA. It was missed by the original research dependency trace and was not removed in Plan 46-01. This blocks CLN-02 ("Any other unreferenced/orphaned view or utility files are identified and removed").

Output: Dead file removed, CLN-02 requirement satisfied.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-code-cleanup-and-mrf-fix/46-VERIFICATION.md
@.planning/phases/46-code-cleanup-and-mrf-fix/46-01-SUMMARY.md

Gap being closed:
- `app/views/procurement-base.js` is a 321-line file that exports render(), init(), and destroy() but is never imported by router.js, admin.js, or any other SPA file.
- grep for "procurement-base" across all .js and .html files returns zero results.
- The file was actually modified in Plan 46-01 (6 ad-hoc console.logs were removed from it) but was not identified as dead — the research dependency trace omitted it entirely.
- CLN-02 remains Pending in REQUIREMENTS.md.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove procurement-base.js and mark CLN-02 complete</name>
  <files>app/views/procurement-base.js, .planning/REQUIREMENTS.md</files>
  <action>
1. Delete the dead file:
   ```bash
   git rm app/views/procurement-base.js
   ```
   This file (321 lines) exports render/init/destroy but has zero imports anywhere in the SPA import chain. It was likely an earlier iteration of procurement.js that was never cleaned up.

2. Verify no dangling references were introduced:
   ```bash
   grep -rn "procurement-base" app/ scripts/ index.html styles/ --include="*.js" --include="*.html" --include="*.css"
   ```
   Expected: zero results (already confirmed during verification).

3. Update REQUIREMENTS.md to mark CLN-02 as complete:
   - Change `- [ ] **CLN-02**:` to `- [x] **CLN-02**:`
   - In the Traceability table, change CLN-02 status from `Pending` to `Complete`

4. Commit with message: `fix(46): remove dead procurement-base.js — close CLN-02 gap`
  </action>
  <verify>
    <automated>cd C:/Users/franc/dev/projects/pr-po && test ! -f app/views/procurement-base.js && echo "DELETED" || echo "STILL EXISTS"</automated>
  </verify>
  <done>procurement-base.js is deleted from the repository, no references remain, and CLN-02 is marked Complete in REQUIREMENTS.md</done>
</task>

</tasks>

<verification>
1. `test ! -f app/views/procurement-base.js` passes (file deleted)
2. `grep -rn "procurement-base" app/ scripts/ index.html` returns zero results (no dangling references)
3. `grep "CLN-02" .planning/REQUIREMENTS.md` shows `[x]` (marked complete)
4. All SPA views still load correctly (no runtime regression — the file was never imported)
</verification>

<success_criteria>
- procurement-base.js no longer exists in the repository
- CLN-02 is marked Complete in REQUIREMENTS.md
- No dangling references to the removed file exist anywhere in the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/46-code-cleanup-and-mrf-fix/46-03-SUMMARY.md`
</output>
