---
phase: 47.1-add-sortable-headers-to-procurement-records-tab
verified: 2026-03-01T07:30:00Z
status: passed
score: 7/7 must-haves verified
re_verification:
  previous_status: passed
  previous_score: 7/7
  gaps_closed:
    - "Procurement Status column header is now sortable"
    - "Sort order follows procurement process flow ordinal (not alphabetical)"
    - "MRFs with multiple POs sort by bottleneck (least-progressed) PO status"
    - "MRFs with no POs (Transport/no POs yet) sort to end"
    - "Clicking Procurement Status header toggles ascending/descending"
    - "Sort indicator shows on Procurement Status when active"
    - "All three existing sortable columns (MRF ID, Date Needed, MRF Status) continue to work"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Navigate to Procurement > Records tab — observe all four column headers (MRF ID, Date Needed, MRF Status, Procurement Status). Click Procurement Status."
    expected: "Rows reorder by procurement process flow (Pending Procurement/Pending first ascending, Delivered/Processed first descending). Sort indicator turns blue with correct arrow."
    why_human: "Requires live Firestore data to observe row reordering. Visual arrow rendering (U+2191/U+2193/U+21C5) and blue color (#1a73e8) need browser confirmation."
  - test: "Sort by Procurement Status while data has MRFs with multiple POs at different statuses."
    expected: "Each such MRF sorts by its least-progressed PO status (bottleneck), not the most advanced."
    why_human: "Bottleneck aggregation is per-page-render — requires live data with mixed-status POs to observe correct ordering."
  - test: "Navigate through multiple pages in Records tab, then sort by Procurement Status."
    expected: "MRFs that appear on earlier pages (and thus have _procurement_status set) sort correctly; MRFs on unvisited pages sort to end."
    why_human: "Page-visit-dependent store-back behavior requires multi-page navigation observation."
---

# Phase 47.1: Add Sortable Headers to Procurement Records Tab — Verification Report

**Phase Goal:** Procurement Records table has sortable column headers (MRF ID, Date Needed, MRF Status, Procurement Status) so users can reorder records by key fields
**Verified:** 2026-03-01T07:30:00Z
**Status:** PASSED
**Re-verification:** Yes — after gap closure plan 47.1-02 (Procurement Status sortable column)

---

## Goal Achievement

### Observable Truths (from 47.1-02-PLAN.md must_haves)

| #   | Truth                                                                                                                           | Status     | Evidence                                                                                              |
|-----|---------------------------------------------------------------------------------------------------------------------------------|------------|-------------------------------------------------------------------------------------------------------|
| 1   | Procurement user can click the Procurement Status column header and rows reorder by procurement status                          | VERIFIED   | `onclick="window.sortPRPORecords('procurement_status')"` on `<th>` at line 2940                       |
| 2   | Procurement Status sort uses process-flow ordinal order (Pending Procurement < Procuring < Procured < Delivered; Pending < Processing < Processed), NOT alphabetical | VERIFIED   | `PROCUREMENT_STATUS_ORDER` constant at lines 62-72 assigns ordinals 1-4 (Material) and 1-3 (SUBCON); `sortPRPORecords()` reads ordinals via `PROCUREMENT_STATUS_ORDER[aVal]` at lines 2441-2443 |
| 3   | MRFs with multiple POs are sorted by the least-progressed (bottleneck) PO status                                               | VERIFIED   | `renderPRPORecords()` at lines 2815-2827 iterates `poDataArray`, finds `minOrdinal` and stores `mrf._procurement_status = summaryStatus` using `<` comparison (minimum ordinal = least progressed) |
| 4   | MRFs with no POs (Transport type or no POs yet) sort to the end in ascending order                                             | VERIFIED   | `mrf._procurement_status = ''` set at line 2829 when `poDataArray.length === 0`; `sortPRPORecords()` checks `if (!aVal)` to push empty values to end at lines 2439-2440 |
| 5   | Clicking the Procurement Status header toggles between ascending and descending order                                           | VERIFIED   | `sortPRPORecords()` at lines 2423-2425 checks `prpoSortColumn === column` and toggles `prpoSortDirection`; same logic for `'procurement_status'` as all other columns |
| 6   | Sort indicator shows arrow direction on Procurement Status when active                                                          | VERIFIED   | `getPRPOSortIndicator('procurement_status')` embedded in the `<th>` template at line 2941; returns blue `↑`/`↓` when `prpoSortColumn === 'procurement_status'`, gray `⇅` otherwise |
| 7   | All other existing sortable columns (MRF ID, Date Needed, MRF Status) continue to work correctly                               | VERIFIED   | `onclick="window.sortPRPORecords('mrf_id')"` at line 2928, `'date_needed'` at line 2932, `'status'` at line 2937 — all unchanged; general `localeCompare` branch in `sortPRPORecords()` still handles these columns (lines 2447-2449) |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact                   | Expected                                                                                                           | Status   | Details                                                                                                                                                                                                                    |
|----------------------------|--------------------------------------------------------------------------------------------------------------------|----------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `app/views/procurement.js` | PROCUREMENT_STATUS_ORDER map, _procurement_status store-back in renderPRPORecords, custom ordinal sort branch in sortPRPORecords, sortable Procurement Status header | VERIFIED | `PROCUREMENT_STATUS_ORDER` at lines 62-72; `_procurement_status` store-back at lines 2813-2830; custom sort branch in `sortPRPORecords()` at lines 2434-2444; custom sort branch in `loadPRPORecords()` at lines 2293-2302; sortable `<th>` at lines 2940-2942 |

**Artifact depth check:**

- **Exists:** Yes — single file modified as planned
- **Substantive:** Yes — all four required symbols present and non-trivial: `PROCUREMENT_STATUS_ORDER` is a 7-entry map with correct ordinals; `_procurement_status` store-back uses min-ordinal bottleneck loop; sort branch uses ordinal lookup with null-to-end handling; `<th>` has onclick, cursor:pointer, user-select:none, and sort indicator
- **Wired:** Yes — `window.sortPRPORecords = sortPRPORecords` registered at line 121 in `attachWindowFunctions()`; `delete window.sortPRPORecords` cleanup at line 626 in `destroy()`

---

### Key Link Verification

| From                                         | To                                         | Via                                                                              | Status  | Details                                                                                                       |
|----------------------------------------------|--------------------------------------------|----------------------------------------------------------------------------------|---------|---------------------------------------------------------------------------------------------------------------|
| `renderPRPORecords()` per-row PO resolution  | `mrf._procurement_status` store-back       | After `poDataArray` built per row, min-ordinal loop writes `mrf._procurement_status` | WIRED   | Lines 2815-2830: `for (const po of poDataArray)` loop finds minimum ordinal PO; `mrf._procurement_status = summaryStatus` or `''` written on same object reference as in `allPRPORecords` |
| `renderPRPORecords() <th onclick>`           | `window.sortPRPORecords('procurement_status')` | onclick attribute on Procurement Status `<th>` element                           | WIRED   | Line 2940: `onclick="window.sortPRPORecords('procurement_status')"` in generated thead template               |
| `sortPRPORecords()`                          | `PROCUREMENT_STATUS_ORDER`                 | When column === 'procurement_status', ordinal map used instead of localeCompare  | WIRED   | Lines 2434-2443: `if (column === 'procurement_status')` branch reads `a._procurement_status`, does `PROCUREMENT_STATUS_ORDER[aVal] || 99` lookup |
| `loadPRPORecords()` sort block               | `PROCUREMENT_STATUS_ORDER`                 | When prpoSortColumn === 'procurement_status', same ordinal branch applies         | WIRED   | Lines 2293-2302: identical branch in the post-fetch sort in `loadPRPORecords()` for sort persistence on Refresh |

---

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                          | Status    | Evidence                                                                                                                                         |
|-------------|-------------|------------------------------------------------------------------------------------------------------|-----------|--------------------------------------------------------------------------------------------------------------------------------------------------|
| SORT-03     | 47.1-01, 47.1-02 | Procurement user can sort the MRF Records table by clicking column headers (MRF ID, Date Needed, MRF Status) | SATISFIED | All four headers (including Procurement Status added via plan-02) have `onclick="window.sortPRPORecords('...')"` with working sort function, state management, and visual indicators |

**Note on SORT-03 scope:** The REQUIREMENTS.md description for SORT-03 lists three columns (MRF ID, Date Needed, MRF Status). Plan 47.1-02 extended the implementation to add a fourth column (Procurement Status) in response to UAT feedback. The requirement is satisfied and then exceeded — the four-column implementation is a superset of the stated requirement.

**Orphaned requirements check:** SORT-03 is the only requirement mapped to Phase 47.1 in REQUIREMENTS.md traceability table (line 121). No other requirement IDs appear in either plan's frontmatter. No orphans.

---

### Commit Verification

| Hash      | Message                                                                 | Status   |
|-----------|-------------------------------------------------------------------------|----------|
| `3b3d578` | feat(47.1-01): add sort state, sort function, indicator helper, window registration | VERIFIED — present in git log |
| `75514e7` | feat(47.1-01): replace static thead with sortable headers and indicators in renderPRPORecords | VERIFIED — present in git log |
| `cdec518` | feat(47.1-02): add sortable Procurement Status column to MRF Records table | VERIFIED — present in git log; diff shows 58 insertions, 1 deletion in `app/views/procurement.js` |

---

### Anti-Patterns Found

| File                       | Line | Pattern                          | Severity | Impact                                                                           |
|----------------------------|------|----------------------------------|----------|----------------------------------------------------------------------------------|
| `app/views/procurement.js` | 3011 | `// Placeholder stubs for...`    | Info     | Section divider comment — the PR & TR Generation functions below it are fully implemented. Not an implementation stub. No action needed. |

No blockers. No warnings.

---

### Human Verification Required

#### 1. Procurement Status sort visual and row reordering

**Test:** Navigate to Procurement > Records tab. Observe all four column headers. Click "Procurement Status".
**Expected:** Rows reorder by process flow (Pending Procurement/Pending first in ascending order, Delivered/Processed first in descending). Active sort indicator turns blue with correct directional arrow. Clicking again reverses order.
**Why human:** Live Firestore data required to observe row reordering. Visual rendering of Unicode arrows and color (#1a73e8 blue, #94a3b8 gray) needs browser confirmation.

#### 2. Bottleneck aggregation for multi-PO MRFs

**Test:** Find an MRF that has multiple POs at different procurement statuses (e.g., one "Procured", one "Procuring"). Sort the Records tab by Procurement Status ascending.
**Expected:** That MRF sorts as if its status is "Procuring" (the least-progressed PO), not "Procured".
**Why human:** Requires live data with specific multi-PO scenarios that cannot be verified statically.

#### 3. Transport MRFs sort to end

**Test:** Sort by Procurement Status ascending with Transport-type MRFs present in the table.
**Expected:** Transport MRFs (which have no POs) appear at the bottom of the sorted list.
**Why human:** Requires live data with Transport MRFs visible in the current page.

#### 4. Sort persistence across Refresh

**Test:** Sort by Procurement Status, then click the Refresh button.
**Expected:** Sort remains on Procurement Status; rows do not reset to Date Needed order.
**Why human:** Requires observing stateful behavior across a button click in a live session.

#### 5. Existing three columns unaffected

**Test:** After sorting by Procurement Status, click MRF ID, Date Needed, and MRF Status headers in turn.
**Expected:** Each click correctly sorts by the clicked column; sort indicator moves to the new active column.
**Why human:** Interaction sequencing (sort-switching behavior) requires live browser session.

---

### Gaps Summary

No gaps. All 7 observable truths from the 47.1-02-PLAN.md must_haves are verified in code:

- `PROCUREMENT_STATUS_ORDER` constant (lines 62-72) has the correct 7-entry map matching process-flow dropdown order
- `renderPRPORecords()` bottleneck store-back (lines 2815-2830) correctly identifies minimum-ordinal PO status per MRF row and writes it as `mrf._procurement_status`
- `sortPRPORecords()` (lines 2423-2454) has the custom `procurement_status` branch with ordinal lookup, empty-to-end handling, and correct toggle logic
- `loadPRPORecords()` sort block (lines 2288-2310) has the same `procurement_status` branch for sort persistence on Refresh
- The Procurement Status `<th>` (lines 2940-2942) has `onclick`, `cursor: pointer`, `user-select: none`, and `getPRPOSortIndicator('procurement_status')` embedded
- All three original sortable columns (MRF ID, Date Needed, MRF Status) retain their sortable headers and their general sort path unchanged
- `window.sortPRPORecords` registered at line 121 and deleted at line 626 — correct lifecycle management

The previous VERIFICATION.md reported 7/7 for the original three sortable columns (plan 47.1-01). This re-verification confirms plan 47.1-02 successfully closed the UAT gap (Procurement Status not sortable) without regressing any of the original seven passing truths. Phase 47.1 goal is fully achieved.

---

_Verified: 2026-03-01T07:30:00Z_
_Verifier: Claude (gsd-verifier)_
