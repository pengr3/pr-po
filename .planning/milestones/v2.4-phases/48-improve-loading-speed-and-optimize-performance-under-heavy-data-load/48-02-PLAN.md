---
phase: 48-improve-loading-speed-and-optimize-performance-under-heavy-data-load
plan: 02
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - app/views/home.js
  - app/views/procurement.js
  - app/views/finance.js
autonomous: true
requirements:
  - PERF-03
  - PERF-04

must_haves:
  truths:
    - "Home dashboard shows last-known stat values immediately on revisit, with a subtle refreshing indicator until fresh data arrives"
    - "Procurement view init() loads reference data (projects, services, suppliers) in parallel rather than sequentially"
    - "Skeleton rows appear in procurement and finance table bodies before real data loads"
  artifacts:
    - path: "app/views/home.js"
      provides: "Stale-while-revalidate for dashboard stats with cachedStats persistence across destroy/init"
      contains: "cachedStats"
    - path: "app/views/procurement.js"
      provides: "Parallel init with Promise.all + skeleton rows in render()"
      contains: "Promise.all"
    - path: "app/views/finance.js"
      provides: "Skeleton rows in render() table bodies"
      contains: "skeletonTableRows"
  key_links:
    - from: "app/views/home.js"
      to: "styles/components.css"
      via: "stat-refreshing CSS class toggled on stat values"
      pattern: "stat-refreshing"
    - from: "app/views/procurement.js"
      to: "app/components.js"
      via: "import { skeletonTableRows }"
      pattern: "skeletonTableRows"
    - from: "app/views/finance.js"
      to: "app/components.js"
      via: "import { skeletonTableRows }"
      pattern: "skeletonTableRows"
---

<objective>
Add skeleton screens to the three heaviest views (procurement, finance, home) and optimize data loading with parallel init and stale-while-revalidate.

Purpose: These three views load the most data and are visited most frequently. Skeleton screens eliminate blank-table flash. Parallel init reduces procurement cold-load time by ~60%. Stale-while-revalidate makes home dashboard feel instant on return visits.

Output: Modified home.js, procurement.js, finance.js with skeleton loading states and optimized init patterns.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/48-improve-loading-speed-and-optimize-performance-under-heavy-data-load/48-CONTEXT.md
@.planning/phases/48-improve-loading-speed-and-optimize-performance-under-heavy-data-load/48-RESEARCH.md
@.planning/phases/48-improve-loading-speed-and-optimize-performance-under-heavy-data-load/48-01-SUMMARY.md
@app/views/home.js
@app/views/procurement.js
@app/views/finance.js
@app/components.js

<interfaces>
<!-- From Plan 01 (must be complete before this plan runs) -->

From app/components.js (added by Plan 01):
```javascript
export function skeletonTableRows(cols = 6, rows = 5) {
    // Returns HTML string of skeleton placeholder rows for tables
}
```

From styles/components.css (added by Plan 01):
- .skeleton — base class with shimmer animation
- .skeleton-row — 40px height table row placeholder
- .skeleton-stat — 28px inline stat placeholder
- .stat-refreshing — subtle opacity pulse for stale values

<!-- Existing patterns in target files -->

From app/views/home.js:
- `let statsListeners = [];` — listener array, cleaned in destroy()
- `let stats = { activeMRFs: 0, pendingPRs: 0, activePOs: 0, activeServices: 0, servicesMRFs: 0 };` — reset in destroy()
- `function updateStatDisplay(elementId, value)` — sets element.textContent
- `function loadStats(mode)` — sets up 3-5 onSnapshot listeners based on mode
- `export async function destroy()` — unsubscribes listeners AND resets stats to 0

From app/views/procurement.js:
- `export async function init(activeTab = 'mrfs')` — sequential: loadProjects → loadServicesForNewMRF → loadSuppliers → loadMRFs
- `export function render(activeTab = 'mrfs')` — returns HTML with multiple tab-specific table bodies
- Key tbody IDs: `mrfListBody`, `suppliersTableBody`, `lineItemsBody`, `poTrackingBody`
- Tab-specific tbody IDs in render(): `mrfListBody` (mrfs tab), `suppliersTableBody` (suppliers tab)
- Records tab tbody is rendered by mrf-records.js controller (handled in Plan 03)

From app/views/finance.js:
- `export function render(activeTab = 'approvals')` — returns HTML with table bodies for 3 tabs
- Key tbody IDs: `materialPRBody`, `transportRequestsBody`, `poTableBody`
- `export async function init(activeTab = 'approvals')` — sets up onSnapshot listeners per tab
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stale-while-revalidate for home dashboard + skeleton stats</name>
  <files>app/views/home.js</files>
  <action>
Modify home.js to implement stale-while-revalidate for dashboard stats per locked decision.

1. **Rename the module-level `stats` object to `cachedStats`** and change its initial values from `0` to `null`:
```javascript
let cachedStats = {
    activeMRFs: null,
    pendingPRs: null,
    activePOs: null,
    activeServices: null,
    servicesMRFs: null
};
```

2. **Update render()** to show cached values immediately if available, otherwise show skeleton:
- In `projectsStatsHtml()`: Change the stat-value span content from `${stats.activeMRFs}` to: if `cachedStats.activeMRFs !== null`, show the cached number; otherwise show `<span class="skeleton skeleton-stat"></span>`. Same for pendingPRs, activePOs.
- In `servicesStatsHtml()`: Same pattern for activeServices, servicesMRFs.
- Example for one stat: `<span class="stat-value" id="stat-mrfs">${cachedStats.activeMRFs !== null ? cachedStats.activeMRFs : '<span class="skeleton skeleton-stat"></span>'}</span>`

3. **Update init()** to add refreshing indicator when showing stale data:
- After calling `loadStats(mode)`, if `cachedStats.activeMRFs !== null` (meaning we have stale data showing), add `stat-refreshing` class to all `.stat-value` elements:
```javascript
if (cachedStats.activeMRFs !== null || cachedStats.activeServices !== null) {
    document.querySelectorAll('.stat-value').forEach(el => el.classList.add('stat-refreshing'));
}
```

4. **Update updateStatDisplay()** to also remove the refreshing indicator and clear any skeleton:
```javascript
function updateStatDisplay(elementId, value) {
    const element = document.getElementById(elementId);
    if (element) {
        element.textContent = value;
        element.classList.remove('loading');
        element.classList.remove('stat-refreshing');
    }
}
```

5. **Update all onSnapshot callbacks in loadStats()** to write to `cachedStats` instead of `stats`:
- Replace all `stats.activeMRFs = ...` with `cachedStats.activeMRFs = ...` (and same for all 5 stat fields).

6. **Update destroy()** to NOT reset cachedStats:
- Remove the stats reset block (`stats = { activeMRFs: 0, ... }` or equivalent)
- Keep the listener cleanup: `statsListeners.forEach(...)` and `statsListeners = [];`
- This is the key difference — cached values survive destroy/init cycles for stale-while-revalidate.

All references to the old `stats` variable must be changed to `cachedStats`. Search the file for any remaining `stats.` references and update them.
  </action>
  <verify>
    <automated>cd C:/Users/franc/dev/projects/pr-po && grep -c "cachedStats" app/views/home.js && grep -c "stat-refreshing" app/views/home.js && grep -c "skeleton-stat" app/views/home.js && ! grep "stats\.activeMRFs" app/views/home.js</automated>
  </verify>
  <done>Home dashboard shows cached stat values immediately on revisit (no "0" flash). Skeleton stat placeholders shown on first-ever visit. stat-refreshing class applied while waiting for fresh data. destroy() no longer resets cached values.</done>
</task>

<task type="auto">
  <name>Task 2: Skeleton screens + parallel init for procurement.js and finance.js</name>
  <files>app/views/procurement.js, app/views/finance.js</files>
  <action>
**procurement.js changes:**

1. Add import at top: `import { skeletonTableRows } from '../components.js';`
   (Add to existing import line if components.js is already imported, or add new import)

2. In `render()`, find the `<tbody id="mrfListBody">` and replace its empty content (or any existing placeholder content) with skeleton rows:
   `<tbody id="mrfListBody">${skeletonTableRows(7, 5)}</tbody>`
   Do the same for `<tbody id="suppliersTableBody">`: `${skeletonTableRows(5, 5)}`
   NOTE: The PO tracking table body (`poTrackingBody`) may not exist in render() directly (it is rendered dynamically) — check if it exists in the HTML template and add skeleton if present. If not present, skip it.

3. In `init()`, replace the sequential awaits with parallel execution:
```javascript
// Reference data is independent — run in parallel
await Promise.all([
    loadProjects(),
    loadServicesForNewMRF(),
    loadSuppliers()
]);
// MRF list can start after reference data is ready (dropdown population)
await loadMRFs();
```
Keep the existing `if (activeTab === 'records')` block as-is after the parallel block.

4. Tab-switch handling: The render() function is called on every tab switch. When switching back to a tab where data is already loaded, the skeleton briefly shows before onSnapshot re-fires. This is acceptable because with persistence enabled (Plan 01), the onSnapshot callback fires from IndexedDB cache in <50ms, making the skeleton flash imperceptible.

**finance.js changes:**

1. Add import at top: `import { skeletonTableRows } from '../components.js';`

2. In `render()`, find the tbody elements and add skeleton rows:
   - `<tbody id="materialPRBody">${skeletonTableRows(8, 5)}</tbody>`
   - `<tbody id="transportRequestsBody">${skeletonTableRows(7, 5)}</tbody>`
   - `<tbody id="poTableBody">${skeletonTableRows(8, 5)}</tbody>`
   Adjust the column count (first argument) to match the actual number of columns in each table's thead.

3. Finance init() already sets up onSnapshot listeners which will replace the skeleton content when data arrives. No structural changes needed to init() — the onSnapshot callbacks already set innerHTML on the tbody elements.

IMPORTANT for both files: Count the actual number of `<th>` columns in each table's thead to set the correct `cols` parameter for skeletonTableRows(). The colspan must match the column count for proper visual alignment.
  </action>
  <verify>
    <automated>cd C:/Users/franc/dev/projects/pr-po && grep -c "skeletonTableRows" app/views/procurement.js && grep -c "Promise.all" app/views/procurement.js && grep -c "skeletonTableRows" app/views/finance.js</automated>
  </verify>
  <done>Procurement and finance views show skeleton rows in tables before data arrives. Procurement init() loads reference data in parallel (Promise.all). No blank-table flash on any tab in these views.</done>
</task>

</tasks>

<verification>
1. Navigate to Home → stats show immediately (cached values or skeleton on first visit)
2. Navigate away from Home → navigate back → stats appear instantly (stale-while-revalidate, no "0" flash)
3. Navigate to Procurement > MRFs tab → skeleton rows visible briefly before MRF list loads
4. Navigate to Finance > Pending Approvals → skeleton rows visible briefly before PR/TR data loads
5. Switch tabs within Procurement → no blank tables (skeleton fills, then data replaces quickly)
6. DevTools Console → no JavaScript errors
7. DevTools Network → throttle to "Slow 3G" → skeleton screens clearly visible during load
</verification>

<success_criteria>
- Home dashboard shows cached stat values on return visits without resetting to 0
- Procurement init() uses Promise.all for parallel reference data loading
- All table bodies in procurement.js and finance.js show skeleton rows before data arrives
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/48-improve-loading-speed-and-optimize-performance-under-heavy-data-load/48-02-SUMMARY.md`
</output>
