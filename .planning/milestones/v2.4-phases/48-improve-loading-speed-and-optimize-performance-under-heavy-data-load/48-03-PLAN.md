---
phase: 48-improve-loading-speed-and-optimize-performance-under-heavy-data-load
plan: 03
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - app/views/projects.js
  - app/views/services.js
  - app/views/clients.js
  - app/views/mrf-form.js
  - app/views/mrf-records.js
  - app/views/assignments.js
  - app/views/user-management.js
  - app/views/role-config.js
autonomous: true
requirements:
  - PERF-02
  - PERF-05

must_haves:
  truths:
    - "All remaining data-loading views (Projects, Services, Clients, MRF Form, MRF Records, Assignments, User Management, Role Config) show skeleton rows before data arrives"
    - "Reference data (suppliers, projects list) loaded via getDocs in multiple views benefits from in-memory TTL cache, reducing redundant Firestore reads on tab switches"
  artifacts:
    - path: "app/views/projects.js"
      provides: "Skeleton rows in project list table"
      contains: "skeletonTableRows"
    - path: "app/views/services.js"
      provides: "Skeleton rows in services list table"
      contains: "skeletonTableRows"
    - path: "app/views/clients.js"
      provides: "Skeleton rows in clients list table"
      contains: "skeletonTableRows"
    - path: "app/views/mrf-form.js"
      provides: "Skeleton rows in My Requests table"
      contains: "skeletonTableRows"
    - path: "app/views/mrf-records.js"
      provides: "Skeleton rows in MRF Records table via controller"
      contains: "skeletonTableRows"
    - path: "app/views/assignments.js"
      provides: "Skeleton rows in assignments table"
      contains: "skeletonTableRows"
    - path: "app/views/user-management.js"
      provides: "Skeleton rows in user management table"
      contains: "skeletonTableRows"
    - path: "app/views/role-config.js"
      provides: "Skeleton rows in role config table"
      contains: "skeletonTableRows"
  key_links:
    - from: "all modified views"
      to: "app/components.js"
      via: "import { skeletonTableRows }"
      pattern: "skeletonTableRows"
---

<objective>
Add skeleton screens to all remaining data-loading views and add in-memory reference data caching.

Purpose: Uniform skeleton loading experience across the entire app (locked decision: "apply uniformly across all views"). In-memory cache reduces redundant getDocs calls during tab switching, especially for suppliers and project lists that rarely change.

Output: All views show skeleton loading states. Reference data has 5-minute in-memory TTL cache.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/48-improve-loading-speed-and-optimize-performance-under-heavy-data-load/48-CONTEXT.md
@.planning/phases/48-improve-loading-speed-and-optimize-performance-under-heavy-data-load/48-RESEARCH.md
@.planning/phases/48-improve-loading-speed-and-optimize-performance-under-heavy-data-load/48-01-SUMMARY.md
@app/views/projects.js
@app/views/services.js
@app/views/clients.js
@app/views/mrf-form.js
@app/views/mrf-records.js
@app/views/assignments.js
@app/views/user-management.js
@app/views/role-config.js
@app/components.js

<interfaces>
<!-- From Plan 01 (must be complete before this plan runs) -->

From app/components.js (added by Plan 01):
```javascript
export function skeletonTableRows(cols = 6, rows = 5) {
    // Returns HTML string of skeleton placeholder rows for tables
}
```

From styles/components.css (added by Plan 01):
- .skeleton — base class with shimmer animation
- .skeleton-row — 40px height table row placeholder

<!-- Key patterns in target files -->

Views that need skeleton:
- projects.js: render() has `<tbody id="projectsTableBody">` — loaded by onSnapshot in init()
- services.js: render() has `<tbody id="servicesTableBody">` — loaded by onSnapshot in init()
- clients.js: render() has a table tbody — loaded by onSnapshot in init()
- mrf-form.js: render() has "My Requests" tab with table — uses mrf-records controller
- mrf-records.js: createMRFRecordsController() returns { render(), init(), destroy() } — render() has table tbody
- assignments.js: render() has assignments table — loaded by onSnapshot
- user-management.js: render() has user table — loaded by onSnapshot
- role-config.js: render() has role table — loaded by getDocs

All views follow the pattern: render() returns HTML → init() sets up onSnapshot → callback replaces tbody innerHTML
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skeleton screens to projects, services, clients, and mrf-form views</name>
  <files>app/views/projects.js, app/views/services.js, app/views/clients.js, app/views/mrf-form.js</files>
  <action>
For each of the four files, add skeleton table rows to the render() function's tbody elements.

**projects.js:**
1. Add import: `import { skeletonTableRows } from '../components.js';`
   (Add to existing imports from components.js if present, or add new import line)
2. In render(), find the projects table tbody (likely `<tbody id="projectsTableBody">`) and replace empty content with `${skeletonTableRows(N, 5)}` where N is the number of columns in the thead. Count the `<th>` elements to determine N.

**services.js:**
1. Add import: `import { skeletonTableRows } from '../components.js';`
2. In render(), find the services table tbody (likely `<tbody id="servicesTableBody">`) and add `${skeletonTableRows(N, 5)}` with correct column count.

**clients.js:**
1. Add import: `import { skeletonTableRows } from '../components.js';`
2. In render(), find the clients table tbody and add `${skeletonTableRows(N, 5)}` with correct column count.

**mrf-form.js:**
1. Add import: `import { skeletonTableRows } from '../components.js';`
2. In render(), the "My Requests" tab has a table populated by the mrf-records controller. Find the tbody in that table and add `${skeletonTableRows(N, 5)}`.
3. The MRF submission form tab itself does NOT need skeleton — it's a form, not data-loading.

IMPORTANT: For each file, read the render() function to find the exact tbody element ID and count the thead columns accurately. Do not guess column counts.

IMPORTANT: If a file already imports from '../components.js', add skeletonTableRows to the existing import destructure rather than adding a duplicate import line.
  </action>
  <verify>
    <automated>cd C:/Users/franc/dev/projects/pr-po && grep -c "skeletonTableRows" app/views/projects.js && grep -c "skeletonTableRows" app/views/services.js && grep -c "skeletonTableRows" app/views/clients.js && grep -c "skeletonTableRows" app/views/mrf-form.js</automated>
  </verify>
  <done>Projects, services, clients, and MRF form views all show skeleton rows in their table bodies before data arrives. Each skeleton has the correct column span matching the table's thead.</done>
</task>

<task type="auto">
  <name>Task 2: Add skeleton screens to mrf-records, assignments, user-management, and role-config views</name>
  <files>app/views/mrf-records.js, app/views/assignments.js, app/views/user-management.js, app/views/role-config.js</files>
  <action>
For each of the four files, add skeleton table rows to their render functions.

**mrf-records.js:**
1. Add import: `import { skeletonTableRows } from '../components.js';`
2. The `createMRFRecordsController()` factory function returns an object with `render()`. Inside that render function, find the table tbody and add skeleton rows with the correct column count.
3. Note: This controller is used in both mrf-form.js (My Requests tab) and procurement.js (Records tab). The skeleton in the controller's render() covers both usage sites.

**assignments.js:**
1. Add import: `import { skeletonTableRows } from '../components.js';`
2. In render(), find the assignments table tbody and add skeleton rows.

**user-management.js:**
1. Add import: `import { skeletonTableRows } from '../components.js';`
2. In render(), find the user table tbody and add skeleton rows.

**role-config.js:**
1. Add import: `import { skeletonTableRows } from '../components.js';`
2. In render(), find the role/permissions table tbody and add skeleton rows.

IMPORTANT: For each file, read the render() function first to locate the exact tbody element and count thead columns. The skeleton colspan must match.

IMPORTANT: If a file already imports from '../components.js', add skeletonTableRows to the existing import destructure.

NOTE: The admin views (assignments, user-management, role-config) are less frequently visited but the locked decision says "apply uniformly across all views" — so they all get skeleton screens.
  </action>
  <verify>
    <automated>cd C:/Users/franc/dev/projects/pr-po && grep -c "skeletonTableRows" app/views/mrf-records.js && grep -c "skeletonTableRows" app/views/assignments.js && grep -c "skeletonTableRows" app/views/user-management.js && grep -c "skeletonTableRows" app/views/role-config.js</automated>
  </verify>
  <done>MRF Records controller, assignments, user management, and role config views all show skeleton rows before data loads. Every data-loading view in the app now has skeleton screens.</done>
</task>

</tasks>

<verification>
1. Navigate to Projects tab → skeleton rows visible briefly before project list loads
2. Navigate to Services tab → skeleton rows visible briefly before services list loads
3. Navigate to Clients tab → skeleton rows visible briefly before client list loads
4. Navigate to Material Request > My Requests → skeleton rows visible
5. Navigate to Procurement > Records → skeleton rows visible (from mrf-records controller)
6. Navigate to Admin > Assignments → skeleton rows visible
7. Navigate to Admin > User Management → skeleton rows visible
8. Navigate to Admin > Role Config → skeleton rows visible
9. DevTools Console → no JavaScript errors on any view
10. DevTools Network → throttle to "Slow 3G" → skeleton screens clearly visible on all views
</verification>

<success_criteria>
- Every data-loading view in the app shows skeleton rows before real data arrives
- No blank-table flash on any view or tab
- No JavaScript errors introduced
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/48-improve-loading-speed-and-optimize-performance-under-heavy-data-load/48-03-SUMMARY.md`
</output>
