---
phase: 45-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/register.js
autonomous: true
requirements:
  - BRD-01

must_haves:
  truths:
    - "Registration page displays the CLMC company logo image, not the 'CL' blue box"
    - "If the image fails to load, the placeholder is silently hidden (no broken image icon)"
  artifacts:
    - path: "app/views/register.js"
      provides: "Registration page render() with <img> logo replacing div placeholder"
      contains: "CLMC Registered Logo"
  key_links:
    - from: "app/views/register.js render()"
      to: "./CLMC Registered Logo Cropped (black fill).png"
      via: "<img src> attribute"
      pattern: "img.*CLMC"
---

<objective>
Replace the "CL" text placeholder on the registration page with the actual CLMC company logo PNG image.

Purpose: BRD-01 — the registration page is the first thing new users see when joining; the "CL" blue box is a developer placeholder, not a brand element.
Output: register.js render() uses `<img src="./CLMC Registered Logo Cropped (black fill).png">` inside `.auth-logo`, matching the pattern already used in pending.js. No CSS changes needed — `.auth-logo img` rule already exists in views.css.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45-visual-polish/45-RESEARCH.md
</context>

<interfaces>
<!-- Pattern already working in pending.js (verified by research): -->
```html
<div class="auth-logo">
    <img src="https://raw.githubusercontent.com/pengr3/pr-po/main/CLMC%20Registered%20Logo.png"
         alt="CLMC Logo"
         onerror="this.style.display='none'">
</div>
```

<!-- Preferred variant using local path (avoids GitHub cross-origin dependency): -->
```html
<div class="auth-logo">
    <img src="./CLMC Registered Logo Cropped (black fill).png"
         alt="CLMC Logo"
         onerror="this.style.display='none'">
</div>
```

<!-- CSS already defined in styles/views.css line 905 (no CSS change needed): -->
```css
.auth-logo img {
    max-width: 120px;
    height: auto;
    display: inline-block;
}
```

<!-- Current register.js placeholder at lines 32-36 to replace: -->
```html
<div class="auth-logo">
    <div style="width: 60px; height: 60px; background: var(--primary); border-radius: 12px; display: flex; align-items: center; justify-content: center; margin: 0 auto;">
        <span style="color: white; font-size: 24px; font-weight: 700;">CL</span>
    </div>
</div>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace CL placeholder with logo img in register.js</name>
  <files>app/views/register.js</files>
  <action>
In `app/views/register.js`, locate the `render()` function. Find the `.auth-logo` div that contains the blue "CL" placeholder (the `<div style="width: 60px...">` with the `<span>CL</span>` inside). Replace the entire inner placeholder div with:

```html
<img src="./CLMC Registered Logo Cropped (black fill).png"
     alt="CLMC Logo"
     onerror="this.style.display='none'">
```

The `.auth-logo` wrapper div stays — only the content inside changes. The `onerror` fallback silently hides the image if the file cannot be found, preventing a broken-image icon.

Do NOT modify login.js — BRD-01 scope is registration page only.
Do NOT add any new CSS — `.auth-logo img` is already defined in views.css.
  </action>
  <verify>Open http://localhost:8000/#/register in a browser. The registration page must display the CLMC logo image instead of a blue box with "CL". The image should appear centered above the "Create Account" heading.</verify>
  <done>register.js render() contains `<img src="./CLMC Registered Logo Cropped (black fill).png"` inside `.auth-logo`; the blue placeholder div is gone.</done>
</task>

</tasks>

<verification>
Open browser to http://localhost:8000/#/register — CLMC logo image is visible above "Create Account" heading. No blue "CL" box. If image were missing, no broken-image icon appears (onerror fires).
</verification>

<success_criteria>
- Registration page shows company logo PNG, not the "CL" blue box
- `app/views/register.js` contains `img src="./CLMC Registered Logo Cropped (black fill).png"` inside `.auth-logo`
- No changes made to login.js, views.css, or components.css in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/45-visual-polish/45-01-SUMMARY.md`
</output>
