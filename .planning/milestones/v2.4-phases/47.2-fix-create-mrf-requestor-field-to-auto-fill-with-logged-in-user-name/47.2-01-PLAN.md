---
phase: 47.2-fix-create-mrf-requestor-field-to-auto-fill-with-logged-in-user-name
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/procurement.js
autonomous: true
requirements:
  - MRF-02

must_haves:
  truths:
    - "When a logged-in user clicks Create MRF in Procurement, the Requestor Name field is pre-filled with their full name"
    - "The Requestor Name field remains editable so procurement staff can override it when creating MRFs on behalf of others"
    - "If getCurrentUser() returns null (edge case), the field falls back to empty string with no error"
  artifacts:
    - path: "app/views/procurement.js"
      provides: "Pre-populated requestor_name in createNewMRF()"
      contains: "getCurrentUser"
  key_links:
    - from: "app/views/procurement.js createNewMRF()"
      to: "window.getCurrentUser()"
      via: "optional chaining on full_name property"
      pattern: "getCurrentUser.*full_name"
---

<objective>
Pre-fill the Requestor Name field in Procurement > Create MRF with the logged-in user's full name.

Purpose: Currently the Requestor Name field starts empty, forcing procurement staff to type their name every time. The standalone MRF form (`mrf-form.js`) already auto-fills this field. This plan brings parity to the Procurement view's Create MRF form.

Output: One-line change in `createNewMRF()` that reads `window.getCurrentUser()?.full_name` into the `currentMRF` object before `renderMRFDetails()` renders it.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@app/views/procurement.js (line 919-937 — createNewMRF function)
@app/views/mrf-form.js (lines 367-374 — reference implementation)

<interfaces>
<!-- getCurrentUser() contract from app/auth.js -->
window.getCurrentUser = getCurrentUser;
// Returns: { uid, email, full_name, role, status, ... } | null

<!-- createNewMRF() sets currentMRF object, then calls renderMRFDetails() -->
<!-- renderMRFDetails() interpolates: value="${mrf.requestor_name || ''}" at line 1051 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-fill requestor_name from getCurrentUser() in createNewMRF()</name>
  <files>app/views/procurement.js</files>
  <action>
In `app/views/procurement.js`, inside `createNewMRF()` (~line 925), change:

```javascript
requestor_name: '',
```

to:

```javascript
requestor_name: window.getCurrentUser?.()?.full_name || '',
```

This is the ONLY change needed. The downstream template in `renderMRFDetails()` already interpolates `${mrf.requestor_name || ''}` into the input's value attribute.

DO NOT:
- Add `readonly` to the input field (procurement staff must be able to override the name)
- Add any async calls or `await getDoc()` — `getCurrentUser()` is synchronous
- Modify `renderMRFDetails()` or any HTML templates
- Touch `mrf-form.js` (it already works correctly)
  </action>
  <verify>
    <automated>cd /c/Users/franc/dev/projects/pr-po && grep -n "requestor_name.*getCurrentUser" app/views/procurement.js | head -5</automated>
  </verify>
  <done>Line 925 of procurement.js reads `requestor_name: window.getCurrentUser?.()?.full_name || '',` — the Create MRF form pre-fills the Requestor Name with the logged-in user's name while remaining editable.</done>
</task>

</tasks>

<verification>
1. `grep -n "requestor_name.*getCurrentUser" app/views/procurement.js` returns a match at ~line 925
2. The matched line uses optional chaining (`?.`) and fallback (`|| ''`) for null safety
3. No `readonly` attribute added to the Requestor Name input in `renderMRFDetails()`
4. No changes to `mrf-form.js`
</verification>

<success_criteria>
- The `createNewMRF()` function sets `requestor_name` to the logged-in user's `full_name`
- The field falls back to empty string if user data is unavailable
- The Requestor Name input remains editable (no `readonly`)
- Only `procurement.js` is modified; no other files changed
</success_criteria>

<output>
After completion, create `.planning/phases/47.2-fix-create-mrf-requestor-field-to-auto-fill-with-logged-in-user-name/47.2-01-SUMMARY.md`
</output>
