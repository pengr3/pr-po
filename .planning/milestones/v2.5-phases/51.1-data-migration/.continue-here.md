---
phase: 51.1-data-migration
task: adding location field to projects and services
total_tasks: 6
status: in_progress
last_updated: 2026-03-01T12:55:12.375Z
---

<current_state>
Adding a "location" optional field to projects and services (both one-time & recurring). This must be done BEFORE Phase 51.1 data migration because the incoming Excel data has location info.

Started editing — only projects.js form field added so far. 5 more edits remain.
</current_state>

<completed_work>

## Phase 51 (Data Wipe) — COMPLETE
- Plan created, executed, UAT passed (5/5), verified, roadmap updated
- `scripts/wipe.js` — dry-run, typed WIPE confirmation, batched deletion of 10 collections

## Post-wipe bugfixes — COMMITTED
- `fdf9dbc` fix: assignments.js — cross-reference assignment counts against existing projects/services
- `cf1882b` fix: user-management.js — same fix for All Users table

## Phase 51.1 (Data Migration) — CONTEXT CAPTURED
- `a9a334b` docs(51.1): capture data migration phase context
- CONTEXT.md written with all decisions: CSV import, 3 collections (clients→projects→services), dry-run, fail-fast, cross-validate references

## Location field — IN PROGRESS (1/6 edits done)
- projects.js: form field added (optional text input after Project Name)
</completed_work>

<remaining_work>

## Location field edits needed (5 remaining):

1. **projects.js — addProject() function (~line 620)**: Read `#projectLocation` value, include `location` in the addDoc() call and recordEditHistory()

2. **services.js — render() form (~line 145)**: Add location form field (optional text input) after Service Type field. Use id="serviceLocation"

3. **services.js — addService() function (~line 654)**: Read `#serviceLocation` value, include `location` in the addDoc() call and recordEditHistory()

4. **project-detail.js — renderProjectDetail() (~line 291)**: Add editable location field in Card 1 grid (after Client, before Personnel). Pattern: `<input type="text" data-field="location" value="${escapeHTML(currentProject.location || '')}" onblur="window.saveField('location', this.value)">`

5. **service-detail.js — renderServiceDetail() (~line 346)**: Add editable location field in Card 1 grid (after Client, before Personnel). Pattern: `<input type="text" data-field="location" value="${escapeHTML(currentService.location || '')}" onblur="window.saveServiceField('location', this.value)">`

## After location field:
- Commit the location field changes
- `/gsd:plan-phase 51.1` — plan the data migration
- `/gsd:execute-phase 51.1` — execute it
</remaining_work>

<decisions_made>

- Location field is OPTIONAL — not required on project/service creation
- Location is a simple text input (not a dropdown) — free-form entry
- Location is editable post-creation in detail views (same inline edit pattern as project_name)
- Field name in Firestore: `location` (string, nullable)
- No validation needed — just a plain text field
</decisions_made>

<blockers>
None — straightforward field addition across 4 files
</blockers>

<context>
User wants location field added before data migration because the incoming Excel data has location info for projects and services. This is a prerequisite for Phase 51.1.

The pattern is consistent across all 4 files — optional text field, same inline-edit pattern as existing fields.
</context>

<next_action>
Start with: Complete the 5 remaining location field edits listed in remaining_work, then commit. After that, proceed to /gsd:plan-phase 51.1 for data migration.
</next_action>
