---
phase: 53.1-dev-firebase-setup
plan: 02
subsystem: infra
tags: [firebase, firestore, dev-environment, security-rules, indexes, firebaserc]

# Dependency graph
requires:
  - phase: 53.1-dev-firebase-setup plan 01
    provides: Dual Firebase config scaffold in app/firebase.js with devConfig placeholders
provides:
  - Real devConfig values in app/firebase.js (clmc-procurement-dev project connected)
  - Firestore security rules deployed to clmc-procurement-dev
  - Firestore composite indexes deployed to clmc-procurement-dev
  - .firebaserc dev alias for convenience deployment to dev project
affects: [all views using firebase.js, firestore.rules, firestore.indexes.json]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Firebase CLI -P flag for targeting non-default project without changing .firebaserc default
    - .firebaserc named aliases (dev/default) for multi-project workflows

key-files:
  created: []
  modified: [app/firebase.js, .firebaserc]

key-decisions:
  - ".firebaserc default project remains clmc-procurement (prod) — dev alias added for convenience only"
  - "Both firestore.rules and firestore.indexes.json deployed to dev project using --project flag, not by changing default"

patterns-established:
  - "Deploy to non-default project: firebase deploy --only firestore:rules --project clmc-procurement-dev"
  - "Named alias pattern: firebase use dev targets clmc-procurement-dev without risking accidental prod deploy"

requirements-completed: [DEV-01]

# Metrics
duration: 10min
completed: 2026-03-02
---

# Phase 53.1 Plan 02: Dev Firebase Setup Summary

**Firestore rules and indexes deployed to clmc-procurement-dev via Firebase CLI -P flag; devConfig wired to real Firebase project; .firebaserc dev alias added without touching prod default**

## Performance

- **Duration:** ~20 min (including human verification)
- **Started:** 2026-03-02T03:15:00Z
- **Completed:** 2026-03-02T11:45:00Z
- **Tasks:** 3 of 3 (all complete)
- **Files modified:** 2

## Accomplishments
- Verified `app/firebase.js` has no `FILL_IN_FROM_FIREBASE_CONSOLE` placeholder strings — devConfig has real values for `clmc-procurement-dev`
- Deployed `firestore.rules` to dev project: `firebase deploy --only firestore:rules --project clmc-procurement-dev` — "Deploy complete!"
- Deployed `firestore.indexes.json` to dev project: `firebase deploy --only firestore:indexes --project clmc-procurement-dev` — "Deploy complete!"
- Added `"dev": "clmc-procurement-dev"` alias to `.firebaserc` (default remains `clmc-procurement`)
- Verified `firebase use` still outputs `clmc-procurement` after .firebaserc edit
- End-to-end dev environment verified by user: yellow DEV ENVIRONMENT banner visible on localhost, login succeeds with dev Firebase credentials, `role_templates` seeded to dev Firestore via browser console, Firestore reads/writes succeed, Netlify prod deployment unaffected

## Task Commits

Each task was committed atomically:

1. **Task 1: Create dev Firebase project and fill in config values** — user action (no code commit, human-action checkpoint)
2. **Task 2: Deploy Firestore rules and indexes to the dev project** - `8b60eb2` (feat)
3. **Task 3: Verify dev environment works end-to-end on localhost** — user verification (approved)

**Plan metadata:** `c1065b2` (docs: complete dev Firebase deployment plan — updated after Task 3 approval)

## Files Created/Modified
- `app/firebase.js` — devConfig fields filled with real clmc-procurement-dev credentials (user action in Task 1)
- `.firebaserc` — Added `"dev": "clmc-procurement-dev"` alias; default project unchanged

## Decisions Made
- Used `--project clmc-procurement-dev` flag for both deploy commands to avoid modifying `.firebaserc` default
- Added `dev` alias to `.firebaserc` so future deployments can use `firebase deploy -P dev` shorthand
- Verified `firebase use` after each change to confirm prod default was never altered

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. Both `firebase deploy` commands succeeded on first attempt. Firebase CLI 15.5.1 already authenticated with correct permissions to the dev project.

## User Setup Required

None - all tasks complete including user-verified end-to-end validation.

Note: During Task 3 verification, the user seeded `role_templates` to dev Firestore manually via browser console (this is expected — dev database starts empty and requires one-time data population).

## Next Phase Readiness
- Dev environment fully operational: localhost hits clmc-procurement-dev, Netlify continues hitting clmc-procurement
- Firestore rules and indexes match production config on dev project
- Phase 53.1 is complete — developers can now work locally without ever touching production data
- Any future local development work proceeds safely against the isolated dev database

---
*Phase: 53.1-dev-firebase-setup*
*Completed: 2026-03-02*

## Self-Check: PASSED

- FOUND: app/firebase.js
- FOUND: .firebaserc
- FOUND: .planning/milestones/v2.5-phases/53.1-dev-firebase-setup/53.1-02-SUMMARY.md
- FOUND: commit 8b60eb2 (feat(53.1-02): deploy Firestore rules and indexes to dev project)
- Task 3 verification: APPROVED by user (yellow banner visible, dev login works, Firestore reads/writes succeed, prod unaffected)
