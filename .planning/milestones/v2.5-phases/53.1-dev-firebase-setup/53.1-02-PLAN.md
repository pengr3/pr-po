---
phase: 53.1-dev-firebase-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 53.1-01
files_modified:
  - app/firebase.js
  - .firebaserc
autonomous: false
requirements:
  - DEV-01
user_setup:
  - service: firebase
    why: "Create and configure the development Firebase project"
    env_vars: []
    dashboard_config:
      - task: "Create Firebase project"
        location: "https://console.firebase.google.com — Add project, name it clmc-procurement-dev (or similar), disable Google Analytics"
      - task: "Enable Firestore"
        location: "Firebase Console > clmc-procurement-dev > Firestore Database > Create database > Native mode, same region as prod"
      - task: "Enable Authentication"
        location: "Firebase Console > clmc-procurement-dev > Authentication > Get started > Email/Password provider > Enable"
      - task: "Create Super Admin auth user"
        location: "Firebase Console > clmc-procurement-dev > Authentication > Users > Add user — use your dev login credentials"
      - task: "Copy Web App config"
        location: "Firebase Console > clmc-procurement-dev > Project Settings > Your apps > Add app (Web) > Copy the firebaseConfig object"
must_haves:
  truths:
    - "Running the app on localhost connects to clmc-procurement-dev Firestore (not prod)"
    - "Developer can log in on localhost with their dev Firebase account"
    - "Firestore reads and writes on localhost succeed (rules deployed, no permission errors)"
    - "Compound queries on localhost succeed (indexes deployed)"
    - "The yellow DEV ENVIRONMENT banner is visible at the bottom of the page on localhost"
    - "The Netlify deployment continues to use clmc-procurement (prod) and shows no banner"
    - ".firebaserc default project remains clmc-procurement (not changed to dev)"
  artifacts:
    - path: "app/firebase.js"
      provides: "Filled-in dev config with real Firebase project values"
      contains: "clmc-procurement-dev"
    - path: ".firebaserc"
      provides: "Dev project alias (optional) — default remains clmc-procurement"
      contains: "clmc-procurement"
  key_links:
    - from: "devConfig.apiKey"
      to: "Firebase Auth on localhost"
      via: "Real API key from Firebase Console"
      pattern: "AIzaSy"
    - from: "firestore.rules"
      to: "clmc-procurement-dev Firestore"
      via: "firebase deploy --only firestore:rules --project clmc-procurement-dev"
      pattern: "firebase deploy"
---

<objective>
Complete the dev Firebase setup: developer creates the second Firebase project, fills in real config values, deploys rules and indexes to it, creates their dev auth account, and verifies end-to-end that localhost uses the dev database.

Purpose: Plan 01 scaffolded the code with placeholder values. This plan makes it real — connecting the placeholder config to an actual Firebase project and verifying the complete dev workflow.
Output: A fully functional dev environment where localhost never touches production data.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v2.5-phases/53.1-dev-firebase-setup/53.1-01-SUMMARY.md
@app/firebase.js
@.firebaserc
@firestore.rules
@firestore.indexes.json
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create dev Firebase project and fill in config values</name>
  <files>app/firebase.js</files>
  <action>Human action required — see how-to-verify for step-by-step instructions.</action>
  <what-built>
Plan 01 updated app/firebase.js with the dual-config scaffold. The devConfig object currently has placeholder values ("FILL_IN_FROM_FIREBASE_CONSOLE") that need to be replaced with real values from a new Firebase project.
  </what-built>
  <how-to-verify>
Complete these steps in order:

**Step 1 — Create the dev Firebase project**
1. Go to https://console.firebase.google.com
2. Click "Add project"
3. Name it `clmc-procurement-dev` (or a name you prefer — note the exact Project ID)
4. Disable Google Analytics (not needed for dev)
5. Wait for project creation to complete

**Step 2 — Enable Firestore**
1. In the dev project: Build > Firestore Database > Create database
2. Choose "Native mode"
3. Choose the same region as your prod project (likely `us-central1` or `asia-southeast1`)

**Step 3 — Enable Authentication**
1. In the dev project: Build > Authentication > Get started
2. Sign-in method tab > Email/Password > Enable > Save

**Step 4 — Copy Web App config**
1. In the dev project: Project Settings (gear icon) > Your apps
2. Click "Add app" > Web icon
3. Give it a nickname like "CLMC Dev Web"
4. Do NOT enable Firebase Hosting
5. Copy the firebaseConfig object that appears — you need: apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId

**Step 5 — Fill in app/firebase.js**
Open `app/firebase.js` and replace the `devConfig` object's placeholder values:
```javascript
const devConfig = {
    apiKey: "<paste your real apiKey here>",
    authDomain: "<projectId>.firebaseapp.com",
    projectId: "<your actual project ID>",
    storageBucket: "<projectId>.firebasestorage.app",
    messagingSenderId: "<paste real messagingSenderId>",
    appId: "<paste real appId>"
};
```
Save the file. Verify no "FILL_IN_FROM_FIREBASE_CONSOLE" strings remain in the file.

**Step 6 — Create a Super Admin auth user in the dev project**
1. In the dev project: Authentication > Users tab > Add user
2. Enter the email and password you will use for local development
3. Click "Add user" — note down these credentials
  </how-to-verify>
  <verify>grep "FILL_IN_FROM_FIREBASE_CONSOLE" app/firebase.js | wc -l — should return 0</verify>
  <done>app/firebase.js devConfig has real values. No placeholder strings remain.</done>
  <resume-signal>
Type "done" when you have completed all 6 steps above and saved app/firebase.js with real dev config values.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Deploy Firestore rules and indexes to the dev project</name>
  <files>.firebaserc</files>
  <action>
Deploy the existing firestore.rules and firestore.indexes.json to the dev Firebase project using the `-P` flag (never change the default project).

Run the following commands from the project root (replace `clmc-procurement-dev` with the actual project ID if the developer chose a different name):

```bash
firebase deploy --only firestore:rules --project clmc-procurement-dev
firebase deploy --only firestore:indexes --project clmc-procurement-dev
```

After deploying, optionally add a `dev` alias to `.firebaserc` for convenience. Open `.firebaserc` and add a `"dev"` entry:
```json
{
  "projects": {
    "default": "clmc-procurement",
    "dev": "clmc-procurement-dev"
  }
}
```

CRITICAL: The `"default"` project MUST remain `"clmc-procurement"` (prod). Do NOT change it. Verify with `firebase use` — it must output `clmc-procurement`.

If `firebase deploy` fails with an auth error, stop and surface a checkpoint for the user to run `firebase login` first, then retry.
  </action>
  <verify>
Run: `firebase use`
Expected output: `clmc-procurement` (not the dev project)

Confirm "Deploy complete!" appeared in the output of both deploy commands.
  </verify>
  <done>
firestore.rules and firestore.indexes.json are deployed to the dev Firebase project. The .firebaserc default project is still clmc-procurement. Developer can now write Firestore security rules once and deploy to both projects with the -P flag.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify dev environment works end-to-end on localhost</name>
  <files>app/firebase.js</files>
  <action>Human verification required — see how-to-verify for step-by-step instructions.</action>
  <what-built>
The dual Firebase environment is now fully configured:
- app/firebase.js has real dev config values
- Firestore rules and indexes are deployed to the dev project
- Production config and .firebaserc default are unchanged
  </what-built>
  <how-to-verify>
**Step 1 — Start local server**
```bash
python -m http.server 8000
```
Open http://localhost:8000 in your browser.

**Step 2 — Confirm the dev banner is visible**
A yellow banner reading "DEV ENVIRONMENT — clmc-procurement-dev" should appear at the bottom of the page. If you do not see it, open DevTools Console and check for Firebase errors.

**Step 3 — Create a Super Admin user document in dev Firestore**
The dev project has a Firebase Auth user (created in Task 1 Step 6) but no Firestore `users` document yet. The app checks Firestore for the user role on login.

Go to Firebase Console > clmc-procurement-dev > Firestore Database:
1. Create collection: `users`
2. Create document with ID = the UID of your dev auth user (find in Authentication > Users tab)
3. Add fields:
   - `email`: your dev email (string)
   - `role`: `super_admin` (string)
   - `status`: `active` (string)
   - `name`: your name (string)

**Step 4 — Log in on localhost**
Use the dev credentials you created in Task 1 Step 6. You should be able to log in and reach the dashboard.

**Step 5 — Verify it is using the dev database**
In the browser DevTools > Application > IndexedDB, look for a database entry. Its name should contain `clmc-procurement-dev`, NOT `clmc-procurement`. This confirms the two databases are isolated.

Alternatively: Check the production Firebase Console and confirm no new read activity from your localhost session appears there.

**Step 6 — Confirm production deployment is unaffected**
If you have the Netlify URL, open it in a separate tab. It should NOT show the yellow banner.
  </how-to-verify>
  <verify>Dev banner visible at localhost:8000, login succeeds with dev credentials, IndexedDB shows clmc-procurement-dev</verify>
  <done>Dev environment fully operational. localhost uses clmc-procurement-dev exclusively. Prod unchanged.</done>
  <resume-signal>
Type "approved" if the dev environment is working correctly (banner visible, login works, dev Firestore is being used). Describe any issues if something is wrong.
  </resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
- app/firebase.js has no "FILL_IN_FROM_FIREBASE_CONSOLE" strings remaining
- .firebaserc default is still prod: shows `"default": "clmc-procurement"`
- Yellow banner appears on localhost:8000
- No Firestore permission errors on localhost (rules deployed successfully)
- Production Netlify URL works normally with no banner
</verification>

<success_criteria>
- localhost:8000 routes all Firestore traffic to clmc-procurement-dev
- Production Netlify deployment uses clmc-procurement (unchanged)
- Developer can log in on localhost with dev credentials
- Firestore reads/writes succeed on localhost (rules and indexes deployed)
- .firebaserc default remains clmc-procurement
- Dev environment is visually distinct (yellow banner)
</success_criteria>

<output>
After completion, create `.planning/milestones/v2.5-phases/53.1-dev-firebase-setup/53.1-02-SUMMARY.md` following the summary template.
</output>
