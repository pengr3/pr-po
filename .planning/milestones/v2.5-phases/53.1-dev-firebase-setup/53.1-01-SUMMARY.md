---
phase: 53.1-dev-firebase-setup
plan: 01
subsystem: infra
tags: [firebase, firestore, dual-environment, dev-prod, hostname-detection]

# Dependency graph
requires: []
provides:
  - Runtime hostname detection (isLocal boolean) in app/firebase.js
  - Dual Firebase config: prodConfig (unchanged) + devConfig scaffold (placeholders)
  - persistentMultipleTabManager for localhost multi-tab Firestore support
  - Dev environment yellow banner injected on localhost only
affects: [53.1-02, firebase, all views that import firebase.js]

# Tech tracking
tech-stack:
  added: [persistentMultipleTabManager (Firebase Firestore)]
  patterns:
    - Runtime environment detection via window.location.hostname
    - Ternary config selection: isLocal ? devConfig : prodConfig
    - Conditional Firestore tab manager based on isLocal flag

key-files:
  created: []
  modified: [app/firebase.js]

key-decisions:
  - "Dev config uses placeholder strings (FILL_IN_FROM_FIREBASE_CONSOLE) — not invented values; will be filled in Plan 02 after Firebase project creation"
  - "persistentMultipleTabManager selected for localhost to allow two browser tabs simultaneously during development"
  - "Dev banner uses warning color (#f59e0b) with pointer-events:none so it does not interfere with UI interaction"

patterns-established:
  - "Dual-config pattern: define prodConfig and devConfig separately, select at runtime with isLocal ternary"
  - "isLocal = ['localhost', '127.0.0.1'].includes(window.location.hostname) — canonical detection"

requirements-completed: [DEV-01]

# Metrics
duration: 2min
completed: 2026-03-02
---

# Phase 53.1 Plan 01: Dev Firebase Setup Summary

**Runtime hostname detection with dual Firebase configs: localhost hits clmc-procurement-dev (scaffold), Netlify continues hitting clmc-procurement (unchanged)**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-02T03:08:17Z
- **Completed:** 2026-03-02T03:10:22Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added `isLocal` boolean detection using `window.location.hostname` — `['localhost', '127.0.0.1']`
- Renamed existing `firebaseConfig` to `prodConfig` with all original production values preserved exactly
- Added `devConfig` scaffold with `clmc-procurement-dev` project identifiers and `FILL_IN_FROM_FIREBASE_CONSOLE` placeholders for the three secrets that require Firebase Console
- Added `persistentMultipleTabManager` import and conditional selection (localhost gets multi-tab, Netlify gets single-tab)
- Added yellow fixed-bottom dev environment banner visible only on localhost (`#dev-env-banner`, `background:#f59e0b`)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add isLocal detection, dual configs, and multi-tab persistence to firebase.js** - `efabe6e` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `app/firebase.js` - Updated initialization block with dual-config pattern; exports section preserved byte-for-byte

## Decisions Made
- Dev config placeholder values left as `"FILL_IN_FROM_FIREBASE_CONSOLE"` strings — plan explicitly required NOT inventing values; Plan 02 will fill in real credentials after the Firebase project is created
- Used `persistentMultipleTabManager` for localhost so developers can open two browser tabs to the same localhost URL simultaneously without Firestore tab-lock conflicts
- Dev banner uses `pointer-events:none` so it floats above content without blocking clicks

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

Plan 02 will handle filling in the `devConfig` credentials after creating the `clmc-procurement-dev` Firebase project. No user action required from this plan.

## Next Phase Readiness
- Plan 02 (53.1-02) can now proceed: the `devConfig` object is scaffolded with correct project IDs and placeholder secrets ready to be filled in
- Production behaviour is identical to before — `isLocal` is `false` on Netlify so `prodConfig` is selected and `persistentSingleTabManager` is used exactly as before

---
*Phase: 53.1-dev-firebase-setup*
*Completed: 2026-03-02*

## Self-Check: PASSED

- FOUND: app/firebase.js
- FOUND: .planning/milestones/v2.5-phases/53.1-dev-firebase-setup/53.1-01-SUMMARY.md
- FOUND: commit efabe6e (feat(53.1-01): add dual Firebase environment config)
