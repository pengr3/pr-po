---
phase: 38-code-quality-dry-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - app/views/finance.js
  - app/views/procurement.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "No hardcoded personal name 'Ma. Thea Angela R. Lacsamana' exists in any live JS file"
    - "All approval flows write approved_by_name and approved_by_uid fields to Firestore for audit trail"
    - "No dead/unreachable functions remain in finance.js or procurement.js"
    - "No commented-out code blocks (3+ lines) remain in view files"
    - "Section header reads 'MRF Records' (verified, no 'PR-PO Records' in live code)"
  artifacts:
    - path: "app/views/finance.js"
      provides: "Clean finance view with approved_by fields on all approval/rejection flows, no hardcoded personal name"
      contains: "approved_by_name"
    - path: "app/views/procurement.js"
      provides: "Clean procurement view with no hardcoded personal name in DOCUMENT_CONFIG"
      contains: "defaultFinancePIC.*Finance"
  key_links:
    - from: "finance.js approvePRWithSignature"
      to: "Firestore prs collection"
      via: "updateDoc with approved_by_name and approved_by_uid fields"
      pattern: "approved_by_name.*approved_by_uid"
    - from: "finance.js approveTR"
      to: "Firestore transport_requests collection"
      via: "updateDoc with approved_by_name and approved_by_uid fields"
      pattern: "approved_by_name.*approved_by_uid"
---

<objective>
Fix hardcoded approver name, add audit trail fields to all approval flows, remove dead code, clean up debug logs, remove commented-out code blocks, and verify section header accuracy.

Purpose: Close remaining code tech debt items from the v2.3 audit — remove the hardcoded personal name that appears in DOCUMENT_CONFIG fallbacks, add approved_by_name/approved_by_uid to all approval and rejection flows for consistent audit trail, and perform a comprehensive sweep for dead code, unused imports, and debug logging noise across all view files.

Output: Clean finance.js and procurement.js with no hardcoded personal names, consistent audit trail fields on all write paths, and no dead code.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-code-quality-dry-cleanup/38-RESEARCH.md
@.planning/phases/38-code-quality-dry-cleanup/38-01-SUMMARY.md

Key context from research:
- DOCUMENT_CONFIG.defaultFinancePIC = 'Ma. Thea Angela R. Lacsamana' exists in BOTH finance.js (line 232) and procurement.js (line 4532)
- This is a FALLBACK for PO document rendering — used when po.finance_approver_name is not stored on the document
- Fix: Replace personal name with generic 'Finance Approver' fallback — keep the fallback mechanism, remove the name literal
- approvePRWithSignature() (finance.js ~1590) already writes finance_approver_user_id and finance_approver_name but does NOT write approved_by_name/approved_by_uid
- approveTR() (finance.js ~1749) already writes finance_approver, finance_approver_user_id, finance_approver_name but does NOT write approved_by_name/approved_by_uid
- submitRejection() (finance.js ~1909) already writes rejected_by and rejected_by_user_id — these are the rejection equivalents, verify consistency
- Dead code candidates: approvePR()/generatePOsForPR() are NOT in live finance.js (already removed). Look for OTHER dead code during full sweep.
- procurement-base.js is NOT in the router module graph — it's an unused artifact
- Section header "MRF Records" is already correct in current procurement.js (lines 171, 275, 280) — verify only
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hardcoded approver name and add audit trail fields to all approval flows</name>
  <files>app/views/finance.js, app/views/procurement.js</files>
  <action>
**Part A: Remove hardcoded personal name from DOCUMENT_CONFIG in both files**

1. In **finance.js** (~line 232), change:
   ```javascript
   defaultFinancePIC: 'Ma. Thea Angela R. Lacsamana',
   ```
   to:
   ```javascript
   defaultFinancePIC: 'Finance Approver',
   ```
   This preserves the fallback mechanism for PO document generation (used at line 565 when `po.finance_approver_name` and `po.finance_approver` are both missing on legacy POs) while removing the hardcoded personal name.

2. In **procurement.js** (~line 4532), make the same change:
   ```javascript
   defaultFinancePIC: 'Finance Approver',
   ```
   This is used at procurement.js line 5087 for the same PO document rendering.

**Part B: Add approved_by_name and approved_by_uid fields to all approval flows**

Per the CONTEXT decision: "Store on document: write approved_by_name AND approved_by_uid fields to Firestore for audit trail" and "Audit ALL approval flows for hardcoded names."

There are 3 approval flows and 2 rejection flows in finance.js. Audit each:

1. **approvePRWithSignature()** (~line 1590) — PR approval updateDoc:
   Already writes: `finance_approver_user_id`, `finance_approver_name`
   ADD these two fields to the same updateDoc call:
   ```javascript
   approved_by_name: currentUser.full_name || currentUser.email || 'Finance User',
   approved_by_uid: currentUser.uid,
   ```

2. **approveTR()** (~line 1749) — TR approval updateDoc:
   Already writes: `finance_approver`, `finance_approver_user_id`, `finance_approver_name`
   ADD these two fields to the same updateDoc call:
   ```javascript
   approved_by_name: currentUser.full_name || currentUser.email || 'Finance User',
   approved_by_uid: currentUser.uid,
   ```

3. **submitRejection() — TR rejection path** (~line 1912):
   Already writes: `rejected_by`, `rejected_by_user_id`
   These are the rejection equivalents. For consistency, also add:
   ```javascript
   approved_by_name: currentUser?.full_name || currentUser?.email || 'Finance User',
   approved_by_uid: currentUser?.uid,
   ```
   NOTE: Even though this is a rejection, the `approved_by_*` fields capture WHO acted on this document (approved or rejected). The `finance_status: 'Rejected'` field distinguishes the action. This provides a single "who acted" query field.

4. **submitRejection() — PR rejection path** (~line 1943):
   Already writes: `rejected_by`, `rejected_by_user_id`
   ADD same fields:
   ```javascript
   approved_by_name: currentUser?.full_name || currentUser?.email || 'Finance User',
   approved_by_uid: currentUser?.uid,
   ```

5. **PO generation (generatePOsForPRWithSignature)**: This writes PO documents with `finance_approver_name` from the PR data. No additional `approved_by_*` needed here — the PO inherits the approver from the PR approval step. Verify this is working correctly.

**Part C: Verify no other approval flows exist**
Grep for `finance_status.*Approved\|finance_status.*Rejected` across all view files to ensure no approval flow was missed.
  </action>
  <verify>
    <automated>cd C:/Users/franc/dev/projects/pr-po && grep -n "Ma\. Thea Angela" app/views/finance.js app/views/procurement.js && echo "FAIL: hardcoded name still present" || echo "PASS: no hardcoded name found"</automated>
    <manual>Grep for approved_by_name in finance.js — should appear in 4 updateDoc calls (approvePRWithSignature, approveTR, submitRejection TR path, submitRejection PR path).</manual>
  </verify>
  <done>No hardcoded personal name in any live JS file. All 4 approval/rejection flows write approved_by_name and approved_by_uid for consistent audit trail. DOCUMENT_CONFIG fallback uses generic 'Finance Approver' string.</done>
</task>

<task type="auto">
  <name>Task 2: Full dead code, debug log, and commented-out code sweep across all view files</name>
  <files>app/views/finance.js, app/views/procurement.js</files>
  <action>
Perform a comprehensive sweep across all view files in app/views/ looking for dead code, debug logs, commented-out code blocks, unused imports, and orphaned window registrations. Focus on the two largest files (finance.js and procurement.js) as they are highest-value targets.

**Audit checklist for each view file:**

1. **Unused imports** — Check each import name against usage in the file. Remove any import names that are never referenced.

2. **Functions defined but never called** — Search for function definitions and check if they have any call site (within the file, on window, or in onclick handlers). Dead functions should be removed entirely.

3. **Orphaned window registrations** — Check `window.X = X` lines where X is never referenced in any onclick/onchange handler in the same file's HTML templates. If the function is registered on window but never called from HTML, consider removing the window registration (keep the function if it's called programmatically).

4. **Commented-out code blocks** (3+ lines) — Per CONTEXT decision: "Remove commented-out code blocks — git has the history." Delete them. Do NOT delete single-line comments that explain logic.

5. **Raw debug console.log()** — Per CONTEXT decision: Keep meaningful prefixed logs like `console.log('[Procurement]', ...)` and `console.log('[Finance]', ...)`. Remove raw `console.log('...')` without a module prefix that appear to be debug leftovers. Be conservative — when in doubt, keep the log.

6. **Section header verification** — Confirm "MRF Records" (not "PR-PO Records") at procurement.js lines 171, 275, 280. Grep for "PR-PO Records" in all live JS files — should return zero matches (only in .backup files and .planning docs).

7. **Label/header accuracy** — Scan for any other user-facing labels or section headers that are inaccurate or outdated. Fix any found.

8. **Dead CSS audit** — Check styles/components.css and styles/views.css for rules with selectors that have no matching HTML in any view's render() function. Note: dept badge uses inline styles (no CSS classes to worry about). Focus on orphaned class rules from removed features.

9. **Firestore optimization opportunities** — While reading each view file, note any:
   - Redundant reads (getDocs calls that could use cached data)
   - Listeners that fire on collections where one-time getDocs would suffice (data that rarely changes)
   - Missing query constraints (e.g., unscoped collection listeners that should have where() clauses)
   If optimization opportunities are found, implement them only if the fix is low-risk and clearly beneficial. Document any deferred optimizations.

**Key files to sweep:** finance.js, procurement.js, home.js, mrf-form.js, projects.js, project-detail.js, services.js, service-detail.js, admin.js, user-management.js, clients.js, role-config.js, service-assignments.js, project-assignments.js. Smaller files (login.js, register.js, pending.js) are unlikely to have dead code but scan their imports.

**DO NOT modify:**
- procurement-base.js (unused artifact, not loaded by router — leave it alone)
- Any .backup or .backup2 files
- Any files in archive/
  </action>
  <verify>
    <automated>cd C:/Users/franc/dev/projects/pr-po && grep -rn "PR-PO Records" app/views/*.js | grep -v backup && echo "FAIL: PR-PO Records still in live code" || echo "PASS: no PR-PO Records in live code"</automated>
    <manual>Open browser DevTools console, navigate through all tabs (Home, Projects, Services, Procurement, Finance, Admin). Check for: no JavaScript errors, no broken UI elements, all tables render correctly with data.</manual>
  </verify>
  <done>All dead/unreachable functions removed. No commented-out code blocks remain. Debug log noise cleaned. Section header verified as "MRF Records". All unused imports removed. No dead CSS rules remain. Any Firestore optimizations documented or implemented.</done>
</task>

</tasks>

<verification>
1. `grep -rn "Ma. Thea Angela" app/views/*.js` returns zero matches (no hardcoded personal name)
2. `grep -rn "approved_by_name" app/views/finance.js` returns 4 matches (one per approval/rejection flow)
3. `grep -rn "PR-PO Records" app/views/*.js | grep -v backup` returns zero matches
4. `grep -rn "function getMRFLabel\|function getDeptBadgeHTML" app/views/*.js | grep -v backup` returns zero matches (only in components.js — verified in 38-01)
5. No JavaScript errors in browser console when navigating all views
6. PO document generation still renders finance approver name (from stored data or generic fallback)
</verification>

<success_criteria>
- Zero instances of hardcoded personal name 'Ma. Thea Angela R. Lacsamana' in live JavaScript files
- approved_by_name and approved_by_uid fields present in all 4 approval/rejection updateDoc calls
- No dead functions, commented-out code blocks (3+ lines), or raw debug console.log() statements
- Section header confirmed as "MRF Records"
- Clean codebase ready for v2.4 milestone
</success_criteria>

<output>
After completion, create `.planning/phases/38-code-quality-dry-cleanup/38-02-SUMMARY.md`
</output>
