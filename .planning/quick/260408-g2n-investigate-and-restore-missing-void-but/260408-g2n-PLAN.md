---
phase: 260408-g2n-investigate-and-restore-missing-void-but
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/finance.js
autonomous: false
requirements:
  - QT-G2N-01
must_haves:
  truths:
    - "Finance user can see all existing non-voided payment records for an RFP inside the Record Payment modal"
    - "Finance user can click a Void button next to any existing active payment record in the modal and (after confirm) mark it voided"
    - "Voided payments disappear from the active payments list and Total Paid / Balance recomputes via the existing onSnapshot re-render"
    - "The flat-list RFP table (D-02) is unchanged — no chevron, no expand row, no history sub-table added"
    - "Record Payment modal continues to function for recording new payments (no regression)"
  artifacts:
    - path: "app/views/finance.js"
      provides: "Enhanced openRecordPaymentModal with Existing Payments section and Void button wiring"
      contains: "Existing Payments"
  key_links:
    - from: "openRecordPaymentModal (finance.js)"
      to: "rfp.payment_records array"
      via: "rfpsData.find(r => r.id === rfpDocId)"
      pattern: "rfp\\.payment_records"
    - from: "Void button onclick in modal"
      to: "window.voidPaymentRecord (line 263)"
      via: "onclick handler passing rfpDocId and payment_id"
      pattern: "window\\.voidPaymentRecord"
    - from: "voidPaymentRecord success"
      to: "rfps onSnapshot listener"
      via: "updateDoc triggers re-render of rfpsData which drives modal state via close+reopen or in-place refresh"
      pattern: "updateDoc.*rfps"
---

<objective>
Restore a UI entry point for voiding payment records in the Finance Payables tab. The void button was lost in Phase 65.1 (commit 468752d) when the expandable payment history sub-table was eliminated per D-02 (flat list only). The underlying `voidPaymentRecord(rfpDocId, paymentId)` function and its `window.voidPaymentRecord` registration are still intact — only the UI trigger is missing.

Purpose: Finance users currently have no way to correct an incorrectly-recorded payment without direct Firestore editing.
Output: Enhanced `openRecordPaymentModal` that renders an "Existing Payments" section listing active payment records with Void buttons, surgically integrated into the already-open modal (respects D-02 by not adding anything to the flat RFP table itself).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/views/finance.js

<interfaces>
<!-- Existing functions in finance.js the executor must reuse — DO NOT re-implement -->

Already registered on window (finance.js line 257-264):
- window.openRecordPaymentModal(rfpDocId)       // line 275 — TO BE MODIFIED
- window.voidPaymentRecord(rfpDocId, paymentId) // line 399 — REUSE AS-IS, do not modify
- window.submitPaymentRecord(rfpDocId)          // line 357 — reused by existing Record Payment button

RFP data shape (from rfpsData):
- rfp.id            // Firestore doc id
- rfp.rfp_id        // user-facing "RFP-{PO-ID}-{n}"
- rfp.amount_requested
- rfp.payment_records: Array<{
    payment_id: string,        // e.g. "PAY-1712345678901"
    amount: number,
    date: string,              // YYYY-MM-DD
    method: string,
    reference: string,
    status: 'active' | 'voided',
    recorded_at: string
  }>

Existing helpers already imported in finance.js (reuse, do not re-import):
- formatCurrency(amount)
- escapeHTML(str)

Render target: The modal is inserted via document.body.insertAdjacentHTML('beforeend', modalHtml) at line 350. The "Existing Payments" section must live INSIDE modalHtml, above the payment entry form fields (Amount / Payment Date / Reference Number).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Existing Payments section with Void buttons to Record Payment modal</name>
  <files>app/views/finance.js</files>
  <action>
Modify ONLY the `openRecordPaymentModal(rfpDocId)` function (lines 275-351). Do NOT touch `renderRFPTable` — the flat-list constraint (D-02) MUST be preserved.

Steps:

1. Inside `openRecordPaymentModal`, after the existing `rfp` lookup and before the `modalHtml` template literal, compute an `existingPaymentsHtml` string:
   - Read `const records = rfp.payment_records || [];`
   - Filter to active records only: `const activeRecords = records.filter(r => r.status !== 'voided');`
   - If `activeRecords.length === 0`, set `existingPaymentsHtml = ''` (render nothing — clean modal for first-time payments).
   - If `activeRecords.length > 0`, build a section containing:
     - A header label styled consistent with existing uppercase labels in the modal (reuse the `font-size:0.75rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;color:#64748b;` pattern already used at line 311). Label text: "Existing Payments ({N})".
     - A compact list (flex column, gap 6px) where each row shows: date, formatted amount via `formatCurrency(r.amount)`, reference (via `escapeHTML`) if present, and a small danger-styled Void button on the right.
     - Each row background `#f8fafc`, border `1px solid #e2e8f0`, border-radius `6px`, padding `8px 12px`, display `flex`, justify-content `space-between`, align-items `center`.
     - Void button: `<button class="btn btn-sm" style="background:#fef2f2;color:#991b1b;border:1px solid #fecaca;padding:4px 10px;font-size:0.75rem;border-radius:4px;cursor:pointer;" onclick="window.voidPaymentRecord('${rfpDocId}', '${escapeHTML(r.payment_id)}'); document.getElementById('recordPaymentModal').remove();">Void</button>`
     - The modal is closed on void click so the user sees the fresh state via the rfps onSnapshot re-render; if they need to void more, they reopen the modal from the RFP row's Record Payment button. This is acceptable because voiding is a corrective action (low frequency).
   - Wrap the entire block in a container div with `margin-bottom:1.25rem;padding-bottom:1.25rem;border-bottom:1px solid #e5e7eb;` so it visually separates from the new-payment form below.

2. Insert `${existingPaymentsHtml}` into the `modalHtml` template literal INSIDE the `modal-body` div, immediately AFTER the closing `</div>` of the supplier/amount grid (currently ends at line 323) and BEFORE the `<div style="display:flex;flex-direction:column;gap:1rem;">` form fields block (line 324). This places it above the Amount / Payment Date / Reference Number inputs.

3. IMPORTANT — status-not-fully-paid gating: When an RFP is Fully Paid, the flat table does NOT render a "Record Payment" button (line 590), so users can't reach the modal via the table. To also allow voiding payments on Fully Paid RFPs (the most common correction case), update the action column render at line 590-592 to ALWAYS render the button, but relabel it:
   - If `status === 'Fully Paid'`, render button text "Manage Payments" instead of "Record Payment", using the same onclick `window.openRecordPaymentModal('${rfp.id}')`, and style with neutral outline (not primary green) — e.g. `class="btn btn-sm btn-outline"`.
   - Otherwise keep the existing "Record Payment" primary button exactly as-is.
   - This is the ONLY change to `renderRFPTable` and it does NOT add rows, columns, chevrons, or expands — D-02 stays satisfied.

4. Inside `openRecordPaymentModal`, when the RFP is Fully Paid, the user should still see the existing payments list but should NOT see the new-payment form. Add a guard:
   - Compute `const isFullyPaid = (activeRecords.reduce((s, r) => s + (r.amount || 0), 0)) >= (rfp.amount_requested || 0);` (this mirrors the existing derivation without importing deriveRFPStatus).
   - If `isFullyPaid`, build an alternative form block: hide the Amount/Date/Reference inputs and the green "Record Payment" submit button; show a note "This RFP is fully paid. You can void existing payments above to record a correction." and change the footer to only show a "Close" button (reuse the existing Discard Payment close handler, relabeled "Close").
   - If not fully paid, keep the existing form + footer exactly as-is.

5. Do NOT modify: `voidPaymentRecord`, `submitPaymentRecord`, `renderRFPTable` filter/sort logic, `buildPOMap`, `derivePOSummary`, any other function. Surgical change only.

6. Manually sanity-check the resulting `openRecordPaymentModal` keeps all existing behavior for RFPs with zero payment records (the most common case): `existingPaymentsHtml = ''` must produce a modal identical to the pre-change version.
  </action>
  <verify>
    <automated>MISSING — no test harness exists for this static SPA per CLAUDE.md ("No build, test, or lint commands"). Verification is manual via browser DevTools and production Firebase per project convention.</automated>
Manual verification (checkpoint task below covers this):
1. `python -m http.server 8000`, open http://localhost:8000, navigate to Finance -> Payables tab.
2. Open DevTools Console — no errors on tab load.
3. Grep check: `grep -n "Existing Payments" app/views/finance.js` should return exactly one line (the header label).
4. Grep check: `grep -n "voidPaymentRecord" app/views/finance.js` should return at least 3 matches (function definition, window registration, and new onclick in modal).
  </verify>
  <done>
    - `openRecordPaymentModal` renders an "Existing Payments (N)" section listing active payment records with working Void buttons when N > 0.
    - Modal renders identically to pre-change version when an RFP has zero active payment records.
    - Record Payment button in flat table is unchanged for non-Fully-Paid RFPs.
    - Fully Paid RFPs now show a "Manage Payments" outline button in the action column.
    - Fully Paid modal hides the new-payment form and shows only the existing payments list + Close button.
    - No changes to `renderRFPTable` structure (no new columns, rows, chevrons, or expand behavior).
    - No edits to `voidPaymentRecord`, `submitPaymentRecord`, or any other function.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser UAT — verify void flow end-to-end</name>
  <what-built>
Enhanced Record Payment modal with Existing Payments section and Void buttons. New "Manage Payments" button for Fully Paid RFPs. Flat RFP table structure unchanged (D-02 preserved).
  </what-built>
  <how-to-verify>
1. Start local server: `python -m http.server 8000`
2. Open http://localhost:8000 and log in as Finance/Admin.
3. Navigate to Finance -> Payables tab.
4. CASE A — RFP with zero payments:
   - Find a Pending RFP (status badge grey/blue "Pending").
   - Click "Record Payment" button.
   - Expected: Modal opens exactly as before with supplier, amount grid, amount/date/reference inputs, and green Record Payment footer button. NO "Existing Payments" section visible. Close modal (Discard Payment).
5. CASE B — RFP with one active payment (Partially Paid):
   - Record a payment against a Pending RFP first (if none exists). Refresh; RFP should now show Partially Paid badge.
   - Click "Record Payment" on that RFP again.
   - Expected: Modal opens; "Existing Payments (1)" header visible above the form; one row shows date, formatted amount, reference, and a red-bordered "Void" button on the right. Form fields for new payment still visible below the separator.
6. CASE C — Void a payment:
   - In the same modal, click the Void button on the existing payment row.
   - Expected: Native confirm dialog "Void this payment record? This cannot be undone." Click OK.
   - Expected: Toast "Payment record voided" appears; modal closes; RFP row in table recomputes — Total Paid drops to 0, Balance returns to full amount, status badge returns to Pending (or Overdue if past due_date).
7. CASE D — Fully Paid RFP:
   - In the RFP status filter, select "Fully Paid" so hidden fully-paid rows appear.
   - Find a Fully Paid RFP — the action column should now show a grey outline "Manage Payments" button (not the green Record Payment button).
   - Click it.
   - Expected: Modal opens with Existing Payments section listing all active payments with Void buttons. NO amount/date/reference inputs visible. Footer shows only a single "Close" button. A note like "This RFP is fully paid..." appears.
   - Void one of the payments.
   - Expected: Modal closes, status badge transitions from Fully Paid to Partially Paid (or Pending), RFP row re-appears in default (non-filtered) view if Fully Paid filter is cleared.
8. CASE E — D-02 preservation check:
   - Inspect the RFP table HTML via DevTools.
   - Expected: No new columns, no chevron elements, no rows with `display:none` that would indicate expand rows. Column count still 11.
9. DevTools Console: No JS errors at any point across all cases.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
- Grep `app/views/finance.js` for `Existing Payments` — exactly one occurrence (the header label).
- Grep `app/views/finance.js` for `window.voidPaymentRecord` — appears in onclick handler inside modal HTML.
- Grep `app/views/finance.js` for `Manage Payments` — exactly one occurrence (the Fully Paid table button label).
- Visual confirmation via Task 2 checkpoint (browser UAT with production Firebase — no staging environment per CLAUDE.md).
- Column count in renderRFPTable `colspan="11"` empty-state cell unchanged — proves no new columns added.
</verification>

<success_criteria>
- Finance users can void an existing payment record from the Record Payment / Manage Payments modal on any RFP (Partial or Fully Paid).
- Voiding triggers the existing rfps onSnapshot re-render, updating Total Paid, Balance, and status badge without a manual refresh.
- Flat RFP table structure is byte-identical in layout (columns, colspan, no expand rows) — D-02 is preserved.
- No regression: Recording a new payment on a Pending RFP works exactly as it did before this change.
- Zero JS console errors across all UAT cases.
</success_criteria>

<output>
After completion, create `.planning/quick/260408-g2n-investigate-and-restore-missing-void-but/260408-g2n-SUMMARY.md` documenting:
- What changed (files, functions, line regions).
- Why this approach (enhance existing modal vs separate history modal).
- D-02 compliance confirmation (no flat-list structural changes).
- UAT results for cases A-E.
</output>
