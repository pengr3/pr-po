---
quick_id: 260615-eo0
description: Apply Projects Financial-Summary card layout to Services for Phase 104 parity
date: 2026-06-15
branch: v3.3
mode: quick (inline — gsd-sdk present but worktrees unsafe on this repo per Windows long-path constraint; single-file presentational change)
---

# Quick Task 260615-eo0: Services Financial-Summary card-grid parity

## Boundary

Phase 104 gave Services its own detail page but its **Financial Summary** card renders
all 8 financial fields in one flat `repeat(2,1fr)` grid as plain label/value text, while
**Projects** (`project-detail.js:704–771`) groups them into three labeled, tinted sections
with card tiles. This task brings Services to visual parity. Presentational only — no schema,
no rules, no data-source or handler changes.

## Reference (Projects — the target)

`app/views/project-detail.js:704–771`:
- **Budget** group: blue section label (`#1a73e8`, 0.65rem uppercase) → 2-col grid:
  Budget input | Contract Cost input (plain), then Projected Expense tile | Rem. Budget tile.
  Tiles: `background:#f0f7ff; border-radius:5px; padding:0.3rem 0.5rem`, 0.65rem uppercase
  label (`#64748b`), 0.85rem bold value.
- **Payables** group: `border-top:1px solid #f1f5f9` divider + red label (`#ef4444`) →
  Paid tile | Rem. Payable tile, `background:#fff5f5`.
- **Collectibles** group: divider + green label (`#059669`) → Collected tile | Outstanding
  tile, `background:#f0fdf4`.

## Target (Services — `app/views/service-detail.js:869–934`)

Restructure the single flat grid into the same three grouped tinted sections, preserving all
service-specific behavior:
- Keep the `PHP …` small annotations on the Budget / Contract Cost labels.
- Keep data sources: `currentServiceExpense` (prTotal+poTotal+rfpFeesTotal for projected;
  `.totalPaid`, `.remainingPayable`) and `currentServiceCollectibles` (`.totalCollected`,
  `.remainingCollectible`).
- Keep `onblur="window.saveServiceField(...)"` handlers + `${!showEditControls ? 'disabled' : ''}`.
- Keep the existing value color logic (rem budget green/red, paid green, rem payable red/green,
  outstanding red/green).
- Leave the Export CSV button, header, DLP finance bar, and Collection Tranches section untouched.

## Tasks

### Task 1 — Restructure the Services Financial-Summary grid into grouped tinted sections
- **File:** `app/views/service-detail.js` (the `<div ... grid-template-columns: repeat(2, 1fr)>`
  block at ~869–934, between `renderServiceDlpFinanceBar()` and the Collection Tranches header)
- **Action:** Replace the flat 8-field grid with three sections mirroring projects:
  (1) Budget (blue label) — Budget + Contract Cost inputs, then Projected Cost + Remaining Budget tiles (#f0f7ff);
  (2) Payables (divider + red label) — Paid + Remaining Payable tiles (#fff5f5);
  (3) Collectibles (divider + green label) — Collected + Outstanding tiles (#f0fdf4).
  Reuse the exact tile styling from project-detail.js. Preserve the PHP annotations, data
  sources, saveServiceField handlers, disabled gate, and value-color logic.
- **Verify:** `node --check app/views/service-detail.js` exits 0; grep confirms three section
  labels (Budget/Payables/Collectibles) and the three tint colors present; `saveServiceField`,
  `currentServiceExpense`, `currentServiceCollectibles` references retained; Export CSV + tranche
  blocks unchanged.
- **Done:** Services Financial Summary renders the same grouped, tinted 2×2 card grid as Projects.

## Out of scope
- No changes to data computation, Firestore, rules, or window-function wiring.
- No changes to the DLP finance bar, tranche editor/lifecycle, or other cards.
