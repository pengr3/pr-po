---
quick_id: 260706-d90
title: Rebrand dev environment banner to lintel.build
status: complete
date: 2026-07-06
branch: demo/2026-07-06
---

# Quick Task 260706-d90: Rebrand Dev Banner to lintel.build — Summary

**One-liner:** Cosmetic swap of the localhost-only dev banner text (`DEV ENVIRONMENT — clmc-procurement-dev` → `lintel.build`) and colors (amber-on-slate → slate-on-light) in `app/firebase.js`, no behavior change.

## What changed

`app/firebase.js` lines 121-122 (inside the existing `if (isLocal) { ... }` block, unchanged):

- **Text:** `banner.textContent = 'DEV ENVIRONMENT — clmc-procurement-dev';` → `banner.textContent = 'lintel.build';`
- **Colors only** (in `banner.style.cssText`): `background:#f59e0b;color:#1e293b;` → `background:#1e293b;color:#f8fafc;`
- All other `cssText` properties (`position:fixed`, `bottom:0`, `left:0`, `right:0`, `text-align:center`, `padding:4px 8px`, `font-size:0.75rem`, `font-weight:600`, `z-index:9999`, `pointer-events:none`) left byte-for-byte unchanged.
- The `isLocal` guard and everything else in the file is untouched — confirmed via `git diff` showing only the 2 modified lines.

## Verification

- `node --check app/firebase.js` → **passed** (exits 0, no syntax errors).
- Grep of `app/firebase.js` confirms:
  - `lintel.build` present (line 121)
  - `background:#1e293b;color:#f8fafc` present (line 122)
  - `DEV ENVIRONMENT` **absent** from the file
  - `#f59e0b` **absent** from the file
- `git diff -- app/firebase.js` reviewed before commit: exactly 2 lines changed (the textContent string and the color pair inside cssText), no other lines touched.
- Browser UAT (localhost banner visual check) not performed by this executor — deferred to the user per task scope (code-only quick task).

## Deviations from Plan

None - plan executed exactly as written.

## Out of scope (per plan, not done)

- Seed-script comments (`scripts/seed-dev-*.js`) referencing old banner text — dev-only console notes, not user-facing, explicitly out of scope per PLAN.md.

## Commit

- `883f872` — `feat(quick-260706-d90): rebrand dev banner to lintel.build` — 1 file changed (`app/firebase.js`), 2 insertions(+), 2 deletions(-).

## Self-Check: PASSED

- FOUND: app/firebase.js (edit present, confirmed via grep above)
- FOUND: commit 883f872 (confirmed via `git show --stat`)
