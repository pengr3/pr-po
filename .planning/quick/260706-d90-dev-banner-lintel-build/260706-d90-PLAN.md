---
quick_id: 260706-d90
title: Rebrand dev environment banner to lintel.build
status: planned
date: 2026-07-06
branch: demo/2026-07-06
---

# Quick Task 260706-d90: Rebrand Dev Banner to lintel.build

## Origin

User asked to replace the localhost-only dev banner text/color. Current banner reads
`DEV ENVIRONMENT — clmc-procurement-dev` on an amber background (`#f59e0b`). Rebrand it to
`lintel.build` with a slate-dark treatment (user-selected color).

## Scope boundary

Cosmetic-only change to the `isLocal` dev banner in `app/firebase.js` (lines 121-122).
No behavior change: the banner still renders only on localhost (`isLocal` gate unchanged),
stays `position:fixed` bottom, `pointer-events:none`, `z-index:9999`. Only the text string and
the two color values change. Seed-script comments that reference the old banner text are
out of scope (dev-only console guidance, not user-facing).

## Tasks

### T1 — app/firebase.js

- **File:** `app/firebase.js`
- **Action:**
  1. Line 121: change `banner.textContent = 'DEV ENVIRONMENT — clmc-procurement-dev';`
     → `banner.textContent = 'lintel.build';`
  2. Line 122: in the `cssText` string change `background:#f59e0b;color:#1e293b;`
     → `background:#1e293b;color:#f8fafc;` (leave every other style prop identical).
- **Verify:** `node --check app/firebase.js` passes; grep confirms new text/colors present and
  old `DEV ENVIRONMENT`/`#f59e0b` gone from the banner block; the `isLocal` guard and other
  cssText props (position, z-index, pointer-events) are untouched.
- **Done:** Banner block reads `lintel.build` with `background:#1e293b;color:#f8fafc`.

## No infra / rules change

- Pure client JS string/style edit. No Firestore/Storage/rules change, no `firebase deploy`.
- JS-only ship: commit on `demo/2026-07-06` (Netlify auto-deploys on push).

## Verification

- `node --check app/firebase.js`.
- Grep: `lintel.build` and `background:#1e293b;color:#f8fafc` present in the banner block;
  `DEV ENVIRONMENT` and `#f59e0b` absent from `app/firebase.js`.
- Browser (localhost): reload `http://localhost:8000`, confirm bottom banner now shows
  `lintel.build` on a slate-dark bar with light text.

## Out of scope

- Seed-script comments (`scripts/seed-dev-*.js`) that mention the old banner text — dev-only
  console notes, not the rendered UI.
