---
phase: quick-260627-kg0
plan: 03
status: code-complete-checkpoints-pending
wave: 3
commits: [7ced1f9c, 01785842]
files_modified:
  - app/views/project-detail.js
  - app/views/service-detail.js
  - app/proposal-modal.js
  - app/views/mrf-form.js
  - app/views/projects.js
  - app/views/home.js
  - app/seed-roles.js
  - scripts/patch-cross-dept-edit-permissions.js (new)
completed: 2026-06-27
---

# Quick 260627-kg0 Plan 03 — UI role-gate sweep + unified nav

## Status

Code COMPLETE and committed (`7ced1f9c` UI sweep + `01785842` patch script).
**Two BLOCKING checkpoints remain — user action:**
- Task 5: patch live role_templates docs (super_admin, DEV) via the new script.
- Task 6: two-direction parity UAT on DEV.

## Role-literal gates converted (all mirrored both directions; admins untouched, D-1)

| Site | Change |
|------|--------|
| project-detail `_canAdvanceProjectStatus` | member arm widened `operations_user` → `operations_user \|\| services_user` |
| service-detail `_canAdvanceServiceStatus` | member arm widened `services_user` → `services_user \|\| operations_user`; **Completed exclusion preserved** above it (services_admin-only) |
| project-detail `LOSS_ASSIGNED_ROLES` | `['operations_user']` → `+ 'services_user'` |
| service-detail `LOSS_ASSIGNED_ROLES` | `['services_user']` → `+ 'operations_user'` |
| service-detail save + toggle role guards | `+ 'operations_user'` (rule still enforces isAssignedToService) |
| proposal-modal `_isCallerAttachedToProposalParent` | both member-drive arms role-agnostic for *_user; parent_collection alignment + admin-only approve preserved |
| mrf-form `showProjects`/`showServices` | each gains the mirror *_user role → both pickers for both *_user |
| projects.js `applyFilters` | dropped services-role force-empty; single assignment-driven scope |
| home.js `getDashboardMode` | `'both'` for *_user; only dept admins single-department |
| home.js `filterProposalsForUser` | *_user see both departments' proposals (modal still gates drive/approve) |
| seed-roles.js | operations_user.services.edit + services_user.projects.edit → true |

`assignedRoles = ['operations_user','services_user']` at project-detail.js:2447 and
service-detail.js:1856 already listed both — left unchanged (confirmed).

## Verify gate results

- `node --check` PASS on all 7 edited JS files + the patch script.
- proj/svc `_canAdvance` widened (count 1 each); svc Completed exclusion still present (count 1).
- proposal-modal both member arms widened (count 1 each); admin approve arms untouched.
- projects.js force-empty `scopedProjects = []` removed (count 0).
- home.js both-`*_user` proposal arm present (count 1).
- seed-roles.js services/projects `edit:true` rows = 5 each (was 4; +operations_user / +services_user).
- `git diff --stat`: exactly 7 source files (wave 3) + 1 new script.

## Nav note

Nav visibility is permission-driven (`auth.js` reads `role_templates.tabs[route].access`); ACCESS
was already `true` on both cross-dept tabs in seed-roles.js. So unified nav needs only the live
`role_templates` edit-flag patch (Task 5) — no index.html / router edits.

## Task 5 checkpoint (BLOCKING — user, DEV)

As super_admin on the DEV app: paste `scripts/patch-cross-dept-edit-permissions.js` into the
browser console, run `await patchCrossDeptEditPermissions()`, hard-refresh.

## Task 6 checkpoint (BLOCKING — user, DEV UAT)

Prereq: a services_user assigned to ≥1 project, an operations_user assigned to ≥1 service (assign
via the existing personnel UI if none — the sync helpers populate assigned_*_codes).
Both directions: nav shows the cross-dept area, list shows ONLY assigned items, detail-page drive
works, MRF picker offers the cross-dept type, procurement records show the project-OR-service union,
proposal drive works (approve does not). On the service side, confirm Completion is NOT offered to
the operations_user. No-regression: a single-dept *_user with no cross assignment sees no change.

## Deferred / debt (PROD)

After DEV UAT passes:
1. `firebase deploy --only firestore:rules` to PROD (wave-1 rules) — standing v3.3 → main debt.
2. Run `patchCrossDeptEditPermissions()` as super_admin on PROD (live role_templates).
3. Backfill watch: pre-existing cross-dept assignees may have `personnel_user_ids` set but stale
   `assigned_*_codes` (re-run a personnel assignment to resync if procurement lists look off).
