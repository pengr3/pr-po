---
phase: quick-260627-kg0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestore.rules
autonomous: false
requirements: [KG0-RULES-SYMMETRIC-WRITE]
user_setup:
  - service: firebase
    why: "Security rules must be deployed to DEV for UAT, then PROD; CLI active project is PROD so DEV needs --project dev"
    dashboard_config:
      - task: "Deploy rules to DEV: firebase deploy --only firestore:rules --project dev"
        location: "Local terminal (Firebase CLI)"

must_haves:
  truths:
    - "An operations_user whose uid is in a SERVICE doc's personnel_user_ids can write service_tasks, the service lifecycle field-mask, a Branch-B proposal on that service, and every journal subcollection on that service."
    - "A services_user whose uid is in a PROJECT doc's personnel_user_ids can write project_tasks, the project lifecycle field-mask, a Branch-B proposal on that project, and every journal subcollection on that project."
    - "Every cross-department write path is opened SYMMETRICALLY — for any rule that newly admits services_user on the projects side, the mirror rule admits operations_user on the services side, and vice versa."
    - "No isRole(...) short-circuit guard protecting an assigned_*_codes / personnel_user_ids dereference is removed; non-assigned-member roles cannot dereference fields that would error."
    - "Out-of-scope gates are untouched: pos.create, prs.create, suppliers, clients, collectibles, billing_requests authority sets, mrfs.create, and ALL users/role_templates/invitation_codes admin rules read exactly as before this plan."
  artifacts:
    - path: "firestore.rules"
      provides: "Role-agnostic assigned-member write predicate applied symmetrically across project_tasks/service_tasks, proposals Branch B (create+update), project/service lifecycle field-masks, and the 6 journal subcollections on both sides."
      contains: "request.auth.uid in resource.data.personnel_user_ids"
  key_links:
    - from: "project_tasks create/update/delete"
      to: "services_user assigned-member branch"
      via: "isRole('services_user') && isAssignedToProject(...) added alongside the existing operations_user branch"
      pattern: "isRole\\('services_user'\\)"
    - from: "service_tasks create/update/delete"
      to: "operations_user assigned-member branch"
      via: "isRole('operations_user') && isAssignedToService(...) added alongside the existing services_user branch"
      pattern: "isRole\\('operations_user'\\)"
    - from: "proposals create/update Branch B"
      to: "cross-dept assigned member"
      via: "department-alignment + parent personnel_user_ids check made role-agnostic for *_user while preserving parent_collection correctness"
      pattern: "parent_collection"
---

<objective>
Open every cross-department member WRITE path in firestore.rules SYMMETRICALLY, using the
existing `request.auth.uid in resource.data.personnel_user_ids` assigned-member predicate
(create-time: `request.resource.data.personnel_user_ids`). After this plan, an
operations_user assigned to a service has the same member-writes a services_user has on that
service, and a services_user assigned to a project has the same member-writes an
operations_user has on that project. Department-role admin scoping (operations_admin /
services_admin) is UNCHANGED.

Purpose: This is the security core and must be reviewable in isolation. It ships FIRST so the
data/display layer (Plan 02) and UI sweep (Plan 03) land against rules that already permit the
cross-department writes those layers will surface.

Output: Edited `firestore.rules` (deployed to DEV for UAT; PROD deploy joins the standing
v3.3 → main rules-deploy debt).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260627-kg0-cross-dept-assignment-parity/260627-kg0-CONTEXT.md
@CLAUDE.md

# The full security contract you are editing. Read it before changing anything.
@firestore.rules

<interfaces>
<!-- Helper functions ALREADY defined in firestore.rules (lines 70-90). Reuse — do NOT redefine.
     Both are SHORT-CIRCUIT-SAFE only when guarded by isRole(...) first (T-105-03 pattern):
     they dereference assigned_*_codes which only exist on the matching *_user doc. -->

function isAssignedToProject(projectCode)  // getUserData().all_projects == true || projectCode in getUserData().assigned_project_codes
function isAssignedToService(serviceCode)  // getUserData().all_services == true || serviceCode in getUserData().assigned_service_codes
function hasRole(roles) / isRole(role) / isActiveUser()

<!-- EXISTING member-write idioms to MIRROR (already in the file):
     - project_tasks (lines 713-745): create/update-tier1/delete use
         (isRole('operations_user') && isAssignedToProject(<code>))
       Tier-2 progress-only uses request.auth.uid in resource.data.assignees.
     - service_tasks (lines 772-804): mirror with services_user + isAssignedToService.
     - projects.update assigned-member branch (lines 220-246): isRole('operations_user')
         && request.auth.uid in resource.data.personnel_user_ids && affectedKeys().hasOnly([field-mask]).
     - services.update (lines 572-575): isRole('services_user') && isAssignedToService(service_code).
     - proposals create Branch B (lines 856-874) + update Branch 2 (lines 898-921): per-role
         department-alignment (operations_user→'projects', services_user→'services') + parent
         personnel_user_ids get(). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Symmetric member-write on project_tasks ⇄ service_tasks + the two task-collection deletes</name>
  <files>firestore.rules</files>
  <action>
Make the two task collections role-agnostic for assigned *_user members, in BOTH directions.

project_tasks (match /project_tasks/{taskId}, ~lines 713-745):
  - create (line 718-720): keep `hasRole(['super_admin','operations_admin'])` and the existing
    `(isRole('operations_user') && isAssignedToProject(request.resource.data.project_code))`,
    and ADD a sibling OR term:
    `(isRole('services_user') && isAssignedToProject(request.resource.data.project_code))`.
    Rationale: a services_user assigned to a project (their assigned_project_codes contains the
    project's code, populated by syncPersonnelToAssignments) may create native tasks on it.
  - update Tier 1 (line 728-729): alongside the existing operations_user term ADD
    `(isRole('services_user') && isAssignedToProject(resource.data.project_code))`.
  - update Tier 2 (progress-only, lines 734-739): the assignees membership check
    `request.auth.uid in resource.data.assignees` is ALREADY role-agnostic via the assignees
    array — but it is currently gated to `isRole('operations_user')`. Add a parallel
    `isRole('services_user')` arm so an assigned services_user listed in task.assignees can write
    progress-only. PRESERVE the affectedKeys().hasOnly(['progress','updated_at']) mask and the
    `resource.data.keys().hasAny(['assignees']) && resource.data.assignees is list` guards verbatim.
  - delete (line 743-744): add `(isRole('services_user') && isAssignedToProject(resource.data.project_code))`.

service_tasks (match /service_tasks/{taskId}, ~lines 772-804): apply the EXACT mirror —
  add `(isRole('operations_user') && isAssignedToService(<service_code>))` to create (using
  request.resource.data.service_code), update Tier 1 (resource.data.service_code), and delete;
  add a parallel `isRole('operations_user')` arm to the Tier-2 progress-only assignees branch.

HARD CONSTRAINTS:
  - The isRole('...') guard MUST precede every isAssignedTo* / assignees dereference (short-circuit
    || prevents a non-*_user doc from dereferencing assigned_*_codes — T-105-03). Never drop a guard.
  - Do NOT widen admin roles. operations_admin must NOT gain service_tasks Tier-1 authority and
    services_admin must NOT gain project_tasks Tier-1 authority (admins stay department-scoped).
  - Symmetry: the diff for project_tasks and the diff for service_tasks must be structural mirrors
    (services_user↔operations_user, isAssignedToProject↔isAssignedToService).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -c "s=open('firestore.rules',encoding='utf-8').read(); o=s.count('{'); c=s.count('}'); print('braces',o,c,'OK' if o==c else 'MISMATCH'); assert o==c"</automated>
    Manual cross-read: in match /project_tasks, `grep -n \"services_user\"` shows it added to create+update-tier1+tier2+delete; in match /service_tasks, `grep -n \"operations_user\"` shows the mirror. Confirm every new term is preceded by its isRole(...) guard.
  </verify>
  <done>project_tasks admits assigned services_user on create/update(both tiers)/delete; service_tasks admits assigned operations_user identically; all isRole guards intact; braces balanced.</done>
</task>

<task type="auto">
  <name>Task 2: Symmetric member-write on project/service lifecycle field-masks + the 6 journal subcollections (both sides)</name>
  <files>firestore.rules</files>
  <action>
projects.update assigned-member branch (lines 220-246): the branch currently reads
`isRole('operations_user') && request.auth.uid in resource.data.personnel_user_ids &&
request.resource.data.diff(resource.data).affectedKeys().hasOnly([...big field-mask...])`.
Make the ROLE check admit services_user too: change the leading `isRole('operations_user')` to
`(isRole('operations_user') || isRole('services_user'))`. Keep the `request.auth.uid in
resource.data.personnel_user_ids` member check and the ENTIRE affectedKeys allow-list verbatim
(project_status, updated_at, loss_reason, status_changed_at, last_activity_at, all the doc-url /
DLP / retention / collection_tranches keys). Do NOT touch the admin/finance branches around it.

services.update (lines 572-575): currently
`hasRole(['super_admin','services_admin']) || (isRole('services_user') &&
isAssignedToService(resource.data.service_code)) || (finance retention field-mask)`.
ADD an OR term mirroring projects: `(isRole('operations_user') &&
request.auth.uid in resource.data.personnel_user_ids)`.
IMPORTANT ASYMMETRY TO PRESERVE: services.update's services_user arm uses isAssignedToService
(code-based, unrestricted full update — services has no field-mask on the member arm today). To
keep the security posture identical for the cross-dept member, gate the new operations_user arm
the SAME way the native services_user arm is gated. Use isAssignedToService(resource.data.service_code)
for the operations_user arm too (NOT personnel_user_ids) so the cross-dept member gets EXACTLY the
same authority a native services_user has — no more, no less. (assigned_service_codes is populated
for operations_user by syncServicePersonnelToAssignments, so the data backs this; and the
isRole('operations_user') guard short-circuits before the isAssignedToService deref.)
  → Net services.update member arms: `(isRole('services_user') && isAssignedToService(resource.data.service_code)) || (isRole('operations_user') && isAssignedToService(resource.data.service_code))`.

Journal subcollections — open the cross-dept assigned member on BOTH sides:
  projects/{projectId} subcollections (lines 256-319):
    - audit_log.create (280-281): add `|| (isRole('services_user') && request.auth.uid in get(/databases/$(database)/documents/projects/$(projectId)).data.personnel_user_ids)` alongside the existing operations_user get().
    - progress_updates.update (304-307): the membership term `request.auth.uid in get(.../projects/$(projectId)).data.personnel_user_ids` is ALREADY role-agnostic (no isRole gate) — leave as-is (any project member, incl. an assigned services_user, already qualifies). Confirm and do NOT narrow it.
    - activity_entries.create / issues.create / issues.update / progress_updates.create are `isActiveUser()` — already open; do NOT change.
    - edit_history.create (261): roles list — leave UNCHANGED (admin/finance append-only audit; not a member-write path in scope).
    - baselines: admin-only; leave UNCHANGED.
  services/{serviceId} subcollections (lines 581-623):
    - audit_log.create (592-593): add `|| (isRole('operations_user') && request.auth.uid in get(/databases/$(database)/documents/services/$(serviceId)).data.personnel_user_ids)` alongside the existing services_user get().
    - progress_updates.update (610-613): the personnel_user_ids term is already role-agnostic — leave as-is; confirm an assigned operations_user qualifies.
    - activity_entries / issues / progress_updates create are isActiveUser() — leave.
    - edit_history.create (583): leave UNCHANGED.

Only audit_log on each side has an isRole-gated member branch that needs the mirror term added.
Everything else is either already role-agnostic (personnel_user_ids without isRole, or isActiveUser)
or intentionally out of scope (edit_history, baselines).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -c "s=open('firestore.rules',encoding='utf-8').read(); o=s.count('{'); c=s.count('}'); print('braces',o,c); assert o==c"</automated>
    Manual: confirm projects.update leading role check now reads `(isRole('operations_user') || isRole('services_user'))` with the field-mask UNCHANGED; services.update has a new `isRole('operations_user') && isAssignedToService(...)` arm; projects audit_log.create has a services_user get() arm and services audit_log.create has an operations_user get() arm (structural mirror).
  </verify>
  <done>Lifecycle field-mask updates and audit_log creates admit the cross-department assigned member symmetrically; the operations_user service arm grants exactly the native services_user authority (isAssignedToService, same as the mirror); all other journal subcollection rules unchanged; braces balanced.</done>
</task>

<task type="auto">
  <name>Task 3: Symmetric proposals Branch B (create + update) for the cross-department assigned member</name>
  <files>firestore.rules</files>
  <action>
The proposals create+update rules already implement department-aligned Branch B for BOTH
operations_user (parent_collection 'projects') and services_user (parent_collection 'services').
Parity here means: a services_user assigned to a PROJECT may drive a project-parented proposal,
and an operations_user assigned to a SERVICE may drive a service-parented proposal — WITHOUT
breaking parent_collection correctness (the get() must target the correct parent collection).

create (lines 856-874): there are two Branch-B arms today —
  (i) operations_user + parent_collection=='projects' + get(projects/<project_id>).personnel_user_ids
  (ii) services_user + parent_collection=='services' + get(services/<project_id>).personnel_user_ids
Add the two CROSS arms so the member check is role-agnostic for *_user while keeping the
get() pointed at the collection named by parent_collection:
  (iii) services_user + parent_collection=='projects' + get(projects/<project_id>).personnel_user_ids + created_by==auth.uid
  (iv) operations_user + parent_collection=='services' + get(services/<project_id>).personnel_user_ids + created_by==auth.uid
Each new arm MUST keep `request.resource.data.created_by == request.auth.uid` (anti-impersonation)
and use `request.resource.data.get('parent_collection','projects')` exactly as the existing arms do.
SIMPLIFICATION NOTE: arms (i)+(iii) are now "role in {operations_user,services_user} AND
parent_collection=='projects' AND in projects.personnel_user_ids"; (ii)+(iv) the services mirror.
You MAY refactor the four arms into two role-set arms
  `((isRole('operations_user') || isRole('services_user')) && <parent=='projects'> && <projects get()>)`
  `((isRole('operations_user') || isRole('services_user')) && <parent=='services'> && <services get()>)`
PROVIDED the parent_collection→get()-collection pairing stays exact (projects parent ⇒ projects
get; services parent ⇒ services get) and created_by pinning is preserved on both.

update Branch 2 (lines 898-921): the attached-member sub-condition currently ORs
  - operations_user + (parent_collection absent or 'projects') + get(projects/<project_id>).personnel_user_ids
  - services_user + parent_collection=='services' + get(services/<project_id>).personnel_user_ids
Mirror identically: admit the cross arms (services_user on a 'projects' parent via projects get;
operations_user on a 'services' parent via services get). Keep the 15-key affectedKeys allow-list,
the created_by/created_at immutability checks, and the top-level pending_internal attachment gate
ALL verbatim. The default-when-absent rule ('projects') must continue to apply only to the
projects-parented arm.

HARD CONSTRAINTS:
  - parent_collection correctness is NON-NEGOTIABLE: a 'projects' proposal must get() the projects
    doc; a 'services' proposal must get() the services doc. Never let a services-parented proposal
    authorize via a projects get() or vice versa.
  - Preserve created_by pinning (create) and created_by/created_at immutability (update).
  - Do NOT alter the admin branches (Branch A / Branch 1) or the delete rule (super_admin only).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -c "s=open('firestore.rules',encoding='utf-8').read(); o=s.count('{'); c=s.count('}'); print('braces',o,c); assert o==c"</automated>
    Manual: in match /proposals, confirm both create and update Branch B admit operations_user AND services_user for EACH parent_collection value, and that each arm's get() targets the collection matching its parent_collection literal. Confirm created_by pinning + the 15-key update mask are intact.
  </verify>
  <done>A services_user assigned to a project can create/update a project-parented proposal and an operations_user assigned to a service can create/update a service-parented proposal; parent_collection→get() pairing exact; impersonation/immutability guards preserved; braces balanced.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Deploy firestore.rules to DEV for compile-check</name>
  <what-built>firestore.rules now opens cross-department member writes symmetrically (project_tasks⇄service_tasks, lifecycle field-masks, audit_log, proposals Branch B). Rules are compile-checkable only at deploy time (Firestore emulator needs Java, absent here).</what-built>
  <how-to-verify>
1. From the repo root run: `firebase deploy --only firestore:rules --project dev`
   (CLI active project is PROD — the `--project dev` flag is REQUIRED; deploying without it
   would push to production. See project memory firebase-dev-prod-deploy.)
2. Confirm the deploy reports "compiled successfully" with no rule errors.
   A compile error here means a malformed predicate (e.g. unbalanced parens, a typo'd field) —
   fix in firestore.rules and redeploy before proceeding.
  </how-to-verify>
  <resume-signal>Reply "deployed" once DEV reports a successful compile, or paste the compile error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore (security rules) | Untrusted client write requests cross here; rules are the only server-side authority (no Cloud Functions). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-KG0-01 | Elevation of Privilege | project_tasks/service_tasks new member arms | mitigate | Every cross-dept arm gated by `isRole('*_user') && isAssignedTo*(code)`; admin Tier-1 authority NOT widened; short-circuit guard prevents assigned_*_codes deref on non-matching docs. |
| T-KG0-02 | Tampering | proposals Branch B cross arms | mitigate | parent_collection→get()-collection pairing kept exact; created_by pinned to auth.uid on create; created_by/created_at immutable on update; 15-key field-mask unchanged so loss_reason/attachment_storage_path remain unwritable by members. |
| T-KG0-03 | Elevation of Privilege | services.update cross-dept operations_user arm | mitigate | New arm grants EXACTLY the native services_user authority (same isAssignedToService gate) — no broader. Admin/finance arms untouched. |
| T-KG0-04 | Spoofing | assigned_*_codes / personnel_user_ids dereference | accept | Backing data (assigned codes, personnel arrays) is written only by admin-authored assignment flows (syncPersonnelToAssignments / syncServicePersonnelToAssignments); a member cannot self-assign because users.update self-edit is field-restricted (role/status/codes admin-only, lines 145-153, UNCHANGED). |
| T-KG0-05 | Elevation of Privilege | out-of-scope finance/procurement gates | accept | pos/prs/suppliers/collectibles/billing_requests/clients gates explicitly NOT modified; verified byte-identical post-edit. |
</threat_model>

<verification>
- `python -c "s=open('firestore.rules',encoding='utf-8').read(); assert s.count('{')==s.count('}')"` passes (braces balanced).
- DEV deploy compiles with zero rule errors (Task 4 checkpoint).
- Cross-read confirms symmetry: every projects-side cross-dept term has a structural mirror on the services side.
- Out-of-scope gates (pos.create / prs.create / suppliers / clients / collectibles / billing_requests / mrfs.create / users / role_templates / invitation_codes) are unchanged from the pre-plan file.
</verification>

<success_criteria>
- Assigned cross-department *_user members can perform the same member writes their native-department counterparts can, on tasks / lifecycle / audit_log / proposals, in BOTH directions.
- No admin role gained cross-department authority.
- No isRole short-circuit guard removed; no out-of-scope gate touched.
- Rules deploy to DEV successfully.
</success_criteria>

<output>
After completion, create `.planning/quick/260627-kg0-cross-dept-assignment-parity/260627-kg0-01-SUMMARY.md`.
Record: which rule blocks gained cross-dept arms, confirmation of symmetry, the DEV deploy result, and a note that PROD `firebase deploy --only firestore:rules` joins the standing v3.3 → main rules-deploy debt.
</output>
