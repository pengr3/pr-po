---
phase: quick-260627-kg0
plan: 01
status: code-complete-deploy-pending
wave: 1
commit: 7ee3c04f
files_modified:
  - firestore.rules
completed: 2026-06-27
---

# Quick 260627-kg0 Plan 01 — firestore.rules symmetric cross-dept member writes

## Status

Code COMPLETE and committed (`7ee3c04f`). **DEV deploy (Task 4) is PENDING — user action.**
No rules are live yet; this is reversible git work on `claude/romantic-saha-38647e`.

## Rule blocks that gained cross-department arms (all symmetric, 16 markers)

| Block | Projects side (now admits) | Services side (now admits) |
|-------|----------------------------|----------------------------|
| `*_tasks` create | services_user + isAssignedToProject | operations_user + isAssignedToService |
| `*_tasks` update Tier 1 | services_user + isAssignedToProject | operations_user + isAssignedToService |
| `*_tasks` update Tier 2 (progress) | services_user in assignees | operations_user in assignees |
| `*_tasks` delete | services_user + isAssignedToProject | operations_user + isAssignedToService |
| lifecycle field-mask update | services_user (via `||` on the role check; personnel_user_ids member check + field-mask verbatim) | operations_user (via `isAssignedToService`, EXACTLY native services_user authority) |
| `audit_log` create | services_user + projects personnel get() | operations_user + services personnel get() |
| `proposals` create Branch B | role-set `(operations_user \|\| services_user)` per parent_collection | same, services-parented arm |
| `proposals` update Branch 2 | role-set per parent_collection (default-when-absent kept on projects arm only) | same, services-parented arm |

## Symmetry confirmation

Every projects-side opening has its structural mirror on the services side
(`services_user`↔`operations_user`, `isAssignedToProject`↔`isAssignedToService`,
`projects` get()↔`services` get()). 16 `Quick 260627-kg0` markers, balanced.

## Invariants preserved (verified)

- **Short-circuit guards (T-105-03):** every new arm leads with `isRole('*_user')` before any
  `isAssignedTo*` / `personnel_user_ids` / `assignees` dereference. None removed.
- **parent_collection correctness:** proposals `'projects'` parent authorizes via the projects
  doc; `'services'` parent via the services doc. Role-set widening did not cross the pairing.
- **Anti-impersonation / immutability:** `created_by == request.auth.uid` on create; `created_by`
  + `created_at` immutability and the 15-key field-mask on update — all verbatim.
- **No admin widening (D-1):** operations_admin gained no service_tasks Tier-1; services_admin
  gained no project_tasks Tier-1. services.update operations_user arm grants exactly the native
  services_user authority (same `isAssignedToService` gate), no broader.
- **Out-of-scope gates byte-unchanged:** `pos.create` (super_admin/finance, line ~457),
  `prs.create`, `mrfs.create` (line ~391, full role list), suppliers, clients, collectibles,
  billing_requests, users, role_templates, invitation_codes.

## Verify gate results

- brace balance: `{`=79 `}`=79 OK
- paren delta (code lines, comments stripped): 0 OK
- `Quick 260627-kg0` markers: 16 (expected)
- `git diff --stat`: only firestore.rules (50 insertions, 15 deletions)
- Firestore emulator compile-check needs Java (absent locally) → the authoritative compile gate is
  the DEV deploy (Task 4, pending user).

## Deferred / debt

- **PROD `firebase deploy --only firestore:rules`** joins the standing v3.3 → main rules-deploy debt
  (do NOT deploy to prod until the full task UATs green; CLI active project is PROD — use
  `--project dev` for DEV per project memory firebase-dev-prod-deploy).

## Task 4 checkpoint (BLOCKING — user)

Run from repo root: `firebase deploy --only firestore:rules --project dev`
Expect "compiled successfully". On a compile error, fix firestore.rules and redeploy before Wave 2.
