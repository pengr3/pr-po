---
phase: quick-260627-kg0
plan: 03
type: execute
wave: 3
depends_on: ["260627-kg0-01", "260627-kg0-02"]
files_modified:
  - app/views/project-detail.js
  - app/views/service-detail.js
  - app/proposal-modal.js
  - app/views/mrf-form.js
  - app/views/projects.js
  - app/views/home.js
  - app/seed-roles.js
autonomous: false
requirements: [KG0-UI-DRIVE-PARITY, KG0-UNIFIED-NAV]

must_haves:
  truths:
    - "On a PROJECT detail page, an assigned services_user can drive every lifecycle gate an assigned operations_user can drive (same gate set, same assigned-member check)."
    - "On a SERVICE detail page, an assigned operations_user can drive every lifecycle gate an assigned services_user can drive, INCLUDING the services-side Completion-gate exclusion (the cross-dept member gets EXACTLY the native member's authority, not more)."
    - "The proposal modal canDrive is granted to the assigned member regardless of whether their role is operations_user or services_user, while parent_collection department-alignment is preserved (a projects proposal still requires the projects-side member; a services proposal the services-side member); approve stays admin-only."
    - "The Create-MRF form shows BOTH the project and service pickers to any star_user who has assignments in that area (the department wall at mrf-form.js:404-405 no longer hides a department by role literal)."
    - "The Projects list no longer force-empties for services roles; a services_user sees the projects they are assigned to (driven by getAssignedProjectCodes), and home surfaces assigned work across BOTH departments for star_user roles."
    - "Every UI change is mirrored in both directions (services_user->projects AND operations_user->services); admin roles (operations_admin/services_admin) keep their existing department scoping."
    - "role_templates grant the cross-department member-edit affordance to both star_user roles (seed source updated AND the live Firestore docs patched), so the unified nav and in-page edit controls actually render."
  artifacts:
    - path: "app/views/project-detail.js"
      provides: "_canAdvanceProjectStatus admits assigned services_user alongside operations_user; project-member role-literal affordances mirrored."
      contains: "services_user"
    - path: "app/views/service-detail.js"
      provides: "_canAdvanceServiceStatus admits assigned operations_user alongside services_user, preserving the Completion-gate exclusion; service-member affordances mirrored."
      contains: "operations_user"
    - path: "app/proposal-modal.js"
      provides: "Role-agnostic assigned-member canDrive while keeping parent_collection department-alignment."
      contains: "parent_collection"
    - path: "app/views/mrf-form.js"
      provides: "Parity-driven project+service picker visibility (department wall removed for star_user)."
      contains: "showProjects"
    - path: "app/views/projects.js"
      provides: "applyFilters no longer force-empties for services roles; services_user sees assigned projects via getAssignedProjectCodes."
      contains: "getAssignedProjectCodes"
    - path: "app/views/home.js"
      provides: "getDashboardMode surfaces both departments for star_user; filterProposalsForUser includes cross-dept assigned proposals."
      contains: "getDashboardMode"
    - path: "app/seed-roles.js"
      provides: "operations_user.tabs.services.edit and services_user.tabs.projects.edit set true (cross-dept member parity)."
      contains: "services_user"
  key_links:
    - from: "_canAdvanceProjectStatus / _canAdvanceServiceStatus"
      to: "cross-department assigned member"
      via: "add the mirror star_user role to the SAME assigned-member (personnel_user_ids.includes(uid)) branch"
      pattern: "personnel_user_ids"
    - from: "role_templates live Firestore docs"
      to: "unified nav + member-edit affordance rendering (auth.js permissions gate)"
      via: "one-time console/script patch of operations_user + services_user tab edit flags matching the seed-roles.js source change"
      pattern: "tabs"
---

<objective>
Sweep the remaining UI role-literal gates so capability follows ASSIGNMENT, not department role,
for the two star_user roles — symmetrically in both directions. After this plan an assigned
services_user acts on a project exactly as an assigned operations_user does, an assigned
operations_user acts on a service exactly as an assigned services_user does, the Create-MRF form
and Projects list stop hiding a department by role literal, and the unified nav (already granted in
role_templates source) is reflected in the live role_templates docs. Department admins remain
department-scoped (out of scope per CONTEXT D-1).

Purpose: Plans 01 (rules) and 02 (helpers + procurement union) already permit and scope the
cross-department work; this plan removes the front-end gates that would otherwise hide the buttons
and lists, completing the user-visible parity.

Output: Edited project-detail.js, service-detail.js, proposal-modal.js, mrf-form.js, projects.js,
home.js, seed-roles.js + a live role_templates data patch (human checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260627-kg0-cross-dept-assignment-parity/260627-kg0-CONTEXT.md
@CLAUDE.md

@app/views/project-detail.js
@app/views/service-detail.js
@app/proposal-modal.js
@app/views/mrf-form.js
@app/views/projects.js
@app/views/home.js
@app/seed-roles.js

<interfaces>
<!-- Load-bearing role-literal gate sites to convert (VERIFY line refs; sweep for any the grep finds):
  project-detail.js _canAdvanceProjectStatus (~2858-2868): admins always; role === 'operations_user'
      + personnel_user_ids.includes(uid). NO Completed exclusion on the project side.
  service-detail.js _canAdvanceServiceStatus (~2213-2223): admins always; targetStatus === 'Completed'
      returns false for non-admins; role === 'services_user' + personnel_user_ids.includes(uid).
  proposal-modal.js _isCallerAttachedToProposalParent (~165-198): super_admin all; else branch by
      parentCollection — 'projects' admits operations_admin(approve+drive) / operations_user(drive if
      assigned); 'services' admits services_admin / services_user(drive if assigned).
  mrf-form.js (~404-405): showProjects = role in [...,'operations_admin','operations_user'];
      showServices = role in [...,'services_admin','services_user'].
  projects.js applyFilters (~800-816): if (role === 'services_user' || role === 'services_admin')
      scopedProjects = [] then the getAssignedProjectCodes path for everyone else.
  home.js getDashboardMode (~46-51): star_user -> single dept; filterProposalsForUser (~76-86):
      star_user -> single-dept proposal filter.
  seed-roles.js: operations_user.tabs.services.edit=false (~68); services_user.tabs.projects.edit=false (~107).
      Tab ACCESS is ALREADY true on both cross-dept tabs — only the member-edit flag needs flipping.

  KEY DISTINCTION for the two _canAdvance gates: ADD the OTHER star_user role to the SAME assigned-
  member branch so the cross-dept member inherits that side's EXACT rule. On services the Completion
  exclusion stays (operations_user on a service is also Completion-blocked — matches native
  services_user). On projects there is no exclusion (services_user on a project drives all gates, same
  as operations_user). Do NOT flatten the two gates into one shared rule — widen the role check only.

  NAV NOTE: nav visibility is permission-driven (auth.js:402-436 reads role_templates tabs[route].access).
  Both star_user roles ALREADY have access:true on the cross-dept tab in seed-roles.js, so the nav link
  already shows once the live role_templates doc matches the seed. This plan flips only the member-edit
  flag in source + patches the live docs; it does NOT edit index.html nav or add a router guard. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Detail-page drive parity — widen both _canAdvance gates + mirror project/service-member role-literal affordances</name>
  <files>app/views/project-detail.js, app/views/service-detail.js</files>
  <behavior>
    - project-detail _canAdvanceProjectStatus: super_admin/operations_admin -> true (unchanged);
      operations_user assigned -> true (unchanged); services_user assigned (uid in
      project.personnel_user_ids) -> true (NEW); other -> false.
    - service-detail _canAdvanceServiceStatus: super_admin/services_admin -> true (unchanged);
      targetStatus==='Completed' and not admin -> false (unchanged, applies to BOTH star_user);
      services_user assigned -> true for non-Completed (unchanged); operations_user assigned (uid in
      service.personnel_user_ids) -> true for non-Completed (NEW); other -> false.
  </behavior>
  <action>
project-detail.js _canAdvanceProjectStatus (~2858-2868): the branch reads
  if (role === 'operations_user') { const ids = ...; return ids.includes(currentUser.uid); }
Widen the role test to also accept services_user:
  if (role === 'operations_user' || role === 'services_user') {
    const ids = Array.isArray(project.personnel_user_ids) ? project.personnel_user_ids : [];
    return ids.includes(currentUser.uid);
  }
This grants an assigned services_user the identical project-gate authority an assigned
operations_user has (no Completed exclusion on the project side — unchanged).

service-detail.js _canAdvanceServiceStatus (~2213-2223): KEEP the
  if (targetStatus === 'Completed') return false;
line ABOVE the member branch so it applies to the new operations_user member too (Completion stays
services_admin-only, D-04). Widen the member role test:
  if (role === 'services_user' || role === 'operations_user') {
    const ids = Array.isArray(service.personnel_user_ids) ? service.personnel_user_ids : [];
    return ids.includes(currentUser.uid);
  }

Also sweep BOTH files for the other in-scope MEMBER-capability role literals and mirror them (do NOT
touch admin-only gates, copy/label maps, or status-color maps):
  - project-detail.js LOSS_ASSIGNED_ROLES (~2773 = ['operations_user']) -> add 'services_user'
    (an assigned services_user driving Mark-as-Loss writes loss_reason via the same field-mask the
    Plan-01 rules now allow for both star_user roles).
  - service-detail.js LOSS_ASSIGNED_ROLES (~2511 = ['services_user']) -> add 'operations_user'.
  - The assignedRoles = ['operations_user','services_user'] arrays at project-detail.js ~2447 and
    service-detail.js ~1856 already list BOTH roles — confirm and leave unchanged.
  - service-detail.js save/toggle guards at ~1235 and ~1364 (['super_admin','services_admin',
    'services_user']): these govern service-doc field writes. The Plan-01 services.update rule now
    admits an assigned operations_user, so add 'operations_user' to BOTH arrays so the client guard
    does not pre-block it. (No equivalent services_user literal exists in project-detail save/toggle,
    so the mirror is N/A on the projects side.)

Preserve listener lifecycle and DOM-by-class selection per CLAUDE.md.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node --check app/views/project-detail.js</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && node --check app/views/service-detail.js</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c "role === 'operations_user' || role === 'services_user'" app/views/project-detail.js</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c "role === 'services_user' || role === 'operations_user'" app/views/service-detail.js</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c "targetStatus === 'Completed') return false" app/views/service-detail.js</automated>
    Both files node --check pass; project-detail shows the widened operations||services member test (count 1); service-detail shows the widened services||operations member test (count 1); the services Completion-exclusion line is still present (count 1).
  </verify>
  <done>Assigned services_user drives all project gates; assigned operations_user drives all non-Completed service gates; Completion stays services_admin-only; LOSS + service save/toggle member arrays mirrored; admin/label/color code untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Proposal-modal drive parity (role-agnostic assigned member; parent_collection alignment + admin-only approve preserved)</name>
  <files>app/proposal-modal.js</files>
  <behavior>
    - super_admin -> canApprove+canDrive (unchanged).
    - parent_collection 'projects': operations_admin -> approve+drive (unchanged); an ASSIGNED member
      who is operations_user OR services_user -> drive only (canDrive true, canApprove false).
    - parent_collection 'services': services_admin -> approve+drive (unchanged); an ASSIGNED member
      who is services_user OR operations_user -> drive only.
    - A non-assigned star_user -> neither.
  </behavior>
  <action>
_isCallerAttachedToProposalParent (~165-198): the member drive arms are currently role-locked per
department —
  in the parentCollection === 'projects' block:  else if (role === 'operations_user' && isAssigned)
  in the parentCollection === 'services' block:  else if (role === 'services_user' && isAssigned)
Make the MEMBER drive arm role-agnostic for star_user while keeping the department-admin approve arms
and the parentCollection branch structure intact:
  - projects block member arm ->
      else if ((role === 'operations_user' || role === 'services_user') && isAssigned) { canDrive = true; }
    (canApprove stays false). The role === 'operations_admin' approve arm above it is UNCHANGED.
  - services block member arm ->
      else if ((role === 'services_user' || role === 'operations_user') && isAssigned) { canDrive = true; }
    The role === 'services_admin' approve arm is UNCHANGED.
isAssigned already = caller uid in _parentDocCache.doc.personnel_user_ids (the parent doc named by
parent_collection), so department-alignment is structurally preserved; only the role literal on the
member arm is widened. Approve authority is NOT extended to the cross-dept member.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node --check app/proposal-modal.js</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c "(role === 'operations_user' || role === 'services_user') && isAssigned" app/proposal-modal.js</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c "(role === 'services_user' || role === 'operations_user') && isAssigned" app/proposal-modal.js</automated>
    node --check passes; both widened member arms present (count 1 each); admin approve arms untouched.
  </verify>
  <done>An assigned member of either star_user role can drive a proposal whose parent_collection matches their assignment; approve stays admin-only; parent_collection alignment preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create-MRF picker parity + Projects list scope fix + home dual-department surfacing</name>
  <files>app/views/mrf-form.js, app/views/projects.js, app/views/home.js</files>
  <behavior>
    - mrf-form: a star_user with assignments in the other department sees that department's picker.
      Concretely, both pickers become available to both star_user roles (capability is then scoped by
      assignment downstream: the project/service load queries already filter by assigned codes, and
      saveNewMRF writes only the selected project/service). Single-dept admins keep their existing
      single-picker view.
    - projects.js applyFilters: a services_user no longer gets an empty Projects list; they see the
      projects in getAssignedProjectCodes() (their cross-dept assignments). operations_user unchanged.
    - home.js: a star_user sees BOTH the Projects and Services dashboard cards/content, and the home
      Proposals list includes cross-department proposals they are assigned to.
  </behavior>
  <action>
mrf-form.js (~404-405): the department wall is
  const showProjects = ['super_admin','finance','procurement','operations_admin','operations_user'].includes(role);
  const showServices = ['super_admin','finance','procurement','services_admin','services_user'].includes(role);
Per CONTEXT (parity for star_user only; admins stay department-scoped), add the OTHER star_user role
to each list so both pickers are available to both star_user roles:
  showProjects list += 'services_user'   (a services_user assigned to a project can file a project MRF)
  showServices list += 'operations_user' (an operations_user assigned to a service can file a service MRF)
Leave operations_admin / services_admin exactly where they are (single-dept). The downstream
loadProjects / loadServices already scope by assignment, and the existing label/placeholder logic
(showProjects/showServices) will now show the combined "Project / Service" affordance for star_user
— which is the intended unified picker. Do NOT change the DOM ids or saveNewMRF read-back logic.

projects.js applyFilters (~800-816): remove the hard force-empty for services roles. The block
  if (role === 'services_user' || role === 'services_admin') { scopedProjects = []; }
  else { const assignedCodes = getAssignedProjectCodes(); ... }
must become the single getAssignedProjectCodes-driven path for everyone (delete the services-role
special case and its else wrapper):
  const assignedCodes = window.getAssignedProjectCodes?.();
  if (assignedCodes !== null) {
    scopedProjects = allProjects.filter(p => !p.project_code || assignedCodes.includes(p.project_code));
  } else {
    scopedProjects = allProjects;
  }
RATIONALE: after Plan 02, getAssignedProjectCodes() returns a services_user's assigned project codes
(so they see only assigned projects — correct), [] for a star_user with no project assignments (empty
list — correct), and null for services_admin (so services_admin now sees all projects via their
view-only project tab; that matches the role_template grant and is acceptable — admins are not the
target of scoping). Keep the existing legacy `!p.project_code ||` defensive term for THIS list (it is
a portfolio list, not an MRF leak surface; the 260615-nlj no-leak rule applies to procurement MRF
predicates, not the projects portfolio — do not conflate them).

home.js getDashboardMode (~46-51): star_user currently collapses to a single department. Change so
both star_user roles return 'both' (surface assigned work across departments per CONTEXT D-4):
  if (role === 'operations_admin') return 'projects';
  if (role === 'services_admin') return 'services';
  return 'both';   // super_admin, finance, procurement, operations_user, services_user, unknown
(Admins keep single-dept; star_user + the existing 'both' roles get both cards. The Procurement card
is already always shown.)

home.js filterProposalsForUser (~76-86): a star_user currently sees only their own department's
proposals. For "surface assigned work across both departments", widen both star_user roles to see
proposals from BOTH parent_collections:
  if (role === 'super_admin') return allProposals;
  if (role === 'operations_admin') return allProposals.filter(p => (p.parent_collection||'projects')==='projects');
  if (role === 'services_admin')  return allProposals.filter(p => (p.parent_collection||'projects')==='services');
  if (role === 'operations_user' || role === 'services_user') return allProposals; // both depts; per-proposal drive still gated in the modal
  return [];
(Admins stay department-filtered. The proposal MODAL still gates drive/approve per assignment +
parent_collection, so showing both departments here is a visibility convenience, not an authority grant.)

CLAUDE.md: preserve render/init/destroy lifecycle, window-function registration, and DOM-by-class
selection. These are predicate/role-list edits inside existing functions.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node --check app/views/mrf-form.js</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && node --check app/views/projects.js</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && node --check app/views/home.js</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c "scopedProjects = \[\]" app/views/projects.js</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c "if (role === 'operations_user' || role === 'services_user') return allProposals" app/views/home.js</automated>
    All three files node --check pass; the projects force-empty `scopedProjects = []` is gone (count 0); home filterProposalsForUser admits both star_user roles (count 1); manual read confirms both mrf-form picker lists gained the mirror star_user role and getDashboardMode returns 'both' for star_user.
  </verify>
  <done>Both MRF pickers available to both star_user roles (assignment-scoped downstream); services_user sees assigned projects in the Projects list; home shows both department cards + cross-dept proposals for star_user; admins unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: role_templates source — flip the cross-department member-edit flags</name>
  <files>app/seed-roles.js</files>
  <behavior>
    - seed-roles.js operations_user.tabs.services.edit becomes true (was false).
    - seed-roles.js services_user.tabs.projects.edit becomes true (was false).
    - All access flags and every other role/tab entry are UNCHANGED (tab ACCESS was already true on
      both cross-dept tabs).
  </behavior>
  <action>
In app/seed-roles.js, in the operations_user permissions.tabs block (~line 68), change
  services: { access: true, edit: false }  ->  services: { access: true, edit: true }
In the services_user permissions.tabs block (~line 107), change
  projects: { access: true, edit: false }  ->  projects: { access: true, edit: true }
Do NOT modify operations_admin / services_admin (department admins keep their scoping), and do NOT
touch any access flag or any procurement_* sub-tab key. This makes the SEED match the assignment-
driven parity; the LIVE role_templates docs are patched in Task 5 (the running app reads the live
Firestore docs, not this seed, except on a fresh seed run).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node --check app/seed-roles.js</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c "services: { access: true, edit: true }" app/seed-roles.js</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c "projects: { access: true, edit: true }" app/seed-roles.js</automated>
    node --check passes; the operations_user services-edit:true line and the services_user projects-edit:true line are present (the services:edit:true count rises to include operations_user's row; the projects:edit:true count rises to include services_user's row).
  </verify>
  <done>seed-roles.js grants cross-department member-edit to both star_user roles; admin rows and all access flags unchanged.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Patch live role_templates docs to match seed (super_admin)</name>
  <what-built>seed-roles.js now grants the cross-department member-edit flag to both star_user roles. The RUNNING app reads the LIVE role_templates Firestore docs, which still carry the old edit:false values until patched. This is the known post-deploy migration gap (project memory: v40_post_deploy_migration_gaps / role_templates sub-tab keys need a browser-console patch).</what-built>
  <how-to-verify>
1. Sign in as super_admin (only super_admin can write role_templates per firestore.rules:168).
2. Open the browser DevTools console on the running app (DEV: clmc-procurement-dev) and patch the two
   live role_templates docs to match the seed:
     import the Firestore handles already on window, or run from the app context:
       await window.firebase.updateDoc(window.firebase.doc(db,'role_templates','operations_user'),
         { 'permissions.tabs.services.edit': true });
       await window.firebase.updateDoc(window.firebase.doc(db,'role_templates','services_user'),
         { 'permissions.tabs.projects.edit': true });
     (Use whatever updateDoc/doc handles this app exposes — see app/firebase.js exports. If a seed
      script path is simpler, re-running the seeder for these two roles is equivalent.)
3. Reload as a services_user and confirm the Projects nav link + in-page project member-edit
   affordances render; reload as an operations_user and confirm the Services nav link + service
   member-edit affordances render.
  </how-to-verify>
  <resume-signal>Reply "patched" once both live role_templates docs show edit:true on the cross-dept tab and the nav/affordances render, or describe what is still hidden.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: End-to-end cross-department parity UAT (both directions + no-regression)</name>
  <what-built>Full cross-department assignment parity is now wired end-to-end (rules from Plan 01, helpers + procurement union from Plan 02, UI gates + nav from this plan). Verify on the running DEV app.</what-built>
  <how-to-verify>
Prereq: have (a) a services_user assigned to at least one PROJECT (uid in that project's
personnel_user_ids; assigned_project_codes populated), and (b) an operations_user assigned to at
least one SERVICE. If none exist, an admin assigns them via the existing personnel UI first (the
sync helpers populate assigned_*_codes).

As the services_user (cross-dept to a project):
  1. Nav shows Projects; the Projects list shows ONLY the assigned project(s), not all projects.
  2. Open the assigned project detail — you can drive its lifecycle gates (e.g. advance status,
     Mark as Loss). A NON-assigned project shows Access Denied.
  3. Create-MRF form offers the project picker; you can file a project MRF for the assigned project.
  4. In Procurement / MRF Records you see the assigned project's MRFs AND any service MRFs you are
     assigned to (the union) — and NOT uncoded/cross-department records you are not assigned to.
  5. Start/drive a proposal on the assigned project (drive yes, approve no).

As the operations_user (cross-dept to a service): mirror every step on the service side, and CONFIRM
the service Completion gate is NOT available to you (services_admin-only).

As an operations_user with NO service assignment and a services_user with NO project assignment:
confirm their visible records/lists are unchanged from before (no regression, no leak).
  </how-to-verify>
  <resume-signal>Reply "approved" if all parity + no-regression checks pass, or list the failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI gate -> user capability | These gates decide which buttons/lists a user SEES. They are defense-in-depth; the hard authority is firestore.rules (Plan 01). A too-open UI gate that the rules still deny yields a failed write, not a breach; a too-closed gate hides a legitimately-permitted action. |
| super_admin -> role_templates docs | Only super_admin may write role_templates (rules:168). The live-doc patch (Task 5) must be performed as super_admin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-KG0-09 | Elevation of Privilege | widened _canAdvance / proposal member arms | mitigate | Member arms only add the mirror star_user role and still require uid in personnel_user_ids; admin/approve authority NOT extended; the services Completion exclusion is preserved for the cross-dept member. firestore.rules (Plan 01) is the backstop. |
| T-KG0-10 | Information Disclosure | projects.js applyFilters services-role un-gating | mitigate | services_user is now scoped by getAssignedProjectCodes (assigned projects only); services_admin gets null=all-projects which matches its existing view-only project-tab grant (not a scoping target per D-1). No procurement MRF predicate touched, so the 260615-nlj invariant is unaffected. |
| T-KG0-11 | Tampering | role_templates live patch | accept | Patch is super_admin-only (rules-enforced) and only flips two member-edit booleans; access flags unchanged. |
| T-KG0-12 | Repudiation | home filterProposalsForUser widening | accept | Showing both departments' proposals on home is visibility only; the proposal modal + rules still gate every drive/approve/edit by assignment + parent_collection. |
</threat_model>

<verification>
- node --check passes on all 6 edited JS files (project-detail, service-detail, proposal-modal, mrf-form, projects, home) and seed-roles.js.
- Both _canAdvance gates show the widened two-role member test; the services Completion exclusion line is intact.
- proposal-modal member arms are role-agnostic for star_user; admin approve arms unchanged.
- mrf-form both picker lists include the mirror star_user role; projects.js force-empty removed; home returns 'both' for star_user and includes both depts in filterProposalsForUser.
- seed-roles.js cross-dept member-edit flags flipped; live role_templates docs patched (checkpoint).
- DEV browser UAT confirms parity in BOTH directions + no regression for single-dept star_user.
</verification>

<success_criteria>
- An assigned services_user has full project member capability; an assigned operations_user has full service member capability (minus the services-side Completion gate) — UI buttons, lists, MRF pickers, and proposals all reflect this.
- Single-department star_user users see no change (no regression, no leak).
- Every UI conversion is mirrored in both directions; admins stay department-scoped.
- Unified nav + member-edit affordances render after the live role_templates patch.
</success_criteria>

<output>
After completion, create `.planning/quick/260627-kg0-cross-dept-assignment-parity/260627-kg0-03-SUMMARY.md`.
Record: each role-literal site converted (with both-direction mirror confirmation), the seed-roles.js flag changes, the live role_templates patch result, the DEV UAT outcome, and an explicit note that PROD needs both the rules deploy (Plan 01 carry) AND the PROD role_templates patch.
</output>
