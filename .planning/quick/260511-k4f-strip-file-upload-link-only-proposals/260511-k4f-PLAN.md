---
quick_id: 260511-k4f
slug: strip-file-upload-link-only-proposals
description: Remove Firebase Storage file upload — link-only attachments in proposals
date: 2026-05-11
files_modified:
  - app/views/proposals.js
  - app/firebase.js
  - firebase.json
files_deleted:
  - storage.rules
---

# Quick Task 260511-k4f: Strip File Upload — Link-Only Attachments

## Objective
Remove all Firebase Storage file upload code from proposals.js. Attachment = link-only.
No Blaze plan required. Zero regression to state transitions, audit log, comms log text, or NOTIF-09/10.

## Tasks

### Task 1: proposals.js — remove storage import line
Remove line 19: `import { storage, ref, uploadBytes, getDownloadURL, deleteObject } from '../firebase.js';`

### Task 2: proposals.js — simplify main attachment widget HTML
Remove the radio group (Paste a link / Upload a file) + file input div.
Render URL input directly (no radio needed when there's only one kind).

### Task 3: proposals.js — delete _switchProposalAttachmentKind + simplify saveProposalAttachment + removeProposalAttachment
- Delete `_switchProposalAttachmentKind` function
- Remove `kind === 'file'` branch + `oldPathToDelete` + `deleteObject` calls from `saveProposalAttachment`
- Remove `wasFile` + `oldStoragePath` + `deleteObject` call from `removeProposalAttachment`
- Remove `window._switchProposalAttachmentKind` registration in init()
- Remove `delete window._switchProposalAttachmentKind` in destroy()

### Task 4: proposals.js — simplify comms log attachment widget + saveCommsEntry + _switchCommsAttachmentKind
- Replace 3-radio group (None/Paste a link/Upload a file) + file input in buildCommsLogSection with a single optional URL input
- Delete `_switchCommsAttachmentKind` function
- Simplify `saveCommsEntry`: remove `const kind = ...`, remove `kind === 'file'` branch, read URL directly
- Simplify `toggleAddCommsForm`: remove radio reset + fileBox references in close path
- Remove `window._switchCommsAttachmentKind` registration in init()
- Remove `delete window._switchCommsAttachmentKind` in destroy()

### Task 5: firebase.js — strip Storage SDK entirely
- Remove firebase-storage.js import block
- Remove `const storage = getStorage(app);`
- Remove `storage` from export line
- Remove `export { getStorage, ref, uploadBytes, getDownloadURL, deleteObject };`
- Remove `window.firebaseStorage = { ... };`

### Task 6: firebase.json + storage.rules — strip storage config
- Remove `"storage"` block from firebase.json
- Delete storage.rules file
