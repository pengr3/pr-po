---
context: spike
phase: collectibles-finance-revamp
spike: 027c-collectibles-table-redesign
status: pending_verification
last_updated: 2026-06-05T19:57:22Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_None identified._

<current_state>
Spike series 027a/027b/027c fully built, all design decisions locked. 027c spike.html now has the final design with all gap fixes applied. Awaiting user browser verification of the complete spike before marking VALIDATED and running `/gsd:spike --wrap-up`.

- **027a** (VALIDATED): 4-chip scorecard strip
- **027b** (VALIDATED, Variant A): Unified banner — pending review + approved-not-filed
- **027c** (PENDING VERIFICATION): Full table redesign — all iterations complete
</current_state>

<completed_work>
- Spike 027c built: 10-col → 5-col redesign (Project·Tranche / Collection Progress / Due·Urgency / Last Payment / Actions)
- Urgency left-border accents: critical/overdue/near-due/active/clean/done
- "Show/hide completed" toggle for fully paid rows
- Group by Project toggle added (accordion expand/collapse per project)
- Search box added (searches project, tranche, COLL ID, code)
- Dept filter: All / Projects only / Services only (replaces hardcoded project-name dropdown)
- Looker Studio-style date range picker: Today / Yesterday / This month / Last 7 days / Last 30 days / Fixed range / Any time
- Gap #1 fixed: `getApprovedNotFiled()` cross-references approved billing requests against existing COLLs — only shows unmatched ones in the banner
- Gap #2 fixed: `sortByUrgency()` applied in `getFiltered()` — critical→overdue→near-due→active→pending→done order; grouped view also sorts groups by worst urgency
- Gap #3 fixed: "Near due (≤7 days)" added to status filter dropdown
- Gap #4 fixed: dept filter + date range picker kept (not dropped)
- All three scenario controls (Real mix / Crisis / Mostly collected) wired through reset correctly
</completed_work>

<remaining_work>
1. **User verifies spike 027c in browser** — open `http://localhost:8000/.planning/spikes/027c-collectibles-table-redesign/spike.html`
   - Confirm 5-col flat view reads better than 10-col Before
   - Confirm urgency sort: Crisis scenario should show critical/overdue rows at top
   - Confirm Group by Project toggle works (flat ↔ grouped)
   - Confirm search filters across project name, tranche, COLL ID
   - Confirm dept filter (All / Projects / Services)
   - Confirm date picker: click "This month" filters to Jun items; "Fixed range" reveals From/To inputs
   - Confirm banner "Approved — File as Collectible" section shows only unmatched approved requests
2. **Record 027c verdict** in README.md + MANIFEST.md (VALIDATED or REJECTED)
3. **Mark all three 027 READMEs VALIDATED** (027a, 027b, 027c — all still say `verdict: PENDING`)
4. **Run `/gsd:spike --wrap-up`** to package the full 027 series into an implementation brief for the finance collectibles revamp phase
</remaining_work>

<decisions_made>
- Flat 5-col is the DEFAULT view; Group by Project is an optional toggle (not a separate tab) — flat for daily payment recording, grouped for portfolio overview
- "All Projects / Services" dropdown = dept filter only (All/Projects/Services) — search handles project name lookup
- Date range = Looker Studio-style unified picker with presets, not raw date inputs
- Gap #1 (approved blind spot): `getApprovedNotFiled()` filters `APPROVED_REQS` against `currentColls` by project+tranche_key — implementation needs a second `billing_requests where status==approved` query cross-referenced against collectiblesData
- Gap #2 (sort order): `sortByUrgency()` always applied in `getFiltered()` — implementation replaces current status-priority sort (Pending→Overdue) with URG_ORDER (critical→overdue→near-due→active→clean→done)
- "Show completed" toggle: flat view only (grouped view always shows all projects; individual tranches visible on expand)
- Dept filter + date range kept (not dropped) — date range covers monthly review use case that search can't handle
</decisions_made>

<blockers>
None. All changes committed, clean state.
</blockers>

## Required Reading (in order)
1. `.planning/spikes/027a-collectibles-page-scorecard/README.md` — scorecard design (VALIDATED)
2. `.planning/spikes/027b-collectibles-billing-pipeline/README.md` — unified banner design (VALIDATED)
3. `.planning/spikes/027c-collectibles-table-redesign/README.md` — table redesign spec
4. `.planning/spikes/027c-collectibles-table-redesign/spike.html` — the actual prototype

## Infrastructure State
- Server: `python -m http.server 8000` from project root (may need restart)
- Branch: `v3.3`
- Firebase: production (no staging)
- All spike changes committed

<context>
Full spike series 027a/b/c explored the Finance Collectibles page revamp. All three spikes validated or near-validated. Once 027c is verified, the three designs together define the complete implementation: aggregate scorecards (027a) + unified billing pipeline banner (027b) + redesigned table with urgency sort, search, dept filter, date picker, and group-by toggle (027c). Implementation will touch finance.js primarily — new billing_requests approved query, sortByUrgency(), new filter bar HTML, new table render.
</context>

<next_action>
Start with: Open `http://localhost:8000/.planning/spikes/027c-collectibles-table-redesign/spike.html` and verify the complete design. Then mark all three 027 READMEs VALIDATED and run `/gsd:spike --wrap-up`.
</next_action>
