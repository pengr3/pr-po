---
context: spike
phase: 003-notification-polish
task: 0
total_tasks: 4
status: in_progress
last_updated: 2026-05-25T10:28:11.952Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified yet — this session was discussion/decompose only._

<current_state>
Mid-spike discussion phase. We analyzed the notification system in full, identified all gaps, proposed a spike table, and applied one clear bug fix. No spike demos have been built yet.

We are at the **align checkpoint** — the spike table was presented to the user, user acknowledged it and asked to capture ideas before pausing. Next session: confirm spike table and start building demos.
</current_state>

<completed_work>

**Bug fix applied (already committed):**
- `app/notifications.js` — `renderDropdownRows()` was injecting its own `<div class="notif-dropdown-header">` and footer into `#notifDropdownRows`, causing a duplicate "Notifications" header visible in the dropdown. Fixed: function now only renders rows, updates `#notifMarkAllBtn` disabled state imperatively, and omits the `visibility:hidden` dead-space button on read rows (DOM-omitted entirely for read rows).
- `index.html` — "View all notifications" footer link was missing `onclick="toggleNotificationsDropdown(event)"`, so the dropdown stayed open after clicking. Fixed.

**Analysis completed:**
- Full read of `app/notifications.js`, `app/views/notifications.js`, `styles/components.css`, `index.html` bell/dropdown markup
- All gaps identified and captured (see below)
</completed_work>

<remaining_work>

**Proposed spike table (awaiting user alignment to start building):**

| # | Spike | Type | Validates | Risk |
|---|-------|------|---------------------------------|------|
| 003a | notif-icon-set-svg | comparison | Given notification type badges, when SVG icons replace text-char/emoji mix, then type recognition is faster and rows feel unified | Medium |
| 003b | notif-icon-set-symbol | comparison | Given notification type badges, when a single consistent Unicode symbol style replaces the mix, then type recognition is faster | Medium |
| 004a | dropdown-slide-animation | comparison | Given closed dropdown, when bell clicked, then slide-down+fade feels natural | Low |
| 004b | dropdown-scale-animation | comparison | Given closed dropdown, when bell clicked, then scale+fade feels natural | Low |
| 005 | unread-row-indicator | standard | Given unread row, when left-border accent + subtle tint replaces full blue block bg, then read/unread distinction is clear but lighter | Low |

**Not yet spiked — straight implementation (no spike needed):**
- History page: add "mark all as read" button
- History page: add `onSnapshot` real-time listener
- Deduplicate `TYPE_META` (defined identically in `notifications.js:70` and `views/notifications.js:43`)
- Relative timestamps freeze at render time — auto-update ticker needed

**Bell animation spike (lower priority — user focused on look first):**
- Bell ring/pulse on new notification arrival
</remaining_work>

<decisions_made>

- Double header was a code bug (not a design question) — fixed immediately without spiking
- User priority order: **look first** (icons, unread state, dead-space button), then feel (animations), then function (history page, dedup)
- Mark-read button on read rows: **DOM-omitted entirely** (not `visibility:hidden`) — decided during fix
</decisions_made>

<blockers>
- None. Session paused by user choice, not a blocker.
</blockers>

## Required Reading (in order)
1. `app/notifications.js` lines 70–89 — TYPE_META icon/color map (current state after fix)
2. `app/views/notifications.js` lines 43–60 — duplicate TYPE_META (identical, needs dedup)
3. `styles/components.css` lines 1793–2042 — all notification CSS
4. `.planning/spikes/MANIFEST.md` — existing spike inventory (spikes 001–002 are update-banner, unrelated)
5. `.planning/spikes/CONVENTIONS.md` — vanilla JS, python http.server, inline CSS, project colors

## Infrastructure State
- Dev server: not running (start with `python -m http.server 8000` from project root)
- Firebase: production (no staging — all writes go live)
- Branch: `v3.3`
- Bug fix committed to `v3.3`, pushed to origin (per user's pause instruction)

<context>
Session was discussion + decompose phase of /gsd-spike for notification polish. User wants to polish the look of the notification system: the type badge icons are inconsistent (text chars + emoji mix), the unread state uses a heavy full-blue-block background, and the dropdown had a double header (now fixed). User explicitly said "look first and foremost" — prioritize visual spikes before animation/function spikes.

The spike session should resume at the **align checkpoint**: confirm the spike table, then build spike demos starting with icon set (003a/b comparison) since that has the highest impact on the "feels cheap" problem.
</context>

<next_action>
Resume with: `/gsd:spike` → confirm spike table → build 003a (SVG icons demo) and 003b (symbol icons demo) back-to-back as comparison spikes. Serve at `http://localhost:8000/.planning/spikes/003-notification-polish/spike.html`.
</next_action>
