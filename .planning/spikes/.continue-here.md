---
context: spike
series: iterations (015a/b/c → 016 → 017 → 018)
current_spike: 015 (a/b/c) — awaiting user winner checkpoint
task: 5
total_tasks: 11
status: in_progress
last_updated: 2026-06-02T00:00:00Z
branch: v3.3
---

<current_state>
We're in the middle of a `/gsd-spike` exploration of "extend baselining to save iterations
/ save-states of the project plan." Six spikes scoped, MANIFEST updated, and the three
restore-semantics variants (015a/b/c) are built and committed (29de95b).

The session paused at the **015 winner checkpoint** — three browser URLs were shared with
the user but no verdict yet. The next move is to capture their pick (or hybrid choice),
write the Results section into each 015 README, and proceed to Spike 016.
</current_state>

## Required Reading (in order)

1. `.planning/spikes/MANIFEST.md` — current Idea section "iterations (015–018)" + the six pending rows for this series at the bottom of the Spikes table
2. `.planning/spikes/CONVENTIONS.md` — vanilla JS, single-file HTML, project color palette, served from `python -m http.server 8000`
3. `.planning/spikes/014-baseline/README.md` — the conceptual neighbor; understand baselines = immutable comparison anchors, distinct from iterations = save-states
4. `.planning/spikes/015a-restore-destructive-replace/README.md` — scary modal variant
5. `.planning/spikes/015b-restore-readonly-preview/README.md` — preview-only banner variant
6. `.planning/spikes/015c-restore-auto-snapshot/README.md` — Undo-link safety net variant

## Infrastructure State

- **Dev server:** `python -m http.server 8000` is running and CONFIRMED LIVE as of 2026-06-02 (all three `015{a,b,c}/spike.html` URLs return 200). If it has since died, restart with `python -m http.server 8000` from repo root. All three spike URLs: `http://localhost:8000/.planning/spikes/015a-restore-destructive-replace/spike.html` (+ `015b-restore-readonly-preview` + `015c-restore-auto-snapshot`).
- **Branch:** `v3.3` (Phase 86.12 closed earlier this session; quick task `20260601-bnm` baseline-naming polish also shipped).
- **HANDOFF.json:** `.planning/HANDOFF.json` — machine-readable companion to this file.

<completed_work>
- Task 1: Added 6 spike rows to MANIFEST.md + new "iterations" Idea series header — done (29de95b)
- Task 2: Built spike 015a (destructive replace + scary red confirm modal) — done (29de95b)
- Task 3: Built spike 015b (read-only amber preview banner, edit attempts blocked + logged) — done (29de95b)
- Task 4: Built spike 015c (auto-snapshot before restore + Undo link in toast, 5s window, auto-snapshot removed on undo) — done (29de95b)
- Task 5: Presented 015 winner checkpoint to user — in progress (URLs shared; verdict pending)
</completed_work>

<remaining_work>
- Task 5: Capture user's verdict for 015 a vs b vs c (or hybrid like "015b for casual viewing + 015c for actual swaps"). Update each 015 README's Results section with the verdict, evidence, and any surprises observed.
- Task 6: Build Spike 016 — snapshot scope. Demo dates-only vs dates+deps vs full task doc. Show what's lost on restore for each scope.
- Task 7: Build Spike 017 — history UX. Three variants: A (modal with rows), B (right rail timeline), C (toolbar dropdown like baseline).
- Task 8: Build Spike 018 — diff view. Side-by-side adds/changes/deletes between current and a chosen iteration, without loading.
- Task 9: Update `.planning/spikes/CONVENTIONS.md` with patterns that emerged across this series.
- Task 10: Final consolidated `/gsd-spike` report + Next Up routing (likely `/gsd-spike --wrap-up` or `/gsd-plan-phase` for the real iterations phase).
</remaining_work>

<decisions_made>
- Iterations are conceptually distinct from baselines. Baselines are immutable comparison anchors (Phase 86.12). Iterations are save-states the user can rewind to.
- Linear history assumed for MVP — no branching / parallel iteration lines. Branching is explicitly out of scope for this series.
- All six spikes will be built in risk order (user's pick at the alignment checkpoint). 015 first because it has the highest kill risk.
- Spike 015c includes Undo in the toast (5s window) AND auto-removes the auto-snapshot on undo — direct response to the "auto cards stack up" clutter concern.
- All three 015 spikes use the same mocked task data + same 3 mocked iterations so the user can compare interactions directly side-by-side.
</decisions_made>

<blockers>
- **User verdict on 015** (human_action) — not really a blocker, just the natural checkpoint pause. Workaround: ask the question more directly on resume.
</blockers>

## Critical Anti-Patterns (do NOT repeat)

_None discovered through failure in this session — every step landed cleanly. Carrying forward the standing project anti-patterns from `MEMORY.md` (no unauthorized changes, root-cause-first, etc.)._

<context>
The user has spent the prior session shipping Phase 86.12 (baselining) and a polish quick
task (20260601-bnm baseline-naming). They had the idea mid-UAT: "we can also extend this
to save state? like we can save different iterations of the project plan." That's a
genuinely larger product question — restore semantics, snapshot scope, history UX, diff
— so we routed it to `/gsd-spike` for experiential exploration rather than racing into
implementation.

The framing that mattered: spelling out that iterations ≠ baselines. If we'd treated this
as "make baselines also restorable" we'd have polluted the baseline mechanic. Calling out
the distinction up front kept the spike scope clean.

For 015 the bias is toward 015c (safety net) being the right answer — clutter is a real
concern but Undo + auto-remove on undo addresses it. But the user might rationally prefer
015b (preview-only) if their use case is "I want to LOOK at the saved plan, not REPLACE
my live one." That distinction is the real prize from this checkpoint.
</context>

## Pre-Execution Critique Required

_Not applicable. Each 015 spike is a self-contained interactive demo; nothing here needs
a design-vs-execution split._

<next_action>
On resume:
1. Read this file + HANDOFF.json.
2. Ask: "What's the 015 verdict? Pick one of (a) destructive + confirm, (b) preview-only,
   (c) auto-snapshot + undo, or describe a hybrid."
3. Once captured, edit each `015{a,b,c}/README.md` Results section with the verdict text
   + 1-2 sentences of observed feel.
4. Commit those edits: `docs(spike-015): record verdict — <winner>`.
5. Proceed to Spike 016 (snapshot scope). The 015 winner shapes 016 scope:
   - 015a or 015c winner → 016 explores "what's preserved for a true restore"
   - 015b winner (preview-only) → 016 narrows to "minimum data to render saved tasks visually"
</next_action>
