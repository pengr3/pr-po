---
context: debug
phase: 86.7-debug
status: paused
last_updated: 2026-05-08T16:57:52Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Both bugs required 4+ fix attempts — prior analyses were wrong about root causes. Read the actual root causes below before assuming any prior fix was correct.

## Current State

Two Gantt bugs in `app/views/project-plan.js` were investigated and fixed (commit `31b5f73`). **Browser UAT is still pending** — neither bug has been verified fixed in-browser yet.

## Root Causes Found (commit 31b5f73)

**Bug 1 — Phantom line on parent bar row:**
- CSS `height: 4px` on SVG `<rect>` does NOT reliably override the SVG `height` presentation attribute across browsers
- The bar stayed 24px tall with dark `#475569` fill, appearing as a "dark horizontal line"
- Fix: `initGanttDragLink()` now sets `height` and `y` SVG attributes directly in JS on every `renderGantt()` call
- CSS `height`/`transform` removed from `.parent-summary-bar .bar` (fill kept)

**Bug 2 — Duplicate tasks on fast typing:**
- The blur capture handler was calling `handleNewRowCommit(input)` for the `__new__` row on ANY blur
- When onSnapshot re-renders restored partial text to the new `__new__` input and focus shifted, blur committed it unexpectedly
- Fix: `__new__` row blur now just returns — commits ONLY via Enter key (`handleNewRowKeydown`)

## Remaining Work

1. **Browser UAT Bug 1**: Open project plan with parent task (e.g. "Demo"), verify parent bar renders as thin 4px charcoal strip — no dark line
2. **Browser UAT Bug 2**: Type single-character task names rapidly with Enter — verify no duplicate tasks appear
3. If either still fails: run `/gsd-debug` with fresh symptom description

## Key Files

- `app/views/project-plan.js` — main fix location
  - `initGanttDragLink()` (~line 1728): SVG attribute loop + mouseenter guard
  - `bindGridEvents()` (~line 480): blur handler `__new__` branch (now just `return`)
- `styles/views.css` (~line 2739): `.parent-summary-bar .bar` — `height`/`transform` removed

## Commits This Session

- `31b5f73` — JS SVG height override + blur-only-on-Enter fix (current)
- `063a195` — Frappe v1.2.2 guard + stale focus timer
- `4c3ecc5` — isConnected guard + class propagation
- `fb916f1` — bindGridEvents order fix (infinite loop)

<next_action>
Browser UAT: `python -m http.server 8000`, open a project plan with a parent task, test both bugs. If passing, the phase is complete. If failing, run `/gsd-debug` with exact observed behavior.
</next_action>
