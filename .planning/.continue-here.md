---
context: quick-task
phase: quick-260704-luf-lifecycle-gate-file-upload
task: complete-pending-uat
total_tasks: 4
status: code_complete_pending_deploy_and_uat
last_updated: 2026-07-04T00:00:00Z
---

# Resume Handoff — quick 260704-luf: Lifecycle Gate File Upload

> **The paused v4.1 "in-app generated documents" milestone was superseded.** The user
> revised scope to "just add a file upload option on the mentioned gates, and that's
> it." That is now built as quick task **260704-luf** on branch `v4.1`. Code is
> complete and committed; **not deployed, not browser-tested.**

## What was done (committed on `v4.1`)

| Commit | Scope |
|---|---|
| `83e39e78` | Re-wire Firebase Storage: `firebase.js`, new `storage.rules`, `firebase.json` block, `firestore.rules` field-mask |
| `36c97bff` | `project-detail.js` — Link/Upload tabs + real `lcAttachFile` upload |
| `7fd4edb9` | `service-detail.js` — same, for services (parity) |

Full detail: `.planning/quick/260704-luf-lifecycle-gate-file-upload/260704-luf-{PLAN,SUMMARY}.md`.

Behaviour: each lifecycle attach zone (Inspection / NTP / Completion / COC, on both
projects and services) now offers **🔗 Link** (existing) **and 📄 Upload** (new). Upload
goes to Firebase Storage (`<collection>/<docId>/<docKey>.<ext>`, 10 MB cap, ext
allowlist), stores the download URL in `<docKey>_url`, and the attached name is a
clickable link. Gate-pass logic is unchanged (still keys off `<docKey>_url`).

## ⚠️ PUNCH LIST — required before this works in the app

1. ✅ **firestore:rules deployed** to dev + prod (2026-07-04, compiled OK).
2. ✅ **Billing fixed** (user reactivated both accounts) — earlier both were
   `open:false` delinquent, which masked as console "unknown error" + a bare 403.
3. ✅ **Default Storage buckets created** — `clmc-procurement-dev.firebasestorage.app`
   and `clmc-procurement.firebasestorage.app` (match app config exactly).
4. ✅ **storage.rules deployed** to dev + prod (compiled OK). Dev object write+delete
   smoke test passed (200/204) — billing healthy, bucket writable.
5. ⏳ **Browser UAT — the only thing left** (run `python -m http.server 8000`, localhost → dev project):
   - Upload a PDF on a project's Inspection gate → name appears, click opens it,
     gate advances. Replace with another file. Remove it.
   - Repeat on a **service** (service-detail parity).
   - Test as an **assigned non-admin** (operations_user/services_user) → exercises the
     projects `hasOnly()` field-mask that now includes `*_storage_path`.
   - Confirm the link option still works.

## Superseded context (v4.1 milestone — do NOT resume the questions)

The earlier `.continue-here.md` (see git history at `6e2966c4`) had 3 open "document
contents" questions + a COC wording approval for an in-app **generation** feature.
That approach is **shelved** in favour of plain file upload. If in-app generation is
ever revived, that handoff commit has the full code map + decisions.

## Infrastructure State
- Branch `v4.1`; 3 new commits (above) on top of `6e2966c4`. Not yet pushed since
  these doc commits — push `v4.1` to origin when resuming.
- No servers/watchers running. Storage NOT enabled on either project yet.
