# Firebase web SDK configuration is intentionally public.
# Firebase web API keys are client-side identifiers, not credentials.
# Security is enforced via Firebase Security Rules + Authentication,
# not by hiding the key. See: https://firebase.google.com/docs/projects/api-keys
paths-ignore:
  - app/firebase.js
